# 2026-03-08

## 全仓项目扫描（Codex）

- 完成对 `Sunset` 仓库的仓库级结构扫描，覆盖根目录文档、`.kiro/specs`、`.kiro/steering`、`Assets`、`Packages`、`ProjectSettings`、`History`、`Docx`、`.claude`、`.codex`、`.openclaw`、`Saves`、`Logs`。
- 确认当前项目是 **Unity 6000.0.62f1 / 2D 农场生活模拟方向**，主运行场景为 `Assets/000_Scenes/Primary.unity`，并存在 `DialogueValidation.unity` 作为对话验证场景。
- 确认当前有效源码主目录为 `Assets/YYY_Scripts/`，运行时代码约 172 个脚本，测试位于 `Assets/YYY_Tests/Editor/`，编辑器工具位于 `Assets/Editor/`。
- 确认项目核心运行系统包括：时间/季节/天气、农田/作物、放置、箱子、背包、世界掉落、树木/矿石、遮挡/光影、对话验证系统。
- 确认持久化链路以 `IPersistentObject + PersistentObjectRegistry + SaveManager + DynamicObjectFactory + PrefabDatabase` 为核心；`PersistentIdAutomator` 会在编辑器保存场景时自动补齐/修复 GUID。
- 确认 `ServiceLocator` 已实现但当前几乎未被业务代码真正采用；主项目仍以单例、`FindFirstObjectByType`、事件订阅和 Inspector 引用为主。
- 确认 `EventBus` 当前真实落地主要集中在 Spring Day1 对话链路（`DialogueManager`、`DialogueUI`、`GameInputManager` 输入锁），并未全面覆盖全项目。
- 确认 `Assets/111_Data` 中物品/工具/种子/箱子数据较完整，但 `Recipes`、`Foods`、`Potions`、`Workstation` 等内容资产仍明显不完整；制作台代码存在，但实际内容资产缺口较大。
- 确认剧情/对话框架代码和验证场景已存在，但仓库中未扫到 `DialogueSequenceSO` 这类真实剧情资产，当前更像“系统骨架 + 验证场景”而非完整内容线。
- 确认 `Assets/Resources/` 为空，因此 `PersistentManagers` 中对 `PrefabDatabase` 的 `Resources.Load` 只是兜底，真实运行更依赖场景 Inspector 已正确挂接数据库资产。
- 确认 `.kiro/specs` 是当前真实工作区主线，`History/2026.03.07-Claude-Cli-历史会话交接/总索引.md` 是交接入口；`Docx` 更像旧规划/归档资料，不应替代活跃工作区。
- 当前近期活跃主线包括：
  - `900_开篇/spring-day1-implementation`：对话系统最小验证闭环，已接入输入锁，仍有锐评/验收上下文。
  - `农田系统`：文档与补丁极多，是当前业务最复杂也最活跃的系统之一。
  - `000_代办清扫/2026.03.06`：正在清理老 TD/代办 wording，强调“改写/降级/合并/补录”而非简单删除。
  - `Steering规则区优化/Codex迁移与规划`：近期明确了 Codex 的轻规则工作流与 memory 收尾纪律。
- 明确现状风险：
  - 命名空间混杂（`FarmGame.*`、`Sunset.*` 与大量全局命名空间并存），说明项目经历过多轮重构但尚未统一。
  - 注释和现实存在历史漂移，例如 `TimeManager` 顶部注释写“1季=7天”，但实际强制使用 `28` 天。
  - 测试覆盖集中在农田/光影/掉落，背包、箱子、放置、存档管理器、剧情内容链仍缺系统性自动化覆盖。
- Git 现场：当前在 `main`，相对 `origin/main` ahead 2；工作区存在未提交的 `.kiro/specs`、`History`、`.codex`、`.openclaw` 等改动/新增，不能把仓库误判为干净基线。

