# Codex规则落地线程记忆补卷 5

## 会话 41 - 2026-03-19（对当前方向与已完成内容做定性重判）
**用户目标**：
> 不是继续追问局部 bug，而是要我正面回答：当前方向到底是什么、已经完成的东西到底是什么、我们真正想解决的问题是什么、现在的核心问题又是什么、接下来到底该怎么走。

**已完成事项**：
1. 基于刚完成的修复后重测与实际截图现象，明确指出：
   - 当前系统已经做成的是 shared root 的安全准入与回正系统
   - 不是持续高吞吐的多线程开发执行系统
2. 将 `遮挡检查` 持槽 11 分钟的现象重新定性为：
   - 当前不是 queue 坏了
   - 而是持槽生命周期设计错了
   - 只读核对、回执、memory、治理写回被一起塞进了 shared root 单槽
3. 给出新的架构分叉判断：
   - 若继续坚持单 shared root / 单执行目录，则只能做到“并行准备 + 串行极短写入”
   - 若目标是多个线程持续推进真实任务，则必须升级到“调度大厅 + 多执行舱”的受控并发模型

**关键决策**：
- 从现在开始，不再把“再多发几轮 queue prompt”当成主线目标。
- 后续更大的问题已经从“安全”转成“吞吐与执行模型”。
- 推荐新立后续阶段，专门承接执行模型重构。

**恢复点 / 下一步**：
- 把这条方向重判同步回 Sunset 治理主线。
- 后续若继续推进，应围绕“shared root 只做调度，长耗时执行移出单槽”设计下一阶段。

## 会话 42 - 2026-03-19（用户指出我对遮挡检查产物有漏报）
**用户目标**：
> 指出我上一轮把 `遮挡检查` 的真实产物讲得过于收缩，要求继续把事实说清。

**已完成事项**：
1. 重新核对提交 `e9f34f16`，确认其中已经包含 `遮挡检查` 本轮修复后重测的 3 份治理层产物：
   - `.../2026.03.19_queue-aware业务准入_02_修复后重测/memory_1.md`
   - `.../2026.03.19_queue-aware业务准入_02_修复后重测/线程回收/遮挡检查.md`
   - `.../23_前序阶段补漏审计与并发交通调度重建/memory_2.md`
2. 确认当时真正留在 working tree 里的只有：
   - `.codex/threads/Sunset/遮挡检查/memory_2.md`
3. 将这次漏报正式定性为：
   - 不是 Git 判断错了
   - 而是我在总结时把“已经进入 main 的 tracked 产物”和“仍滞留工作树的尾项”混写得过于收缩

**关键决策**：
- 必须把 `遮挡检查` 这轮已经实际跑通一次 repaired retest 闭环的事实明确写回治理记忆。
- 这条事实也进一步证明：当前问题已从“能不能安全进入”转向“为什么只读核对都能持槽 11 分钟”。

**恢复点 / 下一步**：
- 把剩余的 `.codex/threads/Sunset/遮挡检查/memory_2.md` 收口到 `main`。
- 然后再基于完整事实回答“我们现在到底在造什么、接下来为什么必须改执行模型”。
