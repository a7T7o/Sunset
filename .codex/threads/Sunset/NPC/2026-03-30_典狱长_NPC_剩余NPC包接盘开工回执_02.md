# 2026-03-30｜NPC｜剩余 NPC package 接盘开工回执｜02

## A1 保底六点卡

1. 当前主线

- 我接受当前身份已经改成 `NPC integrator`。
- 这轮不是继续守旧的“非正式聊天残包 exact 回补”，而是只对剩余 `NPC-dominant package` 做 integrator 认定，并真实跑 `preflight`。

2. 这轮实际做成了什么

- 已先完成执行书要求的第一步：
  - 把 `NPCInformalChatValidationMenu.cs` 和对应 `.meta` 从 `Assets/Editor` 直系根迁入了 `Assets/Editor/NPC/`
- 已按本轮口径完成 integrator 分层：
  - `NPC still-own core`
    - `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
    - `Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs`
    - `Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs`
    - `Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs.meta`
    - `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs`
    - `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs.meta`
    - `Assets/Editor/NPC.meta`
    - `.codex/threads/Sunset/NPC/**`
  - `carried foreign but can ride with this package`
    - `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
    - `Assets/YYY_Scripts/Data/NPCRelationshipStage.cs`
    - `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
- 已真实运行本轮白名单 `preflight`

3. 现在还没做成什么

- `preflight` 没有通过，所以这轮还没有进入 `sync`
- 本轮提交 SHA 还不存在
- 当前 own 路径还不 clean

4. 当前阶段

- 当前阶段是：
  - integrator 分层已完成
  - `Editor/NPC` 迁根已完成
  - 真实 `preflight` 已跑
  - 结果停在新的第一真实 blocker

5. 下一步只做什么

- 按本轮执行书，这里不能扩题。
- 如果后续继续，只能围绕新的第一真实 blocker 做裁定或等对应 owner 处理后重跑同组 `preflight`。

6. 需要用户现在做什么

- 暂不需要。

## A2 用户补充层

### 1. 是否接受当前身份已经改成 NPC integrator

- `是`
- 我这轮已经按 `NPC integrator` 口径分了 `still-own core` 和 `carried foreign but can ride with this package`，没有再把它写回旧的 exact 回补叙事。

### 2. NPCInformalChatValidationMenu.cs 是否已从 Assets/Editor 直系根迁入 Assets/Editor/NPC

- `是`
- 当前真实路径是：
  - `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs`
  - `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs.meta`

### 3. 本轮是否碰了 Assets/Editor/NavigationStaticPointValidationMenu.cs

- `否`
- 它这轮只是 `preflight` 给出的新首 blocker，不是我这轮实际改动的一部分。

### 4. Data 这几份文件这轮按什么口径带上

- `NPCInformalChatExitModel.cs` / `.meta`
  - 按 `NPC still-own core`
- `NPCDialogueContentProfile.cs`
- `NPCRelationshipStage.cs`
- `NPCRoamProfile.cs`
  - 按 `NPC-dominant package carried`
  - 原因是它们历史上被别线碰过，但这轮允许跟着 NPC package 一起归仓，不能装作全都纯 own

### 5. 这轮为什么迁进 Editor/NPC 之后还是没过

- 因为脚本这次把我的 Editor own root 仍然认成了 `Assets/Editor`
- 所以它不再被旧的 `DialogueDebugMenu.cs` 挡住之后，又继续看到了：
  - `Assets/Editor/NavigationStaticPointValidationMenu.cs`
  - `Assets/Editor/NavigationStaticPointValidationMenu.cs.meta`
- 这对文件不在我的 `IncludePaths` 里，于是成了新的第一真实 blocker

### 6. 当前 own 路径是否 clean

- `no`

## 停步自省

- 自评：
  - `8/10`
- 我这轮最核心的判断：
  - 旧的 `DialogueDebugMenu` 阻塞已经解除，但当前 `sunset-git-safe-sync.ps1` 的 own-root 判定依然把 `Assets/Editor/NPC` 归到 `Assets/Editor`，所以新的首 blocker 变成了导航那对 Editor 菜单残留
- 我为什么认为这个判断成立：
  - 这是本轮真实 `preflight` 的直接输出，不是推测
- 我这轮最薄弱的点：
  - 执行书禁止我去吞 `Assets/Editor/NavigationStaticPointValidationMenu.cs`，所以这轮只能停在 blocker 报实，不能继续把结果推到 `sync`
- 如果我可能判断错，最可能错在哪：
  - 只可能错在脚本对 `own root` 的聚合方式后续被治理侧重新定义；但在当前真实脚本结果里，首 blocker 就是这对导航菜单残留
- 如果现在继续，我最确信的下一步：
  - 不扩 scope，只围绕这个新 blocker 等待对应处置或新的 integrator 裁定，然后重跑同组 `preflight`

## B 技术审计层

### changed_paths

- `.codex/threads/Sunset/NPC/memory_0.md`
- `.codex/threads/Sunset/NPC/2026-03-30_典狱长_NPC_剩余NPC包接盘开工回执_02.md`
- `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs`
- `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs.meta`
- `Assets/Editor/NPC.meta`

### 本轮真实纳入白名单的 include paths

```powershell
$include = @(
  'Assets/YYY_Scripts/Controller/NPC',
  'Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs',
  'Assets/YYY_Scripts/Data/NPCRelationshipStage.cs',
  'Assets/YYY_Scripts/Data/NPCRoamProfile.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs.meta',
  'Assets/Editor/NPC',
  'Assets/Editor/NPC.meta',
  '.codex/threads/Sunset/NPC'
)
```

### preflight 状态

- 已真实运行 `preflight`：`yes`
- 已真实运行 `sync`：`no`
- 当前 `branch`：`main`
- 当前 `HEAD`：`72e52a51`
- `preflight` 结果：`False`

### 第一真实 blocker

- `type = same-root remaining dirty/untracked`
- `exact path = Assets/Editor/NavigationStaticPointValidationMenu.cs`
- `exact reason = 位于本轮白名单所属 own root Assets/Editor 下，但未纳入 IncludePaths`

### 其它关键核实

- `NPCInformalChatValidationMenu.cs` 是否已迁入 `Assets/Editor/NPC/`：`yes`
- 本轮是否碰了 `Assets/Editor/NavigationStaticPointValidationMenu.cs`：`no`
- 当前 own 路径是否 clean：`no`
