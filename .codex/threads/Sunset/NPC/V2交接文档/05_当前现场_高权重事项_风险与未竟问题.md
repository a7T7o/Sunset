# 当前现场、高权重事项、风险与未竟问题

## 1. 当前 live Git 事实

- 工作目录：`D:\Unity\Unity_learning\Sunset`
- 当前分支：`main`
- 当前 HEAD：`ee3187573b62891a5b0a8d974f43c192c4125a34`
- `shared-root-branch-occupancy.md` 当前仍写：
  - `owner_mode = neutral-main-ready`
  - `current_branch = main`
  - `is_neutral = true`
- 但它的 `last_verified_head = 98cbb88b`，已经落后于当前 live HEAD，因此只能当“目录口径提示”，不能单独当场景开工许可。

## 2. 当前最高权重事项

当前最高权重事项不是继续开发新功能，而是：

- 等待并确认 `Primary.unity` 的安全写窗口是否真正成立；
- 一旦成立，直接推进 NPC 的最小 scene 集成切片。

## 3. 当前最值得继续的工作

只要 scene 写窗口成立，`NPCV2` 最值得继续的工作顺序应该是：

1. `Primary.unity` 的最小 scene audit
2. `001 / 002 / 003` 的 `HomeAnchor` 正式落点
3. scene 级 roam 区域、路线与停留点的最小可用配置
4. 导航线程交付后的 NPC 侧联调与体验验收

## 4. 当前已接受的 checkpoint

下面这些是当前最有价值、且已真实进 `main` 的 NPC checkpoint：

1. `bd5e588d` / `2026.03.25_NPC_02`
   - 玩家失败反馈气泡、水壶运行时状态链、`ToolUseCommitResult`
2. `4c62ef05` / `2026.03.25_NPC_03`
   - `003.prefab` 默认不再自动启动 stress talker
3. `a9f6b74e` / `2026.03.25_NPC_04`
   - 把 `003` 正式 NPC / 测试模式显式开启的语义推进到工具链层
4. `55e2bccd` / `2026.03.25_NPC_05`
   - 补交测试文件 `.meta`，清掉旧起稿残留
5. `359331b9` / `2026.03.25_NPC_06`
   - 将“已推进到当前安全边界”的结论写入 NPC memory
6. `16ba2293` / `2026.03.26_NPC_01`
   - 将“当前仍不能直接进入 `Primary.unity`”的结论写入 NPC memory

## 5. 当前未接受、未解决或未验证的部分

### 当前未完成

1. `Primary.unity` 中的 `HomeAnchor` 正式落点
2. `001 / 002 / 003` 的 scene 级活动区域、路线和相遇节奏
3. 导航交付后的 NPC 联调整体验收
4. `NPCToolchainRegularizationTests` 的 Unity Test Runner 实跑

### 当前未验证到“可过线”

1. 导航线程尚未交出“玩家不能推着 NPC 走”的真实业务场景闭环证据
2. NPC scene 级摆位还没进入 `Primary.unity`

## 6. 当前最关键的 blocker

当前单一第一 blocker 仍然是：

- `Assets/000_Scenes/Primary.unity` 在 Git working tree 中仍是 dirty，且没有明确独占写归属。

这轮 live 复核的直接证据是：

- `git status --short -- 'Assets/000_Scenes/Primary.unity'` 返回 `M`
- `git diff --stat -- 'Assets/000_Scenes/Primary.unity'` 返回：
  - `76 insertions`
  - `4 deletions`
- 当前没有现成的 active lock 证据可直接证明这份 dirty 归属于 NPC 线

## 7. 当前 live 风险

1. `Primary.unity` 处于热区 B / C，且磁盘态不是 neutral
2. shared root 当前是高活跃 mixed dirty 现场，不只 NPC 一条线在动
3. 当前 NPC 邻近路径里，`Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs` 也仍是 dirty，但本轮没有把它认领为 NPC 自己可安全续写的现场
4. 共享表现层字体 / UI 热区仍可能放大串扰风险

## 8. 当前哪些文件 / 目录 / 文档最关键

### 文档

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC-导航接入契约与联调验收规范.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC系统实施主表.md`

### 代码 / 资源

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCBubbleStressTalker.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCPrefabGeneratorTool.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCSceneIntegrationTool.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCAutoRoamControllerEditor.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\001.prefab`
6. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\002.prefab`
7. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\003.prefab`
8. `D:\Unity\Unity_learning\Sunset\Assets\111_Data\NPC\`

## 9. 如果今天立刻交给新线程，它应该先做什么

### 先做

1. 重做只读 preflight，确认 `Primary.unity` 的 dirty / lock / owner 是否已变化
2. 若仍未变化，继续只读，不要硬进 scene
3. 若窗口已成立，先做最小 scene audit，再只落 `HomeAnchor`

### 后做

1. 再补活动范围和路线
2. 再接导航联调
3. 最后才考虑继续扩 `2.0.0` 的双气泡、关系成长、受击兼容实现

### 绝对别先做

1. 别先回去继续微调气泡
2. 别先改导航核心
3. 别先抢写 `NPCAutoRoamController.cs`
4. 别先把 `Primary.unity` 的 mixed dirty 当成无主空地

## 10. 当前一句话结论

NPC 当前不是“无事可做”，而是“最重要的下一刀已经非常清楚，但 scene 热区窗口尚未成立”；因此这轮进入代际交接是合理且安全的。

