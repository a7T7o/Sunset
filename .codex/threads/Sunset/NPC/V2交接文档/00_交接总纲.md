# NPC 线程 V1 重型交接总纲

- 当前线程名：`NPC`
- 未来继任线程名：`NPCV2`
- 当前 live 工作目录：`D:\Unity\Unity_learning\Sunset`
- 当前 live 分支：`main`
- 当前 live HEAD：`ee3187573b62891a5b0a8d974f43c192c4125a34`

## 一句话身份定义

`NPC` 线程当前真正的身份，不再是“继续微调气泡的小修线程”，而是“负责把 NPC 从 phase2 功能恢复，推进到 2.0 角色化与 scene 集成准备态，并在 scene 热区可安全进入前守住边界与交接质量的主线线程”。

## 当前最高权重职责

当前最高权重职责只有一个：

- 把 NPC 现有成果、边界、阻塞和下一步准确交给 `NPCV2`，让它在未来拿到安全 scene 写窗口时，可以直接进入 `Primary.unity` 的最小集成切片，而不是再重跑一轮历史辨析。

## 当前最重要的 5 件事

1. 认清当前真正 blocker 不是“Unity 不稳”，而是 `Primary.unity` 在 Git 磁盘态里仍是 dirty，且没有独占写归属。
2. 保持 `NPC` 与导航主战场的边界，不去抢 `NavGrid2D`、`PlayerAutoNavigator`、交通裁决和局部避障核心。
3. 把 `001 / 002 / 003` 的角色化 profile、`003` 测试模式正规化、生成器 / scene integration tool 的现状交清楚。
4. 把 2.0.0 设计入口和唯一导航契约交清楚，避免下一代线程继续制造平行文档。
5. 把 `NPCV2` 的第一刀固定为“scene audit + HomeAnchor 最小落点”，而不是重新回到气泡微调或职责混乱。

## 必读顺序

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\05_当前现场_高权重事项_风险与未竟问题.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\01_线程身份与职责.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\02_主线与支线迁移编年.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
6. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC-导航接入契约与联调验收规范.md`
7. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC系统实施主表.md`

## 这条线程最容易被误解成什么，但它其实不是什么

最容易被误解成：

- “已经把 NPC 都做完了，只差一点点 scene 手工摆位。”

它其实不是：

- 不是已完成线程；
- 不是导航核心线程；
- 也不是还在继续做 UI 微调的线程。

它现在真正处于：

- 结构与产品化入口已经铺好；
- 真实 scene 集成还没开刀；
- 但已经稳定到可以安全交给 `NPCV2` 接班。

