# 2026-03-28 UI线程 Phase 1 第一刀放行任务书

你现在可以开始，但只允许开始 **Phase 1 第一刀**。

这不是放你进入完整实现，也不是允许你把 Phase 2/3 顺手一起做掉。

你这轮唯一主刀只有一个：

**把 `spring-day1` 的 runtime UI 创建链，从“代码现场重新长壳”改成“prefab-first，把用户手调 prefab 真正接回 runtime 作为视觉真源”。**

---

## 一、当前已接受的基线

你现在必须建立在下面这份已过审的总方案上继续，不要自己重新换题：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\SpringUI-Day1基线复刻与长期技术路线总方案.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`

这两份文件已经站稳了几个前提：

1. 当前问题本体不是“抽象不够高级”，而是 **用户手调 prefab 没重新接回 runtime**
2. 正确路线不是三选一，而是 **视觉 prefab / 行为代码 / 差异数据**
3. 正确顺序必须是 **先抄 -> 再稳 -> 再抽**
4. 当前明确不能走：
   - 继续纯 `BuildUi()`
   - 把 prefab 继续当参考图
   - 现在直接跳 binder / provider / 模板化
   - 拿当前错版 runtime 去推广到所有工作台

---

## 二、本轮唯一主刀

你这轮只做下面这一刀：

### 让 PromptOverlay 和 WorkbenchOverlay 的 runtime 视觉壳，改为直接吃现有 prefab

也就是：

- `SpringDay1PromptOverlay`
- `SpringDay1WorkbenchCraftingOverlay`

当前还在：

- `EnsureRuntime() -> new GameObject -> AddComponent -> BuildUi()`

你这轮要把它收成：

- runtime 通过现有 prefab 进入场上
- 视觉层级、RectTransform、字号、留白、位置、尺寸不再由 `BuildUi()` 重新发明
- 代码只负责绑定和后续行为入口，不再现场长出另一张视觉壳

一句话：

**这轮只解决“runtime 先长对脸”，不解决“这张脸在复杂状态下怎么更聪明”。**

---

## 三、允许的作用域

这轮允许你动的范围，默认只限于：

- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1PromptOverlay.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1WorkbenchCraftingOverlay.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\Interaction\CraftingStationInteractable.cs`

如果你确实需要增加一个很小的辅助绑定脚本，也只能放在：

- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\`

并且你要在回执里解释：

- 为什么现有脚本内收不住
- 这个新脚本只服务 Phase 1 第一刀，不是偷渡通用层

### 这轮默认不要动

- `Primary.unity`
- 任何场景对象
- Day1 之外的 UI 系统
- SpringUI 通用模板层
- 与 Day1 无关的 shared root

### prefab 资产的边界

你这轮的目标是 **使用现有手调 prefab 作为真源**，不是重新美术精修 prefab。

所以：

- 默认 **不要改 prefab 的视觉参数**
- 只有在“为了把 prefab 正确接回 runtime，必须补序列化引用 / 绑定槽 / 稳定节点命名”时，才允许做最小必要编辑
- 如果你动了 prefab，必须在回执里说清：
  - 改了什么
  - 为什么这不是重新设计视觉
  - 为什么这是 Phase 1 必要动作

---

## 四、本轮明确禁止的漂移

这轮不准顺手做下面这些事：

### 1. 不准提前进入 Phase 2

所以这轮不要顺手做：

- 自适应布局修复
- 日历撕页
- ScrollRect / Viewport / Mask 行为链修复
- 制作按钮 / 进度条状态机
- 离台小进度
- 固定锚定不漂

这些全都属于下一阶段。

### 2. 不准开始抽 SpringUI 通用层

所以这轮不要开始：

- binder 通用层
- provider 抽象
- 模板类拆分
- 多工作台统一架构

这轮只许做 **Day1 的 prefab-first 接回 runtime**。

### 3. 不准继续在 `BuildUi()` 里写新的视觉默认值

如果你一边说“现在吃 prefab”，一边又继续在 C# 里新增：

- 位置
- 尺寸
- 字号
- 留白
- 视觉层级默认值

那就是继续制造双源。

### 4. 不准把“能显示出来”冒充成“Phase 1 已完成”

Phase 1 第一刀的完成标准不是“还能跑”，而是：

- runtime 视觉壳已切回 prefab 真源
- 代码不再现场重造另一张脸

---

## 五、本轮完成定义

这轮做完后，你的结果必须能同时满足下面 4 条：

### 1. runtime 创建链已经不再绕开 prefab

也就是：

- `PromptOverlay` 不是再 `new GameObject + BuildUi()` 造壳
- `WorkbenchOverlay` 也不是再 `new GameObject + BuildUi()` 造壳

### 2. prefab 已成为 runtime 视觉真源

也就是：

- 运行时视觉壳来自现有手调 prefab
- 用户后续继续调 prefab 时，不会天然与 runtime 脱钩

### 3. 代码职责已收窄到“绑定与行为入口”

也就是：

- 视觉壳不再由代码定义
- 代码只负责找到节点、建立引用、保留后续行为入口

### 4. 你能清楚报出“这一刀完成了什么，明确还没做什么”

至少要明确写出：

- 这轮完成的是 Phase 1 第一刀
- 这轮还没做 Phase 2 的体验增强
- 这轮还没进入 Phase 3 的模板化

---

## 六、建议实现方向

你不必照抄实现细节，但方向必须对：

### PromptOverlay

重点看：

- `EnsureRuntime()`
- 当前 `BuildUi()` 如何生成视觉壳

你要做的是：

- 让 runtime 改为从 `SpringDay1PromptOverlay.prefab` 进入
- 把当前脚本收成“绑定 prefab 内节点 + 继续处理 runtime 行为”
- 避免再通过 `BuildUi()` 重建卡片外壳

### WorkbenchOverlay

重点看：

- `EnsureRuntime()`
- 当前 `BuildUi()` 如何生成 PanelRoot、左右列、底部条

你要做的是：

- 让 runtime 改为从 `SpringDay1WorkbenchCraftingOverlay.prefab` 进入
- 脚本改成绑定 prefab 内既有节点和必要槽位
- 当前运行时新增结构里，凡属视觉壳的部分，优先回收到 prefab 权威源

### 交互入口

重点看：

- `CraftingStationInteractable.cs`

你只在必要时动它，目的只能是：

- 让它正确拿到新的 prefab-first runtime overlay

不要顺手把别的业务逻辑一起改了。

---

## 七、你本轮回执必须回答的 6 件事

做完后，你的回执必须明确回答：

1. `PromptOverlay` 的 runtime 创建链现在如何进入 prefab
2. `WorkbenchOverlay` 的 runtime 创建链现在如何进入 prefab
3. 这轮哪些视觉壳不再由 `BuildUi()` 重新生成
4. 如果你改了 prefab，改的是绑定还是视觉，为什么
5. 这轮还明确没做哪些 Phase 2 / Phase 3 内容
6. 现在为什么可以说“prefab 已重新成为 runtime 的视觉真源”

不要给我一份泛泛的“我做了不少重构”。

---

## 八、工作区与记忆要求

你的实现与补记仍然只落在：

- 工作区：`D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI`

这轮做完后，至少更新：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`

补记时写清：

- 这轮唯一主刀是什么
- 改了哪些文件
- 哪些属于 Phase 1 第一刀
- 哪些明确还没做
- 下一步如果继续，应该进入哪一条 Phase 2 项

---

## 九、一句话执行口径

你现在不是去“开始做 SpringUI 全系统”，

你现在只做这一刀：

**把 Day1 那两个用户手调 prefab 真正接回 runtime，让 runtime 先长成对的脸。**
