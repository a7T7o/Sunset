# 2026-03-29｜场景搭建（外包）｜全局警匪定责清扫第三轮｜整线程目录 docs-tail 归仓

## 当前已接受的基线

- 第二轮已经真实证明：只带 `memory_0.md` 跑 `preflight` 不够
- 当前 blocker 不是 UI 实现，不是 `UI系统` 结构迁移，也不是 `spring-day1` 实现面
- 你这条线当前只保留 `docs / governance / 审核分发` 边界

## 本轮唯一主刀

不再只带单个 `memory_0.md`。

这轮只做一件事：

**把 `.codex/threads/Sunset/场景搭建（外包）` 整个线程目录，当成唯一 docs-tail 包，真实跑一次 `preflight -> sync`。**

## 允许 scope

只允许纳入：

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\**`

如果你需要补本轮回执，也只允许落在同一线程目录下。

## 明确禁止

- 不准恢复 UI 实现 owner
- 不准改 `UI系统` 工作区
- 不准改 `spring-day1` / `UI-V1` 任何实现文件
- 不准继续发新 UI 任务书
- 不准把 `.kiro/specs/UI系统/**`、`Assets/**` 任何文件带进白名单

## 完成定义

你这轮只允许交两种结果之一：

### A｜整线程目录 docs-tail 已真实归仓

必须写清：

1. 本轮白名单 / scope 口径
2. 是否已真实运行 `preflight`
3. 是否已真实运行 `sync`
4. 提交 SHA
5. 当前 own 路径是否 clean

### B｜第一真实 blocker 已钉死

必须写清：

1. 本轮白名单 / scope 口径
2. 是否已真实运行 `preflight`
3. 是否已真实运行 `sync`
4. 第一真实 blocker 的 exact path / reason
5. 当前 own 路径是否 clean

## 回写位置

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_全局警匪定责清扫第三轮回执_01.md`

## 聊天最小回复格式

只回 4 行：

1. 已回写文件路径
2. 是否完成本轮要求：`A / B`
3. 当前 own 路径是否 clean：`yes / no`
4. 一句话摘要
