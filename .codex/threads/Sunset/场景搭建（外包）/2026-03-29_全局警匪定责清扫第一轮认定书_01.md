# 2026-03-29_全局警匪定责清扫第一轮认定书_01

这轮不是让你继续发 UI 任务书，也不是继续做审美裁定。

这轮只做一件事：把你这条 `场景搭建（外包）` 线程当前在 UI 线里到底负责什么、已经不负责什么，重新认死。

## 我对你当前身份的初步认定

你更像：

- UI 父线程
- 审核与任务书分发 owner
- UI 工作区迁移与阶段叙事 owner

你不像：

- 当前 `Prompt / Workbench` runtime 代码 owner
- `SpringDay1UiPrefabRegistry` 的实现 owner
- `NpcWorldHintBubble.cs` 的实现 owner

## 你必须先回读这些材料

至少完整回读：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_进度快照.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
- 你自己发出的 `2026-03-28` 到 `2026-03-29` UI 任务书

同时必须复核这些结构性路径：

- `.kiro/specs/UI系统/0.0.1 SpringUI/`
- `.kiro/specs/UI系统/Before03.28/`
- `.kiro/specs/UI系统/old/`
- 被删除的旧 `UI系统/*` 子目录

## 你这轮必须回答的核心问题

### 1. UI 工作区迁移是不是你主导的

你必须把这件事说死：

- 旧目录大批删除
- 新 `0.0.1 SpringUI` 建立
- 新 `Before03.28` 建立

这到底是不是你这条线主动做的迁移组织动作。

如果是，你要说明为什么这样迁；如果不是，你要说清是谁主导、你只是如何引用。

### 2. 你当前真正 own 的 repo 内容到底是什么

不要泛泛说“我是父线程”。

你要明确列出：

- 任务书
- 审核报告
- UI 工作区阶段叙事
- `UI系统/memory.md`
- 你认为属于自己 own 的 docs-only 路径

以及：

- 你明确不 own 的 runtime / prefab / script 路径

### 3. 你有没有把“父线程裁定”说成“自己也在实现”

这条你要自己查。

尤其要检查：

- 你是否把 UI 线程 accepted 图的推进说成了自己这一条线的完成面
- 你是否默认把 `Primary.unity` 或 `SpringDay1UiPrefabRegistry` 当成了自己也可认领的对象

### 4. 你当前和 `spring-day1` 的边界到底是什么

你必须明确给出一句很硬的话：

- 你 own 什么
- `spring-day1` own 什么
- 第二轮如果要正式分清 UI 线责任，哪部分由你保留，哪部分应全部落到 `spring-day1`

### 5. 哪些旧放行 / accepted 叙事现在必须撤回或改口

如果用户后来已经驳回图或要求返工，你必须自己检查：

- 哪些“已可终验”
- 哪些“停给用户裁定”
- 哪些“accepted 证据成立”

现在还成立，哪些已经必须改口。

## 本轮禁止事项

- 不准再发新实现任务书
- 不准顺手改 UI 工作区结构
- 不准代 `spring-day1` 认代码 owner
- 不准把自己写成“父线程 + 实施线程双 owner”

## 固定回写路径

把完整回执写到：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_全局警匪定责清扫第一轮回执_01.md`

## 回执格式

按这个顺序回：

- `A1 保底六点卡`
- `A2 用户补充层`
- `停步自省`
- `B 技术审计层`

并且必须显式回答：

- 当前 own 路径是否 clean
- UI 工作区迁移是否由你主导
- 第二轮你建议自己保留的 docs / governance 面
- 第二轮你建议全部下放给 `spring-day1` 的实现面

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成
- 当前 own 路径是否 clean
- 一句话摘要
