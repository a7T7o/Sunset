# 2026-03-28 UI线程 Phase 2 任务列表显隐与正式面纠偏任务书

你上一轮不是“完全没进步”。

但这轮**仍然不能验收通过**。

原因很直接，用户刚刚给了最新 live 验收：

1. **Workbench 左列 RecipeColumn 还是空的**
2. **Prompt / 任务栏目虽然不再完全乱飞，但正式面依然很差，远没有回到用户手调 prefab 的成熟样子**

所以你现在不要扩散，不要继续泛做“Phase 2 全体验增强”。

你这轮只做一刀：

**把 Workbench 左列“明明有数据但完全没显示”的问题彻底修穿，同时把 Prompt 任务栏收回到用户手调 formal-face 的可接受正式面。**

---

## 一、这轮为什么不能过

用户刚给的 3 张图已经足够说明问题：

### 1. Workbench 左列仍然是空白

图里右侧已经明确出现：

- `Axe_0`
- 描述
- 材料
- 数量
- 预计耗时

这说明：

- 当前不是“整个工作台没数据”
- 不是“没选中 recipe”
- 而是**左列 RecipeColumn 的显示链仍然没站住**

也就是说，用户前面反复说的这件事，到现在还没真正解决：

**左边不是没东西，是有东西但你没有正确显示出来。**

### 2. Prompt / 任务栏目仍然很差

用户这次明确说的是：

- “图二图三就是当前的任务栏目，很差劲”

这句话的意思不是“它完全没了”。

而是：

- 它现在虽然比最坏的时候好一点
- 但整体排版、组件观感、任务行样式、留白和正式面，**仍然没有回到用户手调 prefab 的品质**

所以你不能再用“几何值对了”“Rect 实值对了”来顶替最终观感。

---

## 二、这轮唯一主刀

这轮你只收两件事：

### A. Workbench 左列 RecipeColumn 必须真正显示出来

不是日志里有，不是 hierarchy 里有，

而是玩家在屏幕上真的看得到：

- 左列标题
- recipe 行
- 图标
- 名称 / 内容
- 至少出现当前可选配方项

### B. Prompt / 任务栏目必须重新收回 formal-face

不是只要不飘就算过，

而是要让它重新接近用户手调 prefab 的正式样子：

- 标题区
- 副标题 / 引导语
- 任务行
- 小方块 / 勾选区
- 底部说明条
- 整体留白与层级

都必须回到“看起来像正式 UI”，而不是“能显示的一版临时拼装面”。

---

## 三、你这轮先解决什么，不要猜错

### 1. Workbench 左列问题的判断口径

当前最重要的不是再调右侧。

右侧能显示 `Axe_0`，恰恰证明：

- 数据链大概率已经通了
- 当前主要问题在左列渲染 / 绑定 / viewport / content / item 行可见性这条链

你必须重点排：

- `RecipeColumn`
- `Viewport`
- `Content`
- `Mask / ScrollRect`
- recipe 行 prefab / runtime 节点
- 图标 Image 是否被正确绑定 / 激活
- 文本是否被颜色、alpha、layout、遮罩、尺寸压没
- item 行是否被挤到 viewport 外
- item 行是否实际生成在错误父节点

这轮不要再拿“节点存在”当完成标准，

你要收的是：

**左列实际看得见。**

### 2. Prompt 任务栏问题的判断口径

Prompt 这轮不是“继续调几何数值”。

而是：

**把已经接回 runtime 的 formal-face，重新做回用户会接受的正式排版。**

重点不是横向改尺寸，

而是以下这些是否真正像用户手调 prefab：

- 标题条高度、留白、层级
- 任务行和小方块的关系
- 行内文字位置
- 卡片内空白分布
- 底部说明区和主卡片关系
- 整体视觉有没有“临时拼感”

用户已经再次强调过：

- 自适应是“上面文字变多，下面往下推”
- 不是“左右宽度和整体脸又被改一遍”

所以这轮 Prompt 仍然不允许回到横向乱改壳体那条路。

---

## 四、这轮不准做什么

你这轮不要顺手做：

- 模板化
- provider / binder 新抽象
- 新的兼容层扩张
- 再去做别的 Phase 2 项
- 再去动这轮无关的世界提示、离台小进度、别的工作台

也不要把时间花在“解释为什么链路存在”。

现在用户已经给了最直接的 live 结果：

**左列还空，Prompt 还丑。**

你先把这两个最直观失败点收掉。

---

## 五、这轮必须交的硬证据

你这轮回执不能再只给：

- 代码链
- 组件值
- “理论上应该显示”

你必须交：

### 1. GameView 最终证据

直接用现在已经落地的这套工具：

- `Sunset/Story/Debug/Capture Spring UI Evidence`
- 或 `Sunset/Story/Debug/Bootstrap + Capture Spring UI Evidence`

产物位置：

- `D:\Unity\Unity_learning\Sunset\.codex\artifacts\ui-captures\spring-ui\pending`

如果你确认这一轮结果是对的，再提升到：

- `accepted`

### 2. Workbench 左列可见证据

至少要给一张最终图，能让人一眼看出：

- 左列 recipe 不是空白
- 行项真的出现了
- 图标和文本真的可见

### 3. Prompt 正式面证据

至少要给一张最终图，能让人一眼看出：

- Prompt 任务栏已经不是“将就能显示”
- 而是已经重新回到正式样子

### 4. 技术层补充

再补：

- 这轮到底修的是哪条显示链
- 为什么之前右侧有内容、左侧却空
- Prompt 到底是哪几个节点 / 样式 / 行布局把正式面做丢了

---

## 六、这轮完成定义

只有同时满足下面两条，这轮才算过：

### 1. Workbench 左列过线

- 左列 recipe 列表真实可见
- 图标 / 名称 / 行项不是空白
- 不是只靠选中右侧证明“数据存在”

### 2. Prompt 正式面过线

- Prompt 卡片和任务行视觉重新回到可接受正式面
- 不是只剩“几何冻结了”
- 而是用户肉眼一看不会再说“很差劲”

---

## 七、一句话执行口径

你这轮别再讲大而全，

你就把这两个最直观失败点收掉：

**Workbench 左列必须显示出来，Prompt 任务栏必须重新像正式 UI。**
