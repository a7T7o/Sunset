# UI 最终收尾 prompt（只收口，不再开新实现）

你这轮不是继续扩做，也不是再开一刀新体验项。
这轮只做**最终收尾**。

先说我这边的审核结论，避免你再误判范围：

- 我认可你这轮“玩家 / NPC 对话气泡层级分隔、谁说话谁在上、结束不留脏状态”这条**主功能链基本成立**。
- 但这还只是：
  - `结构 / checkpoint`：成立
  - `targeted probe / 局部验证`：基本成立
  - `真实入口体验`：**尚未验证**
- 所以你这轮**不允许**把它包装成“已经彻底过线”。
- 你这轮的唯一任务，不是再改体验，而是把这刀**干净、诚实、可交接地收掉**。

---

## 一、当前已接受的基线

以下内容我这边已经接受为“这刀结构成立”的基线，不要再顺手改：

1. `PlayerNpcChatSessionService` 已经显式区分当前发言方，并让“谁在说话谁在上面 / 谁拿更高排序加权”。
2. 玩家 / NPC 双气泡已经不再只靠很小偏移硬挤，而是有了更明确的横向 + 纵向分隔。
3. 玩家气泡已经从“几乎复制 NPC”收回到玩家侧语义：
   - 保持正式面质量
   - 但位置、尾巴方向、配色不再糊成 NPC 同一张脸
4. 对话结束 / 立即取消 / 跑开中断时，额外位移、排序加权、焦点状态会一起回收。
5. 相关静态测试已经补上，不再只是口头说做了。

这些是**已接受基线**，这轮不要继续在它们上面开新设计。

---

## 二、本轮唯一主刀

本轮唯一主刀只有一个：

**把这条气泡线做一次“最终收尾归仓”**。

具体只收这 4 件事：

1. **把 `thread-state` 报账改真**
   - 你当前 `UI.json` 的 `owned_paths / expected_sync_paths` 和现场不完全一致。
   - 至少要按真实 dirty / 新增文件把它补齐。
   - 我这边已经看到：
     - `Assets/YYY_Tests/Editor/PlayerNpcConversationBubbleLayoutTests.cs`
       是这轮新增测试，但当前不在你 `UI.json` 的 `owned_paths / expected_sync_paths` 里。
   - 你不要只补这一条了事；请按**这轮真实 own 文件集合**一次性报实。

2. **把这轮停在“checkpoint 级收口”**
   - 你可以说：结构成立、静态验证站住。
   - 你不可以说：真实玩家视面已经过线。
   - 因为当前真实 Unity / GameView 终验仍被“用户当前占着项目实例”挡住。

3. **做一次最终 preflight，决定是 `sync` 还是 `blocker`**
   - 如果当前这轮已完成的代码 / 测试 / memory 可以按白名单安全收口，就走：
     - `Ready-To-Sync`
     - 然后真实 `sync`
   - 如果你判断这轮还不能安全 sync，也可以不硬 sync；但必须把 blocker 说死，不能含糊停车。
   - 不允许停在“own 不 clean，但我也不判断能不能 sync”的中间态。

4. **把最终停手状态写清楚**
   - 这轮做完后，要么：
     - `READY + 已 sync`
   - 要么：
     - `PARKED + blocker 已说死`
   - 不要再留“像做完又像没做完”的模糊状态。

---

## 三、本轮允许的 scope

这轮允许你动的只有：

- `UI` 当前这刀已经涉及到的代码 / 测试 / state / memory / sync 报账
- 为了把这轮收口而必须做的最小 state / memory / sync 修正

---

## 四、本轮明确禁止的漂移

这轮禁止你继续做这些：

1. 不准再开新的体验改动
   - 不再继续修 Prompt / Workbench
   - 不再继续修 world hint / E 键 / 任务提示
   - 不再顺手改 NPC 其他气泡链

2. 不准再开新的审美试错
   - 不再继续调颜色、偏移、字号、节奏来“顺手再美化一下”

3. 不准把“真实终验尚未做”偷换成“已经整体完成”

4. 不准只补 memory，不判断 `sync or blocker`

---

## 五、完成定义

你这轮收尾完成，必须同时满足：

1. `UI.json` 已按真实 own / expected sync 路径改真
2. 当前 completion layer 被你明确写成：
   - `结构 / checkpoint`：已成立
   - `targeted probe / 局部验证`：已成立或基本成立
   - `真实入口体验`：尚未验证
3. 你已经做了最终 preflight，并明确落在以下二选一之一：
   - `已 sync`
   - `未 sync，但 blocker 明确`
4. 你已经把 live 状态收成：
   - `READY` 或 `PARKED`
   - 不再停在模糊叙述

---

## 六、外部 blocker 的正确口径

如果这轮最终仍不能补真实 Unity / GameView 终验，正确口径只能是：

- `这刀结构和静态验证已站住，但真实玩家视面仍待用户让出当前 Unity 实例后补终验。`

不要写成：

- `已经彻底完成`
- `只差一点点感觉`
- `基本等于过线`

---

## 七、固定回执格式

这轮回执不要散讲，严格按下面顺序交：

### 用户可读层
1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### 用户补充层
- 我这轮最核心的判断是什么
- 我为什么认为这个判断成立
- 我这轮最薄弱、最可能看错的点是什么
- 我给自己这轮结果的自评

### 技术审计层
至少写清：
- 本轮最终 `thread-state`
- 是否已跑 `Ready-To-Sync / Park-Slice`
- `sync` 是否已完成；如果完成，给出本轮实际收口范围
- 如果没 sync，blocker 是什么
- `changed_paths`
- `UI.json` 这轮补了哪些真实路径
- memory 写回到了哪些文件
- 当前 own 路径是否 clean

---

## 八、这轮最重要的一句话

**你现在不是再做新东西，而是把这刀作为“checkpoint 级完成物”干净收口。**

如果你这轮还继续加实现，那就是漂移。
如果你这轮只补 memory、不判断 `sync or blocker`，那也是没收完。

---

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
