# 2026-03-29_全局警匪定责清扫第五轮回执_01

## A1 保底六点卡

1. 当前主线
- 第五轮只锁 `Service/Navigation + own docs/thread`，目标是把这包对 mixed-root 的硬编译依赖剥掉，然后真实跑 `preflight -> sync`。

2. 这轮实际做成了什么
- 在 `Assets/YYY_Scripts/Service/Navigation/Editor/NavigationLiveValidationMenu.cs` 内，把 `NPCInformalChatValidationMenu.ExclusiveValidationLockKey` 收成本地常量 `Sunset.NpcInformalChatValidation.Active`。
- 在 `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs` 内新增 `PlayerAutoNavigatorDebugCompat / PlayerAutoNavigatorDebugSnapshot`，把对 `PlayerAutoNavigator.Debug*` 的硬编译访问改成 `Service/Navigation` 内部兼容快照。
- 在 `Assets/YYY_Scripts/Service/Navigation/NavigationStaticPointValidationRunner.cs` 内同步改用同一兼容快照，不再直接咬 `PlayerAutoNavigator` mixed debug API。
- 已真实运行第五轮 still-own 白名单 `preflight`，结果通过；随后已真实运行 `sync`，实现包已归仓到 `acfc7f27`。

3. 现在还没做成什么
- 本轮实现面已经归仓，但这份第五轮回执、child memory、thread memory 和审计日志还没跟着一起补收。

4. 当前阶段
- 第五轮已经从“代码闸门 blocker”推进到“实现包归仓完成，文档与审计收口中”。

5. 下一步只做什么
- 只把这份第五轮回执、`导航检查/memory.md`、`导航检查/memory_0.md` 和 `skill-trigger-log.md` 归仓，不再继续改 `Service/Navigation`、不再补新 live。

6. 需要用户现在做什么
- 无。

## A2 用户补充层

- 这轮实际改掉的 mixed 依赖：
  - `NavigationLiveValidationMenu.cs -> NPCInformalChatValidationMenu.ExclusiveValidationLockKey`
  - `NavigationLiveValidationRunner.cs / NavigationStaticPointValidationRunner.cs -> PlayerAutoNavigator.Debug*`
- 现在 `Service/Navigation` 是否还依赖 `PlayerAutoNavigator.cs` mixed diff：
  - 不再有硬编译依赖；缺失成员时会在兼容层里降级到字符串/布尔/数值/`Vector2.zero` fallback。
- 现在 `NavigationLiveValidationMenu.cs` 是否还依赖 `NPCInformalChatValidationMenu.cs`：
  - 不依赖；锁 key 已在本地常量收口。

## 停步自省

- 自评：
  - 这轮是实质推进，关键不是“又解释了一轮 blocker”，而是把 blocker 从 mixed-root 争议真正打穿成了可归仓的代码收口。
- 最薄弱点：
  - `PlayerAutoNavigator` 缺失 mixed debug 成员时，验证 richness 会退化成 fallback 值；它保证编译独立，但不保证旧 rich debug 在 clean HEAD 下一样丰满。
- 最可能看错的地方：
  - 如果外部 `NPC` 校验锁未来改 key，而没有同步到本地常量，这里会再次漂移；当前判断建立在第五轮执行书指定的 key 值之上。
- 为什么下一步只该这样走：
  - `preflight` 和第一次 `sync` 已经过线，继续碰代码只会把已经拆掉的 mixed 依赖重新搅浑；现在唯一值得做的是把第五轮回执和审计尾账跟着补收。

## B 技术审计层

1. 这轮实际纳入白名单准备归仓的文件有哪些
- `Assets/YYY_Scripts/Service/Navigation/Editor/NavigationLiveValidationMenu.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationLocalAvoidanceSolver.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationStaticPointValidationRunner.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationStaticPointValidationRunner.cs.meta`
- `.kiro/specs/屎山修复/导航检查/*` 当前 still-own dirty/untracked
- `.codex/threads/Sunset/导航检查/*` 当前 still-own dirty/untracked

2. 是否已真实运行 `preflight`
- `yes`

3. 是否已真实运行 `sync`
- `yes`

4. 如果已 sync，提交 SHA 是什么
- `acfc7f27`

5. 如果未 sync，第一真实 blocker 是什么
- `none`

6. 当前 own 路径是否 clean
- `no`
- 原因：本回执、child memory、thread memory 尚未随同本轮审计尾账一起补收。
