# 2026-03-29_全局警匪定责清扫第二轮执行书_01

这轮不是继续给动态线发新 prompt，也不是继续补静态 live。

这轮只做一件事：

**把你已经被正式分配到的“静态 validation 工具链 + 父线程审核 docs”清扫认领干净。**

## 已接受基线

你在第一轮回执里已经站住了这条边界：

- 你 own：
  - `Assets/Editor/NavigationStaticPointValidationMenu.cs`
  - `Assets/YYY_Scripts/Service/Navigation/NavigationStaticPointValidationRunner.cs`
  - 你自己的父线程 docs / memory / 验收清单
- 你不再 own：
  - `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs` 的动态 runtime 主线
  - `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 的 scene cleanup

第二轮不再重打这层裁定。

## 本轮唯一主刀

**只清你自己的静态 validation 链和父线程 docs。**

## 允许 scope

你这轮只允许处理：

1. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NavigationStaticPointValidationMenu.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationStaticPointValidationRunner.cs`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\memory_0.md`
5. 你自己这条父线程新增的导航审核 / 验收 / 分发 docs

## 明确禁止的漂移

这轮明确禁止：

1. 不准继续碰 `PlayerAutoNavigator.cs`
2. 不准继续碰 `Primary.unity`
3. 不准继续补静态 live
4. 不准替 `导航检查V2` 继续裁动态 runtime owner
5. 不准把“父线程观察结论”重新包装成“自己也在写实现”

## 完成定义

这轮完成，不是指“导航问题全完了”，而是指下面 4 件事全部成立：

1. 你把自己的 still-own 清扫面固定成上面那组静态 validation + docs
2. 你没有再继续吞 `PlayerAutoNavigator.cs`、`Primary.unity` 或动态 runtime 责任
3. 你能明确回答当前 own 路径是否 clean
4. 如果还不 clean，你能把剩余路径和原因说得精确到文件级

## 如果还不 clean，允许的唯一表述

你可以报：

- 哪些 still-own 文件仍未 clean
- 为什么没 clean
- 这是不是因为你自己没收完，还是因为 foreign / mixed 现场还卡住

但你不准再用“shared root 太脏”四个字整体糊过去。

## 固定回写路径

把完整回执写到：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第二轮回执_01.md`

## 回执格式

按这个顺序回：

- `A1 保底六点卡`
- `A2 用户补充层`
- `停步自省`
- `B 技术审计层`

并且必须显式回答：

1. 这轮你实际认领并清扫了哪些 still-own 文件
2. 这轮你明确退掉了哪些非 own / 动态 owner claim
3. 当前 own 路径是否 clean
4. 如果不 clean，剩余的 exact path 和原因是什么
5. 你和 `导航检查V2` 的边界一句话现在怎么写

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- 一句话摘要
