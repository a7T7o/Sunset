# 2026-03-29_全局警匪定责清扫第三轮_认领归仓与git上传_01

这轮不再继续做“第二轮那种 own 边界说明”。

这轮只做一件事：

**把你已经认下来的 still-own 包，真的按白名单尝试上 git。**

## 已接受基线

你第二轮已经站住：

- still-own = 静态 validation 工具链 + 你自己的线程 / 工作区 docs
- 不再继续吞：
  - `PlayerAutoNavigator.cs`
  - `Assets/222_Prefabs/UI/Spring-day1/Primary.unity`
  - 动态 runtime owner
  - `导航检查V2` 实现 owner

第三轮不再重打这层裁定。

## 本轮唯一主刀

**只做 still-own 白名单 `preflight -> sync`。**

## 允许 scope

你这轮只允许把下面这些 still-own 面纳入真实归仓尝试：

1. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NavigationStaticPointValidationMenu.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationStaticPointValidationRunner.cs`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\memory_0.md`
5. 你 thread / workspace 下已经被你认成 own 的导航审核、验收、分发 docs
6. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第三轮_认领归仓与git上传_01.md`
7. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第三轮回执_01.md`

## 明确禁止的漂移

这轮明确禁止：

1. 不准再继续讲动态 runtime owner
2. 不准继续碰 `PlayerAutoNavigator.cs`
3. 不准继续碰 `Primary.unity`
4. 不准继续新增工具或功能
5. 不准只停在“remaining path 说明”

## 这轮必须真的做的动作

1. 把你准备认领归仓的 still-own 文件列成真实白名单
2. 用稳定 launcher 跑真实 `preflight`
3. 如果 `preflight` 允许，就继续跑真实 `sync`
4. 只有脚本硬拦下时，才允许停在 blocker

## 完成定义

这轮只认两种完成：

### A｜已上 git

必须同时满足：

1. 已真实跑过 `preflight`
2. 已真实跑过 `sync`
3. 已给出提交 SHA

### B｜第一真实阻断已钉死

必须同时满足：

1. 已真实跑过 `preflight`，且不是口头 blocker
2. 如果允许进入 `sync`，也已真实尝试
3. 已给出第一真实 blocker 的 exact path / exact reason
4. 已明确这是不是已经进入 `carrier-needed / integrator-needed`

## 固定回写路径

把完整回执写到：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第三轮回执_01.md`

## 回执必须显式回答

1. 这轮你实际纳入白名单准备归仓的 still-own 文件有哪些
2. 是否已真实运行 `preflight`
3. 是否已真实运行 `sync`
4. 如果已 sync，提交 SHA 是什么
5. 如果未 sync，第一真实 blocker 是什么
6. 当前 own 路径是否 clean

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- `已上 git / 第一真实阻断已钉死`
