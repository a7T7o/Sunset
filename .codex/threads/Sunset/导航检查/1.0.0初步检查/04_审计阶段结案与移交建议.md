# 导航审计阶段结案与移交建议

> 结案时间：2026-03-16
>
> 当前工作目录：`D:\Unity\Unity_learning\Sunset`
>
> 当前分支：`codex/npc-main-recover-001`
>
> 当前 `HEAD`：`b9b6ac48`

## 1. 阶段定位

`1.0.0初步检查` 的目标不是直接改导航代码，而是把线程旧稿、现状代码、场景挂载、Prefab 分布、Editor 配置和工作区承接关系核实清楚，形成一份可以继续接手的审计基线。

截至本次收口，这个阶段已经具备独立结案条件：

- 旧稿索引已迁入阶段目录
- 现状核实稿已落盘
- 子工作区、父工作区、线程记忆都已有承接记录
- 冻结期快照也已单独落盘到治理汇总区

## 2. 当前阶段文档集合

### 2.1 已固化的审计文档

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\01_导航代码索引.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\02_导航全盘分析报告.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\03_现状核实与差异分析.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\04_审计阶段结案与移交建议.md`

### 2.2 当前可确认的文档迁移态

- 旧路径删除态：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\01_导航代码索引.md`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\02_导航全盘分析报告.md`
- 新路径承接态：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\01_导航代码索引.md`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\1.0.0初步检查\02_导航全盘分析报告.md`

这说明当前线程文档正在从“线程根目录散放旧稿”收束为“阶段目录集中承载”。

## 3. 本阶段已经稳定的审计结论

以下结论已经在本阶段形成稳定基线，可以作为后续是否立项整改的判断前提：

1. 当前导航主链仍是 `GameInputManager -> PlayerAutoNavigator -> NavGrid2D`。
2. `PlacementNavigator` 是 `PlacementManager.Start()` 运行时创建，不是场景静态挂载节点。
3. `CloudShadowManager` 的 NavGrid 反射读取路径属于潜在风险，但在 `Primary.unity` 当前配置下不是主风险。
4. `NavGrid2D` 当前并不是真正 Zero GC，`Physics2D.OverlapCircleAll` / `Physics2D.OverlapPointAll` 的分配风险已经被本轮重新钉实。
5. 当前更像“运行正确性已基本成立，但性能治理与文档一致性尚未收口”的中等成熟业务系统。
6. `chest-interaction.md` 与当前 `ClosestPoint` 导航实现存在可确认的文档漂移。

## 4. 本阶段未进入的内容

本阶段没有进入以下动作：

- 没有开始 `NavGrid2D` 非分配改造
- 没有开始刷新调度器设计落地
- 没有开始 `GameInputManager` 拆分
- 没有开始 steering 文档回写修正
- 没有开始新的 `requirements.md` / `design.md` / `tasks.md`

换句话说，当前停点仍然是“审计基线已形成”，而不是“整改方案已立项”。

## 5. 我对下一步的建议

基于当前主线要求“先做文档重组与阶段状态收口，不直接进入代码整改”，我给出的顺序建议是：

### 建议 A：先以审计基线阶段结案

把 `1.0.0初步检查` 视为已完成阶段，当前线程先停在“审计结论稳定、等待是否立项整改”的状态。

这样做的好处是：

- 线程目标更清楚，不会把“审计”与“整改”混成一轮
- 后续如果真的进入整改，可以单独起下一阶段文档和任务清单
- 当前分支里大量与导航无关的文档/代码脏改不会被误并入导航整改

### 建议 B：若要继续整改，应该先起下一阶段

如果后续明确要继续做代码整改，我建议不要直接在 `1.0.0初步检查` 里往下堆，而是新开下一阶段，例如：

- `2.0.0整改设计`
- 或者按问题拆成独立整改子阶段

并先补：

- `requirements.md`
- `design.md`
- `tasks.md`

再进入真实代码修改。

## 6. 当前结案判断

我的当前判断是：

> 这一轮更适合先以“审计基线阶段结案”收口，而不是马上继续代码整改。

原因不是导航问题不重要，而是当前文档迁移、阶段边界、治理工作区状态都还在收束中；先把审计阶段关干净，后续整改才不会把脏现场和旧阶段混在一起。
