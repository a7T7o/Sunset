# 当前现场、高权重事项、风险与未竟问题

## 当前 live 现场
- `cwd = D:\Unity\Unity_learning\Sunset`
- `branch = main`
- `HEAD = ee3187573b62891a5b0a8d974f43c192c4125a34`
- `shared-root-branch-occupancy.md` 口径：`neutral-main-ready`
- 但当前 working tree 不是物理 clean；shared root 里还有很多其他线程 dirty

## 当前最高权重事项
当前最高权重事项不是继续 Day1 新功能，而是：

> 在 V1 已被纠偏、own checkpoint 已被接受的前提下，把线程安全地交给 `spring-day1V2`。

## 当前最值得继续的工作
只有在 V2 完成新的 live preflight 且热区安全后，才值得继续以下内容：
1. 以 prefab 为视觉基线，继续 Day1 工作台 / Prompt / 任务体验迭代
2. 在不碰 `Primary.unity` 的前提下，延续 own 正式面
3. 如果需要 scene 挂载，先明确 hot-file 安全条件

## 当前已接受的 checkpoint

### 代码 / 文档 / 资源基线
1. `83d809a9`：`spring-day1: migrate 0.0.2 story foundation`
2. `b64d4cdd`：`spring-day1: organize 0.0 and handoff docs on main`
3. `ee318757`：当前已接受的 2026-03-26 正式 checkpoint

### 当前明确接受的 own 正式面
1. `Assets/222_Prefabs/UI/Spring-day1/`
2. `Assets/Resources/Story/SpringDay1Workbench/*.asset`
3. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese*.asset`
4. `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`

## 当前未接受、未解决、未验证的部分
1. `Primary.unity` 里的 Day1 scene 侧痕迹没有完成干净归属清分
2. `003-进一步搭建/memory.md` 后半段存在编码污染，不能当唯一依据
3. shared root 当前存在大量其它线程 dirty，V2 不能把它们误算成自己要收的东西
4. 用户审美口径已非常具体，但“最终美术成品质感”仍然依赖下一轮真实续工与复测

## 外部 blocker

### blocker 1：`Primary.unity` 是 mixed hot-file
当前对 `spring-day1` 最关键的外部 blocker 只有这一个：
- 文件：`Assets/000_Scenes/Primary.unity`
- 当前状态：`dirty + unlocked + mixed`
- 为什么阻断：
  - 它里面同时有 Day1 自己的剧情侧块
  - 也同时有他线的相机 / Player / NPC debug 变动
- 当前结论：
  - 只能只读写进交接
  - 不能继续 scene 续写

### blocker 2：shared root 的其他 dirty 可能制造误导
- 当前 live `git status` 里有大量治理、导航、农田、放置、输入等改动
- 这些不能因为和 Day1 “看起来有点相关”就继续被 `spring-day1` 吞并

## 内部 blocker
内部 blocker 当前已经基本清掉；V1 已完成的纠偏包括：
1. 撤回删治理证据的错误判断
2. 撤回错认他线 owner 的判断
3. 钉死 own checkpoint 边界

所以当前内部 blocker 不是“线程自己还没想明白”，而是“接班时必须继续守边界”。

## 当前 live 风险
1. 如果 V2 不先读交接包，很容易再次回到“我顺手继续收 scene / 清 `.kiro` / 认领别线”的旧路
2. 如果 V2 只看 `003-进一步搭建/memory.md` 后半段，可能被编码污染和旧 hygiene 口径误导
3. 如果 V2 不以 prefab 为视觉基线，工作台 UI 与任务卡很容易再次跑偏

## 当前最关键的文件 / 目录 / 文档
1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\26.03.26-Day1越权删证据纠偏与正规化续工委托-07.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\26.03.26-Day1进入下一代交接前状态确认委托-08.md`
5. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\SpringDay1PromptOverlay.prefab`
6. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\SpringDay1WorkbenchCraftingOverlay.prefab`

## 如果今天立刻交给新线程，它先做什么、后做什么、绝对别先做什么

### 先做什么
1. 先读完整交接包
2. 再做 live preflight：
   - `cwd / branch / HEAD / git status`
   - `shared-root-branch-occupancy`
   - 如果涉及 Unity / MCP，再读 MCP 占用层
3. 确认当前仍以 `ee318757` 为 accepted checkpoint
4. 先以 prefab / asset / test / memory 这些 non-hot 正式面为理解入口

### 后做什么
1. 只有在热区安全时，再考虑继续 Day1 UI / 工作台 / 任务体验续工
2. 如需碰 scene，先单独申请 hot-file 判断，不要自行进入

### 绝对别先做什么
1. 绝对别先碰 `Primary.unity`
2. 绝对别先删 `.kiro` 证据
3. 绝对别先认领 `GameInputManager.cs / PlacementManager.cs / StaticObjectOrderAutoCalibrator.cs / TagManager.asset`
4. 绝对别先从“我要把 shared root 清干净”开始

## 对 V2 的一句话现场结论
V1 已经把线程拉回“可接班”的正规状态；V2 接手时真正要守的不是“还能不能写”，而是“别再把边界写坏”。
