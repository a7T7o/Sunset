# 2026-03-29 UI-V1 全局警匪定责清扫第一轮回执_01

## A1 保底六点卡

### 1. 当前主线

当前主线不是继续做 `Prompt / Workbench` 实现，也不是继续给 Day1 UI 补图补 live。

当前主线是：

**把 `UI-V1 = spring-day1 / SpringUI` 这条实施线当前到底真实 own 什么、没有 own 什么、哪些只是借现场验证、哪些旧叙事必须撤回，重新认死。**

### 2. 这轮实际做成了什么

本轮完成了 6 件事：

1. 完整回读了本轮认定书。
2. 完整回读了：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\memory.md`
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\memory_0.md`
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_进度快照.md`
3. 重新回读了 `Phase 1 第一刀 / Phase 2 放行 / 几何漂移纠偏 / Workbench 显示链与 Prompt 正式面 / 排版自适应与信息层级纠偏` 这 5 份任务书原文。
4. 对认定书点名的 repo 路径重新做了 Git 现场核对。
5. 对 `SpringDay1UiPrefabRegistry -> PromptOverlay / WorkbenchOverlay` 的代码引用链重新做了静态复核。
6. 基于这些证据，重新给出 `own / mixed / foreign / docs-only` 切分，外加第二轮建议继续分给我和应从我头上拿走的面。

### 3. 现在还没做成什么

本轮没有做这些事：

- 没有继续实现任何 UI 逻辑
- 没有补新图
- 没有再跑 live
- 没有做 cleanup
- 没有把 own 路径收成 clean
- 没有替别的线程吞 `NpcWorldHintBubble.cs`、scene 事故面或 UI 工作区迁移面

### 4. 当前阶段

当前阶段是：

**第一轮 owner 定责自查已完成，边界已经足够指导第二轮清扫，但当前 own 路径明确仍不 clean。**

### 5. 下一步只做什么

如果基于这轮定责继续推进，下一步只该做一件事：

**只沿 `Prompt / Workbench` 这条 UI-V1 当前 still-own 的实施链做第二轮清扫，不再把 world hint、scene 事故、父线程治理和历史目录迁移继续混吞。**

### 6. 需要用户现在做什么

本轮不需要用户立刻执行额外操作。

如果要我继续，用户现在只需要拍板一件事：

**是否接受我这轮新边界：`UI-V1` 只继续吃 `Prompt / Workbench + prefab-first + UI 证据 / 测试` 这条实施链；`NpcWorldHintBubble`、same-GUID `Primary.unity` 事故整面、父线程治理和 UI 工作区迁移都不再由我继续吞。**

---

## A2 用户补充层

### 一、`UI-V1 -> spring-day1 / SpringUI` 映射是否成立

结论：

**成立。**

更准确地说：

- 用户侧当前叫 `UI-V1`
- repo 内当前对应的真实实施线是：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI`

也就是说，这轮自查不是替一个全新线程认责，而是替这条已经实际推进过 `Phase 1 / Phase 2` 的 Day1 UI 实施线认责。

### 二、当前 own / mixed / foreign / docs-only 清单

#### 当前 own

我这轮认定 `UI-V1` 当前真实 still-own 的实现面是下面这组：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1PromptOverlay.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1WorkbenchCraftingOverlay.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1UiLayerUtility.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1UiPrefabRegistry.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\Resources\Story\SpringDay1UiPrefabRegistry.asset`
6. `D:\Unity\Unity_learning\Sunset\Assets\Editor\Story\SpringUiEvidenceMenu.cs`
7. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringUiEvidenceCaptureRuntime.cs`
8. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\SpringDay1LateDayRuntimeTests.cs`

理由很硬：

1. `Phase 1` 与 `Phase 2` 任务书都把 Day1 UI 的主刀明确压在 `PromptOverlay / WorkbenchOverlay` 这条链上。
2. `spring-day1` 与 `SpringUI` 两份 memory 都把这组文件写成正式实施与验证链。
3. 当前代码现场也印证了这点：
   - `SpringDay1PromptOverlay.cs` 显式调用 `SpringDay1UiPrefabRegistry.LoadPromptOverlayPrefab()`
   - `SpringDay1WorkbenchCraftingOverlay.cs` 显式调用 `SpringDay1UiPrefabRegistry.LoadWorkbenchOverlayPrefab()`
4. `SpringUiEvidenceMenu.cs / SpringUiEvidenceCaptureRuntime.cs / SpringDay1LateDayRuntimeTests.cs` 都不是旁支噪音，而是这条 UI 线为了最终屏证与 runtime 回归而新增的直接支撑面。

#### 当前 mixed

我这轮认定下面这些面不能轻率说成“完全 foreign”，但也不能按 ordinary own 一口吞成我当前清扫面：

1. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\SpringDay1PromptOverlay.prefab`
2. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\SpringDay1WorkbenchCraftingOverlay.prefab`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1WorldHintBubble.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\Primary.unity`

这 4 项我分别这样认：

1. 两个 Day1 prefab：
   - 它们是用户亲手在运行时精修后拖回项目的视觉真源。
   - 当前 runtime 主链又明确依赖它们。
   - 所以它们既不是我能当 foreign 丢掉的东西，也不是我能把“用户手调真脸 + 运行时接链副产”一把吞进普通 cleanup 的纯 own。
   - 最稳妥认定是：**视觉真源 mixed 面。**
2. `SpringDay1WorldHintBubble.cs`：
   - 它是 Day1 专属，不是 NPC 通用气泡。
   - 但 `spring-day1` 与 `SpringUI` memory 又多次把它写成“这轮主刀范围外的旧噪音 / 另刀再补”的面。
   - 所以它不能算 `NpcWorldHintBubble` 那种 foreign，但也不该默认继续挂在 `Prompt / Workbench` 这轮 active own slice 里。
   - 最稳妥认定是：**Day1 相邻 mixed 面，必须分刀治理。**
3. `Assets/222_Prefabs/UI/Spring-day1/Primary.unity`：
   - 我只认这条 UI 线曾经借它做过 Day1 UI 证据现场。
   - 我不认 scene cleanup owner。
   - 我也不认 same-GUID 事故整面。
   - 最稳妥认定是：**mixed incident / evidence-only 面。**

#### 当前 foreign

我这轮明确不该继续吞的，是下面这组：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\NpcWorldHintBubble.cs`
2. same-GUID `Primary.unity` 事故整面
3. `场景搭建（外包）` 线程的任务书 / 审核 / 分发 / 父层治理面
4. UI 工作区目录迁移这件事本身

理由分别是：

1. `NpcWorldHintBubble.cs`：
   - 它已经被别的线明确钉成 NPC 线 owner 面。
   - `spring-day1V2` 也已经明确写过 `NpcWorldHintBubble.cs` 是 `foreign`。
   - 所以它绝对不能因为跟 Day1 UI 同目录，就继续挂在我这条 UI-V1 的 own 清扫面上。
2. same-GUID `Primary.unity` 事故整面：
   - 我只认 UI 证据现场，不认 scene owner，更不认事故总负责。
3. 父线程治理面：
   - 它属于审核 / 分发 / 阶段裁定，不属于我这条实施线的 repo-side 实现 owner。
4. UI 工作区目录迁移：
   - 我能认的是后续在 `0.0.1 SpringUI` 上施工和补记；
   - 不能认“旧 UI 目录迁移 / 删除 / 重组”这件结构动作是我当前 still-own 的面。

#### 当前 docs-only

我这轮认定下面这些属于 docs-only：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\*.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\Before03.28\**`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
4. 当前这份回执文件

但这里要注意一个边界：

- `0.0.1 SpringUI` 这套文档是我当前实施线的 own docs-only 面。
- `Before03.28` 虽然是 docs-only 历史归档，但它不等于“这轮还归我继续推进的活目录”；它更像 `history/docs-only`，不是 `active own docs`。

### 三、`UI-V1` 和 `场景搭建（外包）` 的边界

这轮我给一句硬边界：

**`UI-V1` own Day1 UI 的实现链；`场景搭建（外包）` own UI 的审核、任务书、阶段裁定与父层治理叙事。**

展开成执行面就是：

1. 我这条 `UI-V1` 继续 own：
   - `Prompt / Workbench` runtime 逻辑
   - prefab-first 接链
   - `SpringDay1UiPrefabRegistry`
   - UI 证据抓取工具
   - Day1 UI runtime 测试
2. 父线程继续 own：
   - Phase 任务书
   - 审核报告
   - 父层 `memory`
   - “这轮该停给用户还是继续返工”的治理裁定

如果第二轮正式分配清扫，我建议：

#### 继续分给我

1. `Prompt / Workbench` 两个 overlay 脚本
2. `SpringDay1UiLayerUtility.cs`
3. `SpringDay1UiPrefabRegistry.cs`
4. `Assets/Resources/Story/SpringDay1UiPrefabRegistry.asset`
5. `SpringUiEvidenceMenu.cs`
6. `SpringUiEvidenceCaptureRuntime.cs`
7. `SpringDay1LateDayRuntimeTests.cs`
8. 两个 Day1 prefab，但只能按 **mixed 视觉真源面** 收，不能把它们误报成“纯我 own 的普通资产”

#### 必须从我头上拿走

1. `NpcWorldHintBubble.cs`
2. same-GUID `Primary.unity` 事故整面
3. 父线程治理 / 审核 / 分发面
4. UI 工作区迁移本身
5. `SpringDay1WorldHintBubble.cs` 如果要修，也应先单独分刀，不应继续默认混在当前 `Prompt / Workbench` cleanup 里

### 四、`Primary.unity` 我到底怎么认

我的最终认定是：

**我只把 `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 当成 UI 证据现场。**

我明确不认 3 件事：

1. 不认它的局部 cleanup owner
2. 不认它的 scene writer owner
3. 不认 same-GUID `Primary.unity` 事故整面

所以这题的最硬答案是：

- 我认的是：`evidence-only / mixed incident`
- 我不认的是：`scene owner`

### 五、`SpringDay1UiPrefabRegistry` 这条链是不是当前活跃主链

结论：

**是，而且已经不是“临时兼容件”，而是当前 active runtime 主链的一部分。**

逐项回答：

1. `SpringDay1UiPrefabRegistry.cs`
   - 是当前活跃主链的一部分。
2. `SpringDay1UiPrefabRegistry.asset`
   - 也是当前活跃主链的一部分。
3. `PromptOverlay / WorkbenchOverlay` 对它的 runtime 依赖
   - 不是口头依赖，而是代码已经直接调用 `LoadPromptOverlayPrefab()` / `LoadWorkbenchOverlayPrefab()`。
4. 它是不是“临时兼容件”
   - 不是当前语义下的“挂着也行、不挂也行”的兼容件。
   - 对现在这条 prefab-first Day1 UI 主链来说，它已经是实际入口。

### 六、哪些东西我不该吞

这轮至少拍板 5 个：

1. `NpcWorldHintBubble.cs`
   - 不该吞。
   - 这是 NPC 线 foreign。
2. `SpringDay1WorldHintBubble.cs`
   - 不该自动吞进当前 `Prompt / Workbench` own 清扫面。
   - 它更像 Day1 相邻 mixed 面，必须单独分刀。
3. UI 工作区目录迁移本身
   - 不该吞。
   - 这是结构 / 治理 / 历史迁移问题，不是我当前 still-own 的实施面。
4. same-GUID `Primary.unity` 事故整面
   - 更不该吞。
   - 我只认 UI 证据现场，不认整张事故面。
5. 父线程治理叙事
   - 不该吞成我的实现 owner 进度。

### 七、哪些旧叙事现在必须撤回或改口

至少要改口这 4 条：

1. **“accepted 图已经成立。”**
   - 现在只能改成：
   - `accepted 图曾经被交出来过，不等于这条线当前 owner 自查就能继续把它当过线证据。`
2. **“现在只剩 same-root hygiene。”**
   - 这条要撤回。
   - 当前不只是 hygiene，当前还存在：
     - own / mixed / foreign 边界没说死时留下的叙事混线
     - 两个用户手调 prefab 的 mixed 真源问题
     - `SpringDay1WorldHintBubble.cs` 这种相邻 mixed 面
3. **“Phase 2 已可终验。”**
   - 这条要撤回。
   - 因为用户已经多轮驳回正式面，而且这轮本身就是 owner 定责自查，不是继续沿用旧终验口径往前走。
4. **“整张 UI 面已经收口。”**
   - 这条也要撤回。
   - 更准确的说法只能是：
   - `Day1 UI 这条线确实做过 Phase 1 / Phase 2 / 证据抓取，但当前仍不能把 mixed 真源面、相邻 world hint 面和 same-root scene 事故统统抹平成“只剩 hygiene”。`

### 八、当前 own 路径为什么不 clean

结论先说：

**当前 own 路径 = 不 clean。**

按我这轮最终认定的 own + active docs-only 面，当前至少能直接看到这些 dirty / untracked：

1. `M D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1PromptOverlay.cs`
2. `M D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1WorkbenchCraftingOverlay.cs`
3. `M D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1UiLayerUtility.cs`
4. `?? D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringDay1UiPrefabRegistry.cs`
5. `?? D:\Unity\Unity_learning\Sunset\Assets\Resources\Story\SpringDay1UiPrefabRegistry.asset`
6. `?? D:\Unity\Unity_learning\Sunset\Assets\Editor\Story\SpringUiEvidenceMenu.cs`
7. `?? D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\UI\SpringUiEvidenceCaptureRuntime.cs`
8. `?? D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\SpringDay1LateDayRuntimeTests.cs`
9. `M D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
10. `?? D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\`
11. `?? D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\2026-03-29_UI-V1_全局警匪定责清扫第一轮回执_01.md`

光这组就已经足够得出：

**当前 own roots = no。**

同时，当前 mixed 面也还在场：

1. `M SpringDay1PromptOverlay.prefab`
2. `M SpringDay1WorkbenchCraftingOverlay.prefab`
3. `M SpringDay1WorldHintBubble.cs`
4. `?? Assets/222_Prefabs/UI/Spring-day1/Primary.unity`

所以这轮结论只能是：

- own 不 clean
- mixed 也没切干净
- 更不能把这一切继续包装成“只剩同根卫生”

---

## 停步自省

### 我这轮最核心的判断

我这轮最核心的判断是：

**`UI-V1` 当前真实 still-own 的是 `Prompt / Workbench + prefab-first + registry + evidence + runtime test` 这条实施链；两个手调 prefab、`SpringDay1WorldHintBubble.cs`、`Primary.unity` 只是 mixed 相邻面，`NpcWorldHintBubble.cs` 和父线程治理 / 目录迁移则明确不该继续吞。**

### 我为什么认为这个判断成立

因为本地证据同时指向 4 件事：

1. 任务书从 `Phase 1` 到 `Phase 2` 都把 `Prompt / Workbench` 这条链写成唯一主刀。
2. `spring-day1` 与 `SpringUI` memory 都把 `SpringDay1UiPrefabRegistry`、证据抓取工具和 `LateDayRuntimeTests` 写进这条 UI 实施链。
3. 当前代码引用链直接把 runtime 主入口挂到了 `SpringDay1UiPrefabRegistry.Load*Prefab()` 上。
4. 同时又有足够多的记忆证据反复提醒：
   - `SpringDay1WorldHintBubble.cs` 是这轮主刀范围外旧噪音
   - `NpcWorldHintBubble.cs` 必须分出去
   - `Primary.unity` 不能继续吞成 own scene

### 我这轮最薄弱的点

我这轮最薄弱的点是：

**我可以高置信判“谁不该继续算我”，但对 mixed 面内部每一行 diff 的最终 owner 还不能仅靠这轮只读材料一次性切死。**

最明显的就是：

- 两个手调 prefab
- `SpringDay1WorldHintBubble.cs`
- `Assets/222_Prefabs/UI/Spring-day1/Primary.unity`

我能稳地说它们不是 ordinary own，也不是简单 foreign；
但要做第二轮真正 cleanup，仍然要按这轮切出来的 mixed 面再单独分刀。

### 我给自己这轮结果的自评

我给自己这轮 `8/10`。

做对的地方：

- 我把 `own / mixed / foreign / docs-only` 边界拉回真话了。
- 我没有继续把 `accepted`、`Phase 2`、scene 证据和 world hint 面混成一锅。

扣分的地方：

- 这轮只能把大边界认死，还没把 mixed 面的后续拆账执行掉。

### 如果现在继续，我最确信的下一步

我最确信的下一步是：

**第二轮如果继续，只清我这轮明确 still-own 的那条 `Prompt / Workbench` UI 实施链；两个 prefab 与 `SpringDay1WorldHintBubble.cs` 先按 mixed 单独管理，不再顺手把 `NpcWorldHintBubble.cs` 或 scene 事故继续吞进来。**

### 如果我可能判断错，最可能错在哪

最可能错在：

- `SpringDay1WorldHintBubble.cs` 到底应该最终回挂给这条 UI-V1，还是另起 Day1 近身提示切片；
- 两个 prefab 在第二轮 cleanup 里到底要不要暂时升成“我负责收 mixed 真源面”的局部 own。

但即便这里将来要再细分，也不影响这轮最重要的主判断：

**`NpcWorldHintBubble.cs`、same-GUID `Primary.unity` 整面、父线程治理和目录迁移，不该继续算在我头上。**

---

## B 技术审计层

### 一、已读取材料

本轮已读取：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\2026-03-29_UI-V1_全局警匪定责清扫第一轮认定书_01.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\memory.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_进度快照.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-28_UI线程Phase1第一刀放行任务书.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-28_UI线程Phase2放行任务书.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-28_UI线程Phase2几何漂移纠偏任务书.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-28_UI线程Phase2-Workbench显示链与Prompt正式面收口任务书.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\2026-03-29_UI线程Phase2-排版自适应与信息层级纠偏任务书.md`

### 二、代码 / Git 现场复核结论

1. `SpringDay1PromptOverlay.cs` 与 `SpringDay1WorkbenchCraftingOverlay.cs` 当前都显式依赖 `SpringDay1UiPrefabRegistry.Load*Prefab()`。
2. 这说明：
   - `SpringDay1UiPrefabRegistry.cs`
   - `SpringDay1UiPrefabRegistry.asset`
   当前都不是死文件，也不是可有可无的临时兼容件。
3. 当前认定书点名路径的 Git 现场，至少可见：
   - own：
     - `M SpringDay1PromptOverlay.cs`
     - `M SpringDay1WorkbenchCraftingOverlay.cs`
     - `M SpringDay1UiLayerUtility.cs`
     - `?? SpringDay1UiPrefabRegistry.cs`
     - `?? SpringDay1UiPrefabRegistry.asset`
     - `?? SpringUiEvidenceMenu.cs`
     - `?? SpringUiEvidenceCaptureRuntime.cs`
     - `?? SpringDay1LateDayRuntimeTests.cs`
   - mixed：
     - `M SpringDay1PromptOverlay.prefab`
     - `M SpringDay1WorkbenchCraftingOverlay.prefab`
     - `M SpringDay1WorldHintBubble.cs`
     - `?? Assets/222_Prefabs/UI/Spring-day1/Primary.unity`
   - foreign / 不该吞：
     - `?? NpcWorldHintBubble.cs`
   - docs-only：
     - `?? .kiro/specs/UI系统/0.0.1 SpringUI/`
     - `?? .kiro/specs/UI系统/Before03.28/`

### 三、最终认定总表

- `UI-V1 -> spring-day1 / SpringUI` 映射：`成立`
- `SpringDay1UiPrefabRegistry` 是否当前活跃主链：`是`
- `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 是否 scene owner：`否`
- `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 当前定位：`UI evidence-only / mixed incident`
- `NpcWorldHintBubble.cs`：`foreign`
- `SpringDay1WorldHintBubble.cs`：`mixed，且不应自动并入当前 overlay own slice`
- `SpringDay1PromptOverlay.prefab / SpringDay1WorkbenchCraftingOverlay.prefab`：`mixed 视觉真源面`
- 当前 own 路径是否 clean：`否`

### 四、第二轮分配建议

#### 建议继续分给我

- `SpringDay1PromptOverlay.cs`
- `SpringDay1WorkbenchCraftingOverlay.cs`
- `SpringDay1UiLayerUtility.cs`
- `SpringDay1UiPrefabRegistry.cs`
- `SpringDay1UiPrefabRegistry.asset`
- `SpringUiEvidenceMenu.cs`
- `SpringUiEvidenceCaptureRuntime.cs`
- `SpringDay1LateDayRuntimeTests.cs`
- 两个 Day1 prefab，但只按 mixed 真源面管理

#### 建议从我头上拿走

- `NpcWorldHintBubble.cs`
- same-GUID `Primary.unity` 事故整面
- 父线程治理 / 审核 / 分发面
- UI 工作区目录迁移本身
- `SpringDay1WorldHintBubble.cs` 若要修，先单独立项，不要再默认混入当前 `Prompt / Workbench` cleanup
