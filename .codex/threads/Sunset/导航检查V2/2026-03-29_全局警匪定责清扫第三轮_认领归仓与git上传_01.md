# 2026-03-29_全局警匪定责清扫第三轮_认领归仓与git上传_01

这轮不是继续讲 `PlayerAutoNavigator.cs` 的历史 residue。

这轮只做一件事：

**把你第二轮已经认下的 current own 包，真的尝试按白名单上 git。**

## 已接受基线

你第二轮已经站住：

- current own = `PlayerAutoNavigator.cs + own docs`
- 不再继续吞：
  - `GameInputManager.cs`
  - static runner / menu
  - `Primary.unity`
- `NavigationLiveValidationRunner.cs / Menu.cs / Solver.cs` 只允许报 exact residue，不再整文件 claim

第三轮不再重打这层裁定。

## 本轮唯一主刀

**只做 `PlayerAutoNavigator.cs + own docs` 的真实归仓尝试。**

## 允许 scope

你这轮只允许处理：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第一轮回执_01.md`
5. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第二轮回执_01.md`
6. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第三轮_认领归仓与git上传_01.md`
7. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第三轮回执_01.md`

## 明确禁止的漂移

这轮明确禁止：

1. 不准继续碰 `GameInputManager.cs`
2. 不准继续碰 static runner / menu / solver
3. 不准继续碰 `Primary.unity`
4. 不准继续跑新的 live
5. 不准只停在“exact remaining 说明”

## 这轮必须真的做的动作

1. 把上面这组 current own 文件列成真实白名单
2. 真实运行 `preflight`
3. 如果允许继续，就真实运行 `sync`
4. 只有脚本或代码闸门挡住，才允许停表

## 完成定义

### A｜已上 git

必须同时满足：

1. 已真实跑过 `preflight`
2. 已真实跑过 `sync`
3. 已给出提交 SHA

### B｜第一真实阻断已钉死

必须同时满足：

1. 不是口头 blocker，而是真实脚本阻断
2. 已给出 exact blocker path / reason
3. 已说明这是 compile / same-root / mixed hot-file 哪一类阻断

## 固定回写路径

把完整回执写到：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第三轮回执_01.md`

## 回执必须显式回答

1. 这轮实际纳入白名单准备归仓的 current own 文件有哪些
2. 是否已真实运行 `preflight`
3. 是否已真实运行 `sync`
4. 如果已 sync，提交 SHA 是什么
5. 如果未 sync，第一真实 blocker 是什么
6. 当前 own 路径是否 clean

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- `已上 git / 第一真实阻断已钉死`
