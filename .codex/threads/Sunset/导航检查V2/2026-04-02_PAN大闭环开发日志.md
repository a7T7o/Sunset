# 2026-04-02 PAN 大闭环开发日志

## 本轮代码刀口

- owned file：
  - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
- 本轮实际新增的最小 runtime 补口：
  1. endpoint 完成语义：
     - `TryFinalizeArrival(...)` 不再直接走 `TryHoldBlockedPointArrival(playerPos, "抵达终点判定")`
     - 保留 `TryHoldPostAvoidancePointArrival(...)`
  2. crowd/corridor 慢爬触发：
     - 新增 `PASSIVE_NPC_CROWD_SLOWCRAWL_REPATH_*` 常量
     - 新增 `ShouldBreakPassiveNpcCrowdSlowCrawl(...)`
     - 在 `ShouldDeferPassiveNpcBlockerRepath(...)` 的 `HasPassiveNpcCrowdOrCorridor(...)` 分支里，把 slow-crawl 也纳入 `crowdForceRepath`

## fresh 样本账本

- `EndpointNpcOccupied raw`（endpoint 补口后）：
  - `pass=True`
  - `outcome=ReachedClickPoint`
  - `playerCenterDistance=0.188`
  - `blockedInputFrames=0`
  - `detourMoveFrames=150`
  - 结论：
    - 旧的 `BlockedInput / Linger` 坏链已经翻绿

- `Crowd raw`（crowd 补口后首样本）：
  - `pass=True`
  - `playerCenterDistance=0.175`
  - `directionFlips=2`
  - `crowdStallDuration=0.268`
  - `blockedInputFrames=0`
  - 结论：
    - 双 NPC 通道慢爬已明显收敛到过线范围

- `SingleNpcNear raw`：
  - `pass=True`
  - `playerCenterDistance=0.164`
  - `blockOnsetEdgeDistance=0.000`
  - `npcPushDisplacement=0.000`
  - 结论：
    - crowd 补口没有把近距单 NPC 场景打坏

- `MovingNpc / Raw Push`：
  - `pass=True`
  - `minClearance=0.022`
  - `playerCenterDistance=0.191`
  - `npcPushDisplacement=0.000`
  - 结论：
    - 提前切 detour 没把 moving NPC 场景打成误推

- `EndpointNpcOccupied raw`（post-crowd patch 再复核）：
  - `pass=True`
  - `playerCenterDistance=0.193`
  - `blockedInputFrames=0`
  - `detourMoveFrames=112`
  - 结论：
    - crowd 第二刀没有回归打坏 endpoint

- `Ground Point Matrix raw`：
  - `pass=True`
  - `reachedCases=6/6`
  - `accurateCenterCases=6/6`
  - `positiveCenterBiasCases=0/6`
  - `maxColliderDistance=0.192`
  - 结论：
    - 结构合同仍然是 `ColliderCenter` 对点击点
    - `Transform / Rigidbody` 仍稳定偏低约 `1.09 ~ 1.24`
    - 这条 pass 不能偷换成“玩家可视停位偏上已经消失”

## 当前真实 blocker

- 继续做第二次 `Crowd raw` 稳定性复核时，Unity fresh 编译冒出新的 external gate：
  - `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs(485,608,627): error CS0103`
  - 缺失符号：
    - `SetConversationBubbleFocus`
- 这不是 `PlayerAutoNavigator.cs` 的 own error，但它会直接阻断继续起 live
- 因此本轮已执行：
  - `Park-Slice`
  - 当前 thread-state=`PARKED`

## 下一次恢复前的第一动作

1. 先确认 `PlayerNpcChatSessionService.cs` 的 compile gate 是否 cleared
2. cleared 后重新 `Begin-Slice`
3. 第一条 live 固定先做 `Crowd raw` 稳定性复核
4. 再按需要补 endpoint / single / moving 的回归样本，不要回头重做 PAN 真因大讨论

## 2026-04-02 16:23 ~ 16:35（`-40` fresh blocker 塌缩与起跑探针）

- fresh compile 塌缩：
  - 先通过命令桥执行：
    - `MENU=Assets/Refresh`
  - fresh `Editor.log` 结果：
    - 未再复现：
      - `PlayerNpcChatSessionService.cs`
      - `SetConversationBubbleFocus`
    - 当前真 blocker 改判为：
      - `Assets/Editor/Tool_002_BatchHierarchy.cs(387,27): error CS0136`
      - `Assets/Editor/Tool_002_BatchHierarchy.cs(406,27): error CS0136`
      - `A local or parameter named 'parent' cannot be declared in this scope because that name is used in an enclosing local scope to define a local or parameter`
    - 同次日志：
      - `*** Tundra build failed (3.65 seconds), 2 items updated, 862 evaluated`
- 受控 live 起跑探针：
  - 菜单：
    - `Tools/Sunset/Navigation/Run Raw Real Input Endpoint NPC Occupied Validation`
  - fresh 日志只出现：
    - `queued_action=RunRawRealInputEndpointNpcOccupied entering_play_mode`
  - 未出现：
    - `editor_dispatch_pending_action`
    - `scenario_start`
    - `scenario_end`
  - `Library/CodexEditorCommands/status.json` 最终回到：
    - `playmode:EnteredEditMode`
- 当前结论：
  - `PlayerNpcChatSessionService / SetConversationBubbleFocus` 不能再写成当前活 blocker
  - 当前外部真 blocker 是：
    - `Tool_002_BatchHierarchy.cs CS0136`
  - 这轮没有任何“当前最终代码连续重跑”的合法新样本
  - `Ground raw matrix` 的 `center-only` 结构绿仍不能偷换成：
    - `右键停位偏上已关闭`

## 2026-04-02 19:18 ~ 19:23（`-42` fresh compile + full matrix）

- fresh compile：
  - `MENU=Assets/Refresh`
  - `19:18:47 +08:00`
  - 结果：
    - `*** Tundra build success (3.04 seconds), 9 items updated, 862 evaluated`
    - 未再复现任何 `Tool_002_BatchHierarchy.cs` `CS0136`
  - 当前判定：
    - `Tool_002_BatchHierarchy = cleared`

- fresh probe 塌缩：
  - 最新干净 endpoint raw probe 已出现：
    - `scenario_queued=RealInputPlayerEndpointNpcOccupied`
    - `scenario_start=RealInputPlayerEndpointNpcOccupied`
    - `scenario_setup=RealInputPlayerEndpointNpcOccupied`
    - `scenario_observe_start=RealInputPlayerEndpointNpcOccupied`
  - 结论：
    - `queued_action-only` 口径失效

- 当前最终代码矩阵：
  - `EndpointNpcOccupied raw ×3`
    - Run1：
      - `pass=True`
      - `outcome=ReachedClickPoint`
      - `playerCenterDistance=0.193`
      - `npcPushDisplacement=0.034`
    - Run2：
      - `pass=True`
      - `outcome=ReachedClickPoint`
      - `playerCenterDistance=0.190`
      - `npcPushDisplacement=0.036`
    - Run3：
      - `pass=True`
      - `outcome=ReachedClickPoint`
      - `playerCenterDistance=0.194`
      - `npcPushDisplacement=0.035`

  - `Crowd raw ×3`
    - Run1：
      - `pass=False`
      - `playerReached=False`
      - `playerCenterDistance=2.921`
      - `directionFlips=10`
      - `crowdStallDuration=3.691`
    - Run2：
      - `pass=False`
      - `playerReached=False`
      - `playerCenterDistance=2.737`
      - `directionFlips=11`
      - `crowdStallDuration=3.540`
    - Run3：
      - `pass=False`
      - `playerReached=False`
      - `playerCenterDistance=2.415`
      - `directionFlips=11`
      - `crowdStallDuration=3.655`

  - `Ground raw matrix ×1`
    - `pass=True`
    - `reachedCases=6/6`
    - `accurateCenterCases=6/6`
    - `positiveCenterBiasCases=0/6`
    - `maxColliderDistance=0.192`
    - 仍旧只能代表 `center-only` 结构绿，不代表“右键停位偏上已关闭”

  - `SingleNpcNear raw ×2`
    - Run1：
      - `pass=True`
      - `playerCenterDistance=0.165`
      - `npcPushDisplacement=0.000`
    - Run2：
      - `pass=True`
      - `playerCenterDistance=0.170`
      - `npcPushDisplacement=0.000`

  - `MovingNpc raw ×1`
    - `pass=True`
    - `minClearance=0.075`
    - `playerCenterDistance=0.123`
    - `pushDisplacement=0.000`

  - `NpcAvoidsPlayer ×1`
    - `pass=True`
    - `minClearance=0.487`
    - `npcReached=True`
    - `recoveryOk=False`

  - `NpcNpcCrossing ×1`
    - `pass=True`
    - `minClearance=0.252`
    - `npcAReached=True`
    - `npcBReached=True`

- 当前结论：
  - 假 Tool002 blocker 已清
  - 假 queued blocker 已清
  - 当前最终代码矩阵已经完整重跑
  - 当前唯一残余红面集中在 `Crowd raw ×3`

## 2026-04-02 20:20 ~ 20:50（`-44` crowd 同簇两刀补口 + 用户现场纠偏）

- 第一刀：
  - 目标：
    - 不再让 crowd/corridor 的慢爬累计因为 blocker 在多个静止 NPC 间切换而归零
  - 实际补口：
    - `PlayerAutoNavigator.cs`
    - 新增：
      - `_passiveNpcCrowdSightings`
    - 在：
      - `HandleSharedDynamicBlocker(...)`
      - `ResetNavigationState()`
      - `ResetStuckDetection()`
      - `ShouldBreakPassiveNpcCrowdSlowCrawl(...)`
      中接入 crowd 级累计
- 第一刀后的 valid crowd 样本：
  - `pass=False / minEdgeClearance=-0.007 / playerCenterDistance=0.173 / directionFlips=5 / crowdStallDuration=0.946 / playerReached=True`
  - `pass=True / minEdgeClearance=-0.005 / playerCenterDistance=0.092 / directionFlips=2 / crowdStallDuration=0.356 / playerReached=True`
  - `pass=False / minEdgeClearance=-0.001 / playerCenterDistance=0.132 / directionFlips=4 / crowdStallDuration=0.218 / playerReached=True`

- crowd 诊断样本：
  - `pass=False / timeout=6.50 / playerCenterDistance=2.600 / directionFlips=11 / crowdStallDuration=2.967 / playerReached=False`
  - 诊断结论：
    - 尾段 `NavAvoid` 一直是 `PathMove`
    - blocker 在两个静止 NPC 间反复切换
    - `shouldRepath=False`
    - 剩余红面仍在 PAN crowd defer/repath 同簇，不是 detour release 漂移

- 第二刀：
  - 目标：
    - 只在同一 crowd 簇里 blocker 连续切换时，提前把这种振荡当成 crowd 坏相放行 repath
  - 实际补口：
    - 新增：
      - `_lastPassiveNpcCrowdBlockerId`
      - `_passiveNpcCrowdBlockerSwitches`
      - `ShouldBreakPassiveNpcCrowdOscillation(...)`
  - 接入点：
    - `HandleSharedDynamicBlocker(...)`
    - `ShouldDeferPassiveNpcBlockerRepath(...)`
- 第二刀后的 valid crowd 样本：
  - `pass=False / minEdgeClearance=-0.011 / playerCenterDistance=0.186 / directionFlips=2 / crowdStallDuration=0.861 / playerReached=True`
  - `pass=False / minEdgeClearance=-0.005 / playerCenterDistance=0.122 / directionFlips=8 / crowdStallDuration=3.069 / playerReached=True`
  - 第 3 次尝试未形成合法样本，已手动 `STOP`

- 当前错误与纠偏：
  - 用户已明确否定当前 crowd 测试口径：
    - 不能把“前方被一堆 NPC 堵住时继续硬撞/撞开所有 NPC”当成正确目标
  - 这意味着：
    - 当前 `Run Raw Real Input Crowd Validation` 不能再被我继续当成主体验真值
    - 本轮两刀代码暂时只能算局部 runtime 尝试，不得宣称 crowd 已修好

- 当前现场：
  - Unity 已确认回到 `Edit Mode`
  - `thread-state` 已 `PARKED`
  - 当前不能 claim：
    - `Crowd raw ×3` 已绿
    - 护栏已回归通过

## 2026-04-02 21:20 ~ 21:35（`-46` crowd 语义纠偏：runner/menu 新 case 落地 + fresh 矩阵 + 留回裁定）

- 本轮 own 写面：
  - `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`
  - `Assets/YYY_Scripts/Service/Navigation/Editor/NavigationLiveValidationMenu.cs`
- 本轮明确不碰：
  - `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`

- 语义纠偏动作：
  1. 旧 crowd case 显式降级为：
     - `LegacyRealInputCrowdBlockedWallStress`
     - 菜单路径：
       - `Tools/Sunset/Navigation/Legacy/Run Real Input Crowd Blocked-Wall Stress`
       - `Tools/Sunset/Navigation/Legacy/Run Raw Real Input Crowd Blocked-Wall Stress`
  2. 新增：
     - `RealInputPlayerPassableCorridor`
     - `RealInputPlayerStaticNpcWall`
  3. run-all 主链不再串旧 crowd，而是改接 `PassableCorridor`

- fresh compile：
  - `Assets/Refresh`
  - `Library/CodexEditorCommands/status.json`
    - 编译后回到 `isPlaying=false / isCompiling=false`
  - `Editor.log` 最新尾段未见本轮 runner/menu 新引入 compile red

- fresh live：
  - `PassableCorridor ×3`
    - #1 `pass=False / outcome=Oscillation / minEdgeClearance=0.255 / playerCenterDistance=0.200 / directionFlips=5 / actionChanges=9 / crowdStallDuration=0.801`
    - #2 `pass=False / outcome=Oscillation / minEdgeClearance=-0.009 / playerCenterDistance=0.148 / directionFlips=5 / actionChanges=7 / crowdStallDuration=1.327`
    - #3 `pass=False / outcome=Oscillation / minEdgeClearance=0.243 / playerCenterDistance=0.157 / directionFlips=5 / actionChanges=9 / crowdStallDuration=1.141`
  - `StaticNpcWall ×3`
    - #1 `pass=False / outcome=ReachedBlockedTarget / forwardProgress=4.114 / wallFrontX=-7.227 / playerCenterDistance=0.172 / minEdgeClearance=-0.009`
    - #2 `pass=False / outcome=Oscillation / forwardProgress=1.942 / wallFrontX=-7.050 / playerCenterDistance=2.542 / minEdgeClearance=-0.008 / directionFlips=12 / actionChanges=16 / crowdStallDuration=2.442`
    - #3 `pass=False / outcome=ReachedBlockedTarget / forwardProgress=4.085 / wallFrontX=-7.297 / playerCenterDistance=0.128 / minEdgeClearance=-0.010 / directionFlips=5 / actionChanges=9 / crowdStallDuration=1.995`
  - 护栏 raw：
    - `EndpointNpcOccupied ×1`：`pass=True`
    - `GroundPointMatrix ×1`：`pass=True`
    - `SingleNpcNear ×1`：`pass=True`
    - `MovingNpc ×1`：`pass=True`

- 本轮裁定：
  - 新语义已落，但当前最终代码在：
    - `PassableCorridor`
    - `StaticNpcWall`
    两边都没站住
  - 最近两刀 PAN crowd 补口当前没有 fresh 正证据，反而仍把 `PassableCorridor` 压成整齐的 `Oscillation`
  - **当前建议：下轮回退最近两刀 `PlayerAutoNavigator.cs` crowd 补口**

- 新的责任点压缩：
  - `PassableCorridor`
    - `ShouldBreakPassiveNpcCrowdOscillation(...)`
    - `ShouldDeferPassiveNpcBlockerRepath(...)`
  - `StaticNpcWall`
    - `HasPassiveNpcCrowdOrCorridor(...)`
    - `ShouldDeferPassiveNpcBlockerRepath(...)`

- 当前现场：
  - Unity 已回 `Edit Mode`
  - `thread-state` 已 `PARKED`
  - 当前不能 claim：
    - crowd 已修好
    - 右键停位偏上已关闭
