# 2026-03-29_全局警匪定责清扫第二轮执行书_01

这轮不是继续推导航新功能，也不是继续跑 live。

这轮只做一件事：

**把 `导航检查V2` 已经被正式裁定下来的 still-own 面，收成一个只剩自己该认内容的最小清扫包。**

## 已接受基线

你在第一轮回执里已经站住了这条边界：

- 当前 still-own 主刀：
  - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
- 当前不该继续吞：
  - `GameInputManager.cs`
  - `NavigationStaticPointValidationRunner.cs`
  - `Assets/Editor/NavigationStaticPointValidationMenu.cs`
  - `Assets/222_Prefabs/UI/Spring-day1/Primary.unity`
- 旧 `PromptOverlay / Workbench / SpringUiEvidenceMenu` blocker 叙事都已撤回，不能再当当前停车位

第二轮不再重打这层裁定。

## 本轮唯一主刀

**只清 `PlayerAutoNavigator.cs` + 你自己的线程 / 工作区文档。**

## 允许 scope

你这轮只允许处理：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第二轮回执_01.md`

## 明确禁止的漂移

这轮明确禁止：

1. 不准继续碰 `GameInputManager.cs`
2. 不准继续碰 static runner/menu
3. 不准继续碰 `Primary.unity`
4. 不准继续碰 `SpringUiEvidenceMenu.cs`
5. 不准继续跑新的 live
6. 不准把历史碰过的 `NavigationLiveValidationRunner.cs / Menu.cs / NavigationLocalAvoidanceSolver.cs` 再重新写成“当前主刀”

## 关于历史 residual 的唯一允许动作

如果你认为：

- `NavigationLiveValidationRunner.cs`
- `NavigationLiveValidationMenu.cs`
- `NavigationLocalAvoidanceSolver.cs`

里仍然夹着你自己的历史 residue，

你这轮只能做一件事：

- 在回执里精确报出“哪一段 residue、为什么仍像你的”

不准直接把整份文件重新吞回 current own。

## 完成定义

这轮完成，不是指导航问题全修完，而是指下面 4 件事全部成立：

1. 你把 still-own 清扫面稳定收窄到 `PlayerAutoNavigator.cs` + own docs
2. 你没有再继续 claim `GameInputManager.cs`、static runner/menu、`Primary.unity`
3. 你能明确回答当前 own 路径是否 clean
4. 如果还不 clean，你能把剩余路径和原因说到文件级，并把历史 residual 与当前主刀分开

## 固定回写路径

把完整回执写到：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\2026-03-29_全局警匪定责清扫第二轮回执_01.md`

## 回执格式

按这个顺序回：

- `A1 保底六点卡`
- `A2 用户补充层`
- `停步自省`
- `B 技术审计层`

并且必须显式回答：

1. 这轮你实际认领并清扫了哪些 still-own 文件
2. 哪些历史 residual 你现在只报 exact residue，不再整文件 claim
3. 当前 own 路径是否 clean
4. 如果不 clean，剩余 exact path 和原因是什么
5. 当前最新 blocker truth 是否仍为“无 fresh blocker，可继续 claim 的旧 blocker 全撤回”

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- 一句话摘要
