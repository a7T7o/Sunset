# 2026-04-03｜导航父线程续工 prompt v2｜继续接 Primary traversal 的 scene integration，本轮不再回到 crowd / runner

先给这轮裁定：

1. 当前用户已经明确接受“两条并行工”：
   - 你们导航父线程继续接 `Primary traversal` 的 scene integration / live closure
   - 工具-V1（`019d4d18-bb5d-7a71-b621-5d1e2319d778`）并行处理它自己的 `Ready-To-Sync` same-root dirty 阻断
2. 所以你这轮不是再审 prompt，不是再做 crowd，不是再管 runner/menu。
3. 你这轮唯一主刀是：
   - 把 `Primary traversal` 剩余闭环真正接起来
4. 工具-V1 当前三脚本基线已经不是“空 contract”了，而是：
   - `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D` 已经各有一刀真实 contract + runtime-support 加固
   - 你默认先消费这套基线，不要先让它重写

---

## 一、你必须先接受的最新事实

### 1. 当前导航版本的顶层事实

1. 用户已认可当前玩家导航版本
2. `PassableCorridor / StaticNpcWall` 继续只算：
   - `targeted probe / 后续 polish 诊断项`
3. 不再作为当前版本“不可用”的主事实

### 2. `导航检查V2` 的边界

1. `导航检查V2` 现在只收：
   - runner/menu 最终验收交接
   - final acceptance pack
   - sync / own-root 判定
2. 它不是你这轮要继续拉回来一起做的主刀

### 3. 工具-V1 的最新边界

工具-V1 现在仍然不是：

1. scene writer
2. binder owner
3. 通用工具 owner

但它已经补过这 3 个脚本：

1. `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
2. `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
3. `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`

而且最新可依赖的新增契约包括：

### `NavGrid2D`

1. `GetWorldBounds()`
2. `IsWithinWorldBounds(Vector2)`
3. `ClampToWorldBounds(Vector2)`
4. `TryFindNearestWalkable(...)` 对 bounds 外一点点的点位不再直接失败

### `PlayerMovement`

1. `HasNavGridReference()`
2. `TryResolveNavGrid(...)`
3. `CanOccupyPosition(Vector2)`
4. `GetNavigationProbePoints(...)`

### `SceneTransitionTrigger2D`

1. `TryResolveTarget(...)`
2. `HasValidTarget` 统一走 target 解析逻辑
3. 只给 scene path 时也能自动推出 scene name

所以你这轮的第一默认判断必须是：

- 先假设脚本底座已经有了
- 优先查 `Primary` 现场 wiring / binder / live integration

只有 fresh 证据表明真的是脚本缺口时，才反抛给工具-V1。

---

## 二、你这轮唯一主线

把用户最初在 `Primary` 里要的 traversal 现场诉求，真正收成 scene integration / live closure：

1. 玩家不能穿过 `Props`
2. 玩家和 NPC 不能进入 `Water`
3. 玩家不能走出 tilemap 外

这轮不包括：

1. crowd runtime
2. `PassableCorridor / StaticNpcWall` 调优
3. runner/menu
4. 镜头边界
5. Town 转化
6. 工具线

---

## 三、允许的 scope

### 允许

1. `Primary.unity`
2. `PrimaryTraversalSceneBinder.cs`
3. 与上面 3 个 traversal 问题直接相关的最小 scene wiring / live integration
4. 与这 3 个 traversal 问题直接相关的最小 navigation glue

### 禁止

1. 不准再碰 `PlayerAutoNavigator.cs` crowd runtime
2. 不准再把这轮和 `导航检查V2` 的 final acceptance 线混报
3. 不准把镜头边界扩进来
4. 不准让工具-V1继续替你写 scene / binder
5. 不准把工具-V1正在做的 same-root dirty 清理混进你这轮

---

## 四、这轮默认执行顺序

1. 先 fresh 核 `Primary` 当前现场：
   - `Props`
   - `Water`
   - tilemap 外边界
   到底哪一层没闭环：
   - scene wiring
   - binder / live apply
   - navigation glue
   - 还是脚本 contract
2. 默认先消费工具-V1已经给出的三脚本基线
3. 如果 scene wiring / binder / live integration 缺环，你直接在你 own 的 scene 面收口
4. 只有当 fresh 证据证明是脚本缺口时，才精确点名回抛：
   - 哪个文件
   - 哪个入口 / 字段 / 行为
   - 为什么现在确定不是 scene 问题

---

## 五、completion 定义

### 形态 A：真正闭环

同时满足：

1. 你已经把 `Primary traversal` 从 crowd / runner 主线里独立报实
2. 玩家朝 `Props` 走时，进不去
3. 玩家朝 `Water` 走时，进不去
4. 玩家朝地图外走时，被拦住
5. NPC / 自动导航也遵守同一语义
6. 你把“结构成立”和“真实入口体验成立”分开报清楚

### 形态 B：压实 blocker

同时满足：

1. 你没把这轮又漂回 crowd / runner
2. 你没越界要求工具-V1继续写 scene
3. 你把第一责任点压到下面三类之一：
   - `Primary.unity` wiring
   - `PrimaryTraversalSceneBinder.cs` / live integration
   - 工具-V1 的精确 contract gap

---

## 六、固定回执要求

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 这轮有没有碰：
   - `Primary.unity`
   - `PrimaryTraversalSceneBinder.cs`
   - `NavGrid2D.cs`
   - `PlayerMovement.cs`
   - `SceneTransitionTrigger2D.cs`
2. `Props / Water / tilemap 外边界` 现在各自处于哪一层：
   - `结构成立`
   - `targeted probe 成立`
   - `真实入口体验成立`
3. NPC / 自动导航是否也已被同一语义拦住
4. 如果仍未闭环，第一责任点是：
   - scene wiring
   - binder / live integration
   - 还是脚本 contract gap

### B 技术审计层

至少回答：

1. changed_paths
2. 是否触碰 scene
3. 是否触碰 `PlayerAutoNavigator.cs`
4. 是否触碰 `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D`
5. pre_sync_validation
6. 当前 own 路径是否 clean
7. blocker_or_checkpoint
8. 一句话摘要

---

## 七、thread-state

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
