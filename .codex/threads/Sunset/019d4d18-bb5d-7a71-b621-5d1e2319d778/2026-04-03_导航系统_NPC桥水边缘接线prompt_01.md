# 2026-04-03｜导航系统续工 prompt｜只收 NPC 接入桥 / 水 / 边缘 traversal contract

> 说明：本文件为 2026-04-05 因误删后按 thread memory 恢复的留档稿，用来补回历史交接材料；它不是当前线程的新主线，也不代表当前线程重新接回导航施工。

## 当前事实

1. 玩家侧桥 / 水 / 边缘版本当时已被用户认可，不允许改坏。
2. 剩余问题集中在 NPC：
   - NPC 走不过桥
   - NPC 没有吃到与玩家一致的桥面 soft-pass / 水面阻挡 / 地图边界约束
3. 当前线程的判断是：
   - 问题不在玩家版本本身
   - 更像 NPC 仍在走旧接线

## 关键责任点

1. `TraversalBlockManager2D` 当时只显式绑定给 `playerMovement`
2. `NPCAutoRoamController` 虽然自己持有 `NavGrid2D` 并通过 `rb.MovePosition(nextPosition)` 驱动移动
3. 但 NPC 没并行吃到玩家这轮新增的 traversal contract：
   - soft-pass bridge 支撑语义
   - bounds enforcement
   - 中心支撑优先

## 本轮只允许处理的范围

- `Assets/YYY_Scripts/Service/NPC/NPCAutoRoamController.cs` 或同等 NPC 漫游入口
- `Assets/YYY_Scripts/Service/Navigation/TraversalBlockManager2D.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
- 与上面三者直接相关的最小 glue

## 明确禁止

- 不准改坏玩家当前已认可版本
- 不准扩回树苗卡顿
- 不准碰 Tool
- 不准碰 camera
- 不准碰 UI
- 不准碰 Town
- 不准碰 scene sync / binder
- 不准把这轮扩成“统一重构整个导航系统”

## 完成定义

只有下面同时成立，这刀才算过：

1. NPC 可以正常过桥
2. NPC 不会下水
3. NPC 不会越出地图边界
4. 玩家当前已认可的桥 / 水 / 边缘表现不被改坏

## 回执口径

回执时至少要说清：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

## 历史备注

- 这份文件只用于保留当时“玩家版本已认可、NPC contract 另线接刀”的历史判断
- 当前线程现行主线已改为纯相机线，不再据此继续施工导航
