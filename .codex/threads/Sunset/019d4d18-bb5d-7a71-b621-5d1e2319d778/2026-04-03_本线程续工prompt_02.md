# 2026-04-03｜本线程续工 prompt v2｜并行只收 same-root blocker，不再扩 scene / crowd / runner

先给这轮裁定：

1. 这是用户明确批准的并行切片。
2. 这轮不是继续写 `Primary/Town` scene。
3. 这轮也不是继续给三脚本追加第二刀 runtime 行为。
4. 这轮唯一主刀是：
   - 把我当前 `Ready-To-Sync` 的 same-root remaining dirty 阻断压到最小、压到可治理、压到可继续分工

这轮的目标不是“把整个 shared root 清干净”，而是：

1. 只处理我这条线自己造成或自己背着的阻断
2. 不碰 foreign dirty
3. 不再把自己拖回 scene / binder / tool / crowd 业务

---

## 一、这轮如何覆盖上一版边界

上一版 prompt 的硬边界仍然成立：

1. 不碰 `Primary.unity`
2. 不碰 `Town.unity`
3. 不碰 `PrimaryTraversalSceneBinder.cs`
4. 不碰通用工具
5. 不把脚本 contract 说成场景已完成

但用户现在又追加了一个新的并行要求：

- 导航线继续去做 `Primary traversal` 的 scene integration
- 我这条线并行处理我自己的 `Ready-To-Sync` same-root dirty 阻断

所以这轮是一个**用户批准的临时 override slice**：

1. 不是恢复我做 scene
2. 也不是恢复我做工具
3. 只是允许我为了解掉当前 sync 阻断，处理我自己 still-own / still-carried 的 same-root 尾账

---

## 二、你这轮唯一主线

把当前 `Ready-To-Sync` blocker 从“大坨 same-root dirty”压成最小真相。

当前 blocker 里至少包括这些 root：

1. `Assets/YYY_Scripts/Service/Player`
2. `Assets/YYY_Scripts/Service/Navigation`
3. `.codex/threads/Sunset/019d4d18-bb5d-7a71-b621-5d1e2319d778`

当前已知 same-root dirty 里，需要你优先核的是：

### `Assets/YYY_Scripts/Service/Player`

1. `PlayerAutoNavigator.cs`
2. `PlayerMovement.cs`
3. `PlayerNpcChatSessionService.cs`
4. `PlayerThoughtBubblePresenter.cs`
5. `PlayerToolFeedbackService.cs`

### `Assets/YYY_Scripts/Service/Navigation`

1. `NavGrid2D.cs`
2. `NavigationLiveValidationRunner.cs`
3. `NavigationLiveValidationMenu.cs`
4. `NavigationStaticPointValidationRunner.cs`

### 线程根目录

1. 当前线程自己的 prompt / memory / untracked 文件

你这轮的目标不是把这些全修完，而是先分成三类：

1. 本轮必须保留的当前切片内容
2. 我自己历史残留但可以安全收掉的内容
3. 明确属于 foreign / 别线的内容

---

## 三、允许的 scope

### 允许

1. 当前线程根目录下的 prompt / memory / untracked 清理与收口
2. 我自己名下、且当前确认是历史残留的 `Assets/YYY_Scripts/Service/Player/*` old dirty 的只读核实与最小清理
3. 当前三脚本：
   - `NavGrid2D.cs`
   - `PlayerMovement.cs`
   - `SceneTransitionTrigger2D.cs`
   仅限于为了和 blocker 清理对齐而做最小 trim / 对账，不准继续加新行为

### 禁止

1. 不准碰 scene
2. 不准碰 binder
3. 不准碰工具
4. 不准继续追加 crowd / runner / validation 业务
5. 不准碰 foreign dirty：
   - `NavigationLiveValidationRunner.cs`
   - `NavigationLiveValidationMenu.cs`
   - `NavigationStaticPointValidationRunner.cs`
   这些如果确认为导航线 own，就只能报实，不能代清
6. 不准把这轮做成“shared root 大清扫”

---

## 四、这轮默认执行顺序

1. fresh 跑一次当前 same-root dirty 清单核实
2. 把 dirty 分成三类：
   - 当前切片必须保留
   - 我自己历史残留、可安全清掉
   - foreign / 需要转交
3. 只清：
   - 我自己历史残留
   - 当前线程根目录里与这轮 prompt / memory 无关的尾账
4. 不动 foreign dirty
5. 再跑一次 `Ready-To-Sync`
6. 最终只允许两种结果：
   - blocker 缩窄成功
   - 或 blocker 被压成更小、更真、更可转交的阻断单

---

## 五、completion 定义

### 形态 A：blocker 缩窄成功

同时满足：

1. 你没有碰 scene / binder / tool
2. 你没有继续给三脚本追加新的业务行为
3. 你已经把自己历史残留的 same-root dirty 清掉一部分
4. `Ready-To-Sync` 的 blocker 现在已经缩窄成：
   - foreign `Navigation` dirty
   - 或更小的我方 residual

### 形态 B：精准阻断单

同时满足：

1. 你没有越界清 foreign dirty
2. 你已经把 blocker 分出：
   - 哪些是我自己的旧残留
   - 哪些是 foreign
   - 哪些现在不能安全自清
3. 你明确写清：
   - 为什么当前不能再由我自己继续扩大 cleanup
   - 下一步该交给谁

---

## 六、这轮必须特别纠正的口径

你不能再把当前状态写成：

- “三脚本纯 contract support 已完成，只剩治理问题”

因为 `NavGrid2D` 和 `PlayerMovement` 已经带进真实 runtime behavior change。

所以你下次回执必须分开报：

1. contract 层
2. runtime behavior 层
3. sync / blocker 层

不准再把这三层揉成一句话。

---

## 七、固定回执要求

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 这轮哪些改动属于 contract 层
2. 这轮哪些改动已经属于 runtime behavior 层
3. 这轮 same-root dirty 里哪些是我自己的历史残留
4. 哪些是 foreign dirty
5. 这轮有没有继续碰 scene / binder / tool
6. 你下一次重新开工的唯一触发条件是什么

### B 技术审计层

至少回答：

1. changed_paths
2. `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D` 各自哪些是 public contract
3. 哪些已经影响 runtime behavior
4. `Ready-To-Sync` latest blocker exact 口径
5. 当前 own 路径是否 clean
6. blocker_or_checkpoint
7. `Begin-Slice / Ready-To-Sync / Park-Slice`

---

## 八、thread-state

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```

---

## 九、一句话总结

**这轮我不再碰 scene，也不再继续给三脚本加业务行为；唯一任务是把当前 `Ready-To-Sync` 的 same-root dirty 阻断压到最小、压到可治理、压到可继续分工。**
