# 2026-04-03｜farm 续工 prompt｜树苗放置卡顿正式交接

请先接受这次最新分责：

1. `树苗放置卡顿` 从本线程正式释放给 `farm`。
2. 我这边下一轮只保留 `桥 / 水 / 边缘` 的视觉与可走范围对齐，不再继续碰 tree stutter。
3. 你这轮不是来接 `桥 / 水 / 边缘`，也不是来接 `Tool_002`、`Primary/Town` scene、camera、UI、转场、binder 或通用工具。
4. 用户最新 live 事实必须压过任何结构乐观口径：
   - 箱子放置现在基本不明显卡
   - 农作物放置现在基本不明显卡
   - 只有树苗放置仍然会卡

---

## 一、这轮唯一主刀

你这轮只做一件事：

**把“树苗放置仍然卡顿”这条线真正收口。**

说人话就是：

1. 不再泛谈“placeable 都会卡”。
2. 不再泛谈“导航是不是整体又坏了”。
3. 直接围绕 `sapling placement stutter` 做定点爆破。
4. 目标不是再给一轮结构解释，而是把“为什么只有树苗还卡”压成最小真实责任点并修掉。

---

## 二、当前已接受基线

### 1. 用户体感基线

用户最新明确反馈是：

1. 树苗放置还是会卡。
2. 箱子基本不再卡。
3. 农作物基本不再卡。

这三句一起成立时，当前主嫌疑默认不再是“所有 placeable 共享的通用导航刷新”，而是：

1. 树苗自己独有的初始化 / 展示 / 碰撞 / 注册链；
2. 或树苗放下后立即恢复预览时，仍然只有树苗会额外触发的那部分热路径。

### 2. 本线程已经做过的历史修改

下面这些不是计划，而是已经真实落过的改动和当前代码现场：

1. `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
   - 已新增 `TryRequestLocalRefresh(...)`
   - 入口在当前文件约 `141` 行
   - 当前不再只有整图 `RefreshGrid() -> RebuildGrid()` 一档
2. `Assets/YYY_Scripts/World/Placeable/ChestController.cs`
   - 放置出生后一帧刷新已改成优先走局部 refresh
   - `RequestNavGridRefresh()` 约在 `646` 行
   - `GetColliderBounds()` 约在 `570` 行
   - 当前它更适合作为“已明显减轻卡顿的对照组”
3. `Assets/YYY_Scripts/Service/Placement/PlacementValidator.cs`
   - 运行时树 / 箱子查询已从 `FindObjectsByType` 改成活动实例表
   - `HasTreeAtPosition(...)` 约在 `421` 行
   - `HasTreeWithinDistance(...)` 约在 `464` 行
   - `GetTreesForCurrentFrame()` / `GetChestsForCurrentFrame()` 约在 `630 / 640` 行
4. `Assets/YYY_Scripts/Controller/TreeController.cs`
   - 已加活动实例表
   - 已加运行时树苗延迟初始化与轻量 presentation 分支
   - `FinalizeDeferredRuntimePlacedSaplingInitialization()` 约在 `322` 行
   - `ApplyRuntimePlacedSaplingLightweightPresentation()` 约在 `440` 行
   - `ShouldSyncColliderShapeForCurrentPresentation()` 约在 `636` 行
   - 当前树苗放置卡顿仍存在，说明这里很可能还有没削干净的重链
5. `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs`
   - 放置成功后的 `ResumePreviewAfterSuccessfulPlacement()` 已被改过
   - 入口约在 `1405` 行
   - 当前仍不能排除它与树苗链组合后造成残余顿点

### 3. 当前最重要的责任判断

请先接受下面这句，不要重审回旧口径：

**“箱子不卡、农作物不卡、只有树苗卡”已经说明当前 residual peak 不该再先按通用导航问题起手，而该先按树苗专属运行时链起手。**

---

## 三、你这轮应该先盯哪几段

### 第一优先级

`TreeController` 的树苗专属首帧链：

1. `InitializeAsNewTree()`
2. `Start()`
3. `FinalizeDeferredRuntimePlacedSaplingInitialization()`
4. `ApplyRuntimePlacedSaplingLightweightPresentation()`
5. `SetStage(0)` 相关展示 / 碰撞同步

你要先确认：

1. 当前是否仍有重复初始化
2. 当前是否仍有重复展示同步
3. 当前是否仍有本不该在 `Stage 0` 触发的 collider / nav / shadow / registry 成本

### 第二优先级

`PlacementManager` 在树苗成功放下后的恢复链：

1. 是否仍然在同一放置完成窗口里做了树苗特有的二次重验
2. 是否仍然会比箱子/农作物多跑一轮 preview 恢复或 validation
3. 是否还有只对树苗触发的额外事件 / 注册 / 表现层刷新

### 第三优先级

`NavGrid2D` 当前局部刷新 contract 是否已经足够：

1. 如果树苗 residual peak 仍明显落在 `NavGrid` 刷新链，再继续精确压是哪段没轻量化干净
2. 但只有在你证明树苗专属链已经不够解释问题时，才回头把第一责任点再抬回 `NavGrid2D`

---

## 四、这轮允许与禁止

### 允许

1. 继续修改：
   - `Assets/YYY_Scripts/Controller/TreeController.cs`
   - `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs`
   - `Assets/YYY_Scripts/Service/Placement/PlacementValidator.cs`
2. 只在有明确证据时再动：
   - `Assets/YYY_Scripts/World/Placeable/ChestController.cs`
   - `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
3. 允许把箱子当成对照组来确认“为什么它不再卡而树苗还卡”
4. 允许做最小 live / profiler / 打点验证，但必须先说清楚要拿什么证据、最多跑几轮、何时停

### 禁止

1. 不准扩回 `桥 / 水 / 边缘`
2. 不准碰 `PlayerMovement.cs`
3. 不准碰 `TraversalBlockManager2D.cs`
4. 不准碰 `Tool_002`
5. 不准碰 `Primary.unity / Town.unity`
6. 不准重开 binder、scene sync、通用工具
7. 不准把 shared-root 当前一堆 unrelated dirty 顺手吞成 cleanup
8. 不准把“脚本侧看起来更轻了”直接写成“用户体感已修好”

---

## 五、这轮完成定义

只有下面两层同时成立，才算这刀真的做完：

### 结构层

1. 你能明确说出“为什么只有树苗还卡”
2. 你能点名第一责任方法或第一责任链
3. 你做的修法不再是泛调参数，而是对准那条链的最小真实修复

### 体验层

1. 用户连续放树苗时，放下那一下不应再明显比箱子 / 农作物更卡
2. 如果你这轮没拿到 live 证据，就必须诚实写成：
   - `结构成立，体验仍待验证`

---

## 六、你这轮必须额外报实的内容

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 为什么现在只剩树苗卡，而不是 placeable 全体都卡
2. 第一责任点到底在：
   - `TreeController`
   - `PlacementManager`
   - `PlacementValidator`
   - 还是 `NavGrid2D`
3. 箱子为什么能作为有效对照组
4. 你这轮拿到的是：
   - `结构证据`
   - `targeted probe`
   - 还是 `真实 live 体验证据`
5. 如果还没彻底修掉，剩下的唯一真 blocker 是什么

### B 技术审计层

至少补：

1. changed_paths
2. 本轮真实 touched 的树苗相关入口 / 方法名
3. 是否触碰 `NavGrid2D.cs`
4. 是否做了 live 验证
5. 当前 own 路径是否 clean
6. blocker_or_checkpoint
7. 一句话摘要

---

## 七、shared-root 现场提醒

当前仓库里还有大量 unrelated dirty / untracked。

所以你这轮必须继续遵守：

1. 只收这把树苗卡顿刀自己的文件
2. 不做顺手 cleanup
3. 不把“仓库脏”偷换成“这轮就先清现场”

---

## 八、thread-state

这轮如果进入真实施工，先跑：

```text
D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1
```

第一次准备 sync 前再跑：

```text
D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1
```

如果这轮停下、让位或不收口，最后跑：

```text
D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1
```

---

## 九、一句话总结

**这轮不要再把“放置卡顿”当成一整包 placeable / 导航问题来修；用户已经把现场压到“只有树苗还卡”，所以你要把第一真实热点钉死在树苗专属运行时链上并收口。**
