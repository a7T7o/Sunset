# 2026-04-03｜本线程续工 prompt v3｜接受 -55 裁定并保持 PARKED，只保留三脚本精确响应权

请先接受 `2026-04-03-工具V1-三脚本contract回执与停工裁定-55.md` 的真实含义：

1. 这刀边界基本守住了
2. 但这刀已经不是 `pure contract-only`
3. `NavGrid2D.cs` 与 `PlayerMovement.cs` 已带入真实 runtime behavior change
4. 所以当前正确动作不是继续清 `Ready-To-Sync` same-root blocker
5. 当前正确状态是：`PARKED`

---

## 一、这轮唯一主线

这轮不再是 cleanup slice，也不是 scene slice。

这轮唯一主线只有一句话：

**保持 `PARKED`，等待父线程 / scene owner 先消化这刀里的 runtime 变化；我只保留三脚本的精确响应权，不再自行扩 scope。**

---

## 二、当前允许与禁止

### 当前允许

1. 保留 exact-own 认领：
   - `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
   - `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
   - `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`
2. 如果父线程 / scene owner 之后精确点名：
   - `contract gap`
   - 或现有 runtime behavior 需要最小 `trim / rollback`
   才允许围绕被点名内容 reopen 一刀最小切片

### 当前禁止

1. 不准继续处理 `Ready-To-Sync` same-root remaining dirty
2. 不准继续做 shared root cleanup
3. 不准碰 `Primary.unity`
4. 不准碰 `Town.unity`
5. 不准碰 `PrimaryTraversalSceneBinder.cs`
6. 不准碰通用工具
7. 不准继续给三脚本追加第二刀、第三刀行为
8. 不准把当前状态再写成“纯 contract support 已完成，只剩治理问题”

---

## 三、下次只有什么情况才允许重新开工

只有满足下面任一条件，才允许我重新从 `PARKED` 转回真实施工：

1. 父线程 / scene owner 明确确认：
   - 某个三脚本 contract gap 仍然存在
   - 并精确点名文件、入口、字段、行为
2. 父线程 / scene owner 明确确认：
   - 这刀里某个 runtime behavior 超出他们需要
   - 需要我做最小 `trim / rollback`

除此之外，不准我自己因为 blocker 难看、dirty 难看、想顺手收口，就擅自 reopen。

---

## 四、重新开工时的硬边界

如果以后被精确点名 reopen，新切片也必须继续守住：

1. 只改被点名的：
   - `NavGrid2D.cs`
   - 或 `PlayerMovement.cs`
   - 或 `SceneTransitionTrigger2D.cs`
2. 不扩到 scene / binder / tool
3. 不碰 foreign dirty
4. 不代清同根 `Navigation` / `Player` 剩余脏改
5. 回执里必须把下面三层分开报：
   - contract 层
   - runtime behavior 层
   - sync / blocker 层

---

## 五、当前阶段定义

当前正确阶段不是：

1. `contract-only slice complete`
2. `cleanup in progress`

当前正确阶段是：

**partial checkpoint：三脚本边界守住，但当前改动已包含 runtime behavior change，必须先停给父线程 / scene owner 消化。**

---

## 六、固定回执格式

如果后续再次被叫醒并需要回执，必须固定按下面格式：

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 哪些属于 contract 层
2. 哪些属于 runtime behavior 层
3. 你现在是不是仍然 `PARKED`
4. 这轮有没有处理 same-root dirty；如果没有，为什么不能由你自己处理
5. 你下一次重新开工的唯一触发条件是什么

### B 技术审计层

至少补：

1. changed_paths
2. `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D` 各自新增了哪些 public contract
3. 各自哪些已经影响 runtime behavior
4. `Ready-To-Sync` blocker exact 口径
5. 当前 own 路径是否 clean
6. blocker_or_checkpoint
7. `Begin-Slice / Ready-To-Sync / Park-Slice`

---

## 七、thread-state

```text
[thread-state 接线要求｜当前状态]
这轮结束时保持：
  PARKED

如果后续被精确点名重新进入真实施工，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1

第一次准备 sync 前，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1

如果再次停下、让位或被 blocker 卡住，继续跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1
```

---

## 八、一句话总结

**我现在不再继续清 same-root dirty，也不再主动扩三脚本；当前正确动作是保持 `PARKED`，等父线程 / scene owner 先消化这刀里的 runtime 变化，只有被精确点名时才做最小响应。**
