# 2026-04-03｜导航父线程续工 prompt v3｜接受工具-V1停车裁定，下一步由你接 Primary traversal 的 scene integration

先接受最新裁定：

1. `2026-04-03-工具V1-三脚本contract回执与停工裁定-55.md` 的性质不是“工具-V1继续自清 blocker 的执行指令”，而是：
   - 工具-V1 这刀边界基本守住
   - 但这刀已经包含 `contract + runtime behavior change`
   - 所以它现在必须先 `PARKED`
2. 因此，工具-V1 当前**不会**继续处理 `Ready-To-Sync` 的 same-root remaining dirty。
3. 从这条裁定往下推，`Primary traversal` 剩余闭环的下一步责任点，已经回到你这个父线程。

---

## 一、这轮唯一主刀

把用户最初要的 `Primary traversal` 现场诉求，真正接成 scene integration / live closure：

1. 玩家不能穿过 `Props`
2. 玩家和 NPC 不能进入 `Water`
3. 玩家不能走出 tilemap 外

这轮不是：

1. 继续修 `PAN crowd runtime`
2. 继续拉 `导航检查V2` 回来做 final pack
3. 继续让工具-V1自清 same-root dirty
4. 镜头边界
5. `Town` 转化

---

## 二、你现在必须接受的分工

### 你来接的面

1. `Primary.unity`
2. `PrimaryTraversalSceneBinder.cs`
3. 与上面 3 个 traversal 问题直接相关的最小 scene wiring / live integration
4. 如确有必要，与这 3 个问题直接相关的最小 navigation glue

### 我这边当前不会动的面

工具-V1（`019d4d18-bb5d-7a71-b621-5d1e2319d778`）现在保持 `PARKED`，不会继续主动碰：

1. `Primary.unity`
2. `Town.unity`
3. `PrimaryTraversalSceneBinder.cs`
4. 通用工具
5. `Ready-To-Sync` same-root cleanup

### 我这边仅保留的最小响应权

我现在只保留下面 3 个脚本的**被动响应权**：

1. `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
2. `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
3. `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`

而且只有在你 fresh 压实“不是 scene wiring / binder 问题，而是脚本 contract 或现有 runtime behavior 需要最小 trim / rollback / 补口”时，我才会按你精确点名的范围 reopen。

---

## 三、你必须先消费的现有脚本基线

不要再把工具-V1当成“纯 contract-only，尚未形成 runtime 影响”的空底座。

你现在应默认接受下面这个事实：

1. `NavGrid2D.cs`
   - 已经提供 world bounds 读取 / 判定 / clamp
   - `TryFindNearestWalkable(...)` 对 bounds 外一点点的点位不再直接失败
2. `PlayerMovement.cs`
   - 已经提供 `HasNavGridReference()` / `TryResolveNavGrid(...)` / `CanOccupyPosition(...)` / `GetNavigationProbePoints(...)`
   - 并且已带入真实移动约束相关的 runtime behavior change
3. `SceneTransitionTrigger2D.cs`
   - 已有统一 target 解析逻辑
   - 但 scene 里有没有放、怎么接，不等于那条 scene 已完成

所以你这轮的第一默认判断必须是：

1. 先假设三脚本底座已存在
2. 优先查 `Primary` 现场 wiring / binder / live integration
3. 只有 fresh 证据证明真是脚本缺口时，才回抛给我

---

## 四、禁止漂移

1. 不准再把 same-root cleanup 推回工具-V1
2. 不准把 crowd / runner / V2 final acceptance 混回这轮
3. 不准为了省事先重写 `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D`
4. 不准把“工具-V1已停车等待消化”写成“工具-V1已经把 traversal 全闭环”
5. 不准把镜头边界混进来

---

## 五、如果你需要我再动，必须怎样点名

只有符合下面全部条件，才允许回抛给工具-V1：

1. 你已经 fresh 核过 `Primary.unity` / `PrimaryTraversalSceneBinder.cs` / 当前 scene wiring
2. 你能明确说明第一责任点**不是** scene integration
3. 你必须精确点名：
   - 哪个文件
   - 哪个入口 / 字段 / 行为
   - 需要的是：
     - `contract gap`
     - 还是 `runtime trim / rollback`
   - 为什么现在确认不是 scene 面问题

如果做不到这 4 条，就不要把球踢回工具-V1。

---

## 六、完成定义

### 形态 A：你这轮真正闭环

同时满足：

1. 玩家朝 `Props` 走时进不去
2. 玩家朝 `Water` 走时进不去
3. 玩家朝 tilemap 外走时被拦住
4. NPC / 自动导航也遵守同一语义
5. 你把 scene wiring / binder / live 证据和脚本基线分开报清楚

### 形态 B：你这轮把 blocker 压到真责任点

同时满足：

1. 你没有把 cleanup 甩回工具-V1
2. 你没有偷改 crowd / runner / V2
3. 你把 blocker 压实到：
   - `Primary.unity`
   - `PrimaryTraversalSceneBinder.cs`
   - 或工具-V1三脚本中的精确 gap

---

## 七、固定回执格式

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 这轮有没有碰：
   - `Primary.unity`
   - `PrimaryTraversalSceneBinder.cs`
   - `NavGrid2D.cs`
   - `PlayerMovement.cs`
   - `SceneTransitionTrigger2D.cs`
2. `Props / Water / tilemap 外边界` 现在各自处于哪一层：
   - `结构成立`
   - `targeted probe 成立`
   - `真实入口体验成立`
3. NPC / 自动导航是否也已被同一语义拦住
4. 如果仍未闭环，第一责任点是：
   - scene wiring
   - binder / live integration
   - 还是脚本 contract / runtime trim gap
5. 你有没有要求工具-V1继续动；如果有，精确点名内容是什么

### B 技术审计层

至少补：

1. changed_paths
2. 是否触碰 scene
3. 是否触碰 binder
4. 是否触碰 `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D`
5. pre_sync_validation
6. 当前 own 路径是否 clean
7. blocker_or_checkpoint
8. `Begin-Slice / Ready-To-Sync / Park-Slice`
9. 一句话摘要

---

## 八、thread-state

```text
[thread-state 接线要求｜本轮强制]
你这轮如果继续真实施工，先跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1

第一次准备 sync 前，必须跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1

如果这轮先停、卡住、让位或不准备收口，改跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1
```

---

## 九、一句话总结

**工具-V1按 `-55` 保持 `PARKED`，不再自清 same-root blocker；从这一步起，`Primary traversal` 的 scene integration / live closure 由你这个父线程接盘，只有在你精确压实脚本缺口后，工具-V1才按点名最小响应。**
