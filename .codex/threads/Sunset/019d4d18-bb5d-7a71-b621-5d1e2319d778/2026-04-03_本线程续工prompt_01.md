# 2026-04-03｜本线程续工 prompt｜只保留 3 个脚本契约与 ownership audit，不再碰 scene / binder / tool

先给这轮自我裁定：

1. 这条线程不是通用工具 owner。
2. 这条线程不是 `Primary.unity / Town.unity` scene writer。
3. 这条线程这轮也不是 `Primary traversal` 的 scene integration owner。
4. 这条线程现在只保留：
   - 3 个脚本契约 owner
   - 分账说明
   - 对导航线的 handoff / contract support

---

## 一、当前已接受的边界

### 不再碰

1. `Assets/000_Scenes/Primary.unity`
2. `Assets/000_Scenes/Town.unity`
3. `Assets/Editor/ScenePartialSyncTool.cs`
4. `Assets/YYY_Tests/Editor/ScenePartialSyncToolTests.cs`
5. `Assets/Editor/Tool_002_BatchHierarchy.cs`
6. `Assets/Editor/StaticObjectOrderAutoCalibrator.cs`
7. `Assets/Editor/Story/PrimaryTraversalSceneBinder.cs`

### 当前 exact-own 只剩

1. `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
2. `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
3. `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`

---

## 二、这条线程前面实际做过什么

只能这样报实，不准再夸大：

1. 我做过 `Primary traversal` 的脚本基础层尝试
2. 我做过：
   - obstacle source / bounds / foot probe 方向的一些脚本侧调整
   - `SceneTransitionTrigger2D` 基础脚本
3. 我也一度错误卷入过：
   - scene live-apply
   - binder
   - 通用工具支撑面
4. 这些漂移后来已被用户明确收边

所以现在对外唯一能 claim 的，只能是：

1. 3 个脚本的契约说明
2. 我不再 own scene/binder/tool
3. 我不能再把“脚本侧成立”说成“场景已完成”

---

## 三、你这轮唯一主刀

这轮只做下面这一件事：

把这 3 个脚本收成“可被别的 scene owner / navigation owner 拿去接 `Primary / Town` 场景”的基础契约版本，并在需要时只回答契约问题。

说人话就是：

1. 只保留脚本层
2. 不再偷带 scene wiring
3. 不再偷带 binder
4. 不再偷带通用工具

---

## 四、这轮允许的 scope

### 允许

1. 继续澄清 `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D` 的契约边界
2. 如果导航线或 scene owner 明确指出：
   - 这 3 个脚本里哪一条契约仍然不够
   - 缺的到底是哪一个字段 / 入口 / 行为
   才允许回到脚本侧做最小补口
3. 继续维护当前线程自己的 prompt / memory / ownership audit

### 禁止

1. 不准再碰 `Primary.unity`
2. 不准再碰 `Town.unity`
3. 不准再碰 `PrimaryTraversalSceneBinder.cs`
4. 不准再碰 `ScenePartialSyncTool`
5. 不准再碰 `Tool_002_BatchHierarchy / StaticObjectOrderAutoCalibrator`
6. 不准顺手扩成：
   - 镜头边界
   - UI
   - Town 转化
   - scene live-apply
   - 综合修复包

---

## 五、对外统一口径

以后这条线程只准这样说：

### `NavGrid2D`

1. 它负责提供 navigation grid / obstacle source 契约
2. scene 里到底拖了哪些 obstacle tilemaps / colliders，不归这条线程 claim 完成

### `PlayerMovement`

1. 它负责提供与 `NavGrid2D` 对接的移动拦截契约
2. 但场景里真实 movement / tilemap / collider 接线是否生效，不归这条线程 claim 完成

### `SceneTransitionTrigger2D`

1. 它负责提供 trigger + target scene 的基础脚本契约
2. 但 scene owner 有没有把 trigger 真正放进场景、有没有在 Inspector 接好，不归这条线程 claim 完成

---

## 六、主线程之后怎么把这些脚本接回场景

这里只准交契约，不准偷写 scene 已完成：

1. `NavGrid2D`
   - scene owner 手动拖显式 obstacle tilemaps / colliders
   - 或显式开启 auto-detection
   - 但是否命中正确 tilemap，必须由 scene / live owner 负责验证
2. `PlayerMovement`
   - scene owner 显式拖 `NavGrid2D`
   - 或允许自动找唯一 NavGrid
   - 但真实移动是否被拦住，必须由 scene / live owner 负责验证
3. `SceneTransitionTrigger2D`
   - scene owner 自己放 trigger collider
   - 在 Inspector 里拖 `SceneAsset`
   - 但真实切场是否成立，必须由 scene / live owner 负责验证

---

## 七、completion 定义

这轮对我自己来说，只有下面两种结果算完成：

### 形态 A：纯 contract support

同时满足：

1. 我没有再碰 scene / binder / tool
2. 我已经把 3 个脚本的职责边界说清
3. 我已经把“我做的”和“导航做的”分账说清
4. 我已经把对导航线的 handoff 交清

### 形态 B：被点名后的最小脚本补口

同时满足：

1. 是导航线或 scene owner 明确点名我这 3 个脚本里的 contract gap
2. 我只补这 3 个脚本
3. 我仍然不 claim `Primary / Town` 场景完成

---

## 八、固定回执格式

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 这轮有没有碰 scene
2. 这轮有没有碰 binder
3. 这轮有没有碰通用工具
4. 这轮如果没碰，为什么没碰
5. 这轮如果后续还要继续，唯一会因为什么再回到代码修改

### B 技术审计层

至少回答：

1. changed_paths
2. 是否触碰：
   - `NavGrid2D.cs`
   - `PlayerMovement.cs`
   - `SceneTransitionTrigger2D.cs`
3. 是否触碰 scene / binder / tool
4. pre_sync_validation
5. 当前 own 路径是否 clean
6. blocker_or_checkpoint
7. 一句话摘要

---

## 九、当前仍未完成的东西，必须老实写明

1. `Primary` 场景没有被我这条线 claim 完成
2. `Town` 场景没有被我这条线 claim 完成
3. 镜头边界不在我这轮 own 范围
4. `Props / Water / tilemap 外边界` 的真实 scene integration 不在我这轮 own 范围
5. 我这条线现在成立的是脚本侧 contract，不是场景终态

---

## 十、thread-state

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
