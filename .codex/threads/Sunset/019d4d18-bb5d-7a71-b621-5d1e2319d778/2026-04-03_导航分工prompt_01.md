# 2026-04-03｜导航分工 prompt｜把 Primary traversal 剩余闭环从 PAN crowd 主线里拆出来

先给这轮裁定：

1. 这次不是让你继续泛做“整个导航系统”。
2. 这次也不是让你接 `runner/menu final pack` 那条并行验收线。
3. 这次是把用户最初那组 `Primary` 现场诉求里，仍未闭环、且本质属于 traversal / blocking / navigation scene integration 的部分，从你们现在的 `PAN crowd runtime` 主线里单独拆出来，作为一个新的可见切片接盘。
4. 这份 prompt 应由父线程 `导航检查` 接单，不是直接发给 `导航检查V2`。

---

## 一、用户最初要的是什么

用户最初在 `Primary` 这边要的是 5 件事：

1. `Props` tilemap 上的内容都不能穿过
2. `Water` 不能靠近，玩家和 NPC 都不能走进 water
3. 玩家也不能走到 tilemap 外
4. 镜头不能拍到场景外
5. 有一个可调大小的空物体触发切场，目标 scene 由 Inspector 拖入

其中当前明确与你们导航线最相关、且仍未闭环的是前 3 件：

1. `Props` 阻挡
2. `Water` 阻挡
3. `tilemap` 外边界阻挡

第 4 件镜头边界不在这次导航分工 prompt 里。
第 5 件切场触发器脚本契约已在别线做过基础版，但 scene wiring 不能再默认算那条线已完成。

---

## 二、这次分账先钉死

### 1. `019d4d18-bb5d-7a71-b621-5d1e2319d778` 这条线前面实际做过什么

它前面实际做过的是：

1. 在脚本层给过 `Primary traversal` 的基础逻辑尝试：
   - `NavGrid2D.cs`
   - `PlayerMovement.cs`
   - `SceneTransitionTrigger2D.cs`
2. 还一度错误卷入过：
   - `PrimaryTraversalSceneBinder.cs`
   - `Primary.unity / Town.unity` 的 scene live-apply 叙事
   - 通用工具 / Tool_002 一类支撑面
3. 之后已经被用户明确收边：
   - 不再碰 `Primary.unity`
   - 不再碰 `Town.unity`
   - 不再碰 `PrimaryTraversalSceneBinder.cs`
   - 不再碰 `ScenePartialSyncTool / Tool_002 / StaticObjectOrderAutoCalibrator`
4. 现在这条线只保留 3 个脚本契约 owner：
   - `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
   - `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
   - `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`

### 2. 你们导航线当前真实在做什么

当前 thread-state 现场显示：

1. 父线程 `导航检查`
   - `status=ACTIVE`
   - `current_slice=导航大刀-PAN crowd runtime 主刀与并行验收分工`
   - 当前 active own 核心在：
     - `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`
2. `导航检查V2`
   - `status=PARKED`
   - 当前 own 只剩：
     - `NavigationLiveValidationRunner.cs`
     - `NavigationLiveValidationMenu.cs`
   - 还被 `Assets/YYY_Scripts/Service/Navigation` 同根 foreign dirty 卡着，当前不适合作为这次 Primary traversal 接盘入口

所以：

1. `PAN crowd runtime` 线是你们父线程的当前真主线
2. `runner/menu final acceptance pack` 是你们并行验收线
3. 这次 `Primary traversal` 剩余闭环，不准再混报进上面两条里

---

## 三、你这轮唯一主刀

把 `Primary` 里下面这 3 个 still-open 问题，收成一个独立可见切片：

1. 玩家不能穿过 `Props`
2. 玩家和 NPC 不能进入 `Water`
3. 玩家不能走出 tilemap 外

这里的完成定义不是“脚本上看起来像有逻辑了”，而是：

1. 场景里真实接线成立
2. 玩家真实入口移动被拦住
3. NPC / 自动导航也遵守同一阻挡语义
4. 回执里能把“结构成立”和“真实入口体验成立”分开报实

---

## 四、允许的 scope

你这轮允许做的是：

1. 你们导航线内部为了闭环这 3 个问题而必需的 scene integration / navigation runtime 收口
2. 围绕 `Primary traversal` 的 fresh 取证
3. 如确有必要，自行在你们线内拆出新的子切片或接盘人

你这轮禁止做的是：

1. 不准把这轮又写成“继续优化整个导航”
2. 不准把这轮和当前 `PAN crowd runtime` 主线混成一个大锅
3. 不准把这轮和 `V2 runner/menu final acceptance pack` 混成一个大锅
4. 不准默认 `019d...` 那条线还会继续替你们写 scene/binder
5. 不准去改：
   - `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
   - `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
   - `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`
   除非你已经明确发现脚本契约缺口，并在回执里点名“需要 019d 这条线补哪一条契约”

---

## 五、你可直接依赖的现有脚本契约基线

目前可以视为 `019d...` 这条线已经给出的脚本侧基线只有这些：

### `NavGrid2D.cs`

1. 可以接显式 obstacle tilemaps / colliders
2. 可以打开或关闭 obstacle tilemap auto-detection
3. 默认不再偷偷按 `Primary/Town` 硬编码 scene 名字帮你猜 wiring

### `PlayerMovement.cs`

1. 有 `NavGrid2D` 的引用入口
2. 有脚底探针相关参数
3. 没接 `NavGrid2D` 时应明确报 warning，而不是静默当作已接好

### `SceneTransitionTrigger2D.cs`

1. 已有基础 trigger + target scene path / name 契约
2. 但 scene 里有没有放、怎么放、Inspector 有没有接好，不算那条线已完成

所以你这轮如果卡住，先分清两种情况：

1. 是 scene wiring / runtime integration 没闭环
2. 还是这 3 个脚本里真的还差契约

只有第 2 种，才回头点名让 `019d...` 这条线补脚本。

---

## 六、completion 定义

这轮只有下面这两种结果算合法完成：

### 形态 A：Primary traversal scene integration 已闭环

同时满足：

1. 你把这轮作为独立切片报实，没有继续混报 PAN crowd 主线
2. 玩家朝 `Props` 走时，进不去
3. 玩家朝 `Water` 走时，进不去
4. 玩家朝 tilemap 外走时，被拦住
5. NPC / 自动导航也遵守同一阻挡语义
6. 你明确写清：
   - 这轮用了哪些 scene wiring
   - 哪些是结构成立
   - 哪些是真实入口体验已验证

### 形态 B：你已经把 blocker 压实

同时满足：

1. 你没有越界去改 `019d...` own 的 3 个脚本
2. 你没有再把 scene/binder 责任偷回 `019d...`
3. 你已经把真正 blocker 压到 file / contract / scene wiring / live evidence 级别
4. 你明确写清：
   - 现在差的是 scene wiring、脚本契约，还是 live 入口验证
   - 下一步到底该由谁接

---

## 七、固定回执要求

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 这轮有没有把 `Primary traversal` 从 `PAN crowd runtime` 主线里单独拆出来
2. 这轮有没有碰 `NavGrid2D.cs / PlayerMovement.cs / SceneTransitionTrigger2D.cs`
3. 如果碰了，为什么这不是越界
4. 现在 `Props / Water / tilemap 外边界` 分别处于：
   - 结构成立
   - targeted probe 成立
   - 真实入口体验成立
   的哪一层
5. NPC / 自动导航是否也已被同一语义拦住

### B 技术审计层

至少回答：

1. changed_paths
2. 是否触碰 scene
3. 是否触碰 `PlayerAutoNavigator.cs`
4. 是否触碰 `NavGrid2D.cs / PlayerMovement.cs / SceneTransitionTrigger2D.cs`
5. 这轮 own 路径是否 clean
6. pre_sync_validation
7. blocker_or_checkpoint
8. 一句话摘要

---

## 八、thread-state

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
