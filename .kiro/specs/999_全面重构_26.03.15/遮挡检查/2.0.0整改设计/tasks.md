# 2.0.0 整改任务

- [x] 建立 `2.0.0整改设计` 工作目录
- [x] 写入 `requirements.md`
- [x] 写入 `design.md`
- [x] 写入 `tasks.md`
- [x] 写入 `memory.md`
- [x] 完成一个最小 checkpoint：`BatchAddOcclusionComponents.cs` 与当前 `OcclusionTransparency` 定义对齐
- [ ] 比较 `TreeController` 是否应显式联动父/子两份 `OcclusionTransparency`
- [ ] 设计“单树双组件是否误触树林保底逻辑”的最小复现实验
- [ ] 评估是否优先补 `OcclusionSystemTests.cs`
