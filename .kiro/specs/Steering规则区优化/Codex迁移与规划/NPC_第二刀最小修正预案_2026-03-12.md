# NPC 第二刀最小修正预案（2026-03-12）

## 1. 对象

- 线程名：`NPC`
- 线程 id：`019cda74-411d-7821-9f94-7e14e36e4e16`
- 第二刀方案：**方案 C**
- 主刀对象：rollout 恢复字段
- 必须联动对象：`threads.git_sha`

## 2. 改前值

### 2.1 `threads` 表

- `cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`
- `rollout_path = C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`

### 2.2 rollout 文件

- `session_meta.cwd = D:\Unity\Unity_learning\Sunset`
- `session_meta.git.branch = main`
- `session_meta.git.commit_hash = 3b45da720500f3df9945a67f06acf69d9fba76ab`
- 全部 `turn_context.cwd` 均为 `D:\Unity\Unity_learning\Sunset`

## 3. 期望改后值

### 3.1 `threads` 表

- `git_sha = 1f068ed1731316a07cb471b90bce8e8af7534277`

### 3.2 rollout 文件

- `session_meta.cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `session_meta.git.branch = codex/npc-generator-pipeline`
- `session_meta.git.commit_hash = 1f068ed1731316a07cb471b90bce8e8af7534277`
- 全部 `turn_context.cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`

## 4. 为什么这刀排第一

- 首刀已证伪“单改 `git_branch` 就够”；
- 当前线程表里的 `cwd` 和 `git_branch` 已经正确，但客户端点击线程仍回落；
- rollout 文件仍完整保存根仓库恢复语义，这比标题脏值和索引残留更直接地解释“点击线程为何回到 `Sunset/main`”；
- `threads.git_sha` 若不联动修正，会继续保留旧提交锚点，因此不能只动 rollout。

## 5. 执行前备份

1. 备份 `C:\Users\aTo\.codex\state_5.sqlite`
2. 复制原始 rollout 文件到同目录带时间戳备份名
3. 再导出 `NPC` 当前整行状态
4. 再记录 `NPC` 真实 worktree 分支与 HEAD

## 6. 写入顺序

1. 复制 rollout 原文件
2. 修改 rollout `session_meta.cwd`
3. 修改 rollout `session_meta.git.branch`
4. 修改 rollout `session_meta.git.commit_hash`
5. 批量改 rollout 全部 `turn_context.cwd`
6. 修改 `threads.git_sha`
7. 回读校验写入结果

## 7. 验收步骤

1. 不重启直接打开 `NPC`
2. 记录是否仍回落到 `Sunset/main`
3. 记录是否仍误触发根仓库切 `codex/npc-generator-pipeline`
4. 记录是否仍报 `branch already used by worktree`
5. 重启 `Codex` 后再打开 `NPC`
6. 复记上述四项结果
7. 对照 `T74` 判断是否达到用户可接受态

## 8. 失败回滚

1. 用 rollout 备份恢复原 rollout 文件
2. 用单线程导出值恢复 `threads.git_sha`
3. 回读 `NPC` 线程行与 rollout 关键字段
4. 重新核对是否恢复到第二刀前现场

## 9. 本刀边界

- 不改 `title`
- 不改 `first_user_message`
- 不改 `session_index.jsonl`
- 不改 `farm`
- 不改 `.codex-global-state.json`

## 10. 推广条件

只有 `NPC` 第二刀出现下列任一结果，才允许把同类顺序推广到 `farm`：

1. 点击线程后稳定留在正确 worktree
2. 即便未完全通过，也明确证明 rollout 恢复层 + `git_sha` 联动比其他候选更接近根因

若第二刀失败但成功回滚，也能为 `farm` 排除一条错误方向；但在 `NPC` 未执行第二刀前，不推广到 `farm`。
