# 当前仓库 Git 自动同步与治理现状说明（2026-03-11）

## 1. 文档目的

- 本文档用于向 `Sunset` 内其他工作区说明当前仓库在 Git 治理、自动同步、分支状态和未收口事项上的真实现场。
- 它的目标不是重复 `.kiro/steering/git-safety-baseline.md` 的规则正文，而是说明“现在仓库已经做到哪一步、还差哪一步、后续线程接手时应如何理解当前状态”。
- 本文档写入位置在 `Steering规则区优化 / Codex迁移与规划`，但内容面向整个仓库。

## 2. 当前 Git 现场总览

### 2.1 分支与远端状态

- 当前本地工作分支：`main`
- 当前本地 `HEAD`：`3b45da72`（提交消息：`2026.03.11-01`）
- 当前本地 `main` 相对 `origin/main`：`behind 3`
- 当前远端主线：`origin/main -> 7f20b29e`
- 当前远端治理/业务混合分支：`origin/codex/npc-generator-pipeline -> 7f20b29e`
- 当前本地额外分支：
  - `codex/npc-generator-pipeline -> 7f20b29e`
  - `worktree-agent-a2df3da0 -> ac20107d`

### 2.2 这代表什么

- 远端主线 `origin/main` 已经包含本轮 Git 自动同步治理的最新结果。
- 但**当前本地 `main` 还没有快进到最新远端主线**，因此本地 `main` 是一个“规则已补第一步、但未跟上远端后续三次治理提交”的旧状态。
- 当前如果有人继续直接在本地 `main` 上开做，而不先同步分支，就会基于过时基线继续工作。

## 3. 本轮已完成的关键治理工作

## 3.1 Git 基线制度已落地

以下仓库级文件已经建立或更新：

- `.gitattributes`
- `.gitignore`
- `.kiro/steering/git-safety-baseline.md`
- `.kiro/hooks/git-preflight.kiro.hook`
- `.kiro/hooks/git-quick-commit.kiro.hook`
- `AGENTS.md`

这些文件已经把以下口径固定下来：

- 什么时候必须建 `codex/` 任务分支
- 什么情况下 `main` 只允许治理性改动
- Git preflight 必须检查什么
- checkpoint / rollback 应如何汇报
- `.claude` 本地噪音如何处理
- `.gitattributes` 如何稳定 Unity 文本 / 二进制资产

## 3.2 自动 Git 同步脚本已落地并验证

已新增统一入口脚本：

- `scripts/git-safe-sync.ps1`

该脚本支持三种动作：

- `preflight`
- `ensure-branch`
- `sync`

已经验证通过的能力包括：

1. 能按白名单列出“本轮允许同步的文件”和“必须保留原位的其他 dirty”。
2. 能在 dirty 工作树里拒绝偷切任务分支。
3. 能只对白名单路径执行 `git add` / `commit` / `push`，而不误带其他主线 dirty。
4. 能在当前分支已有关联远端时直接 `push`，无关联时自动 `push -u`。

## 3.3 远端已实际同步，不只是本地设计

本轮治理并不只是“写了规则和脚本”，而是已经实际产出并上传了以下提交：

| 提交 | hash | 主要内容 | 当前所在位置 |
|------|------|----------|-------------|
| `2026.03.11-01` | `3b45da72` | Git 基线第一阶段落地 | 本地 `main` |
| `2026.03.11-02` | `77f7eec5` | 自动同步脚本与规则接入 | `origin/main`、`origin/codex/npc-generator-pipeline` |
| `2026.03.11-03` | `5d14b8ca` | 结果记录与状态补记 | `origin/main`、`origin/codex/npc-generator-pipeline` |
| `2026.03.11-04` | `7f20b29e` | 最终状态对齐与最新记忆同步 | `origin/main`、`origin/codex/npc-generator-pipeline` |

换句话说：

- 自动 Git 同步已经经过真实远端推送验证；
- 最新有效治理基线应以 `7f20b29e` 为准，而不是以本地 `main` 的 `3b45da72` 为准。

## 3.4 `.claude` 本地噪音已经从根仓库治理

当前已完成：

- `.claude/settings.local.json` 已不再由根仓库继续跟踪
- `.claude/worktrees/agent-a2df3da0` 已不再按根仓库正式受控结构处理
- `.gitignore` 已为这类本地噪音建立过滤策略

注意：

- 这不代表本地目录被删除；
- 只代表它们不该再持续污染 `Sunset` 根仓库的正常状态判断。

## 3.5 本轮新增的跨工作区记录与同步动作

- 已新建本文档，专门承担“给其他工作区说明当前 Git 现场”的职责，不再要求后来者从零散 `memory` 片段里自行拼现场。
- 已更新 `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\tasks.md` 的九阶段看板，使“写说明文档”和“把说明文档与多层记忆安全同步出去”成为显式任务，而不是口头动作。
- 已把本轮结论回写到治理子工作区、治理父工作区、农田受影响工作区，以及当前 Codex 线程记忆，确保之后无论从治理线还是业务线接手，都能读到同一套口径。
- 为了避开当前 dirty 的本地 `main`，本轮采用干净临时 worktree `C:\Users\aTo\AppData\Local\Temp\sunset-status-sync-d380b3e638d04631bf802aabe7814ae1`（分支 `codex/npc-generator-pipeline`，基线 `7f20b29e`）执行白名单同步，只上传本轮治理文档与记忆，不误带其他主线改动。
- 如果你当前仍停在本地 `main = 3b45da72`，看不到 `scripts/git-safe-sync.ps1`、本文档或 `about一致性巡检清单.md`，不是这些文件不存在，而是你的本地 `main` 还没快进到最新远端基线。

## 4. 当前仍然存在的真实问题

以下内容**仍未完成**，也是后续线程必须继续面对的现实：

## 4.1 本地 `main` 还没同步到最新远端主线

当前状态是：

- 本地 `main = 3b45da72`
- 远端 `origin/main = 7f20b29e`
- 差距：本地 `main` 落后 3 个提交

这意味着：

- 现在不能把“本地 `main` 的现场”误当成“整个仓库的最新 Git 治理状态”；
- 任何准备回到 `main` 的线程，都应先在安全前提下同步本地 `main`。

## 4.2 当前工作树仍然是 dirty 状态

当前尚未收口的改动仍跨多条主线，主要包括：

### A. 线程记忆与文档线

- `.codex/threads/OpenClaw/部署与配置龙虾V2/memory_3.md`
- `.codex/threads/Sunset/spring-day1/memory_0.md`
- `.codex/threads/Sunset/项目文档总览/memory_0.md`
- `.kiro/about/00_项目总览与阅读地图.md`
- `.kiro/about/01_系统架构与代码全景.md`
- `.kiro/about/02_当前状态、差异与接手指南.md`
- `.kiro/specs/900_开篇/spring-day1-implementation/memory.md`
- `.kiro/specs/900_开篇/spring-day1-implementation/002-初步搭建/memory.md`
- `.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/`

### B. NPC / 其他新工作区

- `.codex/threads/Sunset/NPC/`
- `.kiro/specs/002_After_26.1.21/`

### C. 当前本地 `main` 仍会看到、但其实已属于远端新治理基线的文件

- `.kiro/specs/Steering规则区优化/Codex迁移与规划/about一致性巡检清单.md`
- `.kiro/specs/Steering规则区优化/Codex迁移与规划/当前仓库Git自动同步与治理现状说明_2026-03-11.md`
- `scripts/git-safe-sync.ps1`

### D. 实现与资源改动

- `Assets/000_Scenes/Primary.unity`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SoftPixel SDF.asset`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
- `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
- `Assets/YYY_Scripts/Service/TimeManager.cs`
- `Assets/YYY_Scripts/Story/Managers/DialogueManager.cs`
- `Assets/YYY_Scripts/Story/Managers/DialogueValidationBootstrap.cs`
- `Assets/YYY_Scripts/Story/UI/DialogueUI.cs`
- `Assets/111_Data/Story/`
- `Assets/Editor/NPCPrefabGeneratorTool.cs`
- `Assets/Editor/Story/DialogueDebugMenu.cs`
- `Assets/Screenshots/`
- `Assets/YYY_Scripts/Anim/NPC/`
- `Assets/YYY_Scripts/Controller/NPC/`
- `Assets/YYY_Scripts/Story/Interaction/`

这些改动说明：

- 当前仓库并没有达到“干净到可以随时为新任务切分支”的程度；
- 只是**Git 自动同步已经可以在这类脏现场里，用白名单安全上传当前治理文件，而不误吃其他改动**。

## 4.3 `10.2.2补丁002` 仍然不能直接进入实现

原因没有变，只是现在表达得更精确：

1. Git 基线和自动同步能力已经具备；
2. 但当前工作树仍混有多条其他主线的 dirty；
3. 农田线还没有从干净基线创建 `codex/farm-10.2.2-patch002`；
4. 因此现在还不能安全开始农田真实实现。

## 5. 后续线程应该如何理解并接手

## 5.1 如果你接的是治理/规则任务

- 优先读取本文件；
- 再读 `.kiro/steering/git-safety-baseline.md`；
- 再读 `AGENTS.md` 中的 Git 自动同步条款；
- 收尾时按“多层记忆 → `git-safe-sync.ps1`”顺序执行。

## 5.2 如果你接的是业务实现任务

- 不要看到远端主线已更新，就误以为当前工作树也干净了；
- 先做 Git preflight；
- 确认当前分支、基线 hash 和 remaining dirty；
- 只有在干净基线上，才创建自己的 `codex/` 任务分支。

## 5.3 如果你准备回到 `main`

- 先确认当前本地工作树是否还有未收口改动；
- 在安全前提下把本地 `main` 快进到 `origin/main`；
- 不要基于本地 `3b45da72` 的旧 `main` 继续推进新工作。

## 6. 当前明确未完成事项清单

截至本说明写入时，仍未完成的事项包括：

1. **本地 `main` 与远端 `main` 的同步**
2. **当前多条主线 dirty 的拆分与收口**
3. **农田 `10.2.2补丁002` 的干净任务分支创建**
4. **农田线正式进入实现前的 preflight**

## 7. 一句话结论

当前 `Sunset` 的状态不是“Git 还没治理好”，而是：

- **Git 规则、Hook、自动同步脚本、远端上传闭环都已经到位；**
- **但仓库现场仍然脏，多条主线尚未收口，所以不能把“自动同步已建好”误读成“所有业务线都已经可以直接开做”。**

## 2026-03-11 现状更新（仓库已整理到可继续开发）

本轮已完成从“脏旧 main”到“干净任务分支”的整理，当前可以继续后续开发。

### 本轮实际完成
- 已先把原现场全部脏改动备份到：`D:\Unity\Unity_learning\Sunset_backups\git_dirty_backup_20260311_160659`
- 备份内包含：脏文件副本、`tracked.diff`、`git_status_short.txt`、`manifest.txt`
- 已将本地旧 `main` 直接对齐到 `origin/main`
- 已把治理提交 `19ffe247` 以 cherry-pick 方式补到 `main`，形成新提交：`5187ca09`
- 已把更新后的 `main` 推送到远端，当前 `origin/main = 5187ca09`
- 已从干净基线创建并推送任务分支：`codex/farm-10.2.2-patch002`

### 当前仓库状态
- 当前分支：`codex/farm-10.2.2-patch002`
- 当前 HEAD：`5187ca09`
- 当前工作树：干净
- `origin/main`：`5187ca09`
- `origin/codex/farm-10.2.2-patch002`：已建立并跟踪

### 这意味着什么
- Git 安全基线不再只是“规则已落地”，而是“现场也已收口”
- 农田 `10.2.2补丁002` 现在已经具备进入实现前的干净入口
- 若要恢复本轮整理前的旧脏现场，不要回仓库硬找，直接去上面的备份目录取回

### 仍未完成的事
- 本轮没有恢复旧脏现场里的业务改动，只做了备份与仓库基线整理
- 如果后续确认旧脏现场里的某些代码/场景改动仍有价值，需要从备份目录按需挑回，而不是整包回灌

### 2026-03-11 最终补记（工作树已彻底干净）
- 在首次同步完成后，又发现一个新的未跟踪线程目录：`.codex/threads/Sunset/备份脚本/`
- 该目录不属于当前任务分支，已先补充备份到 `D:\Unity\Unity_learning\Sunset_backups\git_dirty_backup_20260311_160659\late_extra\备份脚本`
- 随后已从仓库工作树移除该未跟踪目录
- 当前最终状态：`git status --short --branch` 仅显示 `codex/farm-10.2.2-patch002...origin/codex/farm-10.2.2-patch002`，无任何 dirty 项
