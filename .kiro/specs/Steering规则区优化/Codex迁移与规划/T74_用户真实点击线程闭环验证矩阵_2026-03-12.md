# T74 用户真实点击线程闭环验证矩阵（2026-03-12）

## 1. 目的

本矩阵只记录用户真实点击线程后的行为，不再把只读回查误写成验收通过。

它当前服务两个问题：

- 为什么线程点击后仍回到 `Sunset/main`；
- 第二刀完成后，应该按什么标准判断 `NPC` 是否真正恢复。

## 2. 样本基线

| 样本 | 真实现场 | 真实分支 | 当前线程表关键状态 | 当前结论 |
|---|---|---|---|---|
| `NPC` | `D:\Unity\Unity_learning\Sunset_worktrees\NPC` | `codex/npc-generator-pipeline` | `cwd` 正确，`git_branch` 已修正，`git_sha` 仍旧，rollout 仍是根仓库语义 | 首刀失败样本；第二刀对象已明确 |
| `farm-10.2.2-patch002` | `D:\Unity\Unity_learning\Sunset_worktrees\farm-10.2.2-patch002` | `codex/farm-10.2.2-patch002` | `cwd` 已对，`git_branch` 仍旧 | 高价值对照样本，暂不先动 |
| 治理线程 | `D:\Unity\Unity_learning\Sunset` | `main` | 根仓库语义 | 根仓库正常样本 |

## 3. 验证矩阵

| 样本 | 场景 | UI 项目组 | 点击后目录语境 | 点击后分支语境 | 是否回落到 `Sunset/main` | 是否误触发根仓库切功能分支 | 是否报 `branch already used by worktree` | 当前状态 |
|---|---|---|---|---|---|---|---|---|
| `NPC` | 不重启直接打开 | 第二刀已执行，待用户复验 | 本地状态链已对齐到 `D:\Unity\Unity_learning\Sunset_worktrees\NPC`，真实点击结果待回填 | 本地状态链已对齐到 `codex/npc-generator-pipeline@1f068ed1731316a07cb471b90bce8e8af7534277`，真实点击结果待回填 | 待用户复验 | 待用户复验 | 待用户复验 | 第二刀已执行，待用户复验 |
| `NPC` | 重启后再打开 | 第二刀已执行，待用户复验 | 本地状态链已对齐到 `D:\Unity\Unity_learning\Sunset_worktrees\NPC`，真实点击结果待回填 | 本地状态链已对齐到 `codex/npc-generator-pipeline@1f068ed1731316a07cb471b90bce8e8af7534277`，真实点击结果待回填 | 待用户复验 | 待用户复验 | 待用户复验 | 第二刀已执行，待用户复验 |
| `NPC` | 切 provider 后打开 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未验证 |
| `farm-10.2.2-patch002` | 不重启直接打开 | 已知会漂回根仓库项目组语义 | 掉回 `Sunset/main` | 根仓库误尝试切 `codex/farm-10.2.2-patch002` | 是 | 是 | 是 | 失败 |
| `farm-10.2.2-patch002` | 重启后再打开 | 可阶段性回到正确项目文件夹 | 稳定承接未成立 | 仍有回落风险 | 是 | 是 | 风险仍在 | 失败 |
| `farm-10.2.2-patch002` | 切 provider 后打开 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未补充新证据 | 未验证 |
| 治理线程 | 不重启直接打开 | `Sunset` 根仓库项目组 | `D:\Unity\Unity_learning\Sunset` | `main` | 否 | 否 | 否 | 正常样本 |

## 4. `NPC` 当前失败层级

- Git / worktree 本体：正常
- 线程表首刀单字段修正：已成立，但已被第二刀覆盖
- 当前最可能失败层：已执行到 rollout 恢复层修正，等待用户验证是否穿透点击承接
- 当前必须联动字段：`threads.git_sha` 已完成写入
- 当前暂不先动层：`title / first_user_message / session_index.jsonl`

## 5. 用户可接受态

只有满足以下条件，`NPC` 才能从失败样本转为可验收样本：

1. 点开线程直接回到 `D:\Unity\Unity_learning\Sunset_worktrees\NPC`
2. 不再回落到 `Sunset/main`
3. 不再误触发根仓库切 `codex/npc-generator-pipeline`
4. 不再出现 `branch already used by worktree`
5. 重启与不重启两条路径都成立

## 6. 本轮结论

- `NPC` 首刀失败已经成立，且第二刀现已真实执行；
- `T74` 当前不再停留在“候选分析”，而是进入“第二刀已执行，待用户按两条路径复验”；
- `NPC` 当前本地状态链已对齐为：rollout 恢复层 + `threads.git_sha`；
- `farm` 继续冻结，等待 `NPC` 第二刀复验结果后再决定是否进入同顺序第二刀。

## 7. A/B 双层验收补记（2026-03-12）

### 7.1 判定口径

- A 层：重启 `Codex` 后线程是否回到正确分组、正确 worktree、正确分支语义。
- B 层：不重启时线程列表与项目分组是否会在当前会话内自动归位，并在点击后直接进入正确 worktree。

### 7.2 最新矩阵

| 样本 | A 层状态 | A 层证据 | B 层状态 | B 层证据 | 当前阶段 |
|---|---|---|---|---|---|
| `NPC` | 通过 | 用户真实重启后复验：已回到正确分组，重新打开不再回落到 `Sunset` | 未通过 | 用户先前真实点击现象与当前缺少“不重启自动归位”证据 | A 层通过样本，B 层阻断样本 |
| `farm-10.2.2-patch002` | 待复验 | 第二刀已执行，本地回读已对齐到 `farm` worktree/branch/HEAD | 未通过 | 尚无会话内自动归位证据 | A 层待用户复验，B 层未通过 |
| 治理线程 | 通过 | 根仓库线程本就应留在 `D:\Unity\Unity_learning\Sunset@main` | 不适用 | 不作为热刷新目标样本 | 正常基线 |

### 7.3 `farm` 当前用户复验要求

- 路径 A：不重启直接打开 `farm-10.2.2-patch002`
  - 看 `farm` 分组下是否仍显示“无线程”；
  - 看 `Sunset` 下是否仍错误挂着 `farm`；
  - 看点击后是否仍回到 `Sunset/main`；
  - 看是否仍误触发根仓库切 `codex/farm-10.2.2-patch002`；
  - 看是否仍报 `branch already used by worktree`。
- 路径 B：重启 `Codex` 后再重复同样步骤。
- 通过标准：
  - A 路径至少要能证明是否还存在热刷新阻断；
  - B 路径若能稳定落到 `D:\Unity\Unity_learning\Sunset_worktrees\farm-10.2.2-patch002`，则 `farm` A 层通过。

## 8. P/H/U 三层验收补记（2026-03-12）

### 8.1 正式口径

- `P` 层：重启后持久态恢复；
- `H` 层：不重启当前会话内自动热恢复；
- `U` 层：最终用户体验闭环。

### 8.2 当前正式状态

| 样本 | `P` 层 | 证据来源 | `H` 层 | 证据来源 | `U` 层 | 当前卡点 |
|---|---|---|---|---|---|---|
| `NPC` | 通过 | 用户真实重启后复验 + 本地字段链回读 | 未通过 | 当前会话内无自动归位证据 | 未通过 | 列表 / 分组 / 索引热刷新 |
| `farm-10.2.2-patch002` | 通过 | 用户最新口径已说明回到正确分组 + 本地字段链回读 | 未通过 | 当前会话内无自动归位证据 | 未通过 | 列表 / 分组 / 索引热刷新 |
| 治理线程 | 通过 | 根仓库线程本就应留在 `Sunset@main` | 不适用 | 非热恢复目标样本 | 不适用 | 无 |

### 8.3 当前热恢复写入边界

- 允许进入最小修正候选的对象：
  - `session_index.jsonl` 中 `NPC` / `farm` 的重复索引；
  - `title / first_user_message` 的单线程脏值。
- 当前不允许直接写入的对象：
  - `.codex-global-state.json`
  - 黑箱缓存层
  - `NPC` / `farm` 之外的任何线程。

## 9. 2026-03-13 口径补记：本矩阵不再作为当前主线入口

- 本矩阵保留为“独立 worktree 承接修复阶段”的历史验收记录；
- 它仍然能说明 `NPC/farm` 的持久态恢复与热恢复差异；
- 但从 2026-03-13 开始，它不再代表当前最高优先级执行入口。

当前新的主线已改为：

- 评估 `NPC` / `farm` 是否应回归 `D:\Unity\Unity_learning\Sunset`
- 准备业务成果回归 `main`
- 退役它们作为长期独立 worktree 默认线程的定位

## 10. 2026-03-13 总收口补记：本矩阵降级为历史承接证据

- 自 `NPC/farm` 正式进入回归 `Sunset/main` 总收口执行轮起，本矩阵进一步降级为历史承接问题证据；
- 它继续保留两类价值：
  - 说明为什么当时必须做 worktree 承接修复；
  - 说明为什么当前要把 worktree 降级为例外工具链。
- 当前新的验收入口不再是“线程还能否在独立 worktree 中热恢复”，而是：
  - `NPC/farm` 业务成果是否已经回到主项目体系；
  - 线程锚点是否已经回根仓库；
  - 旧 worktree 是否不再作为默认现场。

## 2026-03-13 补记：T74 历史化后补充 spring-day1 恢复说明
- T74 继续仅保留为 NPC/farm 历史线程承接验证矩阵，不再承担当前总恢复主入口职责。
- spring-day1 不进入 T74 的 worktree 承接实验矩阵，而是纳入主项目总恢复执行：
  - 关键缺失对象从 codex/restored-mixed-snapshot-20260311 白名单回带；
  - 线程语义统一回主项目；
  - worktree 快照仅保留为历史对照，不再作为默认开发现场。

## 2026-03-13 最终验收补记：总恢复已不再卡在 spring-day1
- `spring-day1` 已补齐当前主项目工作树里的最后两个增强脚本：`DialogueUI.cs`、`DialogueManager.cs`。
- `NPC/farm` 本轮只做复核，业务文件与线程锚点均未倒退，继续保持在 `D:\Unity\Unity_learning\Sunset@main`。
- 自本条起，T74 继续只承担历史承接验证矩阵角色；当前最终总验收结果已在 `NPC_farm_spring-day1总恢复最终结案报告_2026-03-13.md` 中统一收口。