# T74 用户真实点击线程闭环验证矩阵（2026-03-12）

## 1. 目的

本矩阵从本轮开始正式把“用户真实点击线程后的现象”纳入验证。

它不是只读基线替代品，而是用来回答：

- 点开线程后是否真的回到正确现场；
- 是否还会掉回 `Sunset/main`；
- 是否还需要靠重启暂时恢复；
- 用户眼里现在到底能不能验收。

## 2. 样本说明

- `NPC`：独立 worktree 功能线程
- `farm-10.2.2-patch002`：独立 worktree 功能线程
- 治理线程：根仓库 `main` 样本

## 3. 当前样本基线

| 样本 | 真实 worktree / 根仓库 | 真实分支 | 状态库 `git_branch` | 当前已知用户现象 |
|------|------|------|------|------|
| NPC | `D:\Unity\Unity_learning\Sunset_worktrees\NPC` | `codex/npc-generator-pipeline` | `main` | 点击线程后掉回 `Sunset/main`，并在根仓库误切功能分支，报 worktree 占用 |
| farm | `D:\Unity\Unity_learning\Sunset_worktrees\farm-10.2.2-patch002` | `codex/farm-10.2.2-patch002` | `main` | 点击线程后掉回 `Sunset/main`，并在根仓库误切功能分支，报 worktree 占用 |
| 治理线程 | `D:\Unity\Unity_learning\Sunset` | `main` | `main` | 当前未见同类报错；其作用是作为根仓库正常样本 |

## 4. 验证矩阵

| 样本 | 场景 | UI 项目组 | 线程标题归属 | 打开后真实目录语境 | 分支语境 | 是否误回根仓库 | 是否触发 worktree 占用报错 | 是否需要重启才暂时恢复 | 当前完成态 | 证据来源 |
|------|------|------|------|------|------|------|------|------|------|------|
| NPC | 不重启直接打开 | 待用户实测 | 待用户实测 | 当前已知会掉回 `Sunset/main` | 根仓库误尝试切功能分支 | 是 | 是 | 否 | 未通过 | 用户现象 + 本地只读证据 |
| NPC | 重启后再打开 | 待用户实测 | 待用户实测 | 用户反馈会暂时回到正确项目文件夹 | 待用户实测 | 暂时缓解 | 待用户实测 | 是 | 未通过 | 用户现象 |
| NPC | 切 provider 后打开 | 待用户实测 | 待用户实测 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 未验证 | 待用户验收 |
| farm | 不重启直接打开 | 待用户实测 | 待用户实测 | 当前已知会掉回 `Sunset/main` | 根仓库误尝试切功能分支 | 是 | 是 | 否 | 未通过 | 用户现象 + 本地只读证据 |
| farm | 重启后再打开 | 待用户实测 | 待用户实测 | 用户反馈会暂时回到正确项目文件夹 | 待用户实测 | 暂时缓解 | 待用户实测 | 是 | 未通过 | 用户现象 |
| farm | 切 provider 后打开 | 待用户实测 | 待用户实测 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 未验证 | 待用户验收 |
| 治理线程 | 不重启直接打开 | 预期为 `Sunset` | 待补记 | `D:\Unity\Unity_learning\Sunset` | `main` | 否 | 否 | 否 | L2 样本 | 本地 Git / worktree 只读证据 |
| 治理线程 | 重启后再打开 | 待验证 | 待验证 | 预期仍为根仓库治理现场 | `main` | 待验证 | 否 | 否 | 未验证 | 待用户验收 |
| 治理线程 | 切 provider 后打开 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 未验证 | 待用户验收 |

## 5. 用户可接受态定义

只有满足下列条件，样本才算通过：

1. 点开线程就回到正确 worktree / 根仓库现场；
2. UI 项目组与线程归属一致；
3. 不再误回 `Sunset/main`；
4. 不再触发 `branch already used by worktree`；
5. 不再需要重启才暂时恢复；
6. 若切 provider 后仍成立，可额外记为更高可信度。

## 6. 当前结论

- `T74` 已正式启动，但当前仍处于“矩阵已建立、用户真实点击样本待继续回填”的阶段；
- `NPC` 与 `farm` 当前都还不能通过用户可接受态；
- 当前最该先推进的是：在不粗暴改库的前提下，为 `NPC` 与 `farm` 建立单线程状态投影修正入口，并把结果回填到本矩阵。
