# T74 用户真实点击线程闭环验证矩阵（2026-03-12）

## 1. 目的

本矩阵正式用于记录用户真实点击线程后的结果，而不是只看只读脚本或状态库回读。

它要回答的是：

- 点开线程后是否真的回到正确现场；
- 是否还会掉回 `Sunset/main`；
- 是否还会触发根仓库误切功能分支；
- 当前是否已经达到用户可接受态。

## 2. 样本说明

- `NPC`：独立 worktree 功能线程
- `farm-10.2.2-patch002`：独立 worktree 功能线程
- 治理线程：根仓库 `main` 样本

## 3. 当前样本基线

| 样本 | 真实 worktree / 根仓库 | 真实分支 | 状态库 `git_branch` | 当前已知用户现象 |
|------|------|------|------|------|
| NPC | `D:\Unity\Unity_learning\Sunset_worktrees\NPC` | `codex/npc-generator-pipeline` | `codex/npc-generator-pipeline` | 首刀已执行且字段已对齐；用户真实点击后仍回落，已成立失败样本 |
| farm | `D:\Unity\Unity_learning\Sunset_worktrees\farm-10.2.2-patch002` | `codex/farm-10.2.2-patch002` | `main` | 点击线程后掉回 `Sunset/main`，并在根仓库误切功能分支，报 worktree 占用 |
| 治理线程 | `D:\Unity\Unity_learning\Sunset` | `main` | `main` | 当前未见同类报错；其作用是作为根仓库正常样本 |

## 4. 验证矩阵

| 样本 | 场景 | UI 项目组 | 线程标题归属 | 打开后真实目录语境 | 分支语境 | 是否误回根仓库 | 是否触发 worktree 占用报错 | 是否需要重启才暂时恢复 | 当前完成态 | 证据来源 |
|------|------|------|------|------|------|------|------|------|------|------|
| NPC | 不重启直接打开 | 分组可短暂回到正确项目组，但最终不稳定 | 已知线程点开后仍回落；标题是否回流未完全固化 | 最终仍回到 `Sunset/main` 语义，而不是稳定留在 `D:\Unity\Unity_learning\Sunset_worktrees\NPC` | 状态库字段已是 `codex/npc-generator-pipeline`，但点击承接仍失败 | 是 | 已知会触发根仓库误切；是否每次都弹同一报错仍属未知部分 | 否 | 失败样本（状态层单字段改善已成立，但真实点击承接未通过） | 用户真实点击反馈 + 状态库回读 + 执行记录 |
| NPC | 重启后再打开 | 分组可阶段性归位，但点击线程后仍会回落 | 已知点击后仍失败；标题细粒度回流待补证据 | 重启可阶段性缓解，但线程点击后仍不能稳定停在 `NPC` worktree | `git_branch` 正确并不足以穿透到点击承接层 | 是 | 已知仍存在误切根仓库风险；报错是否每次重现待补证据 | 是 | 失败样本（阶段性恢复 ≠ 用户可接受态） | 用户真实点击反馈 + 状态库回读 + 执行记录 |
| NPC | 切 provider 后打开 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未验证 | 当前轮未新增此路径证据 |
| farm | 不重启直接打开 | 待补细节 | 待补细节 | 当前已知会掉回 `Sunset/main` | 根仓库误尝试切功能分支 | 是 | 是 | 否 | 未通过 | 用户现象 + 本地只读证据 |
| farm | 重启后再打开 | 用户反馈会短暂回到正确项目文件夹 | 待补细节 | 重启可阶段性缓解，但稳定承接未成立 | 待补细节 | 暂时缓解 | 待补细节 | 是 | 未通过 | 用户现象 |
| farm | 切 provider 后打开 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未知部分 | 未验证 | 当前轮未新增此路径证据 |
| 治理线程 | 不重启直接打开 | 预期为 `Sunset` | 待补记 | `D:\Unity\Unity_learning\Sunset` | `main` | 否 | 否 | 否 | L2 样本 | 本地 Git / worktree 只读证据 |
| 治理线程 | 重启后再打开 | 待验证 | 待验证 | 预期仍为根仓库治理现场 | `main` | 待验证 | 否 | 否 | 未验证 | 待补证据 |
| 治理线程 | 切 provider 后打开 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 待验证 | 未验证 | 待补证据 |

## 5. 用户可接受态定义

只有满足下列条件，样本才算通过：

1. 点开线程就回到正确 worktree / 根仓库现场；
2. UI 项目组与线程归属一致；
3. 不再误回 `Sunset/main`；
4. 不再触发 `branch already used by worktree`；
5. 不再需要重启才暂时恢复；
6. 若切 provider 后仍成立，可额外记为更高可信度。

## 6. 当前结论

- `T74` 已正式进入“失败现象已成立、矩阵继续回填细节”的阶段；
- `NPC` 首刀已被真实点击现象证伪：单改 `git_branch` 没有形成可验收样本，只证明状态投影层单字段改善成立；
- `NPC` 当前必须按“失败样本”推进第二刀候选彻查，不再允许写成“待客户端实测中的可能成功样本”；
- `farm` 不再只是一般参照样本，而是用于验证 `NPC` 失败结论是否具有普适性的高价值对照样本；在第二刀对象未排清前，暂不执行首刀。
