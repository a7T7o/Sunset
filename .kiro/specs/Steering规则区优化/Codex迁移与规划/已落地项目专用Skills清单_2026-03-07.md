# 已落地项目专用 Skills 清单（2026-03-07）

> 本清单只记录当前已经实际创建完成的 Codex 用户技能。

---

## 1. 当前已创建 Skills

### 1.1 `sunset-workspace-router`

- 位置：`C:/Users/aTo/.codex/skills/sunset-workspace-router/`
- 用途：
  - 定位当前任务应回到哪个 `.kiro/specs` 工作区
  - 给出必读文件顺序
  - 提示 memory 追加顺序

### 1.2 `sunset-scene-audit`

- 位置：`C:/Users/aTo/.codex/skills/sunset-scene-audit/`
- 用途：
  - 在修改场景 / Prefab / Inspector / ScriptableObject 前执行安全审视
  - 固化“原有配置 / 问题原因 / 建议修改 / 修改后效果 / 风险影响”分析模板

### 1.3 `sunset-review-router`

- 位置：`C:/Users/aTo/.codex/skills/sunset-review-router/`
- 用途：
  - 处理 `锐评`、`审视报告`、`Diff`、`纠正` 等评审类任务
  - 依据项目规则选择 A / B / C 路径

### 1.4 `sunset-unity-validation-loop`

- 位置：`C:/Users/aTo/.codex/skills/sunset-unity-validation-loop/`
- 用途：
  - 固化 Sunset 项目的 Unity 验证闭环顺序
  - 区分项目失败与 MCP 传输失败
  - 在 scene / UI / dialogue / tests 场景下输出最小但可靠的验证结论

---

## 2. 当前作用定位

这 4 个 skill 不负责替代 `.kiro/steering`，只负责降低执行成本：

1. `sunset-workspace-router` 降低工作区切换成本
2. `sunset-scene-audit` 降低场景高风险修改误操作
3. `sunset-review-router` 降低锐评处理的重复判断成本
4. `sunset-unity-validation-loop` 降低 Unity 验证闭环中的误判成本

---

## 3. 与当前优先级的关系

它们对应的就是本轮可直接落地的高优先级项：

- 工作区路由
- 场景安全审视
- 锐评路由

当前尚未落地为 skill 的项：

- `.kiro/specs` / `.kiro/steering` 索引层 MCP

---

## 4. 下一步建议

下一阶段优先考虑：

1. 根据真实使用反馈迭代这 4 个 skill 的描述与操作步骤
2. 继续补第二批与 Unity / 工作区索引相关的 skill
3. 在新的 Unity MCP 选定后，再决定是否把部分步骤改成 MCP 依赖型 skill
