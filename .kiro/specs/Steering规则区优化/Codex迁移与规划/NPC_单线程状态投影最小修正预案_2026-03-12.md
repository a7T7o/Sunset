# NPC 单线程状态投影最小修正预案（2026-03-12）

## 1. 样本定位

- 样本线程：`NPC`
- 线程 id：`019cda74-411d-7821-9f94-7e14e36e4e16`
- 目标：在不直接批量改库的前提下，为 `NPC` 建立首个可执行、可验收、可回滚的单线程状态投影最小修正预案。

## 2. 当前现场

### 2.1 状态库字段现状

- `cwd = D:/Unity/Unity_learning/Sunset_worktrees/NPC`
- `git_branch = main`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`

### 2.2 真实 worktree 现状

- worktree 路径：`D:/Unity/Unity_learning/Sunset_worktrees/NPC`
- 真实分支：`codex/npc-generator-pipeline`
- 真实 HEAD：`1f068ed1731316a07cb471b90bce8e8af7534277`

### 2.3 当前错误现象

- 用户点开 `NPC` 线程后掉回 `Sunset/main`；
- 客户端在根仓库误尝试切到 `codex/npc-generator-pipeline`；
- 随后报：`fatal: 'codex/npc-generator-pipeline' is already used by worktree at ...`。

## 3. 为什么先从 NPC 开始

- `NPC` 同时呈现“真实 worktree 正常 + 状态库 `git_branch` 旧值 + 用户点击线程掉回根仓库”的完整冲突链；
- 它比 `farm` 更适合作为第一刀样本，因为修正优先级和验收路径更集中；
- `farm-10.2.2-patch002` 暂时保留为并行参照样本，用于验证后续推广是否成立，而不是本轮同步下刀对象。

## 4. 最小修正字段集排序

1. `git_branch`
2. `git_sha`
3. `cwd`
4. `rollout_path`

## 5. 字段影响判断

### 5.1 `git_branch`
- 当前真实分支是 `codex/npc-generator-pipeline`，但状态库仍写 `main`；
- 这是当前最强冲突候选，最可能导致线程恢复时被重新投影回根仓库语义。

### 5.2 `git_sha`
- 当前状态库 `git_sha` 仍停留在旧值 `404933466f5538475c0c898b21dc808a60b38ea2`；
- 若客户端恢复线程时同时校验分支与提交，这个旧值会继续加剧投影失配。

### 5.3 `cwd`
- 当前 `cwd` 已指向正确 worktree；
- 这说明它不是首要冲突，但仍要在修正前后核对，避免误把正确字段改坏。

### 5.4 `rollout_path`
- 当前尚未确认它是否直接参与线程恢复；
- 因此只列入低优先级候选，不作为首轮必改字段。

## 6. 改前备份方案

### 6.1 备份对象
- `C:/Users/aTo/.codex/state_5.sqlite`
- `NPC` 线程对应整行状态
- 当前 `NPC` worktree 的真实分支与 HEAD

### 6.2 备份命名
- 数据库文件备份：`state_5.sqlite.bak_<执行日期>_<执行前HEAD>`
- 单线程行导出：`npc_thread_state_backup_<执行日期>_<线程id>.json`
- worktree 现场记录：`npc_worktree_snapshot_<执行日期>.md`

### 6.3 备份命令示例
- `Copy-Item 'C:/Users/aTo/.codex/state_5.sqlite' 'C:/Users/aTo/.codex/state_5.sqlite.bak_2026-03-12_preT80'`
- 单线程行导出与 worktree 现场记录在执行轮再以只读脚本或 SQL 查询固化。

## 7. 单线程最小执行顺序

1. 先完成数据库原文件备份；
2. 再导出 `NPC` 线程整行状态；
3. 核对 `NPC` 真实 worktree 的分支与 HEAD；
4. 首轮只考虑修正 `git_branch`；
5. 若仅修 `git_branch` 仍不能改变点击线程行为，再评估是否补修 `git_sha`；
6. 未经单线程验收，不推广到 `farm`。

## 8. 单线程验收步骤

1. 备份完成后，打开 `NPC` 线程；
2. 记录是否仍掉回 `Sunset/main`；
3. 记录是否仍触发根仓库切 `codex/npc-generator-pipeline`；
4. 记录是否仍报 worktree 占用；
5. 分别验证“重启后打开”和“不重启直接打开”；
6. 同步记录 UI 项目分组是否稳定归在正确位置。

## 9. 通过标准

- 点开 `NPC` 线程回到 `D:/Unity/Unity_learning/Sunset_worktrees/NPC`；
- 不再掉回 `Sunset/main`；
- 不再误触发根仓库切功能分支；
- 不再出现 `branch already used by worktree`；
- UI 项目分组稳定；
- 至少明确“重启后”和“不重启”两条路径分别是否通过。

## 10. 回滚方案

1. 用 `state_5.sqlite` 备份恢复原文件；
2. 如只改单线程字段，则以导出的整行状态回写恢复；
3. 再次运行只读基线脚本，确认 `NPC` 状态恢复到修正前快照；
4. 若修正后 UI 现象更差，立即停止推广到 `farm`。

## 11. 风险说明

- 首轮仍不能承诺“一跑即恢复 UI”；
- 即使 `git_branch` 修正正确，客户端仍可能缓存旧投影；
- 因此本预案是“执行前准备”，不是“已修好”；
- 当前仍禁止直接批量改 `state_5.sqlite`，也禁止未经备份就进入写入。

## 12. 首刀执行结果（2026-03-12）

- 本轮实际已执行：仅把 git_branch 从 main 修正为 codex/npc-generator-pipeline；
- 当前回读显示：cwd 未动、git_sha 未动、ollout_path 未动；
- 当前结论：状态投影层已完成首刀对齐，但 UI / workspace / 线程点击承接仍待客户端实测；
- 因此本轮暂不把同样动作直接推广到 arm。
