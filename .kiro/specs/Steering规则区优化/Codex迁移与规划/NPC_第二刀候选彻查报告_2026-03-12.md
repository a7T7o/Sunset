# NPC 第二刀候选彻查报告（2026-03-12）

## 1. 目标

把 `NPC` 从“首刀失败样本”推进到“第二刀候选已排序、回滚决策已明确”的状态。

## 2. 已排查对象

### 2.1 `threads` 表当前字段
- `cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`
- `rollout_path = C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`
- `title = --+++*`
- `first_user_message = --+++*`

### 2.2 真实 worktree
- 路径：`D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- 分支：`codex/npc-generator-pipeline`
- HEAD：`1f068ed1731316a07cb471b90bce8e8af7534277`

### 2.3 rollout 元数据
- rollout 文件最早 `session_meta.cwd` 仍是 `D:\Unity\Unity_learning\Sunset`；
- 这与线程表中的 `cwd` 已对到 `NPC` worktree 形成直接冲突。

### 2.4 `session_index.jsonl`
- 同一线程 id 仍存在两条索引：`Interpret malformed prompt` 与 `NPC`；
- 说明索引层存在标题/路由残留。

### 2.5 `.codex-global-state.json`
- 当前 `active-workspace-roots = ["D:\Unity\Unity_learning\Sunset"]`；
- 这说明当前全局 UI 根语义仍在根仓库，而不是 `NPC` worktree。

## 3. 第二刀候选排序

1. `rollout_path`
2. `git_sha`
3. `session_index.jsonl` 索引残留（只读候选）
4. `.codex-global-state.json` 当前激活工作区（解释层证据，不是本轮单线程直写对象）

## 4. 为什么这样排序

### 4.1 第一名：`rollout_path`
- 它直接把线程起点锚在根仓库语义；
- 在 `git_branch` 已对齐仍失败的前提下，它是当前最强的“点击线程为何回根仓库”证据。

### 4.2 第二名：`git_sha`
- 它仍停在旧提交；
- 如果客户端恢复线程时同时依赖分支与提交，这个旧值会继续把线程拉回旧现场；
- 它与 rollout 元数据一起，构成当前最像“恢复承接源”的组合候选。

### 4.3 为什么先不选其他字段
- `git_branch` 已经被首刀证明“修正本身无错，但单独不够”；
- `cwd` 当前已经正确，不应重复下刀；
- `title` / `first_user_message` / `session_index` 残留有可疑性，但现阶段证据更适合先做索引层解释，不适合直接写入。

## 5. 回滚决策

- 当前决策：暂不回滚 `NPC.git_branch`；
- 原因：
  - 该修正没有引入副作用；
  - 它把一个已知错误字段排除出变量集合；
  - 保留它更利于把第二刀聚焦到 `rollout_path` / `git_sha` / 索引残留；
  - 现在回滚只会把已排除方向重新混回现场。

## 6. 对 `farm` 的意义

- `farm` 暂不执行首刀；
- 它用于验证 `NPC` 当前失败结论是否具有普适性；
- 若后续 `NPC` 第二刀证明 rollout / sha 才是关键，则 `farm` 应优先按同样顺序复核，而不是先重复 `git_branch` 首刀。
