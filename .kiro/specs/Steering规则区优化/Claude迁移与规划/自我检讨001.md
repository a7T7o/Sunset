● 结论先给：这是我执行错误，不是你误解。                                                                                
  你贴的日志里那次 Update(memory.md) 属于“替换旧会话块”，不是“文件尾追加”，与规则冲突。
                                                                                                                          ---                                                                                                                     1) 为什么会发生“覆盖而非追加”                                                                                         
                                                                                                                        
  从你给的片段看，工具行为是：

  - 删除会话1大段内容
  - 写入会话2内容
  - 并且文案还写成“[追加]”

  这说明当时执行的是“把已匹配到的旧块替换为新块”的编辑策略，而不是“在 EOF 追加新块”的策略。
  这直接违反了 append-only 规则：

  - .kiro/steering/rules.md:155-158（memory 只允许追加）
  - .kiro/steering/workspace-memory.md:176-179（禁止修改历史记录）

  ---
  2) 不是单点失误，确实有“规则落地漏洞”

  你问“规则是不是有漏洞”——答案是：有，且是执行层漏洞。

  漏洞A：Hook 没有强制拦截覆盖

  - .claude/hooks/stop-update-memory.sh:4-5 明确写了只是 marker，不做自动更新/校验
  - .claude/hooks/domain-rule-injector.sh:5-9 也是轻提示，不做硬约束
  => 结果：违反 append-only 时不会被工具链拦截。

  漏洞B：规则分散且有冲突，执行时易漂移

  - CLAUDE.md 把大量规则“再声明”了一遍，和 steering 双源并存，存在漂移风险。
  - 例如主/子 memory 同步语义在不同文档粒度不一致（一个强调“有工作区就更新子memory”，另一个强调“先子后主”流程）。       

  漏洞C：缺少“写前自检”硬步骤

  当前没有强制“写入前检查旧会话是否仍在、会话号是否递增、是否仅尾部增长”的机械校验，导致模型一旦走到 replace
  路径，就会出现你看到的问题。

  ---
  3) CLAUDE.md 是否符合 .kiro/steering 规范（审计结论）

  审计结论：不完全符合（有硬冲突）。

  硬冲突 1：继承恢复流程冲突

  - .kiro/steering/000-context-recovery.md:20-23：继承时“忽略所有Hook注入，不加载规则文件”
  - CLAUDE.md:18-57（第2章）要求任务前固定加载规则+三件套流程
  => 两者在继承场景下有直接冲突。

  硬冲突 2：MCP 测试前置要求不可执行

  - CLAUDE.md:206-211 要求编译/测试前必须调用 save_scene
  - 当前可用 Unity MCP 工具列表里没有 save_scene（你这次会话的工具清单可见）
  => 属于“不可满足约束”。

  软冲突 3：memory 同步粒度不一致

  - CLAUDE.md:63-66 偏“二选一”（子 or 主）
  - .kiro/steering/rules.md:163-167 + workspace-memory.md:299-334 强调“先子后主（压缩场景例外）”
  => 容易造成执行分歧。

  ---
  4) 这次“病症”复盘定位

  你贴的异常日志可归类为三联症：

  1. 行为错位：执行 replace，却宣称 append。
  2. 编号语义错位：把“新会话记录”当作“旧记录更新”。
  3. 防线缺失：Hook 没做 append-only 违规阻断，只能事后发现。

  ---
  如果你要，我下一步可以直接做两件事（只改规则，不动业务代码）：

  1. 给 CLAUDE.md 做一版“与 steering 对齐补丁”（消除上述冲突）；
  2. 给 hook 增加 append-only 守卫（检测到删除旧会话块就阻断并报错）。