# 全面理解报告（Steering × Kiro Hook × Claude Hook）

## 0. 报告目标与方法

- 目标：彻底厘清 `.kiro/steering` 规则体系、`.kiro/hooks` 触发体系、`.claude/hooks` 执行体系与 `CLAUDE.md` 的优先级关系。
- 方法：边读边写，分批追加；每批只落“已证实结论”，避免一次性长文导致遗漏或中断。
- 当前状态：进行中（第1批已完成：`rules.md`、`communication.md`、`smart-assistant.kiro.hook`）。

---

## 1. 第1批结论（已完成）

### 1.1 你对 000-context-recovery 的纠正是正确的

结论：在“非继承场景”下，`000-context-recovery.md` 不应参与当前流程裁决。

- 证据：`smart-assistant.kiro.hook` 明确写了“检测到 summarized/summary 时才走 000-context-recovery；否则执行规则路由”。
- 含义：我之前把它当作当前冲突主因，属于场景判断过度泛化，需要纠正。

### 1.2 当前真实执行主轴应是三层

1) 运行时总规则：`rules.md`（P0、always、canonical）
2) 按需补充规则：`smart-assistant.kiro.hook` 路由到领域文档
3) 沟通增强参考：`communication.md`（已注明多章节“以 rules.md 为准”）

### 1.3 已识别的第一批结构性问题

- Hook 路由提示仍使用旧术语 `readFile`，与 Claude Code 工具名 `Read` 不一致。
- `rules.md` 内部存在互相拉扯的沟通条款（如“直接执行”与“方案讨论先确认”并存），需要在 `CLAUDE.md` 做优先级重排，而不是简单搬运。

---

## 2. 下一批计划（进行中）

将继续审计：
- `.kiro/hooks/memory-update-check.kiro.hook`
- `.kiro/hooks/git-quick-commit.kiro.hook`
- `.kiro/steering/workspace-memory.md`
- `.kiro/steering/code-reaper-review.md`
- `.claude/hooks/*.sh` 全量

每读取一批即追加到本报告。

---

## 3. 第2批结论（已完成）

已读文件：
- `.kiro/hooks/memory-update-check.kiro.hook`
- `.kiro/hooks/git-quick-commit.kiro.hook`
- `.kiro/steering/code-reaper-review.md`
- `.kiro/steering/workspace-memory.md`（关键区段）
- `.claude/hooks/domain-rule-injector.sh`
- `.claude/hooks/pre-bash-block.sh`
- `.claude/hooks/post-edit-format.sh`
- `.claude/hooks/pre-compact-snapshot.sh`
- `.claude/hooks/stop-update-memory.sh`

### 3.1 Kiro Hook 与 Claude Hook 的真实关系

结论：两套 Hook 体系目前是“设计强、落地弱”的不对称状态。

- Kiro 侧（`.kiro/hooks`）包含高强度流程意图（memory 更新、压缩快照、规则路由、一键提交）。
- Claude 侧（`.claude/hooks/*.sh`）当前实现以 marker 为主，只有 `pre-bash-block.sh` 带有限命令黑名单。
- 因此当前不能假设“自动治理已接管”，大多数治理动作仍需人工严格执行。

### 3.2 你指出的关键点再次被证实

- `000-context-recovery` 是“继承场景专用”，不应混入普通对话执行主线。
- 我之前把它当作通用冲突源是错误归因，现已在本报告中修正。

### 3.3 memory 规则链条的硬约束

`workspace-memory.md + memory-update-check.kiro.hook` 共同确认：

1) append-only 是硬规则（禁止改历史）。
2) 同步顺序是“先子后主”（压缩场景例外）。
3) 覆盖“纯问答也要记忆”的要求很强，不仅限代码改动。
4) 分卷制度明确且有阈值（主 200 / 子 300）。

### 3.4 锐评处理的唯一执行协议

`code-reaper-review.md` 已定义完整 A/B/C 分流：

- A/B：可执行路径，不必额外制造报告噪声。
- C：必须生成审视报告并中断等待确认。

这应成为 `CLAUDE.md` 的“引用型上位条款”，而不是再写一套并行细则。

### 3.5 Claude hooks 逐项审计（当前有效性）

1. `domain-rule-injector.sh`：仅提示，不注入规则，不阻断。
2. `post-edit-format.sh`：仅提示，不格式化，不校验。
3. `pre-compact-snapshot.sh`：仅提示，不落快照。
4. `stop-update-memory.sh`：仅提示，不更新 memory，不校验 append-only。
5. `pre-bash-block.sh`：有最小黑名单，但覆盖面窄，且阻断语义依赖 runner 行为；不足以承担完整安全边界。

### 3.6 新增整改方向（较上一版更聚焦）

- 方向 A（优先）：把 `.claude/hooks` 的文案从“看似自动化”改成“显式声明仅 marker”，避免误判。
- 方向 B（关键）：在 `stop-update-memory.sh` 实装 append-only 守卫（至少检测旧会话标题被删除/改写）。
- 方向 C（治理）：`CLAUDE.md` 只保留总纲和优先级，不重复堆叠 steering 细则正文。

---

## 4. 下一批计划（进行中）

继续完成“全 steering 扫描”与 `CLAUDE.md` 优先级重排设计：
- 读取并梳理其余 `.kiro/steering/*.md`（含 domain 规则）
- 读取 `.kiro/steering/README.md` 与 `maintenance-guidelines.md` 的治理约束
- 对照 `CLAUDE.md` 形成“严格项 / 宽松项 / 场景项”三层排序草案

---

## 5. 第3批结论（已完成）

已读文件（本批）：
- `CLAUDE.md`
- `.kiro/steering/README.md`
- `.kiro/steering/maintenance-guidelines.md`
- `.kiro/steering/coding-standards.md`
- `.kiro/steering/debug-logging-standards.md`
- `.kiro/steering/documentation.md`
- `.kiro/steering/layers.md`
- `.kiro/steering/save-system.md`
- `.kiro/steering/chest-interaction.md`
- `.kiro/steering/scene-modification-rule.md`
- `.kiro/steering/scene-hierarchy-sync.md`
- `.kiro/steering/animation.md`
- `.kiro/steering/ui.md`
- `.kiro/steering/systems.md`
- `.kiro/steering/trees.md`

### 5.1 全局优先级重新厘清（修正后）

**第一优先级（硬约束）**
1) 用户当轮明确指令
2) `rules.md`（P0 canonical）
3) 对应领域 canonical 文件（如 `layers.md`、`workspace-memory.md`、`code-reaper-review.md`）

**第二优先级（场景触发）**
4) `smart-assistant.kiro.hook` 的路由逻辑（用于按需加载，不应反客为主）
5) `.kiro/hooks/memory-update-check.kiro.hook`（强调流程意图，但在 Claude 端需人工兜底）

**第三优先级（说明/参考）**
6) 其它 steering manual 文档（P1/P2）
7) `CLAUDE.md`（应作为执行总纲与排序器，而不是与 steering 并列重复定义器）

### 5.2 `CLAUDE.md` 当前最大问题不是“内容少”，而是“排序与边界错位”

结论：`CLAUDE.md` 应该做“优先级编排”，而不是“全量重写 steering 细则”。

当前可见错位：
- 把继承专属逻辑与普通会话主线混用（你指出得完全正确）。
- 把不可执行动作写成硬约束（如 `save_scene` 前置）。
- 把场景修改规则与 MCP 自动化指令发生内部冲突（`scene-modification-rule.md` 要先分析并征求确认）。

### 5.3 严格项 vs 宽松项（为后续改 CLAUDE.md 提供排序模板）

**严格项（必须硬执行）**
- append-only：memory 不改历史（`workspace-memory.md`）
- 先子后主：memory 同步顺序（压缩例外）
- 锐评 A/B/C 分流，C 必须中断并审视报告（`code-reaper-review.md`）
- 场景/组件修改先审视并征求同意（`scene-modification-rule.md`）
- 层级与碰撞核心禁令（`rules.md` + `layers.md`）

**宽松项（可按任务裁剪）**
- 文档写作风格细节（`documentation.md`）
- 代码组织样式偏好（`coding-standards.md`）
- 某些模块级历史经验条款（如具体 UI 文案偏好、日志格式细节）

**场景项（仅触发时启用）**
- 继承恢复流程（仅 summary/继承提示出现时）
- 一条龙模式（仅用户明确“直接全部完成”）
- 存档/箱子/树木/动画等领域规则（按关键词路由）

### 5.4 `.claude/hooks` 的“逐个重审结论”（你要求的重点）

1) `domain-rule-injector.sh`
- 现状：marker。
- 结论：保留可观测性价值，但不应被描述为“规则注入器”。
- 建议：名称或注释显式改为 `domain-rule-marker` 语义，避免误导。

2) `post-edit-format.sh`
- 现状：marker。
- 结论：当前不具备格式化能力。
- 建议：要么接入真实 formatter，要么明确“仅日志标记”。

3) `pre-compact-snapshot.sh`
- 现状：marker。
- 结论：当前不做快照，不能承担继承防丢失职责。
- 建议：在注释中明确“仅提示，不保证快照”。

4) `stop-update-memory.sh`
- 现状：marker。
- 结论：这是当前流程最大缺口之一（没有 append-only 守卫）。
- 建议：P0 增加硬校验，发现历史块删改立即阻断。

5) `pre-bash-block.sh`
- 现状：有限黑名单拦截。
- 结论：有价值，但覆盖窄；不足以替代权限最小化。
- 建议：补充更多破坏命令模式，并核实 exit 语义与 runner 行为一致。

### 5.5 Kiro 规则与 Claude 规则的融合方式（本批形成）

- 融合目标不是“谁覆盖谁”，而是：
  - steering 提供领域事实与执行约束；
  - CLAUDE.md 提供优先级排序、触发边界、冲突仲裁。
- 因此 CLAUDE.md 应该重排为：
  1) 优先级与冲突仲裁
  2) 执行前计划（仅普通会话）
  3) 运行时安全边界（MCP/Bash）
  4) 收尾与 memory 纪律
  5) 场景专用附录（继承、一条龙、锐评）

---

## 6. 下一批计划（进行中）

- 继续补齐尚未精读的 steering 领域文档（items / so-design / placeable-items / archive/farming 等）
- 输出 `CLAUDE.md` 的“优先级重排草案（不改业务代码）”
- 输出 `.claude/hooks` 的“可执行化改造清单（先报告后改）`

---

## 7. 第4批结论（已完成）

已读文件（本批新增）：
- `.kiro/steering/items.md`
- `.kiro/steering/so-design.md`
- `.kiro/steering/placeable-items.md`
- `.kiro/steering/archive/farming.md`
- `.kiro/steering/archive/tech.md`
- `.kiro/steering/archive/product.md`
- `.kiro/steering/archive/progress.md`
- `.kiro/steering/archive/structure.md`
- `.claude/hooks/domain-rule-injector.sh`（复核）
- `.claude/hooks/pre-bash-block.sh`（复核）
- `.claude/hooks/post-edit-format.sh`（复核）
- `.claude/hooks/pre-compact-snapshot.sh`（复核）
- `.claude/hooks/stop-update-memory.sh`（复核）

### 7.1 steering 未覆盖项已补齐，核心结论稳定

- `items.md` 与 `so-design.md` 形成一致链条：
  - 物品 ID 段位、品质系统、ItemDatabase 访问禁令（禁止场景查找 SO）在两份文档内一致。
  - `so-design.md` 明确“SO 结构改动必须同步编辑器批量工具”，这属于“开发流程硬约束”，不能在 `CLAUDE.md` 中丢失。
- `placeable-items.md` 补强了“可放置物体底部对齐 + Sprite 切换链路 + Collider/NavGrid 刷新顺序”的落地细则；本质是 `layers/chest/trees` 的实现补丁，不是可选建议。
- `archive/farming.md` 证明：归档文档中含大量历史语义和阶段结论，价值在“背景追溯”，不应覆盖活跃 canonical 规则。

### 7.2 archive 文档的定位已明确

`archive/product/progress/tech/structure` 的共同特征：
- 优先级均为 P3/P4 或显式 archived 标记；
- 结论存在时效性（如完成度、阶段状态、技术栈快照）；
- 可用于“解释来龙去脉”，不应用于“当前执行裁决”。

因此优先级上应统一归入：
- **背景证据层（低优先）**，仅当主规则缺失时作为参考。

### 7.3 `.claude/hooks` 复核后结论无反转，但风险分级更清晰

1) `domain-rule-injector.sh`
- 事实：纯提示 marker。
- 风险：名字带“injector”容易被误解为“实际注入规则”。

2) `post-edit-format.sh`
- 事实：纯提示 marker。
- 风险：对“格式化已执行”形成误导预期。

3) `pre-compact-snapshot.sh`
- 事实：纯提示 marker。
- 风险：用户可能误判“已自动做快照”。

4) `stop-update-memory.sh`
- 事实：纯提示 marker。
- 风险：append-only 违规无自动阻断，属于当前最高治理缺口。

5) `pre-bash-block.sh`
- 事实：有模式拦截（`rm -rf /`、`git reset --hard`、`git push --force` 等）。
- 风险：
  - 覆盖面偏窄；
  - 退出码与 runner 行为耦合（当前 `exit 0` 语义是否等价“阻断”需按 runner 实测），存在“看似拦截、实则放行”的可能性。

### 7.3补充：`.claude/settings*.json` 的定位也已明确

- `.claude/settings.json` 的职责是：仓库级 `permissions` 声明 + hook wiring。
- `.claude/settings.local.json` 的职责是：本机侧额外 `allow` 补充。
- 这两份文件决定“哪些工具/脚本被放行、在哪些生命周期触发”，**不决定**模型是否自动获得 Kiro 的规则注入、收尾决策或语义路由能力。
- `Bash(*)` 的意义只是权限边界放宽；它不是可靠阻断，也不是自动治理。

### 7.4 `CLAUDE.md` 应纳入的最终三层排序（本批固化版）

**A. 严格项（硬执行）**
- 用户本轮明确指令
- `rules.md` P0 条款
- canonical 领域规则（`workspace-memory.md`、`code-reaper-review.md`、`layers.md`、`scene-modification-rule.md` 等）
- memory append-only + 先子后主
- 锐评 A/B/C，路径 C 中断

**B. 宽松项（按任务裁剪）**
- 文档风格、输出格式偏好
- coding 样式中的非安全/非一致性硬条款
- 历史经验类建议

**C. 场景项（触发才启用）**
- 继承恢复：仅 summary/继承信号触发
- 一条龙模式：仅用户明确要求“直接全部完成”
- 领域专项：根据关键词路由（农田/放置/UI/存档/动画等）

### 7.5 新的冲突消解原则（避免再漂移）

- 当 `CLAUDE.md` 与 steering 细则冲突时：
  - `CLAUDE.md` 只负责“排序和仲裁”；
  - 具体执行细节回落到对应 canonical 文件。
- `CLAUDE.md` 不再重复写完整业务细则正文，避免双源漂移。

---

## 8. 下一批计划（进行中）

- 基于上述三层排序，输出 `CLAUDE.md` 的“最小改动重排草案”（仅结构与优先级，不改业务规则事实）
- 输出 `.claude/hooks` 的“改造优先级清单（P0/P1/P2）”，重点落在：
  - `stop-update-memory.sh` append-only 阻断守卫
  - `pre-bash-block.sh` 阻断语义实测与模式补全
  - 其余 marker 脚本命名/注释去误导化
- 继续按批次追加本报告，直至形成可执行的最终迁移蓝图

---

## 9. `CLAUDE.md` 最小改动重排草案（治理版）

### 9.1 目标

- 不重写 steering 细则正文。
- 只在 `CLAUDE.md` 中明确“优先级、触发边界、冲突仲裁、不可执行条款处理”。
- 让执行端不再出现“规则双源漂移”。

### 9.2 建议章节结构（替换现有重声明布局）

1) **优先级与冲突仲裁（新第1章）**
- 顺序固定：用户指令 > `rules.md` > canonical 领域文档 > 场景触发规则 > 参考文档。
- 明确 `CLAUDE.md` 是排序器，不是第二套业务规则正文。

2) **普通会话主流程（新第2章）**
- 仅保留：任务理解 → 必要文档读取 → 执行计划 → 实施 → 验证 → 收尾。
- 显式排除继承恢复逻辑（继承逻辑放附录并注明触发条件）。

3) **收尾与 memory 纪律（新第3章）**
- append-only、先子后主、分卷阈值、主/子粒度。
- 明确“归档重命名允许，但历史块改写禁止”。

4) **锐评协议（新第4章）**
- 只写入口与优先级，执行细节唯一引用 `code-reaper-review.md`。
- 明确路径 C 中断为硬约束。

5) **MCP/Bash 安全边界（新第5章）**
- 保留可执行条款，删除或改写不可执行条款。
- 例如当前 `save_scene` 前置要求需改成“若工具存在则执行；不存在则提示人工保存并确认”。

6) **场景附录（新第6章）**
- 继承恢复：仅 summary/继承信号触发。
- 一条龙：仅用户明确要求触发。
- 领域路由：按关键词加载对应 steering 文件。

### 9.3 必删/必改条款清单（按风险）

- **P0 必改**：把“不可执行硬约束”改为“能力检测 + 人工转交语义”。
- **P0 必改**：消除“继承专用流程”与“普通会话固定流程”的并行冲突。
- **P0 必改**：memory 同步语义统一为“先子后主（压缩例外）”。
- **P1 建议**：删除重复的模块细则正文，改为 canonical 链接式引用。

---

## 10. `.claude/hooks` 改造优先级清单（执行化）

### 10.1 P0（必须先做）

1) `stop-update-memory.sh`
- 目标：append-only 守卫。
- 最低可行检测：
  - 检测 `memory.md` 历史会话标题（如 `### 会话`）是否出现删除或改写；
  - 检测是否存在非尾部修改；
  - 违规即 fail 并输出统一错误标识（如 `append-only violated`）。

2) `pre-bash-block.sh`
- 目标：把“黑名单提示”提升为“可验证阻断”。
- 要点：
  - 增补危险模式覆盖；
  - 校验 runner 下 exit 语义是否真阻断；
  - 给出“命中规则名 + 命令片段”的可审计输出。

### 10.2 P1（应尽快做）

3) `domain-rule-injector.sh`
- 保留 marker 价值，但名称/注释去误导：显式写明“仅提示，不注入规则”。

4) `post-edit-format.sh`
- 二选一：
  - 接入真实 formatter/校验链；或
  - 显式改名为 marker，避免“已格式化”错觉。

5) `pre-compact-snapshot.sh`
- 显式写明“仅提示，不保证落快照”；
- 若后续接自动快照，应把成功/失败状态输出为结构化标记。

### 10.3 P2（治理增强）

- 统一所有 hook 输出格式（前缀 + 等级 + 可机读标记），便于后续审计和故障定位。
- 增加“hook 能力矩阵”文档：每个 hook 的触发点、是否阻断、失败语义、负责人。

---

## 11. 当前阶段结论（截至第4批）

- steering 全量规则主干已完成剖析，当前争议已从“理解偏差”收敛到“执行化缺口”。
- `CLAUDE.md` 的主修方向已稳定：**从规则复写器改为优先级排序器**。
- hooks 主修方向已稳定：**从 marker 集合改为最小可阻断链路（先 memory，再 bash）**。
