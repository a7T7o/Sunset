# NPC 第二刀执行记录（2026-03-12）

## 1. 执行结论

- 第二刀已真实执行。
- 当前已完成到“第二刀执行后，待用户两条路径复验”。
- 当前尚未完成的唯一部分：用户真实点击复验。

## 2. 执行前备份

- 数据库备份：`C:\Users\aTo\.codex\state_5.sqlite.bak_2026-03-12_secondknife_pre`
- rollout 原文件备份：`C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.pre_secondknife_2026-03-12.jsonl`
- 单线程整行导出：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_thread_state_backup_2026-03-12_secondknife_019cda74-411d-7821-9f94-7e14e36e4e16.json`
- worktree 快照：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_worktree_snapshot_2026-03-12_secondknife.md`
- 第二刀执行前 `.codex-global-state.json` 采样：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_global_state_sample_2026-03-12_secondknife.json`
- 该次采样的 `active-workspace-roots = ["D:\Unity\Unity_learning"]`

## 3. 写入对象清单

1. rollout `session_meta.cwd`
2. rollout `session_meta.git.branch`
3. rollout `session_meta.git.commit_hash`
4. rollout 全部 `turn_context.cwd`
5. `threads.git_sha`

## 4. 改前值

### 4.1 `threads` 表

- `cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`
- `rollout_path = C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`

### 4.2 rollout 文件

- `session_meta.cwd = D:\Unity\Unity_learning\Sunset`
- `session_meta.git.branch = main`
- `session_meta.git.commit_hash = 3b45da720500f3df9945a67f06acf69d9fba76ab`
- 全部 `turn_context.cwd = D:\Unity\Unity_learning\Sunset`

## 5. 改后值

### 5.1 `threads` 表

- `cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 1f068ed1731316a07cb471b90bce8e8af7534277`
- `rollout_path = C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`

### 5.2 rollout 文件

- `session_meta.cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `session_meta.git.branch = codex/npc-generator-pipeline`
- `session_meta.git.commit_hash = 1f068ed1731316a07cb471b90bce8e8af7534277`
- 全部 `turn_context.cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`

## 6. 写入顺序

1. 备份 `state_5.sqlite`
2. 备份 rollout 原文件
3. 导出 `NPC` 当前整行状态
4. 记录 `NPC` 真实 worktree 分支与 HEAD
5. 记录第二刀执行前 `.codex-global-state.json` 采样值
6. 写 rollout `session_meta.cwd`
7. 写 rollout `session_meta.git.branch`
8. 写 rollout `session_meta.git.commit_hash`
9. 批量写全部 `turn_context.cwd`
10. 写 `threads.git_sha`

## 7. 本地回读结果

- `threads` 表回读：
  - `cwd` 正确
  - `git_branch` 正确
  - `git_sha` 已对齐到真实 `NPC` HEAD
  - `rollout_path` 未变，仍指向当前线程 rollout 文件
- rollout 文件回读：
  - `session_meta.cwd` 已对齐到 `NPC` worktree
  - `session_meta.git.branch` 已对齐到 `codex/npc-generator-pipeline`
  - `session_meta.git.commit_hash` 已对齐到 `1f068ed1731316a07cb471b90bce8e8af7534277`
  - 全部 `turn_context.cwd` 已统一对齐到 `D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- 结论：本地状态链已全部达到第二刀预期值。

## 8. 用户现在该如何点

### 路径 A：不重启直接点

1. 先看线程列表里 `NPC` 是否仍挂在 `Sunset` 下
2. 再看 `NPC` 分组下是否还显示“无线程”
3. 直接点击 `NPC` 线程
4. 观察是否仍回到 `Sunset/main`
5. 观察是否仍误触发根仓库切 `codex/npc-generator-pipeline`
6. 观察是否仍报 `branch already used by worktree`

### 路径 B：重启后再点

1. 重启 `Codex`
2. 重复路径 A 的 6 个观察点

## 9. 通过 / 失败标准

### 通过

- `NPC` 归在正确分组
- `NPC` 分组下不再显示“无线程”
- `Sunset` 下不再错误挂着 `NPC`
- 点开后稳定停在 `D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- 不再回落到 `Sunset/main`
- 不再误触发根仓库切功能分支
- 不再报 `branch already used by worktree`

### 失败

- 只要仍回落到 `Sunset/main`
- 或 `NPC` 分组仍显示“无线程”
- 或 `Sunset` 下继续错误挂着 `NPC`
- 或仍误触发根仓库切 `codex/npc-generator-pipeline`
- 或仍报 `branch already used by worktree`

则第二刀未通过。

## 10. 回滚预置

如果用户复验结果为失败，下一步按以下顺序立刻回滚：

1. 用 rollout 备份恢复原 rollout 文件
2. 用单线程备份把 `threads.git_sha` 恢复为 `404933466f5538475c0c898b21dc808a60b38ea2`
3. 回读 `threads` 表与 rollout 关键字段
4. 把第二刀定性为失败样本

## 11. `farm` 门槛

- 只有当 `NPC` 第二刀复验通过，才允许 `farm` 直接按同顺序进入第二刀；
- 如果 `NPC` 第二刀复验失败，`farm` 继续冻结，不重复当前顺序，转回重新审查第三层候选对象。
