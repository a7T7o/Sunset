# NPC 单线程状态投影最小修正预案（2026-03-12）

## 1. 样本定位

- 样本线程：`NPC`
- 线程 id：`019cda74-411d-7821-9f94-7e14e36e4e16`
- 目标：在不直接批量改库的前提下，为 `NPC` 建立首个可执行、可验收、可回滚的单线程状态投影最小修正预案。

## 2. 当前现场

### 2.1 状态库字段现状

- `cwd = D:/Unity/Unity_learning/Sunset_worktrees/NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`
- `rollout_path = C:/Users/aTo/.codex/sessions/2026/03/11/rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`
- `title = --+++*`
- `first_user_message = --+++*`

### 2.2 真实 worktree 现状

- worktree 路径：`D:/Unity/Unity_learning/Sunset_worktrees/NPC`
- 真实分支：`codex/npc-generator-pipeline`
- 真实 HEAD：`1f068ed1731316a07cb471b90bce8e8af7534277`

### 2.3 当前错误现象

- 用户点开 `NPC` 线程后掉回 `Sunset/main`；
- 客户端在根仓库误尝试切到 `codex/npc-generator-pipeline`；
- 随后报：`fatal: 'codex/npc-generator-pipeline' is already used by worktree at ...`；
- 分组可能短暂归位，但线程点击承接最终仍失败。

## 3. 为什么先从 NPC 开始

- `NPC` 同时呈现“真实 worktree 正常 + 首刀后 `git_branch` 已对齐 + 用户点击线程仍掉回根仓库”的完整冲突链；
- 它比 `farm` 更适合作为第一组失败样本，因为第二刀候选更集中；
- `farm-10.2.2-patch002` 暂时保留为高价值对照样本，用于验证后续推广是否成立，而不是本轮同步下刀对象。

## 4. 最小修正字段集排序（已按首刀结果重排）

1. `rollout_path`
2. `git_sha`
3. `session_index.jsonl` 中该线程的标题/索引残留（只读候选，当前不直接写）
4. `git_branch`
5. `cwd`

## 5. 字段影响判断

### 5.1 `rollout_path`
- 当前线程表字段已经指向 `NPC` worktree，但其 rollout 文件最早 `session_meta` 仍是 `cwd = D:\Unity\Unity_learning\Sunset`；
- 这说明线程点击恢复时，客户端仍可能回读根仓库起点语义；
- 在 `git_branch` 已对齐后，`rollout_path` 成为第二刀最优先候选。

### 5.2 `git_sha`
- 当前状态库 `git_sha` 仍停留在旧值 `404933466f5538475c0c898b21dc808a60b38ea2`；
- 真实 worktree HEAD 已是 `1f068ed1731316a07cb471b90bce8e8af7534277`；
- 若客户端恢复线程时同时校验分支与提交，这个旧值会继续把线程拉回旧现场；
- 它与 `rollout_path` 组合起来，构成“线程表字段看似正确，但点击承接仍回旧语义”的核心证据链。

### 5.3 `session_index.jsonl` 标题/索引残留
- 当前同一线程 id 仍存在 `Interpret malformed prompt` 与 `NPC` 两条索引记录；
- 这说明索引层仍有历史标题漂移残留；
- 该层目前只读核定成立，但未证明应该直接写入，因此暂列为只读高可疑项。

### 5.4 `git_branch`
- `git_branch` 首刀从 `main` 修到 `codex/npc-generator-pipeline` 本身没有错；
- 但真实点击现象已证明：单改它不足以穿透到线程点击承接层；
- 因此它从“第一刀最优先”降为“已排除的单字段不足项”，当前不应回滚后再重复试探。

### 5.5 `cwd`
- 当前 `cwd` 已指向正确 worktree；
- 这说明它不是当前首要冲突，但仍要在修正前后核对，避免误把正确字段改坏。

## 6. 改前备份方案

### 6.1 备份对象
- `C:/Users/aTo/.codex/state_5.sqlite`
- `NPC` 线程对应整行状态
- 当前 `NPC` worktree 的真实分支与 HEAD

### 6.2 备份命名
- 数据库文件备份：`state_5.sqlite.bak_<执行日期>_<执行前HEAD>`
- 单线程行导出：`npc_thread_state_backup_<执行日期>_<线程id>.json`
- worktree 现场记录：`npc_worktree_snapshot_<执行日期>.md`

### 6.3 已落盘备份
- 数据库文件备份：`C:\Users\aTo\.codex\state_5.sqlite.bak_2026-03-12_preT80_npc_gitbranch`
- 单线程行导出：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_thread_state_backup_2026-03-12_019cda74-411d-7821-9f94-7e14e36e4e16.json`
- worktree 现场记录：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_worktree_snapshot_2026-03-12.md`

## 7. 单线程最小执行顺序

1. 先完成数据库原文件备份；
2. 再导出 `NPC` 线程整行状态；
3. 核对 `NPC` 真实 worktree 的分支与 HEAD；
4. 首轮只修正 `git_branch`；
5. 首刀失败后，进入第二刀候选彻查；
6. 未经第二刀排序与单线程验收，不推广到 `farm`。

## 8. 单线程验收步骤

1. 记录用户“不重启直接打开”的结果；
2. 记录用户“重启后再打开”的结果；
3. 记录是否仍掉回 `Sunset/main`；
4. 记录是否仍触发根仓库切 `codex/npc-generator-pipeline`；
5. 记录是否仍报 worktree 占用；
6. 同步记录 UI 项目分组是否只是短暂归位，还是最终稳定。

## 9. 通过标准

- 点开 `NPC` 线程回到 `D:/Unity/Unity_learning/Sunset_worktrees/NPC`；
- 不再掉回 `Sunset/main`；
- 不再误触发根仓库切功能分支；
- 不再出现 `branch already used by worktree`；
- UI 项目分组稳定；
- 至少明确“重启后”和“不重启”两条路径分别是否通过。

## 10. 回滚方案

1. 用 `state_5.sqlite` 备份恢复原文件；
2. 如只改单线程字段，则以导出的整行状态回写恢复；
3. 再次运行只读基线脚本，确认 `NPC` 状态恢复到修正前快照；
4. 若修正后 UI 现象更差，立即停止推广到 `farm`。

## 11. 风险说明

- 当前仍不能承诺“一跑即恢复 UI”；
- 即使 `git_branch` 修正正确，客户端仍可能回读 rollout 旧语义、旧索引或当前 UI 激活工作区；
- 因此本预案已从“首刀准备”升级为“失败样本后的第二刀候选预案”；
- 当前仍禁止直接批量改 `state_5.sqlite`，也禁止未经第二刀排序就扩到 `farm`。

## 12. 首刀执行结果（2026-03-12）

- 本轮实际已执行：仅把 `git_branch` 从 `main` 修正为 `codex/npc-generator-pipeline`；
- 当前回读显示：`cwd` 未动、`git_sha` 未动、`rollout_path` 未动；
- 用户真实点击结果已成立：线程仍会回落，说明单改 `git_branch` 不足以修复点击承接层；
- 因此 `NPC` 首刀现在应正式重判为失败样本，而不是“待客户端实测中的可能成功样本”；
- 当前回滚决策为：暂不回滚 `git_branch`，因为该字段修正本身无副作用，且保留它更利于隔离第二刀变量；
- 因此本轮暂不把同样动作直接推广到 `farm`。

## 13. 当前状态

- 本文档继续保留为“首刀预案与失败样本归档”；
- 真正进入下一轮执行的最小写入方案，已经单独收口到 `NPC_第二刀最小修正预案_2026-03-12.md`；
- 当前第二刀执行口径固定为：rollout 恢复层主刀，`threads.git_sha` 联动，标题与索引层暂不先动。
