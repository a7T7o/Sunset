# NPC 单线程首刀执行记录（2026-03-12）

## 1. 写入动作
- 线程 id：`019cda74-411d-7821-9f94-7e14e36e4e16`
- 本轮仅写入字段：`git_branch`
- 改前值：`main`
- 改后值：`codex/npc-generator-pipeline`

## 2. 修后回读
- `cwd = D:\Unity\Unity_learning\Sunset_worktrees\NPC`
- `git_branch = codex/npc-generator-pipeline`
- `git_sha = 404933466f5538475c0c898b21dc808a60b38ea2`
- `rollout_path = C:\Users\aTo\.codex\sessions\2026\03\11\rollout-2026-03-11T09-12-57-019cda74-411d-7821-9f94-7e14e36e4e16.jsonl`
- `title = --+++*`
- `first_user_message = --+++*`

## 3. 备份文件
- 数据库备份：`C:\Users\aTo\.codex\state_5.sqlite.bak_2026-03-12_preT80_npc_gitbranch`
- 单线程整行导出：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_thread_state_backup_2026-03-12_019cda74-411d-7821-9f94-7e14e36e4e16.json`
- worktree 快照：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\npc_worktree_snapshot_2026-03-12.md`

## 4. 实际观测结果
- 该写入动作本身没有出错，状态库字段已成功从错误旧值对齐到真实分支；
- 用户真实点击结果已证明：线程仍会回落，未能稳定承接到 `NPC` worktree；
- 因此本轮不是“尚待实测的进行中样本”，而是“已执行最小修正、已遇到真实点击失败”的失败样本。

## 5. 当前结论
- 本轮已完成最小写入；
- 本轮修正本身没有错：它排除了一个已确认错误的状态字段，证明 `git_branch = main` 不是可继续保留的正确值；
- 用户真实点击结果已表明：单改 `git_branch` 不足以修复线程点击承接层；
- 因此本记录的正式结论是：`NPC` 首刀已执行、已观察到真实失败、当前不能作为可验收样本；
- 当前失败说明的问题层级不在 Git/worktree 本体，而在更深的线程点击承接元数据、UI 路由与工作区恢复逻辑。

## 6. 第二刀承接结论
- 本记录现在不再停留在“首刀失败”本身，而是作为第二刀的前置证据；
- 当前已锁定的第二刀方案为：先修 rollout 恢复层，再联动修正 `threads.git_sha`；
- 当前不回滚 `git_branch`，因为保留首刀结果更利于隔离第二刀变量；
- 下一轮真实修复不再围绕“要不要继续试 `git_branch`”展开，而是直接执行 `NPC_第二刀最小修正预案_2026-03-12.md`。
