# 总恢复最终结论纠偏审视报告（2026-03-13）

## 1. 审视对象
- 审视目标不是旧的 worktree 修复链，而是最近“`NPC`、`farm`、`spring-day1` 已总恢复通过”的结论口径。
- 重点核对象：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\spring-day1白名单恢复执行记录_2026-03-13.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\Codex迁移与规划\NPC与farm回归Sunset_main总收口执行记录_2026-03-13.md`
  - 当前主项目工作树中的 `DialogueUI.cs` 与 `DialogueManager.cs`
  - `codex/restored-mixed-snapshot-20260311` 中对应历史恢复来源

## 2. 路径判定

本次判定为 **Path B**。

原因：
- 锐评如果指出“当前实际情况与此前‘总恢复通过’结论不一致”，这个核心问题成立。
- 但需要局部纠偏，而不是把已成立的 `NPC/farm` 回根仓库、线程锚点回正语义一起推翻。

## 3. 事实核查表

| 争议点 | 当前事实 | 结论 |
|---|---|---|
| `spring-day1` 已完全恢复到原完成进度 | 当前主项目工作树 `DialogueUI.cs` 不含 `CanvasGroup`、`CurrentCanvasAlpha`、`IsCanvasInteractable`、`IsCanvasBlockingRaycasts` 等增强标记；`DialogueManager.cs` 不含 `PauseTime`、`ResumeTime`、`ForceCompleteOrAdvance`、`CompleteCurrentNodeImmediately` | ❌ 不成立 |
| 当前主项目工作树里的 `DialogueUI.cs` / `DialogueManager.cs` 已与增强版一致 | `codex/main-reflow-carrier` 上同名文件同样缺少上述增强标记；`codex/restored-mixed-snapshot-20260311` 上则能直接检出这些增强标记 | ❌ 不成立 |
| `SpringDay1_FirstDialogue.asset` 与 `DialogueDebugMenu.cs` 已回到主项目 | 当前仓库已存在这两个对象 | ✅ 成立 |
| `NPC/farm` 线程锚点已回到 `D:\Unity\Unity_learning\Sunset@main` | `state_5.sqlite` 与三条 rollout 的 `session_meta/first turn_context` 已核到 `cwd = D:\Unity\Unity_learning\Sunset`、`git_branch = main`、`git_sha = 9581c686543d14cbc8d3c1632a7df8c532280ec3` | ✅ 成立 |
| 因为 `spring-day1` 结论失真，就应整体推翻 `NPC/farm` 的回归收口 | 当前证据只反证了 `spring-day1` 的两个增强脚本未回到主项目；没有反证 `NPC/farm` 的业务回流与线程锚点回根仓库 | ⚠️ 不应扩大化 |

## 4. 我认同的部分
- 如果锐评指出“总恢复通过”结论过早，这一点成立。
- 如果锐评指出 `spring-day1` 仍停留在“部分恢复”，这一点成立。
- 如果锐评要求重新打开 `spring-day1` 的最后缺口治理，这个方向成立。

## 5. 我不直接照搬的部分
- 不采纳任何把 `NPC/farm` 也一并打回“未恢复”的扩大化口径。
- 不采纳任何把当前线程锚点修复、回根仓库语义修复一并否定的结论。
- 不采纳任何“整条总恢复路线全部无效、必须整体重做”的表述。

## 6. 独立判断
- 当前真实状态应重写为：
  - `NPC`：主项目可开发状态基本成立，线程锚点回根仓库成立。
  - `farm`：主项目可开发状态基本成立，线程锚点回根仓库成立。
  - `spring-day1`：`SpringDay1_FirstDialogue.asset` 与 `DialogueDebugMenu.cs` 已回主项目，但 `DialogueUI.cs`、`DialogueManager.cs` 的增强版仍未真正回到当前主项目工作树。
  - 因此“总恢复通过”当前不能成立。

## 7. 建议采用的最小修正版
- 主线结论改为：**Path B，局部纠偏，不整体推翻。**
- 下一步只应回到一个最小对象：
  - 用白名单方式把 `codex/restored-mixed-snapshot-20260311` 中 `DialogueUI.cs` 与 `DialogueManager.cs` 的增强版真实带回当前主项目工作树；
  - 完成直接回读后，再重写 `spring-day1` 与“总恢复”验收口径。
