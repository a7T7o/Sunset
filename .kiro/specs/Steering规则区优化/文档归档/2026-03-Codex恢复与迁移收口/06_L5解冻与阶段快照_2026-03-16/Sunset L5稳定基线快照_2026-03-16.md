# Sunset L5稳定基线快照（2026-03-16）

## 1. 快照身份
- 本文件是 `L5` 达成后的阶段快照，不是新的现行状态入口。
- 当前归档阶段目录：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口\06_L5解冻与阶段快照_2026-03-16`
- 与本快照一并归档的冻结样本目录：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口\06_L5解冻与阶段快照_2026-03-16\2026.03.16冻结文档汇总`
- 说明：
  - 冻结汇总目录中的正文保留了当时写入时的原始路径表述，这是阶段证据的一部分，不再逐份重写。

## 2. 稳定基线
- 当前稳定基线：
  - `main@9b9a6bd0dd7c5ee7d18cc82e3ea9da74a146bf9d`
- `main` 与 `origin/main`：
  - `ahead=0, behind=0`
- 当前真实工作树分支：
  - `codex/farm-1.0.0-1.0.1`
- 当前真实 `HEAD`：
  - `9b9a6bd0dd7c5ee7d18cc82e3ea9da74a146bf9d`
- 结论：
  - 当前稳定基线已经固化在 `main -> origin/main`；
  - 但当前用户实际工作树并不等于 `main`，后续治理或实现都必须继续先认真实际分支，再解释 IDE 视图与工作树状态。

## 3. 锁池与热文件
- 当前 `.kiro/locks/active`：
  - 空，仅剩 `.gitkeep`
- 最近一轮历史释放记录：
  - `A__Assets__000_Scenes__Primary.unity.lock.20260316-120551.released.json`
  - `A__Assets__YYY_Scripts__Story__UI__DialogueUI.cs.lock.20260316-120551.released.json`
  - `A__Assets__000_Scenes__Primary.unity.lock.20260316-164140.released.json`
- 当前定性：
  - 没有活动 A 类写锁；
  - `Primary.unity` 与上一轮对话 hot-file 已完成释放，不再处于锁占用态；
  - 之后若再次写 A 类热文件，仍按 `sunset-lock-steward` + 锁脚本流程执行。

## 4. Unity 运行现场
- 活动场景：
  - `Primary`
- 场景路径：
  - `Assets/000_Scenes/Primary.unity`
- 当前场景状态：
  - `isDirty=false`
  - `isLoaded=true`
  - `rootCount=8`
- 当前 Console 快照：
  - `0` 条 error
  - `1` 条 warning
- 当前 warning 内容：
  - `Assets\Editor\NPCPrefabGeneratorTool.cs(355,9): warning CS0618: 'TextureImporter.spritesheet' is obsolete...`
- 当前运行态结论：
  - 项目当前没有新的 Unity 项目级阻断；
  - 现行非阻断 warning 焦点已从先前的 `There are no audio listeners in the scene` 转为 `NPCPrefabGeneratorTool.cs(355,9)` 的 obsolete API 债。

## 5. 当前推荐入口
- 当前唯一状态入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset当前唯一状态说明_2026-03-16.md`
- 当前开工总览：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset L5开工清单与治理续办总览_2026-03-16.md`
- 当前总索引：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\文档重组总索引_2026-03-13.md`
- 当前治理续办入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 6. 当前线程结论
- `NPC`
  - 仍是最需要优先收口的实体资产与实现 WIP 风险点。
- `spring-day1`
  - 已从 hot-file 收口阶段退出，可继续真实剧情 / 对话新增需求。
- `农田交互修复V2`
  - `L5` 后只读复验已过；当前用户实际工作树已落在该线程分支上，但是否继续新需求仍需线程内裁决。
- `项目文档总览`、`导航检查`、`遮挡检查`
  - 当前更像文档 / 审计尾账，需要先固化阶段成果，再决定是否进入新一轮整改。
- `Skills和MCP`
  - `unityMCP` 已稳定；治理型 skills 已形成四件套，后续重点转为历史首版文档和线程文档收口。

## 7. 阶段样本与截图保留规则
- 冻结样本：
  - `2026.03.16冻结文档汇总` 已转入本阶段归档目录，后续不再悬挂在活跃治理层。
- 截图样本：
  - `D:\Unity\Unity_learning\Sunset\Assets\Screenshots\farm_checkpoint_primary_maincamera.png`
  - 当前应视为 `farm` 线程的验收证据，而不是默认基线资产。
- 当前建议：
  - 在 `farm` 线程明确结案或形成自己的任务提交前，暂不删除该截图；
  - 但它不应被混入治理类白名单提交，也不应长期冒充主项目稳定资源；
  - 后续应由 `farm` 线程决定是把该截图移为阶段证据，还是在任务收口时清理。

## 8. 当前仍需继续收口的事项
1. `NPC` 资产与实现 WIP 固化。
2. 03-15 历史首版、旧 `tasks.md` 兼容件与部分历史错码文档退役。
3. 是否要把“每次对话强制先走治理型 skill”写成更强约束。

## 9. 一句话结论
- `Sunset` 当前已经拥有稳定基线、空锁池、无项目级 Unity error、以及“线程唤醒 / 锁裁决 / 编码审计 / 阶段快照”治理四件套；接下来真正的主任务不再是救火，而是清旧债、固化线程 WIP，并在现行入口下恢复正常开发节奏。
