# Sunset 当前唯一状态说明（2026-03-17）

## 1. 文件身份
- 本文件是 Sunset 当前唯一状态入口。
- 它优先回答“项目现在到底处于什么状态、默认从哪里进入、哪些目录是 live、哪些只是记录层”。

## 2. 当前最重要的现实
- shared root 当前真实现场是：
  - `D:\Unity\Unity_learning\Sunset @ main`
- 阶段 20 的 A 阶段 Git 外科已经执行完成：
  - `codex/npc-roam-phase2-003` 已从错位导航提交 `2ecc2b75` 回正到 `c81d1f99`
  - shared root 已切回 `main`
- 阶段 20 的治理同步也已完成：
  - shared root 当前重新回到 neutral
- `git worktree list --porcelain` 当前只剩 shared root：
  - `D:\Unity\Unity_learning\Sunset`
- Sunset 想要恢复的目标模型仍然是：
  - `main-common + branch-task + checkpoint-first + merge-last`
- 当前这件事已经恢复成立：
  - shared root 常驻 `main`
  - 业务线程按闸机切入自己的 `codex/...`
  - 到 checkpoint 就归还
- `worktree` 当前只保留为例外机制：
  - 事故隔离
  - cleanroom
  - 特殊实验

## 3. 当前目录分层
- 规则正文源：
  - `D:\Unity\Unity_learning\Sunset\.kiro\steering`
- 现行活文档入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则`
- 正式治理工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
- 代办镜像区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 4. 当前治理口径
- `Codex规则落地` 承载治理正文：
  - 阶段目录
  - 阶段 `tasks.md`
  - 阶段 `memory.md`
  - 治理设计说明
- `000_代办/codex` 不再承载治理正文：
  - 不写根层 `memory.md`
  - 不写阶段正文
  - 只保留 TD 与读取入口

## 5. shared root 当前状态
- 当前 shared root 占用文档：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
- 当前裁定：
  - `current_branch = main`
  - `is_neutral = true`
  - 当前默认目标模型已经收紧为 `main-common + branch-task + checkpoint-first + merge-last`
  - 这意味着：shared root 已重新成为默认中性入口
  - 当前 `git-safe-sync.ps1` 已经补到第二波最小闸机：
    - occupied shared root 必须匹配 `owner_thread + current_branch`
    - shared root 若仍有 remaining dirty，`task` 模式会直接阻断

## 6. Unity / MCP 当前口径
- 当前 Unity / MCP 单实例入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-log.md`
- 结论：
  - shared root 在 `main` 只代表默认入口模型已恢复
  - 任何 Unity / MCP 读写仍需单独核单实例层

## 7. 三条业务线当前入口
- 当前 shared root 已重新成为可以进入的默认中性入口。
- 三条业务线后续都应统一按：
  - 先从 shared root 进入
  - 再过 preflight / startup guard
  - 再切到各自 `codex/...` 分支

## 8. 当前阶段判断
- `11_main-branch-only回归与worktree退役收口`：保留历史完成记录，但不再代表当前 live 现场
- `12_治理工作区归位与彻底清盘`：保留历史完成记录，但不再承接当前 live 回正
- `20_shared-root现场回正与物理闸机落地`：当前新的治理主线
- `03-16` 命名的共享表、dirty 归属表、活跃线程总表：
  - 只按历史快照理解
  - 不再作为当前实时进入裁定的第一依据
- 当前 live 回正与治理收口已经闭环完成：
  - shared root 不再停在 NPC 任务分支
  - Git worktree 挂载表里也已只剩 shared root
- 当前唯一常态入口重新稳定为 `D:\Unity\Unity_learning\Sunset @ main`
- 当前治理入口应直接进入：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\20_shared-root现场回正与物理闸机落地`

## 9. 默认进入顺序
1. 先读本文件
2. 再读：
   - `Sunset现行入口总索引_2026-03-17.md`
   - `基础规则与执行口径.md`
   - `Sunset Git系统现行规则与场景示例_2026-03-16.md`
   - 如任务涉及 Unity / MCP，再看：
     - `mcp-single-instance-occupancy.md`
     - `mcp-hot-zones.md`
3. 如属治理续办，再进入：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
4. 如只看待办，再进入：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 10. 一句话口径
- Sunset 现在已经回到 `main-common + branch-task + checkpoint-first + merge-last` 常态：shared root 常驻 `main`、Git worktree 挂载表只剩 shared root、业务线程只能在各自 `codex/...` 分支上真写，到 checkpoint 即归还。
