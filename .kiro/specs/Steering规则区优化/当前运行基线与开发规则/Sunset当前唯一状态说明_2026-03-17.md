# Sunset 当前唯一状态说明（2026-03-17）

## 1. 文件身份
- 本文件是 Sunset 当前唯一状态入口。
- 它优先回答“项目现在到底处于什么状态、默认从哪里进入、哪些目录是 live、哪些只是记录层”。

## 2. 当前最重要的现实
- shared root 已稳定恢复为：
  - `D:\Unity\Unity_learning\Sunset @ main`
- `git worktree list --porcelain` 当前只剩 shared root。
- Sunset 默认开发模型已经回到：
  - `shared root + branch-only`
- `worktree` 当前只保留为例外机制：
  - 事故隔离
  - cleanroom
  - 特殊实验

## 3. 当前目录分层
- 规则正文源：
  - `D:\Unity\Unity_learning\Sunset\.kiro\steering`
- 现行活文档入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则`
- 正式治理工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
- 代办镜像区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 4. 当前治理口径
- `Codex规则落地` 承载治理正文：
  - 阶段目录
  - 阶段 `tasks.md`
  - 阶段 `memory.md`
  - 治理设计说明
- `000_代办/codex` 不再承载治理正文：
  - 不写根层 `memory.md`
  - 不写阶段正文
  - 只保留 TD 与读取入口

## 5. shared root 当前状态
- 当前 shared root 占用文档：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
- 当前裁定：
  - `current_branch = main`
  - `is_neutral = true`
  - 可以作为默认进入现场

## 6. Unity / MCP 当前口径
- 当前 Unity / MCP 单实例入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-log.md`
- 结论：
  - shared root 在 `main` 只代表 Git 入口中性
  - 任何 Unity / MCP 读写仍需单独核单实例层

## 7. 三条业务线当前入口
- farm：
  - `D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-cleanroom001`
- NPC：
  - `D:\Unity\Unity_learning\Sunset @ codex/npc-roam-phase2-002`
- spring-day1：
  - `D:\Unity\Unity_learning\Sunset @ codex/spring-day1-story-progression-001`

## 8. 当前阶段判断
- `11_main-branch-only回归与worktree退役收口`：已完成并封板
- `12_治理工作区归位与彻底清盘`：已完成
- `09_强制skills闸门与执行规范重构`：继续承接后续闸门深化
- `03-16` 命名的共享表、dirty 归属表、活跃线程总表：
  - 只按历史快照理解
  - 不再作为当前实时进入裁定的第一依据
- 当前没有仍在执行中的 worktree 退役尾巴
- 如果后续继续推进治理，应在 `Codex规则落地` 下新开阶段，不再回写 `11`

## 9. 默认进入顺序
1. 先读本文件
2. 再读：
   - `Sunset现行入口总索引_2026-03-17.md`
   - `基础规则与执行口径.md`
   - `Sunset Git系统现行规则与场景示例_2026-03-16.md`
   - 如任务涉及 Unity / MCP，再看：
     - `mcp-single-instance-occupancy.md`
     - `mcp-hot-zones.md`
3. 如属治理续办，再进入：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
4. 如只看待办，再进入：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 10. 一句话口径
- Sunset 现在已经完成 shared root 回 `main`、worktree 退役、治理工作区归位和代办镜像降级；默认工作方式就是 `D:\Unity\Unity_learning\Sunset @ main` 下的 `main + branch-only`，但 Unity / MCP 仍要再过单实例层。
