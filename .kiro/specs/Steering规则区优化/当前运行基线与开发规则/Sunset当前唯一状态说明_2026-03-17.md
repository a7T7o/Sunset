# Sunset 当前唯一状态说明（2026-03-17）

## 1. 文件身份
- 本文件从 `2026-03-17` 起承接 Sunset 的当前唯一状态入口。
- `2026-03-16` 版状态说明已降级为上一稳定锚点，仅供归档回看。

## 2. 当前最重要的现实
- `11_main-branch-only回归与worktree退役收口` 已经真实完成。
- Sunset 当前已经恢复到：
  - `D:\Unity\Unity_learning\Sunset @ main`
  - 默认 workflow 为“共享根目录 + 分支”
- 当前治理分层应理解为：
  - `09`：继续承接强制 skills、启动闸门与执行规范的后续深化
  - `10`：保留事故链路、污染分支和 carrier 裁定的历史结论
  - `11`：已完成 Git/现场终局收口，不再继续承接新尾巴

## 3. 当前共享根目录状态
- 共享根目录：`D:\Unity\Unity_learning\Sunset`
- 当前真实 checkout：`main @ 763ba4a1`
- 当前口径：
  - 这里已经重新成为 Sunset 的默认中性现场
  - 治理类动作默认留在 `main`
  - 业务类动作默认先从这里进入，再切到对应 `codex/...` 分支
- 当前 `git status --short --branch` 为 clean，且 `origin/main` 已同步到同一终局提交。

## 4. 当前 worktree 口径
- 当前 registered worktree 只剩共享根目录本身。
- 事故期遗留的第二批物理容器已经全部退役：
  - `farm-1.0.2-cleanroom001`
  - `NPC_roam_phase2_continue`
  - `NPC_roam_phase2_rescue`
  - `spring-day1-story-progression-001`
- 用户红线已经锁定：
  - `worktree` 只能是迫不得已的事故隔离例外
  - 最终必须回到 `共享根目录 + 分支`
- 已真实退役的历史容器：
  - `main-reflow-carrier`
  - `NPC`
  - `farm-10.2.2-patch002`
- 当前结论：
  - `worktree` 已退出 Sunset 当前日常工作流
  - 今后只保留给高风险隔离、事故 cleanroom 或特殊实验例外使用

## 5. 三大业务线现状

### 5.1 farm
- 合法 continuation branch carrier 已明确：
  - `codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
- 当前判断：
  - 它已完成 shared root 下的 branch-only 检出验证
  - 它当前只保留“分支 carrier”语义
  - 对应 cleanroom worktree 已退役
- 当前唯一入口：
  - `D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-cleanroom001`

### 5.2 NPC
- 合法 continuation branch carrier 已明确：
  - `codex/npc-roam-phase2-002 @ 6e2af71b`
- 当前判断：
  - NPC 核心恢复与 branch-only 回归都已成立
  - `NPC_roam_phase2_continue` 与 `NPC_roam_phase2_rescue` 已退役
  - `npc_restore.zip` 已移出仓库工作树，不再作为现场噪音
- 当前唯一入口：
  - `D:\Unity\Unity_learning\Sunset @ codex/npc-roam-phase2-002`

### 5.3 spring-day1
- 当前 clean checkpoint 已明确：
  - `codex/spring-day1-story-progression-001 @ a9c952b7`
- 当前判断：
  - shared root 与 rescue 的字体双现场问题已完成证据导出与 dirty 清空
  - `spring-day1-story-progression-001` 物理容器已退役
- 当前唯一入口：
  - `D:\Unity\Unity_learning\Sunset @ codex/spring-day1-story-progression-001`

## 6. 其他线程当前判断
- `导航检查`
  - 当前是审计/重构准备线程
  - 不需要新建 worktree
  - 若后续进入整改，优先共享根目录 + 分支
- `遮挡检查`
  - 当前是审计成果固化线程
  - 不需要新建 worktree
  - 若后续进入整改，优先共享根目录 + 分支
- `项目文档总览`
  - 当前应继续降级为历史总览/归档辅助线
  - 不再争夺现行入口身份

## 7. 当前使用顺序
1. 先读本文件。
2. 再读：
   - `Sunset现行入口总索引_2026-03-17.md`
   - `000_代办/codex/11_main-branch-only回归与worktree退役收口/终局快照_2026-03-17.md`
   - `Sunset Git系统现行规则与场景示例_2026-03-16.md`
   - `基础规则与执行口径.md`
3. 如果属于治理续办，再进入：
   - `000_代办/codex/09_强制skills闸门与执行规范重构`
   - `000_代办/codex/10_共享根仓库分支漂移与现场占用治理`
   - `000_代办/codex/11_main-branch-only回归与worktree退役收口`
4. 如果需要回看 `11` 的执行过程，再补读：
   - `总进度与收口清单_2026-03-17.md`
   - `所有线程回归誓言.md`
   - `第二批worktree核验表_2026-03-17.md`
   - `分发Prompt_2026-03-17.md`

## 8. 当前一句话口径
- Sunset 当前已经完成 shared root 回 `main`、三条业务线 branch-only 验证和事故期 worktree 退役，现阶段的默认工作方式就是 `D:\Unity\Unity_learning\Sunset @ main` 下的 `main + branch-only`。
