# Sunset 当前唯一状态说明（2026-03-17）

## 1. 文件身份
- 本文件从 `2026-03-17` 起承接 Sunset 的当前唯一状态入口。
- `2026-03-16` 版状态说明已降级为上一稳定锚点，仅供归档回看。

## 2. 当前最重要的现实
- 项目并没有退回“大冻结救火期”。
- 当前治理主线已经分成三层：
  - `09_强制skills闸门与执行规范重构`
  - `10_共享根仓库分支漂移与现场占用治理`
  - `11_main-branch-only回归与worktree退役收口`
- 其中：
  - `09` 负责“先过启动闸门再做事”
  - `10` 负责“事故止血与分支承载面找回”
  - `11` 负责“彻底退役临时 worktree，回到 main + branch-only 常态”

## 3. 当前共享根目录状态
- 共享根目录：`D:\Unity\Unity_learning\Sunset`
- 当前真实 checkout：`codex/farm-1.0.2-correct001 @ 11e0b7b4`
- 当前口径：
  - 这里不是 `main`
  - 这里不是中性治理现场
  - 这里当前视为 `farm` 事故取证现场
- 因此当前共享根目录仍处于“业务写入冻结”：
  - 允许只读取证
  - 允许治理线程写治理文档与记忆
  - 不允许业务线程继续把这里当作正常实现现场

## 4. 当前 worktree 口径
- 当前所有 worktree 都应理解为：
  - 事故隔离容器
  - cleanroom / rescue / 历史取证点
  - 不是 Sunset 的默认工作方式
- 用户红线已经锁定：
  - `worktree` 只能是迫不得已的事故隔离例外
  - 最终必须回到 `共享根目录 + 分支`
- 已真实退役的历史容器：
  - `main-reflow-carrier`
  - `NPC`
  - `farm-10.2.2-patch002`
- 当前剩余 worktree：
  - `farm-1.0.2-cleanroom001`
  - `NPC_roam_phase2_continue`
  - `NPC_roam_phase2_rescue`
  - `spring-day1-story-progression-001`

## 5. 三大业务线现状

### 5.1 farm
- 合法 continuation branch carrier 已明确：
  - `codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
- 当前判断：
  - 它已编译通过
  - 它可以接替污染分支
  - 但对应 worktree `farm-1.0.2-cleanroom001` 仍只是临时 cleanroom 容器
- 当前剩余目标：
  - 不是继续长期停在 cleanroom
  - 而是给出回归 branch-only 的迁回方案

### 5.2 NPC
- 合法 continuation branch carrier 已明确：
  - `codex/npc-roam-phase2-002 @ 6e2af71b`
- 当前判断：
  - NPC 核心恢复已成立
  - `NPC_roam_phase2_continue` 当前只是 continuation 临时容器
  - `NPC_roam_phase2_rescue` 仍保留为取证/待退役现场
- 特别说明：
  - rescue 中剩余 4 个 TMP 资源 dirty 已确认归属 `spring-day1`，不归属 NPC
- 当前剩余目标：
  - 不是接受 NPC 长期停在 continuation worktree 里
  - 而是把 NPC 重新收回到共享根目录 + 分支模式

### 5.3 spring-day1
- 当前 clean checkpoint 已明确：
  - `codex/spring-day1-story-progression-001 @ a9c952b7`
- 当前判断：
  - 它不是本次分支漂移事故的主责线程
  - 但它与 4 个 TMP 资源尾巴直接相关
- 当前剩余目标：
  - 明确这 4 个 TMP 资源最终怎么处理
  - 明确共享根目录恢复后，spring-day1 如何只靠 branch-only 继续
  - 禁止把新 continuation worktree 当成默认提案

## 6. 其他线程当前判断
- `导航检查`
  - 当前是审计/重构准备线程
  - 不需要新建 worktree
  - 若后续进入整改，优先共享根目录 + 分支
- `遮挡检查`
  - 当前是审计成果固化线程
  - 不需要新建 worktree
  - 若后续进入整改，优先共享根目录 + 分支
- `项目文档总览`
  - 当前应继续降级为历史总览/归档辅助线
  - 不再争夺现行入口身份

## 7. 当前使用顺序
1. 先读本文件。
2. 再读：
   - `Sunset现行入口总索引_2026-03-17.md`
   - `Sunset Git系统现行规则与场景示例_2026-03-16.md`
   - `基础规则与执行口径.md`
3. 如果属于治理续办，再进入：
   - `000_代办/codex/09_强制skills闸门与执行规范重构`
   - `000_代办/codex/10_共享根仓库分支漂移与现场占用治理`
   - `000_代办/codex/11_main-branch-only回归与worktree退役收口`
4. 如果属于 branch-only 回归、线程归位、shared root 恢复，再优先补读：
   - `共享根目录dirty归属初版_2026-03-17.md`
   - 线程回复回来后，再看 `所有线程回归誓言.md` 这个回包汇总位

## 8. 当前一句话口径
- Sunset 当前不是“不能开发”，而是“共享根目录仍冻结、三条主线已找回合法分支承载面、第一批历史 worktree 已退役，但还没有完成从事故期临时容器回归到 `main + branch-only` 常态的最后收口”。
