# Sunset 当前唯一状态说明（2026-03-17）

## 1. 文件身份
- 2026-03-22 起，当前现行规范已由：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset当前规范快照_2026-03-22.md`
  接管。
- 本文件继续保留，但现在主要承载历史状态背景与阶段说明。
- 如果本文件与 `2026-03-22` 规范快照冲突，以规范快照为准。

## 2. 当前最重要的现实
- 当前现行默认模型已经改成：
  - `main-only + whitelist-sync + exception-escalation`
- `D:\Unity\Unity_learning\Sunset @ main` 仍是默认 shared root 语义入口。
- 但“当前在 `main`”不再自动等于：
  - 所有人都能无边界提交
  - Unity / MCP 一定可安全写
  - 不需要看 shared root 占用文档或热区状态
- `branch / worktree` 现在没有彻底废除，但只保留给例外：
  - branch carrier 迁入 `main`
  - 高危隔离
  - 特殊实验
  - 独立施工线
- `scene-build` 仍是当前明确保留的独立施工 worktree：
  - `D:\Unity\Unity_learning\Sunset_worktrees\scene-build-5.0.0-001`

## 3. 当前目录分层
- 规则正文源：
  - `D:\Unity\Unity_learning\Sunset\.kiro\steering`
- 现行活文档入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则`
- 正式治理工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
- 代办镜像区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`

## 4. 当前治理口径
- `Codex规则落地` 承载治理正文：
  - 阶段目录
  - 阶段 `tasks.md`
  - 阶段 `memory.md`
  - 治理设计说明
- `000_代办/codex` 不再承载治理正文：
  - 不写根层 `memory.md`
  - 不写阶段正文
  - 只保留 TD 与读取入口

## 5. shared root 当前状态
- 当前 shared root 占用文档：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
- 当前裁定：
  - 是否中性、当前分支、当前 owner，以 occupancy 文档的实时字段为准
  - 即使 `current_branch = main` 且 `is_neutral = true`，也只代表默认 Git 入口可读，不代表可以忽略单实例层和高危撞车
  - 当前 `git-safe-sync.ps1` 的现行实义已经变成：
    - `task` 模式允许在 `main` 上做白名单 `sync`
    - unrelated dirty 不再构成默认一刀切阻断
    - 只有当 occupancy 明确声明非中性，或线程进入 branch carrier / worktree 例外场景时，才继续要求 `owner_thread + current_branch` 精确匹配

## 6. Unity / MCP 当前口径
- 当前 Unity / MCP 单实例入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-log.md`
- 结论：
  - shared root 在 `main` 只代表默认入口模型已恢复
  - 任何 Unity / MCP 读写仍需单独核单实例层

## 7. 当前业务线程入口
- 普通业务线程当前默认入口都是：
  - 先从 shared root 进入
  - 先过 preflight / startup guard
  - 再判断自己是否命中高危目标、Unity / MCP 单写冲突或 branch carrier 迁入窗口
- 如果没有命中这些例外，就继续留在 `main` 语义下开发，并按白名单直接收口。
- 不再把“先切到自己的 `codex/...` 分支”当成普通业务线程的默认真相。

## 8. 当前阶段判断
- 当前真正的治理正文主线已经进入：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\25_vibecoding场景规范与main收口`
- 当前执行层壳仍保留在：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01`
- `11`、`12`、`20` 等旧阶段继续保留历史价值，但不再单独定义当前默认开发模型。
- `03-16` 命名的共享表、dirty 归属表、活跃线程总表：
  - 继续按历史快照理解
  - 不再作为当前实时进入裁定的第一依据

## 9. 默认进入顺序
1. 先读本文件
2. 再读：
   - `Sunset当前规范快照_2026-03-22.md`
   - `D:\Unity\Unity_learning\Sunset\AGENTS.md`
   - `Sunset现行入口总索引_2026-03-17.md`
3. 再进入当前活跃工作区正文：
   - 治理续办看 `Codex规则落地`
   - 执行壳 / 回收卡看 `2026.03.21_main-only极简并发开发_01`
4. 如任务涉及 Git / dirty / main 收口，再补读：
   - `基础规则与执行口径.md`
   - `Sunset Git系统现行规则与场景示例_2026-03-16.md`
5. 如任务涉及 Unity / MCP，再额外看：
   - `mcp-single-instance-occupancy.md`
   - `mcp-hot-zones.md`

## 10. 一句话口径
- Sunset 当前默认真相已经不是“普通线程先切 branch 再开发”，而是“默认 main-only 白名单提交，只有少数例外才升级到 branch / worktree / 治理强裁”。
