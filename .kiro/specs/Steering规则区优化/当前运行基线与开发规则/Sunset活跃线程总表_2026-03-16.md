# Sunset 活跃线程总表（2026-03-16）

## 1. 快照锚点
- 当前实际工作树分支：
  - `codex/npc-main-recover-001`
- 当前实际 `HEAD`：
  - `8aed637f`
- 默认基线仍是：
  - `main -> origin/main`
- 当前锁池：
  - 空，仅剩 `.kiro/locks/active/.gitkeep`
- Unity live 口径：
  - 继续沿用 [Sunset当前唯一状态说明_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset当前唯一状态说明_2026-03-16.md) 中已核实的 `Primary / isDirty=false / 无项目级阻断` 结论

## 2. 活跃线程总表

| 线程 | 类型 | 当前主线 | 当前可信状态 | 当前阻塞 / 风险 | 下一步 |
|---|---|---|---|---|---|
| `Codex规则落地` | 治理 | L5 后治理续办与入口重构 | 已完成 03-16 状态入口拆分、Git 规则重写、代办总入口挂接 | 活文档、共享表、skills 仍有后续治理债 | 继续重建共享表、修 skills/AGENTS、推进四件套规范 |
| `NPC` | 功能 / 资产 | 先固化资产与实现 WIP，再谈新功能 | 当前物理工作树已位于 `codex/npc-main-recover-001`，说明 NPC 线仍是最贴近现场的实现线程；同时存在 NPC Prefab / 工具 / 新脚本 WIP | 资产与代码 WIP 还没完成线程内固化 | 优先收口 NPC 资产与脚本，再决定下一功能点 |
| `spring-day1` | 功能 / 集成 | 从热修复阶段切回真实剧情 / 对话新增需求 | hot-file 收口已通过，不再占锁；当前只剩自己的记忆与可能的字体资产尾账 | 尚未明确下一项真实需求 | 先报下一项剧情 / 对话功能，不再围绕旧收口点打转 |
| `农田交互修复V2` | 功能 | 判断本轮结案还是进入新需求 | 只读复测通过，当前未暴露必须立即写补的 farm 专属问题；当前物理工作树已不再停留在 farm 分支 | 仍需决定是否继续承担 warning 清理或新需求 | 先做结案 / 新需求判断 |
| `项目文档总览` | 文档总览 | 对齐现行入口，再决定是否继续维护总览 | 现行入口已发生 03-16 纠偏 | 旧总览思路仍可能引用过时入口 | 先对齐新入口，再收口自己的文档状态 |
| `导航检查` | 审计 | 收口当前审计文档与重组状态 | 当前更像审计尾账，不是立即进入代码整改 | 文档重组 WIP 尚未固化 | 先固化当前文档与线程记忆 |
| `遮挡检查` | 审计 | 收口当前审计文档与重组状态 | 当前更像审计尾账，不是立即进入代码整改 | 文档重组 WIP 尚未固化 | 先固化当前文档与线程记忆 |
| `Skills和MCP` | 工具 / 治理 | 已归档，不再作为活跃线程排期 | `unityMCP` 主线稳定、治理 skills 已落地；用户已明确该线程直接归档 | 不再进入唤醒队列，只保留历史参考价值 | 后续相关治理动作直接由 `Codex规则落地` 承接 |

## 3. 当前统一判断
- 当前真正高风险、会继续互相阻塞的，不再是 `spring-day1` 的 hot-file，而是 `NPC` 的资产与实现 WIP。
- 当前治理线已经从“救火”切到“整理入口、重建共享表、收口规则执行机制”。
- 当前 `spring-day1`、`farm` 都已经从“阻断线程”降到“正常业务线程”。
- 当前物理工作树已经切到 `codex/npc-main-recover-001`，因此共享层快照判断也应以 `NPC` 收口优先为主，而不是继续沿用旧的 farm 现场印象。
- 当前最适合继续并行推进的顺序仍是：
  1. `NPC`
  2. `spring-day1`
  3. `农田交互修复V2`
  4. 文档 / 审计线程并行收口

## 4. 当前一句话口径
- 2026-03-16 当前活跃线程格局已经从“多线程相互救火”切回“当前物理 worktree 落在 `NPC` 线、`NPC` 优先收口，其余线程恢复正常节奏，`Skills和MCP` 已归档退出活跃排期”。
