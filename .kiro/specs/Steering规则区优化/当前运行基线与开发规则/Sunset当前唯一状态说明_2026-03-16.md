# Sunset 当前唯一状态说明（2026-03-16）

## 1. 文件身份
- 本文件从 `2026-03-16` 起，正式取代 `Sunset当前唯一状态说明_2026-03-13.md` 的“现行唯一状态入口”身份。
- `2026-03-13` 那一版状态正文已归档到：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口\06_L5解冻与阶段快照_2026-03-16\01_03-13历史状态锚点退役_2026-03-16\Sunset当前唯一状态说明_2026-03-13.md`
- 后续再出现“当前唯一状态说明”更新时，必须新建新的当前文件，不允许继续把最新状态叠写回旧日期文件名。

## 2. 当前唯一状态
- 默认开发现场固定为 `D:\Unity\Unity_learning\Sunset`。
- 默认开发分支固定为 `main`。
- 默认推送分支固定为 `origin/main`。
- 当前治理收口基线为 `main@9b9a6bd0dd7c5ee7d18cc82e3ea9da74a146bf9d`。
- 当前 `main` 已与 `origin/main` 同步。
- 当前 `Primary.unity` 与 `DialogueUI.cs` 已在 shared `main` 中 clean，对齐完成。
- 当前活动锁池为空，仅剩 `.kiro/locks/active/.gitkeep`。
- 当前项目已达到 `L5`，可以恢复全面正常开发。
- 这不等于当前物理工作树永远干净；当前是否可继续提交，仍以 `dirty / WIP` 归属表和 `preflight` 结果为准。
- `NPC`、`farm`、`spring-day1` 当前都按“主项目默认入口 + 必要时切 `codex/...` 分支”的语义工作，不再以独立 worktree 作为默认现场。
- `unityMCP` 当前可稳定读取活动场景 `Primary` 与 Console。
- `Skills和MCP` 线程已归档，后续相关 skills / MCP 治理动作统一由 `Codex规则落地` 承接。

## 3. 当前默认开发规则
### 治理类改动
- 适用范围：
  - 规则
  - 索引
  - `memory`
  - Hook 文档
  - 脚本说明
- 默认姿势：
  - 留在 `main`
  - 走 `git-safe-sync.ps1 -Mode governance`
  - 必须显式白名单

### 真实任务改动
- 适用范围：
  - `Assets/`
  - `Packages/`
  - `ProjectSettings/`
  - 业务脚本
  - 场景
  - Prefab
  - ScriptableObject
  - 资源
- 默认姿势：
  - 先在主项目目录读取现场与做 preflight
  - 再切到 `codex/...` 分支实现
  - 走 `git-safe-sync.ps1 -Mode task`
  - 不允许直接在 `main` 上收尾业务实现

### A 类共享热文件
- 当前应按 A 类对待的典型对象：
  - `Assets/000_Scenes/Primary.unity`
  - `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
- 规则：
  - 写前先查锁
  - 需要写就先拿锁
  - 写完先形成 checkpoint，再释放锁
  - 同文件跨 owner 转交前，上一 owner 必须已经进入 `Committed`、`Parked` 或 `Abandoned`
  - 禁止 `Mixed-in-place`

### worktree 的定位
- 不是默认开发方式。
- 只用于：
  - 高风险隔离
  - 故障修复
  - 特殊实验

## 4. 当前入口分层
### 当前有效入口
- 当前状态入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset当前唯一状态说明_2026-03-16.md`
- 当前开工总览：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset L5开工清单与治理续办总览_2026-03-16.md`
- 当前总索引：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset现行入口总索引_2026-03-16.md`

### 当前治理续办入口
- 新的代办工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`
- 该工作区负责：
  - 现行入口与活文档重构
  - skills / AGENTS / 执行机制重构
  - 线程解冻与 WIP 收口
  - 冻结文档归档与版本快照
  - 四件套与代办体系重构
- 当前四件套与代办规范入口：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset工作区四件套与代办规范_2026-03-16.md`

### 当前历史入口
- 历史归档目录：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口`
- `Codex迁移与规划` 当前身份：
  - 迁移期历史入口
  - 旧阶段索引
  - 路由页

## 5. 当前默认基线与线程 WIP 边界
- 当前默认基线只包含已经同步到 `main -> origin/main` 的稳定内容，以及现行规则 / 当前入口 / 当前代办入口。
- 以下内容仍属于线程自有 WIP，不得混入默认基线提交：
  - `Assets/100_Anim/NPC/`
  - `Assets/222_Prefabs/NPC/`
  - `Assets/Sprites/NPC/*.meta`
  - 其他线程自己的 `memory.md` / `memory_0.md` dirty
- 业务线程自己的资源、Prefab、meta、截图、实验样本，不得伪装成治理基线的一部分。

## 6. 当前共享协作入口
- 活跃线程总表：`Sunset活跃线程总表_2026-03-16.md`
- 文件认领与锁定表：`Sunset文件认领与锁定表_2026-03-16.md`
- 当前工作树 dirty 与 WIP 归属表：`Sunset当前工作树dirty与WIP归属表_2026-03-16.md`
- 统一汇总验收模型：`Sunset统一汇总验收模型_2026-03-16.md`
- 线程统一汇总模板：`Sunset线程统一汇总模板_2026-03-16.md`
- 工作区四件套与代办规范：`Sunset工作区四件套与代办规范_2026-03-16.md`

## 7. 当前非阻断遗留
- 最近一次 Unity Console 快照未读到新的 error；当前仅见 `Assets\Editor\NPCPrefabGeneratorTool.cs(355,9)` 的 obsolete API warning，且不构成项目级阻断。
- `farm` 责任 warning 仍需后续分线程继续清理。
- NPC 工具链仍有 obsolete API 债待后续线程处理。
- `03-13` 旧状态正文与 `03-15` 首版共享表已退出活目录并完成归档；当前活目录仍保留一个兼容 `tasks.md` 路由页，以及部分历史记忆里的旧路径引用，这些历史引用不应再被当作现行入口。
- 当前现行总索引已升级为 `Sunset现行入口总索引_2026-03-16.md`；旧 `文档重组总索引_2026-03-13.md` 仅保留历史路由身份。
- 核心活文档、`.kiro/steering` 与核心治理 skills 当前按 `UTF-8` 读取健康；此前大量“乱码”属于 `Default` 读法噪音，不应再泛化成文件本体损坏。
- 当前唯一已实锤修复过的编码对象仍是 `sunset-thread-wakeup-coordinator/agents/openai.yaml`；后续重点转为持续巡检新 skills 元数据和历史归档对象，而不是盲目重写活文档。
- 当前已核实健康并可直接使用的治理相关 skills 包括：`skills-governor`、`global-learnings`、`sunset-workspace-router`、`sunset-unity-validation-loop`、`sunset-thread-wakeup-coordinator`、`sunset-lock-steward`、`sunset-doc-encoding-auditor`、`sunset-release-snapshot`。

## 8. 当前使用顺序
1. 先读本文件。
2. 再读 `Sunset L5开工清单与治理续办总览_2026-03-16.md`。
3. 如果要推进治理续办，进入 `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`。
4. 如果要追历史，进入归档目录，优先看 `06_L5解冻与阶段快照_2026-03-16` 下的退役与快照目录。
