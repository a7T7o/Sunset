# Sunset L5开工清单与治理续办总览（2026-03-16）

## 1. 当前坐标
- 当前默认开发现场：`D:\Unity\Unity_learning\Sunset`
- 当前默认开发分支：`main`
- 当前默认推送分支：`origin/main`
- 当前治理收口基线：`main@9b9a6bd0dd7c5ee7d18cc82e3ea9da74a146bf9d`
- 当前 `main` 与 `origin/main`：`ahead=0, behind=0`
- 当前 Unity live 场景：`Primary`
- 当前活动锁池：空，仅剩 `.kiro/locks/active/.gitkeep`
- 当前 Unity Console：无新的项目级阻断；最近一次快照读取到 `Assets\\Editor\\NPCPrefabGeneratorTool.cs(355,9)` 的 obsolete API warning

## 2. 当前最终定性
- 项目已通过治理收口，可以恢复全面正常开发。
- 当前已经达到 `L5`，但这里的 `L5` 含义是“回到按规则运行的常态开发”，不是回到无边界、无分支、无锁、无白名单的旧状态。
- 也就是说：
  - 可以继续开发；
  - 可以继续并行；
  - 但仍必须遵守 `main + governance`、`codex/... + task`、A 类热文件写锁、白名单同步这些现行纪律。

## 3. 从今天起的默认开发姿势
### 3.1 治理类改动
- 适用范围：
  - 规则
  - 索引
  - `memory`
  - Hook 文档
  - 脚本说明
- 默认姿势：
  - 留在 `D:\Unity\Unity_learning\Sunset @ main`
  - 先读 [Sunset当前唯一状态说明_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset当前唯一状态说明_2026-03-16.md)
  - 带着本轮 `IncludePaths` / `ScopeRoots` 再做 `preflight`
  - 最后走 `git-safe-sync.ps1 -Mode governance`

### 3.2 真实任务改动
- 适用范围：
  - `Assets/`
  - `Packages/`
  - `ProjectSettings/`
  - 业务脚本
  - 场景
  - Prefab
  - ScriptableObject
  - 资源
- 默认姿势：
  1. 先在 `D:\Unity\Unity_learning\Sunset @ main` 读现场；
  2. 先看共享状态层与锁表；
  3. 确认是否涉及 A 类热文件；
  4. 如需实现，切到 `codex/...` 分支；
  5. 用 `git-safe-sync.ps1 -Mode task` 白名单提交。

### 3.3 A 类热文件
- 当前应按 A 类对待的典型对象：
  - `Assets/000_Scenes/Primary.unity`
  - `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
  - 后续若出现新的共享核心场景/脚本，再由治理层补入。
- 规则：
  - 改之前先查锁；
  - 需要写就先拿锁；
  - 改完先形成 checkpoint，再释放锁；
  - 同文件跨 owner 转交前，上一 owner 必须已经 `Committed`、`Parked` 或 `Abandoned`。

### 3.4 worktree 的正确定位
- 不是默认开发方式。
- 仅用于：
  - 高风险隔离
  - 故障修复
  - 特殊实验
  - 像本轮这种 shared hot-file 固化这类短期治理性隔离动作

## 4. 当前第一批开工顺序
## 第一梯队：先收口有“实体资产流失风险”的线程
### `NPC`
- 当前风险：
  - 持有 `Assets/100_Anim/NPC/`
  - 持有 `Assets/222_Prefabs/NPC/`
  - 持有 `Assets/Sprites/NPC/*.meta`
  - 这批内容很多还是未跟踪或线程私有 WIP
- 当前首要动作：
  - 不是先写新功能；
  - 而是先把现有 NPC 资产 WIP 固化到自己的任务分支，避免资产继续长期飘在 shared worktree 外围。

## 第二梯队：健康业务线程，可立即恢复推进
### `spring-day1`
- 当前状态：
  - hot-file 固化与 Missing script 清理已完成；
  - 不再占用锁；
  - 可转入真正的剧情/交互新增需求。
- 当前首要动作：
  - 明确“Day1 下一步要做什么新功能”，而不是继续围着旧收口点兜圈。

### `农田交互修复V2`
- 当前状态：
  - 只读复测通过；
  - 当前无强制写补需求；
  - 主链健康。
- 当前首要动作：
  - 决定是结案封存这一轮补丁，还是继续推进新的农田交互需求；
  - 若继续推进，再按正常任务分支方式开工。

## 第三梯队：文档/审计/工具线程，可并行推进
### `项目文档总览`
- 当前问题：
  - 对现行入口的同步还不够新；
  - 需要重新对齐当前活文档目录。

### `导航检查`
- 当前问题：
  - 仍有文档重组中的删改与未跟踪项；
  - 尚未明确是继续进入整改，还是先以审计基线结案。

### `遮挡检查`
- 当前问题：
  - 同样处于文档重组与阶段归档之间；
  - 需要先把自己当前阶段提交固化。

### `Skills和MCP`
- 当前状态：
  - 用户已明确该线程直接归档；
  - 后续相关 skills / MCP 治理动作由 `Codex规则落地` 线程直接承接。

## 5. 当前遗留问题总表
## 5.1 业务与运行层遗留
- `farm` 仍有责任线程级 warning 待清：
  - `_hasPendingFarmInput` obsolete
  - `PlacementNavigator` 缺少 `PlayerAutoNavigator`
  - `PlacementPreview` 缺少 Sprite
- NPC 工具链仍有 API 升级债：
  - `TextureImporter.spritesheet` obsolete
- 当前 Console 最近一次快照仅见一条非阻断 warning：
  - `Assets\Editor\NPCPrefabGeneratorTool.cs(355,9): warning CS0618: 'TextureImporter.spritesheet' is obsolete...`

## 5.2 资产与工作树风险
- `NPC` 相关动画 / Prefab / Sprite meta 仍是最需要优先收口的实体资产风险点。
- 冻结阶段遗留的一批线程文档与 memory 仍散落在 shared worktree 中，虽不阻断开发，但需要分线程分批固化。

## 5.3 活文档健康问题
- 当前已经建立一组 03-16 现行健康文档：
  - [Sunset当前唯一状态说明_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset当前唯一状态说明_2026-03-16.md)
  - [基础规则与执行口径.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/基础规则与执行口径.md)
  - [Sunset Git系统现行规则与场景示例_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset%20Git系统现行规则与场景示例_2026-03-16.md)
  - [Sunset现行入口总索引_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset现行入口总索引_2026-03-16.md)
  - [Sunset活跃线程总表_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset活跃线程总表_2026-03-16.md)
  - [Sunset文件认领与锁定表_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset文件认领与锁定表_2026-03-16.md)
  - [Sunset当前工作树dirty与WIP归属表_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset当前工作树dirty与WIP归属表_2026-03-16.md)
  - [Sunset统一汇总验收模型_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset统一汇总验收模型_2026-03-16.md)
  - [Sunset线程统一汇总模板_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset线程统一汇总模板_2026-03-16.md)
  - [Sunset工作区四件套与代办规范_2026-03-16.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset工作区四件套与代办规范_2026-03-16.md)
  - 当前 `memory.md` 的新增记录
  - 本文档
- 旧 `文档重组总索引_2026-03-13.md` 已降级为历史路由页，不再承担现行总索引职责。
- 当前已退出活目录、转入 `06_L5解冻与阶段快照_2026-03-16` 的旧入口：
  - `01_03-13历史状态锚点退役_2026-03-16/Sunset当前唯一状态说明_2026-03-13.md`
  - `02_03-15首版共享表与模型退役_2026-03-16/` 下的 `Git`、`多线程分析`、`线程总表`、`锁表`、`dirty 表`、`验收模型`、`汇总模板` 首版文档
- 当前仍需继续治理的旧入口或编码巡检对象：
  - `tasks.md` 已缩退为兼容路由页，但必须继续禁止把新代办写回这里
  - `Codex迁移与规划/README_迁移结束与路由说明_2026-03-14.md` 等历史路由页仍需持续保持与现行入口一致
  - 个别新建 skill 的 agent 元数据需要持续做编码巡检

## 5.4 技能层健康问题
- 当前全局治理 skill 中：
  - `C:\Users\aTo\.codex\skills\skills-governor\SKILL.md` 当前按 `UTF-8` 读取健康；此前主要是 `Default` 读法造成的假乱码
  - `C:\Users\aTo\.codex\skills\global-learnings\SKILL.md` 已复核为健康可读
- 当前仍正常可读的 Sunset / 治理相关 skill：
  - `skills-governor`
  - `global-learnings`
  - `sunset-workspace-router`
  - `sunset-unity-validation-loop`
  - `sunset-thread-wakeup-coordinator`
  - `sunset-lock-steward`
  - `sunset-doc-encoding-auditor`
  - `sunset-release-snapshot`
- 当前结论：
  - skills 机制本身是可用的；
  - 当前唯一已实锤修复过的 agent 编码问题是 `sunset-thread-wakeup-coordinator/agents/openai.yaml`；
  - 目前抽查到的核心活文档、steering 文档与核心治理 skill，均更接近“UTF-8 健康 + Default 假乱码噪音”，不是文件本体普遍损坏；
  - 当前不应再把“历史长卷里偶发的乱码片段”泛化成“活文档全体损坏”，更不应盲目重写健康文件；
  - 后续新增治理 skill 和 Sunset 专项 skill 仍需要持续校验，不应再靠口头假设它们“应该能用”。

## 6. 现在还存在的治理漏洞
- 漏洞 1：
  - 旧全局 `tasks.md` 虽已缩退为兼容路由页，但只要有人继续把新代办写回这里，活入口就会再次膨胀。
- 漏洞 2：
  - 历史路由页如果继续指向退役文件，会把线程重新带回旧入口；`Codex迁移与规划` 这类目录必须持续校正。
- 漏洞 3：
  - `git-safe-sync.ps1` 的 `governance` 默认白名单已经在脚本层收紧；
  - 当前剩余风险变成“有人仍沿用旧认知，以为 governance 会自动兜住 `.kiro/specs/...` 与 `.codex/threads/...`”，因此必须持续强调显式 `IncludePaths` / `ScopeRoots`。
- 漏洞 4：
  - “线程唤醒后先处理遗留状态，再谈新功能”虽已形成 prompt 与 skill，但还没对全部业务线程完成一轮真实复工闭环。
- 漏洞 5：
  - 新增 skill 虽已开始落地，但仍需要持续用真实任务验证编码、触发条件和默认 prompt；
  - 否则很容易再次退化成“写了 skill 目录，但没有执行约束”的假落地。

## 7. 当前治理 skills 状态
- `sunset-thread-wakeup-coordinator`（已落地）
  - 用途：统一管理线程解冻、首要动作、汇报格式、锁前检查，并直接产出可发送 prompt。
- `sunset-lock-steward`（已落地）
  - 用途：把查锁、发锁、解锁、交接判定、`Committed/Parked/Abandoned` 审核做成固定流程。
- `sunset-doc-encoding-auditor`（已落地）
  - 用途：扫描活文档 / 线程文档 / skill 文本中的错码文件，并输出“可直接修 / 必须重写 / 暂缓处理”三类清单。
- `sunset-release-snapshot`（已落地）
  - 用途：在每次阶段性收口后自动生成版本快照、当前锁池、当前 Console、当前活文档入口与线程状态摘要。

## 8. 当前建议的治理续办任务
### G1. 活入口兼容件收短
- 目标：
  - 旧 `tasks.md` 只保留最小兼容路由，不再承接新任务；
  - 历史路由页统一只指向 03-16 现行入口或 `06_L5` 归档目录。
- 优先级：
  1. `tasks.md`
  2. `Codex迁移与规划/README_迁移结束与路由说明_2026-03-14.md`
  3. 当前总索引与现行状态说明中的历史路径一致性
  4. 剩余错码对象的编码巡检

### G2. skills 编码巡检
- 目标：
  - 持续复核 `global-learnings`、全局注册表与 Sunset 专项 skills 的一致性
  - 继续用真实任务校准 `sunset-thread-wakeup-coordinator`、`sunset-lock-steward`、`sunset-doc-encoding-auditor`、`sunset-release-snapshot`
  - 视需要继续补更细的治理技能，而不是重新回到手工中继
  - 把“终端假乱码”与“真实编码损坏”严格区分，不再泛化误报

### G3. 线程 WIP 固化
- 目标：
  - 优先收口 `NPC` 资产类 WIP
  - 再收口 `导航检查`、`遮挡检查`、`项目文档总览` 的文档改动

### G4. 冻结汇总目录归档
- 目标：
  - `2026.03.16冻结文档汇总` 已归入：
    - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口\06_L5解冻与阶段快照_2026-03-16`
  - 当前同阶段还新增了：
    - `Sunset L5稳定基线快照_2026-03-16.md`
  - 后续这一阶段资料不再留在当前活跃治理层。

## 9. 用户 / 治理线程 / 业务线程分工
## 9.1 你现在最需要做的事
- 不需要再当中继站来回转述所有线程上下文。
- 你现在更适合做的事：
  - 按优先级唤醒线程；
  - 审批“这条线程是先收遗留，还是开新功能”；
  - 决定哪些治理续办先做，哪些可以后推。

## 9.2 我接下来最适合继续做的事
- 持续收紧活文档入口与兼容路由页；
- 修历史文档与 skill agent 元数据中已经实锤的编码漂移；
- 把当前开工流程和共享状态层继续压实；
- 把治理续办工作持续沉淀到 `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`；
- 视你选择，继续直接接管 `NPC` 资产收口或文档治理线。

## 9.3 各线程默认第一动作
- `NPC`：先固化资产 WIP，再谈新功能。
- `spring-day1`：先明确下一项剧情/对话新增需求。
- `农田交互修复V2`：先决定结案还是继续新需求。
- `项目文档总览`：先对齐现行活文档入口，再收口自己的文档改动。
- `导航检查`：先固化文档重组结果，再决定是否进入代码整改。
- `遮挡检查`：先固化当前审计与文档迁移结果，再决定是否进入代码整改。
- `Skills和MCP`：已归档，不再进入新的唤醒或排期动作。

## 10. 各线程唤醒 Prompt 成品
## `NPC`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你先不要写新功能。你当前的第一优先级是收口你手里的实体资产 WIP：
- Assets/100_Anim/NPC/
- Assets/222_Prefabs/NPC/
- Assets/Sprites/NPC/*.meta

请你现在只做以下事情：
1. 重新核对你当前真实工作目录 / 分支 / HEAD / dirty 范围。
2. 明确哪些是必须保留的 NPC 资产，哪些是临时产物。
3. 在你的 `codex/...` 任务分支上，用 task 白名单方式把应保留的 NPC 资产与相关记忆固化。
4. 完成后按固定格式汇报：
   - 本轮固化了哪些文件
   - 还有哪些 NPC WIP 未固化
   - 下一步你准备推进什么新功能

本轮禁止顺手碰 Primary.unity、DialogueUI.cs、GameInputManager.cs，除非你先说明并申请 A 类热文件写锁。
```

## `spring-day1`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你的上一轮 hot-file 固化、Missing script 清理和对话验收已经通过。
你现在不再处理旧收口问题，而是请直接进入下一项真实剧情 / 对话新增需求判断。

请你现在只做以下事情：
1. 重新核对你当前真实工作目录 / 分支 / HEAD。
2. 告诉我 Day1 剧情线接下来最值得推进的一项新功能是什么。
3. 说明这项新功能会不会碰：
   - Primary.unity
   - GameInputManager.cs
   - 其他 A 类共享热文件
4. 给出你的下一轮最小 checkpoint 计划与验收点。

如果你准备开工真实实现，按 codex 任务分支 + task 白名单同步执行。
```

## `农田交互修复V2`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你的上一轮只读复测通过，当前没有强制写补需求。
你现在先不要默认继续修 bug，而是先判断本线程当前是“结案”还是“进入新需求”。

请你现在只做以下事情：
1. 重新核对你当前真实工作目录 / 分支 / HEAD。
2. 判断当前农田线程是：
   - 本轮补丁已可结案
   - 继续推进新的农田交互需求
3. 如果继续推进，明确下一项最小目标、涉及文件、是否会碰 A 类热文件。
4. 补充汇报当前仍属于你负责的 warning 清理计划。
```

## `项目文档总览`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你现在的第一动作不是扩写新总览，而是先对齐现行入口。
请先以 `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/` 为当前唯一活文档根，再决定你的总览层应该保留什么、归档什么。

请你现在只做以下事情：
1. 重新核对当前工作目录 / 分支 / HEAD / dirty。
2. 明确你手上的文档改动哪些应固化，哪些应归档。
3. 用 governance 方式收口你当前文档改动。
4. 汇报你后续是否仍需保持活跃，还是降级为历史总览线程。
```

## `导航检查`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你当前首先处理的是“文档重组与阶段状态收口”，不是直接进入代码整改。

请你现在只做以下事情：
1. 核对当前工作目录 / 分支 / HEAD / dirty。
2. 明确你现在的文档删改、迁移、未跟踪项分别是什么。
3. 先把本轮审计阶段文档固化。
4. 再告诉我：你下一步是继续做代码整改，还是先以审计基线阶段结案。
```

## `遮挡检查`
```text
全局已解冻，项目当前已恢复到可全面继续开发状态。

你当前首先处理的是“当前审计成果的固化与阶段口径澄清”，不是直接默认继续改代码。

请你现在只做以下事情：
1. 核对当前工作目录 / 分支 / HEAD / dirty。
2. 说明你当前持有的文档重组 / 删除 / 未跟踪内容。
3. 先固化本轮遮挡审计成果。
4. 再判断后续是否要进入代码整改阶段，并给出最小目标。
```

## `Skills和MCP`
- 已归档，不再下发新的复工 Prompt。
- 后续相关 skills / MCP 治理动作统一由 `Codex规则落地` 线程直接承接。

## 11. 当前一句话结论
- `Sunset` 现在已经回到可全面继续开发的常态；
- 但后续最值得优先继续做的不是再救火，而是：
  - 收口 `NPC` 资产 WIP
  - 修活文档与 skill 的错码
  - 让各线程按现行规则有序复工
