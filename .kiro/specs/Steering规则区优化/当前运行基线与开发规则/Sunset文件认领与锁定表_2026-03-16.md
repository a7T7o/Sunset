# Sunset 文件认领与锁定表（2026-03-16）

## 1. 快照锚点
- 默认基线口径仍是：
  - `main -> origin/main`
- 当前物理锁目录：
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\active`
- 当前活动锁池：
  - 空，仅剩 `.gitkeep`
- 当前表是 2026-03-16 的共享锁与认领快照，不替代线程自己的记忆与 checkpoint。

## 2. 状态定义
- `治理维护中`：默认允许只读，但涉及规则入口改写时需先同步治理线。
- `占用中`：当前 owner 正在推进或正在收口，未经同步不直接改。
- `共享只读`：允许读，不默认写；如需写，先走查锁 / 认领 / 分支流程。
- `待线程收口`：当前没有活动锁，但 owner 线程仍有未固化 WIP，需要先由原线程收口。

## 3. 当前认领表

| 路径 / 路径根 | 当前 owner 线程 | 当前状态 | 说明 | 下一步口径 |
|---|---|---|---|---|
| `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/` | `Codex规则落地` | `治理维护中` | 现行状态、Git 规则、当前入口都在这里 | 允许只读；若继续重构活文档，先按治理白名单推进 |
| `.kiro/specs/000_代办/codex/` | `Codex规则落地` | `占用中` | 当前治理续办与代办总入口 | 后续治理续办默认从这里承接 |
| `Assets/000_Scenes/Primary.unity` | 无活动 owner | `共享只读` | 当前锁已释放，文件已从上一轮 hot-file 收口中恢复 | 如需继续写，先新申请 A 类文件写锁 |
| `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs` | 无活动 owner | `共享只读` | 当前无活动锁，不再视为项目级阻断 | 如需继续写，先新申请 A 类文件写锁 |
| `Assets/YYY_Scripts/Story/UI/DialogueUI.cs` | 无活动 owner | `共享只读` | 上一轮对话显隐/字体链收口已通过，当前不再占锁 | 后续如需继续实现，仍走任务分支白名单 |
| `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese*.asset` | `spring-day1` | `待线程收口` | 当前仍见字体资产 WIP 痕迹，但已不构成主阻断 | 由 `spring-day1` 决定是继续新需求还是先固化尾账 |
| `Assets/100_Anim/NPC/` | `NPC` | `占用中` | 仍属于 NPC 资产 WIP 风险区 | 优先由 `NPC` 线程收口 |
| `Assets/222_Prefabs/NPC/` | `NPC` | `占用中` | 当前已有 Prefab WIP 在推进 | 优先由 `NPC` 线程收口 |
| `Assets/Sprites/NPC/*.meta` | `NPC` | `待线程收口` | 仍属于 NPC 资源链的一部分 | 不混入默认基线 |
| `Assets/YYY_Scripts/Controller/NPC/` | `NPC` | `占用中` | 当前出现新的 NPC 行为脚本 WIP | 由 `NPC` 在线程分支内收口 |
| `Assets/Editor/NPCPrefabGeneratorTool.cs` | `NPC` | `占用中` | 当前工具脚本已有 WIP，同时仍挂着 obsolete 升级债 | 由 `NPC` / 工具治理线收口 |
| `.kiro/specs/NPC/` | `NPC` | `占用中` | NPC 工作区仍在推进中 | 先固化再谈新功能 |
| `.kiro/specs/900_开篇/spring-day1-implementation/` | `spring-day1` | `待线程收口` | 当前业务主线已从热修复切回新需求判断 | 由 `spring-day1` 确认下一项真实需求 |
| `.kiro/specs/农田系统/` | `农田交互修复V2` | `共享只读` | 本轮只读复测已通过，暂不需要写改 | 若进入新需求，再重新认领相关实现路径 |
| `.kiro/specs/999_全面重构_26.03.15/导航检查/` | `导航检查` | `待线程收口` | 当前以审计与文档重组为主 | 先固化当前文档状态 |
| `.kiro/specs/999_全面重构_26.03.15/遮挡检查/` | `遮挡检查` | `待线程收口` | 当前以审计与文档重组为主 | 先固化当前文档状态 |
| `.kiro/about/` | `项目文档总览` | `共享只读` | 作为接手资料继续存在，但不是当前唯一入口 | 总览线程应先对齐现行入口后再决定是否继续维护 |

## 4. 当前锁纪律
- 当前“没有活动锁”不等于“任何线程可以顺手改共享热文件”。
- 当前正确理解是：
  - 没有人持有活动写锁；
  - 因而下一位 writer 必须先显式查锁、上锁；
  - 如果只是只读核查，不需要拿写锁。
- 后续若继续扩共享热文件列表，应统一补回本表，不允许只写在线程汇报里。

## 5. 当前一句话口径
- 2026-03-16 当前锁池为空，但 `Primary.unity` / `GameInputManager.cs` 仍然是共享热文件；下一位真正要写的人，必须重新查锁并显式认领。
