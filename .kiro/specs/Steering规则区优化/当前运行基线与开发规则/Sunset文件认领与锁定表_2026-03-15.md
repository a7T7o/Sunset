# Sunset 文件认领与锁定表（2026-03-15 首版）

## 1. 使用说明
- 本表用于解决“谁正在改什么、谁当前只读、哪些路径改动前必须先同步”的问题。
- 认领状态定义：
  - `占用中`：未经 owner 同步，不直接改
  - `协同改动`：涉及多线程，改前必须在统一快照里留痕
  - `共享只读`：允许读取，不默认改动
  - `待释放`：本轮 checkpoint 后可重新评估归属

## 2. 当前认领表

| 路径 / 路径根 | 当前 owner 线程 | 当前状态 | 认领原因 | 依赖线程 | 释放条件 |
|---|---|---|---|---|---|
| `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/` | `Codex规则落地` | `占用中` | 现行规则入口、共享状态层、Git 规则说明都在这里 | 全部线程只读依赖 | 本轮共享层文档与规则冲突清理完成 |
| `.codex/threads/线程分支对照表.md` / `.json` | `Codex规则落地` | `占用中` | 当前需要把旧 worktree 口径改为现行入口口径 | 全部线程只读依赖 | 线程映射与当前唯一状态说明一致 |
| `Assets/000_Scenes/Primary.unity` | `spring-day1` | `协同改动` | 当前 live 对话验收、字体链和显隐收口都依赖主场景 | `导航检查`、`遮挡检查`、`农田交互修复V2`、`NPC` | spring-day1 完成主场景 checkpoint 并同步归属 |
| `Assets/YYY_Scripts/Story/UI/DialogueUI.cs` | `spring-day1` | `占用中` | 对话 UI 显隐、字体库接线和 live 验收仍在推进 | `Codex规则落地` 只读观察 | `fontLibrary` 接线完成并形成新 checkpoint |
| `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese*.asset` | `spring-day1` | `占用中` | 当前属于对话字体链与主场景字体 key 收口范围 | 无默认并行改动线程 | 字体链完成 live 验收 |
| `Assets/111_Data/Story/Dialogue/` | `spring-day1` | `共享只读` | 当前剧情资产作为主场景验收输入，不宜被其他线程顺手改动 | `NPC`、`项目文档总览` | 当前剧情验收轮结束 |
| `Assets/100_Anim/NPC/` | `NPC` | `占用中` | 当前为未跟踪的 NPC 动画资源 WIP | 无 | NPC 线程形成资源 checkpoint 或明确丢弃 |
| `Assets/222_Prefabs/NPC/` | `NPC` | `占用中` | 当前为未跟踪的 NPC Prefab WIP | `spring-day1` 依赖 `001.prefab` 语义 | NPC 线程形成 Prefab checkpoint |
| `Assets/222_Prefabs/NPC/001.prefab` | `NPC` | `协同改动` | NPC 本体拥有资源归属，但 `spring-day1` 依赖其对话测试语义 | `spring-day1` | 双方确认 Prefab 与场景实例谁为真基线 |
| `Assets/Sprites/NPC/*.meta` | `NPC` | `占用中` | 当前为 NPC 资源链 WIP 的一部分 | 无 | NPC 线程完成资源归属 |
| `.kiro/specs/NPC/` | `NPC` | `占用中` | 当前 NPC 规划工作区与子工作区都在这里 | `项目文档总览` 只读引用 | NPC 工作区结构稳定 |
| `Assets/YYY_Scripts/Data/Items/ItemData.cs`、`Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`、`Assets/YYY_Scripts/Service/Placement/` | `农田交互修复V2` | `待释放` | 当前已完成 warning 收口，但现场交互验收尚未补一轮统一快照 | `spring-day1` 间接依赖 `GameInputManager.cs` | farm 完成交互验收并确认无新增阻塞 |
| `.kiro/specs/农田系统/` | `农田交互修复V2` | `共享只读` | 当前属于 farm 功能线主文档区 | `项目文档总览` 只读引用 | farm 当前验收轮结束 |
| `.kiro/specs/999_全面重构_26.03.15/导航检查/`、`.codex/threads/Sunset/导航检查/` | `导航检查` | `待释放` | 当前是审计结论文档重组阶段 | 无默认并行改动线程 | 导航线程决定继续整改或冻结归档 |
| `.kiro/specs/999_全面重构_26.03.15/遮挡检查/`、`.codex/threads/Sunset/遮挡检查/` | `遮挡检查` | `待释放` | 当前是审计结论文档重组阶段 | 无默认并行改动线程 | 遮挡线程决定继续整改或冻结归档 |
| `.kiro/about/` | `项目文档总览` | `共享只读` | 当前主要作为项目级接手资料，不应被其他线程顺手重构 | 全部线程可只读引用 | 总览线程确认继续维护或归档 |

## 3. 当前执行口径
- 没在本表里、且属于高风险共享路径的文件，不应由任何线程“默认顺手改”。
- 认领状态为 `占用中` 或 `协同改动` 的路径，必须先在统一快照里说明再改。
- 后续若某线程完成 checkpoint，应同步更新本表，把路径状态从 `占用中` 调整为 `待释放` 或 `共享只读`。
