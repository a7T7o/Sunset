# 耕地判定改进设计

**创建日期**: 2026-03-03
**基于**: 耕地判定锐评001.md + 耕地判定锐评001审视报告.md
**目标**: 修复检测范围错误 + 楼层隔离 + 配置化 + 性能优化

---

## 1. 核心问题

### 1.1 当前实现的缺陷

| 问题 | 当前实现 | 用户需求 | 差距 |
|------|---------|---------|------|
| 检测范围 | `0.9f x 0.9f` | `1.5f x 1.5f` | 缩小 2.8 倍 |
| 树苗检测 | 只检测中心格子 | 检测 1+8 范围 | 遗漏周围8格 |
| 箱子检测 | 只检测中心格子 | 检测 1+8 范围 | 遗漏周围8格 |
| 楼层隔离 | 无 | 需要 | 跨楼层误判 |
| 配置方式 | 硬编码 | Inspector 可配置 | 不可调整 |

### 1.2 锐评指出的致命缺陷

1. **性能黑洞**: 原分析报告在 3x3 循环内调用 `FindObjectsByType`，会导致严重掉帧
2. **重复造轮子**: 无视现有 `PlacementLayerDetector` 和 `FarmToolPreview.currentSortingLayer`
3. **坐标判定漂移**: 使用 `transform.position` 而非 `Collider.bounds.center`

---

## 2. 设计原则

### 2.1 性能优先

- ✅ 在方法外一次性获取全场景列表
- ✅ 遍历列表判断距离，避免循环内全局查找
- ❌ 禁止在循环内调用 `FindObjectsByType`

### 2.2 复用现有架构

- ✅ 使用 `FarmToolPreview.currentSortingLayer` 传递楼层信息
- ✅ 使用 `SpriteRenderer.sortingLayerName` 进行楼层对比
- ✅ 使用 `Collider.bounds.center` 获取物体位置

### 2.3 配置化而非硬编码

- ✅ 检测半径配置在 `FarmTileManager`
- ✅ 障碍物标签配置在 `FarmTileManager`
- ✅ 白名单标签配置在 `FarmTileManager`（后续补充）

---

## 3. 架构设计

### 3.1 配置中心：FarmTileManager

```csharp
[Header("耕地障碍物检测配置")]
[Tooltip("耕地障碍物检测半径（1.5 = 中心1格 + 周围8格半格）")]
[SerializeField] private float farmingObstacleCheckRadius = 1.5f;

[Tooltip("耕地障碍物标签（可多选）")]
[SerializeField] private string[] farmingObstacleTags = new string[] { "Tree", "Rock", "Building" };

[Tooltip("耕地白名单标签（这些物体不算障碍物，如稻草人、洒水器）")]
[SerializeField] private string[] farmingWhitelistTags = new string[0];

[Header("耕地地面检测配置")]
[Tooltip("是否检测周围8格地面（防止悬空耕地）")]
[SerializeField] private bool requireGroundAround = true;

// 公开访问器
public float FarmingObstacleCheckRadius => farmingObstacleCheckRadius;
public string[] FarmingObstacleTags => farmingObstacleTags;
public string[] FarmingWhitelistTags => farmingWhitelistTags;
public bool RequireGroundAround => requireGroundAround;
```

### 3.2 检测逻辑：PlacementValidator

```csharp
/// <summary>
/// 检测 1+8 范围内是否有农田障碍物
/// </summary>
/// <param name="cellCenter">格子中心世界坐标</param>
/// <param name="targetSortingLayer">目标楼层名称（如 "Layer 1"）</param>
/// <returns>true=有障碍物，false=无障碍物</returns>
public static bool HasFarmingObstacle(Vector3 cellCenter, string targetSortingLayer)
{
    // 从 FarmTileManager 读取配置
    if (FarmTileManager.Instance == null)
        return false;

    float radius = FarmTileManager.Instance.FarmingObstacleCheckRadius;
    string[] obstacleTags = FarmTileManager.Instance.FarmingObstacleTags;
    string[] whitelistTags = FarmTileManager.Instance.FarmingWhitelistTags;

    // 1. 碰撞体检测（扩大到配置的半径）
    Vector2 boxSize = new Vector2(radius, radius);
    Collider2D[] hits = Physics2D.OverlapBoxAll(cellCenter, boxSize, 0f);

    foreach (var hit in hits)
    {
        // 楼层隔离
        if (!IsSameSortingLayer(hit.transform, targetSortingLayer))
            continue;

        // 白名单检查
        if (HasAnyTagStatic(hit.transform, whitelistTags))
            continue;

        // 障碍物标签检查
        if (HasAnyTagStatic(hit.transform, obstacleTags))
            return true;
    }

    // 2. 检测无碰撞体的树苗（1+8 范围）
    if (HasTreeInArea(cellCenter, radius, targetSortingLayer))
        return true;

    // 3. 检测无碰撞体的箱子（1+8 范围）
    if (HasChestInArea(cellCenter, radius, targetSortingLayer))
        return true;

    return false;
}
```

---

## 4. 关键方法实现

### 4.1 楼层隔离检查

```csharp
/// <summary>
/// 检查 Transform 是否在目标楼层（通过 SortingLayer 判断）
/// </summary>
private static bool IsSameSortingLayer(Transform t, string targetSortingLayer)
{
    var renderer = t.GetComponentInChildren<SpriteRenderer>();
    if (renderer != null)
    {
        return renderer.sortingLayerName == targetSortingLayer;
    }

    // 如果没有 SpriteRenderer，默认认为在同一层（兼容旧代码）
    return true;
}
```

### 4.2 高效范围检测（树苗）

```csharp
/// <summary>
/// 检测区域内是否有树木（高效版：一次性获取列表）
/// </summary>
private static bool HasTreeInArea(Vector3 center, float radius, string targetSortingLayer)
{
    // 一次性获取全场景树木列表（避免循环内调用）
    var allTrees = Object.FindObjectsByType<TreeController>(FindObjectsSortMode.None);

    // 使用 AABB 正方形判断（与 Physics2D.OverlapBoxAll 保持一致）
    float halfSize = radius / 2f;

    foreach (var tree in allTrees)
    {
        // 楼层隔离
        if (!IsSameSortingLayer(tree.transform, targetSortingLayer))
            continue;

        // 获取树木位置（使用 Collider bounds 而非 transform.position）
        Vector3 treePos = GetTreePosition(tree);

        // AABB 正方形距离判断（与物理碰撞体检测形状一致）
        if (Mathf.Abs(treePos.x - center.x) <= halfSize && Mathf.Abs(treePos.y - center.y) <= halfSize)
            return true;
    }

    return false;
}

/// <summary>
/// 获取树木位置（优先使用 Collider bounds）
/// </summary>
private static Vector3 GetTreePosition(TreeController tree)
{
    var collider = tree.GetComponentInChildren<Collider2D>();
    if (collider != null)
        return collider.bounds.center;

    // Fallback: 使用父级 Transform（树木根部）
    return tree.transform.parent != null
        ? tree.transform.parent.position
        : tree.transform.position;
}
```

### 4.3 高效范围检测（箱子）

```csharp
/// <summary>
/// 检测区域内是否有箱子（高效版：一次性获取列表）
/// </summary>
private static bool HasChestInArea(Vector3 center, float radius, string targetSortingLayer)
{
    // 一次性获取全场景箱子列表
    var allChests = Object.FindObjectsByType<ChestController>(FindObjectsSortMode.None);

    // 使用 AABB 正方形判断（与 Physics2D.OverlapBoxAll 保持一致）
    float halfSize = radius / 2f;

    foreach (var chest in allChests)
    {
        // 楼层隔离
        if (!IsSameSortingLayer(chest.transform, targetSortingLayer))
            continue;

        // 获取箱子位置（使用 Collider bounds）
        Vector3 chestPos = GetChestPosition(chest);

        // AABB 正方形距离判断（与物理碰撞体检测形状一致）
        if (Mathf.Abs(chestPos.x - center.x) <= halfSize && Mathf.Abs(chestPos.y - center.y) <= halfSize)
            return true;
    }

    return false;
}

/// <summary>
/// 获取箱子位置（优先使用 Collider bounds）
/// </summary>
private static Vector3 GetChestPosition(ChestController chest)
{
    var collider = chest.GetComponentInChildren<Collider2D>();
    if (collider != null)
        return collider.bounds.center;

    // Fallback: 使用 Transform
    return chest.transform.position;
}
```

---

## 5. 调用链修改

### 5.1 FarmToolPreview.UpdateHoePreview

```csharp
// 修改前
bool hasObstacle = PlacementValidator.HasFarmingObstacle(cellCenter);

// 修改后
bool hasObstacle = PlacementValidator.HasFarmingObstacle(cellCenter, currentSortingLayer);
```

### 5.2 FarmToolPreview.UpdateWateringPreview

```csharp
// 修改前
bool hasObstacle = PlacementValidator.HasFarmingObstacle(cellCenter);

// 修改后
bool hasObstacle = PlacementValidator.HasFarmingObstacle(cellCenter, currentSortingLayer);
```

### 5.3 其他调用点

搜索所有 `HasFarmingObstacle` 调用点，统一添加 `currentSortingLayer` 参数。

---

## 6. 可视化调试

### 6.1 Gizmos 绘制检测范围

```csharp
// 在 FarmToolPreview 或 PlacementValidator 中添加
private void OnDrawGizmos()
{
    if (!showFarmingDebug || !Application.isPlaying)
        return;

    if (FarmTileManager.Instance == null)
        return;

    float radius = FarmTileManager.Instance.FarmingObstacleCheckRadius;

    // 绘制检测范围（1.5 单位正方体）
    Gizmos.color = currentState == FarmPreviewState.Valid
        ? new Color(0, 1, 0, 0.3f)
        : new Color(1, 0, 0, 0.3f);

    Vector3 cellCenter = GetCellCenterWorld(lastLayerIndex, lastCellPosition);
    Gizmos.DrawWireCube(cellCenter, new Vector3(radius, radius, 0));
}
```

### 6.2 调试开关

```csharp
[Header("调试")]
[SerializeField] private bool showFarmingDebug = false;
```

---

## 7. 地面检测优化（可选）

### 7.1 FarmTileManager.CanTillAt 扩展

```csharp
public bool CanTillAt(int layerIndex, Vector3Int cellPosition)
{
    // ... 现有检查 ...

    // 检测周围8格地面（可选，通过配置开关）
    if (RequireGroundAround)
    {
        for (int dx = -1; dx <= 1; dx++)
        {
            for (int dy = -1; dy <= 1; dy++)
            {
                if (dx == 0 && dy == 0) continue; // 跳过中心格子

                Vector3Int neighborPos = cellPosition + new Vector3Int(dx, dy, 0);
                TileBase neighborTile = tilemaps.groundTilemap.GetTile(neighborPos);

                // 如果周围格子没有地面，不允许耕地
                if (neighborTile == null)
                    return false;
            }
        }
    }

    return true;
}
```

---

## 8. 性能对比

### 8.1 原分析报告方案（错误）

```
鼠标移动一格 = 1次物理检测 + 9次全场景树木遍历 + 9次全场景箱子遍历
= 1 + 9 * O(N_trees) + 9 * O(N_chests)
```

### 8.2 改进方案（正确）

```
鼠标移动一格 = 1次物理检测 + 1次全场景树木遍历 + 1次全场景箱子遍历
= 1 + O(N_trees) + O(N_chests)
```

**性能提升**: 约 9 倍（在大型农场中差距更明显）

---

## 9. 待定事项

### 9.1 白名单物品标签

| 物品 | 标签 | 状态 |
|------|------|------|
| 稻草人 | `Scarecrow` | ⚠️ 待设计 |
| 洒水器 | `Sprinkler` | ⚠️ 待设计 |
| 其他 | TBD | ⚠️ 待补充 |

**记录位置**: 记入主工作区待定事项（TD_000_农田系统.md）

---

## 10. 总结

### 10.1 核心改进

1. ✅ 检测范围从 `0.9f` 扩大到 `1.5f`（配置化）
2. ✅ 楼层隔离（使用 `sortingLayerName` 对比）
3. ✅ 高效范围检测（避免循环内全局查找）
4. ✅ 正确坐标判定（使用 `Collider.bounds.center`）
5. ✅ 配置化（在 `FarmTileManager` 中可视化配置）
6. ✅ 可视化调试（Gizmos 绘制检测范围）

### 10.2 修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `FarmTileManager.cs` | 修改 | 添加配置字段 + 公开访问器 |
| `PlacementValidator.cs` | 修改 | 重写 `HasFarmingObstacle` + 新增辅助方法 |
| `FarmToolPreview.cs` | 修改 | 传入 `currentSortingLayer` 参数 + Gizmos 调试 |
| `GameInputManager.cs` | 修改（可能） | 如有直接调用需同步修改 |

### 10.3 下一步

创建任务清单（`耕地判定改进任务.md`）并执行。
