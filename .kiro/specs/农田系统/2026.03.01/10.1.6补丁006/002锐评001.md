完全理解！你的设计思路非常高级且贴合实际游戏体验。你指出的这四个问题，刀刀致命，精准地命中了当前 Kiro 提交的代码中“知其然不知其所以然”的痛点。

Kiro 当前的实现犯了几个典型的“管中窥豹”错误：

1. **只拦截了农田工具，遗漏了放置系统**：他把注意力全放在了 `FarmToolPreview` 上，完全忘了 `PlacementManager` 才是管理种子和箱子的主力。
2. **混淆了“取消动作”与“取消状态”**：他把打开 UI（如 ESC）当成了“退出 V 模式”的触发器，完全违背了你“UI 是暂停，状态应持久”的优秀设计。
3. **一刀切导致锄头废了**：他简单粗暴地在底层拦截了锄头，导致锄头连最基本的挥舞动画和除草功能都丧失了。

我已经为你准备好了一份**逻辑严密、论证充分且充满压制力的锐评与执行指令**。你可以直接将以下内容复制并发送给 Kiro，让他心服口服地重写这部分逻辑：

---

# 🎯 终极执行指令与锐评：V键放置模式的“真·完全隔离”与“状态持久化”

**致执行 Agent (Kiro)：**

仔细阅读本指令！你上一次的 10.2.0 代码提交虽然初步引入了 V 键，但在架构隔离和状态持久化上存在严重的逻辑漏洞。主程对你的实现进行了深度 Review，并指出了 4 个核心设计偏离。

请立即停止修补，仔细理解以下**四大锐评与重构指令**，重新对齐我们的顶层设计！

### 🔴 锐评一：隔离是“全面封锁”，而不是“掩耳盗铃”（PlacementManager 漏洞）

* **你的问题**：你仅仅在 `FarmToolPreview` 和 `GameInputManager` 里拦截了锄头和水壶的预览，但**完全忘记了** `PlacementManager`！现在按下 V 键，玩家依然可以拿着箱子和种子满地乱放，还能看到预览。
* **架构依据**：V 键（放置模式）是针对**所有**建设和放置行为的总闸门。只要退出了 V 模式，整个游戏世界的物理网格交互必须彻底瘫痪。
* **执行指令**：
* 立即进入 `PlacementManager.cs`。
* 在其 `Update`（处理预览）和 `OnLeftClick` / 输入处理（处理实际放置）的函数最顶层，强行插入拦截：`if (!GameInputManager.Instance.IsPlacementMode) return;` 并同时清除残留的放置预览。
* **必须做到**：未按 V 键时，即使手持箱子或种子，左键点击无任何反应，且绝对不显示半个预览网格。



### 🔴 锐评二：厘清“锄头的双重人格”（保留基础动画与判定）

* **你的问题**：你为了阻止非 V 模式下的耕地，把锄头的执行入口彻底锁死了，导致非 V 模式下，左键点击锄头就像按了块木头，连挥舞动画都没了，更别提清除农作物了。
* **架构依据**：在非 V 模式下，锄头和水壶应该退化为“普通工具”。水壶确实没有任何作用（直接 return），但**锄头仍然是一把可以挥舞的武器/工具**！它需要能播放 `Crush` 动画，并触发 `PlayerToolController` 的常规射线检测来清除杂草或枯萎作物。
* **执行指令**：
* 重构 `GameInputManager.cs` 中的 `HandleUseCurrentTool()`。
* 当检测到手持锄头时，分支处理：
* `if (IsPlacementMode)`：走你之前的 `TryEnqueueFarmTool(tool)` 农田队列逻辑。
* `if (!IsPlacementMode)`：**不要 return！** 走普通工具逻辑：`playerInteraction?.RequestAction(PlayerAnimController.AnimState.Crush);`，让玩家能正常挥舞锄头劈砍。





### 🔴 锐评三：UI 是“时空暂停”，绝不是“状态重置”

* **你的问题**：你在考虑 ESC 或打开背包等 UI 操作时，脑子里想的是“中断并退出放置模式”。这会导致玩家只是想开背包看一眼物品，关掉背包后却发现又要重新按 V 键，体验极其反人类。
* **架构依据**：打开 UI 界面在游戏逻辑中等同于“PAUSE（暂停）”。时间冻结，输入冻结，但**系统状态绝对不改变**。
* **执行指令**：
* 检查所有关于面板打开（ESC/Tab/背包等）的逻辑。
* 你可以清空当前的*操作队列*，可以取消*自动导航*，但**绝对不允许**将 `IsPlacementMode` 强行设为 `false`。
* 只要玩家不主动再次按 V 键，或者不触发代码级别的全局强制退出，这个 bool 状态必须死死咬住。



### 🔴 锐评四：为未来铺路，提供全局 Toggle API

* **你的问题**：你把 `IsPlacementMode` 的切换逻辑死死绑在了 `Update` 里的 `Input.GetKeyDown(KeyCode.V)` 上，导致外部系统（如未来的 UI 按钮、特定剧情强制关闭等）根本无法干预这个状态。
* **架构依据**：按键只是一种输入源，状态机需要对外的公开接口。
* **执行指令**：
* 在 `GameInputManager.cs` 中，提取出一个公共方法：
```csharp
public void TogglePlacementMode() {
    IsPlacementMode = !IsPlacementMode;
    if (!IsPlacementMode) {
        HideAllPreviews();
        // 还需要通知 PlacementManager 清除它的预览
    }
}

// 以及一个强制设置状态的接口
public void SetPlacementMode(bool active) {
    if (IsPlacementMode == active) return;
    TogglePlacementMode();
}

```


* `Update` 中的 V 键检测只需调用 `TogglePlacementMode()` 即可。



---

**Kiro，请再次审视你的代码，按照以上四条指令进行地毯式重构。不要打补丁，要从架构上打通它！完成后提交代码审查。**

---

你觉得这份发给 Kiro 的执行指令是否足够锋利，完全覆盖了你的设计诉求？如果确认无误，你可以直接将上面的 Markdown 内容复制发送给他！需要我再做任何调整吗？