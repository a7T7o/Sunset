# 004-第二刀进入live终验与验收分流

你这轮我先给真实裁定：

- `第 1 刀补强回归通过`
- `第 2 刀第一轮代码闭环通过`

这不是空话。  
你这轮已经把下面几件事真正推进了：

- `reach envelope` 不再只剩格子框
- 放置事务有了 `commit / rollback` 最小语义
- 树苗有了 `post-spawn confirm`
- 箱子链补上了 `Save()/Load()` 回归

所以这条线现在不能再说“还只是写了点想法”。  
代码层第一轮闭环，我认账。

但你下一句“现在只差用户自己去 Unity live 手动复测”，这一步我不接受作为线程停点。

## 一、当前审核结论

你现在的真实状态不是：

- 第二刀已经通过验收

你现在的真实状态是：

- 第二刀代码层 checkpoint 已成立
- 但 live 终验还没拿到

所以从这一轮开始，你不能把“等用户自己试”当成默认收尾方式。  
这条线下一轮必须自己进入：

- `single-instance + 8888 MCP live 终验`

## 二、这一轮主线不是再写代码，而是把第二刀跑到真实验收结果

你这一轮的核心任务非常明确：

1. 用当前主工程单实例做 live 验证
2. 把第 2 刀的 4 个场景真正跑一遍
3. 根据结果进入分流：
   - 全部通过：立刻转“第二刀验收报告”
   - 任何一条失败：锁第一真实失败点，修完后重跑同一条

默认禁止：

- 还没做 live 终验就继续往第 3 刀走
- 还没拿到真实通过 / 失败点就回去扩写文档
- 把 `run_tests total=0` 当成通过证据

## 三、这一轮固定 live 验证顺序

验证顺序固定，不要自由发挥：

1. 确认 `Edit Mode`
2. 清空 Console
3. 进入 Play
4. 按下面 4 条逐项验证
5. 如有失败，直接记首个真实失败点
6. 若需要修，修完只重跑对应条目
7. 结束后退回 `Edit Mode`

## 四、必须验证的 4 条 live 场景

### 1. 箱子放置 reach envelope

目标：

- 手持箱子，在人物略远但可达的位置点击放置
- 玩家应走到边上后稳定放下
- 不允许再出现 retry 3 次后原地停下

你必须明确回答：

- 是否通过
- 如果失败，失败时玩家停在什么位置、预览边界和放置边界哪里不一致

### 2. 放置成功后的 preview 即时重刷

目标：

- 箱子放成功后，如果背包里还有同物品，鼠标还停在原格
- preview 应立即重刷
- 不允许继续沿旧 preview 误判

你必须明确回答：

- 是否通过
- 如果失败，是 preview 没刷新、还是旧缓存没清、还是格子占位状态没及时更新

### 3. 树苗连续快速点击的“幽灵占位”

目标：

- 手持树苗连续快速点击同格或相邻格
- 成功后一格必须立刻被下一轮识别为已占位
- 不允许出现“成功但没显示 / 幽灵占位”

你必须明确回答：

- 是否通过
- 如果失败，是事务提交时序、post-spawn confirm、还是表现层刷新有残缺

### 4. 箱子 `Save()/Load()` 回归

目标：

- 存档再读档
- 重新打开箱子
- 内容一致
- 不触发 bridge 递归

你必须明确回答：

- 是否通过
- 如果失败，是 `authoritative V2` 丢失、legacy mirror 不一致、还是 UI 重开路径有断层

## 五、这一轮的分流规则

### 分支 A：4 条全部通过

如果这 4 条全部通过：

- 不准继续开第 3 刀
- 直接进入“第二刀验收报告”

你必须新增一份验收文件：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\第二刀验收报告_2026-03-24.md`

这份报告至少要写：

1. 第二刀目标
2. 改动清单
3. 自动验证证据
4. live 验证证据
5. 用户复测要点
6. 当前是否可接受为“第二刀完成”
7. 剩余内容应该如何移交到后续刀次

### 分支 B：任一条失败

如果任一条失败：

- 不准泛泛说“还有一点边界”
- 直接锁“首个真实失败点”
- 修完后只重跑对应条目

也就是说，这轮不接受“我大概觉得差不多了”。  
你要么拿通过，要么拿首个真实失败样本。

## 六、本轮执行纪律

这轮明确禁止：

- 把“等用户手动试”当成线程停点
- 还没 live 终验就转第 3 刀
- 顺手扩写第 3 刀 / 第 4 刀需求
- 用 `run_tests total=0` 继续给自己加通过滤镜

你现在只做一件事：

- 把第二刀真正跑到 live 结果

## 七、本轮回执格式

下一次回执，只按这个格式回复：

- 当前在改什么：
- 当前是第几刀：
- 是否已完成 second-blade live 终验：
- 4 条 live 场景分别是否通过：
- 是否已生成第二刀验收报告：
- changed_paths：
- 新增或修改了哪些测试 / live 证据：
- 用户可直接复测的步骤：
- 当前剩余 blocker：
- blocker_or_checkpoint：
- 一句话摘要：

## 八、最后再钉一次口径

你这轮不是没完成。  
相反，你已经把第二刀代码层推进到了一个像样的 checkpoint。

但真正能不能过，下一轮不靠“等用户有空试”，而是靠你自己把 live 结果拿出来。  
现在继续，不要停。
