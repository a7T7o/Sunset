# 007-shared-root脏改清扫与归属核对

这轮不是让你继续推进 `1.0.4` 新实现，  
这轮只做 shared root 脏改清扫。

## 一、当前 shared root 里优先怀疑属于你这条线的 dirty

当前先核这一项：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\006-续工裁决入口与用户补充区.md`

按当前 shared root 盘点，肉眼可见最明确落在你这条线工作区里的 tracked dirty 先是这一个。

## 二、你这轮要先回答的不是“继续做什么”，而是“现在到底还剩多少脏改是你的”

所以这轮你先做归属核对：

1. 当前 shared root 里，是否真的只剩上面这一项属于你
2. 如果你判断还有别的 `农田交互修复V2` owned dirty 没被这次盘到，也要主动补报
3. 如果这份 `006` 应保留为 live 文档，就把它和你本线程必要记忆一起白名单收干净
4. 如果你判断这份 `006` 不该保留，也不要自己静默删，先明确回执

## 三、本轮禁止事项

这轮明确禁止：

- 顺手继续推 `1.0.4` 新功能
- 顺手补第三刀实现
- 顺手碰导航 / NPC / `Primary.unity`
- 顺手重开一轮 live 验证

## 四、本轮最小目标

你要做到的是：

- 把“当前到底哪些 dirty 真属于农田线”说清楚
- 把你这条线自己的 shared root 尾账收干净

如果当前真的只剩 `006` 这一项属于你，也可以直接如实回复，不需要硬凑别的改动。

## 五、本轮回执格式

只按这个格式回复：

- 当前在扫什么：
- 你确认属于农田线的 dirty：
- 你确认不属于农田线、因此没有去碰的 dirty：
- 本轮是否已完成清扫：
- changed_paths：
- blocker_or_checkpoint：
- 一句话摘要：

## 六、最后一句

这轮不是继续种地，  
这轮是先把你自己留在 shared root 的地扫干净。
