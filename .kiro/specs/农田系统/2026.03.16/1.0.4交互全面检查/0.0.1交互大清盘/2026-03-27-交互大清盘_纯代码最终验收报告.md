# 2026-03-27 交互大清盘｜纯代码最终验收报告

> 工作区：`D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\0.0.1交互大清盘`
>
> 审计时间：`2026-03-28`
>
> 审计方式：纯代码只读复核，不使用 `UnityMCP`，不进入 `Play Mode`，不做 Scene / Prefab live 回读
>
> 审计目标：把最近几轮待验项与历史返工代码一起重审，重新给出“从代码看，现在到底过没过、还剩什么”的最终判断

## 1. 审计边界

这份报告只回答一件事：

从代码事实看，农田这条交互线现在到底补到了哪一步，哪些地方已经有闭环，哪些地方还明显没关门。

它不替代下面 3 件事：

1. 用户真实手感验收
2. 动画 / 气泡 / 遮挡观感裁判
3. 运行态时序、Scene 引用和 Unity live 证据

因此，本文里出现的“代码层基本通过”，含义是：

结构上已经补出了闭环，当前没有再看到同级别的纯代码硬缺口。

它不等于：

用户已经验收通过。

## 2. 本轮实际复核范围

本轮不是只看最后一两处改动，而是把当前主链代码重新串了一遍。核心复核链路包括：

- 放置链：`PlacementManager.cs`、`PlacementValidator.cs`、`PlacementPreview.cs`
- 树木链：`TreeController.cs`
- 玩家 / 工具链：`PlayerInteraction.cs`、`ToolRuntimeUtility.cs`、`GameInputManager.cs`
- 农田预览与遮挡链：`FarmToolPreview.cs`、`OcclusionManager.cs`、`OcclusionTransparency.cs`
- 箱子 / 导航链：`PlayerAutoNavigator.cs`、`ChestController.cs`、`ChestInventoryV2.cs`
- 背包 / Toolbar / Tooltip 链：`InventoryPanelUI.cs`、`InventoryInteractionManager.cs`、`InventorySlotUI.cs`、`SlotDragContext.cs`、`ToolbarSlotUI.cs`、`ItemTooltip.cs`、`ItemTooltipTextBuilder.cs`、`EnergyBarTooltipWatcher.cs`

## 3. 总结论

当前不能判全线通过。

纯代码口径下，最准确的总判断是：

事务主链已经补出骨架，但 `hover / chest / inventory 真源 / tooltip` 还没有全部关门，其中当前最硬的代码 blocker 是 `C2 Tooltip`。

按代码状态拆开看：

- 代码层已经补出主闭环，待真人终验的项：
  - `A1` 树苗连续放置事务
  - `A2` 树木倒下事务
  - `A3` 工具失效强制收尾事务
  - `B4` 高树冷却输入层前置拦截
  - `C3` 无碰撞体脚下放置整包规则
- 代码层仍明显未过的项：
  - `B1` hover 遮挡最终口径
  - `B2` 箱子交互链
  - `B3` Toolbar / Hotbar 选中态真源
  - `B5` 玩家气泡样式终线
  - `C2` Tooltip 时机回归
- 代码层当前更适合作为观察项，而不是主 blocker 的项：
  - `C1` 箱子双库存递归 / SaveLoad 回归

其中当前最值得优先处理的，是 `C2 Tooltip`。

原因不是“它看起来有点怪”，而是代码里已经能直接看到一条高风险自灭链：

- `ItemTooltip` 在排队显示前先把自己 `SetActive(false)`
- 然后再去启动延迟显示协程
- 这很可能直接让延迟显示根本起不来
- 精力条 tooltip 也走同一条 `ItemTooltip.ShowCustom(...)` 链

这意味着它不是一个只影响背包 hover 的边角问题，而是整条 tooltip 入口都可能一起失效。

## 4. 分项验收结论

### A1 树苗连续放置事务

**代码结论**：代码层基本闭环，待体验终验

**大白话判断**：

这条已经不是之前那种“鼠标不抖一下就不刷新”的旧状态了。

当前代码已经明确补了 4 件关键事：

1. 玩家主占格满足条件时，树苗 / 播种可以按近身 `3x3` 直放处理
2. 玩家所在单元格不是简单取中心点，而是按碰撞体主占比算 dominant cell
3. 放置成功后，preview 会按世界候选格重算，而不是继续死抱旧格
4. 鼠标压在已占格边缘时，会按鼠标倾向尝试邻格，而不是一直卡在当前格

**纯代码判断**：

从结构上看，这条已经有闭环，不再像之前那样明显断在“世界候选格和屏幕像素位移混用”。

但用户最后一次要求的是“手感真的对”，这件事不能靠纯代码自证，所以当前只能判成：

代码层基本到位，体验层待终验。

### A2 树木倒下事务

**代码结论**：逻辑闭环基本成立，表现层仍未算通过

**大白话判断**：

树木“砍倒后又站起来”这类最硬的事务漏洞，当前代码上已经明显收住了。

当前树木倒下链已经具备这些单向锁：

1. 倒下期间直接拒绝新的命中
2. `ChopDown()` 只允许提交一次
3. 原树 sprite 在砍倒提交后会被隐藏
4. 原树 collider 和遮挡会在倒下事务内被关闭
5. 树桩的 collider 会等 fall lock 完成后再恢复

**纯代码判断**：

如果只问“之前那个死而复生逻辑洞现在还在不在”，从代码看已经不是主问题。

但用户后来点名的是“倒下动画本身很丑”，这个属于表现层质量，不是纯代码能替用户裁判的，所以这条不能直接写成最终通过。

### A3 工具失效强制收尾事务

**代码结论**：核心事务闭环已补出，展示链仍未完全关门

**大白话判断**：

工具耐久这条最核心的语义，现在代码上已经比之前统一很多了：

1. 挥砍前会先做可否开始动作的前检
2. 允许开始的这一次动作，会在完成后再提交工具消耗
3. 如果这次提交把最后一点耐久扣光，后续动作会被强制收尾
4. 农具自动链清理队列预览和导航的入口也已经补上

**纯代码判断**：

“最后一刀能挥完，挥完就坏，下一刀起不来”这套核心事务，现在从代码结构看已经基本成立。

但这条还不能判完全通过，原因有两个：

1. 水壶 / 耐久显示 / Tooltip 这组展示链没有全部关门
2. `C2 Tooltip` 的硬 blocker 会直接影响工具反馈链的可见性

所以这条当前更准确的结论是：

核心事务闭环已补出，外围展示链未彻底收口。

### B1 hover 遮挡最终口径

**代码结论**：未通过

**大白话判断**：

这条现在最核心的问题，不是“还没做收缩”，而是前后两边根本没吃同一个事实源。

当前代码里至少存在两套口径：

1. 农田 preview 的焦点 footprint 已经缩得很小，只看中心格附近
2. 遮挡物那一侧仍然拿整张主 `SpriteRenderer.bounds` 去判遮挡

这就会直接带来两种错位：

- 农田这边你想看的是中心一小块，遮挡那边却还在拿大 bounds 说话
- placeable preview 改回 footprint 之后，又可能出现另一头欠触发

**纯代码判断**：

这条从代码看仍然没过。

它现在的问题不是“再调一点数值就行”，而是 hover 预览范围和 occluder 真实判定范围没有统一到同一事实源。

### B2 箱子交互链

**代码结论**：未通过

**大白话判断**：

箱子这条现在不是完全没修，而是修到了“方向对了，但标尺还没统一”。

代码里能看到两件互相打架的事：

1. 到位后会先停导航，再尝试交互，这个方向是对的
2. 但距离判断仍然保留了额外容差和 chest 专属补丁距离

这意味着：

- 导航停下的位置
- 真正允许打开 UI 的位置

还不是彻底共用同一把尺子。

**纯代码判断**：

用户反馈“走到了但不开箱子”这件事，跟当前代码状态是对得上的。

所以这条不能算过，仍应视为未完全关门。

### B3 Toolbar / Hotbar 选中态真源

**代码结论**：未通过

**大白话判断**：

这条现在也不是没补，而是补到了“一半真源化”。

好的部分是：

- 单击和拖拽起手已经会主动把背包源格设成选中
- 最终落点也会回写到背包内部选中索引

但没关门的地方也很清楚：

1. 背包面板打开时，只在 `OnEnable()` 从 hotbar 同步一次
2. 没有看到它持续订阅 hotbar 选中变化并实时回拉
3. 第一行格子在 panel 打开时，仍优先读背包面板自己的选中索引

**纯代码判断**：

当前代码已经把“背包拖拽完全不走真源”的旧问题补掉一大块。

但它还没彻底变成“背包和 Toolbar 始终只认一套实时真源”，所以这条仍不能判通过。

### B4 高树冷却输入层前置拦截

**代码结论**：代码层基本对齐，待真人终验

**大白话判断**：

按你最后口径，这条最关键的是：

- 第一次等级不足挥砍给反馈
- 进入冷却期后，不允许继续起动画

当前代码上已经能看到这两层：

1. 树木本体有“冷却期间前置拦截低级斧头继续挥砍”的判断
2. 输入层起播前也会走 `ShouldBlockToolActionBeforeAnimation(...)`
3. 长按续挥砍前同样会过这一层前检

**纯代码判断**：

如果只问“冷却期里还会不会从老路子继续放行动画”，从代码看已经不是明显破口。

但这条还是需要真人实际按住去砍，确认体验完全符合你的语义，所以当前判定为：

代码层基本对齐，待终验。

### B5 玩家气泡样式终线

**代码结论**：未通过

**大白话判断**：

这条现在的问题已经不是“能不能弹出来”，而是它还没有真正回到 NPC 那套样式语言。

当前代码里：

- 玩家气泡有自己一套 token
- NPC 气泡还是另一套 token

这说明它虽然做过可读性和换行修补，但还没做到你要的“对齐 NPC 样式语言”。

**纯代码判断**：

这条当前仍然没过，属于表现层一致性没关门，不是功能 blocker，但也绝不能写成完成。

### C1 箱子双库存递归 / SaveLoad 回归观察

**代码结论**：当前未见新的明确回退，继续观察

**大白话判断**：

从代码结构看，箱子当前仍是：

- legacy 库存做兼容镜像
- `ChestInventoryV2` 做 authoritative 真源
- 两边通过 silent bridge 同步

本轮只读复核里，没有再看到新的明显递归自咬点。

**纯代码判断**：

这条当前更适合继续观察，不应该抢在前面的 blocker 前面升级成主刀。

### C2 Tooltip 时机回归

**代码结论**：阻塞未过，当前最硬代码 blocker

**大白话判断**：

这条不是“可能有点问题”，而是代码里就能直接看到一条高风险断链。

现在 `ItemTooltip` 的排队显示顺序是：

1. 先记下待显示数据
2. 先把 tooltip 对象自己关掉
3. 然后再起延迟显示协程

这条顺序非常危险，因为一个常见结果就是：

对象先失活了，后面的延迟协程根本起不来。

更要命的是，精力条 tooltip 也不是独立系统，而是直接走 `ItemTooltip.ShowCustom(...)`，所以它也会一起受影响。

**纯代码判断**：

这条必须重新列回 blocker，而且优先级要抬高。

它不是单个 UI 样式问题，而是 tooltip 总入口本身就可能被自己打断。

### C3 无碰撞体脚下放置整包回归

**代码结论**：代码层基本对齐，待终验

**大白话判断**：

这条当前代码口径已经比较清楚：

1. 普通 placeable 会先判断自己在真实放置态有没有启用中的实体碰撞体
2. 没有实体碰撞体的对象，会忽略玩家自己这一个障碍
3. 树苗按 Stage 0 配置判定，当前是允许脚下放置
4. 播种明确允许脚下进行
5. 有实体碰撞体的 placeable 仍然继续阻挡

**纯代码判断**：

从代码上看，这条已经基本对齐你的规则，当前主要差的是整包真人终验，而不是再一层明显逻辑洞。

## 5. 当前剩余问题总表

### 第一优先级：明确 blocker

1. `C2 Tooltip`
   - 问题本质：tooltip 入口可能在排队显示前先把自己关掉
   - 影响范围：物品 tooltip、精力条 tooltip、工具反馈链可见性
2. `B1 hover`
   - 问题本质：preview 焦点范围和 occluder bounds 没统一真源
   - 结果：可能过大，也可能欠触发
3. `B2 箱子交互链`
   - 问题本质：导航停点和实际可交互距离仍不是同一把尺子
4. `B3 选中态真源`
   - 问题本质：背包面板和 hotbar 仍是“一次同步 + 分头持有”，不是持续同源

### 第二优先级：体验与样式未过

1. `B5 玩家气泡`
   - 当前已改善可读性，但还不是 NPC 同源样式
2. `A2 树木倒下表现`
   - 事务层基本补好，但动画好不好看仍不能靠代码判过

### 第三优先级：结构已补，仍需真人终验

1. `A1` 连续放置手感
2. `A3` 工具收尾 + 展示链整体验收
3. `B4` 高树冷却输入层体验
4. `C3` 无碰撞体脚下放置整包回归

## 6. 最终判断

如果现在只允许我给一句最终结论，那我会写成：

当前这条交互线不能算最终通过，但也不是“前面都白做了”。  
更准确的状态是：核心事务骨架已经补出来了，真正还没关门的是 `Tooltip`、`hover`、`箱子距离口径`、`背包/Toolbar 真源` 和 `玩家气泡终线` 这几处，其中 `Tooltip` 是当前最硬的纯代码 blocker。

如果下一轮继续按代码优先级扫盘，我建议顺序固定为：

1. `C2 Tooltip`
2. `B1 hover`
3. `B2 箱子交互链`
4. `B3 选中态真源`
5. `B5 玩家气泡`
6. 然后再回到真人终验包，重验 `A1 / A2 / A3 / B4 / C3`

这就是当前纯代码口径下，我愿意正式签字的最终判断。
