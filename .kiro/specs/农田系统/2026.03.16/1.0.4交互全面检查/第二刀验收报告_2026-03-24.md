# 第二刀验收报告（2026-03-24）

## 1. 第二刀目标

第二刀的验收目标只有 4 条：

1. 箱子放置 `reach envelope` 在 live 场景里稳定成立，不再出现 `retry 3 次后原地停下`
2. 放置成功后的 preview 会立刻重刷，不再沿用旧 preview 继续误判
3. 树苗连续快速点击时，不再出现“成功但没被下一轮验证识别”的幽灵占位
4. 箱子 `Save() / Load()` 往返后，authoritative `ChestInventoryV2` 与 legacy mirror 都能恢复，且不重新引入 bridge 递归

## 2. 改动清单

本刀最终交付面如下：

- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementPreview.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementManager.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementExecutionTransaction.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementSecondBladeLiveValidationRunner.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\Editor\PlacementSecondBladeLiveValidationMenu.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Inventory\ChestInventoryV2.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\Editor\ChestInventoryBridgeTests.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\Editor\PlacementReachEnvelopeTests.cs`
- `D:\Unity\Unity_learning\Sunset\Assets\Editor\PlacementExecutionTransactionTests.cs`

其中本轮补齐的真实闭环点是：

- `PlacementPreview` 把交互 envelope 与视觉 envelope 分开
- `PlacementManager` 在成功放置后立即重刷 preview，并给普通 placeable / 树苗加最小提交事务
- `PlacementExecutionTransaction` 明确 `Spawned / VisualReady / InventoryCommitted / OccupancyCommitted` 的回滚边界
- 树苗分支增加 `post-spawn confirm`
- `ChestInventoryV2.ToSaveData()` 现在会把 `InventoryItem` 动态属性写入 `InventorySlotSaveData.properties`
- second-blade live runner / menu 用于在 `Primary` 里统一复跑 4 条验收场景

## 3. 自动验证证据

已执行并通过：

- `Assembly-CSharp.rsp` 独立编译通过
- `Assembly-CSharp-Editor.rsp` 独立编译通过
- `ChestInventoryBridgeTests` 已覆盖 `Save() / Load()` 往返恢复动态属性
- `PlacementReachEnvelopeTests`、`PlacementExecutionTransactionTests` 维持可编译状态
- Unity 清 Console 后重新编译，无项目级 `error`

说明：

- MCP 自带 `run_tests` 仍存在 `total=0` 的不可信返回，因此本报告不把它当作通过依据

## 4. Live 验证证据

本轮在 `D:\Unity\Unity_learning\Sunset` 的单实例 Unity（`Primary`）里，用菜单：

- `Tools/Sunset/Placement/Run Second Blade Live Validation`

完成了 second-blade 的 live 终验。

最终有效结果如下：

1. `ChestReachEnvelope`
   - `pass=True`
   - 证据：`placedNearTarget=True movedDistance=1.04 ... state=Preview`
2. `PreviewRefreshAfterPlacement`
   - `pass=True`
   - 证据：`stayedOnOriginalCell=True previewInvalid=True previewPos=(-9.50, 1.50, 0.00)`
3. `SaplingGhostOccupancy`
   - `pass=True`
   - 证据：`secondPlantBlocked=True previewInvalid=True treeDelta=1`
4. `ChestSaveLoadRegression`
   - `pass=True`
   - 证据：`runtimeRestored=True mirrorRestored=True uiOpened=True chestIsEmpty=False`

补充说明：

- 第一次 live 尝试发生在一个过期的 `PlayMode transition` 窗口里，`ChestReachEnvelope` 出现过一次假失败；在完整 `Stop -> Play -> 等待稳定 -> 重跑` 后，4 条全部通过
- 本轮 Console 仍可见几条非阻断噪音：
  - `There are no audio listeners in the scene`
  - MCP package 的 WebSocket 警告
  - `PersistentObjectRegistry` 的 GUID 冲突提示
  - `ItemDatabase` 对测试物品 ID `1701 / 1702` 的缺省提示
- 上述噪音没有阻断 second-blade 目标，也没有产生项目级红编译

## 5. 用户复测要点

用户现在可以在 `Primary` 里直接复测：

1. 手持箱子，在略远但可达的位置点击放置，确认玩家会靠近后稳定放下
2. 放下箱子后，不移动鼠标且背包仍有同类物品，确认 preview 会立刻在原格重刷并转为无效
3. 手持树苗，对同格或相邻格连续快速点击，确认成功后一格会马上被下一轮识别为已占位
4. 往箱子里写入带动态属性的物品后触发 `Save() / Load()`，确认重新打开箱子时 runtime item 与 UI mirror 都恢复

## 6. 当前是否可接受为“第二刀完成”

可以。

当前判断依据：

- 第二刀代码层闭环已完成
- 第二刀要求的 4 条 live 场景已经全部拿到真实通过结果
- 本轮新增的 `ChestInventoryV2` 存档补丁已经把 `ChestSaveLoadRegression` 的最后失败点补上

## 7. 后续移交

第二刀完成后，后续内容不再属于本刀验收范围，应移交到 `1.0.4` 的后续刀次继续处理。

当前建议移交的真实剩余内容是：

- Toolbar / 背包 / 锁定态相关的后续交互问题
- 用户后来补充的 `Toolbar 左键后 A/D 触发独立框选` 问题
- 更高层的工具耐久 / 精力 / Tooltip / SO 参数统一口径
