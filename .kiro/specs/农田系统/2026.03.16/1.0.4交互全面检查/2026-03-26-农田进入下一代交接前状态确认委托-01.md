# 2026-03-26-农田进入下一代交接前状态确认委托-01

这轮不是继续补 `placeable` 代码，也不是继续刷 second-blade / sapling live。

你现在只判断一件事：

> `农田交互修复V2` 当前是否已经满足进入下一代线程交接的条件？

如果满足，就直接进入交接；如果不满足，就停下回执，不要硬进。

## 当前唯一主刀

只做 `农田交互修复V2` 的交接前状态确认。

明确禁止：

1. 不继续泛修 `PlacementManager`；
2. 不继续把范围重新放大回整条 placeable 主链；
3. 不继续顺手做新的 hygiene 清扫；
4. 不继续碰与这轮确认无关的 `Unity live`；
5. 不继续把新增 6 条需求当场拆成新的实现刀次。

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\2026-03-26_V1交接文档统一写作Prompt.md`
2. 你自己的线程记忆：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V2\memory_0.md`
3. 当前子工作区记忆：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\memory.md`
4. 当前需求总入口：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\全面理解需求与分析.md`
5. 当前最后一轮真正有效的 placeable 收口入口：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\013-placeable主链checkpoint后只收runner稳定性与遗漏卫生.md`

## 我的当前初判

按你最新 memory，我当前初判是：

- 你**已经非常接近可交接**；
- 但你必须先自己确认：当前应以“`012` 已把 placeable 主链拉回至少不比 `124caccc` 更差，`013` 只剩 sapling runner 稳定性，新增 6 条需求已并入总入口”的口径进入 handoff；
- 而不是还有一个必须你先补的动作，否则会让新线程误以为 placeable 主链仍未恢复。

## 你必须先判断的 5 件事

1. 当前主线是否已经清楚区分为：
   - `已恢复的工作基线`
   - `尚未完全收口的 runner 稳定性`
   - `新增 6 条后续硬验收项`
2. 当前 single first blocker 是否真的只是：
   - `SaplingGhostOccupancy` 的 fresh 取样稳定性 / 并发 live 窗口问题；
3. 当前 own / mixed-owner / non-own 边界是否已经足够清楚，不会让新线程继续错动 `GameInputManager.cs` 或别线现场；
4. 当前是否还存在一个必须由你先补的最小动作，否则交接会失真；
5. 新线程接手后，是否已经可以在“placeable 主链已恢复”的基线上继续处理 runner 稳定性与新增需求，而不是重新回到事故定性阶段。

## 分支 A：如果你确认“可以进入交接”

那你不要再等下一轮确认，直接执行：

1. 按统一写作 prompt 生成交接包；
2. 正式写入：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V2\V2交接文档\`
3. 未来继任线程名按新规则计算：
   - `农田交互修复V3`
4. 交接完成后，只能追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V2\memory_0.md`
5. 聊天按最小格式回执。

## 分支 B：如果你确认“暂时不能进入交接”

那你必须停下，不准硬进交接。

你只按下面格式回执：

- 当前是否可进入交接：`no`
- 当前单一第一阻塞点是什么
- 为什么这个阻塞点会让交接失真或失去意义
- 到什么条件成立后，才可以进入交接
- 一句话摘要

## 这轮最小聊天回执格式

如果进入交接：

- 已回写文件路径
- 是否完成本轮要求
- 一句话摘要

如果未进入交接：

- 当前是否可进入交接
- 当前单一第一阻塞点
- 进入交接所需最小条件
- 一句话摘要

## 一句话总口径

这轮先回答“农田这条线现在能不能安全进入下一代交接”；如果能，就按“已恢复基线 + 剩余 runner 稳定性 + 新增 6 条需求”这三个层次直接进；如果不能，就只交那个会让 handoff 失真的唯一阻塞点。
