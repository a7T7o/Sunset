# 2026-03-26-农田交互修复V3第二轮续工委托-03

你现在继续作为 `农田交互修复V3` 施工。

这轮不是重判 `012`，也不是把新增 6 条需求提前拉进来一起做。

## 当前已接受的基线

1. `012` 仍已接受为：
   - placeable 主链至少不比 `124caccc` 更差
2. `013` 当前唯一剩余点仍是：
   - `SaplingGhostOccupancy` 的 runner 稳定性尾差
3. 首轮 `V3` 已经证明：
   - 至少拿到过 1 轮无 `NavValidation` 并发的 sapling-only 窗口
   - patched runner 仍未在新的干净窗口里稳定转绿

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\2026-03-26-农田交互修复V3首轮启动委托-02.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementSecondBladeLiveValidationRunner.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\Editor\PlacementSecondBladeLiveValidationMenu.cs`

## 这轮唯一主刀

只做：

> 再拿 1 个新的干净 sapling-only 窗口，并只验证 patched runner 是否转绿

更具体地说，这轮只允许你完成下面这 1 刀：

1. 先做 shared root / Unity live preflight，确认当前窗口是否又被 `NavValidation` 并发污染；
2. 只重跑：
   - `SaplingGhostOccupancy`
3. 如果这轮 fresh 没有 `NavValidation` 并发且直接通过：
   - 立刻 `Stop`
   - 把 `013` 收口为 runner 尾差已压掉
4. 如果这轮 fresh 没有 `NavValidation` 并发但仍失败：
   - 只允许在 `PlacementSecondBladeLiveValidationRunner.cs` 再补 1 个最小 runner 侧补口
   - 然后最多再补 1 次同范围 fresh
5. 如果 fresh 窗口再次混入 `NavValidation`：
   - 立刻停在外部并发 blocker
   - 不准继续硬刷 live

## 允许的作用域

这轮只允许你读取和必要时修改这些范围：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\2026-03-26-农田交互修复V3第二轮续工委托-03.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\PlacementSecondBladeLiveValidationRunner.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Placement\Editor\PlacementSecondBladeLiveValidationMenu.cs`
6. 只读参考可用：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Farm\FarmRuntimeLiveValidationRunner.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Farm\Editor\FarmRuntimeLiveValidationMenu.cs`

## 明确禁止

1. 不要重判 `012`
2. 不要大改 `PlacementManager.cs`
3. 不要大改 `GameInputManager.cs`
4. 不要把 second-blade 整包重刷
5. 不要提前扩到新增 6 条需求
6. 不要把再次混入并发污染的 live 窗口硬算成“fresh 失败证据”

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已明确回答这轮 fresh 窗口是否干净、是否仍混入 `NavValidation`
2. 你已对 `SaplingGhostOccupancy` 做了最小 fresh 验证
3. 如果通过：
   - 你已把 `013` 收口为 runner 尾差转绿
4. 如果未通过：
   - 你已把单一第一阻塞点压成
     - runner 自身具体问题，或
     - 外部并发 blocker
5. 你已在拿到结论后立刻 `Stop` 并退回 `Edit Mode`
6. 你已把这轮结果追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\memory_0.md`

## 最小回执格式

- 已回写文件路径
- `SaplingGhostOccupancy` 是否已在干净窗口稳定转绿：`yes / no`
- 当前单一第一进展或第一阻塞点
- 一句话摘要

## 一句话总口径

你这轮只干一件事：在新的干净 sapling-only 窗口里验证 patched runner，能绿就收口，不能绿就把 blocker 再压窄，但绝不回头把范围放大。
