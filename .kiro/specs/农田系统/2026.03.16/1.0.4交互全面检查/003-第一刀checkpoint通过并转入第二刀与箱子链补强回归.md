# 003-第一刀checkpoint通过并转入第二刀与箱子链补强回归

你这一刀我先给明确结论：

- `checkpoint 接受`

这一刀已经证明两件事：

- 你确实把 `ChestInventory / ChestInventoryV2 / ChestController` 从“双向事件互踢”收成了 `ChestInventoryV2 authoritative + legacy mirror 静默桥接`
- 当前最直接阻断交互验收的 `StackOverflowException` 已经被切掉了

所以第 1 刀不是无效劳动，这一点我认账。

但你现在还没有资格把这条线表述成：

- “箱子链已经彻底稳定”
- “箱子系统已经完全收口”

原因也很具体，不是我故意抬杠，而是你这轮证据层还差一块关键拼图。

## 一、当前审核发现

你这轮新增的 `ChestInventoryBridgeTests`，现在实际覆盖到的是：

- legacy 写入到 V2
- V2 写回 legacy mirror
- 快速 set / clear 序列不再递归爆栈

这说明：

- bridge 内存层最小闭环已经通了

但它还没有覆盖下面两条真正容易复发的路径：

1. `Save() / Load()` 持久化回归
2. 真实箱子打开、关闭、重开后的 UI 绑定刷新链

换句话说：

- 你这轮拿到的是“bridge 运行时最小稳定”
- 不是“整条箱子交互链彻底稳定”

这点你必须自己口径收紧，不准外推。

## 二、第 1 刀裁定

这一刀我给你的真实裁定是：

- `第 1 刀 checkpoint 通过`
- 但必须带一个附加要求：
  - 在进入或推进第 2 刀时，顺手补一条 `save/load` 或 `reopen chest UI` 回归验证

这里不是让你回头重写箱子系统。  
而是要求你把“bridge 通了”再往前补一格，避免后面用户一存档、一重开箱子，你这条线又翻车。

## 三、现在正式切入第 2 刀

你后续主线不要漂移，继续按既定硬顺序推进：

1. 第 1 刀已完成：箱子递归同步 / 爆栈阻断清除
2. 现在进入第 2 刀：
   - 树苗快速连续放置时的“幽灵占位”
   - 箱子放置时导航 retry 3 次后原地停下

这两条你不能拆成两个互不相干的小 bug。

你必须正面把这几件事统一起来：

- 导航到达判定
- 可放置距离
- 预览碰撞体边界
- 最终成功放置边界
- 玩家移动中的快速点击时序
- 放置失败或成功后的表现刷新 / 回滚时机

如果这些口径不统一，“幽灵占位”和“retry 3 次原地停下”后面还会反复冒出来。

## 四、这一轮新增的强制补强项

除了第 2 刀本体之外，你这轮还必须额外补一条箱子回归：

最低要求二选一：

1. `Save() -> Load()` 后箱子内容仍一致，且不会重新触发递归
2. 打开箱子 -> 放入 / 取出 / 清空 -> 关闭 -> 重新打开，UI 与库存镜像保持一致

如果你能两个都补上更好；  
如果本轮只能补一个，优先补：

- `Save() / Load()` 持久化回归

因为这一层目前是现成代码里最明显还没被你验证到的薄弱点。

## 五、本轮执行纪律

你这一轮不要再回到：

- 继续长篇分析
- 再写一版“大方向理解”
- 一边修第 2 刀，一边顺手发散到耐久 / Tooltip / Toolbar

这轮的目标很清楚：

- 把第 2 刀真正打进去
- 同时把箱子链的持久化或重开验证补上一格

不要重新摊大饼。

## 六、本轮回执格式

下一次回执，严格按这个格式回：

- 当前在改什么：
- 当前是第几刀：
- 第 1 刀补强回归是否已完成：
- 第 2 刀目标是否已完成：
- changed_paths：
- 新增或修改了哪些测试 / 验证：
- 用户可直接复测的步骤：
- 当前剩余 blocker：
- blocker_or_checkpoint：
- 一句话摘要：

## 七、最后再钉一次口径

你这轮不是没做事。  
相反，第 1 刀已经把最危险的爆栈主因切掉了。

但治理口径必须准确：

- 可以说：`第 1 刀 checkpoint 已通过`
- 不能说：`箱子链已经彻底稳定`

现在继续往前推，不要回头梦游。
