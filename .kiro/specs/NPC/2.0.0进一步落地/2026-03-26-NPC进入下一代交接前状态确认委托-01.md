# 2026-03-26-NPC进入下一代交接前状态确认委托-01

这轮不是继续做 NPC 新功能，也不是继续争取 `Primary.unity` 的 scene 写窗口。

这轮只做一件事：

先判断你当前是否已经满足进入“下一代线程交接”的条件；如果满足，就直接进入交接；如果不满足，就停下回执，不要硬进。

## 当前唯一主刀

只做 `NPC` 这条线程的“交接前状态确认”。

明确禁止：

1. 不继续 scene 级写入；
2. 不继续碰 `Primary.unity`；
3. 不继续顺手修导航 / 交互 / 表现层；
4. 不把这轮变成新的 hygiene 或 owner 扫地。

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\2026-03-26_V1交接文档统一写作Prompt.md`
2. 你自己的线程记忆：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
3. 你当前直接相关的工作区 `memory.md`
4. 你最新一轮回执中关于：
   - `Primary.unity` dirty + unlocked + mixed
   - read-only-first
   - 可否安全开工
   的判断依据。

## 你必须先回答的问题

你先判断：

> 当前 `NPC` 线程是否已经满足“可以把当前状态交给下一代线程”的条件？

判断标准至少包括：

1. 当前主线是不是已经清楚；
2. 当前 own / non-own 边界是不是已经清楚；
3. 当前 blocker 是否已经清楚且可交代；
4. 当前没有必须由你先继续补完、否则交接会失真的关键动作；
5. 当前线程虽然不能开工，但是否已经足够“稳定可接手”。

## 分支 A：如果你判断“可以进入交接”

那你不要再等下一轮确认，直接执行：

1. 按统一写作 prompt 生成交接包；
2. 正式写入：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\`
3. 未来继任线程名按新规则计算：
   - 当前线程名不带版本号，则为 `NPCV2`
4. 交接完成后，只能追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
5. 聊天按最小格式回执。

## 分支 B：如果你判断“暂时不能进入交接”

那你必须停下，不准硬进交接。

你只按下面格式回执：

- 当前是否可进入交接：`no`
- 当前单一第一阻塞点是什么
- 为什么这个阻塞点会让交接失真或失去意义
- 到什么条件成立后，才可以进入交接
- 一句话摘要

## 这轮最小聊天回执格式

如果进入交接：

- 已回写文件路径
- 是否完成本轮要求
- 一句话摘要

如果未进入交接：

- 当前是否可进入交接
- 当前单一第一阻塞点
- 进入交接所需最小条件
- 一句话摘要

## 一句话总口径

这轮先回答“NPC 现在能不能安全进入下一代交接”；如果能，就直接进；如果不能，就停在清晰阻塞点，不要硬做。
