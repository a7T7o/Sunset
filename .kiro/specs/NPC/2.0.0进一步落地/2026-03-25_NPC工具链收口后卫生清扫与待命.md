# NPC 工具链收口后卫生清扫与待命

这轮我先把你的线程角色重新钉死：

1. 我接受你这轮已经把 `003` 工具链正规化推进到当前安全边界；
2. 我也接受你现在不该越界去抢导航主战场；
3. 当前用户主注意力不在 NPC 新功能推进，而在导航 / 农田；
4. 所以你这轮唯一任务不是再扩实现，而是做 hygiene + 待命。

---

## 一、当前已接受的基线

当前我接受这些事实：

1. 你 own 的核心代码文件当前 shared root 里没有继续挂 dirty；
2. 你的两次提交已经在 `main`；
3. 你前一轮已经主动删除了一个旧起稿残留；
4. 你现在停下来的理由是合理的：
   - 不抢热区
   - 不抢导航主战场
   - 不替别线代收共享资源脏改

但这不等于 hygiene 已经自动完成。

---

## 二、这轮唯一主刀

### 这轮唯一主刀固定为：

> 不继续写 NPC 新实现，  
> 只做你自己这条线的卫生清扫、边界确认和待命收口。

---

## 三、这轮必须做的 hygiene

你必须做一次只读自扫，并明确回答：

1. 你这轮 own 的代码 / 测试 / 文档当前是否都已 clean；
2. 当前 shared root 里那些带 `NPC` 痕迹的脏改：
   - 哪些不是你的
   - 哪些你不能碰
   - 哪些如果真是你的残留，你会怎么处理
3. 你 own 的：
   - `NPCBubbleStressTalker.cs`
   - `NPCPrefabGeneratorTool.cs`
   - `NPCSceneIntegrationTool.cs`
   - `NPCAutoRoamControllerEditor.cs`
   - `NPCToolchainRegularizationTests.cs`
   - NPC 工作区 / 线程 memory
   当前是否已全部收干净
4. 当前是否还有你 own 的 draft / 临时文档 / 过时说明残留没删

---

## 四、这轮允许做什么

### 允许：

1. 删除你 own 的旧 draft / 临时文档 / 过时产物；
2. 如果发现你 own 的 memory 口径还没和当前停手状态对齐，补一次最小同步；
3. 如果发现某个 shared-root 资源其实真是你的残留，但不涉及热区抢写，允许做最小清扫。

---

## 五、这轮明确禁止

### 不允许：

1. 不准继续写 NPC 新功能；
2. 不准碰导航主战场；
3. 不准替别线代收 `Primary.unity` / TMP 材质 / `TagManager.asset` 这类共享热区；
4. 不准把“现在没活了”写成一句空话交差。

---

## 六、这轮完成定义

只有下面这个结局才算完成：

### 结局：NPC 线 hygiene 已收口，并进入待命

你要明确给出：

1. 当前你 own 的代码 / 测试 / 文档是否 clean；
2. 这轮删掉了哪些残留；
3. 保留了哪些文件，为什么；
4. 当前 shared root 里那些仍带 `NPC` 痕迹的脏改，哪些不是你 own；
5. 你现在是否已经进入“等待导航 / 用户后续需要时再接入”的待命状态。

---

## 七、下一次回执固定格式

- 当前在改什么
- 这轮是否仍只做 hygiene，不继续 NPC 新实现
- 当前实际清扫了哪些文件 / 文档 / 草稿
- 当前保留了哪些文件，为什么
- 当前 own 的代码 / 测试 / 文档是否 clean
- 当前 shared root 里仍带 `NPC` 痕迹但不是你 own 的脏改有哪些
- 当前是否已进入待命状态
- blocker_or_checkpoint
- 一句话摘要
