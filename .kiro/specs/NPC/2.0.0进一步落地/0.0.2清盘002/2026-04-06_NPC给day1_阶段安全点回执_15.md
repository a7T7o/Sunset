# 2026-04-06｜给 day1｜NPC 阶段安全点回执 15

## 一句话总判断

`NPC` 这边这轮已经把 `formal consumed -> resident fallback` 又往下压了一层，不再只停在闲聊 bundle，连“玩家靠近村民时的 nearby 轻反馈”也接成了 `StoryPhase aware` 的常驻居民语义；这意味着你后面在 day1 导演/runtime 里吃回时，不只是“按 E 闲聊”能回 resident，连平时擦肩而过的居民反应也能按 phase 变味。

## 这轮我实际新增做成的内容

### 1. resident fallback 从“闲聊 bundle”继续压到“nearby feedback”

我这轮补的是：

1. `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
   - 新增 `PhaseNearbySet`
   - 新增 `phaseNearbyLines`
   - `GetPlayerNearbyLines(relationshipStage, storyPhase)`
   - `TryGetPhaseNearbySet(...)`
2. `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
   - 新增 `GetPlayerNearbyLines(relationshipStage, storyPhase)` 透传
3. `Assets/YYY_Scripts/Service/Player/PlayerNpcNearbyFeedbackService.cs`
   - 当前靠近 NPC 的轻反馈，现在会先读 `NpcInteractionPriorityPolicy.ResolveCurrentStoryPhase()`
   - nearby line 解析改成按 `storyPhase` 走

结果就是：

- formal 已消费后，不只是闲聊主链能掉回 resident
- 玩家靠近村民时的自动轻反馈，也能按当前剧情阶段切 resident 语义

### 2. crowd bootstrap 已把 8 人的 phase nearby lines 真落进资产

我已经把下面这条生成链补齐：

1. `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
   - `ApplyDialogueContentPayload` 会写 `phaseNearbyLines`
   - 新增 `BuildPhaseNearbySets`
   - 新增 `BuildPhaseNearbyPayloads`
   - 新增 `PhaseNearbyPayload`
   - 8 个 NPC 都补了 phase nearby resident lines

当前覆盖口径：

1. `101`：`EnterVillage / DinnerConflict / DayEnd`
2. `102`：`ReturnAndReminder / FreeTime / DayEnd`
3. `103`：`EnterVillage / DinnerConflict / DayEnd`
4. `104`：`WorkbenchFlashback / DinnerConflict / DayEnd`
5. `201`：`HealingAndHP / DinnerConflict / DayEnd`
6. `202`：`FarmingTutorial / ReturnAndReminder / DayEnd`
7. `203`：`DinnerConflict / ReturnAndReminder / DayEnd`
8. `301`：`ReturnAndReminder / FreeTime / DayEnd`

### 3. validation 菜单和 tests 也一起补了

我没有只写 runtime 代码，连护栏也补了：

1. `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`
   - 新增 `ValidatePhaseNearbyCoverage(...)`
   - 现在会检查每个 NPC 应有的 `phase nearby set` 是否存在、是否非空
2. `Assets/YYY_Tests/Editor/NpcInteractionPriorityPolicyTests.cs`
   - 新增 `NearbyFeedback_ShouldPreferPhaseSpecificResidentLines_WhenCurrentStoryPhaseMatches`
3. `Assets/YYY_Tests/Editor/NpcCrowdDialogueNormalizationTests.cs`
   - 新增 `CrowdDialogueAssets_ShouldContainPhaseAwareResidentNearbyLines`
4. `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
   - 新增 `PlayerNpcNearbyFeedback_UsesPhaseAwareResidentNearbyLines`

## 这轮我已经拿到的真值

### 代码层 / 编译层

以下文件 `manage_script validate` 已过：

1. `NPCDialogueContentProfile.cs`
2. `NPCRoamProfile.cs`
3. `PlayerNpcNearbyFeedbackService.cs`
4. `SpringDay1NpcCrowdBootstrap.cs`
5. `SpringDay1NpcCrowdValidationMenu.cs`
6. `NpcInteractionPriorityPolicyTests.cs`
7. `NpcCrowdDialogueNormalizationTests.cs`

`SpringDay1DialogueProgressionTests.cs` 这次没有拿到同规格 `manage_script` 绿卡，不是 own red，而是那一瞬间 Unity 工具口抽风 / 外部 console 噪音干扰；`validate_script` 返回的是：

- `native_validation=clean`
- `owned_errors=0`
- `assessment=external_red`

外部噪音是现场已有的：

1. `The referenced script (Unknown) on this Behaviour is missing!`
2. 字体 `ellipsis` warning
3. `OcclusionTransparency` warning

我没有把这类东西 claim 成我自己的 red。

### 资产 / 菜单层

我已经重新跑过：

1. `Tools/NPC/Spring Day1/Refresh Crowd Dialogue Assets`
2. `Tools/NPC/Spring Day1/Validate New Cast`

最新 `Editor.log` 里仍然是：

- `[SpringDay1NpcCrowdValidation] PASS | npcCount=8 | totalPairLinks=16`

### diff 层

我这轮 own 路径跑过：

- `git diff --check`

当前 own 路径是过的，没有新的 trailing whitespace 尾巴。

### 测试层

这里我给你报实两层：

1. `NPC Formal Consumption Tests`
   - 当前最后一份稳定结果仍是：
   - `Library/CodexEditorCommands/spring-day1-npc-formal-consumption-tests.json`
   - `passCount=3`
   - `failCount=0`
2. `Resident Director Bridge Tests`
   - 当前最后一份稳定结果还是之前那次：
   - `Library/CodexEditorCommands/npc-resident-director-bridge-tests.json`
   - 之前稳定是 `passed`
   - 我这次重新点了一次菜单，但结果文件停在 `started/running`，没给出新的 completed/passed 收口，所以我不把这次 rerun 包装成新通过

也就是说：

- 我能继续认的是“上一次稳定 pass 还在”
- 我不能乱认的是“这次 rerun 也已经稳定 pass”

## 你现在可以直接吃回什么

你现在可以直接吃回两层 resident 真值：

### A. phase informal fallback

也就是我前一刀已经给你的：

- `phaseInformalChatSets`

### B. phase nearby resident feedback

这是我这刀新补的：

- `phaseNearbyLines`

所以从 day1 视角，你现在可以把 NPC 当成：

1. formal 未消费时
   - 还能进正式剧情
2. formal 已消费后
   - 按当前 `StoryPhase` 回落到 resident / informal
3. 玩家只是靠近、不一定按 E 时
   - 也能按当前 `StoryPhase` 吐出不同的居民近身反应

## 我没继续吞、也仍然不建议我继续吞的部分

这轮我还是没有去碰：

1. `SpringDay1Director.cs`
2. `SpringDay1NpcCrowdDirector.cs`
3. `Town.unity`
4. `Primary.unity`
5. UI 玩家面提示壳
6. `GameInputManager.cs`

所以现在真正还留给你主刀的，仍然是：

1. resident deployment / scene 常驻落位
2. director 真正吃回这些 resident phase 语义
3. “看起来像本来就住在村里”这件事在 runtime 里的最终闭环

## 我这边当前安全点判断

我认为现在已经到了一个适合你调度、也适合我先停表的安全点：

1. NPC own 这边这轮该补的语义底座已经补到位了
2. 资产也已经刷新
3. cast validation 还是 PASS
4. own `git diff --check` 过了
5. 再往下如果我继续压，就更容易越界去吞 deployment / director / Town runtime

## 你如果下一步要继续吃 NPC 这边，我建议优先这样用

1. 先把 resident 常驻部署吃回
   - 不要再只把 101~301 当“到点刷出来的 crowd”
2. 吃回时同时看两层字段
   - `phaseInformalChatSets`
   - `phaseNearbyLines`
3. formal 已消费后的承接，不要只顾着“按 E 闲聊”
   - 也要考虑“靠近时的 resident 轻反馈”已经能按 phase 变味
4. 如果 runtime 里还看起来像“居民突然冒出来”
   - 先优先排 deployment / resident roots / director consumption
   - 不要先反打回 NPC content 底座

## 一句话收尾

这轮我新增交给你的，不是又一批泛对白，而是 `resident fallback` 又下一层的真实 contract：现在 NPC 不只会在“按 E 闲聊”时按 phase 回 resident，连“玩家靠近村民时”的常驻反馈也已经 phase-aware 了；后面该你主刀吃回的，主要就是 deployment / director / runtime 常驻落位。
