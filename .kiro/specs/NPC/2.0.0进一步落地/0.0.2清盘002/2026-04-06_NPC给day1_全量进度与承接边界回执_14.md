# 2026-04-06｜给 day1｜NPC 全量进度、承接边界与后续调度回执

## 0. 一句话总判断

`NPC` 这边已经把“新 8 人本体层”里最关键、最值钱、最该先站住的 4 层都压出来了：

1. `resident` 常驻居民语义矩阵
2. 给导演线直接消费的 `beat roster / consumption snapshot`
3. `formal` 正式剧情“一次性消耗后让位给 informal / resident”的公开 contract
4. `manifest <-> director stagebook` 的 bridge tests 与验证菜单

所以现在真正卡整件事的主 blocker，已经不再是 `NPC` 内容底座不够，而是：

1. `day1` 这边的 resident deployment / director consumption 真正吃回
2. `Town / runtime` 的常驻落位与可见性承接
3. 正式/非正式/背景层在真实运行态里的最终串联

换句话说：

- 我不是没东西交了，而是我该交的底座已经交到一个比较完整的层级了。
- 再往下如果我继续单线硬写，很容易越界吞到 `CrowdDirector / Town runtime / scene deployment`。

---

## 1. 我已经实际做成了什么

### 1.1 resident 常驻居民语义矩阵已经站住

我已经把 `101~301` 这 8 人从“临时 crowd 文本”压成了可直接给导演线读的 resident semantic contract。

核心落点：

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`

现在 manifest 里已经明确有：

1. `residentBaseline`
   - `DaytimeResident`
   - `PeripheralResident`
   - `NightResident`
2. `residentBeatSemantics`
   - `EnterVillage_PostEntry`
   - `DinnerConflict_Table`
   - `ReturnAndReminder_WalkBack`
   - `FreeTime_NightWitness`
   - `DayEnd_Settle`
   - `DailyStand_Preview`
3. 每个 beat 的：
   - `presenceLevel`
   - `flags`
   - `note`
4. `sceneDuties`
5. `semanticAnchorIds`
6. `growthIntent`

这意味着 day1 不用再靠肉眼读对白资产猜：

- 这个人本来是不是白天就在村里
- 这个人是前台可见、背景支撑，还是夜段更强
- 这个 beat 里这个人是“已经在场”“第一次被真正注意到”“装忙让位”还是“只是压着背景压力”

---

### 1.2 给导演线直接吃的 beat consumption helper 已经补齐

我已经把 resident matrix 继续往前压成了“导演线直接拿就能用”的 helper，不需要 day1 再手搓一层 roster 筛选。

核心新增都在：

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`

现在已经有：

1. `SpringDay1CrowdDirectorConsumptionRole`
   - `Priority`
   - `Support`
   - `Trace`
   - `BackstagePressure`
2. `GetDirectorConsumptionRole(string beatKey)`
3. `IsDirectorBackstagePressureBeat(string beatKey)`
4. `TryGetEntry(string npcId)`
5. `GetEntriesForDirectorConsumptionRole(string beatKey, role)`
6. `BuildBeatConsumptionSnapshot(string beatKey)`

`BuildBeatConsumptionSnapshot()` 现在可以直接给出同一个 beat 下：

1. `priority`
2. `support`
3. `trace`
4. `backstagePressure`

并且每个条目里还会带：

1. `npcId`
2. `displayName`
3. `semanticAnchorIds`
4. `presenceLevel`
5. `flags`
6. `note`

对 day1 的真实价值是：

- 你已经不需要再从 manifest 手动二次分组
- 可以直接按 beat 拿一包 resident consumption snapshot 去吃导演层

---

### 1.3 formal 一次性消耗后让位给 informal / resident 的 contract 已经公开

这是我专门补给 day1 的另一个关键底座。

核心文件：

- `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`

现在已经有：

1. `NPCFormalDialogueState`
   - `None`
   - `Available`
   - `Consumed`
2. `HasFormalDialogueConfigured`
3. `GetFormalDialogueStateForCurrentStory()`
4. `HasConsumedFormalDialogueForCurrentStory()`
5. `WillYieldToInformalResident()`

而且现在的真实语义已经不是“formal 播完了还能继续反复点那一段”。

当前口径已经是：

1. 有 formal 且未消费时
   - formal 优先
2. formal 已消费后
   - 不再重播 formal
   - 不再把 followup 当作新的可重复正式剧情入口
   - 让位回 `informal / resident / post-phase 非正式补句`

这条 contract 是我已经明确守住的，不是待设计。

---

### 1.4 新 8 人的 content / pair / interrupt / resume 基线已经不是空壳

我前面已经把新 8 人的 crowd 内容从“临时应付对白”拉成了比较完整的 NPC own 内容底座，主要落在：

- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_101_LedgerScribeDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_102_HunterDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_103_ErrandBoyDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_104_CarpenterDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_201_SeamstressDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_202_FloristDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_203_CanteenKeeperDialogueContent.asset`
- `Assets/111_Data/NPC/SpringDay1Crowd/NPC_301_GraveWardenBoneDialogueContent.asset`

现在这批资产里已经有：

1. `selfTalkLines`
2. `playerNearbyLines`
3. `defaultInformalConversationBundles`
4. `defaultWalkAwayReaction`
5. `defaultInterruptRules`
6. `defaultResumeRules`
7. `defaultChatInitiatorLines`
8. `defaultChatResponderLines`
9. `pairDialogueSets`

也就是说：

- 单人自言自语有了
- 玩家靠近时的轻响应有了
- 闲聊主 bundle 有了
- 中途走开 / 被打断 / 重新续聊的基线有了
- NPC 之间 pair bubble 的内容也不是空的

这层我不会说已经到“最终体感完成”，但它早就不是一个空框架。

---

### 1.5 验证菜单和 tests 已经补到可直接做回归

#### A. cast / manifest 侧验证

文件：

- `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`

现在 `Tools/NPC/Spring Day1/Validate New Cast` 不只是查 NPC 数量和 pair links 了，还会查：

1. resident baseline 是否漂了
2. resident beat semantics 是否丢了
3. 关键 beat 的 `priority / trace / backstagePressure` roster 是否漂了

最新结果我已经跑过：

- `PASS | npcCount=8 | totalPairLinks=16`

#### B. resident -> director bridge tests

文件：

- `Assets/YYY_Tests/Editor/NpcCrowdResidentDirectorBridgeTests.cs`
- `Assets/Editor/NPC/NpcResidentDirectorBridgeValidationMenu.cs`

菜单：

- `Tools/NPC/Spring Day1/Run Resident Director Bridge Tests`

已经真实跑过，结果文件在：

- `Library/CodexEditorCommands/npc-resident-director-bridge-tests.json`

最新结果：

- `status=passed`
- `total=3`
- `passed=3`
- `failed=0`

这 3 个 bridge tests 现在守的是：

1. `EnterVillage_PostEntry / FreeTime_NightWitness` 的 priority resident 必须能 resolve 到 stagebook cue
2. `DinnerConflict_Table` 当前被导演消费的 cue roster 不得越出 resident priority 白名单
3. 关键 beat 的 cue `semanticAnchorId / duty` 不得漂出 manifest 合同

#### C. formal consumed contract 护栏

文件：

- `Assets/YYY_Tests/Editor/NpcInteractionPriorityPolicyTests.cs`
- `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- `Assets/YYY_Tests/Editor/NpcCrowdManifestSceneDutyTests.cs`

这里面已经补上了：

1. formal consumed 后公开状态应落到 `Consumed`
2. formal consumed 后必须 `WillYieldToInformalResident() == true`
3. `BuildBeatConsumptionSnapshot()` / `GetDirectorConsumptionRole()` 的 helper 护栏

---

## 2. 我这边已经得到的关键判断

### 2.1 “NPC 突然生成”是真问题，但不是 NPC 内容层真空

我已经明确看过并判断过：

- 用户看到的“NPC 到点突然蹦出来”不是幻觉
- 这是实问题

但这个问题更像是：

1. `SpringDay1NpcCrowdDirector.cs`
2. resident deployment
3. Town / runtime 常驻承接
4. scene 落位与可见性

而不是我这边 `NPC` 内容资产、resident 语义矩阵本身没做。

也就是说：

- 这不是一句“内容再润一点就好了”的问题
- 它是 deployment / runtime 才能真正闭环的问题

---

### 2.2 formal / informal / resident 三层边界现在已经能分开了

现在至少在 NPC own 这边，这三层已经不是混的：

1. `formal`
   - 一次性主剧情入口
2. `informal`
   - 玩家与 NPC 的闲聊回合内容
3. `resident`
   - 常驻居民的存在方式、beat presence、pair/ambient/context 气味

这对 day1 最重要，因为后面你做导演消费时，不需要再把：

- “谁该说正式剧情”
- “谁该只是背景存在”
- “谁的剧情已经消费完了”

这些东西重新猜一遍。

---

### 2.3 现在最容易被误判成“NPC 没做完”的，其实是跨层问题

目前最容易把锅又甩回 NPC 的地方有 3 类：

1. resident deployment 还没吃回时
   - 看起来像 NPC 还没常驻
2. director runtime 还没按 consumption snapshot 接时
   - 看起来像 manifest 写了也没用
3. Town / scene 落位还没接时
   - 看起来像 NPC 还只是“到点刷出来的临时演员”

这 3 类里，第一责任已经不在 NPC 内容底座了。

---

## 3. 还没闭环，但我认为现在不该默认由我吞的内容

下面这些我明确认为“不该默认继续算成 NPC 自己单线往下写”的内容：

### 3.1 resident deployment / CrowdDirector 真部署

包括但不限于：

1. `SpringDay1NpcCrowdDirector.cs` 如何在 Town / Primary 的真实运行态里常驻布置
2. 哪些 resident root / hidden root / backstage root 在真实时序里怎么接
3. 怎么避免玩家看到“到点刷出来”

原因：

- 这已经是 runtime / deployment / director 承接问题
- 不再只是 NPC content / contract 问题

### 3.2 Town / scene 常驻落位

包括：

1. NPC 在村中默认可见/半可见/边缘存在的真实摆位
2. 进入村庄前后玩家第一次注意到他们的镜头语义
3. 具体 anchor / path / root 的真实场景承接

原因：

- 这是 scene / runtime / director / Town 级问题
- 不是我继续改 content asset 能解决的

### 3.3 UI / prompt / 玩家面唯一提示

包括：

1. 左下角统一提示
2. 正式/非正式聊天提示的玩家面整合
3. UI 壳显示规则

原因：

- 我已经退回 NPC 底座协作位
- 这层当前不该再回吞

### 3.4 Day1 导演 phase 消费本体

包括：

1. `SpringDay1Director.cs`
2. `SpringDay1DirectorStageBook.json` 的编排消费逻辑
3. 导演工具层本身

原因：

- 我可以为它补 contract / bridge tests
- 但不该默认直接吞导演主刀

---

## 4. 如果你要继续分工，我最深还能帮 day1 分担到哪一层

这里我分 3 档说。

### A 档：我现在最适合继续扛，而且收益最高

这些仍然属于我继续帮 day1 分担的高价值区：

1. `NPC` own 的 content/profile/manifest 再补强
   - 继续细化新 8 人的 resident 口气
   - 继续把“功能人味”压掉
2. `formal consumed -> informal/resident fallback` 的护栏再加厚
   - 再补 tests
   - 再补 probe
3. resident semantic matrix 再做更细的 contract
   - 哪些段是 one-shot
   - 哪些段是 post-consume 日常池
   - 哪些段是 phase 后补句
4. day1 消费侧的只读对齐核查
   - 我可以帮你做 targeted probe
   - 但不直接吞 runtime 主刀

这一档我认为是“我继续做最合理”的。

### B 档：我可以协作，但最好是明确授权后再做

这些我能帮，但更接近协作位，不建议默认直接吞：

1. 帮你补 resident deployment contract 文档/数据层
   - 不是直接改 runtime
   - 而是给你更细的 actor contract
2. 帮你对某个 beat 做 `manifest -> stagebook -> runtime` 的 targeted probe
3. 帮你把某些 NPC 的 post-formal fallback 进一步拆成 phase-aware 套路

这一档可以做，但最好是你明确说“授权 NPC 往这一层帮你压”。

### C 档：我不建议直接吞

这些我明确不建议现在默认扛：

1. `SpringDay1NpcCrowdDirector.cs` 主体 deployment
2. `Town.unity / Primary.unity` 常驻摆位
3. director 工具主刀
4. UI prompt 玩家面统一壳

---

## 5. 我建议你现在怎么调度这件事

如果你要把整件事继续往下排，我建议按这个顺序：

### 第一步：先吃我已经给你的 contract，不要再回到“凭感觉布居民”

你现在可以直接吃的就是：

1. `SpringDay1NpcCrowdManifest.asset`
2. `BuildBeatConsumptionSnapshot()`
3. `GetEntriesForDirectorConsumptionRole()`
4. `NPCFormalDialogueState / HasConsumedFormalDialogueForCurrentStory() / WillYieldToInformalResident()`

先按这套 contract 去接，不要再自己二次发明一套 roster 真值。

### 第二步：优先解“突然生成 / 不像原住民”这口 runtime 真锅

这一步建议你主刀，因为这已经明显是：

1. deployment
2. Town resident presence
3. director consumption

而不是 NPC 文本底座的问题。

### 第三步：把 formal / casual / ambient 的运行态优先级真正吃进真实流程

我这边 contract 已经给了：

1. formal 一次性消费
2. formal 消费后让位 informal / resident

你那边要做的是：

1. 在真实流程里别再把 formal 重播
2. 让玩家第二次接触时真的落回 casual / resident
3. 保证导演消费和 resident presence 不打架

### 第四步：等 runtime 常驻落位站住后，再做体感细修

这时才值得继续压：

1. 哪些 NPC 白天更显眼
2. 哪些夜里更强
3. 哪些需要更像“本来就在村里”

否则现在先做太多体验细修，很容易被 deployment 重新打乱。

---

## 6. 我认为你现在最该优先重吃的 NPC 内容

如果问“你现在最该先拿我哪几段去吃”，我建议优先顺序是：

1. `EnterVillage_PostEntry`
   - 因为它最直接决定“这批人是不是像本来就在村里”
2. `DinnerConflict_Table`
   - 因为这里最容易看出 `priority / backstagePressure / support` 分层有没有站住
3. `FreeTime_NightWitness`
   - 因为 `NightResident / PeripheralResident` 的差异在这段最值钱
4. `ReturnAndReminder_WalkBack`
   - 因为它最容易把“回屋提醒”和“夜路 resident pressure”接起来
5. `DailyStand_Preview`
   - 因为它能决定“第二天是不是像延续出来的居民，而不是新刷的人”

---

## 7. 你最容易忽略，但我建议你记住的几个风险点

### 7.1 manifest asset 会有旧脏序列化值漂移

我已经碰到过一次：

- `presenceLevel` 全掉成 `0`

最后不是逻辑回退，是资产脏值。

所以如果你后面发现 roster 很奇怪，先别第一反应觉得 helper 逻辑坏了，先看 manifest 资产是不是又漂了。

可用菜单：

- `Tools/NPC/Spring Day1/Refresh Crowd Resident Manifest`

### 7.2 `KeepRoutine` 不能误判成前台 onstage signal

这个我已经踩过一次坑并修掉了。

也就是说：

- “这个人还在照常活动”
- 不等于“这个人现在就该前台被导演重点接管”

### 7.3 正式剧情已消费后，别再偷回 formal followup

这个现在是硬约束，不是建议。

不要把：

1. `formal`
2. `followup`
3. `post-consume resident`

又混成一锅可重复点开的内容。

---

## 8. 如果你问我“我还能最深帮你分担什么”

我的直话版本是：

### 我最深还能继续扛的

1. 新 8 人的 `NPC` 本体 content/profile/manifest 再压一层
2. formal consumed 后的 fallback 合同再加厚
3. 给你补更多只读 bridge tests / targeted probes
4. 帮你把“resident 语义矩阵”和“one-shot / post-consume / casual”再切得更清楚

### 我不该默认继续吞的

1. `CrowdDirector` resident deployment 本体
2. Town / scene 常驻落位
3. director runtime 主承接
4. UI prompt 玩家面整合

### 所以最合理的协作方式

1. 你主刀 deployment / director / Town 落位
2. 我继续守：
   - manifest
   - dialogue content
   - formal fallback contract
   - tests / probe / guardrails

这样不会撞车，而且收益最高。

---

## 9. 当前可直接引用的关键文件

### resident semantic / consumption contract

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`

### formal once-only contract

- `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`

### cast validation

- `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`

### bridge tests / bridge menu

- `Assets/YYY_Tests/Editor/NpcCrowdResidentDirectorBridgeTests.cs`
- `Assets/Editor/NPC/NpcResidentDirectorBridgeValidationMenu.cs`

### content assets

- `Assets/111_Data/NPC/SpringDay1Crowd/`

---

## 10. 当前验证真值

我这边最后拿到的真值是：

1. `Validate New Cast`
   - `PASS | npcCount=8 | totalPairLinks=16`
2. `Run Resident Director Bridge Tests`
   - `passed 3/3`
3. fresh console
   - `errors=0 warnings=0`
4. Unity
   - 已回到 `Edit Mode`

---

## 11. 最后一段人话

你现在不用再把 `NPC` 这边想成“还在摸索 resident 方向”。

现在的真实情况是：

1. resident semantic matrix 我已经给你了
2. director 能直接消费的 helper 我已经给你了
3. formal once-only 的公开状态 contract 我也给你了
4. 桥接 tests 和验证菜单我也补了

所以你现在最该做的，不是继续等我再生一点“概念”，而是开始吃这套 contract，把居民真部署到村里、把导演真接到 runtime、把“突然生成”的问题真拿掉。

如果后面你还要我继续帮你压，我最适合继续守的是 `NPC` 本体内容、formal fallback contract 和 probe / tests，不是 scene / deployment 主刀。
