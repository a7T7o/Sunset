# 2026-04-06｜给 day1｜NPC 阶段安全点回执 16

## 1. 当前主线

这轮我继续只做 `NPC` own 的 resident 常驻语义、`formal consumed` 之后的 resident/informal 回落，以及给 `day1` 真要吃的 validation/contract。

我没有继续往 `runtime spawn / deployment / Town resident 主逻辑` 漂，也没有回吞 `Primary / Town / GameInputManager / CrowdDirector 主刀`。

---

## 2. 这轮实际做成了什么

### 2.1 phase-aware resident selfTalk 已经打通到资产层

我把 `NPC` 的长停自语从“只有默认常量文案”补成了真正按 `StoryPhase` 取 resident 句池：

1. `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
   - 新增 `PhaseSelfTalkSet`
   - 新增 `phaseSelfTalkLines`
   - 新增 `GetSelfTalkLines(StoryPhase)`
   - 新增 `TryGetPhaseSelfTalkSet(...)`
2. `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
   - 新增 `GetSelfTalkLines(StoryPhase)` 透传
3. `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
   - 长停自语入口改成优先走当前 `StoryPhase`
   - 新增 `TryShowResidentSelfTalk(...)`
4. `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
   - 新增 `BuildPhaseSelfTalkSets`
   - 新增 `BuildPhaseSelfTalkPayloads`
   - 8 个 crowd NPC 都补了 phase selfTalk
5. 8 份 `Assets/111_Data/NPC/SpringDay1Crowd/*DialogueContent.asset`
   - 已真实刷新出 `phaseSelfTalkLines`

结果就是：

- `formal` 过去以后，居民不只是“能聊”和“靠近会回话”
- 连平时自己停下来嘀咕的内容，也会按当前剧情阶段变味

### 2.2 phase-aware walkAwayReaction 继续补深

我继续把 `formal consumed -> resident/informal fallback` 往下补了一层，不再只是 phase bundle 存在，而是 phase 内被玩家中途走开时的反应也按 beat 变味：

1. `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
   - 新增 `BuildPhaseWalkAwayReactionPayload(...)`
   - `phaseInformalChatSets` 现在会优先吃 phase-specific `walkAwayReaction`
2. 8 份 crowd dialogue assets
   - 已真实刷入对应的 phase-specific walkAway cue
   - 例如：
     - `101-enter-village-walkaway`
     - `203-return-reminder-walkaway`
     - `301-free-time-walkaway`

结果就是：

- 同一个 NPC 在不同 beat 下被玩家聊到一半走开时
- 不再只掉回同一套默认模板
- 而是更像“这个居民此刻正处在什么阶段、心里在挂什么事”

### 2.3 validation / tests 也一起补了

这轮不是只写内容源头，我把护栏也一起补了：

1. `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`
   - 新增 `ValidatePhaseSelfTalkCoverage(...)`
   - 还顺手给 runtime targeted probe 补了 selfTalk 段位入口，后面如果要 live 核实可以直接用
2. `Assets/YYY_Tests/Editor/NpcCrowdDialogueNormalizationTests.cs`
   - 新增 `CrowdDialogueAssets_ShouldContainPhaseAwareResidentSelfTalkLines`
   - 新增 `CrowdDialogueAssets_ShouldContainPhaseAwareWalkAwayReactions`
3. `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
   - 新增 `NpcAutoRoamSelfTalk_UsesPhaseAwareResidentLines`
   - 新增 `CrowdBootstrap_UsesPhaseAwareWalkAwayReactionsAfterFormalConsumed`

### 2.4 当前爆红已止血

你刚刚报的两条红：

- `SpringDay1NpcCrowdDirector.cs(1166,46)`
- `SpringDay1NpcCrowdDirector.cs(1177,51)`

我已经做了最小 blocker fix：

1. `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
   - 补回缺失的 `EnumerateAnchorNames(...)`
2. fresh `errors`
   - 现在是 `errors=0 warnings=0`

这一步我按 `carried foreign blocker fix` 处理，没有顺手扩 `CrowdDirector` 主逻辑。

---

## 3. 现在还没做成什么

下面这些我明确还没 claim done：

1. `Town` 原生 resident 的真实 scene 落位
2. 用户手摆 `home anchor` 后，`day1` / `Town` runtime 真正把这批居民原生吃回
3. resident 不再“像突然生成”的最终观感闭环
4. runtime targeted probe 里的 selfTalk 段位
   - 代码入口我已经补了
   - 但这轮没有再去跑 live 取证，不乱 claim

---

## 4. 当前阶段

我认为现在阶段已经很明确：

`NPC` 这边不是“底座还不够”，而是“NPC own 的 resident 语义、formal fallback、content density、validation 护栏又厚了一层，下一手最该由 day1/Town 去吃原生常驻承接”。

也就是说：

- 我这边继续补 `NPC` own 的收益还在
- 但已经从“补基础功能”变成“补深 resident contract / tests / probe”
- 不该再把 `runtime resident deployment` 当成我这边的主线

---

## 5. 下一步只做什么

如果我下一轮继续开工，最合理只做这三类：

1. 继续补 `NPC` own 的 native-resident-facing contract / tests / probe
2. 继续补 `formal consumed` 之后的 resident / informal / post-phase 内容差异
3. 继续做更窄的 bridge / validation，帮助 `day1` 吃 manifest / fallback contract

不会再默认去做：

1. `Town` scene resident deployment
2. `CrowdDirector` 主消费逻辑
3. runtime 假身位 / 临时生成预热

---

## 6. 需要用户现在做什么

现在最需要的是：

1. `day1` 先按这轮新增 contract 继续吃回
2. 如果你要继续分工，明确下一刀是：
   - 让我继续补 `NPC` own contract / probe
   - 还是让 `day1 / Town` 主刀去吃原生 resident 落位

如果只是问我现在是否适合继续自己往下吞 `runtime resident`：

- 不适合

---

## 7. 我这轮最核心的判断是什么

我的核心判断是：

`NPC` 这边现在最值钱的继续推进，不是再去做“居民怎么突然出现”的 runtime 适配，而是把 resident 常驻内容层和 formal-consumed 回落 contract 压得更厚，让 day1/Town 吃回时不会空、不会薄、不会只有壳。`

---

## 8. 我为什么认为这个判断成立

因为这轮我已经拿到了这些真值：

1. `Validate New Cast`
   - `PASS | npcCount=8 | totalPairLinks=16`
2. fresh `errors`
   - `errors=0 warnings=0`
3. 这轮最贴的 EditMode 小集
   - job `succeeded`
4. own 路径 `git diff --check`
   - 通过
5. Unity
   - 当前是 `Edit Mode`

再加上这轮实际新增的是：

1. `phaseSelfTalkLines`
2. phase-specific `walkAwayReaction`
3. 对应 validation / tests
4. blocker fix

这说明：

- `NPC` own 的 content / contract / guardrail 还在继续增值
- 但“看起来像突然生成”的那口问题，已经不再是内容层主责

---

## 9. 我这轮最薄弱、最可能看错的点是什么

我这轮最不放心的点有两个：

1. `SpringDay1NpcCrowdValidationMenu` 里新补的 selfTalk runtime probe
   - 代码我已经补好了
   - 但这轮没有再 live 跑它
   - 所以我只 claim “validation 入口已补”，不 claim “runtime selfTalk live 已证实”
2. `CrowdDirector` 的 carried foreign blocker fix
   - 我只做了最小止血
   - 没有继续深入读它的 resident deployment 主链
   - 所以我只 claim “爆红已清”，不 claim “那条线的逻辑已经被我顺便看透”

---

## 10. 我给自己这轮结果的自评

我给这轮一个 `8/10`。

满意的点：

1. 这轮没有漂去 `runtime spawn`
2. 把 resident 常驻内容层又补厚了两层
3. 把爆红清掉了
4. 没把 Unity 留在 `Play Mode`

不满意的点：

1. selfTalk runtime probe 只补了入口，还没 live 证明
2. 这轮没有继续把 phase-specific resume/interrupt 再往下压

---

## 技术审计层

### changed_paths

1. `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
2. `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
3. `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
4. `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
5. `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`
6. `Assets/YYY_Tests/Editor/NpcCrowdDialogueNormalizationTests.cs`
7. `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
8. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_101_LedgerScribeDialogueContent.asset`
9. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_102_HunterDialogueContent.asset`
10. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_103_ErrandBoyDialogueContent.asset`
11. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_104_CarpenterDialogueContent.asset`
12. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_201_SeamstressDialogueContent.asset`
13. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_202_FloristDialogueContent.asset`
14. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_203_CanteenKeeperDialogueContent.asset`
15. `Assets/111_Data/NPC/SpringDay1Crowd/NPC_301_GraveWardenBoneDialogueContent.asset`
16. `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`

### 验证

1. `validate_script`
   - `NPCDialogueContentProfile.cs`：`errors=0`
   - `NPCRoamProfile.cs`：`errors=0`
   - `NPCAutoRoamController.cs`：`errors=0 warnings=1`
   - `SpringDay1NpcCrowdBootstrap.cs`：`errors=0`
   - `SpringDay1NpcCrowdValidationMenu.cs`：`errors=0`
   - `NpcCrowdDialogueNormalizationTests.cs`：`errors=0`
   - `SpringDay1DialogueProgressionTests.cs`：`errors=0`
   - `SpringDay1NpcCrowdDirector.cs`：`errors=0 warnings=2`
2. `Tools/NPC/Spring Day1/Refresh Crowd Dialogue Assets`
   - 已执行
3. `Tools/NPC/Spring Day1/Validate New Cast`
   - `PASS | npcCount=8 | totalPairLinks=16`
4. targeted EditMode test job
   - `status=succeeded`
5. `py -3 scripts/sunset_mcp.py errors --count 20 --output-limit 10`
   - `errors=0 warnings=0`
6. `git diff --check`
   - 通过

### thread-state

1. `Begin-Slice`
   - 已执行
2. `Ready-To-Sync`
   - 未执行
   - 原因：这轮还没准备 sync
3. `Park-Slice`
   - 待本轮 memory / 回执写完后执行
4. 当前状态
   - `ACTIVE`
