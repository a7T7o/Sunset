# 2026-03-27-NPC-0.0.2清盘002-中继续推提示-非正式聊天首个闭环切片-01

日期：`2026-03-27`  
状态：`生效中`  
目标线程：`NPC`

---

## 1. 先把你当前状态钉死

你这轮没有跑偏。

你刚刚做的这两件事是对的，而且保留：

1. 已把 `0.0.2清盘002` 的现状回退矩阵落出来
2. 已把 `0.0.2清盘002` 的详细任务账本落出来

所以这条提示不是让你回头重写，也不是让你切去 `Primary.unity`。

这条提示只做一件事：

> 让你保持当前 `0.0.2` 方向继续干，但不要继续停在“写文档解释问题”，而是立刻进入第一个真正可玩的非正式聊天闭环切片。

---

## 2. 这轮唯一主刀

这轮唯一主刀固定为：

> 在不破坏 `001` 现有 `spring-day1` 正式剧情入口的前提下，先把 `002 / 003` 的 NPC 非正式聊天首个闭环做出来。

更具体一点：

1. 玩家靠近 `002` 或 `003`
2. 玩家按 `E`
3. 先出现玩家自己的气泡
4. 玩家气泡文本有动效
5. NPC 延迟 `0.5 ~ 1.0s`
6. NPC 面向玩家
7. NPC 回复自己的气泡
8. 再按一次 `E`
9. 先跳过当前文本动效，不跳过整句
10. 能继续推进到下一轮对话
11. 玩家中途跑开时，先有一个最小可见的中断分支

这轮先只要求：

- `002 / 003` 跑通
- 至少 2 轮来回
- 至少 1 个中断反应入口

不要一上来把所有 NPC、所有关系分支、所有 UI 美化一次吞完。

---

## 3. 这轮不是让你做什么

这轮不是让你：

1. 回去继续扩 `0.0.2` 文档正文
2. 去做 `Primary.unity`
3. 去碰 `GameInputManager.cs`
4. 去全面重构 `spring-day1`
5. 去先把所有提示 UI 美化完
6. 去把好感度、表情包、跑路、正式剧情、内容包库一次全做满

现在最重要的是先把“真正的按 `E` 发起的双向 NPC 非正式聊天”打出第一个可玩闭环。

---

## 4. 当前边界怎么理解

### 4.1 `001`

`001` 当前已经有 `spring-day1` 正式剧情入口。

这轮对 `001` 的要求是：

1. 不破坏它现在能进入正式剧情的链
2. 如需兼容未来 NPC 非正式聊天，只允许先预留边界
3. 不准为了统一入口，把 `001` 现有正式剧情链拆烂

### 4.2 `002 / 003`

这轮真正要打通的是：

1. `002` 按 `E` 能进入 NPC 非正式聊天
2. `003` 按 `E` 能进入 NPC 非正式聊天

所以这轮优先级明确是：

- `002 / 003` 先跑通
- `001` 先保持稳定

### 4.3 提示 UI

用户说当前 NPC 提示框丑，这个判断成立。

但这轮正确处理方式是：

1. 如果要改，只做“支撑本轮闭环所需的最小 NPC 提示整理”
2. 不要把工作台提示 UI 一起吞掉
3. 不要让“先美化提示框”抢走“先把聊天闭环做出来”的主刀

---

## 5. 建议你这轮直接按这个顺序干

### 第一步

停止继续扩解释文档，保留你已经落的 `0.0.2` 两份主文档即可。

### 第二步

把第一刀实现锁在：

- `T-002-C01`
- `T-002-C02`
- `T-002-C03`
- `T-002-C04`

也就是：

1. 会话对象
2. 玩家发起回合
3. NPC 回复回合
4. `E` 键推进 / 跳过动效

### 第三步

如果你发现现有 `NPCDialogueInteractable` 适合作为近距 `E` 键触发外壳，就复用它。

但硬条件是：

1. 不准破坏 `001` 当前 `spring-day1` 正式剧情绑定
2. 如果复用它会把 `001` 搅乱，那就新增 NPC 自己的非正式聊天入口组件 / 会话服务
3. 不要为了“统一”而把正式剧情轨和非正式聊天轨揉烂

### 第四步

在 `002 / 003` 首个闭环已经能跑的前提下，再补：

- `T-002-E01`

也就是：

- 玩家跑开会进入最小中断分支

这轮的中断分支先不要求你把所有情绪做满，但至少要做到：

1. 能识别跑开
2. 玩家侧有一句中断表达，或者 NPC 侧有一句最小反应
3. 这个反应不是“像什么都没发生”

### 第五步

只有当上面已经跑通，你才决定要不要顺手补最小验收文档。

不要还没跑通闭环，就又停在文档层整理。

---

## 6. 这轮允许的真实取舍

为了让你别被范围压死，这轮允许你这样取舍：

1. 关系阶段
   - 先只接到：
     - 取句差异
     - 最小中断反应差异
   - 不要求第一刀就做满好感度结算系统

2. 情绪表现
   - 先允许只用：
     - 气泡文本
     - 简单浮字 / 简单状态提示
   - 不要求第一刀就上复杂表情包系统

3. 内容包库
   - 先用最小结构支撑：
     - 玩家发起句
     - NPC 回复句
     - 中断句
   - 不要求第一刀就做完整海量内容

关键是：

先把闭环打出来，再继续往内容厚度和观感厚度加。

---

## 7. 这轮明确禁止

1. 不准切去 `Primary.unity`
2. 不准切去 `GameInputManager.cs`
3. 不准把 `001` 正式剧情链拆掉重来
4. 不准把工作台提示 UI 业务一起吞掉
5. 不准继续只写“现状解释 / 任务文档”而不进实现
6. 不准把“玩家靠近时 NPC 单向冒一句”重新包装成“闭环已完成”

---

## 8. 这轮完成定义

这轮只有满足下面这些，才算你真的继续对了：

1. `002 / 003` 至少一名 NPC 已经能按 `E` 进入 NPC 非正式聊天
2. 玩家气泡先出现
3. NPC 延迟回复且面向玩家
4. `E` 键优先跳过当前动效，而不是直接跳句
5. 至少能推进到第二轮
6. 跑开时至少进入一个最小中断反应
7. `001` 的 `spring-day1` 正式剧情链没有被你搞坏

---

## 9. 固定回执格式

聊天里只按下面格式回复，不要再写成长解释稿：

```text
已回写文件路径：
当前在改什么：
当前首个闭环切片：
这轮是否已从文档层进入实现层：`yes / no`
当前跑通到哪一步：
changed_paths：
是否触碰 `Primary.unity / GameInputManager.cs`：
当前 own 路径是否 clean：
blocker_or_checkpoint：
一句话摘要：
```

---

## 10. 一句话总口径

你现在方向是对的，不要停；但从这一刻开始别再扩文档解释，直接把 `002 / 003` 的按 `E` 发起、玩家先说、NPC 再回、可跳过动效、可中断的第一个 NPC 非正式聊天闭环打出来。
