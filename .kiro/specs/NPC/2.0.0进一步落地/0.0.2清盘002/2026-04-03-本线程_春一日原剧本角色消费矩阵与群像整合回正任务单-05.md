# 2026-04-03｜本线程自用｜春一日原剧本角色消费矩阵与群像整合回正任务单

这份不是给 `NPC-v` 的。

这是我给自己下一轮用的任务单。

用户已经明确指出：

- 当前 `101~301` 的人物设定很可能是我后补发挥，不一定是原案
- 现在不能继续把这批人包装成“Day1 剧情已经完整扩上来了”

所以我下一轮不再先冲运行态 claim。

我要先把：

- 原剧本里真正有哪些 Day1 角色
- 工程里现有 `NPC001 / NPC002 / NPC003` 各自是什么语义
- 当前 `101~301` 应该被当成什么

这三件事一次说死。

---

## 0. 当前统一认知

### 0.1 原案真相来源已经查实

这轮后续判断，优先按下面顺序：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_Gemini\1.0.0策划\002_事件编排重构\Deepseek聊天记录001.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\0.0阶段\0.0.1剧情初稿\春1日_坠落_融合版.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\0.0阶段\0.0.1剧情初稿\初步规划文档.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_Gemini\1.0.0策划\001_剧本篇章集\000_Deepseek-2\Deepseek-2-P1.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_Gemini\1.0.0策划\001_剧本篇章集\000_Deepseek-2\省流版VIP8.md`

### 0.2 原 Day1 主角色链已经能明确钉住

当前至少可以确定：

- `马库斯`
  - 村长
- `艾拉`
  - 村长女儿
- `卡尔 / 研究儿子`
  - 村长二儿子
  - 研究 / 魔法学徒向角色
- `大儿子`
  - 在外打猎
  - 原设存在，但并未在 Day1 当前主镜头里被落实成稳定在场角色
- `老铁匠 / 老乔治`
- `老杰克`
- `老汤姆`
- `小米`
- `围观村民 / 饭馆村民 / 小孩`

### 0.3 当前工程里的老 Day1 角色底座更接近原案

当前已经查实：

- `Assets/111_Data/NPC/NPC_001_VillageChief*`
- `Assets/111_Data/NPC/NPC_002_VillageDaughter*`
- `Assets/111_Data/NPC/NPC_003_Research*`

这表示：

- `NPC001` 才是当前 Day1 的 `VillageChief` 主链底座
- `NPC002` 才是当前 Day1 的 `VillageDaughter` 主链底座
- `NPC003` 当前更像 `Research` 语义底座

所以：

- 当前 `101~301` 不能覆盖这三条老主链
- 它们最多只能作为“新增群众层 / 次级角色层 / 后续扩展层”

### 0.4 当前 crowd 技术状态不是这个问题的答案

即使 `NPC-v` 已经拿到：

- `8/8 instance` 过
- `8/8 informal` 过
- `2/2 walk-away` 过
- `2/2 pair bubble` 失败但已压到 `NPC-v own`

这也只能证明：

- 当前 8 人本体链大体能跑

不能证明：

- 他们现在的名字 / 身份 / phase 语义就是对的

---

## 1. 我这轮唯一主刀

只做一刀：

- 继承当前已有的 crowd integration 结构底座
- 但先把“原剧本角色消费矩阵”和“当前群像整合误差”查实
- 必要时只做最小回正，让 `spring-day1` 不再把现编 8 人设误判成 Day1 正式剧情已完成

一句话：

- 这轮不是继续吹 “群像已经扩上去”
- 这轮是先纠正 “Day1 到底扩了谁、还没扩谁、现在这批人算什么”

---

## 2. 我这轮必须先读的文件

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_Gemini\1.0.0策划\002_事件编排重构\Deepseek聊天记录001.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\0.0阶段\0.0.1剧情初稿\春1日_坠落_融合版.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\0.0阶段\0.0.1剧情初稿\初步规划文档.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_Gemini\1.0.0策划\001_剧本篇章集\000_Deepseek-2\Deepseek-2-P1.md`
5. `D:\Unity\Unity_learning\Sunset\Assets\111_Data\NPC\NPC_001_VillageChiefDialogueContent.asset`
6. `D:\Unity\Unity_learning\Sunset\Assets\111_Data\NPC\NPC_002_VillageDaughterDialogueContent.asset`
7. `D:\Unity\Unity_learning\Sunset\Assets\111_Data\NPC\NPC_003_ResearchDialogueContent.asset`
8. `D:\Unity\Unity_learning\Sunset\Assets\Resources\Story\SpringDay1\SpringDay1NpcCrowdManifest.asset`
9. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPC\SpringDay1NpcCrowdBootstrap.cs`
10. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\Managers\SpringDay1NpcCrowdDirector.cs`
11. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\Managers\SpringDay1Director.cs`

---

## 3. 我这轮具体要做什么

### 3.1 先做一张“原 Day1 角色消费矩阵”

至少按下面这些阶段梳理：

1. `CrashAndMeet`
2. `EnterVillage`
3. `HealingAndHP`
4. `WorkbenchFlashback`
5. `FarmingTutorial`
6. `DinnerConflict`
7. `ReturnAndReminder`
8. `FreeTime`
9. `DayEnd`

每个阶段至少回答：

1. 原剧本里必须在场的角色是谁
2. 哪些只是氛围群众，不要求具名
3. 哪些阶段根本不该提前冒出一大批后补角色

### 3.2 再做“现有工程承载矩阵”

至少拆成三组：

1. `老 Day1 主角色链`
   - `NPC001 / VillageChief`
   - `NPC002 / VillageDaughter`
   - `NPC003 / Research`
2. `当前 crowd 新 8 人`
   - `101~301`
3. `尚无稳定承载的原案角色`
   - 例如老杰克 / 老乔治 / 老汤姆 / 小米 / 未具名围观村民等

### 3.3 再做“当前整合误差判断”

至少要回答：

1. 当前 `101~301` 是否错误地抢了老 Day1 主角色的戏份
2. 当前 manifest 的 phase 配置，是否把一些没有原案依据的人物过早放进了 Day1 主链
3. 当前这批 crowd，是否更应被判为：
   - `群众层 / 次级氛围层`
   - 而不是 `原 Day1 核心角色层`

### 3.4 如果证据已经足够，只做最小回正

如果在不碰 `Primary` 的前提下，已经足够明确当前 manifest 的语义写偏了，

那么只允许做最小回正，优先顺序：

1. 回正文档和判断口径
2. 必要时最小改 `SpringDay1NpcCrowdManifest.asset` 的 `displayName / roleSummary / phase`
3. 若不改 manifest 也能先说死，就先不改代码和资产

目标不是马上“把 8 人全改成另一套正式角色”，而是先避免：

- 继续把明显不稳的人设误当成 Day1 已完成事实

### 3.5 把当前真实结论讲成人话

最后必须明确回答用户：

1. 这 8 个人现在大多不是用户原案里已经定好的 Day1 正式角色
2. 他们目前更多是：
   - 一批新增 crowd 槽位
   - 其中只有少数可能和原案角色语义接近
3. 用户现在在 `Primary` 里“没看到他们”
   - 不是因为已经正式整完了只是用户没找到
   - 而是因为这批人现在主要停留在：
     - 产物链
     - manifest
     - NPC-v 自己的 targeted runtime probe
   - 还没有拿到一份干净的、完全对齐原剧本的 Day1 玩家面消费结论

---

## 4. 当前不再允许做的事

- 不要继续把 `101~301` 的现编名字 / 人设当成 Day1 已确认人物表
- 不要继续把 `spring-day1` 这边的 claim 建立在这些现编人设之上
- 不要回吞 `NPC-v` 的 prefab / content / pair bubble 修复
- 不要碰 `Primary.unity`
- 不要碰 `GameInputManager.cs`
- 不要去做 UI / 字体 / 玩家面提示壳
- 不要重新把旧 `002/003` 闲聊线和这批新 crowd 混成一锅

---

## 5. 这轮允许 scope

优先只写：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1V2\memory_0.md`

如确有必要，才允许最小写：

- `D:\Unity\Unity_learning\Sunset\Assets\Resources\Story\SpringDay1\SpringDay1NpcCrowdManifest.asset`
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Story\Managers\SpringDay1NpcCrowdDirector.cs`

但默认优先：

- 先判断
- 再决定是否需要写代码

---

## 6. 完成定义

这轮完成，不是指“我又跑了一遍 phase probe”。

而是至少要同时满足：

1. 我能给出原 Day1 角色消费矩阵
2. 我能给出现有工程承载矩阵
3. 我能明确指出：
   - 哪些是老 Day1 主角色链
   - 哪些是当前新增 crowd 槽位
   - 哪些原案角色还没有被稳定承载
4. 我能明确回答：
   - 当前 `101~301` 到底算不算已经把原剧本扩上来了
5. 如果需要最小回正，我已经只在 own 范围做了最小回正；
   如果还不能改，我能报第一真实 blocker

---

## 7. 固定回执格式

回复时先按这个顺序：

1. 当前主线：
2. 这轮实际做成了什么：
3. 现在还没做成什么：
4. 当前阶段：
5. 下一步只做什么：
6. 需要用户现在做什么（没有就写无）：

然后再补技术审计层：

- changed_paths：
- 验证状态：
- 是否触碰高危目标：
- blocker_or_checkpoint：
- 当前 own 路径是否 clean：
- 原 Day1 角色消费矩阵：
  - 阶段
  - 原剧本必须角色
  - 原剧本群众层
  - 当前工程承载
- 现有工程承载矩阵：
  - 老主角色链
  - 新 crowd 槽位
  - 尚无稳定承载的原案角色
- 当前判断：
  - `101~301` 哪些是可继续保留的群众层
  - 哪些现在不应继续 claim 为正式角色
  - 这条线距离“原剧本真正扩上来”还差什么
- thread-state：
  - 是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
  - 如果没跑，原因是什么
  - 当前是 `ACTIVE / READY / PARKED / BLOCKED` 哪一个

---

## 8. 最后一条

这轮必须守住一个底线：

- 当前 `spring-day1` 线不能再把“新增 8 个 crowd 槽位”直接说成“原剧本角色已经扩完”

如果这层口径不先回正，后面跑再多 probe，也是在给错误对象做验证。

---

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
