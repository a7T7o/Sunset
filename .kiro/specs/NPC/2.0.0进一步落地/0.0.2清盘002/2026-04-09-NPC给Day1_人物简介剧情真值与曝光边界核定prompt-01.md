# 2026-04-09 NPC 给 Day1：人物简介剧情真值与曝光边界核定 prompt

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\2026-04-09-NPC人物简介排版分区与内容梳理.md`

这轮不是让你改 UI，不是让你回写 NPC 主表实现，也不是让你接关系页代码。

## 当前已接受基线

1. `NPC` 这边已经先把 11 个 NPC 的简介结构、分区逻辑和推荐文案方向梳出来了。
2. 但这份稿子目前还是 `NPC` 视角下的“可写版本”，还需要 `Day1` 从剧情真值和曝光边界角度核一遍。
3. 这轮的目标不是“把文案写华丽”，而是先判：
   - 哪些现在能写
   - 哪些会剧透
   - 哪些应该降级

## 你这轮唯一主刀

只做 `11 个 NPC 人物简介` 的剧情真值与曝光边界核定。

一句话说清：

- 你只负责判这批简介在 `Day1` 当前阶段能不能成立
- 不负责 UI 排版
- 不负责 NPC 主表实现

## 允许 scope

只允许围绕下面这些判断：

- 当前 Day1 已正式成立的角色感知
- 玩家在 Day1 里真实会知道到什么程度
- 哪些句子会超出当前曝光边界
- 哪些角色应该只留轮廓，不能写太满
- 哪些“记忆钩子”是安全的

## 明确禁止

不要碰：

- `PackageNpcRelationshipPanel.cs`
- `DialogueUI`
- `NpcCharacterRegistry` 实现
- `NPC_Hand`
- Town / Primary / 导演工具 / runtime resident 主逻辑

不要把这轮偷换成“顺手补剧情实现”。

## 完成定义

请按 `001/002/003/101/102/103/104/201/202/203/301` 逐个给出：

1. 当前可写的身份锚点
2. 当前可写的简介上限
3. 当前可写的“今日你会看到的样子”
4. 当前不能写的点
5. 是否：
   - 放行
   - 降级
   - 暂缓

## 回写要求

请回写成一份可接班的核定稿，不要只给一句“可以/不可以”。

每个 NPC 至少要有：

- `放行内容`
- `禁止越线`
- `建议口径`

## 固定回执格式

1. 当前在核什么
2. 这轮核定通过了哪些 NPC
3. 哪些 NPC 被降级或暂缓
4. 当前最大的剧情边界风险
5. NPC 下一步可以按什么口径继续写
6. 一句话摘要

## thread-state

如果这轮会继续真实施工：

- 开始前先跑 `Begin-Slice`
- 第一次准备 sync 前先跑 `Ready-To-Sync`
- 如果这轮先停或让位，先跑 `Park-Slice`
