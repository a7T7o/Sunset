# 2026-04-03｜本线程自用｜春一日新 NPC 群像 Day1 整合并行任务单

这份不是发给 `NPC-v` 的。

这是我给我自己留的并行任务单，用来保证下一轮真的是：

- `NPC-v` 负责 NPC 本体层
- 我负责 `spring-day1` / `Day1 integration` 层
- 两边并行推进，最后一起把“春一日新 NPC 群像”做完

配对的 `NPC-v` prompt 在：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\2026-04-03-NPC-v_春一日新NPC群像联合完工续工prompt-02.md`

---

## 0. 当前统一目标

不是单纯把 8 个新 NPC 资源放进仓库，也不是只让他们“理论上能出现”。

最终目标是：

1. 8 个 NPC 本体层成立
2. 8 个 NPC 在 `spring-day1` 的 phase / 时机 / 出场顺序成立
3. 群像对白与“村长逃跑已发生”这条现实一致
4. `Primary` 运行时能实际消费这批 NPC
5. 最终能判断：
   - 哪些已经完成
   - 哪些还没完成
   - 这条线能不能进 `sync-ready`

---

## 1. 我这边已经完成的基线

下面这些已经做完，不要下一轮又从头重做：

### 1.1 群像接入代码底座已存在

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
- `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`

### 1.2 8 个 NPC 的身份、人设、phase 基线已写入 bootstrap

当前继承基线：

- `101 莱札`：抄录员，`EnterVillage -> DayEnd`
- `102 炎栎`：猎户，`FarmingTutorial -> DayEnd`
- `103 阿澈`：跑腿少年，`EnterVillage -> DayEnd`
- `104 沈斧`：木匠，`WorkbenchFlashback -> DayEnd`
- `201 白槿`：织补师，`HealingAndHP -> DayEnd`
- `202 桃羽`：花铺姑娘，`FarmingTutorial -> DayEnd`
- `203 麦禾`：饭馆大姐，`DinnerConflict -> DayEnd`
- `301 朽钟`：守墓骨差，`ReturnAndReminder -> FreeTime`

### 1.3 实物资产已经生成

当前工程里已经真实落盘：

- `Assets/222_Prefabs/NPC/101.prefab ~ 301.prefab`
- `Assets/111_Data/NPC/SpringDay1Crowd/*.asset`
- `Assets/100_Anim/NPC/101~301/*`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`

`Editor.log` 已明确记录：

- `[SpringDay1NpcCrowdBootstrap] 已生成 8 名 spring-day1 新 NPC 的 prefab、profile、对话包与 crowd manifest。`

---

## 2. 这条线总共要完成的需求盘点

当前我把这件事拆成 5 类需求：

### 2.1 产物层

- sprite
- anim/controller
- prefab
- dialogue content
- roam profile
- crowd manifest

### 2.2 角色内容层

- 身份
- 人设
- 台词方向
- 与“村长逃跑”叙事的一致性

### 2.3 NPC 本体运行层

- roam 成立
- informal chat 成立
- facing / anchor / spawn offset 成立
- prefab 不空壳

### 2.4 Day1 接入层

- `StoryPhase -> 群像出场/退场` 成立
- `Primary` 里 anchor / fallback / runtime root 成立
- 不与原有 Day1 正式链乱撞

### 2.5 最终闭环层

- 区分已完成 / 未完成
- 区分 NPC-v own / 我的 own / shared blocker
- 判断 `sync-ready` 还是继续 blocked

---

## 3. 边界重新钉死

### 3.1 `NPC-v` 的区

`NPC-v` 只负责 NPC 本体层：

- `Assets/Sprites/NPC`
- `Assets/100_Anim/NPC`
- `Assets/111_Data/NPC`
- `Assets/222_Prefabs/NPC`
- `Assets/Editor/NPC*`
- NPC 自己的 content / roam / prefab / generator / informal chat 本体问题

### 3.2 我的区

我只负责 `spring-day1` / `Day1 integration`：

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- 必要时最小范围内的 `SpringDay1Director` 消费关系
- 必要时对 `Primary` 做最小 Day1 接入判断
- phase / runtime / story integration / 最终放行判断

### 3.3 明确不再混

- 我不再主做 NPC prefab / content / roam / anim 生产链
- `NPC-v` 不再主吞 Day1 integration / phase consumption / 最终 story runtime 裁定

---

## 4. 我下一轮唯一主刀

### 4.1 先做 Day1 integration preflight

只检查这些：

1. `SpringDay1NpcCrowdDirector` 是否已经能在 `Primary` 自动拉起
2. `SpringDay1NpcCrowdManifest.asset` 是否被 runtime 正确读取
3. manifest 里的 `anchorObjectName` 在 `Primary` 里是否真实可解析
4. 当前 `StoryPhase` 流程和 8 人的 `minPhase / maxPhase` 是否语义合理
5. 这 8 人是否会与已有 `NPC001/002/003` 的既有 Day1 正式链产生明显冲突

### 4.2 再做我 own 的最小修补

我只修这些：

1. crowd director 的 runtime phase 消费问题
2. manifest 和 director 之间的 Day1 侧整合问题
3. 必要但最小的 `SpringDay1Director` / story integration 接口问题
4. 若不改 scene 就能闭环，优先不改 `Primary`
5. 若 scene 必须动，只允许最小触碰并准确报实

### 4.3 最后做运行态判断

我要收的不是“文件看起来齐了”，而是：

1. 不同 `StoryPhase` 下，这批人会不会按预期出现
2. phase 外会不会不该冒的时候乱冒
3. fallback / anchor / runtime root 是否会导致明显错位
4. 这条线当前是：
   - `sync-ready`
   - 还是仍有第一真实 blocker

---

## 5. 我这边当前还没做成的事

下面这些才是我 own 区真正未闭的：

1. `Primary` 里 crowd director 的真实 phase 消费证据
2. 8 人在 Day1 实际时机里的出场/退场运行态证据
3. 与现有 `spring-day1` 事件顺序的最小整合结论
4. 最终 `sync-ready / blocked` 裁定

---

## 6. 我这轮禁止事项

- 不要回头重做 `NPC-v` 的 prefab / profile / dialogue / roam 生产链
- 不要重写 8 人设定主轴
- 不要顺手吞 UI、字体、`GameInputManager.cs`
- 不要为了省事把整个 `Primary.unity` 大改
- 不要把 NPC-v own 的脏改全认到自己头上

---

## 7. 我这轮完成定义

我这轮完成，不是指“我又写了一些代码”，而是至少要达到：

1. 我能明确说出 Day1 integration 哪些已经成立
2. 我能明确指出哪些仍是 `NPC-v` own，哪些是我 own
3. 我能给出这条线是否 `sync-ready` 的 Day1 侧判断
4. 我的回执能和 `NPC-v` 的回执拼成一张完整账，而不是两份各说各话

---

## 8. 固定回执格式

我下一轮对用户回时，先按这个顺序：

1. 当前主线：
2. 这轮实际做成了什么：
3. 现在还没做成什么：
4. 当前阶段：
5. 下一步只做什么：
6. 需要用户现在做什么（没有就写无）：

然后再补技术审计层：

- changed_paths：
- 验证状态：
- 是否触碰高危目标：
- blocker_or_checkpoint：
- 当前 own 路径是否 clean：
- 这轮总需求盘点：
  - 一共几类需求
  - 哪些是 `NPC-v` 的
  - 哪些是我的
- thread-state：
  - 是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
  - 如果没跑，原因是什么
  - 当前是 `ACTIVE / READY / PARKED / BLOCKED` 哪一个

---

## 9. 下一轮开工时先读

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`
- `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md`
- 配对文件：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\2026-04-03-NPC-v_春一日新NPC群像联合完工续工prompt-02.md`

---

## 10. 最后一条

下一轮必须记住：

- `NPC-v` 不等于我
- 我不等于 `NPC-v`
- 但这条线最终是我和他并行做完，不是让其中一边一个人吞完整案
