# 2026-04-03｜本线程自用｜春一日新 NPC 群像 Day1 剧情消费 probe 任务单

这份不是给 `NPC-v` 的。

这是我下一轮给自己用的任务单。

用户当前真正要的，不是继续讲“资源已经导好了”。

而是：

- 在现有 `spring-day1` 主剧情已经实现的基础上，
- 把新增群像真正扩进去，
- 让玩家在对的阶段看到这些人出现，并且他们说的是对的话。

---

## 0. 当前已接受基线

### 0.1 我的 integration 基线已经进主分支

当前已提交并推送：

- `03c0bf87`
- `35144958`

当前 `spring-day1V2` 已：

- `PARKED`

### 0.2 已经成立的结构基线

我这边已经做成：

- `SpringDay1NpcCrowdManifest.cs`
- `SpringDay1NpcCrowdDirector.cs`
- `SpringDay1Director.cs` 对 crowd/world-hint/player-facing 的 snapshot 接入
- `CurrentRuntimeSummary`
- anchor 解析优先回到 `*_HomeAnchor`
- `Ready-To-Sync` 和白名单 sync 已完成

### 0.3 `NPC-v` 当前基线

`NPC-v` 最新回执已经说明：

- 新 8 人产物链和 preflight 工具基本成立
- `Validate New Cast` 当前 `PASS`
- `totalPairLinks=16`
- 但 runtime targeted probe 还没开始跑
- 当前 own 路径仍不 clean，`SpringDay1NpcCrowdValidationMenu.cs` 还没归仓

所以我这轮不要再回去替它做本体层。

### 0.4 当前真正缺的不是结构，而是运行态证据

距离用户最初要的“把 Day1 剧本真正扩上来”现在还差两段：

1. `NPC-v` 的：
   - 新 8 人本体 runtime targeted probe
2. 我的：
   - `Day1 phase / runtime consumption` targeted probe

也就是说：

- 结构层大部分已经到位
- 运行时“玩家真的看见他们在对的时候出现并说对的话”还没闭环

---

## 1. 我这轮唯一主刀

只做一刀：

- 继承当前已提交的 crowd integration 基线，
- 用现有 `spring-day1` 验收入口拿到“新 8 人是否按 Day1 phase 正确出现/退场”的运行态矩阵；
- 如当前 snapshot 不足以判定，再只在我 own 范围内补最小 probe 支撑。

一句话：

- 这轮不是继续补结构。
- 这轮是把 `Day1` 消费证据拿到手。

---

## 2. 这轮允许 scope

我这轮只允许写：

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\memory.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1V2\memory_0.md`

只读参考允许：

- `Assets/Editor/Story/DialogueDebugMenu.cs`
- `Assets/Editor/NPC/SpringDay1NpcCrowdValidationMenu.cs`
- `Assets/Editor/NPC/SpringDay1NpcCrowdBootstrap.cs`
- `Assets/111_Data/NPC/SpringDay1Crowd/*`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\NPC.json`

---

## 3. 明确禁止漂移

这轮禁止：

- 不要写 `Primary.unity`
- 不要改 `GameInputManager.cs`
- 不要回吞 `NPC-v` 的 prefab / content / roam / pairDialogueSets
- 不要碰 UI / 字体 / 玩家面提示壳
- 不要顺手去修 legacy `001 / 002 / 003` scene 漂移
- 不要把这轮扩成“整个 spring-day1 全量终验”

---

## 4. 这轮具体要做什么

### 4.1 先用现有入口拿 Day1 crowd phase 矩阵

优先复用现有 `spring-day1` 验收入口：

- `DialogueDebugMenu`
- `SpringDay1LiveValidationRunner`
- `BuildSnapshot / LogSnapshot / TriggerRecommendedAction`

不要上来先发明新大工具。

### 4.2 要拿到的最小矩阵

至少把下面这些阶段跑出 crowd 读数：

1. `CrashAndMeet`
   - 预期：新群像不应提前乱冒
2. `EnterVillage`
   - 预期：`101, 103`
3. `HealingAndHP`
   - 预期：`101, 103, 201`
4. `WorkbenchFlashback`
   - 预期：`101, 103, 104, 201`
5. `FarmingTutorial`
   - 预期：`101, 102, 103, 104, 201, 202`
6. `DinnerConflict`
   - 预期：`101, 102, 103, 104, 201, 202, 203`
7. `ReturnAndReminder`
   - 预期：8 人全在
8. `FreeTime`
   - 预期：8 人全在
9. `DayEnd`
   - 预期：`301` 退场，其余 DayEnd 范围内的人按配置存在或进入结束态；如果当前导演语义要求全清，也要据实报

### 4.3 还要判断 3 件事

除了 active set，还要补清：

1. 当前 spawn 是否真的走到了预期 anchor，而不是 fallback 漂移
2. 新 8 人与 legacy `NPC001/002/003` 的正式链有没有明显冲突
3. 当前 crowd summary 是否足够支撑用户判断“Day1 剧本已经被扩进来”

### 4.4 如果现有 snapshot 不够，就只补最小 probe

如果当前 `CurrentRuntimeSummary / BuildSnapshot` 还不足以回答：

- 哪个 NPC 在
- 哪个 NPC 不在
- 是 anchor 命中还是 fallback
- 是否有明显错位

那么只允许在我 own 范围里补最小 probe 字段。

允许的最小补口示例：

- 每个 active npcId 的 position
- anchor 命中名
- 是否走 fallback

但不要扩成一整套新 editor 工具。

---

## 5. 完成定义

这轮完成，不是指“我又确认了一遍大方向”。

而是至少要达到：

1. 我能拿出 Day1 phase 消费矩阵：
   - 哪些阶段哪些新 NPC 在
   - 哪些阶段不该在的没有乱冒
2. 我能明确说出：
   - 哪些问题还是 `NPC-v own`
   - 哪些问题是 `Day1 integration own`
   - 哪些已经是 legacy scene 背景风险
3. 我能回答：
   - 距离“把 Day1 剧本真正扩上来”还差哪一段
4. 如果当前 probe 做不下去，我能报第一真实 blocker，
   而不是继续笼统说“还要跑 live”

---

## 6. 如果被 blocker 卡住，怎么报

只能分到下面三类之一：

1. `NPC-v own`
   - 比如 prefab/chat 本体没站住，导致我这边没法判断消费
2. `Day1 integration own`
   - 比如 phase / spawn / anchor / runtime summary 这层自己就不对
3. `legacy scene / Primary`
   - 比如旧 scene 现场直接阻断 probe，但这轮我不会去修 scene

---

## 7. 固定回执格式

回复时先按这个顺序：

1. 当前主线：
2. 这轮实际做成了什么：
3. 现在还没做成什么：
4. 当前阶段：
5. 下一步只做什么：
6. 需要用户现在做什么（没有就写无）：

然后再补技术审计层：

- changed_paths：
- 验证状态：
- 是否触碰高危目标：
- blocker_or_checkpoint：
- 当前 own 路径是否 clean：
- Day1 phase 矩阵：
  - `CrashAndMeet`
  - `EnterVillage`
  - `HealingAndHP`
  - `WorkbenchFlashback`
  - `FarmingTutorial`
  - `DinnerConflict`
  - `ReturnAndReminder`
  - `FreeTime`
  - `DayEnd`
- 问题归类：
  - `NPC-v own`
  - `Day1 integration own`
  - `legacy scene / Primary`
- thread-state：
  - 是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
  - 如果没跑，原因是什么
  - 当前是 `ACTIVE / READY / PARKED / BLOCKED` 哪一个

---

## 8. 开工前先读

- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdManifest.cs`
- `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
- `Assets/Resources/Story/SpringDay1/SpringDay1NpcCrowdManifest.asset`
- `Assets/Editor/Story/DialogueDebugMenu.cs`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\spring-day1V2.json`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\NPC.json`
- 配对文件：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\2026-04-03-NPC-v_春一日新NPC群像运行态probe与本体归仓续工prompt-03.md`

---

## 9. 最后一条

我这轮不要再回到“integration 已经 ready-to-sync 了，所以先停”。

用户现在真正要的，是：

- 新 8 人有没有被 Day1 在对的阶段消费
- 这件事离“剧本真正扩上来”还差哪一段

所以这轮我的工作不是讲方向，而是把这段运行态证据拿出来。

---

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
