# 2026-04-09 NPC 自刀：按简介结构稿回填文案与关系册内容 prompt

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\2026-04-09-NPC人物简介排版分区与内容梳理.md`

这轮不是继续扩 UI，也不是继续改头像链。

## 当前已接受基线

1. `NPC_Hand -> 主表同步 -> runtime 字典 -> prefab fallback` 这一刀已经站住。
2. 人物简介的排版分区与内容结构稿也已经整理出来了。
3. 当前剩下的 `NPC own` 任务，不是再发散，而是把这份结构稿正式回填成可消费内容。

## 你这轮唯一主刀

只做 `NPC` 自己名下的人物简介文案回填与关系册内容层收口。

一句话说清：

- 按既定结构稿，把内容真正收回主表 / 关系册可消费层
- 不要再去碰 UI 外壳

## 允许 scope

只允许做：

- 身份锚点文案
- 两句人物简介
- 今日剧情感知句
- 记忆钩子
- 关系册内容字段的同源收口

如果需要补充 `NpcCharacterRegistry` 或 NPC own 内容字段承载这套结构，可以做。

## 明确禁止

不要碰：

- `PackageNpcRelationshipPanel.cs` 的玩家面外壳
- `DialogueUI`
- Prompt / Workbench / Town / Primary
- 导演工具
- runtime resident 主逻辑
- UI 线程 own 的版式整合

## 完成定义

做完时至少要站住：

1. 11 个 NPC 都有统一层级的人物简介口径
2. 不再有人像百科、有人像占位
3. 关系册消费层能稳定区分：
   - 身份与位置
   - 今日你会看到的样子
   - 你为什么会记住他
4. 文案不超出 Day1 当前曝光边界

## 结构线与体验线分开报

### 结构线

- 你把哪些字段收回同源了
- 哪些内容进入主表
- 哪些内容仍待 Day1 核定

### 体验线

- 玩家现在读这页时会不会更像人物册
- 还剩哪些必须等 UI 版式合并后才能真判断

## 固定回执格式

1. 当前在改什么
2. changed_paths
3. 这轮新做成了什么
4. 还没做成什么
5. 当前第一 blocker
6. 一句话摘要

## thread-state

如果这轮会继续真实施工：

- 开始前先跑 `Begin-Slice`
- 第一次准备 sync 前先跑 `Ready-To-Sync`
- 如果这轮先停或让位，先跑 `Park-Slice`
