# 2026-03-26-NPCV2首轮启动委托-02

你现在是新线程 `NPCV2`。

这轮不是继续微调气泡，也不是继续抢导航核心代码。

## 当前已接受的基线

1. 当前最重要的下一刀已经明确为：
   - `Primary.unity` 的最小 scene 集成切片
2. 当前单一第一 blocker 也已经明确为：
   - `Assets/000_Scenes/Primary.unity` 在 working tree 中仍 dirty，且没有明确独占写归属
3. 当前 V1 已把 own / non-own 边界交清，不需要你再回去重辨析 NPC 线程到底管什么

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\00_交接总纲.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\05_当前现场_高权重事项_风险与未竟问题.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\06_证据索引_必读顺序_接手建议.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC-导航接入契约与联调验收规范.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC系统实施主表.md`

## 这轮唯一主刀

只做 `NPCV2` 接班后的：

> `Primary.unity` 安全写窗口只读复核 + `HomeAnchor` 最小 scene 切片准入裁定

更具体地说，这轮只允许你完成下面这 1 刀：

1. 先只读复核：
   - `cwd / branch / HEAD`
   - `git status --short -- 'Assets/000_Scenes/Primary.unity'`
   - `git diff --stat -- 'Assets/000_Scenes/Primary.unity'`
   - `shared-root-branch-occupancy.md`
   - `mcp-single-instance-occupancy.md`
   - `mcp-hot-zones.md`
2. 然后只回答一个问题：
   - `Primary.unity` 的安全写窗口现在是否成立，足以让你进入 `HomeAnchor` 最小 scene 切片？
3. 如果成立：
   - 只把第一刀固定成 `scene audit -> HomeAnchor`
   - 不顺手扩到路线、停留点、相遇节奏
4. 如果不成立：
   - 停在 blocker，不准硬进 scene

## 明确禁止

1. 不要先改导航核心
2. 不要先抢 `NPCAutoRoamController.cs`
3. 不要先继续做气泡表现层微调
4. 不要把 `shared-root-branch-occupancy = neutral` 误当成 scene 可直接写
5. 不要把 `Primary.unity` 的 mixed dirty 当成无主空地
6. 不要新起一批平行设计文档

## 允许的作用域

这轮只允许你读取和必要时写入这些范围：

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
6. `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
7. `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`
8. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity` 只读核查

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已经把 `Primary.unity` 当前 dirty / lock / owner 现场复核清楚；
2. 你已经明确回答 scene 写窗口是否成立；
3. 你已经把 V2 第一刀固定为：
   - `HomeAnchor` 最小 scene 切片，或者
   - 清晰 blocker
4. 你没有越界扩到导航核心、气泡微调或其它 scene 子目标；
5. 你已把这轮结果追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPCV2\memory_0.md`

## 最小回执格式

- 已回写文件路径
- 当前是否确认 scene 写窗口成立：`yes / no`
- V2 第一刀或第一 blocker
- 一句话摘要

## 一句话总口径

你这轮不是来继续补 NPC 杂项，而是要先把 `Primary.unity` 的 scene 准入条件重新读实，然后只把第一刀钉在 `HomeAnchor` 最小集成或明确 blocker 上。
