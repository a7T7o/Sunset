# 2026-03-27-NPC-P1-02-Primary场景接盘与最小点位落地委托-01

日期：`2026-03-27`  
状态：`生效中`  
目标线程：`NPC`

---

## 1. 当前已接受基线

先把这轮不能再回头争论的事实钉死：

1. `P0` 已完成，`T-P1-01` 已完成。
2. 当前 `Primary.unity` 已不是“ownerless stale mixed scene”，而是已经正式发给 `NPC` 的 scene writer 窗口：
   - 锁文件：
     - `D:\Unity\Unity_learning\Sunset\.kiro\locks\active\A__Assets__000_Scenes__Primary.unity.lock.json`
   - `owner_thread = NPC`
   - `task = primary-scene-takeover`
3. 当前 shared root 里仍同时存在这些 hot / mixed 面：
   - `Assets/000_Scenes/Primary.unity`
   - `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
   - `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
   - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese*.asset`
4. 上面这些事实这轮只作为边界，不作为你顺手 cleanup 的邀请。
5. 因此，`T-P1-02` 旧意义上的“blocked-hotfile 原地空转”到此为止；这轮你不再反复论证 scene 能不能进，而是直接在已授予的写窗里完成 NPC 自己的最小 scene 主线。

---

## 2. 这轮唯一主刀

这轮唯一主刀固定为：

> 使用当前 `Primary.unity` 的 `NPC` scene writer 窗口，完成 `P1` 的最小场景落点切片，把三名 NPC 从“只有 HomeAnchor”推进到“已经有最小真实点位对象”。

更窄一点说：

- 这轮只做 `T-P1-02 -> T-P1-03 / T-P1-04 / T-P1-05`
- 不进入 `T-P1-06`
- 不做导航 runtime
- 不做 mixed cleanup
- 不做给 `spring-day1` 的交棒

---

## 3. 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.1全面清盘\2026-03-27-NPC全盘详细落地任务列表.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.1全面清盘\2026-03-27-NPC-P1-01-三名NPC场景点位方案卡.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.1全面清盘\2026-03-27-NPC高速推进与测试排队日志.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\locks\active\A__Assets__000_Scenes__Primary.unity.lock.json`
5. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`

---

## 4. 允许的 scope

这轮只允许你动这些：

1. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.1全面清盘\2026-03-27-NPC全盘详细落地任务列表.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.1全面清盘\2026-03-27-NPC高速推进与测试排队日志.md`
4. 如需给用户交可验收说明，允许新增 1 份本轮验收包
5. 你自己按线程纪律需要回写的 `memory` / 线程记忆

这轮默认不允许改任何 `.cs`。

只有当你能明确证明“没有一个极小代码补口就连这组 scene 点位都无法成立”时，才允许升级汇报；不要自己顺手扩到代码。

---

## 5. 这轮精确要落的 scene 切片

### 5.1 先接受已有基线

1. 不重建 `001_HomeAnchor / 002_HomeAnchor / 003_HomeAnchor`
2. 不把这轮偷换成 `HomeAnchor` cleanup
3. 现有 `HomeAnchor` 继续视为 base 事实

### 5.2 这轮最小对象集

按 `P1-01` 方案卡，至少把下面这组对象真实落进 `Primary.unity`：

1. `NPC_001_Stay_SquareWatch`
2. `NPC_002_Stay_YardSun`
3. `NPC_003_Stay_StudyBench`
4. `NPC_SHARED_Meet_MainCrossing`
5. `NPC_001_Solo_PathShoulder`
6. `NPC_002_Solo_GardenEdge`
7. `NPC_003_Solo_ObservationEdge`

### 5.3 层级约束

1. 如需层级整理，最多只允许新增一个总根：
   - `NPC_P1_Points`
2. 除这组点位对象及其必要层级归拢外，不准顺手改别的 scene 结构。
3. 命名必须和方案卡一致，不要临场再起新名。

### 5.4 语义约束

每个点位都必须满足两件事：

1. 名字和方案卡一致
2. 空间位置和方案卡语义一致

不要为了“先落进去”就把三个人的点位堆在同一小块区域里。

---

## 6. 这轮明确禁止

1. 不准把这轮改成 `Primary.unity` mixed cleanup。
2. 不准吞 `DialogueChinese*` 字体。
3. 不准碰 `GameInputManager.cs`。
4. 不准碰 `NPCAutoRoamController.cs`、导航 runtime、`PlayerAutoNavigator.cs`。
5. 不准顺手进入 `T-P1-06`、路线接线、时间组织、行为调度。
6. 不准顺手做 broad scene polish、无关层级整理或别的对象搬家。
7. 不准自己直接把 scene writer 交给 `spring-day1`。

---

## 7. 完成定义

这轮只有同时满足下面这些，才算你这刀闭环：

1. `T-P1-02`
   - 不再停留在“旧 blocker 复述”
   - 而是已经被你转成明确的 scene 写入边界说明
2. `T-P1-03 / T-P1-04 / T-P1-05`
   - 上面列出的最小对象集已经真实落进 `Primary.unity`
3. 你已补一份用户可读的本轮验收包，至少写清：
   - 本轮新增了哪些功能点
   - 具体新增了哪些 scene 对象
   - 在 Unity 里怎么找到
   - 用户现在能验什么
   - 现在还不能验什么
4. 你已更新任务列表与高速日志
5. 这轮结束后停在：
   - `ready-for-warden-review`
   - 不是你自己直接切到 `spring-day1`

---

## 8. 这轮要交的用户可读结果

你这轮不能只回最小聊天回执。

你还必须额外交一份详细汇报，要求：

1. 按功能点组织，而不是按文件堆清单
2. 讲清楚每个功能点用户可以怎么验
3. 给出 Unity 内的操作路径
4. 给出失败时应该怎么理解

建议你直接新增一份类似下面口径的文件：

- `2026-03-27-NPC-P1-03-05-Primary场景最小点位落地验收包.md`

---

## 9. 固定回执格式

聊天里只按下面格式回复，不要再写长解释稿：

```text
已回写文件路径：
当前在改什么：
当前 scene slice：
scene_objects_added：
changed_paths：
是否触碰高危目标：
当前 own 路径是否 clean：
Primary 锁当前状态：
ready_for_release：
blocker_or_checkpoint：
一句话摘要：
```

其中：

- `是否触碰高危目标` 这轮应明确写 `yes（仅 Primary.unity）`
- `ready_for_release`
  - 只回答你是否已经做到“可交给父线程复核后决定是否释放给 spring-day1”
  - 不是让你自己当场放锁

---

## 10. 一句话总口径

这轮不是让你继续讨论 scene 能不能写，而是让你用已经发给你的 `Primary.unity` 写窗，完成三名 NPC 的最小点位对象落地，然后停在可复核、可决定是否放给 `spring-day1` 的 checkpoint。
