# NPC 高速推进与测试排队日志

- 版本：v1.0
- 日期：2026-03-27
- 状态：进行中
- 用途：
  - 作为这次 NPC “高速列车”模式下的滚动执行日志
  - 记录每一刀的推进、已测结果、未测原因、测试排队和下一刀
  - 避免后续继续丢失“做到哪了 / 哪些已验 / 哪些想测但没测成”

## 1. 使用规则

- 每推进一刀，就追加一条记录。
- 每次只记录 4 类事实：
  - 当前做了什么
  - 当前有没有去测
  - 如果没测成，为什么
  - 下一刀准备做什么
- 这里记录的是“滚动执行事实”，不是完整需求正文。
- 正式任务定义仍以：
  - `2026-03-27-NPC全盘详细落地任务列表.md`
  为准。

## 2. 当前总线状态快照

- 当前主线：
  - 从 `P0` 已完成，正式进入 `P1`
- 当前最近已确认完成：
  - `T-P0-01`
  - `T-P0-02`
  - `T-P0-03`
  - `T-P0-04`
  - `T-P0-05`
- 当前下一刀：
  - `T-P1-01`
- 当前不准备硬撞的面：
  - `Primary.unity` mixed cleanup
  - `DialogueChinese*`
  - `NPCAutoRoamController.cs` 导航 runtime 改刀

## 3. 测试队列约定

- `已测通过`
  - 已有明确结果，可暂时从测试队列移出
- `待补测试`
  - 逻辑已推进，但还没补到合适验证
- `测试受阻`
  - 想测，但当前现场不允许或工具不给力
- `用户已确认`
  - 已由用户在 Unity 现场直接确认

## 4. 滚动记录

### 2026-03-27｜高速启动

- 当前完成：
  - 用户已确认 `001 / 002 / 003` 的 `Home Anchor` Inspector 现场正确。
  - `P0` 相关任务正式回填为完成态。
  - 已建立本日志，后续按它持续滚动。
- 当前测试结果：
  - `T-P0-01`
    - `用户已确认`
  - `T-P0-05`
    - `用户已确认`
    - 且用户明确说明后续不再强求额外 live / 多余测试
- 当前测试受阻：
  - `unityMCP` 会话层此前仍出现过 `no_unity_session`
  - 因此后续若再遇到“想测但测不成”，直接记这里，不反复原地打转
- 下一刀：
  - 先做 `T-P1-01` 三名 NPC 场景点位方案卡
  - 再看是否能并行预建 `P2` 内容载体

### 2026-03-27｜P1 收口并前置铺开 P2 内容资产层

- 当前完成：
  - `T-P1-01` 已正式收口为完成态，方案卡保持为当前有效的最小 scene 点位依据。
  - 已新增 `NPCDialogueContentProfile`，把角色内容从漫游参数里拆成独立资产层。
  - 已为 `001 / 002 / 003` 新增三份正式内容资产，分别补入：
    - 单人环境气泡
    - 玩家轻响应句池
    - 两两相遇矩阵
  - `NPCAutoRoamController` 当前相遇链已按 partner `npcId` 取 pair-specific 句池，不再只吃一套通用相遇文案。
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - `Editor.log`
    - 已看到 `Requested script compilation / CompileScripts / Reloading assemblies`
    - 当前未抓到这刀对应的新 `error CS`
- 当前测试受阻：
  - `refresh_unity`
    - 等待 editor ready 超时
  - `read_console`
    - 返回 `no_unity_session`
  - 因此这轮仍按“静态闸门 + Editor.log 编译迹象”记账，不把 live 验收冒充成已完成
- 下一刀：
  - 先复核 `T-P1-02`
  - 若 `Primary.unity` 仍是 `blocked-hotfile`，则继续转做 `T-P3-01`

### 2026-03-27｜Primary 继续 blocked 时，先补 P2 验收包与 P3-01 正式规范

- 当前完成：
  - 已再次只读复核 `T-P1-02`：
    - `Primary.unity` 仍是大体量 mixed diff
    - 当前不能因为 shared root 处于 `main + neutral` 就误判 scene 可直接写
    - 因此 `T-P1-02` 继续维持 `blocked-hotfile`
  - 已补出 `T-P2-07`：
    - `2026-03-27-NPC-P2-07-角色化内容验收包.md`
  - 已补出 `T-P3-01`：
    - `2026-03-27-NPC-P3-01-玩家与NPC双气泡视觉规范正式稿.md`
- 当前测试结果：
  - 本轮以静态事实收口为主：
    - 三份内容资产
    - 三份 `roamProfile` 映射
    - `NPCToolchainRegularizationTests` 已覆盖 pair-specific 内容断言
  - 未新增 live 验收冒充结果
- 当前测试受阻：
  - `Primary.unity`
    - 仍不适合当前线程直接写入
  - `unityMCP`
    - 会话层仍不稳定，不能把 live 触发覆盖率说成已拿到
- 下一刀：
  - 先评估 `T-P3-02` 的代码切口是否能避开当前 mixed hot-file
  - 如果玩家侧热文件仍不适合写，就继续找不撞热区的下一项独立切片

### 2026-03-27｜P3-02 先只落玩家气泡样式层，不碰输入与 scene

- 当前完成：
  - 已只修改：
    - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
  - 当前玩家气泡已从“照抄 NPC 基线”切到正式玩家样式：
    - 晨雾青绿色填充
    - 深青灰边框
    - 深墨色文字
    - 更轻一点的整体浮动
    - 更轻一点的尾巴跳动
  - 同时继续保留与 NPC 的共骨架：
    - `10` 字一行
    - 同级边距
    - 同级尾巴尺寸
    - 同级 show / hide 节奏
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - 这轮还没有 claim live 视觉实证
- 当前测试受阻：
  - `git-safe-sync`
    - 首次白名单收口被同根尾账拦住：
      - `Assets/YYY_Scripts/Service/Player/PlayerInteraction.cs`
    - 当前已审明这不是外线 hot-file，而是此前玩家反馈 / `ToolUseCommitResult` 链留下的同根尾账
  - `PlayerInteraction.cs`
    - 现阶段不再按“外线 mixed”处理
    - 而是按“同根旧尾账需要一并收口”处理
  - `GameInputManager.cs`
    - 仍是 hot-file，不在这刀内接轻响应
  - 因此这轮只 claim `T-P3-02` 的样式层完成
- 下一刀：
  - 继续看 `T-P3-03`
  - 重点是能不能在不碰 `GameInputManager.cs` 的前提下，把玩家靠近轻响应先接到安全入口

### 2026-03-28｜T-P3-03 以纯 player-side 邻近反馈服务接上

- 当前完成：
  - 已新增：
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcNearbyFeedbackService.cs`
  - 已补 `.meta`：
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcNearbyFeedbackService.cs.meta`
  - 已修改：
    - `Assets/YYY_Scripts/Service/Player/PlayerToolFeedbackService.cs`
  - 当前玩家靠近 NPC 时，已可由 player-side service：
    - 扫描附近 `NPCAutoRoamController`
    - 读取 `roamProfile.PlayerNearbyLines`
    - 直接驱动对应 NPC 的 `NPCBubblePresenter`
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - 白名单 sync 已通过代码闸门：
    - `Assembly-CSharp`
  - 这轮还没有 claim live 场景实证
- 当前测试受阻：
  - `unityMCP`
    - 当前仍不适合把 live 近身触发覆盖率记成已拿到
  - `Primary.unity`
    - 仍 blocked，不做 scene 辅助接线
- 下一刀：
  - 继续看 `T-P3-04`
  - 如果还能在非热区推进，就把“正式对话轨 / 日常气泡轨分工”先收成安全切片

### 2026-03-27｜T-P3-04 分轨接线完成，并补出 P3-05 验收包

- 当前完成：
  - 已修改：
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcNearbyFeedbackService.cs`
  - 已新增最小静态回归断言：
    - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - 当前正式对话轨 / 日常气泡轨的最小分工已经落下：
    - 玩家侧轻反馈会监听 `DialogueStartEvent / DialogueEndEvent`
    - 也会主动复核 `DialogueManager.IsDialogueActive`
    - 正式对话开始接管时，会主动回收此前留下的日常 NPC 气泡
    - 正式对话进行中，不再继续探测新的近身轻反馈
  - 已补出：
    - `2026-03-27-NPC-P3-05-轻交互与双气泡验收包.md`
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
- 当前测试受阻：
  - `unityMCP`
    - 仍不适合把 live 体验覆盖率记成已拿到
  - `Primary.unity`
    - 仍按 hot / mixed 现场处理，不在这刀里顺手写 scene
- 下一刀：
  - 先看 `T-P4-01`
  - 如果 `Primary.unity` 仍不可安全写，就继续走非 hot-file 的关系成长最小模型

### 2026-03-27｜P4 关系成长最小底座已接进内容层

- 当前完成：
  - 已新增：
    - `Assets/YYY_Scripts/Data/NPCRelationshipStage.cs`
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcRelationshipService.cs`
    - `Assets/Editor/NPC/PlayerNpcRelationshipDebugMenu.cs`
  - 已修改：
    - `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
    - `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcNearbyFeedbackService.cs`
    - `Assets/YYY_Tests/Editor/NPCToolchainRegularizationTests.cs`
    - `Assets/111_Data/NPC/NPC_001_VillageChiefDialogueContent.asset`
    - `Assets/111_Data/NPC/NPC_002_VillageDaughterDialogueContent.asset`
    - `Assets/111_Data/NPC/NPC_003_ResearchDialogueContent.asset`
  - 当前关系成长已具备：
    - `陌生 / 认识 / 熟悉 / 亲近` 四档阶段
    - 按阶段切不同近身句
    - `PlayerPrefs` 最小持久化
    - `Tools/NPC/Relationship/*` 的手动切换入口
  - 已补出：
    - `2026-03-27-NPC-P4-04-关系成长首版验收包.md`
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - 静态自动化断言已补：
    - 关系阶段分流
    - 关系阶段持久化
- 当前测试受阻：
  - `unityMCP`
    - 仍不适合把 live 覆盖率记成已拿到
  - `Primary.unity`
    - 仍不进入 scene 热区
- 下一刀：
  - 先看 `T-P5-01`
  - 如果继续不碰 hot-file，就往“受击 / 工具命中 / 反应兼容”推进

### 2026-03-27｜`0.0.2` 闭环 trace 强化：`002` 两轮已取证，但 `003` 与中断仍受外部 live blocker 抢跑

- 当前完成：
  - 已新增 / 修改：
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs`
    - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
    - `Assets/Editor/NPCInformalChatValidationMenu.cs`
  - 当前 Editor 验证入口已补到：
    - `002 / 003` 的 closure trace
    - `002 / 003` 的 interrupt trace
    - 会话真实状态快照
    - 触发失败不再记假阳性
- 当前测试结果：
  - `validate_script`
    - 上述 3 份脚本已测通过
  - `git diff --check`
    - 已测通过
  - 已拿到 `002` 的完整两轮 live 证据：
    - 首轮玩家句 / NPC 句
    - 第二轮玩家句 / NPC 句
- 当前测试受阻：
  - `SpringDay1LiveValidation / DialogueDebugMenu / NavValidation`
    - 会在同一轮 Play 内自动拉起：
      - `001` 正式对白
      - 工作台回忆
      - 导航实跑
    - 直接抢掉 `002 / 003` 闲聊 trace 的窗口
  - 因此当前仍不能 claim：
    - `002` 中断已 live 通过
    - `003` 闭环已 live 通过
- 下一刀：
  - 等 Play 窗口干净后，优先补：
    - `002 interrupt`
    - `003 closure`
    - `003 interrupt`
  - 如果仍被外部 live 验证抢跑，就按明确 blocker 记账，不再把 trace 超时伪装成 NPC 本身未实现。

### 2026-03-27｜`0.0.2` 剩余三条 live 证据补齐，当前只剩白名单收口

- 当前完成：
  - 已在真实被 `NavValidation` 抢跑的 Play 窗口里，补齐：
    - `002 interrupt`
    - `003 closure`
    - `003 interrupt`
  - 已明确看到 NPC trace 会让当前导航 runner 退场：
    - `runner_disabled`
    - `runner_destroyed`
  - 当前已拿齐的硬证据包括：
    - `002` 两轮来回
    - `002` 跑开中断
    - `003` 两轮来回
    - `003` 跑开中断
- 当前测试结果：
  - Unity 已退回 Edit Mode
  - `validate_script`
    - 5 份脚本已测通过
  - `git diff --check`
    - 已测通过
  - 当前无新增 own error
  - 仅余非阻断 warning：
    - `PlayerNpcChatSessionService.cs`
    - `NPCInformalChatInteractable.cs`
    - `String concatenation in Update() can cause garbage collection issues`
- 当前测试判断：
  - `0.0.2清盘002`
    - 当前可判 `done`
  - 当前不再是“功能未闭环”，而是“功能已闭环、尚未做 Git 白名单收口”
- 下一刀：
  - 已尝试白名单 sync，但被 `NPCV2` 自己的 same-root 尾账阻断：
    - `Assets/YYY_Scripts/Service/Player/*`
    - `Assets/YYY_Scripts/Story/Interaction/*`
    - `.kiro/specs/NPC/2.0.0进一步落地/*`
  - 当前下一刀应先清：
    - `EnergySystem.cs`
    - `HealthSystem.cs`
    - `PlayerAutoNavigator.cs`
    - `PlayerInteraction.cs`
    - `PlayerNpcNearbyFeedbackService.cs`
    - `PlayerNpcRelationshipService.cs`
    - `PlayerThoughtBubblePresenter.cs`
    - `CraftingStationInteractable.cs`
    - `NPCDialogueInteractable.cs`
    - 以及 `0.0.2 / 0.0.1` 根下未纳入的文档尾账
  - 这些 same-root 尾账收干净后，再做正式 sync。

### 2026-03-27｜追加排队：自动续聊矩阵与 001 提示卡首轮美化

- 本轮新增实现：
  - `PlayerNpcChatSessionService`
    - 已移除“第二轮必须再按一次 E”依赖
    - 已补 `AutoContinuing`
    - 已补会话期双气泡错位
  - `PlayerThoughtBubblePresenter / NPCBubblePresenter`
    - 已做玩家 / NPC 双样式分型
    - 已补 `reactionCue` 的紧凑情绪 cue 入口
  - `NpcWorldHintBubble`
    - 已完成 `001 / 002 / 003` 共用 NPC 提示卡首轮重做
- 本轮已测通过：
  - `validate_script`
    - `PlayerNpcChatSessionService.cs`
    - `PlayerThoughtBubblePresenter.cs`
    - `NPCBubblePresenter.cs`
    - `NPCInformalChatValidationMenu.cs`
    - `NpcWorldHintBubble.cs`
  - `git diff --check`
    - 已测通过
  - `live`
    - `002 interrupt`
      - 已看到 `已进入自动续聊观察`
      - 已看到 `已在第二轮等待回复阶段直接触发跑开中断`
    - `003 interrupt`
      - 已看到 `已进入自动续聊观察`
      - 已看到 `已在第二轮等待回复阶段直接触发跑开中断`
- 当前待测：
  - `001` 提示卡是否真的好看
  - `002 / 003` 双气泡在真实镜头里是否还会重叠
  - 玩家真实手按 `E` 的体验是否顺手
- 当前备注：
  - 这轮没有碰 `SpringDay1WorldHintBubble`，工作台提示不在本轮范围内
  - Unity 已退回 Edit Mode

### 2026-03-28｜补测与 P0 回归：离开矩阵第一刀已拿到新证据

- 本轮新增实现：
  - `PlayerNpcChatSessionService`
    - 已补离开宽限
    - 已补取消原因拆分
    - 已补 `LeavePhase` 快照
    - 已把 `PlayerTyping -> 跑开` 改成原子接管
  - `NPCInformalChatValidationMenu`
    - 已补 `002 / 003 PlayerTyping Interrupt`
    - 已补 `abortCause / leavePhase` 日志
    - 已修闭环 trace 的误导性收尾文案
- 本轮已测通过：
  - `validate_script`
    - `PlayerNpcChatSessionService.cs`
    - `NPCInformalChatValidationMenu.cs`
  - `git diff --check`
    - 已通过
  - `live`
    - `002 PlayerTyping Interrupt`
      - `endReason=WalkAwayInterrupt`
      - `leavePhase=PlayerSpeaking`
    - `003 PlayerTyping Interrupt`
      - `endReason=WalkAwayInterrupt`
      - `leavePhase=PlayerSpeaking`
    - `003` 二轮等待中断回归
      - `leavePhase=NpcThinking`
    - `002` 闭环回归
      - `闭环收尾完成`
      - `endReason=Completed`
- 当前仍待测：
  - `001` 提示卡画面级审美
  - `002 / 003` 双气泡真实镜头里的防重叠效果
  - 玩家真实手按 `E` 的体感
  - `SystemTakeover / TargetInvalid` 的专门 live trace

### 2026-03-28｜新增排队：P1 续接底座已写完，当前只做最小脚本闸门与 blocker 记账

- 本轮新增实现：
  - `PlayerNpcChatSessionService`
    - 已补中断续接快照
    - 已补同 NPC 续接 bundle / exchange
    - 已补 `DistanceGraceExceeded / BlockingUi / DialogueTakeover` 的 phase-aware fallback reaction
  - `NPCInformalChatInterruptMatrixTests`
    - 已补续接轮次、过期失效、跨 NPC 不串线、`NpcSpeaking` fallback 的 Editor 测试
- 本轮已测通过：
  - `validate_script`
    - `PlayerNpcChatSessionService.cs`
    - `NPCInformalChatInterruptMatrixTests.cs`
  - `git diff --check`
    - 已通过
- 本轮未继续跑 live：
  - 用户当前明确要求先不要继续测试
- 当前 blocker 记账：
  - Console 仍是外部编译 blocker，不是本轮 own 红：
    - `NPCDialogueInteractable.cs`
    - `CraftingStationInteractable.cs`
    - 原因均为 `SpringDay1WorldHintBubble.HideIfExists` 缺失
- 当前待排队：
  - `SystemTakeover / TargetInvalid` 的专门验证入口
  - continuity 是否要继续从“续轮次”深化成“续语义”

### 2026-03-28｜新增排队：续聊补口已数据驱动化，后续只差 live 时再验证

- 本轮新增实现：
  - `NPCDialogueContentProfile / NPCRoamProfile / NPCInformalChatInteractable`
    - 已补 resume intro / resume rule 链
  - `PlayerNpcChatSessionService`
    - 已改成先查 resume rule，再 fallback
  - `NPCInformalChatValidationMenu`
    - 已补 `BlockingUi / DialogueTakeover` 的续聊 trace 菜单
  - `NPCInformalChatInterruptMatrixTests`
    - 已补 resume rule 命中与暴露链测试
- 本轮已测通过：
  - `validate_script`
    - 上述 6 个脚本通过
  - `git diff --check`
    - 已通过
- 本轮未继续跑 live：
  - 仍遵守“当前先不继续测试”的口径
- 当前待排队：
  - `TargetInvalid` 验证入口
  - continuity 的 outcome / cooldown 是否还要继续拆

### 2026-03-28｜新增排队：编译错已清，`TargetInvalid` 与 resume cooldown 已做完

- 本轮新增实现：
  - `NPCInformalChatValidationMenu`
    - 已清 `CS7036`
    - 已补 `Trace 002/003 TargetInvalid Abort`
  - `PlayerNpcChatSessionService`
    - 已补 `TargetInvalid / PlayerUnavailable / ServiceDisabled` fallback
    - 已补 resume intro cooldown
  - `NPCInformalChatInterruptMatrixTests`
    - 已补 `TargetInvalid` fallback 与 cooldown 测试
- 本轮已测通过：
  - `validate_script`
    - 上述 3 个脚本通过
  - `git diff --check`
    - 已通过
  - 编译刷新
    - 这轮 own `CS7036` 已从 Console 消失
- 当前待排队：
  - continuity 是否还要继续拆 `resume outcome`
  - 是否顺手处理 `String concatenation in Update()` warning

### 2026-03-28｜新增排队：resume outcome 已补，当前更偏细化与降噪

- 本轮新增实现：
  - `PlayerNpcChatSessionService`
    - 已补 `ConversationResumeOutcome`
    - 已补 `LastResumeOutcomeName / LastResumeNpcId`
  - `NPCInformalChatValidationMenu`
    - 续聊 trace 已带 `resumeOutcome`
  - `NPCInformalChatInterruptMatrixTests`
    - 已补 outcome 相关测试
- 本轮已测通过：
  - `validate_script`
    - 上述 3 个脚本通过
  - `git diff --check`
    - 已通过
  - 编译刷新
    - 无新增 own 编译红
- 当前待排队：
  - 是否要继续降 `String concatenation in Update()` warning
  - 是否再细化 resume / abort outcome

### 2026-03-28｜live 追加：`002 BlockingUi Resume` 已拿证，`TargetInvalid` 当前被导航抢跑

- 本轮已测通过：
  - `live`
    - `002 BlockingUi Resume`
      - 已看到 `pending resume snapshot`
      - 已看到 `resume intro`
      - 已看到 `resumeOutcome=ResumedWithIntro`
      - 已看到第二轮继续完成
- 本轮测试受阻：
  - `002 TargetInvalid Abort`
    - 两次尝试只看到 `[NavValidation]`
    - 没进入 `[NPCValidation]`
    - 当前记为外部 live blocker
- 当前待排队：
  - 还需要在更安静窗口下补：
    - `TargetInvalid Abort`
    - `DialogueTakeover Resume`

### 2026-03-28｜补记排队：`002 DialogueTakeover Resume` 曾卡在首句首字，需先用 fresh Play 复核

- 本轮测试现象：
  - `live`
    - `002 DialogueTakeover Resume`
      - 超时停在 `state=PlayerTyping`
      - `playerText="我"`
      - `npcText=""`
- 当前判断：
  - 更像“首句玩家打字阶段卡首字”的真实异常点
  - 但因为紧接着的 `002 closure` 对照没拿到稳定 Play，所以还不能直接下最终结论
- 现场状态：
  - 后续已确认：
    - `Already stopped (not in play mode)`
    - Console 已清
- 当前待排队：
  - 重新进 fresh Play
  - 先跑 `002 closure`
  - 再跑 `002 DialogueTakeover Resume`
  - 若复现，再转代码排查 `PlayerTyping`

### 2026-03-28｜排队回收：`resume / target-invalid` live 组已补齐，续聊完成日志也已收口

- 本轮已测通过：
  - `live`
    - `002 closure`
      - `endReason=Completed`
    - `002 DialogueTakeover Resume`
      - `resumeOutcome=ResumedWithIntro`
      - `endReason=Completed`
    - `003 BlockingUi Resume`
      - `resumeOutcome=ResumedWithIntro`
    - `003 DialogueTakeover Resume`
      - `resumeOutcome=ResumedWithIntro`
    - `002 TargetInvalid Abort`
      - `endReason=TargetInvalid`
    - `003 TargetInvalid Abort`
      - `endReason=TargetInvalid`
- 本轮新增实现：
  - `NPCInformalChatValidationMenu`
    - 已把 `续聊完成` 日志改成等待真正收尾后再打
    - 不再把 `endReason=None` 当成最终成功记录
- 本轮判断：
  - 之前 `002 DialogueTakeover Resume` 的 `PlayerTyping / "我"` 现象，当前已被 fresh Play 实跑推翻，判定为现场抖动，不再列为当前业务 bug
- 当前待排队：
  - 非正式聊天底层只剩更偏内容厚度与体验验收的项
