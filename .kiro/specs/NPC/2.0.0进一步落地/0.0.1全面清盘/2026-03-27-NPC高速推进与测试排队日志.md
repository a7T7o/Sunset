# NPC 高速推进与测试排队日志

- 版本：v1.0
- 日期：2026-03-27
- 状态：进行中
- 用途：
  - 作为这次 NPC “高速列车”模式下的滚动执行日志
  - 记录每一刀的推进、已测结果、未测原因、测试排队和下一刀
  - 避免后续继续丢失“做到哪了 / 哪些已验 / 哪些想测但没测成”

## 1. 使用规则

- 每推进一刀，就追加一条记录。
- 每次只记录 4 类事实：
  - 当前做了什么
  - 当前有没有去测
  - 如果没测成，为什么
  - 下一刀准备做什么
- 这里记录的是“滚动执行事实”，不是完整需求正文。
- 正式任务定义仍以：
  - `2026-03-27-NPC全盘详细落地任务列表.md`
  为准。

## 2. 当前总线状态快照

- 当前主线：
  - 从 `P0` 已完成，正式进入 `P1`
- 当前最近已确认完成：
  - `T-P0-01`
  - `T-P0-02`
  - `T-P0-03`
  - `T-P0-04`
  - `T-P0-05`
- 当前下一刀：
  - `T-P1-01`
- 当前不准备硬撞的面：
  - `Primary.unity` mixed cleanup
  - `DialogueChinese*`
  - `NPCAutoRoamController.cs` 导航 runtime 改刀

## 3. 测试队列约定

- `已测通过`
  - 已有明确结果，可暂时从测试队列移出
- `待补测试`
  - 逻辑已推进，但还没补到合适验证
- `测试受阻`
  - 想测，但当前现场不允许或工具不给力
- `用户已确认`
  - 已由用户在 Unity 现场直接确认

## 4. 滚动记录

### 2026-03-27｜高速启动

- 当前完成：
  - 用户已确认 `001 / 002 / 003` 的 `Home Anchor` Inspector 现场正确。
  - `P0` 相关任务正式回填为完成态。
  - 已建立本日志，后续按它持续滚动。
- 当前测试结果：
  - `T-P0-01`
    - `用户已确认`
  - `T-P0-05`
    - `用户已确认`
    - 且用户明确说明后续不再强求额外 live / 多余测试
- 当前测试受阻：
  - `unityMCP` 会话层此前仍出现过 `no_unity_session`
  - 因此后续若再遇到“想测但测不成”，直接记这里，不反复原地打转
- 下一刀：
  - 先做 `T-P1-01` 三名 NPC 场景点位方案卡
  - 再看是否能并行预建 `P2` 内容载体

### 2026-03-27｜P1 收口并前置铺开 P2 内容资产层

- 当前完成：
  - `T-P1-01` 已正式收口为完成态，方案卡保持为当前有效的最小 scene 点位依据。
  - 已新增 `NPCDialogueContentProfile`，把角色内容从漫游参数里拆成独立资产层。
  - 已为 `001 / 002 / 003` 新增三份正式内容资产，分别补入：
    - 单人环境气泡
    - 玩家轻响应句池
    - 两两相遇矩阵
  - `NPCAutoRoamController` 当前相遇链已按 partner `npcId` 取 pair-specific 句池，不再只吃一套通用相遇文案。
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - `Editor.log`
    - 已看到 `Requested script compilation / CompileScripts / Reloading assemblies`
    - 当前未抓到这刀对应的新 `error CS`
- 当前测试受阻：
  - `refresh_unity`
    - 等待 editor ready 超时
  - `read_console`
    - 返回 `no_unity_session`
  - 因此这轮仍按“静态闸门 + Editor.log 编译迹象”记账，不把 live 验收冒充成已完成
- 下一刀：
  - 先复核 `T-P1-02`
  - 若 `Primary.unity` 仍是 `blocked-hotfile`，则继续转做 `T-P3-01`

### 2026-03-27｜Primary 继续 blocked 时，先补 P2 验收包与 P3-01 正式规范

- 当前完成：
  - 已再次只读复核 `T-P1-02`：
    - `Primary.unity` 仍是大体量 mixed diff
    - 当前不能因为 shared root 处于 `main + neutral` 就误判 scene 可直接写
    - 因此 `T-P1-02` 继续维持 `blocked-hotfile`
  - 已补出 `T-P2-07`：
    - `2026-03-27-NPC-P2-07-角色化内容验收包.md`
  - 已补出 `T-P3-01`：
    - `2026-03-27-NPC-P3-01-玩家与NPC双气泡视觉规范正式稿.md`
- 当前测试结果：
  - 本轮以静态事实收口为主：
    - 三份内容资产
    - 三份 `roamProfile` 映射
    - `NPCToolchainRegularizationTests` 已覆盖 pair-specific 内容断言
  - 未新增 live 验收冒充结果
- 当前测试受阻：
  - `Primary.unity`
    - 仍不适合当前线程直接写入
  - `unityMCP`
    - 会话层仍不稳定，不能把 live 触发覆盖率说成已拿到
- 下一刀：
  - 先评估 `T-P3-02` 的代码切口是否能避开当前 mixed hot-file
  - 如果玩家侧热文件仍不适合写，就继续找不撞热区的下一项独立切片

### 2026-03-27｜P3-02 先只落玩家气泡样式层，不碰输入与 scene

- 当前完成：
  - 已只修改：
    - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
  - 当前玩家气泡已从“照抄 NPC 基线”切到正式玩家样式：
    - 晨雾青绿色填充
    - 深青灰边框
    - 深墨色文字
    - 更轻一点的整体浮动
    - 更轻一点的尾巴跳动
  - 同时继续保留与 NPC 的共骨架：
    - `10` 字一行
    - 同级边距
    - 同级尾巴尺寸
    - 同级 show / hide 节奏
- 当前测试结果：
  - `git diff --check`
    - `已测通过`
  - 这轮还没有 claim live 视觉实证
- 当前测试受阻：
  - `git-safe-sync`
    - 首次白名单收口被同根尾账拦住：
      - `Assets/YYY_Scripts/Service/Player/PlayerInteraction.cs`
    - 当前已审明这不是外线 hot-file，而是此前玩家反馈 / `ToolUseCommitResult` 链留下的同根尾账
  - `PlayerInteraction.cs`
    - 现阶段不再按“外线 mixed”处理
    - 而是按“同根旧尾账需要一并收口”处理
  - `GameInputManager.cs`
    - 仍是 hot-file，不在这刀内接轻响应
  - 因此这轮只 claim `T-P3-02` 的样式层完成
- 下一刀：
  - 继续看 `T-P3-03`
  - 重点是能不能在不碰 `GameInputManager.cs` 的前提下，把玩家靠近轻响应先接到安全入口
