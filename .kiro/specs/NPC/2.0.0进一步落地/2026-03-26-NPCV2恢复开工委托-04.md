# 2026-03-26-NPCV2恢复开工委托-04

你现在继续作为 `NPCV2` 施工。

这轮已经不是 cleanup，也不是继续只读等 `Primary.unity` 自己变干净。

## 当前已接受的基线

1. `Primary.unity` 的历史 mixed dirty 已由治理线程按当前 integration baseline 收口到 `main`。
2. `NPCV2` own cleanup 已完成，对自己 clean。
3. `003` 的正式 / 验证样本口径、`NPCSceneIntegrationTool`、`NPCAutoRoamControllerEditor` 的前置工具链已在旧基线中准备好。
4. 当前最值得继续的一刀，仍然是：
   - `Primary.unity` 中 `001 / 002 / 003` 的 `HomeAnchor` 最小 scene 集成

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\05_当前现场_高权重事项_风险与未竟问题.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\V2交接文档\06_证据索引_必读顺序_接手建议.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC场景化真实落点与角色日常设计.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC-导航接入契约与联调验收规范.md`
6. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\NPC系统实施主表.md`
7. `D:\Unity\Unity_learning\Sunset\.kiro\steering\scene-modification-rule.md`
8. `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
9. `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
10. `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`

## 这轮唯一主刀

只做：

> 按热区陪跑口径拿 `Primary.unity` 的最小 scene 写窗口，然后把 `001 / 002 / 003` 的 `HomeAnchor` 最小落点真正落进 scene，并交出用户可验收的最小 scene 结果

更具体地说，这轮只允许你完成下面这 1 刀：

1. 先做热区 preflight 与 scene audit：
   - 复核 `cwd / branch / HEAD`
   - 复核 `Check-Lock.ps1 -TargetPath 'Assets/000_Scenes/Primary.unity'`
   - 复核 `shared-root-branch-occupancy.md`、`mcp-single-instance-occupancy.md`、`mcp-hot-zones.md`
   - 按 `scene-modification-rule.md` 先写清：
     - 原有配置
     - 问题原因
     - 建议修改
     - 修改后效果
     - 对原有功能的影响
2. 若 scene 写窗口成立，只在 `Primary.unity` 内做最小 scene 集成：
   - audit `001 / 002 / 003` 当前 scene 实例与 `HomeAnchor` 现状
   - 为三者各自建立或绑定明确的 `HomeAnchor`
   - 位置以 `NPC场景化真实落点与角色日常设计.md` 的角色方向为准
   - 只补 anchor 层级、引用和基础位置，不扩到 roam 区域、路线、停留点、相遇节奏
3. 如果仅 scene 不足以闭环，只有在 `HomeAnchor` 赋值链直接阻断时，才允许最小补口：
   - `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCSceneIntegrationTool.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCAutoRoamControllerEditor.cs`
   - 这类补口只能服务 `HomeAnchor` 落点，不能顺手扩成 roam 行为改造
4. 做最小自验：
   - 回读 `001 / 002 / 003` 各自 `HomeAnchor` 的 hierarchy 路径、引用状态和基础位置
   - 如需最短 Play / MCP 窗口，只验证 scene 未坏、NPC 已能以 `HomeAnchor` 为起点正常启动
   - 一拿到结论立刻 `Stop` 并退回 `Edit Mode`

## 允许的作用域

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\2026-03-26-NPCV2恢复开工委托-04.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPCV2\memory_0.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`
5. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCSceneIntegrationTool.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NPCAutoRoamControllerEditor.cs`
7. 只读参考可用：
   - `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\001.prefab`
   - `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\002.prefab`
   - `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\NPC\003.prefab`
   - `D:\Unity\Unity_learning\Sunset\Assets\111_Data\NPC\`

## 明确禁止

1. 不要顺手扩到 roam 区域、路线、停留点、相遇节奏。
2. 不要回去继续做气泡、关系成长、双气泡、受击反应。
3. 不要改导航核心，不要碰 `GameInputManager.cs`。
4. 不要把 `Primary.unity` 这轮扩成“NPC 全量 scene 化”。
5. 不要绕过 `scene-modification-rule.md` 直接动已有场景配置。
6. 不要再新起平行设计文档。

## 详细汇报落点

本轮详细汇报必须写到：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\2026-03-26-NPCV2恢复开工详细汇报-04.md`

详细汇报必须按下面 4 个功能点组织：

1. `001` 的 `HomeAnchor`
2. `002` 的 `HomeAnchor`
3. `003` 的 `HomeAnchor`
4. 最小 scene 自验

每个功能点至少写清：

- 做了什么
- 改了什么
- 自验证据
- 用户可验收点
- 测试前提
- 入口路径
- 操作步骤
- 预期结果
- 失败判读

另外，详细汇报开头必须先有一段 `scene audit 五段式结论`：

- 原有配置
- 问题原因
- 建议修改
- 修改后效果
- 对原有功能的影响

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. `001 / 002 / 003` 各自的 scene 实例都已有明确 `HomeAnchor`；
2. 你能交出三者各自的 hierarchy 路径、引用状态和基础位置说明；
3. 你已完成最小 scene 自验，并明确 scene 目前是否仍稳定；
4. 你已把详细汇报写到指定路径；
5. 你已更新：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\memory.md`
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPCV2\memory_0.md`
6. 你已对白名单路径完成 `sync` 到 `main`，并确保 `当前 own 路径是否 clean = yes`。

## 聊天最小回执格式

- 已回写文件路径
- 详细汇报文件路径
- 是否完成本轮要求：`yes / no`
- 当前 own 路径是否 clean：`yes / no`
- 一句话摘要

## 一句话总口径

你这轮不要再讲“NPC 后面还能做很多”，只把 `Primary.unity` 里的 `HomeAnchor` 最小 scene 集成真正落下，并把用户能怎么验写清楚。
