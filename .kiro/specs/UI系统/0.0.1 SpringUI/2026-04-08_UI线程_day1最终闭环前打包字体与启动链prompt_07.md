# 2026-04-08｜UI 线程｜Day1 最终闭环前打包字体与启动链 prompt 07

请先完整读取：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-06_UI线程_给day1阶段回执_25.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-06_UI线程给day1全量回执_01.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-08_spring-day1_Day1最终闭环主控清单_30.md`

从这一条开始，不要再回“任务卡布局 / workbench 壳体微调 / 文案美化”主线。

当前唯一主刀固定为：

把会直接毁掉 demo 体验的字体链硬问题收掉：

1. 打包后字体异常 / 缺字 / 显示不正常
2. 编辑器与打包版字体链不一致

---

## 你必须继承的硬事实

1. 你之前已经把：
   - formal 提示壳优先级
   - resident fallback 最小语义
   - Workbench 左列恢复链
   往前推过一轮
2. 用户现在最新最高优先级的 UI 抱怨是：
   - 打包字体损坏/显示异常
3. 用户刚补的 profiler 已经坐实：
   - 启动大卡顿主峰不在 UI，而在 `PersistentPlayerSceneBridge.Start()` 与 `NavGrid2D.RebuildGrid()`

你这轮的职责不是把所有锅都吞了，而是把自己真 own 的打包字体链责任先打穿。

---

## 这轮唯一主刀

### 先收死打包字体链

完成定义至少包括：

1. 打包版里 `DialogueUI / Prompt / Workbench / 正式对白` 的中文字体显示正常
2. 不再出现编辑器正常、打包后丢字/错字形/回退字体异常
3. 给主控台一份明确结论：
   - 真正原因是什么
   - 已修到哪
   - 哪些面已经过线

---

## 允许的 scope

1. 字体 bootstrap
2. UI runtime 初始化
3. `Resources.Load` / 预热 / 首帧字体绑定链
4. 为了修字体链而必要触碰的相关 UI 资源与脚本

---

## 明确禁止事项

1. 不要回主刀剧情导演
2. 不要碰 `Town.unity / Primary.unity`
3. 不要碰 `GameInputManager.cs`
4. 不要继续泛修任务卡样式
5. 不要再把启动大卡顿主责往自己身上乱吞
6. 不要把“fresh console = 0”冒充成打包体验已过线

---

## 完成定义

这轮完成至少要让主控台一眼知道：

1. 打包字体链现在是否已过线
2. 编辑器与打包版是否一致
3. 字体 bootstrap 是否仍有肉眼可感卡顿
4. 如果还有字体链问题，主 blocker 在哪

最好给：

1. fresh build / runtime 证据
2. 明确影响面
3. 最终仍未解决的余项

---

## 回执时必须写清

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要主控台现在做什么
7. 打包字体链结论
8. 字体 bootstrap 额外说明（如果有）

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
