# `spring-day1` 存档边界回执

请直接在对应字段后填写；没有内容也写 `无` / `尚未` / `不需要`。

## 基本状态

- 当前线程：`spring-day1`
- 当前切片：`day1-finish-remaining-owner-work-2026-04-06-b`
- 当前是否仍在施工：`是；主线仍在推进，本回执在最近安全点补写。当前 thread-state 已重新 ACTIVE。`
- 本回执填写时间：`2026-04-06 20:13:44+08:00`

## 一句话结论

- 你对 `存档系统` 第一刀范围的总判断：`第一版存档应只收 Day1 的长期剧情进度与任务完成态，不应直接存玩家面显示、导演摆位、UI 当前页或运行时瞬时演出态；存档系统线程可以先做 Story/Dialogue/Director 的数据接线，但 Day1 语义最后一跳更建议由本线程自收。`

## 必须纳入第一版存档的 Day1 长期态

- 列表：
  - `StoryManager.CurrentPhase`
  - `StoryManager.IsLanguageDecoded`
  - `DialogueManager` 已完成正式对白序列集合
  - `SpringDay1Director` 的教学目标完成态：
    - `_tillObjectiveCompleted`
    - `_plantObjectiveCompleted`
    - `_waterObjectiveCompleted`
    - `_woodObjectiveCompleted`
  - `SpringDay1Director._craftedCount`
  - `SpringDay1Director._freeTimeEntered`
  - `SpringDay1Director._freeTimeIntroCompleted`
  - `SpringDay1Director._dayEnded`
  - 工作台首次提示消费：
    - 当前语义对应 `CraftingStationInteractable` 的 `WorkbenchHintConsumedKey`
  - 补充建议：
    - 如果第一版存档要支持“存档后回来不重播正式对白”，那 `DialogueManager.HasCompletedSequence(...)` 这组已完成序列应视为必存，而不是可选

## 明确不该直接持久化的 Day1 状态

- 列表：
  - `PromptCardModel`
  - `SpringDay1PromptOverlay` 当前展示内容
  - `DialogueUI` 当前打字进度 / 当前正在显示到第几个字
  - `SpringDay1NpcCrowdDirector` 的当前站位 / parent / 临时摆位 / runtime root 归属
  - `SpringDay1NpcCrowdDirector` 当前在 `Primary/Town` 哪个 runtime scene 生效
  - 工作台 Overlay 当前选中配方 / hover / 当前进度文案 / 左列展示态
  - `SpringDay1LiveValidationRunner` 的 snapshot 内容
  - 当前世界提示气泡 / NPC 提示 copy / 玩家面提示壳当前文本
  - 任意“这一帧正在播、这一帧 hover 到哪、这一帧排练站在哪”的瞬时导演态

## 你认为“可由源状态派生”的内容

- 列表：
  - `PromptCardModel` 全量内容可由：
    - `StoryManager.CurrentPhase`
    - `DialogueManager` 已完成正式序列
    - `SpringDay1Director` 教学目标完成态
    - `craftedCount / freeTimeEntered / freeTimeIntroCompleted / dayEnded`
    派生
  - `SpringDay1PromptOverlay` 当前页内容可由上面的源状态重建
  - `DialogueUI` 是否应显示、显示什么、是否乱码，可由当前 phase + 当前对白状态重新进入时生成，不该直接回存打字进度
  - `SpringDay1NpcCrowdDirector` 的 resident 消费摘要、parent 归属、临时站位都应由 beat / phase / manifest / runtime sync 重新算
  - `CrowdDirector` 当前跑在 `Primary` 还是 `Town` 也只该由当前 scene + runtime contract 现算，不该直接回存 scene 名或运行时根节点路径
  - 工作台 Overlay 的当前文案、配方列表、进度提示应由：
    - 当前 station
    - 当前 recipe 数据
    - 当前 crafting service / director 进度
    重新生成
  - `OneShot` / `BeatConsumption` 这类 live snapshot 只应作为验收观测值，不应直接进存档

## 允许 `存档系统` 线程直接改的文件 / 状态

- 列表：
  - [StoryManager.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/StoryManager.cs)
    - 允许做 phase / decoded 的 save-load 数据接线
  - [DialogueManager.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/DialogueManager.cs)
    - 允许做已完成正式对白序列集合的 save-load 接线
  - [SpringDay1Director.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs)
    - 允许先做最小数据结构、序列化容器、restore hook 预留
    - 但只建议做到“接线层 / 数据容器层”
  - [CraftingStationInteractable.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Interaction/CraftingStationInteractable.cs)
    - 允许把 `WorkbenchHintConsumedKey` 从散落的 `PlayerPrefs` 语义接到统一存档入口
    - 前提是只改“读/写来源”，不要顺手改工作台交互语义

## 不允许直接改，建议由 `spring-day1` 自收的文件 / 状态

- 列表：
  - [SpringDay1Director.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs) 里的“最后一跳语义恢复”
    - 建议由本线程自收
    - 理由：哪些字段 restore 后应立刻补 phase 跳转、哪些字段只代表已完成不该重播，这层是 Day1 owner 语义，不只是存档 plumbing
  - `DialogueManager` 已完成序列恢复后如何影响 Day1 正式桥段 one-shot 判定
    - 建议由本线程自收
    - 理由：这已经直接影响正式剧情是否重播
  - `SpringDay1Director` 中教学目标 restore 后是否立即补 UI / prompt / runtime crowd 承接
    - 建议由本线程自收
    - 理由：这是 Day1 体验语义，不是纯数据落回
  - 任意涉及 `PromptOverlay / DialogueUI / CrowdDirector` 直接恢复显示态或摆位态的实现
    - 不建议存档系统线程直接做

## 如果只允许 `存档系统` 做接线，你建议它做到哪一层

- 结论：`建议它先做到“SaveData 结构定义 + StoryManager/DialogueManager/Director 的最小读写接口 + CraftingStationInteractable 的工作台首次提示消费接线”。恢复后由 Day1 owner 再收“phase 恢复顺序、正式对白是否重播、教学链与自由时段如何回落”的最后一跳。`

## 你认为应自动升级为正式许可的情况

- 列表：
  - 需要直接改 `SpringDay1Director` 的阶段推进语义，而不只是加字段 / 接口
  - 需要改变 `DialogueManager` 已完成序列的消费时机或 one-shot 判定逻辑
  - 需要把 UI 当前页 / Crowd 当前摆位 / 导演当前临时状态也纳入持久化
  - 需要动到 `PromptOverlay / DialogueUI / SpringDay1NpcCrowdDirector` 的恢复语义
  - 需要让存档恢复后“直接跳过/重播”某段正式剧情，这种就不是普通接线，应升正式许可
  - 需要从 `PlayerPrefs` 迁移更多 Day1 分散状态且改动面超出工作台首次提示消费

## 额外风险 / 提醒

- 列表：
  - 不要把“结构上能存”误写成“体验上应该存”；例如打字进度、当前 hover、当前提示文案结构上都能存，但体验上不该存
  - 第一版存档更适合“回到稳定剧情节点”，不适合“原封不动恢复到当前导演帧”
  - `freeTimeEntered / freeTimeIntroCompleted / dayEnded` 三个状态有先后关系，restore 时应防止组合出不合法状态
  - `DialogueManager` 已完成正式序列集合与 `StoryManager.CurrentPhase` 必须一起看，不能各自独立恢复完就算结束
  - 工作台首次提示消费当前挂在 `PlayerPrefs`，若要并回统一存档，注意不要造成旧存档和旧 `PlayerPrefs` 双源冲突

## 最终建议

- 你建议 `存档系统` 第一刀先落在哪里：`先落 StoryManager + DialogueManager 的长期态，再加 SpringDay1Director 的最小任务态容器，最后顺手把 CraftingStationInteractable 的工作台首次提示消费并到统一 save 数据。`
- 一句话理由：`这条路先把“正式剧情不重播”和“任务完成不丢失”这两个最值钱的长期态站住，同时把 Day1 owner 才能判断的最后一跳语义留给本线程自收。`
