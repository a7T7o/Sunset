# 2026-04-08｜存档系统｜Day1 居民运行态与场景切换持久化 prompt 02

请先完整读取：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\存档系统\2026-04-06_spring-day1_存档边界回执_01.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\存档系统\2026-04-06_NPC_存档边界回执_02.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-08_spring-day1_Day1最终闭环主控清单_30.md`

从这一条开始，不要停在边界讨论，不要只补一份“我建议怎么存”的说明。

当前唯一主刀固定为：

把 `Day1` 第一版真正需要持久化的“剧情长期态 + resident 最小运行态”接成能落地的存档 contract，重点解决：

- 切场景后 NPC 又回到编辑态起点
- 读档后 resident 状态丢失
- `PersistentPlayerSceneBridge.Start()` / `RebindScene(...)` 这条 scene-rebind 启动大卡顿

但你不能把这条线漂成“恢复任意导演帧”。

---

## 你必须继承的硬事实

1. `spring-day1` 已经明确给了你第一版长期态边界：
   - `StoryManager.CurrentPhase`
   - `DialogueManager` 已完成正式对白序列
   - `SpringDay1Director` 教学目标完成态与自由活动/日终状态
   - `WorkbenchHintConsumedKey`
2. `NPC` 这边也已经明确：
   - `phaseSelfTalk`
   - `walkAwayReaction`
   - `nearby / bubble / typing`
   这些都不该直接进存档
3. 用户当前新增抱怨已经非常具体：
   - 场景切换后 NPC 位置不持久
   - 这不是 UI 问题，也不是 scene 摆点问题
4. 当前正确目标不是“保存一切瞬时演出态”，而是：
   - 回到一个稳定、正确、不会重播正式剧情的 Day1 节点
   - resident 不要回到最原始编辑态
5. 用户刚补的 profiler 已经坐实：
   - 首帧/开场大卡顿里，`PersistentPlayerSceneBridge.Start()` 是主峰之一
   - 这条锅不能再模糊地甩给 UI

---

## 这轮唯一主刀

这轮只做一个主刀：

### 把 `Day1` 的 resident 最小持久态接出来

完成定义至少包括：

1. 切场景或读档后，resident 不再一律回到编辑态初始站位
2. 已消费正式剧情不因恢复而重播
3. 已完成任务态恢复后，Day1 还能回到合法稳定节点

---

## 允许你这轮做到的层级

### 第一层：长期剧情态正式接线

1. `StoryManager`
2. `DialogueManager`
3. `SpringDay1Director`
4. `CraftingStationInteractable` 的工作台首次提示消费

### 第二层：resident 最小运行态

你这轮要补的不是“完整导演回放”，而是最小且值钱的 resident snapshot。

建议至少收这类信息：

1. stable resident identity
2. 当前 scene / resident group 的合法归属
3. 当前是否处于脚本接管态或已回落常驻态
4. 恢复后足以避免直接掉回编辑态起点的最小站位/锚点语义

如果你判断某个字段不该直接存，就明确把它收成：

- 可由源状态派生
- 需要 `NPC` 或 `spring-day1` 最后一跳恢复

### 第三层：把 scene-rebind 启动峰值收窄

你这轮要顺手查和收的，不是“所有启动性能”，而是你 own 的这条：

1. `PersistentPlayerSceneBridge.Start()`
2. `RebindScene(...)`
3. `PromoteSceneRuntimeRoots(...)`
4. `ReapplyTraversalBindings(...)`

目标至少是二选一：

1. 真修掉一部分明显重复/重型操作
2. 或非常具体地交代第一真实 blocker，不再让主控台误判 owner

---

## 明确禁止事项

1. 不要去存 UI 当前页
2. 不要去存打字进度
3. 不要去存当前气泡文案
4. 不要去存这一帧导演队列
5. 不要把“任意时刻原地恢复”包装成第一版目标
6. 不要擅自改 `Town.unity / Primary.unity`
7. 不要替 `spring-day1` 改 Day1 最后一跳语义判断

---

## 你和 `NPC / spring-day1` 的边界

### 你 own

1. save data 结构
2. save/load 读写入口
3. resident 最小 snapshot 的序列化与恢复 plumbing
4. scene change / load 后把 snapshot 交回消费方
5. `PersistentPlayerSceneBridge` 这条 scene-rebind 的性能责任面

### 你不 own

1. 最终导演语义怎么恢复
2. 正式剧情是否应立刻补哪段桥接
3. Town/Primary 场景里 native resident 本体怎么摆
4. 玩家面 UI 恢复长什么样

如果你需要 `NPC` 提供 resident snapshot surface，就明确按 contract 对接，不要自己吞掉 NPC 语义层。

---

## 完成定义

这轮完成不是“结构上多了几个字段”，而是至少要让主控台能直接继续吃：

1. 一份清楚的 resident snapshot 数据结构
2. 一条可实际接入 `SaveManager / StoryProgressPersistenceService` 的读写链
3. 明确哪些 resident 状态已可持久化
4. 明确哪些仍必须由 `spring-day1` 或 `NPC` 最后一跳恢复
5. 至少一份验证证据，证明恢复后不会把 resident 一律打回编辑态起点

---

## 回执时必须写清

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要主控台现在做什么
7. 当前 save contract 已经能覆盖哪些 Day1 case
8. 还不能覆盖哪些 case

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
