# 给 `农田交互修复V3` 线程的存档边界确认 prompt

你这轮不用切到“存档系统”做实现，也不要求你暂停当前或下一轮业务切片。

请先完整读取：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\存档系统\2026-04-06_农田交互修复V3_存档边界回执_01.md`

## 你当前收到的请求

`存档系统` 线程准备在现有 slot-save 底座上补齐当前 demo 第一版范围。

这轮需要你从“旧存档底座 owner / 维护者”视角确认：
- 现有 `SaveManager -> Registry -> IPersistentObject -> worldObjects` 主链哪些部分绝不能被误伤
- 新增长期状态更适合如何接入，才不会破坏旧底座
- 哪些文件允许 `存档系统` 线程直接改
- 哪些文件或语义最好由 owner 自己收最后一跳

## 这轮统一前提

1. 默认你正在干活，不要求你暂停当前切片。
2. 请在你当前切片的最近安全点，或本轮切片收尾时，再补这份回执。
3. 不需要单独聊天回我；只回写回执文件。
4. 允许你开 subagent 做并行只读对照；最终仍只把结论写进回执文件。
5. 这轮不是让你立刻修存档功能，而是确认接线边界。

## 你需要重点确认的内容

请结合你对旧存档底座的真实理解，重点回答这些问题：

1. 下面这些文件里，哪些可以由 `存档系统` 线程直接改，哪些更适合 owner 自收：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Data\Core\SaveManager.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Data\Core\SaveDataDTOs.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Data\Core\PersistentObjectRegistry.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Data\Core\DynamicObjectFactory.cs`
2. 对新增剧情 / Day1 / NPC 长期态，你更推荐哪种接入方式：
   - 新建独立 `IPersistentObject`
   - 回填 `PlayerSaveData`
   - 回填根 DTO
   - 其他做法
3. 你认为哪些旧字段已经是历史残留，不该继续扩：
   - 例如根层 `farmTiles`
   - 例如只写不读或读写不闭环的旧字段
4. 你认为哪些改动会最容易破坏现有主链：
   - `runtime item`
   - `Inventory / Equipment`
   - `Chest / Drop / Crop`
   - 读档顺序
   - 其他
5. 哪些情况你认为必须从“边界确认”升级成“正式许可”。

## 明确禁止的漂移

- 不要把这轮扩成旧底座大重构
- 不要顺手改无关验证链
- 不要把“可以技术上塞进去”误判成“适合现在这样接”
- 不要额外在聊天里再写一份长回执

## 回执方式

只在这个文件里回写：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\存档系统\2026-04-06_农田交互修复V3_存档边界回执_01.md`

如果你判断某块必须由 owner 自己收，请在回执里直接写明：
- `建议由本线程自收`
- 并给出一句理由
