# 导航检查｜执行层 smoke test 01｜治理镜像回收卡

- 状态：已回收，已完成退场恢复
- 首轮执行结果：
  - `request-branch`：成功获批
  - `ensure-branch`：成功进入 `codex/navigation-audit-001`
  - 只读核对：已完成 docs-fast-lane 最小 checkpoint
  - 首次 `return-main`：失败
- 首次阻断根因：
  - waiting 线程写入的 `.codex/drafts/**` 在 `导航检查` 当时所处旧分支上没有被忽略
  - `return-main` 将这些 Draft 误判成 foreign dirty，导致 shared root 卡在 `codex/navigation-audit-001`
- 治理恢复动作：
  - 在仓库本地 `D:\Unity\Unity_learning\Sunset\.git\info\exclude` 增加 `.codex/drafts/**` 兜底忽略
  - 重新执行 `return-main` 后成功归还 shared root
- 恢复后现场：
  - `main @ c09ac560`
  - `git status --short --branch = ## main...origin/main`
  - `shared-root-branch-occupancy.md = main + neutral`
  - queue 中 `ticket 2 / 导航检查` 已转为 `completed`
- 持槽复盘：
  - `hold_category = docs-fast-lane`
  - 推荐窗口：`3` 分钟
  - 实际持槽：`11.25` 分钟
  - 结论：本轮 smoke test 已证明“旧分支忽略缺口 + 持槽过长”都会真实压低吞吐
- 当前治理裁定：
  - `导航检查` 这轮 smoke test 已闭环，不再继续占用 shared root
  - 后续如继续导航业务，应回到 `codex/navigation-audit-001`，但那将属于下一次单独唤醒，不属于本轮回收
- 说明：本文件由治理线程根据聊天最小回执统一回填；业务线程不得直接修改。
