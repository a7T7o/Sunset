# 遮挡检查｜执行层 smoke test 01｜治理镜像回收卡

- 状态：已回收，waiting 保留
- 首轮执行结果：
  - `request-branch`：`LOCKED_PLEASE_YIELD`
  - `ticket`：`5`
  - `ensure-branch`：未执行
  - `return-main`：未执行
- Draft 证据：
  - `D:\Unity\Unity_learning\Sunset\.codex\drafts\遮挡检查\20260319-182529-codex-occlusion-audit-001.md`
- 当前队列位置：
  - 当前排在 `NPC / 农田交互修复V2` 之后
  - continuation branch 仍为 `codex/occlusion-audit-001`
- 当前治理裁定：
  - 本轮 smoke test 的 waiting 结果有效，后续无需重复验证 Draft 挂起链路
  - 必须等待前序条目推进或治理线程重排，不得自行重试 `ensure-branch`
- 说明：本文件由治理线程根据聊天最小回执统一回填；业务线程不得直接修改。

## 续跑结果（治理补记）
- `wake-next`：已由治理线程执行，grant 已正式发放给 `遮挡检查`
- `request-branch`：`ALREADY_GRANTED`
- `ensure-branch`：成功
- `return-main`：成功
- `post_return_evidence_mode`：`minimal-tracked-allowed`
- 只读核对范围：
  - 当前“树 Prefab 父 / 子双 OcclusionTransparency 是否为主根因”的调查 carrier 是否仍在位
  - 相关文档与关键线索是否仍在位
- 续跑完成后的治理裁定：
  - `遮挡检查` 本轮 smoke test 只读闭环已成立
  - queue 中 `ticket 5 / 遮挡检查` 已转为 `completed`
  - 因为这是队列最后一条，本轮 `return-main` 已返回 `minimal-tracked-allowed`
