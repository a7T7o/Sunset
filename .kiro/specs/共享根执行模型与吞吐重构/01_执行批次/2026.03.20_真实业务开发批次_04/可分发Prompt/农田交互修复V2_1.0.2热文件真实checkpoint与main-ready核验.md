# 农田交互修复V2 - 1.0.2 热文件真实 checkpoint 与 main-ready 核验

```text
【真实业务开发批次 04｜农田交互修复V2｜1.0.2 热文件真实 checkpoint 与 main-ready 核验】

你的 continuation branch：
- codex/farm-1.0.2-cleanroom001

这轮不是 smoke，不是 carrier 清洗复述，也不是再次回“上轮 checkpoint 已经在 branch 里”。
这轮的唯一目标是：把 `1.0.2` 的首个热文件 checkpoint 做实，并明确它对 `main` 的真实影响。

你已知的 live 业务事实：
- 上轮已确认：下一 checkpoint 要进入 `GameInputManager.cs` 热文件阶段
- 非热文件面的 carrier 清洗和基础改动此前已经在 branch 中
- 所以这轮如果拿到槽位，默认应该直指真实热文件 checkpoint，而不是再停在描述层

你的第一动作 MUST 是：
1. 执行：
   powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action request-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001" -CheckpointHint "farm-1.0.2-hotfile-real-dev-04" -QueueNote "dev-batch-04"

分流规则：

- 如果返回 GRANTED 或 ALREADY_GRANTED：
  1. 执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action ensure-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001"
  2. 进入 branch 后先执行：
     git diff --name-status main...HEAD
  3. 本轮允许保留 / 修改 / 提交的路径范围只有：
     - Assets/YYY_Scripts/Controller/Input/GameInputManager.cs
     - Assets/YYY_Scripts/Farm/
     - Assets/YYY_Scripts/Service/Placement/
     - Assets/YYY_Scripts/UI/Inventory/
     - Assets/YYY_Scripts/Service/Inventory/HotbarSelectionService.cs
     - .kiro/specs/农田系统/2026.03.16/
     - .kiro/specs/农田系统/memory.md
     - .codex/threads/Sunset/农田交互修复V2/memory_0.md
  4. 这轮必须完成的真实工作是：
     - 先把 live diff 分成“1.0.2 业务交付面”与“治理噪音”
     - 然后把首个热文件 checkpoint 做实，优先收束以下至少一条真实主入口链路：
       - `GameInputManager <-> FarmToolPreview`
       - `GameInputManager <-> PlacementManager / PlacementNavigator / PlacementPreview`
       - `GameInputManager <-> Inventory / HotbarSelectionService`
     - 不准只做 carrier 清洗描述
  5. 如果需要触碰 `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`：
     - 先查锁：
       powershell -ExecutionPolicy Bypass -File .kiro/scripts/locks/Check-Lock.ps1 -TargetPath "Assets/YYY_Scripts/Controller/Input/GameInputManager.cs"
     - 若为 unlocked，再申请锁：
       powershell -ExecutionPolicy Bypass -File .kiro/scripts/locks/Acquire-Lock.ps1 -TargetPath "Assets/YYY_Scripts/Controller/Input/GameInputManager.cs" -OwnerThread "农田交互修复V2" -Task "farm-real-dev-batch-04" -Checkpoint "1.0.2 hotfile real checkpoint"
     - 只有拿到锁后，才允许修改该文件
  6. 本轮禁止保留到最终交付面中的文件包括但不限于：
     - AGENTS.md
     - scripts/git-safe-sync.ps1
     - 与农田 `1.0.2` 无关的治理文档 / 规则漂移
     - NPC / 导航 / 遮挡 / spring-day1 相关文件
  7. 本轮必须显式回答两件事：
     - `carrier_ready`
     - `main_ready`
     其中：
     - `carrier_ready = yes` 只表示 branch 内这轮 checkpoint 已收束
     - `main_ready = yes` 只允许在“你本轮 return-main 后，当前 main 不会因为农田 branch-only 代码 / 资源而出现主入口依赖缺口”时填写
     - 如果 `main_ready = no`，必须写出具体依赖面
  8. 如果形成了安全 checkpoint，执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action sync -Mode task -OwnerThread "农田交互修复V2" -ScopeRoots "Assets/YYY_Scripts/Controller/Input","Assets/YYY_Scripts/Farm","Assets/YYY_Scripts/Service/Placement","Assets/YYY_Scripts/UI/Inventory","Assets/YYY_Scripts/Service/Inventory",".kiro/specs/农田系统/2026.03.16",".kiro/specs/农田系统",".codex/threads/Sunset/农田交互修复V2" -IncludePaths "<按实际改动逐条填写>"
  9. 如果本轮拿过 `GameInputManager.cs` 锁，sync 成功后先释放锁：
     powershell -ExecutionPolicy Bypass -File .kiro/scripts/locks/Release-Lock.ps1 -TargetPath "Assets/YYY_Scripts/Controller/Input/GameInputManager.cs" -OwnerThread "农田交互修复V2" -ReleaseNote "1.0.2 hotfile real checkpoint complete"
  10. 然后立刻执行：
      powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action return-main -OwnerThread "农田交互修复V2"

- 如果第 5 步查锁发现已被占用：
  - 不准硬抢
  - 不准修改 `GameInputManager.cs`
  - 若本轮还能在非热文件范围内形成真实 checkpoint，可先 sync 再 return-main
  - 若本轮推进被热文件完全阻断，则直接 return-main，并把 `blocker_or_checkpoint` 写成 `hotfile-locked-by-other`

- 如果你已经拿到 `GameInputManager.cs` 锁，但中途无法形成安全 checkpoint：
  - 不准自释锁
  - 不准 return-main
  - 立刻停写
  - 聊天把 `hotfile_lock` 写成 `held-blocked`
  - 等治理接管

- 如果返回 LOCKED_PLEASE_YIELD：
  - 不准 ensure-branch
  - 不准查锁或拿锁
  - 不准在 main 写 tracked 文档
  - 只在 Draft 写下“热文件 checkpoint 目标 + 你对 carrier/main-ready 的初判”后等待

本轮聊天只回复：
- 已读 prompt 路径
- request-branch: <结果>
- ensure-branch: <成功 / 未执行 / 失败>
- hotfile_lock: <acquired / locked / not-needed / held-blocked / 未执行>
- sync: <成功 / 未执行 / 失败>
- return-main: <成功 / 未执行 / 失败>
- carrier_ready: <yes / no>
- main_ready: <yes / no>
- main_ready_basis: <一句话>
- changed_paths: <路径列表 / none>
- blocker_or_checkpoint: <一句话>
- 一句话摘要

本轮禁止：
- Primary.unity
- DialogueUI.cs
- Unity / MCP / Play Mode
- 在 main 写 tracked 文档
- 长篇复述过程
```
