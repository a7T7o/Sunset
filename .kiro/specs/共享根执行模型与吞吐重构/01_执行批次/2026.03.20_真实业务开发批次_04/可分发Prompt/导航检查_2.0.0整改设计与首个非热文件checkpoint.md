# 导航检查 - 2.0.0 整改设计与首个非热文件 checkpoint

```text
【真实业务开发批次 04｜导航检查｜2.0.0 整改设计与首个非热文件 checkpoint】

你的 continuation branch：
- codex/navigation-audit-001

这轮不是再次重述审计结论，也不是只读回包。
这轮的唯一目标是：让 `导航检查` 正式从 `1.0.0` 审计基线进入 `2.0.0` 整改设计，并尽量做出首个非热文件 checkpoint。

你已知的 live 业务事实：
- 当前导航审计基线已经成立
- 主链仍是：`GameInputManager -> PlayerAutoNavigator -> NavGrid2D`
- 当前用户优先级上，导航应先于遮挡进入真实整改

你的第一动作 MUST 是：
1. 执行：
   powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action request-branch -OwnerThread "导航检查" -BranchName "codex/navigation-audit-001" -CheckpointHint "navigation-2.0.0-real-dev-04" -QueueNote "dev-batch-04-wave-b"

分流规则：

- 如果返回 GRANTED 或 ALREADY_GRANTED：
  1. 执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action ensure-branch -OwnerThread "导航检查" -BranchName "codex/navigation-audit-001"
  2. 进入 branch 后先执行：
     git diff --name-status main...HEAD
  3. 本轮允许保留 / 修改 / 提交的路径范围只有：
     - Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs
     - Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs
     - .kiro/specs/999_全面重构_26.03.15/导航检查/
     - .codex/threads/Sunset/导航检查/memory_0.md
  4. 这轮必须完成的真实工作是：
     - 如果 `2.0.0整改设计` 目录尚不存在，先在 `.kiro/specs/999_全面重构_26.03.15/导航检查/` 下创建：
       - `2.0.0整改设计/requirements.md`
       - `2.0.0整改设计/design.md`
       - `2.0.0整改设计/tasks.md`
       - `2.0.0整改设计/memory.md`
     - 然后至少完成以下二选一：
       - docs-first checkpoint：把 `2.0.0` 的整改目标、边界与首个代码切入点正式钉死
       - code checkpoint：在不碰热文件的前提下，围绕 `NavGrid2D.cs` / `PlayerAutoNavigator.cs` 做出首个最小整改
  5. 本轮禁止碰：
     - Assets/YYY_Scripts/Controller/Input/GameInputManager.cs
     - Assets/000_Scenes/Primary.unity
     - Unity / MCP / Play Mode
  6. 本轮必须显式回答两件事：
     - `carrier_ready`
     - `main_ready`
     其中：
     - `carrier_ready` 回答这轮 branch checkpoint 是否收束
     - `main_ready` 回答 return-main 后当前 main 是否仍保持导航相关主入口可用
     - 如果你只是 docs-first / 非热文件 checkpoint，且没有制造 main 依赖缺口，通常 `main_ready = yes`
  7. 如果形成了 checkpoint，执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action sync -Mode task -OwnerThread "导航检查" -ScopeRoots "Assets/YYY_Scripts/Service/Navigation","Assets/YYY_Scripts/Service/Player",".kiro/specs/999_全面重构_26.03.15/导航检查",".codex/threads/Sunset/导航检查" -IncludePaths "<按实际改动逐条填写>"
  8. sync 成功后立刻执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action return-main -OwnerThread "导航检查"

- 如果返回 LOCKED_PLEASE_YIELD：
  - 不准 ensure-branch
  - 不准在 main 写 tracked 文档
  - 只在 Draft 写下“2.0.0 首个 checkpoint 计划 + 你对 carrier/main-ready 的初判”后等待

本轮聊天只回复：
- 已读 prompt 路径
- request-branch: <结果>
- ensure-branch: <成功 / 未执行 / 失败>
- hotfile_lock: not-needed
- sync: <成功 / 未执行 / 失败>
- return-main: <成功 / 未执行 / 失败>
- carrier_ready: <yes / no>
- main_ready: <yes / no>
- main_ready_basis: <一句话>
- changed_paths: <路径列表 / none>
- blocker_or_checkpoint: <一句话>
- 一句话摘要
```
