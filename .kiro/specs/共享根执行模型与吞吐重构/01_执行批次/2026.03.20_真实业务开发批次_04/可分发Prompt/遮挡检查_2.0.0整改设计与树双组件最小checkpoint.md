# 遮挡检查 - 2.0.0 整改设计与树双组件最小 checkpoint

```text
【真实业务开发批次 04｜遮挡检查｜2.0.0 整改设计与树双组件最小 checkpoint】

你的 continuation branch：
- codex/occlusion-audit-001

这轮不是再次复述审计成果，也不是只读说明 worktree / branch 口径。
这轮的唯一目标是：让 `遮挡检查` 正式从审计基线进入 `2.0.0` 整改设计，并尽量做出围绕“树双组件粒度”的首个最小 checkpoint。

你已知的 live 业务事实：
- 当前审计基线已经明确：树 Prefab 存在父/子双 `OcclusionTransparency`
- 后续最小整改目标不应一上来就大改整个 `OcclusionManager`
- 更合理的切入点是：
  - 树双组件粒度与 `SetCanBeOccluded()` 链路
  - 过时批处理工具
  - 测试覆盖当前真实主链

你的第一动作 MUST 是：
1. 执行：
   powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action request-branch -OwnerThread "遮挡检查" -BranchName "codex/occlusion-audit-001" -CheckpointHint "occlusion-2.0.0-real-dev-04" -QueueNote "dev-batch-04-wave-b"

分流规则：

- 如果返回 GRANTED 或 ALREADY_GRANTED：
  1. 执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action ensure-branch -OwnerThread "遮挡检查" -BranchName "codex/occlusion-audit-001"
  2. 进入 branch 后先执行：
     git diff --name-status main...HEAD
  3. 本轮允许保留 / 修改 / 提交的路径范围只有：
     - Assets/YYY_Scripts/Service/Rendering/OcclusionManager.cs
     - Assets/YYY_Scripts/Service/Rendering/OcclusionTransparency.cs
     - Assets/YYY_Scripts/Controller/TreeController.cs
     - Assets/Editor/BatchAddOcclusionComponents.cs
     - Assets/YYY_Tests/Editor/OcclusionSystemTests.cs
     - .kiro/specs/999_全面重构_26.03.15/遮挡检查/
     - .codex/threads/Sunset/遮挡检查/memory_0.md
  4. 这轮必须完成的真实工作是：
     - 如果 `2.0.0整改设计` 目录尚不存在，先在 `.kiro/specs/999_全面重构_26.03.15/遮挡检查/` 下创建：
       - `2.0.0整改设计/requirements.md`
       - `2.0.0整改设计/design.md`
       - `2.0.0整改设计/tasks.md`
       - `2.0.0整改设计/memory.md`
     - 然后至少完成以下三选一中的一个最小 checkpoint：
       - 树双组件粒度 / `SetCanBeOccluded()` 链路的首个修复
       - `BatchAddOcclusionComponents.cs` 与当前组件定义对齐
       - `OcclusionSystemTests.cs` 扩到当前真实主链
  5. 本轮禁止碰：
     - Assets/000_Scenes/Primary.unity
     - Assets/YYY_Scripts/Controller/Input/GameInputManager.cs
     - Unity / MCP / Play Mode
  6. 本轮必须显式回答两件事：
     - `carrier_ready`
     - `main_ready`
     其中：
     - `carrier_ready` 回答这轮 branch checkpoint 是否收束
     - `main_ready` 回答 return-main 后当前 main 是否仍保持遮挡相关主入口可用
     - 如果本轮只是 docs-first / 非场景 checkpoint，且没有制造 main 依赖缺口，通常 `main_ready = yes`
  7. 如果形成了 checkpoint，执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action sync -Mode task -OwnerThread "遮挡检查" -ScopeRoots "Assets/YYY_Scripts/Service/Rendering","Assets/YYY_Scripts/Controller","Assets/Editor","Assets/YYY_Tests/Editor",".kiro/specs/999_全面重构_26.03.15/遮挡检查",".codex/threads/Sunset/遮挡检查" -IncludePaths "<按实际改动逐条填写>"
  8. sync 成功后立刻执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action return-main -OwnerThread "遮挡检查"

- 如果返回 LOCKED_PLEASE_YIELD：
  - 不准 ensure-branch
  - 不准在 main 写 tracked 文档
  - 只在 Draft 写下“2.0.0 首个 checkpoint 计划 + 你对 carrier/main-ready 的初判”后等待

本轮聊天只回复：
- 已读 prompt 路径
- request-branch: <结果>
- ensure-branch: <成功 / 未执行 / 失败>
- hotfile_lock: not-needed
- sync: <成功 / 未执行 / 失败>
- return-main: <成功 / 未执行 / 失败>
- carrier_ready: <yes / no>
- main_ready: <yes / no>
- main_ready_basis: <一句话>
- changed_paths: <路径列表 / none>
- blocker_or_checkpoint: <一句话>
- 一句话摘要
```
