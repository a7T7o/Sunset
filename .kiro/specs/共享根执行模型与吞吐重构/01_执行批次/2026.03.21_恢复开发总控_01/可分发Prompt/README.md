# 可分发 Prompt README

## 当前原则
- 这些 prompt 现在都是直接开发 prompt，不再分“测试放行”和“正式开发”两段。
- 默认直接在 `main` 上干活。
- 只有撞高危目标、撞 Unity / MCP live 单写、或者把现场写坏了，才停下来。

## 当前建议直接发这些文件
- `scene-build_高质量初稿后续精修与spring-day1剧本对齐.md`
- `NPC_当前开发放行.md`
- `农田交互修复V2_当前开发放行.md`
- `导航检查_当前开发放行.md`
- `遮挡检查_当前开发放行.md`
- `spring-day1_当前开发放行.md`

## 哪些文件视为旧口径
- 带有 `验收失败返工`、`只读准备`、`结构层继续施工`、`逻辑层继续施工`、`回读自检` 等明显旧阶段字样的文件，默认都只当历史参考，不再当当前入口。

## 统一最小回执
- 当前在改什么
- changed_paths
- 是否触碰 Unity / MCP live 写
- 是否撞到高危目标
- blocker_or_checkpoint
- 一句话摘要
