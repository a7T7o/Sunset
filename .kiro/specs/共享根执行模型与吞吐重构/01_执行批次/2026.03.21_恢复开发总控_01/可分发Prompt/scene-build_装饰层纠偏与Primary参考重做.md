# scene-build - 装饰层纠偏与 Primary 参考重做

```text
【恢复开发总控 01｜scene-build｜装饰层纠偏与 Primary 参考重做】
你当前继续使用自己的专属 worktree：
- 工作目录：D:\Unity\Unity_learning\Sunset_worktrees\scene-build-5.0.0-001
- 分支：codex/scene-build-5.0.0-001
- 当前稳定基线：0717172a

这轮不是继续随手堆装饰物，也不是补几件散点道具。当前 SceneBuild_01 的装饰层审美不达标：
- 画面更像“房子和杂物被临时扔在底图上”
- 缺少 Primary 现有场景里那种地块组织、层次、前后景、院落关系和生活逻辑
- 你必须先学习 Primary，再回来重做当前 Decor_Farmstead

这轮你的目标是：
- 先只读对照 shared root 的 `Assets/000_Scenes/Primary.unity`
- 再把 SceneBuild_01 的装饰层从“随机摆件”重做到“和地形、建筑、入口、留白相互成立的小院场景”

你这轮 MUST 先做的事：
1. 先对照 `Primary.unity` 与当前 `SceneBuild_01`，至少提炼 3 个必须落地的组织原则。
   允许你聚焦但不限于：
   - 地表 / 路径 / 建筑 / 水边的衔接关系
   - 物件成组而不是撒点
   - 前景 / 中景 / 后景层次
   - 建筑周边的留白、入口和生活动线
   - 装饰服务于环境叙事，而不是单纯增加数量
2. 再在自己的 worktree 内重做 `Decor_Farmstead` 和建筑周边装饰。
3. 这轮重点不是“再多放几个对象”，而是纠正整体审美方向。

本轮允许：
- 在自己的 worktree 内继续修改 scene、文档、线程记忆
- 继续使用 Unity / MCP 或 Scene YAML；优先选你当前最稳定的路径
- 形成新的最小 Git checkpoint

本轮必须达到：
- 视觉上能看出你真的参考了 `Primary`，而不是另起一套乱搭
- 建筑与地表不能继续呈现“贴在灰底上”的割裂感
- 装饰要形成簇、边、入口、院落关系，而不是随机散点
- 不能继续用“多摆几件道具”冒充装饰层推进

本轮禁止：
- 不处理 shared root 残留的 `SceneBuild_01.unity`
- 不把“施工推进成功”写成“Unity live 验收通过”
- 不只交一段“我学了什么”的口头总结而没有真实重做

本轮回执只回复：
- 已读 prompt 路径
- 当前 branch / HEAD
- 当前 git status
- 本轮 checkpoint
- 是否触碰 Unity / MCP
- 是否已经 clean
- 对 Primary 学到的 3 点
- blocker_or_checkpoint
- 一句话摘要
```
