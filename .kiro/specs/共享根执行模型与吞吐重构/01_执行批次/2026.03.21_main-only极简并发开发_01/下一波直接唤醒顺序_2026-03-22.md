# 下一波直接唤醒顺序｜2026-03-22

## 当前结论
- 这一波不再做“统一收口回执”，而是直接恢复真实开发。
- 继续停下的线程只有：
  - `spring-day1`
  - 全局 skills
- 当前最该继续的 4 条线：
  1. `scene-build`
  2. `导航检查`
  3. `NPC`
  4. `遮挡检查`

## 唤醒顺序
1. `scene-build`
   - 原因：它是当前最耗时、最影响 Day1 承载体验的主施工线，而且已经有稳定 worktree 现场，不该再挂起。
   - 口径：独立 worktree 持续施工；每一刀都直接在 worktree 分支里收 checkpoint。
   - prompt：
     - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\可分发Prompt\scene-build_当前版本更新并继续施工.md`

2. `导航检查`
   - 原因：NPC 后续更完整的动态规避、让行和玩家自动导航绕移动 NPC，最终都要靠导航核心推进。
   - 口径：不再停在 docs-first；直接进入首个非热文件真实代码 checkpoint。
   - prompt：
     - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\可分发Prompt\导航检查_当前版本更新并继续直接开发.md`

3. `NPC`
   - 原因：当前 main-only 第一刀已经完成，后续应继续收 NPC 自己的表现层与碰撞体感，而不是再等导航线程全做完。
   - 口径：继续只认 NPC 自己的代码范围，不越界去做导航核心。
   - prompt：
     - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\可分发Prompt\NPC_当前版本更新并继续直接开发.md`

4. `遮挡检查`
   - 原因：它有明确的真实问题和已完成审计结论，但之前一直停在 branch carrier / docs-first。
   - 口径：先做最小但真实的遮挡整改 checkpoint，不再停在只读审计态。
   - prompt：
     - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\可分发Prompt\遮挡检查_当前版本更新并继续直接开发.md`

## 当前不唤醒
- `spring-day1`
  - 当前职责已完成，代码与文档都已进入 `main`，现在不需要再拉起来制造噪音。
- 全局 skills
  - 这轮给 Sunset 的本地裁定已经被治理线吸收；除非后面要做旧 trigger log 归一化或上升 global learning，否则先不再唤醒。

## 固定回执口径
- 不要让项目经理手动再建文件。
- 所有线程都直接把本轮回执追加到：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\001部分回执.md`

## 一句话口径
- 这一波不是“再证明能不能开始”，而是“直接按新制度恢复真实开发，并做到本轮承诺的 checkpoint 再停”。
