# 遮挡检查｜当前版本更新并继续直接开发

```text
当前版本更新，你这条线不要再停在 branch carrier / 审计态，也不要再只做 docs-first。

先纠正你上一轮回复里的一个已经作废的旧口径：
- `codex/occlusion-audit-001 @ 295e8138` 还没进 `main`，这件事本身不再构成你当前不能继续的理由。
- 从现在开始，你不能再把“整改 checkpoint 还停在业务分支上”当作默认 blocker 回来。
- 你这轮必须自己把这批历史成果处理掉，而且要和真实遮挡整改放在同一轮里完成。

当前唯一真实基线：
- `D:\Unity\Unity_learning\Sunset @ main`
- 默认模型：`main-only + whitelist-sync + exception-escalation`
- 你完成一刀后，默认自己白名单提交自己的 checkpoint
- 如果这轮改 `.cs`：
  - 收口前 `git-safe-sync.ps1` 会自动跑代码闸门
  - 你不能再等用户贴编译错误后才补修

你先完整回忆并汇报：
1. 你之前的审计到底已经锁定了哪些真实遮挡问题。
2. `codex/occlusion-audit-001 @ 295e8138` 里哪些还值得继续，哪些现在直接判废。
3. 你之前停在分支收口判断的原因是什么，哪些现在已经作废。
4. 你这轮准备先做哪个“最小但真实的遮挡整改 checkpoint”。

你这轮要一次性做完两件事，不允许只做其中一半：
1. 先处理 `295e8138` 的历史遗留：
   - 如果这组内容里仍有价值的最小文档/工具/代码，就直接白名单迁入 `main`
   - 如果其中一部分只是旧垫片或已失效思路，就当场判废，不再挂成 blocker
2. 然后立刻开始真实整改，不要继续把时间花在重复审计结论上。

本轮优先目标建议按这个顺序做：
1. 先围绕“树 parent/child 双 `OcclusionTransparency` 粒度问题”做首刀修正或最小可验证改造。
2. 同步把过时工具 `BatchAddOcclusionComponents.cs` 收到当前真实组件定义。
3. 如果有余力，再补最小测试覆盖，不要先扩到大面积 scene 手改。

当前职责边界：
- 你可以改：
  - 遮挡系统脚本
  - 遮挡相关编辑器工具
  - 遮挡专项测试
  - 必要的 tree prefab 侧配置*
- 你这轮尽量先不要碰：
  - `Primary.unity`
  - `GameInputManager.cs`
  - 与 NPC / 导航无关的别的系统

只有命中下面情况你才停：
1. 撞到同一个高危目标。
2. 你要做 Unity / MCP live 写，但已经有人在写。
3. 你把遮挡现场写坏了，需要先收口。

不允许再回：
- “295e8138 还在分支上，所以我先不动”
- “我这轮最多只确认主线收口策略，不直接扩写新业务”

直接做到这轮承诺的完整结果再停：
1. `295e8138` 的历史遗留已处理
2. `main` 上首个真实遮挡整改 checkpoint 已形成

回执直接追加到：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\001部分回执.md`

聊天只回这些字段：
- 当前在改什么
- 上次 checkpoint / 当前恢复点
- 旧分支遗留如何处理：迁入 / 判废 / 部分保留
- 剩余任务清单
- 本轮下一步最多推进到哪里
- changed_paths
- 是否触碰高危目标
- 是否需要 Unity / MCP live 写
- `code_self_check: pass / fail / not-applicable`
- `pre_sync_validation: pass / fail / not-run`
- 当前是否可以直接提交到 `main`
- blocker_or_checkpoint
- 一句话摘要
```
