# spring-day1 - 当前任务回执与向scene-build交接

```text
这不是新的大开发 prompt。
你先把手里当前这一刀收口到可描述状态，然后停下来给我一份“向 scene-build 交接”的回执。

这轮要求：
- 不再扩新波次
- 先把你手里的当前一刀收成一个清楚的交接口
- 重点不是证明你能不能开始，而是把 scene-build 真正需要吃的内容讲清楚

你要按真实现场回答下面这些字段：
- 已读 prompt 路径
- 当前 branch / HEAD
- 当前 git status：clean 还是 dirty
- 当前这一刀已经完成了什么
- 可以直接交给 scene-build 的内容
- scene-build 还必须承载的空间 / 动线 / 触发 / 道具位 / 教学位
- 不应该由 scene-build 重复做的内容
- changed_paths
- 是否触碰 Unity / MCP live 写
- handoff_ready: yes / no
- next_scene_build_focus
- blocker_or_checkpoint
- 一句话摘要

如果 `handoff_ready = no`，请明确写清：
- 还差哪一个最小尾项
- 这个尾项收完后，交接材料会落在哪些路径
```
