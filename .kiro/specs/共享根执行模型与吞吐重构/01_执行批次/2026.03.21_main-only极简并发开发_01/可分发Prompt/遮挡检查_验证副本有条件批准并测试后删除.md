# 遮挡检查｜验证副本有条件批准并测试后删除

```text
这次对 `D:\Unity\Unity_learning\Sunset_occlusion_validation` 的裁定是：**有条件批准**。

先说清楚：
- 你这次最大的问题，不是改坏了主仓库，也不是偷偷切了 git worktree。
- 真正的流程问题是：你没有先报备，就擅自扩张了验证策略，直接新建了一个测试副本目录。
- 这件事这次我不按事故处理，但也不允许它变成长期第二现场。

当前批准口径：
1. 允许你把 `D:\Unity\Unity_learning\Sunset_occlusion_validation` 继续保留到**这轮遮挡 EditMode 测试跑完**。
2. 它只允许作为**一次性测试沙盒**存在。
3. 它不是：
   - 新项目根
   - 新 worktree
   - 新开发现场
   - 长期验证环境

这轮你现在必须一次性做完这些事：
1. 只在这个副本里完成你刚刚已经启动的 EditMode 测试。
2. 收回测试结果：
   - 是否跑完
   - 是否通过
   - 测试 XML / 日志路径
3. 测完立刻删除：
   - `D:\Unity\Unity_learning\Sunset_occlusion_validation`
4. 删除后回执说明：
   - 副本已删除
   - 主仓库没有从副本反写 tracked 文件

明确禁止：
- 不要在副本里继续改业务代码
- 不要在副本里继续改 Scene / Prefab / memory
- 不要从副本做 git 提交 / push / sync
- 不要把这个副本长期保留成第二现场

如果测试过程中你发现：
- 只是导入太慢，还没出结果：等这轮跑完再删
- 测试已经卡死且没有结果：停止测试，删除副本，并如实回执“未拿到结果”

这轮你的目标不是再解释为什么建副本，而是把这件事收干净。

回执直接追加到：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\001部分回执.md`

聊天只回这些字段：
- 当前在做什么
- 测试是否跑完：yes / no
- 测试结果：pass / fail / incomplete
- 测试 XML / 日志路径
- 副本是否已删除：yes / no
- 主仓库是否被副本反写：yes / no
- blocker_or_checkpoint
- 一句话摘要
```
