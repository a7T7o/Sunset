# 遮挡检查｜主工程单实例验证窗口批准

```text
这轮我批准你按“主工程单实例 + MCP”方案继续做遮挡验证。

但这是**有条件批准**，不是无限制放行。

批准范围只有这一个验证窗口：
- 项目：`D:\Unity\Unity_learning\Sunset`
- 只用当前已经打开的主工程 Unity 实例
- 不再创建任何副本、worktree、复制目录
- 只做：
  - `Edit Mode` 确认
  - Console 清空
  - 新增 6 条行为测试
  - 全套 `OcclusionSystemTests`
  - 对失败点做最小修正
  - 立刻重跑同组测试

这轮你需要一个**清净时刻**，具体含义是：
1. 当前验证窗口里，不要有别的线程同时占用同一个 Unity/MCP live 写入口。
2. 不要有别的线程同时改同一批遮挡相关脚本、树相关脚本、`Primary.unity`。
3. 你把这轮验证做完、回执后，这个窗口就自动结束。

当前证据落点固定为：
- MCP 返回的测试 job 结果
- Console 前后快照
- `D:\Unity\Unity_learning\Sunset\.codex\drafts\遮挡检查\main-live-validation_2026-03-23.md`

明确禁止：
- 不要再创建任何验证副本
- 不要顺手扩大到别的系统
- 不要把这轮验证窗口拖成长期开发窗口

如果这轮中途再次遇到 MCP / Unity 入口阻塞：
- 你可以停
- 但不要自行扩张验证策略
- 直接回我当前卡点

这轮你的目标不是“继续解释方案”，而是拿到这三种结果之一：
1. 理想：新增 6 条行为测试 + 全套 `OcclusionSystemTests` 都有明确通过结果
2. 次优：拿到首个真实失败点，并修掉后重跑出新结果
3. 最差：明确证明卡在 MCP / Unity 入口，而不是再拿不出证据

回执直接追加到：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\001部分回执.md`

聊天只回这些字段：
- 当前在做什么
- 是否已拿到清净验证窗口：yes / no
- 测试是否跑完：yes / no
- 测试结果：pass / fail / incomplete
- 失败点或通过摘要
- MCP / Console 证据路径
- 是否改了主工程 tracked 文件：yes / no
- blocker_or_checkpoint
- 一句话摘要
```
