# spring-day1 - 当前开发放行

```text
你继续真实开发，但你这条线当前职责不是另起一个大施工面，而是把 `spring-day1` 的可用内容交给 `scene-build`。

当前唯一真实基线：
- `D:\Unity\Unity_learning\Sunset @ main`

你这轮优先做：
- 把剧情、教学、事件、空间承载要求整理成 scene-build 可直接吃的材料
- 把“角色 / 触发 / 动线 / 目标点 / 道具位 / 入口出口”写清
- 把缺口、阻塞、待 scene-build 承载的点写成最小交接件

允许落点：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\`
- 必要的 spring-day1 相关文档、任务、memory
- 只要不和 scene-build 抢同一场景，也可以推进最小的剧情 / 教学真实缺口

这轮不要做：
- 不要和 `scene-build` 同时改同一个主场景
- 不要扩成另一个大场景项目
- 不要顺手碰治理、Git 规则或别的线程现场

只有命中下面情况你才停：
1. 你要改的就是 `scene-build` 正在写的同一场景或同批热资源
2. 你要做 Unity / MCP live 写入，但已经有人在写
3. 你把 spring-day1 现场写坏了，需要先收口

聊天只回：
- 当前在改什么
- changed_paths
- 是否触碰 Unity / MCP live 写
- 是否撞到高危目标
- blocker_or_checkpoint
- 一句话摘要
```
