# 导航检查｜当前版本更新并继续直接开发

```text
当前版本更新，你这条线不要再停在 docs-first，也不要再沿用旧的 branch waiting 口径。

先纠正你上一轮回复里的一个已经作废的旧口径：
- `codex/navigation-audit-001` 上还留着历史成果，这件事本身不再构成你当前不能开工的理由。
- 从现在开始，你不能再把“成果还在分支上”当作默认 blocker 回来。
- 你这轮必须自己把这件事处理掉，而且要和真实导航开发放在同一轮里完成，而不是再停下来等治理线程替你迁。

当前唯一真实基线：
- `D:\Unity\Unity_learning\Sunset @ main`
- 默认模型：`main-only + whitelist-sync + exception-escalation`
- 你完成一刀后，默认自己白名单提交自己的 checkpoint

你先完整回忆并汇报：
1. 你现有 `2.0.0整改设计` 到底已经确定了哪些导航问题。
2. `codex/navigation-audit-001` 上到底哪些内容仍有价值，哪些现在直接判废。
3. 你之前停在 docs-first / 分支等待的原因是什么，哪些现在已经作废。
4. 你这轮准备先做哪个“首个非热文件真实代码 checkpoint”。

你这轮要一次性做完两件事，不允许只做其中一半：
1. 先处理旧分支遗留：
   - 如果 `codex/navigation-audit-001` 上有仍值得保留的最小文档/代码，就直接白名单迁入 `main`
   - 如果那批东西只是旧 docs-first 垫片，就当场判废，不再继续把它挂成 blocker
2. 然后立刻进入真实导航整改，不要继续扩写设计稿。

本轮优先目标：
1. 围绕 `NavGrid2D` / `PlayerAutoNavigator` 做第一刀真实代码推进。
2. 优先解决“玩家自动导航绕移动 NPC”与“为后续 NPC/NPC 动态规避预留入口”的基础能力。
3. 尽量先避开 `GameInputManager.cs` 这种热文件，除非你真的证明第一刀绕不开。

当前职责边界：
- 你认领导航核心：
  - 动态障碍
  - 动态重规划
  - 玩家绕移动 NPC
  - NPC/NPC 局部规避与让行基础
- 不要越界去改 NPC 气泡、NPC 碰撞体视觉参数或 scene-build 的场景搭建

只有命中下面情况你才停：
1. 撞到同一个高危目标。
2. 你要做 Unity / MCP live 写，但已经有人在写。
3. 你把导航现场写坏了，需要先收口。

不允许再回：
- “我还在分支上，所以现在不能继续”
- “我先只确认分支怎么收，再说开发”

直接做到这轮承诺的完整结果再停：
1. 旧分支遗留已处理
2. `main` 上首个真实导航代码 checkpoint 已形成

回执直接追加到：
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\001部分回执.md`

聊天只回这些字段：
- 当前在改什么
- 上次 checkpoint / 当前恢复点
- 旧分支遗留如何处理：迁入 / 判废 / 部分保留
- 剩余任务清单
- 本轮下一步最多推进到哪里
- changed_paths
- 是否触碰高危目标
- 是否需要 Unity / MCP live 写
- 当前是否可以直接提交到 `main`
- blocker_or_checkpoint
- 一句话摘要
```
