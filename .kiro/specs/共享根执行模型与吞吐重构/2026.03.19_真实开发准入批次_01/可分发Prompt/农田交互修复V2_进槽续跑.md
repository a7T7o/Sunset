# 农田交互修复V2 - 真实开发准入批次 01 - 进槽续跑

```text
【真实开发准入批次 01｜农田交互修复V2｜进槽续跑】
使用前提：
- NPC 已完成本轮并成功 return-main
- shared root 已恢复 main + neutral + clean
- 你已经完成准备态 Draft

你的 continuation branch：
- codex/farm-1.0.2-cleanroom001

你的本轮目标：
- 基于你在准备态写好的 Draft，进入最小真实修复 / checkpoint
- 优先做不依赖 A 类热文件的那部分
- 不要在本轮扩张到整轮大重构

你的第一动作：
1. 执行：
   powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action request-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001" -CheckpointHint "farm-real-repair-01" -QueueNote "dev-batch-01"

分流规则：

- 如果返回 GRANTED 或 ALREADY_GRANTED：
  1. 执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action ensure-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001"
  2. 进入 branch 后，只做你在 Draft 中预先写好的第一检查点
  3. 本轮优先允许的作用域：
     - Assets/YYY_Scripts/Farm/
     - Assets/YYY_Scripts/Service/Placement/
     - Assets/YYY_Scripts/Data/Items/
     - Assets/111_Data/Items/
     - Assets/111_Data/Farm/
     - 对应 .meta
  4. 本轮禁止直接写：
     - Assets/000_Scenes/Primary.unity
     - Assets/YYY_Scripts/Controller/Input/GameInputManager.cs
     - 其他 A 类热文件
  5. 如果本轮最小改动全部落在允许作用域内，执行 task sync：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action sync -Mode task -OwnerThread "农田交互修复V2" -ScopeRoots "Assets/YYY_Scripts/Farm","Assets/YYY_Scripts/Service/Placement","Assets/YYY_Scripts/Data/Items","Assets/111_Data/Items","Assets/111_Data/Farm" -IncludePaths "<按实际改动逐条填写>"
  6. sync 成功后立刻执行：
     powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 -Action return-main -OwnerThread "农田交互修复V2"

- 如果返回 LOCKED_PLEASE_YIELD：
  - 不准 ensure-branch
  - 读取 DRAFT_FILE_HINT
  - 只在 Draft 沙盒补写等待恢复点
  - 不准写 tracked 文件

- 如果你发现真正要推进的第一检查点离不开：
  - GameInputManager.cs
  - Primary.unity
  - 其他 A 类热文件
  那么本轮不要硬写，请明确回报 hot-file blocked，并停止在证据层。

持槽纪律：
- 只做一个最小 checkpoint
- 不把长分析、复盘、治理补记塞进持槽窗口

本轮聊天只回复：
- 已读 prompt 路径
- request-branch: <结果>
- ensure-branch: <成功 / 未执行 / 失败>
- sync: <成功 / 未执行 / 失败>
- return-main: <成功 / 未执行 / 失败>
- changed_paths: <路径列表 / none>
- blocker_or_checkpoint: <一句话>
- 一句话摘要
```
