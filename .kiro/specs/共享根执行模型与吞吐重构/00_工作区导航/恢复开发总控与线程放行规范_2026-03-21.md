# 恢复开发总控与线程放行规范 - 2026-03-21

## 现在的核心口径
- 这份文件从现在开始只做一件事：帮项目经理快速判断“谁现在可以直接干活，谁只有在高危时才要停”。
- 当前最高目标是进度飞跃，不是流程完美。
- 当前唯一真实开发基线是 `D:\Unity\Unity_learning\Sunset @ main`。
- 不再把 branch / grant / return-main 当作普通开发前置。

## 现在保留的规则只有 4 条
1. 所有线程都可以直接开始开发，不用先等治理线程批准。
2. 如果撞上同一个高危目标，先到先做，后到立刻停。
3. 如果需要 Unity / MCP live 写入，而另一个线程已经在写，也必须停。
4. 如果已经把现场写坏了，先收烂摊子，再继续扩开发。

## 什么叫高危目标
- 同一个 Scene 文件
- 同一个 Prefab
- `Primary.unity`
- `Assets/000_Scenes/SceneBuild_01.unity`
- `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
- 任意当前正在被别的线程 live 写入的 Unity 资源

## Git 口径
- 从现在起，Git 不再作为开发前置交通系统。
- 不再要求普通开发先走 branch / grant / return-main。
- 默认就在 `main` 上开发。
- 只保留一个底线：不做破坏性 Git 操作。
- checkpoint 什么时候做，以“这一刀值不值得留”为准，不再以流程为准。

## 治理线程现在只负责什么
- 发现并打断高危撞车。
- 发现 Unity / MCP 单写冲突时提醒停下。
- 某条线程把现场写坏时帮忙收烂摊子。
- 维护最小总控文档和当前 prompt。

## 当前线程状态

### 现在可以直接开干
- `scene-build`
  - 允许继续。
  - 目标不是泛化美术修补，而是继续对齐 `spring-day1` 剧本，把场景做成能承载剧情的空间。
  - 如果还在旧 worktree / 旧 branch 上，这是过渡现场，不再扩张为长期模型；后续应尽快把有效成果带回 `main`。
- `NPC`
  - 允许继续。
  - 当前重点是把气泡位置、碰撞体、人与人/人与玩家穿透这些真实问题做掉。
- `农田交互修复V2`
  - 允许继续。
  - 当前重点是继续做真实交互问题，不再受 branch-ready / main-ready 口径拖住。
- `导航检查`
  - 允许继续。
- `遮挡检查`
  - 允许继续。
- `spring-day1`
  - 允许继续。
  - 但如果碰到和别人同一个主场景 / 同一个热脚本，就按高危撞车规则停。

### 必须停下的情况
- 已经有人在改你要改的同一个高危目标。
- 你也要进 Unity / MCP live 写，但现在已经有人在写。
- 你准备做危险 Git 动作。
- 你刚把编译、场景、引用或资源关系写坏了。

## 给项目经理的最短执行法
1. 直接发当前 prompt 给线程，不用再先做放行测试。
2. 线程回你时，只看 5 件事：
   - 现在在改什么
   - 改了哪些文件
   - 有没有进 Unity / MCP live 写
   - 有没有撞上高危目标
   - 当前 blocker 是什么
3. 如果没撞高危，就继续让他干。
4. 只有撞高危、撞 Unity live 写、或者把现场写坏了，才把回执贴给治理线程处理。

## 当前 prompt 入口
- `scene-build`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\scene-build_高质量初稿后续精修与spring-day1剧本对齐.md`
- `NPC`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\NPC_当前开发放行.md`
- `农田交互修复V2`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\农田交互修复V2_当前开发放行.md`
- `导航检查`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\导航检查_当前开发放行.md`
- `遮挡检查`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\遮挡检查_当前开发放行.md`
- `spring-day1`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\spring-day1_当前开发放行.md`

## 一句话口径
- 现在别再排队等放行了；大家直接在 `main` 上干，只有撞同一个高危目标、撞 Unity live 写、或者把现场写坏了，才停下来找治理线程。
