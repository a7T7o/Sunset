# 当前总览与待办 - 2026-03-20

## 1. 本工作区负责什么
- 负责 `shared root` 执行模型、批次分发、队列/让行/归还规则，以及“线程说自己收口了”是否等于“用户回到 `main` 就能正常开发”的终局验收。
- 这里不是具体业务系统工作区；这里负责的是业务线程如何安全、高吞吐地进入、交付、归还。

## 2. 目录约定
- 根目录只保留四类常驻正文：
  - `requirements.md`
  - `design.md`
  - `tasks.md`
  - `memory.md`
- `00_工作区导航/`
  - 放总览、导航、待办总表
- `01_执行批次/`
  - 放各轮 `smoke / 准入 / 真实业务开发批次`
- `02_专题分析/`
  - 放按日期归档的专题分析、事故反思、讨论稿

## 3. 当前 live 基线
- 本轮整理开始前，`Sunset` 已恢复到：
  - `main + clean`
  - shared root `main + neutral`
- 本轮目录整理属于治理文档重组，不涉及业务代码与 Unity 资源修改。

## 4. 当前问题的定性
- `NPC` 事故已经物理修复，但它暴露了一个真实治理缺口：
  - 以前只检查 `carrier-ready`
  - 现在必须同时检查 `main-ready`
- 这不是为了绕开定责，而是为了把两层责任拆开：
  - 线程有没有把交付面真正落到 `main`
  - 治理模板有没有把这个问题问出来

## 5. 当前总待办

### A. 立即推进
1. `真实业务开发批次 04`
   - 状态：已生成，可直接分发
   - 根入口：
     - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-03-20_批次分发_08_真实业务开发批次_04.md`
   - 本轮已补入：
     - `carrier_ready`
     - `main_ready`
     - `main_ready_basis`
2. `NPC`
   - 状态：可继续
   - 当前真实目标：在 `codex/npc-roam-phase2-003` 上继续 `phase2` 真实业务推进，而不是再做“资源是否存在”的探测。
   - 新验收要求：如果 `Primary.unity` 或主运行入口依赖该线资产，必须显式回答是否已经 `main-ready`。
3. `农田交互修复V2`
   - 状态：可继续
   - 当前真实目标：继续 `1.0.2` 交付面推进，不再停留在 carrier 清洗描述。
   - 风险点：如果进入 `GameInputManager.cs`，必须严格走热文件锁。
4. `导航检查`
   - 状态：待纳入下一轮真实业务批次
   - 当前已知基础：审计基线已建立，且用户优先级上应先于 `遮挡检查`。
   - 下一步应从“只读审计收口”切到“真实整改设计 / 首个最小整改 checkpoint”。
5. `遮挡检查`
   - 状态：待纳入下一轮真实业务批次
   - 当前已知基础：审计基线已建立，最小整改目标先锁定树 Prefab 双 `OcclusionTransparency` 粒度问题。
   - 排位：放在 `导航检查` 之后。
6. `spring-day1`
   - 状态：未纳入本轮共享根真实业务批次
   - 当前类型：集成型 / 场景型 / Unity 风险更高，不应混同于 `NPC / 农田 / 导航 / 遮挡` 的短事务线。
   - 下一步：需要单独决定它是进入下一轮只读准备，还是单开集成波次。

### B. 治理尾项
1. `main-ready` 模板化
   - 状态：已进入批次 04 prompt / 回收卡
   - 下一步从“生成模板”切到“根据回执验证口径是否足够”
2. `dirty` 分级与清扫推送机制
   - 当前只有讨论稿，未批准上线。
   - 后续要做成：分级、owner、takeover 条件、失败 runbook。
3. 下一轮波次排程
   - 至少要明确：
     - `NPC / 农田` 是继续优先推进，还是只保留其中一条先收第一段真实业务 checkpoint
     - `导航 / 遮挡 / spring-day1` 如何并入波次，而不是继续停留在“以后再说”

### C. 特殊执行类型
1. 重度 `MCP` 场景搭建线程
   - 当前已形成正式方案，且已完成隔离：
     - branch：`codex/scene-build-5.0.0-001`
     - worktree：`D:\Unity\Unity_learning\Sunset_worktrees\scene-build-5.0.0-001`
   - 这意味着它当前不再占用 shared root 的 `main`，可以继续在专属 branch 内写自己的文档与后续 WIP。
   - 但它现在仍应保持“docs / 规划 / 资产普查优先”；真正进入 Unity / MCP 写入前，仍要按“单线程独占 + Unity/MCP 单写者 + 验证场景优先”单独开闸。

## 6. 推荐下一步顺序
1. 直接执行 `批次 04`：
   - 第一波先发 `NPC`、`农田`
   - 第二波再发 `导航`、`遮挡`
2. 波次建议：
   - `NPC` 与 `农田` 继续作为第一波真实开发
   - `导航` 进入下一波最小整改设计 / 非热文件 checkpoint
   - `遮挡` 排在 `导航` 之后
   - `spring-day1` 单独判断是否另开集成波次
3. `dirty` 机制留在治理窗口，不与当前真实业务推进混写。

## 8. 全量后续路线
1. `场景搭建线程`
   - 已转入 `codex/scene-build-5.0.0-001`
   - 继续推进 `5.0.0场景搭建 / 1.0.1初步规划`
   - 当前允许：文档、资产普查、scene 命名、Prefab 候选池整理
   - 当前不建议：直接进入 Unity / MCP 高频施工
2. `真实业务开发批次 04 - 第一波`
   - `NPC`
   - `农田交互修复V2`
   - 现在可发，前提是继续保持 shared root `main + clean + neutral`
3. `真实业务开发批次 04 - 第二波`
   - `导航检查`
   - `遮挡检查`
   - 放在第一波至少完成一条真实 checkpoint 之后
4. `spring-day1`
   - 不进 batch04，不是遗漏
   - 原因是它属于集成型 / 主场景型 / 高 Unity 风险线程，不适合塞进这轮短事务 shared root 波次
   - 后续应单开一轮 `spring-day1` 集成波次，而不是混在 `NPC / 农田 / 导航 / 遮挡` 里
5. `dirty 分级与清扫推送机制`
   - 继续保留在治理窗口
   - 当前仍是讨论稿，未批准上线
6. `main-ready` 回收口径复盘
   - 等 batch04 第一波和第二波收件后，再判断模板是否足够严

## 7. 本文件的作用
- 这不是历史总结，而是本工作区当前可执行的总表。
- 后续如果再出现“聊天里想明白了，但没有正式挂到工作区里”的情况，优先更新这里，再发下一轮批次。
