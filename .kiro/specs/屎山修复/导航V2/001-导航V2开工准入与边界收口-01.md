# 导航V2 开工准入与边界收口委托-01

## 当前已接受的基线

1. `000-gemini锐评-1.1.md` 当前维持 `Path C`，这个方向判断基本成立。
2. `000-gemini锐评-1.1审视报告.md` 的大方向判断有效：
   - 可以吸收 Gemini 对控制流崩坏、职责过重、交通惯性、shape-aware 的提醒；
   - 不能把 Gemini 文档升格成高于 `006/007` 的上位法；
   - 不能把当前主刀重新带回 `TrafficArbiter / MotionCommand`。
3. `000-gemini锐评-1.0.md` 当前“判成 `Path B`”这件事可以保留，但它的可吸收边界还没有被你彻底钉死。
4. 当前 `导航V2` 仍然是“锐评审核 / 自省 / 下一代交接前认知收口”工作区，不是导航实现施工入口。

## 当前不接受的点

1. 你上一轮把“父工作区记忆和线程记忆都已同步更新”写成：
   - `屎山修复/memory.md`
   - `导航检查/memory_0.md`
   但现场实际还存在独立线程记忆：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
   这说明你的“线程记忆”表述存在父子线程边界混淆，必须纠偏。
2. 你对 `000-gemini锐评-1.0.md` 虽然给了 `Path B`，但还没有把：
   - 哪些点是“可以吸收的问题意识”
   - 哪些点“绝不能直接转成施工硬令”
   - 它们如何重新服从 `006/007`
   单独冻结成一份清晰可裁判的边界结论。
3. 基于上面两点，当前还不能直接批准你进入导航实现施工。

## 本轮唯一主刀

只做一件事：

> 把 `000-gemini锐评-1.0.md` 的 `Path B` 边界、`导航检查V2` 的线程记忆边界、以及“何时才允许从锐评审核切到实现施工”的开工准入条件，一次性钉死。

## 允许的 scope

只允许读取、更新或新增以下范围内的文档与记忆：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\000-gemini锐评-1.0.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\000-gemini锐评-1.1.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\000-gemini锐评-1.1审视报告.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\memory.md`
5. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
6. 如需解释父层同步，再只读引用：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\memory.md`
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\memory_0.md`
7. 上位依据只允许继续引用：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\006-Sunset专业导航系统需求与架构设计.md`
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\007-Sunset专业导航底座后续开发路线图.md`
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\005-genimi锐评-4.0审视报告.md`

## 本轮禁止漂移

1. 不准开始任何导航实现代码施工。
2. 不准跑 Unity、MCP、compile、tests、live。
3. 不准重新扩写 `TrafficArbiter / MotionCommand / DynamicNavigationAgent / 全量 linearVelocity` 大讨论。
4. 不准把这轮再写成“我觉得 Gemini 大方向很对”这种泛化心得。
5. 不准继续把父线程记忆、子线程记忆、父工作区记忆混成同一个“线程记忆已同步”表述。

## 这轮你必须正面完成的事

### 1. 钉死 `000-gemini锐评-1.0.md` 的 `Path B` 边界

如果你继续维持 `Path B`，你必须明确冻结下面三层：

1. 可吸收的问题意识
2. 不能直接转成施工指令的部分
3. 它在 Sunset 当前语境里只能怎样被使用

这部分必须形成正式文档落点；不能只在聊天里一句话带过。

### 2. 纠正 `导航检查V2` 的线程记忆边界与回执报实

你必须明确回答：

1. `导航检查V2` 是否有自己的线程记忆？
2. 如果有，为什么你上一轮回执里把“线程记忆”写成了 `导航检查/memory_0.md`？
3. 父线程记忆如果这轮也同步了，它应该被表述成“父线程补同步”，不能再冒充 `导航检查V2` 的线程记忆。

### 3. 给出“是否可开工”的准入判定

你这轮要明确回答的不是“我想不想开工”，而是：

1. 当前 `导航V2` 是否已经具备“从锐评审核切到导航实现施工”的最小条件？
2. 如果还不具备，差的最后一层是什么？
3. 下一轮应该继续停留在：
   - `继续审核 / 认知收口`
   还是
   - `允许进入实现施工`

这里必须是可裁判结论，不能写成模糊愿景。

## 完成定义

只有同时满足下面 3 条，这轮才算完成：

1. `000-gemini锐评-1.0.md` 的 `Path B` 边界被正式钉死，不再只剩一句“问题意识整体有效”。
2. `导航检查V2` 的线程记忆边界被纠正，回执不再把父线程记忆误报成自己的线程记忆。
3. 你明确给出“当前仍不可开工”或“现在可以进入实现施工”的准入判定，并写清理由。

## 固定回执格式

只按下面格式回：

```text
审核结论：
是否允许开工：
`000-gemini锐评-1.0.md` 可吸收点：
`000-gemini锐评-1.0.md` 禁转施工点：
线程记忆纠偏：
changed_paths：
当前 own 路径是否 clean：
blocker_or_checkpoint：
一句话摘要：
```
