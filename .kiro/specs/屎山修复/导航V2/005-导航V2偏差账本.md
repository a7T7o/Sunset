# 导航V2偏差账本

版本: v1.0  
日期: 2026-03-27  
状态: 生效中  
工作区: `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2`

---

## 1. 文档定位

这不是新的设计稿。  
这也不是新的 prompt 汇总。

它是一份给 `导航V2` 使用的**实时偏差账本**，作用只有一个：

> 持续记录“目标蓝图 / 当前路线 / 当前 runtime 事实 / 当前主刀”之间的偏差与收敛，避免导航线再次退回只靠聊天记忆推进的状态。

这份账本必须从现在开始持续维护。  
它不是事后尸检，而是实时记账。

---

## 2. 使用规则

以下情况必须更新本账本：

1. `002 / 004` 被修订
2. 外部锐评被审核并形成稳定结论
3. 当前第一阻塞点发生变化
4. 当前已接受 checkpoint 发生变化
5. 当前冻结项发生变化
6. 当前阶段判断发生变化
7. 当前实现线程入口发生变化
8. 当前需要上报的升级信号发生变化

---

## 3. 记录模板

每次追加至少使用下面这些字段：

- 日期：
- 事件类型：
- 触发来源：
- 当前正式依据：
- 当前单一第一阻塞点：
- 当前已接受 checkpoint：
- 当前未接受部分：
- 当前冻结项：
- 本轮新增偏差或纠偏：
- 对实现线程委托的影响：
- 是否需要上报：
- 下一稳定动作：

---

## 4. 当前基线账本（2026-03-27）

### 记录 001

- 日期：2026-03-27
- 事件类型：接班自治基线建立
- 触发来源：父线程对 `导航V2` 独立性问题的治理分析
- 当前正式依据：
  - `006-Sunset专业导航系统需求与架构设计.md`
  - `007-Sunset专业导航底座后续开发路线图.md`
  - `002-导航V2开发宪法与阶段推进纲领.md`
  - `004-导航V2接班准入与自治规约.md`
- 当前单一第一阻塞点：
  - detour owner 还没有稳定接管执行层
- 当前已接受 checkpoint：
  1. `BuildPath / RebuildPath / ActualDestination / 路径后处理` 已开始退壳
  2. `stuck / repath / 恢复入口` 已开始退壳
  3. `detour create / clear / recover` 已开始迁到共享 owner
  4. 玩家侧已拿到首个 detour owner 有效执行窗口
- 当前未接受部分：
  1. `NpcAvoidsPlayer` 的 NPC 侧 runtime 仍未稳定过线
  2. NPC 自己的 `HomeAnchor` 运行中补口链仍是用户可见阻塞
  3. 体验线仍未通过，不能 claim “导航自然”
- 当前冻结项：
  1. `Primary.unity` mixed cleanup
  2. `DialogueChinese*` 字体 cleanup
  3. `TrafficArbiter / MotionCommand / DynamicNavigationAgent` 争论
  4. broad scene hygiene
- 本轮新增偏差或纠偏：
  1. 已确认 `导航V2` 当前仍是交接态，不是自治态
  2. 已确认缺口不再是“再来一篇总架构文档”，而是：
     - 接班准入与自治规约
     - 实时偏差账本
- 对实现线程委托的影响：
  - 暂不直接扩写新的 runtime 委托
  - 应先完成 `导航V2` 自治层的稳定化
- 是否需要上报：
  - 否
- 下一稳定动作：
  - 由 `导航V2` 在后续回执中明确承接自己的自治 owner 身份，并基于本账本继续维护现行状态

### 记录 002

- 日期：2026-03-27
- 事件类型：Gemini 2.0 审核后现行口径纠偏
- 触发来源：`000-gemini锐评-2.0.md -> Path B`
- 当前正式依据：
  - `006`
  - `007`
  - `002 v1.1`
  - `004`
- 当前单一第一阻塞点：
  - 仍为 detour owner 稳定接管执行层失败
- 当前已接受 checkpoint：
  - 同记录 001
- 当前未接受部分：
  - 同记录 001
- 当前冻结项：
  - 同记录 001
- 本轮新增偏差或纠偏：
  1. 纠正了“状态账本等 `P0` 稳住后再补”的过晚口径
  2. 纠正了“完全没有代码级隔离”的绝对化说法
  3. 纠正了“完全没有 Hysteresis 基准”的绝对化说法
  4. 强化了“当前前门式 `P0`”与“未来结构退出指标”必须分层
- 对实现线程委托的影响：
  - 后续实现委托必须同时携带：
    - 当前前门目标
    - 当前实时账本中的第一阻塞点
    - 当前禁止漂移项
- 是否需要上报：
  - 否
- 下一稳定动作：
  - 后续若再出现新锐评、新 checkpoint、新阶段切换，必须继续在本账本追加，而不是只写回执

### 记录 003

- 日期：2026-03-27
- 事件类型：自治验收与 owner 分发裁定
- 触发来源：`006-导航V2自治验收委托-01.md`
- 当前正式依据：
  - `006-Sunset专业导航系统需求与架构设计.md`
  - `007-Sunset专业导航底座后续开发路线图.md`
  - `002-导航V2开发宪法与阶段推进纲领.md`
  - `004-导航V2接班准入与自治规约.md`
  - `005-导航V2偏差账本.md`
  - `导航检查 -> 导航检查V2` 的 `00-06` 七份交接正文
- 当前单一第一阻塞点：
  - NPC 侧 detour release / recover 链仍未给 owner 留出稳定恢复窗口，当前最窄责任点继续锁在：
    - `TryReleaseSharedAvoidanceDetour(... rebuildPath:false)`
    - `-> TryHandleSharedAvoidance() return true`
    - `-> TickMoving()` 当帧直接 `return`
- 当前已接受 checkpoint：
  1. `BuildPath / RebuildPath / ActualDestination / 路径后处理` 已开始退壳
  2. `stuck / repath / 恢复入口` 已开始退壳
  3. `detour create / clear / recover` 已开始迁到共享 owner
  4. 玩家侧已拿到首个 detour owner 有效执行窗口
  5. `TickMoving()` 中 `ClearedOverrideWaypoint -> StopMotion -> linearVelocity = 0` 这段 NPC 专有硬停副作用已被先行排除，不再是当前第一责任点
- 当前未接受部分：
  1. `NpcAvoidsPlayer` 的 NPC 侧 runtime 仍未稳定过线
  2. 当前还不能 claim detour owner 已在 NPC 侧稳定统治执行层
  3. 体验线仍未通过，不能 claim “导航自然”或“P0 已过”
- 当前冻结项：
  1. `Primary.unity` mixed cleanup
  2. `DialogueChinese*` 字体 cleanup
  3. `TrafficArbiter / MotionCommand / DynamicNavigationAgent` 争论
  4. broad scene hygiene
  5. 长窗 live / 多场同轮 / broad cleanup
- 本轮新增偏差或纠偏：
  1. 已确认 `导航V2` 当前身份不是实现线程，而是规范 owner / 审核 owner / 调度 owner / 升级裁定 owner
  2. 已确认本轮未命中 `004` 的 8 类上报阈值，不应继续把判断交还父线程
  3. 已把当前第一阻塞点从“detour owner 没稳定接管执行层”继续压窄到 NPC 侧 release 后恢复窗口未成立
- 对实现线程委托的影响：
  - 本轮应继续分发给 `导航检查V2`
  - 但只能继续锁 NPC 侧 `release / recover` 链上的单一主刀
  - 不得回漂 solver 泛调、`TrafficArbiter / MotionCommand`、broad cleanup 或长窗 live
- 是否需要上报：
  - 否
- 下一稳定动作：
  - 由 `导航V2` 以 owner 身份向 `导航检查V2` 发一份新的窄委托，继续只锁 NPC 侧 release 后的恢复窗口

### 记录 004

- 日期：2026-03-27
- 事件类型：`P0-A / P0-B` 运行态基线实证更新
- 触发来源：高速开发模式下的连续短窗 live 复核
- 当前正式依据：
  - `006-Sunset专业导航系统需求与架构设计.md`
  - `007-Sunset专业导航底座后续开发路线图.md`
  - `002-导航V2开发宪法与阶段推进纲领.md`
  - `004-导航V2接班准入与自治规约.md`
  - `005-导航V2偏差账本.md`
- 当前单一第一阻塞点：
  - `P0` 的双前门已暂时不再是用户可见 blocker；当前剩余更像“如何守住这条基线并选择下一最小切片”，而不是继续把 `NpcAvoidsPlayer / HomeAnchor` 写成未过线
- 当前已接受 checkpoint：
  1. `BuildPath / RebuildPath / ActualDestination / 路径后处理` 已开始退壳
  2. `stuck / repath / 恢复入口` 已开始退壳
  3. `detour create / clear / recover` 已开始迁到共享 owner
  4. 玩家侧已拿到首个 detour owner 有效执行窗口
  5. NPC 侧 `release / recover` 经 post-release recovery window + 路径恢复补口后，已连续拿到 `61458 / 62333 / 63640` 三条 `NpcAvoidsPlayer pass=True`
  6. `001 / 002 / 003` 的 `HomeAnchor` 已在 Play 中回读为非空，且三者都满足 `IsRoaming = true`
- 当前未接受部分：
  1. 当前仍不能 claim “导航自然”或整条体验线已经通过
  2. `Primary.unity` 仍是 mixed hot 面，不能把 runtime 过线误写成 scene 可直接写
  3. 全局 shared root 仍未 clean，不能把当前 slice 的成功误写成全仓收口完成
- 当前冻结项：
  1. `Primary.unity` mixed cleanup
  2. `DialogueChinese*` 字体 cleanup
  3. `TrafficArbiter / MotionCommand / DynamicNavigationAgent` 争论
  4. broad scene hygiene
  5. 借 pass 顺势扩大 live 窗口
- 本轮新增偏差或纠偏：
  1. 纠正了“`NpcAvoidsPlayer` 的 NPC 侧 runtime 仍未稳定过线”这一旧状态描述
  2. 纠正了“`HomeAnchor` 运行中补口链仍是当前用户可见 blocker”这一旧状态描述
  3. 钉死当前更准确的口径是：
     - `P0-A / P0-B` 已有 runtime 基线
     - 但不等于体验线、scene 热区和全局收口一起过线
- 对实现线程委托的影响：
  - `导航检查V2` 如再继续实现，不应再重复把同一条 `NpcAvoidsPlayer` 失败链当成当前事实
  - 更合理的是：
    - 先把现有 pass/non-null 基线记账
    - 再基于新的最小用户可见问题选下一条 slice
    - 或在同链回退时只做回归修复
- 是否需要上报：
  - 否
- 下一稳定动作：
  - 先把 `009`、工作区记忆、线程记忆同步到当前 runtime 事实
  - 保持 `Primary.unity` 只读
  - 后续仅在出现新回退或新高权重现象时，再决定下一条最小导航切片
