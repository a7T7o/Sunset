# 导航V2高速开发执行日志

版本: v0.1  
日期: 2026-03-27  
状态: 进行中  
工作区: `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2`

---

## 1. 用途

这不是新的总设计。  
这是一份只服务于本次“高速开发模式”的执行日志。

它负责持续记三类东西：

1. 当前正在做什么
2. 还有什么没做
3. 哪些东西想测、测了没过、或暂时没测成

原则只有一个：

> 做完一段就看日志；能测就测；不能测就继续做下一段；不要再把待测和待做散落在聊天里。

---

## 2. 当前硬边界

### 当前阶段

- 当前仍在 `P0`
- 当前双阻塞的运行态前门已拿到实证：
  - `P0-A`：`NpcAvoidsPlayer` 已新增 `61458 / 62333 / 63640` 三条 `pass=True` 短 fresh
  - `P0-B`：`HomeAnchor` 已在 Play 中回读到 `001 / 002 / 003` 的 `HomeAnchor != null` 且 `IsRoaming = true`
- 当前仍未放开的边界：
  - 上述 runtime 实证不等于 `Primary.unity` 可写
  - 也不等于体验线已自然过线

### 当前唯一正式依据

1. `006-Sunset专业导航系统需求与架构设计.md`
2. `007-Sunset专业导航底座后续开发路线图.md`
3. `002-导航V2开发宪法与阶段推进纲领.md`
4. `004-导航V2接班准入与自治规约.md`
5. `005-导航V2偏差账本.md`

### 当前禁区

1. 不回漂 `TrafficArbiter / MotionCommand / DynamicNavigationAgent`
2. 不做 solver 泛调
3. 不吞 `Primary.unity` mixed cleanup
4. 不吞 3 个 `DialogueChinese*` 字体
5. 不碰 `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
6. 不把 broad cleanup 升格成主线

---

## 3. 当前主任务队列

### 队列 A：`NpcAvoidsPlayer`

- 当前目标：
  - 把 NPC 侧 detour `release / recover` 链当前已过线的结果固定成后续回归基线
- 当前 owned scope：
  - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
- 当前最近已知 fresh：
  - `61458:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - `62333:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - `63640:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
- 当前回归口径：
  - 若后续再回退，只继续锁 `TickMoving / TryHandleSharedAvoidance / TryReleaseSharedAvoidanceDetour` 这一条链
  - 不把 pass 误写成可以回漂 solver、大架构或 scene 热窗

### 队列 B：`HomeAnchor`

- 当前目标：
  - 把运行态 `HomeAnchor` 非空事实固定成当前基线，而不是继续悬空依赖 Inspector
- 当前最新实证：
  - Play Mode 下 `001 / 002 / 003` 的 `NPCAutoRoamController.HomeAnchor` 都已指向各自 `*_HomeAnchor`
  - 三者同时满足 `IsRoaming = true`
- 当前高相关文件：
  - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
- 当前回归口径：
  - 若后续再出现 runtime 空引用，只继续锁 `NPCAutoRoamController` 的 runtime 绑定链
  - 不把 runtime 非空误写成允许重开 `Primary.unity` scene 施工

---

## 4. 开发流水

### 记录 001

- 时间：2026-03-27
- 动作：
  - 建立高速开发执行日志
  - 重新冻结本轮 `P0-A / P0-B` 双队列
  - 明确这次开发的 own 范围先只锁导航热区代码与本日志/记忆
- 结果：
  - 日志入口已建立
  - 接下来先做 `NpcAvoidsPlayer`，再做 `HomeAnchor`
- 备注：
  - `NPCAutoRoamController.cs` 当前已混有其他增量，后续编辑必须只在目标热区做最小补口，不能误回滚别的 diff

### 记录 002

- 时间：2026-03-27
- 动作：
  - 在 `NpcAvoidsPlayer` 同链上补了 release 稳定窗
  - 给 `NPCAutoRoamController` 暴露 detour create / release / no-blocker 调试计数
  - 给 `NavigationLiveValidationRunner` 补了 `NpcAvoidsPlayer` timeout/pass 的 detour 调试输出
- 结果：
  - 现在 fresh 如果再失败，不会只剩 `npcReached=False`
  - 可以直接看到 detour 是否创建、release 是否命中、无阻挡窗口是否形成
- 备注：
  - 这一步属于“同链可观察性 + 最小行为补口”，没有扩到 solver 或大架构

### 记录 003

- 时间：2026-03-27
- 动作：
  - 在 `NPCAutoRoamController` 增加 runtime `HomeAnchor` 自愈链：
    - 运行时优先找现成同名 anchor
    - 找不到就临时创建并绑定
    - `Awake / StartRoam / DebugMoveTo / GetRoamCenter` 都会补做绑定
- 结果：
  - `HomeAnchor` 不再只能依赖 Inspector 被选中时的 Editor 补口
  - `P0-B` 已从“纯 Editor 链”前推到 runtime 自愈链
- 备注：
  - 这一步没有碰 `Primary.unity`
  - 也没有把 scene cleanup 扩成主线

### 记录 004

- 时间：2026-03-27
- 动作：
  - 顺手解掉会直接拦 live 的外部测试装配 blocker
  - 将 `NPCToolchainRegularizationTests.cs` 对 `NPCRoamProfile / NPCDialogueContentProfile` 的强类型直连改成反射/`UnityEngine.Object` 级读取
  - 再次触发 `NpcAvoidsPlayer`
- 结果：
  - 外部 `error CS0246` 已不再出现
  - Unity 重新编译成功：`*** Tundra build success (2.70 seconds), 13 items updated, 862 evaluated`
  - 但这次仍只走到：
    - `queued_action=SetupNpcAvoidsPlayer entering_play_mode`
  - 后续 `runtime_launch_request / scenario_start / scenario_end` 仍未出现
- 备注：
  - 当前验证面已经从“测试装配编译炸”进一步收缩成“菜单触发后 runtime handoff 没真正接上”

### 记录 005

- 时间：2026-03-27
- 动作：
  - 给 `NavigationLiveValidationMenu.cs` 增加 editor 侧 handoff 兜底：
    - `InitializeOnLoad` 静态构造里注册 `playModeStateChanged`
    - 编译 / domain reload 后用 `delayCall` 检查挂起中的 pending action
    - 若重载把原始 play 请求冲掉，则自动重新进入 Play
    - 进入 Play 后再补一次 `editor_dispatch_pending_action`
- 结果：
  - `queued_action -> play mode -> runtime/runner` 这条链不再只押注 `RuntimeInitializeOnLoadMethod`
  - 后续即使被 forced recompile / domain reload 打断，也有 editor 侧续接机会
- 备注：
  - 这一步仍然只是在修 `NpcAvoidsPlayer` 的 live handoff
  - 没有回漂到 solver / scene / `Primary.unity`

### 记录 006

- 时间：2026-03-27
- 动作：
  - 继续压缩 `NpcAvoidsPlayer` 的取证链
  - 把 `NavigationLiveValidationRunner` 的 `scenario_end=NpcAvoidsPlayer` 摘要改成直接携带：
    - `detourActive`
    - `detourCreates`
    - `releaseAttempts`
    - `releaseSuccesses`
    - `noBlockerFrames / blockingFrames`
    - `recoveryOk`
- 结果：
  - 下次 fresh 不必再只看 `npcReached=False`
  - 也不必翻 `all_completed` 细项，单看 `scenario_end` 就能判断 detour release/recover 是否真的动起来
- 备注：
  - 这是同一条 `NpcAvoidsPlayer` 观察链的证据提纯
  - 没有新增业务判定阈值

### 记录 007

- 时间：2026-03-27
- 动作：
  - 在 Unity 实例恢复后重新执行 1 条 `NpcAvoidsPlayer` fresh
  - 真实拿到：
    - `queued_action=SetupNpcAvoidsPlayer entering_play_mode`
    - `runtime_launch_request=SetupNpcAvoidsPlayer`
    - `scenario_start=NpcAvoidsPlayer`
    - `scenario_end=NpcAvoidsPlayer ...`
  - 取证后已主动把 Unity 退回 Edit Mode
- 结果：
  - editor -> runtime handoff 这条链已确认接上
  - 当前最新 fresh 不是“没跑起来”，而是“跑起来了但仍失败”
  - 最新 fresh：
    - `scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.832 npcReached=False detourActive=True detourCreates=11 releaseAttempts=164 releaseSuccesses=10 noBlockerFrames=6 blockingFrames=0 recoveryOk=False`
- 备注：
  - 这条 fresh 很关键，说明下一刀不该再修 handoff
  - 应回到 detour `release / recover` 本身

### 记录 008

- 时间：2026-03-27
- 动作：
  - 继续只锁 `NPCAutoRoamController.cs` 的同链补口
  - 新增极短 post-release recovery window
  - `ClearedOverrideWaypoint` 后不再当帧硬 `return`，而是重新评估 waypoint
  - detour release 成功时记录 release 时间、清理计数，并按当前 detour 后位置重建主路径恢复线
- 结果：
  - failure 形态继续变化，不再停在旧的高频 churn：
    - `59710:[NavValidation] scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.558 npcReached=False detourActive=True detourCreates=8 releaseAttempts=119 releaseSuccesses=7 noBlockerFrames=9 blockingFrames=0 recoveryOk=False`
  - 说明 post-release 这条链已经开始改变 runtime 结果，而不是继续原地空转
- 备注：
  - 这一步仍只在同一条 detour `release / recover` slice 内推进
  - 没有扩到 solver、scene 或 `Primary.unity`

### 记录 009

- 时间：2026-03-27
- 动作：
  - 在第二刀补口后连续执行 2 条短 fresh `NpcAvoidsPlayer`
  - 每次拿到 `scenario_end` 后立即 stop，并退回 Edit Mode
- 结果：
  - 连续 2 条 pass：
    - `61458:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
    - `62333:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - 这说明当前 slice 已从“继续修”进入“稳定性确认”
- 备注：
  - 当前 pass 不是 scene 假阳性
  - `heartbeat` 显示 NPC 实际从玩家下侧绕开并推进到目标

### 记录 010

- 时间：2026-03-27
- 动作：
  - 再补 1 条短 fresh `NpcAvoidsPlayer`
  - 随后开一段最短 Play，只读回查 `001 / 002 / 003` 的 `NPCAutoRoamController`
- 结果：
  - 第 3 条 fresh 继续 pass：
    - `63640:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - `HomeAnchor` 运行态实证成立：
    - `001 -> HomeAnchor=001_HomeAnchor / IsRoaming=true`
    - `002 -> HomeAnchor=002_HomeAnchor / IsRoaming=true`
    - `003 -> HomeAnchor=003_HomeAnchor / IsRoaming=true`
  - 控制台未出现新的 `HomeAnchor` 相关报错
- 备注：
  - 到这一步为止，`P0-A / P0-B` 两条前门都已有 runtime 证据
  - 但 `Primary.unity` 仍然保持 mixed hot 面只读口径

---

## 5. 待测队列

### 待测 001

- 项目：`NpcAvoidsPlayer`
- 目标：
  - 在后续再碰同链代码时，用 1 条短 fresh 复核当前 pass 基线没有回退
- 期望信号：
  - `scenario_end=NpcAvoidsPlayer pass=True`
  - `recoveryOk=True`
- 当前状态：
  - 已由 `61458 / 62333 / 63640` 连续三条 pass 暂时移出活跃队列

### 待测 002

- 项目：`HomeAnchor`
- 目标：
  - 若用户或其他线程再次报告 runtime 丢 anchor，再用最短 Play 复查三只 NPC
- 期望信号：
  - `001 / 002 / 003` 的 `HomeAnchor != null`
  - `IsRoaming = true`
- 当前状态：
  - 已在本轮 Play 回读通过，暂时移出活跃队列

### 待测 003

- 项目：下一条导航切片
- 目标：
  - 在不回漂禁区的前提下，选择新的最小用户可见问题继续推进
- 期望信号：
  - 新切片不重开 `Primary.unity`、solver 泛调或大架构争论
- 当前状态：
  - 仍待根据最新用户反馈或更高权重现象决定

---

## 6. 已测结果

### 已测 001

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - `34741:[NavValidation] scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.826 npcReached=False`
- 判定：
  - baseline，作为本轮继续下刀前对照

### 已测 002

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - `35610:[NavValidation] scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.832 npcReached=False`
- 判定：
  - `ClearedOverrideWaypoint` 后置 recover 补口未翻转结果
  - 当前责任点继续前移到 release 窗口本身

### 已测 003

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - 已成功向 Unity 发送 `SetupNpcAvoidsPlayer` 菜单命令
  - `Editor.log` 只走到 `queued_action=SetupNpcAvoidsPlayer entering_play_mode`
  - 随后被 `Assets/YYY_Tests/Editor/NPCToolchainRegularizationTests.cs` 的 `NPCRoamProfile / NPCDialogueContentProfile` 缺失编译错误打断
- 判定：
  - 当前 fresh 失败不是我这条 detour 链直接编译炸了
  - 是外部测试装配 blocker 抢先截断了 live

### 已测 004

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - 已清掉外部测试装配 `error CS0246`
  - 再次触发后新增：
    - `*** Tundra build success (2.70 seconds), 13 items updated, 862 evaluated`
  - 但仍未拿到：
    - `runtime_launch_request=SetupNpcAvoidsPlayer`
    - `scenario_start=NpcAvoidsPlayer`
    - `scenario_end=NpcAvoidsPlayer`
- 判定：
  - live blocker 已继续压窄
  - 当前不是测试装配找不到类型，而是 `entering_play_mode -> scenario runtime` 的接力仍未真正发生

### 已测 005

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - `46988:[NavValidation] scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.832 npcReached=False detourActive=True detourCreates=11 releaseAttempts=164 releaseSuccesses=10 noBlockerFrames=6 blockingFrames=0 recoveryOk=False`
- 判定：
  - 这次 fresh 已证明 handoff 恢复完成
  - 当前真实阻塞重新收缩为：
    - detour 确实被反复创建 / release
    - `noBlockerFrames=6` 已形成
    - 但 `recoveryOk=False`
    - NPC 最终仍未恢复到有效前进

### 已测 006

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - `59710:[NavValidation] scenario_end=NpcAvoidsPlayer pass=False timeout=6.51 minClearance=0.558 npcReached=False detourActive=True detourCreates=8 releaseAttempts=119 releaseSuccesses=7 noBlockerFrames=9 blockingFrames=0 recoveryOk=False`
- 判定：
  - post-release recovery window 已经改变 failure 形态
  - 说明同链补口开始产生实效，而不是继续停在原地

### 已测 007

- 项目：`NpcAvoidsPlayer`
- 时间：2026-03-27
- 结果：
  - `61458:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - `62333:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
- 判定：
  - 当前 `NpcAvoidsPlayer` 已从“仍失败”翻转成连续通过
  - 这两条 short fresh 足以作为当前 slice 的第一层稳定性基线

### 已测 008

- 项目：`NpcAvoidsPlayer + HomeAnchor`
- 时间：2026-03-27
- 结果：
  - `63640:[NavValidation] scenario_end=NpcAvoidsPlayer pass=True minClearance=0.858 npcReached=True detourActive=False detourCreates=0 releaseAttempts=0 releaseSuccesses=0 noBlockerFrames=0 blockingFrames=0 recoveryOk=True`
  - Play Mode 回读：
    - `001 -> HomeAnchor=001_HomeAnchor / IsRoaming=true`
    - `002 -> HomeAnchor=002_HomeAnchor / IsRoaming=true`
    - `003 -> HomeAnchor=003_HomeAnchor / IsRoaming=true`
  - `read_console(filter=HomeAnchor)` 返回 `0` 条
- 判定：
  - `P0-A` 已有第 3 条 pass 证明不是一次性偶然
  - `P0-B` 的 runtime 非空基线也已经拿到

---

## 7. 阻塞与暂停项

### 阻塞 001

- 项目：全局收口
- 类型：shared root 存在大量 unrelated dirty
- 说明：
  - `P0-A / P0-B` 当前都已有 runtime 证据
  - 但本轮仍不能直接 claim 全局 clean 或安全 sync done
  - 原因是 shared root 还混有大量非本刀 dirty / hot 面 dirty

### 阻塞 002

- 项目：warnings 观察
- 类型：warning-only / non-owned
- 说明：
  - 当前 `validate_script` 仅剩 warning：
    - `NPCAutoRoamController.cs` 的字符串拼接 GC 提示
    - `NavigationLiveValidationRunner.cs` 的 `FixedUpdate`/字符串拼接提示
  - Unity Console 当前只见：
    - `There are no audio listeners in the scene`
    - `Some objects were not cleaned up when closing the scene`
  - 这些当前都不是我本刀新增的 blocking error，但后续仍要区分清楚，不得混说成“项目已炸”

### 已测 009

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.009 blockOnsetEdgeDistance=1.114 playerReached=True`
  - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.009, blockOnsetEdgeDistance=1.114, maxPlayerLateralOffset=2.180, timeout=2.00`
- 判定：
  - 这是下一条真实用户可见切片的 baseline
  - 当前失败不是 `NpcAvoidsPlayer` 旧链，也不是 `HomeAnchor` 空引用
  - 新问题收缩为：玩家真实点击近身单 NPC 时，过早大绕行

### 已测 010

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.011 blockOnsetEdgeDistance=0.954 playerReached=True`
  - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.011, blockOnsetEdgeDistance=0.954, maxPlayerLateralOffset=1.723, timeout=1.67`
  - heartbeat 证据：
    - `npcAState=Inactive`
    - `pathCount=1 waypoint=(-5.80, 4.45)`
    - 首轮说明 blocker 实际是 `passive / sleeping NPC`
- 判定：
  - 责任点纠偏成功：不是 active stationary NPC，而是 passive NPC blocker
  - 第一刀压掉了一部分过大侧绕，但还远未达标

### 已测 011

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.009 blockOnsetEdgeDistance=0.737 playerReached=True`
  - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.009, blockOnsetEdgeDistance=0.737, maxPlayerLateralOffset=1.167, timeout=2.03`
  - heartbeat 证据：
    - `pathCount=1 pathIndex=0 waypoint=(-5.80, 4.45)`
    - `detour=False detourOwner=0`
- 判定：
  - 当前路径本身是直的
  - detour 也已被压掉
  - 剩余主因收缩为：`NavigationLocalAvoidanceSolver` 对 passive NPC 的侧推仍过早

### 已测 012

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - `scenario_end=RealInputPlayerSingleNpcNear pass=False timeout=6.51 minEdgeClearance=0.260 blockOnsetEdgeDistance=1.114 playerReached=False`
  - `details=playerClickIssued=True, playerReached=False, minEdgeClearance=0.260, blockOnsetEdgeDistance=1.114, maxPlayerLateralOffset=0.054, timeout=6.51`
  - heartbeat 证据：
    - `detour=False`
    - `playerActive=False`
    - `pathCount=0`
- 判定：
  - 这一刀把 failure 形态翻成了“几乎不绕，但直接停住/取消”
  - 说明 passive NPC 的 detour / 推进闸门已经被压得过严
  - 当前 checkpoint 已不再是原来的鬼畜大绕行

### 阻塞 003

- 项目：`RealInputPlayerSingleNpcNear`
- 类型：同链微调未闭环
- 说明：
  - 当前 slice 已明确不该再回漂：
    - `NpcAvoidsPlayer`
    - `HomeAnchor`
    - `Primary.unity`
    - solver 大架构空谈
  - 下一刀应继续只锁：
    - `PlayerAutoNavigator`
    - `NavigationLocalAvoidanceSolver`
    - `NavigationLiveValidationRunner`
  - 目标不是再追“更不绕”，而是从当前 `timeout / playerActive=false / pathCount=0` 恢复到“近身后仍能轻微有效推进”

### 记录 011

- 时间：2026-03-27
- 动作：
  - 清理继续阻断导航验证的同源编译 blocker
  - `PlayerNpcNearbyFeedbackService.cs` 去掉对不存在 `PlayerNpcChatSessionService` 的直接引用
  - `NPCInformalChatInteractable.cs` 改为用反射桥接会话服务，避免 `Sunset.Story` 命名空间继续把整仓编译拖红
- 结果：
  - Unity 脚本刷新后 Console 已回到 `0 error`
  - 当前只剩 `There are no audio listeners in the scene` warning
- 备注：
  - 这是为导航 fresh 清障，不是主线切去修剧情系统

### 已测 013

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.001 blockOnsetEdgeDistance=0.594 playerReached=True`
  - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.001, blockOnsetEdgeDistance=0.594, maxPlayerLateralOffset=0.806, timeout=4.53`
  - heartbeat / NavAvoid 证据：
    - `pathCount=1`
    - `detour=False`
    - `closeApplied=True`
    - close-constraint 首次稳定介入已压到 `blockingDistance≈0.77`
    - `moveScale` 先掉到 `0.42`，随后贴近期长期维持 `0.18`
- 判定：
  - 当前 best-known stable checkpoint 已从 `0.885` 继续压到 `0.594`
  - `playerReached=True` 仍保持成立
  - 剩余主锅继续锁死在 passive NPC 的 close-constraint / 软避让 onset 太早，而不是 handoff、compile、path 或 detour owner

### 已测 014

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - 试过把 passive NPC 的 solver 软避让窗口进一步收窄，并同时减小 measured radius cap
  - fresh 直接回退为：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False timeout=6.50 minEdgeClearance=0.275 blockOnsetEdgeDistance=1.114 playerReached=False`
    - heartbeat 出现：
      - `action=HardStop`
      - `moveScale=0.00`
      - `closeHard=True`
      - `playerActive=False`
      - `pathCount=0`
- 判定：
  - “同时缩 solver + 缩 radius”会把系统重新推回 `HardStop + stuck cancel`
  - 这条实验结果已判废，不保留在最终代码上

### 已测 015

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - 继续把 passive NPC measured radius bias 压成负值后，再次出现：
    - 玩家几乎不起步
    - `playerActive=False`
    - `pathCount=0`
    - `blockOnsetEdgeDistance=1.114`
    - `maxPlayerLateralOffset=0.000`
- 判定：
  - 纯几何 bias 再往负方向推会把 slice 重新打回“起步即取消”
  - 当前代码已回退到更稳的 `PASSIVE_NPC_CLOSE_CONSTRAINT_EXTRA_RADIUS = 0.05f`，不继续保留负 bias 试验

### 阻塞 004

- 项目：`RealInputPlayerSingleNpcNear`
- 类型：参数试探已进入振荡区
- 说明：
  - 当前已验证：
    - `0.594 + playerReached=True` 是目前最稳的玩家侧 checkpoint
    - 更激进的 solver/radius 收缩会退回 `HardStop` 或“起步即取消”
  - 因此下一刀不应继续只拧同一组几何参数
  - 更合理的后续方向应改为：
    - 只审 passive NPC close-constraint 与 stuck cancel 的衔接条件
    - 或只审“何时允许继续 PathMove 而不是立刻掉进 cancel”

### 记录 012

- 时间：2026-03-27
- 动作：
  - 在 `PlayerAutoNavigator.cs` 撤掉 passive NPC close-constraint 命中时的 `ResetProgress(...)` 回退实验
  - 将 `PASSIVE_NPC_CLOSE_CONSTRAINT_EXTRA_RADIUS` 明确收回到 `0.05f`
  - 在 `NavigationLiveValidationRunner.cs` heartbeat 里新增 `closeForward`
- 结果：
  - 玩家侧代码已从“已知更差实验态”回正到更接近 `0.594` checkpoint 的形态
  - 后续 fresh 不再只能看到 `closeApplied / closeClearance`，还能直接看到 close-constraint 接管时的前冲角度
- 备注：
  - 这一步不是继续泛调几何参数，而是先撤掉已知回退补丁并补齐证据

### 已测 016

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - fresh 再次拿到：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.005 blockOnsetEdgeDistance=0.594 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.005, blockOnsetEdgeDistance=0.594, maxPlayerLateralOffset=0.820, timeout=4.53`
  - 新增 heartbeat / NavAvoid 证据：
    - `closeForward` 在 close-constraint 期间从约 `0.55` 持续降到 `0.09`
    - 但 `moveScale` 基本一直被压在 `0.18`
    - `detour=False`
    - `pathCount=1`
- 判定：
  - 当前 `0.594` best-known checkpoint 没有回退
  - 第一责任点继续锁死在“close-constraint 接手后压速过久”，而不是 detour、path 或 compile

### 记录 013

- 时间：2026-03-27
- 动作：
  - 在 `PlayerAutoNavigator.cs` 增加 passive NPC close-constraint 的自适应恢复速度下限
  - 规则改为：
    - 刚正面顶上 blocker 时，仍允许低速贴边
    - 当 `closeForward` 下降、说明玩家已经开始绕过 blocker 后，`moveScale` 可从固定 `0.18` 逐步恢复
- 结果：
  - 这刀继续只打 `close-constraint -> PathMove` 衔接，不回漂 solver 泛调
- 备注：
  - 当前只完成代码前推，仍需下一条有效 fresh 证明 `blockOnsetEdgeDistance` 是否继续下降

### 阻塞 005

- 项目：`RealInputPlayerSingleNpcNear`
- 类型：MCP / live transport 抖动
- 说明：
  - 在自适应速度下限补口后尝试再拿 1 条 fresh
  - 这次没有拿到有效 `scenario_end`
  - Console 仅出现：
    - `WebSocket is not initialised`
    - `runner_disabled`
    - 若干 `OcclusionTransparency ... 未找到OcclusionManager（等待超时）`
  - 这轮应判定为 live 窗口抖动，不足以外推成导航逻辑回退

### 记录 014

- 时间：2026-03-27
- 动作：
  - 根据用户新反馈，把“多人经过时乱跑、慢卡、后面没人了还像被拖住”的 crowd 症状正式拉进 live 主线
  - 在 `PlayerAutoNavigator.cs` 增加 crowd pressure 旁路：
    - 当玩家周围存在 `>= 2` 个近距离 passive NPC blocker 时，不再傻等 `24` 帧 sighting 才允许切 detour
  - 把 `NavigationLiveValidationRunner.cs` 的玩家侧 heartbeat 扩到 `RealInputPlayerCrowdPass`
- 结果：
  - 现在 crowd 场景也能看到：
    - `pathCount / pathIndex / waypoint`
    - `detour / detourOwner`
    - `blocker / blockerSightings`
    - `moveScale / closeForward`
- 备注：
  - 这一步不是回漂大架构，而是直接针对用户说的 crowd 卡死 bug 做玩家侧 detour 触发补口

### 已测 017

- 项目：`RealInputPlayerCrowdPass`
- 时间：2026-03-27
- 结果：
  - baseline failure：
    - `scenario_end=RealInputPlayerCrowdPass pass=False minEdgeClearance=-0.005 directionFlips=0 crowdStallDuration=4.900 playerReached=False`
    - 运行态表现为：
      - `shouldRepath=True` 持续很久
      - `detour=False`
      - 最终落回 `HardStop + stuck cancel`
- 判定：
  - 这条样本与用户描述的“乱跑、慢卡、后面没人了还像被卡住”高度一致
  - 第一责任点钉死为：多人 passive NPC pressure 下 detour 触发过晚

### 已测 018

- 项目：`RealInputPlayerCrowdPass`
- 时间：2026-03-27
- 结果：
  - crowd pressure 旁路补口后 fresh：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.008 directionFlips=1 crowdStallDuration=0.356 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.008, directionFlips=1, crowdStallDuration=0.356, timeout=3.20`
  - heartbeat 关键证据：
    - 早期即出现 `detour=True detourOwner=-5942`
    - 后续重新并回主路径后 `moveScale` 恢复到 `1.00`
- 判定：
  - 用户反馈的 crowd 慢卡 / 乱跑主症状已经在当前验证切片内翻成 pass
  - 这条 crowd bug 当前应视为已有一条 fresh 过线，而不是“仍只停留在口头分析”

### 已测 019

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - 在 crowd 补口之后，single NPC kept 版本 fresh：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.005 blockOnsetEdgeDistance=0.594 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.005, blockOnsetEdgeDistance=0.594, maxPlayerLateralOffset=0.695, timeout=2.98`
- 判定：
  - crowd 补口没有把 single NPC 再打回 `playerReached=False`
  - 但 single NPC 仍未过线，当前 kept 版本还是约 `0.594`

### 阻塞 006

- 项目：后续 live
- 类型：外部线程污染
- 说明：
  - 本轮后段 live 窗口出现：
    - `NPCValidation` 相关对话 trace
    - `ManageGameObject move_relative failed: This cannot be used during play mode`
  - 这说明 Unity live 不再只属于导航验证
  - 因此本轮后续没有继续硬拿更多 fresh，而是把 speculative tweak 回撤，只保留已验证版本

### 记录 015

- 时间：2026-03-27
- 动作：
  - 继续只锁 `PlayerAutoNavigator.cs`
  - 新增 crowd stale-owner 补口：
    - 当前 `blocking agent` 已切换、旧 `detour owner` 不再是当前 blocker 时，允许主动释放旧 detour
  - 收紧 crowd pressure 计数：
    - `CountNearbyPassiveNpcBlockers(...)` 不再按纯半径数人
    - 改为只统计前方通道内的 passive NPC blocker，排除侧后方 crowd 持续维持 repath 压力
- 结果：
  - 本轮没有回漂 solver / scene / hot file
  - 仍保持在玩家侧 detour owner / crowd pressure 同链
- 备注：
  - 这两刀都是直接针对用户说的“后面没人了还像被拖住”“人一多就容易持续乱绕”做的恢复补口

### 已测 020

- 项目：`RealInputPlayerCrowdPass`
- 时间：2026-03-27
- 结果：
  - stale-owner / 前方通道计数补口后 fresh：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.005 directionFlips=2 crowdStallDuration=0.449 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.005, directionFlips=2, crowdStallDuration=0.449, timeout=2.74`
  - heartbeat 关键证据：
    - 起步仍会短暂进入 `detour=True detourOwner=-5942`
    - 很快回到 `detour=False`
    - 后续 `blocker=-6584` 仍存在时，`avoidRepath=False / moveScale=1.00`
- 判定：
  - 当前 crowd 线继续保持 pass
  - 新补口至少没有把 crowd 再打回“长时间慢卡 + 远侧绕”

### 已测 021

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - 本轮 crowd 补口后的 single watch 样本：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.011 blockOnsetEdgeDistance=0.737 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.011, blockOnsetEdgeDistance=0.737, maxPlayerLateralOffset=0.832, timeout=4.42`
  - heartbeat 同步显示：
    - 全程 `detour=False detourOwner=0`
    - 仍是 passive NPC close-constraint 慢压速链
- 判定：
  - 这条样本比当前 best-known stable checkpoint `0.594` 更差
  - 但由于本轮新增代码没有命中 single 的 `detour` 分支，这条结果先按 watch 记账，不覆盖现有 kept baseline

### 记录 016

- 时间：2026-03-27
- 动作：
  - 继续只锁 `PlayerAutoNavigator.cs`
  - 回读 `Editor.log` 中 `RealInputPlayerSingleNpcNear` 的 `0.404 / 0.594 / 0.737 / 0.665` 样本
  - 将玩家侧两处 detour release 分支从“`ForceImmediateMovementStop() + return true`”改成“detour 清掉后同帧继续回到主路径评估”
- 结果：
  - 当前没有继续回漂 solver、close floor 或 forward bias
  - 新补口继续只打玩家侧 detour `release / recover` 的恢复停帧
  - 静态闸门通过：
    - `git diff --check -- PlayerAutoNavigator.cs`
    - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
- 备注：
  - 本轮未拿到新的 single fresh，不是因为这刀代码红了，而是 live 前置核查时发现 Console 已被外部 `SpringDay1LateDayRuntimeTests.cs` 编译错误污染
  - 因此这轮不覆盖当前 kept baseline，仍保留：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.005 blockOnsetEdgeDistance=0.594 playerReached=True`

### 阻塞 007

- 项目：`RealInputPlayerSingleNpcNear`
- 类型：external compile blocker
- 说明：
  - 按 live 基线先执行：
    - `stop`
    - `refresh_unity`
    - `read_console(error)`
  - 结果发现外部编译错误：
    - `Assets\\YYY_Tests\\Editor\\SpringDay1LateDayRuntimeTests.cs` 多条 `CS0246`
  - 按当前导航线程口径，这属于 external blocker，不是我这轮 `PlayerAutoNavigator.cs` 新引入的 owned red
  - 在 Console 非 `0 error` 状态下，这轮 single fresh 不再继续执行，避免把无效 live 样本记成导航结论

### 已测 022

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - 清空 Console 并重新核对后，之前那组 `SpringDay1LateDayRuntimeTests.cs` 编译错误没有再次出现
  - 当前 live 前置复核结果：
    - `stop -> Edit Mode`
    - `read_console(error) => 0 compile error`
  - 说明上一轮 external compile blocker 当前更像 stale console residue，而不是持续性真实 blocker
- 判定：
  - single fresh 可继续执行
  - 当前不需要接手 `SpringDay1LateDayRuntimeTests.cs` 的 untracked 文件

### 已测 023

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - first fresh after patch：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.011 blockOnsetEdgeDistance=0.236 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.011, blockOnsetEdgeDistance=0.236, maxPlayerLateralOffset=0.838, timeout=4.54`
  - heartbeat 关键证据：
    - 中后段开始稳定出现 `pathCount=2 pathIndex=1`
    - blocker 清空后回到 `moveScale=1.00`
    - `avoidRepath=False`
- 判定：
  - 玩家 detour release 同帧恢复补口直接把 kept baseline 从 `0.594` 压到了 `0.236`
  - 当前 single 主锅已不再是“release 后硬停一帧无影响”

### 已测 024

- 项目：`RealInputPlayerSingleNpcNear`
- 时间：2026-03-27
- 结果：
  - second fresh after patch：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.002 blockOnsetEdgeDistance=0.120 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.002, blockOnsetEdgeDistance=0.120, maxPlayerLateralOffset=0.824, timeout=4.83`
  - heartbeat 关键证据：
    - `pathCount=2 pathIndex=1`
    - `closeApplied=True` 期间维持推进
    - blocker 消失后 `moveScale=1.00`
- 判定：
  - `RealInputPlayerSingleNpcNear` 当前已拿到补口后的首条明确 pass
  - 当前 best-known baseline 已从 `0.594` 更新为：
    - `pass=True`
    - `blockOnsetEdgeDistance=0.120`
    - `playerReached=True`

### 已测 025

- 项目：`RealInputPlayerCrowdPass`
- 时间：2026-03-27
- 结果：
  - 第一次 watch 仅出现：
    - `runner_disabled`
  - 最小 retry 后拿到有效样本：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.015 directionFlips=2 crowdStallDuration=0.309 playerReached=True`
    - `details=playerClickIssued=True, playerReached=True, minEdgeClearance=-0.015, directionFlips=2, crowdStallDuration=0.309, timeout=2.83`
- 判定：
  - 当前这刀 detour release 恢复补口没有把 crowd watch 打坏
  - crowd 继续维持 pass，可保持回归 watch

### 记录 026

- 时间：2026-03-27
- 动作：
  - 沿 `RealInputPlayerSingleNpcNear` 继续只锁 `PlayerAutoNavigator.cs`
  - 回读多轮 `Editor.log` / live heartbeat，定位当前 single 失败不再只是“参数不好”，而是 close-constraint / blocked-input / stuck 检测三者在同一条链上互相打架
- 关键证据：
  - 在 `BlockedInput` 窗口内，出现：
    - `playerActive=False`
    - `pathCount=0`
    - `action=Inactive`
  - 普通日志同步给出：
    - `检测到卡顿（1/3）`
    - `检测到卡顿（2/3）`
    - `卡顿 3 次，取消导航`
- 判定：
  - 当前 single 的第一责任点已从“继续盲拧 moveScale / blocked-input 阈值”收窄为：
    - `CheckAndHandleStuck()` 把短距 NPC 避让误判成真实卡死
  - 这轮中途出现过多条无效或更差样本：
    - `0.287`
    - `0.260`
    - `0.885`
    - `timeout + playerReached=False`
  - 它们都已只作为排错证据记账，不作为当前 kept baseline

### 记录 027

- 时间：2026-03-27
- 动作：
  - 在 `PlayerAutoNavigator.cs` 上继续做 single close-constraint 同链最小收口：
    - 将 `stuck` 免疫前移到近距 blocker / 低速避让窗口
    - 将 “NPC blocker 识别” 与 “静止 NPC blocker 识别” 拆开
    - move floor / measured clearance 改为优先吃 avoidance 当前帧 blocker 数据
    - 将“轻微负 overlap 但未 hard block”的灰区纳入 soft-overlap 放松
    - passive close-constraint 最低 move floor 抬到 `0.22`
- 静态闸门：
  - `git diff --check -- PlayerAutoNavigator.cs`
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
- 结果：
  - 迭代中曾拿到 watch：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.010 blockOnsetEdgeDistance=0.232 playerReached=True`
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.010 blockOnsetEdgeDistance=0.264 playerReached=True`
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.009 blockOnsetEdgeDistance=0.353 playerReached=True`
  - 但最终连续 fresh 已重新进入 pass 区间：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.011 blockOnsetEdgeDistance=0.100 playerReached=True`
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.012 blockOnsetEdgeDistance=0.170 playerReached=True`
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.012 blockOnsetEdgeDistance=0.170 playerReached=True`
  - 同轮 crowd watch：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.012 directionFlips=1 crowdStallDuration=0.192 playerReached=True`
- 判定：
  - 当前 single 切片已从“会被 stuck 自杀 / timeout / 长时间慢爬”推进到：
    - 最新连续两条 single pass
    - crowd watch 继续 pass
  - 当前最新 best-known single baseline 更新为：
    - `pass=True`
    - `blockOnsetEdgeDistance=0.100`
    - `playerReached=True`
  - 当前仍需继续 watch 的不是 detour release 主责任点，而是：
    - 轻微负 clearance 灰区
    - `0.23 ~ 0.35` 这类剩余波动样本是否还会复发

### 记录 028

- 时间：2026-03-28
- 动作：
  - 按用户新口径暂停 Unity 验收，先只做代码层基础收口；
  - 继续只锁：
    - `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`
    - `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`
  - 对玩家侧 close-constraint / blocked-input / stuck 同链补了三类纯代码修正：
    - passive NPC 专属阈值只再对 `passive blocker` 生效，不再误挂到任意 NPC blocker；
    - `RecoverPath / RebuildPath / ReleaseDetour / SwitchDetourOwner / CreateDetour` 这些提前返回分支统一重置 avoidance debug 状态，避免上一段 blocker 状态残留到下一帧；
    - live runner heartbeat 补出：
      - `blockDist`
      - `npcBlocker`
      - `passiveNpcBlocker`
- 静态闸门：
  - `git diff --check -- Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
  - `validate_script(NavigationLiveValidationRunner.cs) => errors=0 warnings=2`
- 结果：
  - 当前“passive NPC 专属逻辑”和“任意 NPC blocker 逻辑”的边界已更一致；
  - crowd 多 NPC 下那类“人已经散开了但还像被旧 blocker 状态拖住”的代码层风险已进一步收窄到：
    - stale debug state
    - passive / non-passive 条件混挂
  - 本记录未执行新的 Unity / live / fresh；
    - 这是按用户要求把验收统一推迟到代码层基础收口之后。
- 备注：
  - 目前 kept baseline 仍沿用上一轮已验证通过的事实：
    - single latest best-known：`0.100 pass`
    - crowd latest watch：`0.192 pass`
  - 下一次真正进入 Unity 集中跑时，优先顺序仍是：
    - `RealInputPlayerSingleNpcNear`
    - `RealInputPlayerCrowdPass`

### 记录 029

- 时间：2026-03-28
- 动作：
  - 继续只在 `PlayerAutoNavigator.cs` 上补 crowd / single 共用的 detour release 执行链；
  - 将 `TryReleaseDynamicDetourForSwitchedBlocker(...)` 命中后的控制流，从：
    - 释放旧 owner 后继续同帧往 repath / create-detour 分支下坠
    - 改成：
    - 释放旧 owner 后当帧直接回到主路径 / close-constraint 评估；
  - 同时给 `SwitchDetourOwner` 释放链补 1 个最小恢复冷却：
    - `_lastDynamicObstacleRepathTime = Time.time`
- 静态闸门：
  - `git diff --check -- Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
- 结果：
  - 当前 switched-owner 场景下，不会再出现：
    - 刚释放旧 detour，当前帧又立刻重开新 detour
  - 下一帧也会先经历一个最小 repath cooldown，而不是马上又被新 blocker 拉回 detour churn
- 判定：
  - 这一步仍属于“代码层基础减震”，不是新导航策略；
  - 它直接对应用户描述的 crowd 尾症状之一：
    - blocker 在人群里频繁切换时的乱窜 / 被拖远 / 明明前方松了却还像被旧 detour 拖着

### 记录 030

- 时间：2026-03-28
- 动作：
  - 用户批准恢复 Unity / MCP live 后，按最小验证环执行：
    - 先过 MCP live 基线
    - 再做 `single -> crowd` 的短窗取证
    - 每次拿到 `scenario_end` 后立即 `Stop` 回 Edit Mode
- 现场核查：
  - `check-unity-mcp-baseline.ps1 => baseline_status: pass`
  - `unityMCP` resources / templates 正常暴露
  - 实例确认：
    - `Sunset@21935cd3ad733705`
  - 首轮进入前，Console 曾出现：
    - `The referenced script (Unknown) on this Behaviour is missing!`
  - 但清空 Console 并回到 Edit Mode 后未再复现；
    - 本轮按 stale residue 处理，不当作持续 blocker
  - `editor_state` 全程仍带 `stale_status`
    - 但实际 MCP 工具调用、Play / Stop、Console 读取都正常
    - 当前按 MCP 状态摘要滞后处理，不判项目红错
- live 结果：
  - single fresh：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.013 blockOnsetEdgeDistance=0.169 playerReached=True`
  - crowd fresh #1：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.006 directionFlips=1 crowdStallDuration=0.154 playerReached=True`
  - crowd fresh #2：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.005 directionFlips=1 crowdStallDuration=0.154 playerReached=True`
- 判定：
  - 当前 `SwitchDetourOwner` 的恢复窗口补口没有把 crowd 打坏；
  - crowd 当前两条 fresh 都没有复发用户所说的“长时间慢卡 + 被拖远 + 明明前方松了仍像被旧 detour 拖着”的形态；
  - 这轮运行态末尾只剩 1 条非阻断 warning：
    - `There are no audio listeners in the scene`

### 记录 031

- 时间：2026-03-28
- 动作：
  - 在 live 中继续追 single 残差；
  - 复现到 1 条 `playerReached=True` 但 `blockOnsetEdgeDistance=0.353` 的 single fail 后，立即回到代码层补最小口：
    - `PASSIVE_NPC_CLOSE_CONSTRAINT_SOFT_OVERLAP_SPEED: 0.18 -> 0.22`
    - `passive slight-overlap` 窗口不再默认走 `BlockedInput`
- 关键证据：
  - fail heartbeat：
    - `detour=False`
    - `passiveNpcBlocker=True`
    - `action=BlockedInput`
    - `moveScale=0.18`
    - `closeClearance=-0.040`
  - fail 摘要：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False minEdgeClearance=-0.005 blockOnsetEdgeDistance=0.353 playerReached=True`
- 补口后 live：
  - single retry：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.012 blockOnsetEdgeDistance=0.170 playerReached=True`
  - crowd guard：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.009 directionFlips=1 crowdStallDuration=0.174 playerReached=True`
- 判定：
  - 当前 single 残差已进一步从“大概是轻微 overlap 慢爬”收窄为：
    - passive slight-overlap + BlockedInput + 低 moveScale
  - 这次补口后，single 已从 `0.353 fail` 重新回到 `0.170 pass`
  - crowd 回归没有被这刀打回去

### 记录 032

- 时间：2026-03-28
- 动作：
  - 用户明确纠偏：
    - 之前的 startup cancel 不是导航别的问题，而是验证时误触发了和 `001` 的聊天 / 剧情；
  - 因此本轮先不回漂 `GameInputManager.cs`，而是只在 `NavigationLiveValidationRunner.cs` 做验证隔离：
    - 点击前先清 `GameInputManager.ClearPendingAutoInteraction()`
    - 对 `001 / 002 / 003` 整棵层级临时切 `Ignore Raycast`
    - 仅包住 `DebugIssueAutoNavClick(...)` 那一帧
    - 点击发出后立即恢复层
- 证据：
  - 连续跑：
    - `single x2`
    - `crowd x1`
  - 期间 Console：
    - `Dialogue` 相关日志 `0`
    - `NPCDialogue` 相关日志 `0`
    - `InformalChat` 相关日志 `0`
- 判定：
  - 当前“和 001 聊天触发剧情”这条验证污染链已被切掉；
  - 本轮无需回到 `GameInputManager.cs` 扩刀；
  - 从这一刻起，single 暴露出来的行为才该被当成导航本体继续判断。

### 记录 033

- 时间：2026-03-28
- 动作：
  - 验证污染切掉后，single 首轮仍出现：
    - `0.665 fail`
    - `0.462 fail`
  - 但 heartbeat 同时仍显示：
    - `moveScale=0.22`
  - 与本地代码已改出的新 floor 不一致，因此先强制：
    - `refresh_unity(mode=force, scope=scripts, compile=request)`
    - 确认 `external_changes_dirty=false`
  - 再继续只在 `PlayerAutoNavigator.cs` 补最小口：
    - `PASSIVE_NPC_CLOSE_CONSTRAINT_SOFT_OVERLAP_SPEED: 0.22 -> 0.26`
    - `GetPassiveNpcCloseConstraintMoveFloor(...)` 改为复用同一常量
    - passive NPC 的正 clearance 分支只有在 `blockingDistance <= 0.72` 时才允许进入 `BlockedInput`
- 静态闸门：
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
  - `validate_script(NavigationLiveValidationRunner.cs) => errors=0 warnings=2`
  - `git diff --check` 通过
- fresh live：
  - single fresh #1：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.005 blockOnsetEdgeDistance=0.178 playerReached=True`
  - single fresh #2：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.013 blockOnsetEdgeDistance=0.168 playerReached=True`
  - crowd guard：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.007 directionFlips=1 crowdStallDuration=0.221 playerReached=True`
- 判定：
  - 当前新程序集已真正生效，证据就是 heartbeat 的 `moveScale` 已从旧的 `0.22` 变为新的 `0.26`
  - 这轮把“验证污染”和“passive 早卡窗”两件事一起收掉了；
  - 当前 latest stable window 可更新为：
    - single：`0.169 / 0.170 / 0.178 / 0.168`
    - crowd：`0.154 / 0.154 / 0.174 / 0.221`

### 记录 034

- 时间：2026-03-28
- 动作：
  - 继续按 `single -> crowd -> single` 做重复稳定性确认；
  - 过程中先复现：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False ... blockOnsetEdgeDistance=0.287`
  - 然后在 `NavigationLiveValidationRunner.cs` 增加一次性 `block_onset` 取证日志。
- 关键证据：
  - fail 当帧：
    - `block_onset edgeClearance=0.189`
    - `action=PathMove`
    - `moveScale=0.26`
    - `blockDist=0.753`
    - `passiveNpcBlocker=True`
    - `closeApplied=True`
    - `closeClearance=-0.009`
- 判定：
  - 这轮 residual 不是 001 对话污染回来了；
  - 也不是 crowd 被打坏；
  - 而是 passive close-constraint 在 `blockDist` 仍偏大时过早触发。

### 记录 035

- 时间：2026-03-28
- 动作：
  - 继续只在 `PlayerAutoNavigator.cs` 上收同一条链：
    - `MaybeRelaxPassiveNpcCloseConstraint(...)` 增加 `avoidance` 入参
    - 当 `blockingDistance > 0.72` 且仍只是 `soft-overlap` 级别时，直接撤掉这次 passive close-constraint
    - `ShouldResetShortRangeAvoidanceProgress(...)` 增加 `blockingDistance <= 0.72`
    - `ShouldApplyPassiveNpcCloseConstraintMoveFloor(...)` 增加 `blockingDistance <= 0.72`
- 静态闸门：
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
  - `git diff --check` 通过
- fresh live：
  - single fresh #1：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.001 blockOnsetEdgeDistance=0.078 playerReached=True`
  - crowd guard：
    - `scenario_end=RealInputPlayerCrowdPass pass=True minEdgeClearance=-0.013 directionFlips=1 crowdStallDuration=0.057 playerReached=True`
  - single fresh #2：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=True minEdgeClearance=-0.008 blockOnsetEdgeDistance=0.137 playerReached=True`
- 判定：
  - 当前新补口已把 `single` 的 `0.287` 残差重新压回 pass；
  - 现在 latest stable window 可进一步更新为：
    - single：`0.169 / 0.170 / 0.178 / 0.168 / 0.078 / 0.137`
    - crowd：`0.154 / 0.154 / 0.174 / 0.221 / 0.057`

### 记录 036

- 时间：2026-03-28
- 动作：
  - 从局部 `single / crowd` 重新切回整包 `Run Live Validation`
  - 首轮没有直接进入导航结论，而是先撞到 compile blocker：
    - `NPCDialogueInteractable.cs` 需要 `SpringDay1WorldHintBubble.HideIfExists(...)`
    - 但 `SpringDay1WorldHintBubble.cs` 缺这个静态方法
  - 本轮先做最小兼容补口：
    - 在 `SpringDay1WorldHintBubble.cs` 新增 `HideIfExists(Transform anchorTarget = null)`
- 静态闸门：
  - `validate_script(SpringDay1WorldHintBubble.cs) => errors=0 warnings=1`
  - `git diff --check` 通过
- 判定：
  - 这一步不是导航新策略，而是为整包 live 清 compile blocker；
  - 不清掉这类 blocker，后面的导航判断都会是假现场。

### 记录 037

- 时间：2026-03-28
- 动作：
  - 编译恢复后，整包第一次 `RunAll`
- 结果：
  - `RealInputPlayerAvoidsMovingNpc => pass`
  - `RealInputPlayerSingleNpcNear => fail 0.192`
  - `RealInputPlayerCrowdPass => pass`
  - `NpcAvoidsPlayer => pass`
  - `NpcNpcCrossing => pass`
  - `all_completed=False`
- 关键证据：
  - `single` fail 的 `block_onset`：
    - `edgeClearance=0.192`
    - `action=BlockedInput`
    - `moveScale=0.39`
    - `blockDist=0.728`
    - `closeApplied=False`
    - `closeClearance=-0.034`
- 判定：
  - 这条 residual 已经进一步收窄为：
    - passive blocker 的负 clearance 分支在 `blockDist≈0.728` 时也会抢跑 `BlockedInput`

### 记录 038

- 时间：2026-03-28
- 动作：
  - 继续只在 `PlayerAutoNavigator.cs` 同链补 1 刀：
    - 对 passive blocker 的 `clearance <= 0` 分支增加门槛：
      - 若 `!HardBlocked`
      - 且 `blockingDistance > 0.72`
      - 且仍只是 `soft-overlap`
      - 则不提前 `BlockedInput`
- 静态闸门：
  - `validate_script(PlayerAutoNavigator.cs) => errors=0 warnings=2`
  - `git diff --check` 通过
- fresh 整包：
  - `RunAll #1 => all_completed=True`
    - `single => 0.088 pass`
    - `crowd => 0.060 pass`
  - `RunAll #2 => all_completed=True`
    - `single => 0.130 pass`
    - `crowd => 0.063 pass`
  - `NpcAvoidsPlayer / NpcNpcCrossing` 两轮继续保持 pass
  - `Dialogue / InformalChat = 0`
  - 末尾 Console `error / warning = 0`
- 判定：
  - 当前导航已经不是“局部样本绿”；
  - 而是整包 `5 scenario` 连续两轮全绿；
  - 当前主线已进入“可以考虑更长轮次确认 / Git 收口”的阶段。

### 记录 039

- 时间：2026-03-28
- 动作：
  - 继续执行第 3 轮整包 `RunAll`
  - 过程中 single 重新出现：
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False blockOnsetEdgeDistance=0.462`
  - 立刻回看同轮 `block_onset` 证据
- 关键证据：
  - `playerDelta=(0.00, 0.00)`
  - `action=PathMove`
  - `moveScale=1.00`
  - `blockDist=1.130`
  - `closeApplied=False`
- 判定：
  - 这组信号说明当前 red 不是导航本体回退，而是 `NavigationLiveValidationRunner.UpdateBlockOnsetMetric(...)` 把 single 启动空帧误记成 onset；
  - 因此这轮不继续乱修 `PlayerAutoNavigator` 或 solver。

### 记录 040

- 时间：2026-03-28
- 动作：
  - 只在 `NavigationLiveValidationRunner.cs` 修 onset 记账条件：
    - 只有在“已有有效位移样本”或“已出现真实减速/阻塞信号（HardStop / BlockedInput / closeApplied / hardBlocked / moveScale<0.95）”时，才允许记录 `block_onset`
  - 随后重新执行：
    - `RunAll x1`
    - `RealInputPlayerCrowdPass x3`
    - `RealInputPlayerSingleNpcNear x1`
- 静态闸门：
  - `validate_script(NavigationLiveValidationRunner.cs) => errors=0 warnings=2`
  - `git diff --check` 通过
- live 结果：
  - `RunAll => all_completed=True`
    - `single => 0.089 pass`
    - `crowd => 0.167 pass`
  - `crowd-only`：
    - `0.160 pass`
    - `0.098 pass`
    - `0.220 pass`
  - `single-only`：
    - `0.156 pass`
- 判定：
  - 第 3 轮整包里的 `single 0.462` 已实锤为 validation false positive，不再是新的导航根因；
  - crowd 多跑没有复现“长时间慢卡 / 被旧 detour 拖着走远”的用户症状；
  - 当前导航主线已进一步接近可收口状态。
