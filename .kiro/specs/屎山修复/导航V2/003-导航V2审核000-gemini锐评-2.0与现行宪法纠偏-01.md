# 导航V2 审核 `000-gemini锐评-2.0` 与现行宪法纠偏委托

日期: 2026-03-26  
状态: 生效中  
工作区: `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2`

---

## 1. 当前已接受基线

先把当前已经钉死的东西复述清楚，不允许你重新发明背景：

1. `006-Sunset专业导航系统需求与架构设计.md`
   - 仍是目标蓝图
2. `007-Sunset专业导航底座后续开发路线图.md`
   - 仍是阶段路线
3. `002-导航V2开发宪法与阶段推进纲领.md`
   - 仍是当前 V2 的现行开发宪法
4. `导航V2` 当前不是实现线程
   - 你当前这一轮的职责仍然是：审核、厘清、纠偏、必要时修正文档
5. 当前不准偷换：
   - 一份新的 Gemini 锐评不等于自动升格成 `006/007/002` 之上的新上位法

---

## 2. 本轮唯一主刀

本轮唯一主刀固定为：

> 审核 [000-gemini锐评-2.0.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航V2/000-gemini锐评-2.0.md)，并正面回答：它对“我们现在的真实情况”到底说对了多少、说错了多少、哪些地方必须吸收进 `002`，哪些地方必须被纠正或拒绝。

注意，这轮不是让你去实现导航代码。  
这轮也不是让你去给 `导航检查V2` 直接下实现令。  
这轮就是要你把 `2.0` 审干净，把现行宪法要不要纠偏钉死。

---

## 3. 你必须核清的 4 个硬点

这轮不允许泛泛而谈，必须逐条正面核：

### 3.1 “状态账本”介入时机

`000-gemini锐评-2.0.md` 明确攻击了一个点：

- “偏差账本 / 状态图”如果等 `P0` 稳住后再补，会变成事后尸检，而不是实时记账

你必须核清：

1. 这条批评是不是成立
2. 如果成立，成立到什么程度
3. 这里说的“P0”到底对应：
   - `002` 里当前前门式的 `P0`
   - 还是未来真正进入下一代重构时的结构性 `P0`
4. 如果要纠偏，应该：
   - 直接修改 `002`
   - 还是追加一份更窄的补记 / 账本入口

禁止偷懒写成：

- “方向上有启发”
- “后续可以考虑”

你必须给出明确判断。

### 3.2 “代码级物理隔离”是否真的缺失

`2.0` 说：不能只靠 markdown 宪法，必须先建代码级隔离墙。

这条你必须事实核查，至少要核到下面这些现实：

1. 现在仓库里已经存在什么：
   - `INavigationUnit`
   - `NavigationAgentRegistry`
   - `NavigationPathExecutor2D`
   - 其他现有共享契约 / 共享状态资产
2. 现在仓库里还不存在什么：
   - 例如它举例的 `ITrafficArbiter`、`ILocomotionReceiver` 之类
3. 因此正确结论到底是：
   - “完全没有代码级隔离”
   - 还是“已经有部分隔离，但还没到足以编译期强制约束的程度”
4. 如果 `2.0` 这条只对一半，你必须把那一半写清楚，不能让它用绝对化语言盖过现状

### 3.3 `P0` 退出条件到底该看什么

`2.0` 认为：

- “用户可见阻塞稳定”不是架构退出条件
- `P0` 必须引入客观的依赖切断指标

这条你必须分层判断，至少要区分：

1. `002` 当前写的 `P0`
   - 本质是不是“当前阶段前门 / 用户可见阻塞压制”
2. 真正进入下一代结构施工时的 `P0`
   - 是否确实应该补充依赖切断型红线
3. 因此当前应该纠偏到什么程度：
   - 维持原句不动
   - 增补一段“前门指标 ≠ 结构退出指标”
   - 还是重写 `P0` 的完成定义

不允许把“当前前门”与“未来结构退壳阶段”混成一锅。

### 3.4 “状态惯性 / Hysteresis 测试基准缺失”是否属实

`2.0` 说当前遗漏了 `Hysteresis` 测试基准。

这条你必须核代码与测试事实，不能拍脑袋。

至少核清：

1. 现在是否已经有：
   - detour clear hysteresis
   - recovery cooldown
   - 对应的 `NavigationAvoidanceRulesTests`
2. 这些现有测试覆盖到哪里，没覆盖到哪里
3. `2.0` 这条更准确的表达应不应该是：
   - “完全没有基准”
   - 还是“已有 detour / recovery 侧基准，但未来裁决状态机还缺正式测试”

---

## 4. 本轮允许 scope

只允许你动这些：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\000-gemini锐评-2.0.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\002-导航V2开发宪法与阶段推进纲领.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航V2\memory.md`
4. 如需 `Path C`，允许你新增同目录审视报告
5. 允许只读核对：
   - `006`
   - `007`
   - 相关 memory
   - 相关代码 / 测试文件

---

## 5. 本轮明确禁止

这轮不允许你漂去：

1. `导航检查V2` 的实现施工
2. 新开 live / Unity / runner
3. 顺手写新的实现蓝图
4. 借 `2.0` 重开：
   - `TrafficArbiter`
   - `MotionCommand`
   - `DynamicNavigationAgent`
   - 全量运动学统一
5. 把“问题意识正确”直接偷换成“施工方案立即生效”

---

## 6. 完成定义

这轮只有满足下面这些，才算完成：

1. 你给出 `000-gemini锐评-2.0.md` 的路径判断：
   - `Path A / B / C`
2. 你必须明确区分：
   - 哪些问题判断成立
   - 哪些事实表述过度绝对化或与当前文档 / 代码不符
3. 你必须明确回答：
   - `002` 要不要修
   - 如果要修，具体修哪一处
   - 修的是“表述时机 / 边界澄清 / 结构定义”，不是乱重写整份文档
4. 如果你判成 `Path C`
   - 必须新增审视报告
5. 如果你判成 `Path B`
   - 你必须把局部纠偏真正落到文档里，而不是只在回执里口头说

---

## 7. 固定回执格式

只按下面格式回复，不要加长篇解释稿：

- 审核结论：
- `000-gemini锐评-2.0.md` 成立的点：
- `000-gemini锐评-2.0.md` 需要纠正的点：
- `002` 是否已修订：
- changed_paths：
- 当前 own 路径是否 clean：
- blocker_or_checkpoint：
- 一句话摘要：

---

## 8. 一句话要求

这轮你不是去“帮 Gemini 说服项目”，而是要把 `2.0` 审成一份对 Sunset 当前真实现场负责的结论，并把该纠正的宪法边界纠正掉。
