# 2026-04-02-父线程-V2-48验收尺与最终导航并行大刀拒收清单-49

## 1. 这份文档的目的

这是父线程下一次收 `-48` 回执时的固定验收尺。

它专门防五件事：

1. `V2` 明明已经被改派去做 runner/menu + live 复判，却又回去碰 `PlayerAutoNavigator.cs`
2. `V2` 没把 final acceptance pack 落出来，却拿零散 case 冒充“最终矩阵已跑完”
3. `V2` 把旧 `legacy crowd` 混回主 acceptance
4. `V2` 没把红绿总图说清楚，导致用户还是不知道离最终验收还差什么
5. `V2` 再次把 `Ground raw matrix` 的结构绿偷换成“右键停位偏上已关闭”

---

## 2. 下一次回执的固定审查顺序

### 第一关：先审它有没有守住并行分工

必须先看：

1. 父线程主刀是不是仍然：
   - `PlayerAutoNavigator.cs`
2. `V2` 主刀是不是仍然：
   - `NavigationLiveValidationRunner.cs`
   - `NavigationLiveValidationMenu.cs`
3. `V2` 这轮有没有碰：
   - `PlayerAutoNavigator.cs`

只要 `V2` 又碰 PAN，继续拒收。

### 第二关：再审它有没有真的补 final acceptance pack

必须先看：

1. 是否新增了一个明确的 final pack 入口
2. 顺序是否固定为：
   - `PassableCorridor ×3`
   - `StaticNpcWall ×3`
   - `EndpointNpcOccupied ×1`
   - `Ground raw matrix ×1`
   - `SingleNpcNear ×1`
   - `MovingNpc ×1`
   - `NpcAvoidsPlayer ×1`
   - `NpcNpcCrossing ×1`
3. 旧 `legacy crowd` 是否被排除在主 pack 外

### 第三关：再审它有没有真的 fresh 跑完

必须逐项看见：

1. `PassableCorridor ×3`
2. `StaticNpcWall ×3`
3. `EndpointNpcOccupied ×1`
4. `Ground raw matrix ×1`
5. `SingleNpcNear ×1`
6. `MovingNpc ×1`
7. `NpcAvoidsPlayer ×1`
8. `NpcNpcCrossing ×1`

### 第四关：最后才审它有没有把总图说清

必须看见：

1. 当前红面还剩哪几条
2. 当前绿面已经有哪些
3. 现在能不能把“导航基本可用”写出来
4. 它有没有继续诚实承认：
   - `右键停位偏上 = 不能关闭`

---

## 3. 下一次回执的最小通过条件

如果它想把 `-48` 判成“并行验收侧站住”，最少必须同时满足：

1. 守住写域分工，没有碰 `PlayerAutoNavigator.cs`
2. final acceptance pack 已补出来或已证实现有入口可完全等价
3. fresh compile clean
4. 当前最新 PAN 工作树上的最终矩阵已完整报实
5. 红绿总图已能让用户一眼看出“还差多少”
6. 没有偷换“右键停位偏上已关闭”

---

## 4. 继续拒收的四种假完成

### 假完成 A

- “我虽然没补 final pack，但我一条条点完了，差不多就是最终矩阵”

拒收条件：

- 没有明确固定入口

### 假完成 B

- “父线程改得慢，我顺手也帮它补了点 PAN”

拒收条件：

- 越权改写 PAN

### 假完成 C

- “旧 crowd case 也一起看一下，作为参考”

拒收条件：

- 旧 `legacy crowd` 被混回主 acceptance

### 假完成 D

- “Ground raw 继续 pass，所以停位偏上应该可以先算关”

拒收条件：

- 仍没有玩家可视停位真值成立证据

---

## 5. 父线程下一次固定追问

1. 你这轮有没有碰 `PlayerAutoNavigator.cs`？
2. final acceptance pack 入口到底叫什么？
3. 当前最新 PAN 工作树下，最终矩阵红面还剩哪几条？
4. 当前已经稳定转绿的条目有哪些？
5. 为什么现在仍然不能把“右键停位偏上”写成已关闭？
