# 002-prompt-8

这轮我不给你留任何“解释空间”，只给你留施工空间。

先把当前总裁定吃进去：

- 你现在这条导航线，**不能叫完成**。
- 从验收口径看，当前不是“差最后一点”，而是**`S0-S6` 还没有闭环，且用户体验线等于未交卷**。
- 你可以说你搭出了一些新骨架，但**用户没有验收到内容**，这条事实优先级高于你历史上任何一次 `probe` 过线。

所以从现在开始，你不要再把自己放在“解释我其实做了很多”的位置。

你现在的身份只有一个：

> **导航底座迁移施工线程。**

你的任务不是再证明方向没错，而是把没有交出来的东西交出来。

---

## 一、这轮复工的上位依据

你后续所有动作，统一以上面这几份为硬依据：

- [006-Sunset专业导航系统需求与架构设计.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/006-Sunset专业导航系统需求与架构设计.md)
- [007-Sunset专业导航底座后续开发路线图.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/007-Sunset专业导航底座后续开发路线图.md)
- [008-给Codex与Gemini的导航验收审稿prompt.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/008-给Codex与Gemini的导航验收审稿prompt.md)
- [002-prompt-7.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/002-prompt-7.md)
- [统一导航重构阶段设计与执行主表.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/统一导航重构阶段设计与执行主表.md)
- [memory.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/memory.md)

尤其是 `008` 的审稿结论，你必须完全接受，不能再自己改写：

- `S0`：文档完成，但执行没有真正守住
- `S1`：部分完成
- `S2`：部分完成
- `S3`：未完成
- `S4`：部分完成
- `S5`：未完成
- `S6`：未完成

这就是你现在的真实起点。

不要再从“三条 `probe` 过了，所以差不多完成”起步。  
你现在必须从“**过渡系统仍未交卷**”起步。

---

## 二、这轮复工的唯一目标

这轮不是再修一个现象点。

这轮唯一目标是：

> **把当前“带新骨架的过渡系统”，推进成 `S0-S6` 第一版真实闭环。**

更具体地说，你至少要把下面 4 件事真的做出来：

1. **旧私有导航闭环真正下线**
   - `PlayerAutoNavigator` 不再偷偷维护完整私有导航主循环
   - `NPCAutoRoamController` 不再偷偷维护完整私有导航主循环
   - 它们必须真的退化成 `brain / bridge / intent layer`

2. **交通裁决真正前置**
   - 不再是先吃 solver 的方向修正，再翻译成 `Proceed / Yield / Wait / Recover`
   - 而是先形成交通裁决，再由求解层消费

3. **最终运动执行语义真正统一**
   - 不是只统一接口名
   - 而是玩家 / NPC 在 `停、让、绕、恢复` 这些状态上的最终收口语义真正一致

4. **体验线证据真正补上**
   - 不再只拿 `NavigationLiveValidationRunner` 的 `probe` 结果说话
   - 必须补能证明“玩家不再像推土机、NPC 不再像包着大圆壳”的真实玩法证据

---

## 三、这轮你必须停止的事

### 1. 停止再拿“有骨架进展”当交付

以下这些话，后面都不再算有效交付：

- “方向没错”
- “已经有共享交通裁决了”
- “已经有统一运动命令了”
- “不是完全没进展”

这些只能算背景，不算结果。

### 2. 停止再把功能线和体验线混说

你后面任何“通过 / 完成 / 闭环”的表述，都必须拆开：

1. 功能线
2. 体验线

只过功能线，不得再外推成完成。

### 3. 停止再把旧 patch 当新底座托底

你必须主动证明：

- 不是旧 detour / old fallback / private loop 在偷偷兜底
- 不是表面接上新层，实际主循环还在旧控制器里跑

如果这个证明拿不出来，就默认算没做完。

### 4. 停止再补新大分析文档

你现在不是继续产出长文。  
`006 / 007 / 008` 已经够了。

你可以更新主表和 memory，  
但前提是：

> **同步交真实代码落地和真实验证证据。**

---

## 四、这轮允许你的推进方式

这轮我允许你继续按同一条主线连续推进，不用每修一个点就停下来等批准。

但注意，这不是放飞。

你可以：

1. 在同一轮里连续推进多个热区代码 checkpoint
2. 先做结构收口，再做 live 验证，再继续往下一层推进
3. 自主安排 `S0-S6` 内的最优实现顺序

你不可以：

1. 又退回“先补一条 solver 参数看看”
2. 又退回“先让三个 `probe` 过了再说”
3. 又把主线拆回 5 个松散小刀，各自都不交闭环

如果本轮没做到 `S0-S6` 真闭环，也只接受一种未完成口径：

> 你明确指出当前唯一剩余 blocker 是哪一个，  
> 它卡在 `S0-S6` 的哪一层，  
> 为什么不是继续碎 patch 能掩过去的问题。

---

## 五、这轮你真正该怎么做

我不给你写死实现，但施工顺序我给你钉住：

### 阶段 A：先把 `S0` 从文档状态变成执行状态

你不是没写过 `S0`，你是没真的执行它。

所以你先要把这几个事实落实到实现面，而不是继续只写在文档里：

1. 当前保留资产到底有哪些
2. 当前必须废止的旧语义到底有哪些
3. 第一批热区之外默认不碰哪些文件
4. 这轮最小闭环样例是什么
5. 当前“完成”的定义到底是什么

`S0` 这轮的通过标准不是“又写了一遍”，  
而是后面的代码推进真的开始受它约束。

### 阶段 B：把 `S2-S3` 真正做成“先裁决、后求解”

这是你当前最核心的真空层。

你必须把下面这件事做实：

- `NavigationTrafficArbiter` 不再只是 solver 结果解释器
- `NavigationLocalAvoidanceSolver` 不再是上位决策替身

也就是说：

- `Proceed / Yield / Wait / SidePass / Recover`
  必须先成为真实裁决
- `AdjustedDirection / SpeedScale / ShouldRepath`
  只能变成求解/执行层消费结果，不再反向决定交通语义

如果这一层做不出来，后面所有“统一底座”都还是壳。

### 阶段 C：把 `S4-S6` 真正做成“底座主循环接管”

你后面必须正面解决 3 件事：

1. `PathCorridor / detour / repath / stuck / arrival`
   的责任到底收在哪
2. `PlayerAutoNavigator` 如何退出私有建路 / stuck / detour 主循环
3. `NPCAutoRoamController` 如何退出私有建路 / stuck / detour 主循环

不要再给我“接口已经统一了”的话术。

你必须明确证明：

- 旧主循环真的退了
- 新底座真的接管了
- 控制器真的只剩脑层 / 桥接层职责

### 阶段 D：把体验线证据补齐

你这轮不准再只交：

- `PlayerAvoidsMovingNpc`
- `NpcAvoidsPlayer`
- `NpcNpcCrossing`

这三条受控结果。

你还必须补：

1. 真实右键导航场景下，玩家面对漫游 NPC 的体验证据
2. 为什么现在不会再像推土机
3. 为什么现在不会再像活体外面包着大圆壳
4. 旧私有闭环退出后，这些体验没有靠老逻辑偷偷托底

没有这一层，仍然算未交卷。

---

## 六、这轮不准碰的范围

默认继续不碰：

- `S7 / S8`
- 怪物 / 宠物 / 牲畜真实接线
- 农田 / 背包 / UI / Dialogue / spring-day1
- 与导航无关的业务线

也不要顺手把 `Primary.unity` 当垃圾场。

如果你必须碰：

- `Primary.unity`
- `TagManager.asset`
- 其他热资源

你必须在回执里明确讲清：

1. 为什么这一步对 `S0-S6` 是必要的
2. 你留了什么
3. 清了什么
4. 当前是否已对你这条线 clean

---

## 七、你这轮必须补出来的证据

至少分 4 层：

### 1. 代码结构证据

你必须能明确指出：

- 哪些旧方法已经退场
- 哪些责任已经从控制器迁到共享底座
- 哪些旧 patch 已经不再担任运行时托底

### 2. 测试证据

你要补能锁住新裁决链和旧主循环退出的测试，  
不只是继续堆规则测试。

### 3. 功能线 live 证据

至少继续守住：

- `PlayerAvoidsMovingNpc`
- `NpcAvoidsPlayer`
- `NpcNpcCrossing`

但它们现在只能证明功能线。

### 4. 体验线证据

你必须正面回答：

- 现在玩家为什么不再是推土机
- 现在 NPC 为什么不再像包着大圆壳
- 玩家 / NPC 为什么终于像同一套活体交通系统

如果答不出来，就说明你还没做完。

---

## 八、用户补充区

如果用户在你施工过程中继续补观察、补骂、补体验反馈：

- 先吸收
- 再判断它落在 `S0-S6` 哪一层
- 然后继续沿主线施工

不要因为用户现场一句话，就把主线改题。  
也不要因为用户说得狠，就回到解释模式。

---

## 九、这轮只接受两种结局

### 结局 A：`S0-S6` 真闭环

你可以直接交：

1. 哪些阶段已经真正完成
2. 旧私有导航闭环是怎么退的
3. 交通裁决是怎么前置的
4. 统一运动执行语义是怎么落的
5. 功能线与体验线证据分别是什么
6. 为什么当前仍不进入 `S7/S8`

### 结局 B：没彻底闭环，但推进到了新的硬 checkpoint

也可以。

但前提是你必须明确：

1. 当前推进到了 `S0-S6` 的哪一层
2. 哪些已经真的完成
3. 当前唯一剩余 blocker 是什么
4. 为什么它是唯一 blocker，而不是你又在用旧 patch 兜着跑

我不再接受第三种结局：

- 改了一堆
- 解释了一堆
- 但说不清自己到底推进到了哪一步

---

## 十、本轮回执格式

下一次回执，只按下面格式回复：

- 当前主线推进到 `S0-S6` 的哪一步：
- 当前在改什么：
- 当前是否已把 `S0` 从文档状态落实为执行状态：
- 当前是否已完成“先裁决、后求解”的交通中轴改造：
- 当前是否已完成统一运动执行语义：
- 当前是否已完成 `PlayerAutoNavigator` 私有导航闭环下线：
- 当前是否已完成 `NPCAutoRoamController` 私有导航闭环下线：
- 当前功能线 live 结果：
- 当前体验线证据：
- changed_paths：
- 是否触碰 `Primary.unity` / `TagManager.asset` / 其他热资源：
- 新增或修改了哪些测试 / 验证：
- 当前是否仍有旧 patch / old fallback / private loop 在托底：
- 当前剩余未完成的是 `S0-S6` 中哪一项：
- blocker_or_checkpoint：
- 一句话摘要：

---

## 十一、最后再钉一次

你这轮不是“继续优化”。

你这轮是：

> **基于已经明确的不及格审核结论，彻底复工，把没交出来的 `S0-S6` 交出来。**

你可以有多个 checkpoint，  
但主线只有一条：

> **让导航底座从“过渡系统”真正变成“玩家 / NPC 第一版专业闭环成立”。**

现在开始施工，不要再写自我辩护稿。
