# 2026-03-26-导航检查V2首轮启动委托-02

你现在是新线程 `导航检查V2`。

这轮不是重新做总审计，也不是继续发散去调 solver、重谈 `TrafficArbiter / MotionCommand`，更不是先跑一大轮 live。

## 当前已接受的基线

1. 当前主叙事已经稳定收敛为：
   - `ShouldRepath` 已出现
   - detour owner 没有稳定接管执行层
2. 当前 single first blocker 已固定为：
   - `PlayerAutoNavigator / NPCAutoRoamController / NavigationPathExecutor2D`
   - 在 detour owner 的创建、保活、释放上没有形成稳定闭环
3. 当前不再接受把主刀漂回：
   - solver 权重泛调
   - `TrafficArbiter / MotionCommand`
   - `Primary.unity`

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\V2交接文档\00_交接总纲.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\V2交接文档\05_当前现场_高权重事项_风险与未竟问题.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\V2交接文档\06_证据索引_必读顺序_接手建议.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\002-prompt-17.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`

## 这轮唯一主刀

只做：

> detour owner 保活最小闭环

更具体地说，这轮只允许你完成下面这 1 刀：

1. 只锁这三处热区：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
2. 目标只做一件事：
   - 让 detour 一旦创建成功，至少能稳定活过一个有效执行窗口；
   - 不再被“瞬时无 blocker / cooldown / 旧 rebuild fallback / clear override”马上吞掉。
3. 允许你为了这件事补最小状态机、最小 guard 或最小生命周期接线；
4. 但不允许把目标扩成“完整解决所有体感问题”。

## 明确禁止

1. 不要先调 `NavigationLocalAvoidanceSolver` 权重
2. 不要先重开 `TrafficArbiter / MotionCommand`
3. 不要先碰 `Primary.unity`
4. 不要先跑大窗 full live
5. 不要把 scope 扩到“所有导航体验一起修完”
6. 不要先大改 `PlayerMovement.cs`，除非你能证明这是 detour owner 保活的唯一必要补口

## 允许的作用域

这轮只允许你读取和必要时修改这些范围：

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\V2交接文档\`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
7. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\NavigationAvoidanceRulesTests.cs`
8. 如确属必要，才可只读引用：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLocalAvoidanceSolver.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerMovement.cs`

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已经把 detour owner 最小保活闭环接上；
2. 你已经通过代码闸门：
   - `git diff --check`
   - relevant `validate_script` / 编译检查
3. 你已经跑 1 组最小 fresh live：
   - 优先 `RealInputPlayerAvoidsMovingNpc`
   - 拿到结果后立刻 `Stop`
   - 退回 `Edit Mode`
4. 你的回执必须能明确回答：
   - detour 是否终于进入有效 owner 窗口
   - 如果还没过线，新的单一第一阻塞点是什么
5. 你已把本轮结果追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

## 最小回执格式

- 已回写文件路径
- detour owner 是否已进入有效执行窗口：`yes / no`
- 当前单一第一阻塞点或当前单一第一进展
- 一句话摘要

## 一句话总口径

你这轮不是来继续讨论“为什么会这样”，而是只打一刀：让 detour owner 真正活起来，哪怕先只活过一个有效窗口。
