# 2026-04-02-父线程-V2-31验收尺与右键停位假关闭拒收清单-32

## 1. 这份文档的目的

这不是给 `V2` 的新施工正文。

这是父线程下一次收 `-31` 回执时的固定验收尺，用来防三种新漂移：

1. 把 `-29` 的 fake green removal 直接包装成“runtime 真因已经实锤”
2. 把 endpoint / crowd 的 `Linger` 直接偷换成“已经证明就是提前失活”
3. 把 ground / static / 右键停位偏上继续拿 `Collider.center` 单指标冒充“用户体感已关闭”

---

## 2. 当前已经接受的事实

后续不再反复追问这些：

1. dedicated endpoint 的 fake green 已剔掉
2. dedicated endpoint 的 menu/toolchain 已接回
3. `-29` 只算 `carried partial checkpoint`
4. 当前 dedicated endpoint 不是 interaction hijack，也不是 blocker shell 误绿

---

## 3. 下一次回执的固定审查顺序

### 第一关：先审它有没有把“真因”说实

必须先看它有没有真的回答：

1. fresh 失败瞬间的 `Requested / Resolved / Transform / Rigidbody / Collider / Foot`
2. `IsActive / pathCount / pathIndex / DebugLastNavigationAction`
3. 当前到底是哪条 PAN 分支在吃窗口

如果它还是只有：

- `outcome=Linger`
- `我判断是提前失活`

但没有把分支签名和状态量钉出来，这轮就只能算：

- `怀疑成立`

不能算：

- `真因已实锤`

### 第二关：再审它有没有继续偷换右键停位偏上

只要下一次回执里出现下面任一叙事，默认继续拒收：

1. `Ground raw ×1 pass=True，所以偏上关闭`
2. `Collider 对准点击点，所以用户体感算关闭`
3. 没有给 `Transform / Rigidbody / Foot`，却直接写 `static 已关闭`

父线程固定口径：

- center-only 绿，只能证明结构合同
- 用户最新真人反馈仍说右键停位怪、像落在上面
- 所以这条没有玩家可视真值前，不准绿

### 第三关：再审 scope 有没有乱漂

下一次如果它又改了：

1. `NavigationLiveValidationRunner.cs`
2. `NavigationLiveValidationMenu.cs`
3. solver
4. NPC roam
5. scene / static runner

而没有给出“为什么 `PlayerAutoNavigator.cs` 无法承接这刀”的硬证据，就默认判 scope 漂移。

### 第四关：最后才审 fresh matrix

只有前 3 关都过了，才看：

1. `EndpointNpcOccupied raw ×3`
2. `Crowd raw ×1`
3. `Ground raw ×1`
4. `SingleNpcNear raw ×1`
5. `NpcAvoidsPlayer ×1`
6. `NpcNpcCrossing ×1`

---

## 4. 下一次回执的最小通过条件

如果它想把 `-31` 判成“这轮站住”，最少必须同时满足：

1. 明确指出 endpoint / crowd 当前失败瞬间是哪条 PAN 分支在吃窗口
2. 这条分支已经被最小修正，而不是继续靠猜
3. `EndpointNpcOccupied raw ×3` 不再全是：
   - `playerCenterDistance ≈ 1`
   - `npcPushDisplacement = 0`
   - `blockedInputFrames = 0`
   - `outcome=Linger`
4. `Crowd raw ×1` 不再维持同一 linger 签名
5. 它没有再把 `Ground raw ×1` 的 center-only 结构绿，偷换成“用户可视停位已关闭”

---

## 5. 继续拒收的两种新假完成

### 假完成 A

- “这轮虽然还没完全修掉，但已经能确定就是 `TryFinalizeArrival(...)` 了”

拒收条件：

- 没给 fresh 状态量
- 没给失败瞬间分支证据
- 只有主观归因

### 假完成 B

- “ground / static 已关闭，只剩 endpoint / crowd lingering”

拒收条件：

- 没给 `Transform / Rigidbody / Foot`
- 或这些量仍明显在点击点上方
- 或用户可视裁定不是明确 `已关闭`

---

## 6. 父线程下一次固定追问

下次 `V2` 回来，我固定先问：

1. 失败瞬间到底是哪条 PAN 分支在吃窗口？
2. 这条分支的旧退出条件是什么？你改成了什么新保持 / 恢复条件？
3. endpoint 和 crowd 现在的 fail 签名是否还是同一条？
4. `Ground raw ×1` 的 `Transform / Rigidbody / Foot` 与点击点的关系是什么？
5. 你现在还能不能把“右键停位偏上”写成已关闭？

答不上来，就继续拒收。

---

## 7. 当前阶段判断

当前最准确的阶段判断是：

1. `-29` 已把假绿剔掉
2. `-31` 应该只打 PAN runtime 真因
3. 用户关心的“右键停位还怪、像落在上面”不能再被结构指标吞掉

所以父线程下一步仍然是：

- 继续发 prompt

但只发这一刀，不停给用户验收。
