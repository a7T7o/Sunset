# 2026-04-01-导航检查V2-继续追打Player主线多簇责任点与终点NPC专案-27

先给这轮裁定：

1. 我接受你现在继续留在 `PlayerAutoNavigator.cs` 主线；
2. 我接受你没有再漂回 `-25`、NPC roam、solver、scene；
3. 我接受你这轮没有把 crowd fail 说成 done；
4. 但我**不接受**你把当前剩余问题继续收束成“只剩一个 crowd slow-crawl 点”。

原因很直接：

- 你这轮回执里把剩余问题继续压成：
  - `ShouldUseBlockedNavigationInput(...)` 之后的 late crowd slow-crawl
- 但当前代码现场显示，至少还有三簇共同在起作用：
  1. `HasPassiveNpcCrowdOrCorridor(...)`
  2. `TryGetPointArrivalNpcBlocker(...)`
  3. `TryFinalizeArrival(...) / ShouldHoldPostAvoidancePointArrival(...)`
- 另外，`-26` 明写要求的：
  - **“终点有 NPC 停留”的 dedicated case**
  你这轮还是没补出来。

所以这轮不准再继续神话“我已经把根因压成单点”。

你要做的是：

- **继续留在 Player 主线**
- **但按多簇共同责任去收**
- **并把 dedicated 的“终点有 NPC 停留”专案补出来**

---

## 一、当前已接受的基线

这些继续接受，不要回头打散：

1. `-25` 继续只算 `carried partial checkpoint`
2. `-26` 当前仍是唯一主线
3. 普通地面点导航 contract truth 当前继续按 `Collider(center)` 看
4. `Ground raw matrix` 当前可以继续视为站住
5. `SingleNpcNear raw ×3` 当前可以继续视为站住
6. `MovingNpc raw ×1` 当前可以继续视为 guardrail 未坏

这些都不准回漂。

---

## 二、这轮唯一主刀

只收两件强绑定的东西：

1. **Player 主线的多簇共同责任点**
2. **“终点有 NPC 停留” dedicated case**

不再接受：

- “我先继续压 slow-crawl，剩下的以后再说”

因为你现在最容易看错的，正是把多簇耦合问题讲成单点。

---

## 三、当前共同责任点，必须一起审

这轮你必须把下面三簇按**共同责任点**回答清楚：

### A. `HasPassiveNpcCrowdOrCorridor(...)`

你必须回答：

1. 这条 corridor 识别现在到底在帮你，还是在误触发过宽
2. 当前双近距 NPC / crowd 通道 lingering 时，它到底还在不在错误地把场景判成 corridor
3. 这条识别与当前 slow-crawl 的关系是：
   - 主因
   - 共因
   - 还是已基本排除

### B. `TryGetPointArrivalNpcBlocker(...)`

这条现在不能再轻描淡写。

你必须回答：

1. 当前终点 NPC lingering / 死避让坏相，与这条终点 blocker 判定的关系是什么
2. 你现在对 candidate 占位半径的裁剪是否仍可能低估 blocker
3. dedicated “终点有 NPC 停留” case 里，它到底有没有误判 / 漏判

### C. `TryFinalizeArrival(...) / ShouldHoldPostAvoidancePointArrival(...)`

你必须回答：

1. 当前 through-corridor 之后 lingering 的最后一段，是不是仍然有完成链的参与
2. 现在的 hold / finalize 邻域，到底是：
   - 正在过早拦住完成
   - 正在正确延迟完成
   - 还是已经基本排除

---

## 四、这轮必须补出的 dedicated case

### “终点有 NPC 停留” dedicated case

这轮**不准再只用 crowd 末段代理顶过去**。

你必须补 dedicated case，最小要求：

1. 玩家右键目标点附近，固定有一个静止 NPC 占位
2. 玩家从一条比较干净的主路径接近终点
3. 不依赖 crowd 两侧通道，不依赖 moving NPC
4. 让这个 case 直接回答用户最烦的那种坏相：
   - 到终点时反复死避让
   - 终点前 lingering
   - 明明该停却一直像要撞开 NPC

如果你绝对需要验证入口，只允许最小增量地改：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`
2. 或 `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationStaticPointValidationRunner.cs`

但只允许新增这一条 dedicated player-side case。

不准顺手扩成新矩阵框架。

---

## 五、这轮允许的作用域

### 允许修改

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`

如确有必要，再最多允许：

2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationStaticPointValidationRunner.cs`

以及你自己的：

4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\memory.md`
6. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

### 不允许修改

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLocalAvoidanceSolver.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\Editor\NavigationLiveValidationMenu.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NavigationStaticPointValidationMenu.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`
6. scene / prefab / hotfile
7. broad cleanup / sync / own-root 收口作为主线

---

## 六、这轮明确禁止的漂移

1. 不准再把剩余问题写成“只剩一个 slow-crawl 点”
2. 不准再缺 dedicated 的“终点有 NPC 停留” case
3. 不准再继续用 crowd 代理长期替代 dedicated case
4. 不准回漂 `-25` 或 NPC roam
5. 不准回漂 solver / scene / menu / static unrelated 争论
6. 不准把 `sync`、`Ready-To-Sync`、broad own roots blocker 写成下一步主线

---

## 七、这轮验证矩阵

### 1. `Ground raw matrix`

这轮只需最小复核 `×1`，确认你没有把 ground contract 打坏。

### 2. `SingleNpcNear raw ×3`

继续 fresh，不准拿旧 run 顶。

### 3. `终点有 NPC 停留 dedicated case ×3`

这是这轮新增硬要求。

必须 fresh `×3`，并逐条报：

1. `pass / fail`
2. `playerCenterDistance`
3. `npcPushDisplacement`
4. `directionFlips`
5. `blockedInputFrames`
6. `detourMoveFrames`
7. 当前 lingering / 死避让到底主要落在哪一簇

### 4. `Crowd raw ×3`

继续 fresh，但这轮它不再承担“代理终点 NPC”职责。

它只负责回答：

- 双近距 NPC / corridor 通道 lingering 这条还剩多少

### 5. `MovingNpc raw ×1`

继续只做 guardrail。

### 6. 如果你碰了 runner，再补

1. `NpcAvoidsPlayer ×1`
2. `NpcNpcCrossing ×1`

它们依然只是 guardrail，不是主成功标准。

---

## 八、这轮完成定义

### 结局 A：可接受

必须同时满足：

1. `-25` 继续明确写成 `carried partial checkpoint`
2. 你没有再把 remaining 问题过早收束成单点
3. dedicated 的“终点有 NPC 停留” case 已补出
4. 你能用 fresh 证据说明三簇共同责任里，谁是主因、谁是共因、谁已基本排除
5. `SingleNpcNear raw ×3` 继续稳
6. `Crowd raw ×3` 至少继续前进，不能回弹到更坏的鬼畜区
7. `MovingNpc raw ×1` 不坏

### 结局 B：仍 fail

你必须明确回答：

1. dedicated 的“终点有 NPC 停留” case 为什么还没成立
2. 三簇里当前真正最像主因的是哪一簇
3. 哪一簇只是共因
4. 哪一簇已经可以暂时降级
5. 为什么不能继续把问题讲成“只剩一个点”

---

## 九、固定回执格式

### A1. 用户可读汇报层

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### B. 技术审计层

1. 当前在改什么
2. `-25` 当前如何定性；必须直接写 `carried partial checkpoint`
3. 这轮为什么不接受“只剩单一 slow-crawl 点”的说法
4. `HasPassiveNpcCrowdOrCorridor(...)` 这轮的裁定
5. `TryGetPointArrivalNpcBlocker(...)` 这轮的裁定
6. `TryFinalizeArrival(...) / ShouldHoldPostAvoidancePointArrival(...)` 这轮的裁定
7. `Ground raw matrix ×1` 结果
8. `SingleNpcNear raw ×3` 结果
9. `终点有 NPC 停留 dedicated case ×3` 结果
10. `Crowd raw ×3` 结果
11. `MovingNpc raw ×1` 结果
12. 如果碰了 runner，再补：
    - `NpcAvoidsPlayer ×1`
    - `NpcNpcCrossing ×1`
13. 如果仍 fail，新的第一责任点是什么
14. changed_paths
15. 当前 own 路径是否 clean
16. blocker_or_checkpoint
17. 一句话摘要
18. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
19. 如果没跑，原因是什么
20. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 十、一句话提醒

你这轮真正要证明的，不是“我还能继续压几个指标”。

你这轮真正要证明的是：

- **你没有再把多簇耦合问题神话成单点**
- **你终于把“终点有 NPC 停留”单独拿出来打**
- **你还在继续收玩家右键真实入口，而不是写一堆局部 checkpoint 自我安慰**

---

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
