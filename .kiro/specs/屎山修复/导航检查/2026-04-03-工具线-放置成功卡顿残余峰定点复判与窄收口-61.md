# 工具线：只复判“放置成功那一下卡”的残余峰，不再混桥面/相机/场景大包

先接受已经成立的最新现场事实：

1. 这轮不是继续修整条导航主链。
2. 这轮也不是继续修 `Primary traversal`。
3. `导航检查` 父线程当前唯一主刀仍是：
   - `Primary.unity`
   - `PrimaryTraversalSceneBinder.cs`
   - `Primary traversal` 的 scene integration / live closure
4. `导航检查V2` 当前已冻结为：
   - accepted navigation version 的最终验收交接
   - 不再接这类运行时卡顿主刀
5. 当前真正和“放置成功时卡顿”强相关、而且已经在动刀的，是你这条工具线。
6. 用户最新明确裁定是：
   - 这个问题已经很多轮了，直到现在“没有任何被解决的迹象”；
   - 你必须回到现在的实际情况重新思考；
   - 并且要拿“之前树苗放置根本没有任何问题”这条历史基线来反推。

---

## 一、这轮唯一主刀

这轮唯一主刀只有一句话：

**只对“放置成功那一下卡”的残余峰做定点复判，并把责任重新压到一个最小真实热点上。**

要回答的不是“导航是不是相关”，而是：

1. 现在这个残余峰到底还在不在 `NavGrid` 刷新链里；
2. 还是已经转移到了树苗/箱子实例化后的运行时初始化链；
3. 如果你前几轮新增的 `local refresh contract` 已经不是主嫌疑，就不要继续堆新 contract，而要考虑 trim / rollback / 收窄。

---

## 二、先重新锚定当前真实现场

### 1. 你当前实际现场，不是“没动过”

你当前 `thread-state` 现在已经是 `PARKED`，最近一次切片是：

- `tree-stutter-and-bridge-precision-fix-v2`

也就是说，这轮不是让你继续假装自己还在旧的 `ACTIVE` 施工态里，而是要求你基于当前这份最新停车现场，重新决定要不要以更窄的一刀重新接回真实施工。

账面 own 路径报的是：

- `Assets/YYY_Scripts/Controller/TreeController.cs`
- `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs`
- `Assets/YYY_Scripts/Service/Placement/PlacementValidator.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
- `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`

但当前 working tree 真实脏文件还包括：

- `Assets/YYY_Scripts/World/Placeable/ChestController.cs`

也就是说，**你现在真实动刀范围已经超过 thread-state 账面 own 范围**。这轮必须先把这件事报实，不能继续一边改 `ChestController.cs`，一边账面假装它不在 slice 里。

### 2. 农田线历史上已经自己收过几轮，不要把旧账又算回“导航父线程没处理”

请先知晓农田线此前已经做过这些事：

1. 早期农田线碰过：
   - `GameInputManager.cs`
   - `PlayerAutoNavigator.cs`
   - 主要语义是箱子 `pending auto interaction`、走近到位后补交互、stop radius 收紧
2. 后续农田线已经自己切过：
   - 同一点击里的重复 `RefreshPlacementValidationAt(...)`
   - 树苗 `Stage 0` 无碰撞阶段误刷 `NavGrid`
3. 农田线先前的判断是：
   - 这一下卡顿和导航系统有关
   - 但不应直接甩给导航父线程大包

### 3. 你这条线已经继续做过的尝试

从你自己的 memory 和 working tree 现状看，你已经做过至少这几组尝试：

1. `NavGrid2D`
   - 新增 `TryRequestLocalRefresh(...)`
   - 新增 `RefreshGrid(Bounds ...)`
   - 新增 `RefreshGridRegion(...)`
2. `ChestController`
   - 放置出生后一帧改走局部刷新
   - 运行时状态变化也尝试用 bounds 局部刷新
3. `TreeController`
   - 聚合 bounds 刷新
   - `InitializeAsNewTree()` 简化
   - `Start()` / 延迟初始化继续收窄
4. `PlacementValidator`
   - 从 `FindObjectsByType<TreeController/ChestController>` 改成活动实例表
5. `PlacementManager`
   - 树苗放置链减少多余 `SetStage(0)`

但用户最新实际反馈是：

**做到这里，问题仍然没有任何被解决的迹象。**

所以这轮不准再把“已经做了很多结构补丁”偷换成“说明方向就是对的”。

---

## 三、必须回看的历史基线

请你强制回看“之前树苗放置根本没有任何问题”的老基线，至少对比到下面这一层：

### 参考基线

- `124caccc` 及其附近当时树苗放置可接受的版本

### 你必须回答的对比问题

1. 当时树苗放置成功后，真正跑了哪些步骤？
2. 当时 `PlacementManager -> TreeController` 的成功链为什么轻？
3. 当时 `TreeController.SetStage(0)` 的成本和现在相比，究竟多了哪几层？
4. 当时有没有现在这套：
   - local refresh contract
   - bounds 聚合刷新
   - deferred runtime sapling initialization
   - active instance registry
   - bridge / traversal / camera 一起混刀
5. 如果老版本在没有这些“新结构”的情况下反而不顿，说明现在真正的回归点是什么？

这轮如果不先把“旧版为什么不顿”讲明白，就不准继续叠新推测。

---

## 四、这轮允许与禁止

### 允许

只允许围绕下面这些文件和责任链做判断/最小收口：

1. `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs`
2. `Assets/YYY_Scripts/Controller/TreeController.cs`
3. `Assets/YYY_Scripts/Service/Placement/PlacementValidator.cs`
4. `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
5. `Assets/YYY_Scripts/World/Placeable/ChestController.cs`
6. 必要时：
   - `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`

### 禁止

1. 不准继续混修桥面
2. 不准继续混修 `TraversalBlockManager2D.cs`
3. 不准继续混修 `CameraDeadZoneSync.cs`
4. 不准继续碰 `Primary.unity`
5. 不准继续碰 `PrimaryTraversalSceneBinder.cs`
6. 不准把问题再泛化成“导航父线程回来接整包”
7. 不准再写“我已经补了 local refresh，所以方向肯定没问题”这种结构自证

---

## 五、你这轮必须回答的唯一问题

请只回答这个问题：

**在当前 working tree 下，用户仍然感知到的“放置成功那一下卡”，第一真实热点到底是哪一个？**

只能在下面几类里选，并给出代码证据：

### A. 仍然是 `NavGrid` 刷新主热点

如果你选 A，必须继续压实到更细一层：

1. 是 `TryRequestLocalRefresh(...)` 实际经常回退进 `RebuildGrid()`？
2. 还是 `Physics2D.SyncTransforms()` 本身就是当前主成本？
3. 还是 bounds 过大，导致所谓“局部刷新”本质上已经接近整图？

### B. 已经不是 `NavGrid`，而是实例化 / 初始化链

如果你选 B，必须继续压实到更细一层：

1. 是 `ChestController.Start()` / `TreeController.Start()` / 注册链 / 持久化链的哪一段？
2. 是 `RefreshTreePresentation(...)` / collider / sprite / shadow / registry 哪条？
3. 是 `ResumePreviewAfterSuccessfulPlacement()` 收尾链仍然在同帧做了重活？

### C. 当前 residual peak 是两段叠加，但主段已转移

如果你选 C，必须明确说：

1. 哪一段是现在第一主嫌疑
2. 哪一段只是放大器
3. 因此下一刀该砍谁，不该再砍谁

---

## 六、完成定义

### 形态 A：当前仍由你这条工具线继续收

必须同时满足：

1. 你能精确点名第一真实热点
2. 你能说明为什么它仍落在你当前工具线 own 范围内
3. 如果要继续改，只允许是一刀更窄的最小补口
4. 同时你要把 `ChestController.cs` 的 ownership 账说清：
   - 正式带回
   - 或明确剥离

### 形态 B：当前不该继续由你扩刀

必须同时满足：

1. 你能证明当前第一热点已经不是 `NavGrid` contract
2. 你能点名应回退/冻结的新增结构
3. 你能把问题精确回交给对应线程，而不是继续把 scope 做大

---

## 七、固定回执格式

### A1. 用户可读汇报层

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2. 用户补充层

必须额外显式回答：

1. 你最终选的是 `A / B / C` 哪类
2. 当前第一真实热点是哪一段
3. 为什么“之前树苗放置根本没问题”，现在却有问题
4. 你前几轮已经加上的 `local refresh / active instances / deferred init`，哪些现在被证明是真正有效，哪些还不能自证
5. `ChestController.cs` 当前为什么出现在真实脏改里，却没出现在你的 thread-state own 路径里

### B. 技术审计层

至少补：

1. changed_paths
2. 当前 `thread-state`
3. 本轮是否继续 `ACTIVE`
4. 是否碰了 `ChestController.cs`
5. 是否需要更新 own 路径
6. blocker_or_checkpoint
7. 一句话摘要

---

## 八、一句话总结

**这轮不再让你继续泛修“导航相关卡顿”，只让你把“放置成功残余峰”的第一真实热点重新压实，并用“旧版树苗为什么不顿”来裁定你现在这套大包修法到底还有没有继续扩张的资格。**

---

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
