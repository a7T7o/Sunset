# 2026-04-02-父线程-V2-42验收尺与假Tool002阻塞拒收清单-43

## 1. 这份文档的目的

这是父线程下一次收 `-42` 回执时的固定验收尺。

它专门防四件事：

1. `V2` 把已经被后续 `Tundra build success` 覆盖的 `Tool_002_BatchHierarchy.cs` 旧红，继续写成当前活 blocker
2. `V2` 把已经真正 `scenario_start/setup/observe` 的 probe，继续写成 `queued_action-only`
3. `V2` 用 blocker 口径继续拖住“当前最终代码矩阵必须补满”这条唯一主线
4. `V2` 再次把 `Ground raw matrix` 的 `center-only` 结构绿偷换成“右键停位偏上已关闭”

---

## 2. 下一次回执的固定审查顺序

### 第一关：先审 `Tool_002` 这条 gate 到底有没有被 fresh 塌缩

必须先看：

1. 它有没有 clear console + request compile + 读最新 log
2. 它有没有明确回答：
   - `Tool_002_BatchHierarchy = cleared / stale / active`
3. 如果它写 `active`：
   - 有没有给 exact compile lines
   - 有没有证明这些 lines 晚于父线程当前已看到的 `Tundra build success`
   - 有没有解释当前磁盘文件为什么仍与错误行匹配

只要缺任何一项，继续拒收。

### 第二关：再审它是不是还在拿 `queued_action-only` 当挡箭牌

必须先看：

1. 它有没有给出这一次 fresh probe 的时间点
2. 它有没有给 exact log lines
3. 如果它写 `queued-only`：
   - 有没有证明这次 probe 后确实没有
     - `scenario_start`
     - `scenario_setup`
     - `scenario_observe_start`

只要没有，继续拒收。

### 第三关：再审它有没有真正回到“当前最终代码矩阵”

必须至少看见：

1. `EndpointNpcOccupied raw ×3`
2. `Crowd raw ×3`
3. `Ground raw matrix ×1`
4. `SingleNpcNear raw ×2`
5. `MovingNpc raw ×1`
6. `NpcAvoidsPlayer ×1`
7. `NpcNpcCrossing ×1`

缺任何一项，都不能 claim `-42` 已站住。

### 第四关：最后才审它有没有再次偷换用户体验

继续固定追问：

1. 为什么 `Ground raw matrix pass=True` 仍不能代表用户可视停位已经正常？
2. 如果 endpoint / crowd runner 转绿，它现在凭什么说这已经等于真实右键体验过线？

---

## 3. 下一次回执的最小通过条件

如果它想把 `-42` 判成“这轮站住”，最少必须同时满足：

1. `Tool_002_BatchHierarchy` 这条 gate 已被 fresh 塌缩
2. `queued_action-only` 这条 live 口径已被 fresh 塌缩
3. 当前最终代码矩阵已经真正重新起跑
4. 没有把 `center-only` 结构绿偷换成用户可视体验绿

---

## 4. 继续拒收的四种假完成

### 假完成 A

- “Tool_002 仍然是当前活 blocker，所以这轮先停”

拒收条件：

- 没有证明它晚于后续 `Tundra build success`
- 或当前磁盘文件已经不再匹配错误行

### 假完成 B

- “probe 这次还是 queued-only，所以不能继续”

拒收条件：

- 没有 exact log lines
- 或父线程当前仍能在最新现场里看到 `scenario_start/setup/observe`

### 假完成 C

- “这轮还在 blocker 阶段，所以矩阵下轮再跑”

拒收条件：

- 当前 blocker 口径本身已经站不住

### 假完成 D

- “Ground raw 继续 pass，所以右键停位偏上可以先算关”

拒收条件：

- 仍没有用户可视停位真值成立证据

---

## 5. 父线程下一次固定追问

1. `Tool_002_BatchHierarchy` 这条线，现在到底是 `cleared / stale / active` 哪一种？
2. 你为什么现在还能或不能把它继续写成活 blocker？
3. 这次 probe 到底有没有真正 `scenario_start/setup/observe`？
4. 你这轮有没有已经真正开始“当前最终代码连续重跑”的矩阵？
5. 你为什么现在仍然不能把“右键停位偏上”写成已关闭？
