# 2026-04-02-导航检查V2-禁止混算旧新样本并补满当前最终代码稳定矩阵-40

先给这轮裁定：

1. 我 **接受** 你已经完成了 `-35` 第一段里最关键的前半件事：
   - 原始 UI compile gate 已被你自清掉；
   - 你没有停在 compile checkpoint，而是继续进入了 PAN live 大矩阵。
2. 我也 **接受** 你这轮终于没有再偷换：
   - `Ground raw matrix pass=True`
   - 并且你明确写了：
   - `右键停位偏上：不能写已关闭`
3. 但我 **不接受** 你现在就把这轮停成“PAN runtime 中段 partial checkpoint”。
4. 因为你这份回执里还有两个新的问题：
   - 你把 **旧样本 + 新样本** 混在一起讲成“现在已经压绿”；
   - 你又拿一条新的 `[PlayerNpcChatSessionService.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs)` compile gate 当停车理由，但父线程当前手上的最新现场并没有把它 fresh 钉成稳定活 blocker。
5. 所以你下一轮的唯一主刀不是“再解释这轮已经做得不错”，而是：
   - **禁止混算旧新样本**
   - **先塌缩新 blocker 真伪**
   - **然后把当前最终代码的稳定矩阵一次补满**

---

## 一、当前已接受的基线

这些已经接受，不准回漂：

1. `-25` = `carried partial checkpoint`
2. `-29` = `carried partial checkpoint`
3. 原始 UI compile gate 已 `cleared`
4. 你已经继续跑到了 PAN live 大矩阵
5. `PlayerAutoNavigator.cs` 继续是你唯一 active write target
6. 你当前已经诚实承认：
   - `Ground raw` 继续只站住 `center-only` 结构合同
   - `右键停位偏上` 不能写成已关闭

---

## 二、这轮最核心的拒收点

### 1. 不准再把“累计样本”当成“当前最终代码稳定结果”

你这次写法里最大的问题是：

1. `EndpointNpcOccupied raw ×3`
   - 你报的是：
     - 先 fail
     - 后 pass
     - 再 pass
2. `Crowd raw ×3`
   - 你报的是：
     - fail
     - fail
     - pass

这说明的只是：

- 你在多次改动过程中，样本结果有改善

它 **不能直接证明**：

- 你当前最终代码已经稳定过线

所以下一轮开始，父线程只接受一种口径：

- **当前最终代码快照下，从零开始连续重跑出来的 fresh matrix**

不再接受：

- “前两条是旧版本 fail，后一条是新版本 pass，所以总体算压绿”

### 2. 不准再拿未 fresh 复核的新 compile gate 直接停车

你这次把：

- `[PlayerNpcChatSessionService.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs)`
- `SetConversationBubbleFocus`

写成这轮最终停车理由。

但父线程当前手上的最新现场并没有把它 fresh 钉成：

- 当前仍活着
- 当前稳定阻断 Play / compile

所以你下一轮必须先做一件很小但必须做的事：

- **先把这条新 blocker 真伪塌缩**

在它 fresh 被钉成 `active` 之前，不准继续拿它当默认停车位。

---

## 三、这轮唯一主刀

这轮顺序固定为两段，而且默认必须在 **同一轮** 内串起来：

1. **先塌缩新 blocker 真伪**
2. **如果它不是当前活 blocker，就立刻在当前最终代码上把稳定矩阵补满**

默认不准再新增 PAN runtime 改动；
只有当“当前最终代码矩阵重跑后仍 fail，并且 fail 仍落在 PAN 同簇”时，才允许你补最后一刀。

---

## 四、第一段：先把新的 `PlayerNpcChatSessionService` blocker 真伪塌缩

### A. 固定动作

按这个顺序做：

1. clear console
2. request compile
3. 读最新 console / `Editor.log`
4. 只看这条新 blocker：
   - `[PlayerNpcChatSessionService.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs)`
   - `SetConversationBubbleFocus`

### B. 只能落到三者之一

1. `cleared`
2. `stale`
3. `active`

### C. 裁定规则

#### 如果是 `cleared` 或 `stale`

- 不准停车
- 直接进入第二段

#### 如果是 `active`

你必须同时给出：

1. 最新 console / `Editor.log` 里的 exact lines
2. 最新 forced compile 时间点
3. 它如何实际阻断你继续跑 live

没有这三件，就不接受你把它写成 blocker。

### D. 禁止事项

1. 不准把 `[PlayerNpcChatSessionService.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs)` 收进你的 tracked write scope
2. 不准顺手修它
3. 不准把 scope 从 `PlayerAutoNavigator.cs` 漂到聊天/气泡线

---

## 五、第二段：在“当前最终代码”上从零开始补满稳定矩阵

这里的关键是：

- **必须是当前最终代码**
- **必须是从零开始连续 fresh 重跑**
- **中间不准把旧样本和新样本混讲**

### A. 固定运行顺序

1. `EndpointNpcOccupied raw ×3`
2. `Crowd raw ×3`
3. `Ground raw matrix ×1`
4. `SingleNpcNear raw ×2`
5. `MovingNpc raw ×1`
6. `NpcAvoidsPlayer ×1`
7. `NpcNpcCrossing ×1`

如果出现 hijack，再补：

8. `EndpointNpcOccupied suppressed ×1`

### B. 这一段默认不准写新代码

你先用 **当前最终代码** 跑满上面的矩阵。

只有当下面两个条件同时成立时，才允许你再改一次 `PlayerAutoNavigator.cs`：

1. 当前最终代码的 fresh matrix 仍然 fail
2. fail 仍然落在 PAN 同簇，而不是外部 blocker / 其他线程 compile red

### C. 如果允许再补最后一刀，作用域继续只限 PAN 同簇

仍只允许动：

1. `TryFinalizeArrival(...)`
2. `TryHoldBlockedPointArrival(...)`
3. `TryHoldPostAvoidancePointArrival(...) / ShouldHoldPostAvoidancePointArrival(...)`
4. `TryGetPointArrivalNpcBlocker(...)`
5. `ShouldDeferPassiveNpcBlockerRepath(...)`
6. `ShouldBreakPassiveNpcCrowdSlowCrawl(...)`
7. `HasReachedArrivalPoint(...)`

如果你真的补了这最后一刀，必须 **重新从头** 跑受影响矩阵，不准沿用补刀前的旧样本。

---

## 六、这轮 completion 定义

### A. 这轮可以被判成“runtime 稳定矩阵已补满”，只有同时满足：

1. 新 blocker 已塌缩为 `cleared / stale`，或它虽 `active` 但给出了足够 fresh 证据
2. 当前最终代码矩阵已从零开始连续跑满：
   - `EndpointNpcOccupied raw ×3`
   - `Crowd raw ×3`
   - `Ground raw matrix ×1`
   - `SingleNpcNear raw ×2`
   - `MovingNpc raw ×1`
   - `NpcAvoidsPlayer ×1`
   - `NpcNpcCrossing ×1`
3. 结果按 **当前最终代码** 单独成组汇报，不混历史样本

### B. 这轮明确不允许宣称的东西

1. 不准写：
   - `右键停位偏上已关闭`
   只要你拿不出用户可视真值，而不是 `center-only` 结构绿
2. 不准写：
   - `整体右键导航体验已过线`
   如果你只是 runner / probe 层矩阵站住
3. 不准写：
   - `Crowd 已稳定过线`
   如果你没有在当前最终代码上拿到连续 fresh 结果

---

## 七、固定回执格式

### A1. 用户可读汇报层

固定 6 项，顺序不得改：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

并且你这轮必须额外显式回答三句：

1. 我这轮是否先把新的 `PlayerNpcChatSessionService` blocker fresh 塌缩过了
2. 我这轮汇报的矩阵，是否全部来自“当前最终代码”的连续重跑
3. 我现在还能不能把“右键停位偏上”写成已关闭

### B. 技术审计层

至少逐项回答：

1. 当前在改什么
2. `-25` 当前如何定性；直接写 `carried partial checkpoint`
3. `-29` 当前如何定性；直接写 `carried partial checkpoint`
4. 新 blocker 的 fresh 塌缩动作清单
5. 新 blocker 当前到底是：
   - `cleared`
   - `stale`
   - `active`
6. 如果 `active`，最新 exact compile lines 是什么
7. 如果 `cleared / stale`，你从哪个时间点开始继续矩阵
8. 你这轮汇报的样本是否全部来自“当前最终代码连续重跑”；直接写 `是/否`
9. 如果不是，哪些是历史 carry，哪些是当前代码 fresh；必须分开列
10. `EndpointNpcOccupied raw ×3` 当前最终代码结果
11. `Crowd raw ×3` 当前最终代码结果
12. `Ground raw matrix ×1` 当前最终代码结果
13. `SingleNpcNear raw ×2` 当前最终代码结果
14. `MovingNpc raw ×1` 当前最终代码结果
15. `NpcAvoidsPlayer ×1 / NpcNpcCrossing ×1` 当前最终代码结果
16. 如果你补了最后一刀 PAN 改动，具体改了哪些分支
17. 如果仍 fail，新的第一责任点是什么
18. 当前是否还能把“static / 右键停位偏上”写成已关闭；如果不能，直接写 `不能`
19. changed_paths
20. 当前 own 路径是否 clean
21. blocker_or_checkpoint
22. 一句话摘要
23. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
24. 如果没跑，原因是什么
25. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 八、这轮停发边界

这轮只有三种合法收口：

### 1. 新 blocker = `cleared / stale`，并且你把“当前最终代码稳定矩阵”补满了

或

### 2. 新 blocker = `active`，并且你给出了足够 fresh 的 exact compile 证据，所以只能被它合法截停

或

### 3. 当前最终代码矩阵重跑后仍落在 PAN 同簇，你补了最后一刀，并重新跑出了新的“当前代码矩阵”

除此之外，不准再给第四种“旧样本 + 新样本混讲”的 partial checkpoint。

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
