# 2026-04-02-导航检查V2-先自清Unity编译态再同窗完成PAN大闭环-35

先给这轮裁定：

1. 我接受你上一轮把 compile gate 从“也许 stale”压成了：
   - `Editor.log` 中最新 forced recompile 仍报真红
2. 但我 **不接受** 你因此直接停成“等别人清掉 UI 再说”。
3. 因为父线程已经额外替你查掉了 4 个常见源码侧借口：
   - 当前工作树里这两份文件都真实有方法本体；
   - 仓库里没有第二份同名类在抢编译；
   - 方法调用点与定义点的标识符字节完全一致，不是零宽字符/异体字符；
   - 文件括号结构正常、无 NUL 脏字节。
4. 所以你现在不能再把这件事描述成“外部文件还没修”。
5. 这轮唯一允许的前置动作是：
   - **先自清 Unity 当前编译态 / 导入态**
6. 如果自清后 gate 消失，你就必须 **在同一个窗口里** 继续 PAN 大闭环；
7. 只有在你做完这套自清恢复、gate 仍稳定真红时，才允许把它正式升级成：
   - `external live blocker`

---

## 一、当前已接受的基线

这些继续接受，不准回漂：

1. `-25` 继续只算 `carried partial checkpoint`
2. `-29` 继续只算 `carried partial checkpoint`
3. dedicated endpoint fake green 已剔掉
4. dedicated endpoint menu/toolchain 已接回
5. `PlayerAutoNavigator.cs` 继续是你唯一 active write target
6. 用户真人反馈仍有效：
   - 右键导航整体体验还是胡闹
   - 停位仍有“落在点击点上方”的体感
   - NPC 近距通道仍会漂、卡、徘徊

---

## 二、这轮唯一主刀

这轮顺序固定为两段，而且默认必须在同一轮内串起来：

1. **先做 Unity 编译态 / 导入态自清恢复**
2. **一旦恢复成功，立刻继续同窗完成 PAN 大闭环**

不准再停在“compile gate truth 已确认”的小回卡。

---

## 三、这轮第一段：只允许做“无 tracked 业务写入”的自清恢复

### A. 这轮你必须自己做的恢复动作

按下面顺序做，直到 gate 清掉或明确仍活：

1. clear console
2. request compile
3. 读最新 console / `Editor.log`
4. `Assets/YYY_Scripts/UI/Inventory/InventorySlotUI.cs` 与 `ToolbarSlotUI.cs` 做 **只读 reimport / refresh / asset database refresh**
5. 再 request compile
6. 再读最新 console / `Editor.log`
7. 如果仍红，再做一次 **Editor 级恢复动作**：
   - 退回 `Edit Mode`
   - 触发 domain reload / 脚本刷新 / editor restart 级别的恢复
8. 恢复后再 request compile，并再次读最新 console / `Editor.log`

### B. 这段动作允许什么

#### 允许

1. clear console
2. recompile
3. reimport / refresh / restart
4. 只读检查 `Editor.log`
5. 只读检查这两个 UI 文件

#### 禁止

1. 不准把 `InventorySlotUI.cs` / `ToolbarSlotUI.cs` 收进你的 tracked write scope
2. 不准顺手修这两个 UI 文件
3. 不准把 scope 借机漂到 UI 线

### C. 这一段完成后，你必须把 gate 定成三者之一

1. `cleared`
2. `active-but-self-recoverable-failed`
3. `stale` 

如果你最后仍写 `active`，必须同时给出：

1. 最新 forced recompile 的日志
2. 自清恢复动作清单
3. 自清后仍然红的最新证据

没有这三件，不准再写 `active blocker`。

---

## 四、如果 gate 已 cleared：这轮不准停车，直接继续 PAN 大闭环

只要你在这轮自清恢复后把 gate 清掉，就必须直接继续下面这段，不准中途停车报 checkpoint。

### A. 先跑这轮最小 fresh matrix

1. `EndpointNpcOccupied raw ×3`
2. `Crowd raw ×3`
3. `Ground raw matrix ×1`
4. `SingleNpcNear raw ×2`
5. `MovingNpc raw ×1`
6. `NpcAvoidsPlayer ×1`
7. `NpcNpcCrossing ×1`

如果出现 hijack，再补：

8. `EndpointNpcOccupied suppressed ×1`

### B. 然后只在 PAN 同簇补代码

只允许回：

1. `TryFinalizeArrival(...)`
2. `ShouldDeferActiveDetourPointArrival(...)`
3. `TryHoldBlockedPointArrival(...)`
4. `TryHoldPostAvoidancePointArrival(...) / ShouldHoldPostAvoidancePointArrival(...)`
5. `TryGetPointArrivalNpcBlocker(...)`
6. `HasReachedArrivalPoint(...)`

只有当 fresh 证据明确证明测量语义是第一责任点，才允许克制触及：

7. `GetPlayerPosition()`
8. `GetResolvedPathDestination()`

### C. 这轮 completion 继续保持“大刀”定义

这轮不是只钉一个小真因，而是要尽量把下面三条一起往可用闭环推进：

1. dedicated endpoint lingering
2. crowd / 双 NPC 通道 lingering / slow-crawl / 徘徊
3. ground / 右键停位可视真值

我这轮仍然要求：

- 至少两条出现用户可感知改善

---

## 五、如果 gate 仍 active：这轮也不能只报一句“等别人修”

如果你做完上面的自清恢复动作后，gate 仍稳定真红，你这轮回执必须额外补全下面 4 件事：

1. 你已经做了哪些恢复动作
2. 每一步后的最新 compile 结果
3. 为什么现在可以排除“只是导入态没刷新”
4. 为什么它现在必须升级成你 **无法自清** 的外部 live blocker

也就是说：

- 这轮允许被 blocker 卡住
- 但前提是你已经先做完“自清恢复”整套动作

---

## 六、这轮固定回执格式

### A1. 用户可读汇报层

固定 6 项，顺序不得改：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

并且你这轮必须额外显式回答两句：

1. 这轮我是否已经完成了“自清恢复”整套动作
2. 如果还没进 PAN 大闭环，是因为 gate 仍不可自清，还是因为我自己没有继续做

### B. 技术审计层

至少逐项回答：

1. 当前在改什么
2. `-25` 当前如何定性；必须直接写 `carried partial checkpoint`
3. `-29` 当前如何定性；必须直接写 `carried partial checkpoint`
4. 这轮自清恢复动作完整清单
5. 每一步动作后的：
   - console 结果
   - `Editor.log` 关键片段
6. compile gate 当前到底是：
   - `cleared`
   - `active-but-self-recoverable-failed`
   - `stale`
7. 如果仍 active，为什么现在可以排除“只是导入态没刷新”
8. 如果 gate 已 cleared，你从哪个时间点开始继续 PAN live
9. 这轮 fresh 先钉出来的第一真实 runtime 失败签名是什么
10. 它具体落在哪个 PAN 分支 / 条件上
11. 这轮具体改了 `PlayerAutoNavigator.cs` 的哪几个分支 / 条件
12. 它从哪个旧退出条件，改成了哪个新保持 / 恢复条件
13. `Ground raw matrix ×1` 结果
14. ground 的：
   - `Requested / Resolved / Transform / Rigidbody / Collider / Foot`
   - 以及你对“用户可视停位是否仍偏上”的直接裁定
15. `SingleNpcNear raw ×2` 结果
16. `MovingNpc raw ×1` 结果
17. `EndpointNpcOccupied raw ×3` 结果
18. `Crowd raw ×3` 结果
19. `NpcAvoidsPlayer ×1 / NpcNpcCrossing ×1` 结果
20. 如果仍 fail，新的第一责任点是什么
21. 当前是否还能把“static / 右键停位偏上”写成已关闭；如果不能，直接写 `不能`
22. changed_paths
23. 当前 own 路径是否 clean
24. blocker_or_checkpoint
25. 一句话摘要
26. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
27. 如果没跑，原因是什么
28. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 七、这轮停发边界

这轮只有两种合法收口：

### 1. gate 已自清成功，并且你继续做了 PAN 大闭环

或

### 2. 你已经做完整套自清恢复动作，gate 仍稳定真红，因此可以升级成无法自清的外部 live blocker

除这两种外，不准再给第三种“小 blocker 回卡”。

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
