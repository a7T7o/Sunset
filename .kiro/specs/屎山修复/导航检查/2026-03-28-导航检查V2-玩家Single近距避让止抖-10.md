# 2026-03-28-导航检查V2-玩家Single近距避让止抖-10

你现在继续作为 `导航检查V2` 实现线程施工。

这轮父线程不替你设计 patch，  
也不替你规定具体算法。  

父线程只做一次纠正和指导：

> 当前真正的问题，不是 cleanup，不是误触 NPC 交互，也不是再讲大架构；  
> 当前真正的问题是：玩家在单个静止 NPC 近距绕行时，虽然 probe 还能 pass，但体感上是在 `HardStop + 动作切换震荡` 里“抖着过”。

你要自己思考、自己决定怎么改，  
但必须只在这个热区里做后续开发。

---

## 当前已接受基线

1. `Raw Real Input Player Avoids Moving NPC`
   - `pass=True`
   - `pendingAutoInteractionAfterClick=False`
2. `Raw Real Input Crowd Pass`
   - `pass=True`
   - `pendingAutoInteractionAfterClick=False`
3. `NpcAvoidsPlayer`
   - `pass=True`
4. `NpcNpcCrossing`
   - `pass=True`
5. 父线程亲测后已明确：
   - 当前主要坏相不是“点击被 NPC 交互吃掉”
   - 当前主要坏相是 single 近距避让体验难看

## 父线程这轮亲测得到的硬证据

### `Raw Real Input Single NPC Near` 第 1 次

- `pass=True`
- `pendingAutoInteractionAfterClick=False`
- `hardStopFrames=26`
- `actionChanges=10`
- `blockOnsetEdgeDistance=0.087`

### `Raw Real Input Single NPC Near` 第 2 次

- `pass=True`
- `pendingAutoInteractionAfterClick=False`
- `hardStopFrames=26`
- `actionChanges=9`
- `blockOnsetEdgeDistance=0.108`

### 这组证据的解释

这说明现在 single 场景不是“根本走不过去”，  
而是“能过，但过得很难看”。  

所以这轮唯一主刀必须固定为：

> 压 single 近距静止 NPC 场景中的 `HardStop / actionChanges`，让玩家绕行不再踉踉跄跄

---

## 这轮唯一主刀

只做：

> 玩家 `SingleNpcNear` 近距避让止抖

更具体地说：

1. 你要继续锁 `PlayerAutoNavigator.cs`
2. 你要自己判断当前 single 场景里最该下刀的具体分支
3. 但你的目标必须直接服务于：
   - 减少不必要 `HardStop`
   - 减少 `PathMove / DetourMove / HardStop` 来回切换
   - 保持 raw click 下 `pendingAutoInteractionAfterClick=False`

父线程这轮不替你指定“应该改哪一个 if”，  
这是你自己要完成的判断。  

---

## 允许的 scope

这轮允许你读取和必要时修改：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
2. 如确属必要，可修改：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\NavigationAvoidanceRulesTests.cs`
3. 只读对照：
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`
   - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\Editor\NavigationLiveValidationMenu.cs`
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

---

## 明确禁止

1. 不准回漂 `cleanup-only`
2. 不准回漂 `NavigationLocalAvoidanceSolver` 大范围权重重调
3. 不准回漂 `TrafficArbiter / MotionCommand / DynamicNavigationAgent`
4. 不准把 `pendingAutoInteractionAfterClick=False` 的现状重新包装成“还是交互误触主导”
5. 不准再通过修改 runner 的 pass 条件、阈值、统计口径来“把数据变好看”
6. 不准碰：
   - `GameInputManager.cs`
   - `NPCAutoRoamController.cs`
   - `Primary.unity`
   - `SpringDay1WorldHintBubble.cs`
   - 字体 / UI / broad hygiene
7. 不准开长窗 live

---

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`

---

## 完成定义

只有满足下面任一条，这轮才算有效完成。

### 结局 A：single 近距止抖过线

你要明确给出：

1. `Raw Real Input Single NPC Near` fresh `pass=True`
2. `pendingAutoInteractionAfterClick=False`
3. `hardStopFrames` 已明显低于当前基线 `26`
4. `actionChanges` 已明显低于当前基线 `9~10`
5. 至少 1 条护栏验证仍然成立：
   - `Raw Real Input Crowd Pass`
   - 或 `Raw Real Input Player Avoids Moving NPC`

父线程建议你把“明显低于”至少做到：

- `hardStopFrames <= 8`
- `actionChanges <= 4`

你可以自己决定实现方式，  
但完成判断按这个口径对齐。

### 结局 B：single 还没过线，但责任点继续压窄

前提是你必须明确证明：

1. 这轮没有再靠改 runner 口径顶账
2. `hardStopFrames / actionChanges` 至少有实质下降
3. 新的第一责任点已经从“整条 single 近距避让链”压窄到一个更小的具体分支
4. 你能解释为什么这轮还不能 claim 体验改善

如果只是“还是 pass / fail，但我又补了一些结构”，这轮不算完成。

---

## live 纪律

1. 先编译、先最小静态检查
2. fresh live 只允许：
   - `Raw Real Input Single NPC Near`
3. 护栏最多再加 1 条：
   - `Raw Real Input Crowd Pass`
   - 或 `Raw Real Input Player Avoids Moving NPC`
4. 一旦拿到 `scenario_end`，立刻 `Stop`
5. 完成后必须退回 `Edit Mode`

---

## 固定回执格式

```text
已回写文件路径：
当前在改什么：
你自己判断的当前第一责任点：
这轮你选择打的具体分支：
`Raw Real Input Single NPC Near` 最新 fresh 结果：
`pendingAutoInteractionAfterClick` 是否仍为 `False`：
`hardStopFrames / actionChanges` 相比基线变化：
护栏验证结果：
changed_paths：
当前 own 路径是否 clean：
blocker_or_checkpoint：
一句话摘要：
```

---

## 一句话总口径

这轮别再证明“它还能 pass”，而是把 `SingleNpcNear` 从“抖着过”压成真正更自然的近距绕行。
