# 2026-03-26-导航检查V2复工准入后续工委托-06

你现在恢复为 `导航检查V2` 实现线程施工。  
`导航V2` 的锐评审核 / 自省 / 开工准入裁定这条支线到这里先停，不再继续占用实现入口。

这轮不是回去继续讲 Gemini，也不是再做一次“能不能开工”的讨论。

## 当前已接受的基线

1. `导航V2` 审核支线已经收口：
   - `000-gemini锐评-1.0.md` 的 `Path B` 边界已冻结；
   - `000-gemini锐评-1.1.md` 继续维持 `Path C`；
   - `导航检查V2` 的线程记忆边界已纠正；
   - 当前已经具备“从审核工作区切回实现线程”的治理批准。
2. 当前实现基线仍以首轮 accepted checkpoint 为准：
   - detour owner 保活最小闭环已接上；
   - `RealInputPlayerAvoidsMovingNpc` 已拿到首个有效执行窗口。
3. 上面的审核收口不是新的代码进展，不要把它误写成“导航体验已经整体过线”。

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2第二轮续工委托-03.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`

## 这轮唯一主刀

只做：

> 在同一 detour owner 闭环上，补 1 组 NPC 侧 fresh 证据，优先 `NpcAvoidsPlayer`

## 允许的 scope

这轮只允许你读取和必要时修改这些范围：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2复工准入后续工委托-06.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2第二轮续工委托-03.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
7. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
8. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\NavigationAvoidanceRulesTests.cs`

## 明确禁止

1. 不要再回 `导航V2` 审核工作区继续讲锐评边界。
2. 不要回漂 `NavigationLocalAvoidanceSolver` 权重。
3. 不要重开 `TrafficArbiter / MotionCommand / DynamicNavigationAgent / 全量 linearVelocity`。
4. 不要碰 `Primary.unity`。
5. 不要跑大窗 full live。
6. 不要把这轮扩成 `NpcNpcCrossing` 或三场同轮一起补。
7. 不要把“已有治理批准”误写成“这轮已经不用验证 NPC 侧”。

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已拿到 1 组 NPC 侧 fresh 结果，优先 `NpcAvoidsPlayer`。
2. 你已明确回答 detour owner 是否也在 NPC 侧进入有效执行窗口。
3. 如果未过线：
   - 你只允许在同一 owner keepalive / release 闭环里补 1 个最小口；
   - 然后最多再补 1 组同场景 fresh。
4. 一旦拿到结论，立刻 `Stop`，并退回 `Edit Mode`。
5. 你已把本轮结果追加到：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

## 固定回执格式

```text
已回写文件路径：
当前在改什么：
`NpcAvoidsPlayer` 最新 fresh 结果：
detour owner 是否已在 NPC 侧进入有效执行窗口：`yes / no`
changed_paths：
当前 own 路径是否 clean：
blocker_or_checkpoint：
一句话摘要：
```

## 一句话总口径

审核支线先停，重新回到实现主线；这轮只拿 `NpcAvoidsPlayer` 的 NPC 侧 fresh 结果，证明 detour owner 不是只在玩家侧偶然活了一次。
