# 导航检查V2：保持停车，冻结最终验收交接，只等专用 cleanup 再唤醒

先接受当前已经成立的事实：

1. 你这条线已经完成：
   - 最终验收交接定性
   - accepted navigation version 与 `Primary traversal` 的分账
2. 你这条线当前没有完成：
   - legal-sync
3. 当前第一真实 blocker 仍是：
   - own-root remaining dirty / untracked
4. 当前真正剩余的业务主线不在你这里，而在父线程的：
   - `Primary traversal`

---

## 一、这轮唯一主线

这轮不再继续修导航，也不再继续跑 live。

这轮唯一主线只有一句话：

**保持 `PARKED`，冻结当前最终验收交接结论，不再主动 reopen；只有在后续明确开“专用 sync-cleanup slice”时才重新接单。**

---

## 二、这轮明确禁止

1. 不准重跑 `FinalPlayerNavigationAcceptancePack`
2. 不准继续修 `PlayerAutoNavigator.cs`
3. 不准继续改 runner/menu 阈值、case 语义、pack 结果
4. 不准把 `Primary traversal` 混回你这条 handoff
5. 不准继续把 targeted probe 红面写成 release blocker
6. 不准自己扩大 own 范围去吞 foreign dirty

---

## 三、这轮你唯一要守住的 truth

以后如果这条线被问到，只准继续按下面四层报实：

1. 真实入口体验层：
   - 当前玩家导航版本已被用户认可

2. targeted probe 层：
   - `PassableCorridor ×3`
   - `StaticNpcWall ×3`
   仍红，但只是后续 polish 诊断项

3. 结构工具层：
   - runner/menu
   - `FinalPlayerNavigationAcceptancePack`
   已完成

4. 分账层：
   - `Primary traversal` 属于父线程独立切片
   - 不属于你这条线的已完成范围

---

## 四、后续只有什么情况才允许你重新开工

只有下面这一种情况，才允许你从 `PARKED` 转回真实施工：

1. 用户或治理位明确开一条新的、独立的 `sync / cleanup` 切片
2. 并且明确告诉你：
   - 你这轮只处理 own-root remaining dirty / untracked
   - 不再碰 runtime

除此之外，不准你因为“看起来还没 clean”就自己 reopen。

---

## 五、如果后续被唤醒，固定回执格式

### A1. 用户可读汇报层

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2. 用户补充层

必须额外显式回答：

1. 当前玩家导航版本是否仍已被用户认可
2. targeted probe 红面是否仍保留
3. 这些红面当前是不是 release blocker
4. `Primary traversal` 是否仍不属于你这轮范围
5. 这轮是不是专用 sync-cleanup；如果不是，为什么你仍保持 `PARKED`

### B. 技术审计层

至少补：

1. changed_paths
2. 这轮是否重跑 live；如果没有，为什么合法不需要
3. 当前 own 路径是否 clean
4. blocker_or_checkpoint
5. `Begin-Slice / Ready-To-Sync / Park-Slice`
6. 当前 live 状态

---

## 六、thread-state

```text
[thread-state 接线要求｜当前状态]
这轮结束时保持：
  PARKED

如果后续被明确分配专用 sync-cleanup slice，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1

第一次准备 sync 前，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1

若再次停下或被 blocker 卡住，继续跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1
```

---

## 七、一句话总结

**下一轮 `导航检查V2` 不再继续动 runtime，也不再重跑 pack；它只保持 `PARKED`，冻结当前最终验收交接 truth，等后续真的开专用 cleanup slice 再唤醒。**
