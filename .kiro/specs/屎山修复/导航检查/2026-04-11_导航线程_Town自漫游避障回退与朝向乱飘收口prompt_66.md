# 2026-04-11｜导航线程｜Town 自漫游避障回退与朝向乱飘收口 prompt - 66

这轮不要继续把 `resident scripted control` 冻结链当唯一主刀。

上一刀已经确认并补掉了一个真实 own 漏洞：

- [NPCAutoRoamController.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs) 的 `OnDisable()` 在 resident scripted control 持有期间不该再走 `StopRoam()` 清空恢复意图。

这刀可以认，但它不是用户当前主痛点。

---

## 用户刚补充的最新现场

### 现实基线

1. `Town` 才是主现场，因为这里才有常驻 NPC。
2. `001~003` 走路本身基本正常，不会抽搐、不会乱摆头。
3. 但 `001~003` 的避障/避让也回退了。
4. 其它常驻 NPC 仍然是问题青年：
   - 路径大方向是对的
   - 不会真的回头乱走
   - 但朝向会乱飘、左右晃、像摇头晃脑
5. 小动物也疑似有同类问题。
6. 剧情内预设走位基本正常；只要进入 autonomous / free-roam，自走状态才开始出问题。

### 一句话钉死

这轮不是查“冻结”。

这轮要查的是：

**为什么 Town 常驻 NPC 一进入 autonomous / free-roam，就出现“避障回退 + 朝向乱飘”，而剧情走位不会。**

---

## 本轮唯一主刀

只收：

### `autonomous / free-roam` 这条链里，`shared avoidance / local avoidance / facing contract` 的第一真问题

必须先回答：

1. `001~003` 为什么能基本走稳但避障回退？
2. 其它常驻 NPC 为什么会在“路径没错”的前提下朝向乱飘？
3. 这两个坏相是不是同一个第一真问题的两种表象？
4. 第一真问题到底落在：
   - `NPCAutoRoamController.TryHandleSharedAvoidance(...)`
   - `NavigationAgentRegistry`
   - `NavigationLocalAvoidanceSolver`
   - `NPCMotionController` 的 facing contract
   - prefab/profile/runtime owner 差异
   里的哪一层

---

## 明确允许的 scope

### 允许只读核对

- `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
- `Assets/YYY_Scripts/Controller/NPC/NPCMotionController.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationAgentRegistry.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationLocalAvoidanceSolver.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationAvoidanceRules.cs`
- `Assets/222_Prefabs/NPC/*.prefab`
- `Assets/222_Prefabs/FarmAnimals/*.prefab`

### 允许真实落代码

如果第一真问题仍在导航 own，只允许在下面文件里做最小修补：

- `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
- `Assets/YYY_Scripts/Controller/NPC/NPCMotionController.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationAgentRegistry.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationLocalAvoidanceSolver.cs`
- `Assets/YYY_Scripts/Service/Navigation/NavigationAvoidanceRules.cs`

---

## 明确禁止的漂移

1. 不要再回去主刀 `resident scripted control`。
2. 不要再把 `显示层日志更全了` 当成主成果。
3. 不要扩回 `NavGrid2D / PlayerAutoNavigator / TraversalBridge`。
4. 不要碰 scene / prefab 大批量治理。
5. 不要做“统一导航大重构”。
6. 不要把“剧情里正常”偷换成“系统整体没问题”。

---

## 完成定义

这轮完成至少满足其一：

### A. 第一真问题仍在导航 own

你要：

1. 给出 exact root cause；
2. 做最小补口；
3. 跑最小代码层验证；
4. 明确回答：
   - 为什么这刀会同时影响 `避障回退`
   - 以及 `朝向乱飘`

### B. 第一真问题不在导航 own

你要：

1. 指出 exact owner / exact file / exact method / exact condition；
2. 说明为什么 `001~003` 与其它常驻 NPC / 小动物会分化；
3. 停在 exact blocker，不要越权代修。

---

## 强制回答格式

### A1 保底六点卡
1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层
必须额外明确：

1. `001~003` 当前站住了什么
2. 其它常驻 NPC 当前坏在什么
3. 小动物当前与 NPC 是否同根
4. 你这轮为什么判断“避障回退”和“朝向乱飘”是同根或不同根
5. 你这轮最薄弱、最可能看错的点
6. 你的自评

### B 技术审计层
至少包含：

- 当前在改什么
- changed_paths
- 当前 failing scene / failing NPC group
- exact avoidance / facing caller chain
- code_self_check
- pre_sync_validation
- 当前是否可以直接提交到 `main`
- blocker_or_checkpoint
- 当前 `own path` 是否 clean
- 一句话摘要

---

## thread-state

如果这轮继续真实施工：

1. 先跑 `Begin-Slice`
2. 第一次准备 sync 前跑 `Ready-To-Sync`
3. 如果这轮停下或卡住，跑 `Park-Slice`

回执里补：

- `Begin-Slice / Ready-To-Sync / Park-Slice` 是否已跑
- 如果没跑，原因是什么
- 当前 live 状态是 `ACTIVE / READY / PARKED` 还是被 blocker 卡住

---

## 一句话钉死

你这轮不是来继续证明“我更会打印 NPC 日志了”。

你这轮是来钉死：

**为什么 Town 常驻 NPC 一进入 autonomous / free-roam，就会同时出现“避障回退”和“朝向乱飘”。**
