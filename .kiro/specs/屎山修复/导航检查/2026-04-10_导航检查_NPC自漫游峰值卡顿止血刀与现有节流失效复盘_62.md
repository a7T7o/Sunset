当前已接受的基线：

1. 这次不是泛导航评审，也不是再回到“玩家/NPC/统一导航长期重构”大题。
2. 当前唯一现场是用户已经给出的这一次真实 profiler 峰值：
   - 发生在 `day1` 的 `Town` 围观剧情过完之后
   - 以及玩家跟着村长继续在 `Town` 走的时候
   - 用户明确观察到：剧情里导演驱动走位、剧情散会后回 anchor 还相对正常；真正发疯的是 NPC 自漫游
3. 当前主控审计后的裁定也必须继承，不要推翻：
   - 你抓对了大方向：病灶大概率在 `NPC 自漫游 driver`
   - 但你上一轮还不够深，不能直接拿“从零开始的泛方案”当施工口令
   - 因为当前代码里已经存在一部分止血机制，你必须先解释“为什么现有止血没挡住这次 spike”
4. 用户当前要的是：最快把这次 demo 级峰值卡顿、撞墙硬走、一步三回头压住。
5. 这轮不要再回“最快修复 vs 全部修复”的空方案对比；现在已经进入直接施工轮。

当前唯一主刀：

只收 `NPC 自漫游坏 case 的 demo 级止血刀`，并且必须同时完成两件事：

1. 先把“现有节流为什么失效”钉死
2. 再把真正能压住这次峰值的最小修复直接落进代码

不要扩成统一导航大重构，不要顺手做玩家导航，不要回吞剧情导演线。

你这轮必须先继承并且不要推翻的硬事实：

1. 用户给的三张图里，热点峰值确实压在：
   - `NPCAutoRoamController.Update()`
   - 伴随大量 `Physics2D` 查询和明显 GC
2. 但当前 live 代码里并不是“所有阻挡检测都还在直接主链用 `OverlapCircleAll`”：
   - 例如 [NavGrid2D.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs) 当前主阻挡检测已经是 non-alloc `Physics2D.OverlapCircle(..., _colliderCache)`
3. 当前 live 代码里也并不是“完全没有止血”：
   - [NPCAutoRoamController.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs) 里已经有：
     - `REBUILD_REQUEST_DEDUPE_WINDOW_SECONDS`
     - `TryAcquirePathBuildBudget()`
     - `BLOCKED_ADVANCE_RECOVERY_COOLDOWN`
     - `PHYSICS_HEAVY_DECISION_REUSE_SECONDS`
     - `PATH_BUILD_FAILURE_* backoff`
4. 所以你这轮不能把问题表述成“当前完全没有 dedupe/backoff，所以我要从头加”。
5. 更准确的任务是：
   - 现有止血为什么没有压住这次坏 case
   - 哪几个点必须继续收紧，才能在 demo 现场快速止血

允许 scope：

- [NPCAutoRoamController.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs)
- [NPCMotionController.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCMotionController.cs)
- [NavGrid2D.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs)
- 最小验证脚本 / tests / profiler 取证补口

明确禁止的漂移：

1. 不要扩成“统一导航重构下一阶段”
2. 不要顺手改玩家导航
3. 不要回吞 `day1 / Town / NPC 剧情导演` 主逻辑
4. 不要再写一版泛泛的“最快修复/全部修复方案说明书”
5. 不要只说“自漫游 driver 有问题”，却不解释当前已有止血为什么失效
6. 不要把旧版本认知和当前 live 代码混写

这轮先做的第一件事，不是开改，而是补齐 2 个必须钉死的判断：

1. 解释当前已有止血为什么没挡住这次 spike
   你必须明确回答：
   - `REBUILD_REQUEST_DEDUPE_WINDOW_SECONDS`
   - `TryAcquirePathBuildBudget()`
   - `BLOCKED_ADVANCE_RECOVERY_COOLDOWN`
   - `PHYSICS_HEAVY_DECISION_REUSE_SECONDS`
   - `PATH_BUILD_FAILURE backoff`
   这几条在用户这次现场里，到底是哪条没命中、哪条命中了但力度不够、哪条被别的坏路径绕开了。

2. 解释 profiler 图里的 `OverlapCircleAll + GC 15.3MB` 和当前 live 源码为什么对不上
   你必须明确给出下面三选一，不允许糊弄：
   - `A`：这次峰值真正 alloc 的不是 `NavGrid2D.IsPointBlocked` 主链，而是别的 `OverlapCircleAll` / 其它分配点
   - `B`：当前 profiler 对应的是旧 build / 旧二进制，不是当前源码现场
   - `C`：虽然主链已 non-alloc，但 buffer resize / 其它路径仍造成等价分配，并给出具体链路

如果这两个判断没钉死，就不要把“止血刀已做对”往外报。

然后才进入这轮唯一施工：

把 `NPC 自漫游坏 case` 压成 demo 可接受的止血行为，至少覆盖下面 4 条：

1. 封闭区域 / 无效目标 / 连续走不通
   - 不能继续硬走、硬重试、硬重建
   - 必须更早失败、更早放弃当前 roam 目标，进入 pause 或重新选点

2. blocked / no-progress / same-position 抖动
   - 不能让同一个 NPC 在极短时间内反复跑重查询、重建路径、blocked recovery
   - 现有节流要么收紧，要么补新的短路条件，但必须是基于你上面钉死的失效原因

3. 每帧重决策过载
   - 现在代码里已经有一部分 reuse 机制；你这轮要把它收成真正能挡这次坏 case 的程度
   - 目标不是“理论上复用”，而是用户这次 spike 现场真的降下来

4. 一步三回头 / 晕头转向
   - [NPCMotionController.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCMotionController.cs) 的朝向不能继续被噪声速度乱带
   - 这轮要把朝向优先收敛到“最终提交的移动决策 / 有效 move command”，而不是 blocked/no-progress 时仍跟着观测速率抖

完成定义：

这轮完成，不是“代码改了几处”，而是至少满足下面这些：

1. 你已经解释清楚“当前已有止血为什么失效”
2. 你已经解释清楚 `OverlapCircleAll` 与当前源码现场的差异
3. `NPC 自漫游` 在封闭区域 / 连续走不通 / 撞墙时，不再无限硬算
4. 峰值报错 / 卡顿 / 大抖头至少有明确止血效果
5. 剧情导演驱动走位、剧情散会回 anchor 这两条既有相对正常的链路，不被你这轮误伤

这轮的验证要求，不允许只给静态推断：

你至少要给出一份真正能裁判的证据卡，优先包括：

1. 这次坏 case 的 before / after
   - `NPCAutoRoamController.Update()` 峰值
   - 相关 `Physics2D` 查询量
   - 相关 GC 量
2. 至少一个真实现场或最小 live/probe 证据，证明：
   - NPC 在坏 case 下不再无限硬走
   - 朝向不再一步三回头
3. 如果你拿不到 live 证据，只能诚实报 blocker：
   - 是工具问题
   - 还是 Unity 现场问题
   - 不是一句“静态上应该好了”

这轮结束后，仍然明确没做完的内容也请报实：

- 统一导航大重构
- 玩家/NPC/怪物/牲畜统一 traversal contract
- 更长期的结构性收薄

这些都不是这轮的主刀，不要混进“已完成”。

固定回执格式：

### A1. 保底六点卡

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2. 用户补充层

至少补清楚这 4 件事：

1. 现有止血为什么失效
2. `OverlapCircleAll` 与当前源码为什么对不上
3. 这轮最快止血刀具体压住了哪些坏 case
4. 你这轮最薄弱、最可能还没压住的地方是什么

### B. 技术审计层

- changed_paths
- before/after 指标
- 关键 profiler / live / probe 证据
- No-Red 证据卡 v2
- 当前是否可直接 sync
- 当前 git status / own path 是否 clean
- 当前 thread-state

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
