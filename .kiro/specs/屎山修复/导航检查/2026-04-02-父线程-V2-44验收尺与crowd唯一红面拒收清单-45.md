# 2026-04-02-父线程-V2-44验收尺与crowd唯一红面拒收清单-45

## 1. 这份文档的目的

这是父线程下一次收 `-44` 回执时的固定验收尺。

它专门防三件事：

1. `V2` 明明只剩 crowd 红，却又把 scope 漂去别的链
2. `V2` 把 crowd 只改善一点，就包装成“整体右键导航体验过线”
3. `V2` 再次把 `Ground raw matrix` 的 `center-only` 结构绿偷换成“右键停位偏上已关闭”

---

## 2. 下一次回执的固定审查顺序

### 第一关：先审它是不是只动了 PAN crowd 同簇

必须先看：

1. 这轮是否只改了 `[PlayerAutoNavigator.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs)`
2. 具体改动是否仍落在：
   - `HasPassiveNpcCrowdOrCorridor(...)`
   - `ShouldDeferPassiveNpcBlockerRepath(...)`
   - `ShouldBreakPassiveNpcCrowdSlowCrawl(...)`
   - 或与它强绑定的一小步

如果 scope 漂去 runner / solver / NPC / scene / UI，继续拒收。

### 第二关：再审 crowd 是否真的在当前最终代码上重跑了

必须至少看见：

1. `Crowd raw ×3`
2. 三条都是当前最新代码的 fresh 样本
3. 没有混入补刀前历史 carry

如果缺任一项，继续拒收。

### 第三关：再审护栏是否还活着

必须至少看见：

1. `EndpointNpcOccupied raw ×1`
2. `Ground raw matrix ×1`
3. `SingleNpcNear raw ×1`
4. `MovingNpc raw ×1`
5. `NpcAvoidsPlayer ×1`
6. `NpcNpcCrossing ×1`

只要 crowd 变绿但护栏回归，继续拒收。

### 第四关：最后才审它有没有再次偷换体验

继续固定追问：

1. 就算 crowd 变绿了，它为什么仍不能把“右键停位偏上”写成已关闭？
2. 它现在凭什么说“crowd 绿了”不等于“整体玩家右键导航体验过线”？

---

## 3. 下一次回执的最小通过条件

如果它想把 `-44` 判成“这轮站住”，最少必须同时满足：

1. `Crowd raw ×3` 在当前最新代码上已 fresh 起跑
2. crowd 至少被进一步压缩，最好直接压绿
3. 护栏没有回归
4. 没有偷换“右键停位偏上已关闭”

---

## 4. 继续拒收的四种假完成

### 假完成 A

- “crowd 虽然还红，但其它都绿了，所以先算差不多”

拒收条件：

- `Crowd raw ×3` 仍红

### 假完成 B

- “我去改了别的链，因为感觉 crowd 本质不在这里”

拒收条件：

- 没有 fresh 证据证明第一责任点已离开 PAN crowd 同簇

### 假完成 C

- “crowd 过了一条，所以剩下两条可以先不管”

拒收条件：

- `Crowd raw ×3` 没有完整结果

### 假完成 D

- “Ground raw 继续 pass，所以停位偏上应该可以先算关”

拒收条件：

- 仍没有用户可视停位真值成立证据

---

## 5. 父线程下一次固定追问

1. `Crowd raw ×3` 这轮到底是几绿几红？
2. 你这轮是不是只改了 `PlayerAutoNavigator.cs` 的 crowd 同簇？
3. 护栏有没有任何一条回归？
4. 你为什么现在仍然不能把“右键停位偏上”写成已关闭？
5. 如果 crowd 绿了，你为什么仍然不能直接 claim 整体玩家导航体验过线？
