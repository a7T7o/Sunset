# 2026-04-01-父线程自工单-静态点契约回归与点击点上偏复核-19

这轮父线程不要继续写 runtime 补丁，也不要去替 `导航检查V2` 改动态避让。

你自己的唯一职责只有一个：

把“静态点契约 / 点击点上偏”这条验收线守住，明确判断它有没有被 `-17` 那刀实质回退。

---

## 一、当前已接受的基线

这些我接受：

1. 你自己的 static runner / static menu 工具链已经收到了可信状态：
   - `case_start origin` 已恢复正常
   - `all_completed` 已能打出
   - static runner 已不是第一责任点
2. 你已经拿到过明确 fresh verdict：
   - `centerDistance≈1.04~1.12`
   - `rigidbodyDistance / transformDistance≈0.155~0.161`
3. `导航检查V2 -17` 只证明了一件事：
   - premature inactive 旧链收掉了

这些不接受：

1. 把 `-17` 的 `Rigidbody/Transform` 特判直接当成“普通点契约已经修好”；
2. 在没有重新 fresh 复核前，就默认“静态修复没有被回退”；
3. 把 targeted probe 的局部 pass 说成“用户实际点击点对齐体验已成立”。

---

## 二、这轮唯一主刀

只做这一件事：

在 `导航检查V2` 下一轮补口回来后，用父线程自己的 static / 验收链，重新裁定两件事：

1. 静态点导航契约有没有真正恢复；
2. 玩家实际到达位置是否还存在“落在点击点上方/下方错层”的问题。

这轮不是去修它，
而是去做父线程该做的 acceptance / rejection 裁定。

---

## 三、允许的 scope

只允许触碰：

1. 父线程自己的文档 / memory
2. 如果真的进入 live 复核，只允许读：
   - `Tools/Sunset/Navigation/Run Static Point Accuracy Validation`
   - console / `Editor.log`
   - 现成的 gizmo / 路径证据

不允许碰：

1. `PlayerAutoNavigator.cs`
2. `NavigationLocalAvoidanceSolver.cs`
3. `NavigationStaticPointValidationRunner.cs`
4. `NavigationStaticPointValidationMenu.cs`
5. scene / prefab / NPC runtime

---

## 四、执行顺序固定

等 `导航检查V2 -19` 回来后，按这个顺序：

1. 先看它有没有继续保留“普通点走 `Rigidbody/Transform` 特判”；
2. 若 compile clean，则只拿 1 个短 Unity live 窗口；
3. 只跑：
   - `Tools/Sunset/Navigation/Run Static Point Accuracy Validation`
4. 再补 1 组最小用户视角裁定：
   - 普通地面点点击的到点证据
   - 如果仍有“终点 NPC 占位代理”证据，也一并对照
5. 最后只回答：
   - static 契约有没有恢复
   - 点击点上偏有没有消失
   - 如果没有，当前 rejection 应直接打回哪一条 runtime 链

---

## 五、这轮完成定义

### 结局 A：可以接受

必须同时满足：

1. `StaticPointCase1 / StaticPointCase2` 不再出现明显的 `centerDistance` 与 `rigidbodyDistance / transformDistance` 错层；
2. 用户视角下，普通点击的实际到点不再给出“人停在点击点上方/下方”的明显感受；
3. 这轮结论能明确支持：
   - “静态修复没有被撤回”
   - 或者
   - “之前的回退已被重新收回”

### 结局 B：拒收

你必须明确给出：

1. 哪一层被回退了：
   - 静态契约
   - 用户可见点击点对齐
   - 或两者都被回退
2. 当前 rejection 应直接打回给 `导航检查V2` 的哪条链；
3. 为什么不能因为其它 probe pass 就放过这条线。

---

## 六、固定回执格式

### A1. 用户可读汇报层

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### B. 技术审计层

1. 当前在做什么
2. `导航检查V2` 当前 runtime 口径里，普通点是否仍保留 `Rigidbody/Transform` 特判
3. static fresh 是否真实重跑
4. `StaticPointCase1 / StaticPointCase2` 当前结果
5. `centerDistance / rigidbodyDistance / transformDistance` 关键值
6. 当前用户视角“点击点上偏”是否仍成立；证据是什么
7. 当前 rejection / acceptance 应该落到哪条链
8. changed_paths
9. 当前 own 路径是否 clean
10. blocker_or_checkpoint
11. 一句话摘要

---

## 七、一句话提醒

父线程这轮不是继续修代码。

父线程这轮要做的是：

别让 “局部旧锅收掉了” 这件事，
掩盖掉 “静态点契约和实际点击对齐又被回退了”。
