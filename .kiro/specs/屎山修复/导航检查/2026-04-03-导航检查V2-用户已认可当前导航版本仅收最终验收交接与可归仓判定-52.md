# 导航检查V2：用户已认可当前导航版本，只收最终验收交接与可归仓判定

## 一、这轮唯一主线

用户刚刚已经给出新的顶层裁定：

- “现在的导航其实没什么好挑剔的了”
- “就是避让有点走得太多”
- “但是完全可以接受”
- “我认为这个版本我认可了”

所以你这轮**唯一主线**已经不再是继续修 `PlayerAutoNavigator.cs`、继续压 `PassableCorridor / StaticNpcWall` 两簇红面，也不是继续追求 `final acceptance pack` 全绿。

你这轮只收一件事：

**把你这条 runner/menu 验收线程，转换成“当前版本已被用户真实入口体验认可”的最终验收交接与可归仓判定。**

---

## 二、这轮明确禁止

1. **不准**再碰：
   - `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`
   - 任何 PAN runtime
   - 任何 NPC runtime
   - scene / prefab / hotfile

2. **不准**再把这轮主目标写成：
   - 继续修 `PassableCorridor`
   - 继续修 `StaticNpcWall`
   - 继续追 `final acceptance pack` 全绿

3. **不准**为了让矩阵更好看而：
   - 偷改 pack 阈值
   - 偷改 case 语义
   - 偷把 targeted probe 红面写成“已关闭”

4. **不准**把：
   - `GroundRawMatrix` 绿
   - 或 `center-only` 结构绿
   写成：
   - “右键停位偏上已关闭”

5. **不准**继续长时间占 Unity live 窗口。
   如果这轮不需要 fresh live，就不要再重跑 pack。

---

## 三、你这轮必须先接受的新定性

从现在开始，这条线的三层完成定义必须改成下面这个口径同时报实，不准再混成一个结论：

1. **真实入口体验层**
   - 当前版本：**用户已认可 / 可接受**
   - 这是新的最高优先级事实

2. **targeted probe / 局部验证层**
   - `final acceptance pack` 仍有红面：
     - `PassableCorridor ×3`
     - `StaticNpcWall ×3`
   - 这些红面现在只能继续被报成：
     - **后续可打磨的诊断项 / polish 债**
   - **不能**再继续被报成“当前版本不可用”

3. **结构 / 验收工具层**
   - 你的 `final acceptance pack` 入口、runner/menu、fresh live 取证链已经完成

换句话说，你这轮要交的是：

**“用户终验已认可，但 targeted probe 仍保留两个诊断红面；runner/menu 验收工具已成型。”**

---

## 四、这轮只允许做的动作

只按下面顺序做，不准加戏：

### 1. 先只读复核

只读回看以下事实是否一致：

- 你上一轮自己的最新回执
- `FinalPlayerNavigationAcceptancePack` 已完成这件事
- 当前用户最新认可裁定
- 当前你 own 的文件仍只限于：
  - `NavigationLiveValidationRunner.cs`
  - `NavigationLiveValidationMenu.cs`
  - 本轮 memory / handoff 文档

### 2. 再收一份“最终验收交接稿”

你这轮必须把自己的结果收成一份**明确面向用户验收闭环**的交接材料或 memory 补记，核心必须写清：

1. 当前版本为何可以被视为：
   - **用户真实体验已接受**
2. 为什么同时又仍保留：
   - `PassableCorridor / StaticNpcWall` targeted probe 红面
3. 这些红面现在的正确身份是什么：
   - **后续 polish 诊断项**
   - 不是本轮 release blocker
4. 哪些事仍然不能 claim：
   - `右键停位偏上已关闭`
   - `final acceptance pack 全绿`

### 3. 最后只做一次可归仓判定

这轮只允许做一次最小 `Ready-To-Sync` 判定，目标不是强行归仓，而是把状态压清：

- 如果你 own 路径已经能合法 sync，就同步你 own 的 runner/menu + 这轮文档
- 如果 `Assets/YYY_Scripts/Service/Navigation` 同根 blocker 仍在，而且是别的线程或父线程 remaining dirty：
  - **不要**清 foreign dirty
  - **不要**扩大 own 范围
  - 直接把状态报成：
    - `用户验收事实已完成`
    - `归仓被同根 blocker 合法阻断`

### 4. 如果不需要 live，就别再跑

这轮默认**不需要**再 fresh 跑 `final acceptance pack`。

只有在你发现：

- 你上一轮的 pack 事实本身写错了
- 或当前交接稿缺一条关键 exact 结果

才允许最小补读 `Editor.log`，但仍然**不要**为了“再追一次更绿结果”去重跑。

---

## 五、这轮完成标准

你这轮只有满足下面全部条件，才算完成：

1. 你已经明确接受并写入：
   - 用户真实入口体验已认可当前版本

2. 你已经明确区分三层：
   - 真实入口体验：已认可
   - targeted probe：仍有两个诊断红面
   - 结构工具层：pack 已完成

3. 你没有再碰：
   - `PlayerAutoNavigator.cs`
   - runtime
   - scene / prefab / hotfile

4. 你已经重新给出：
   - 当前 own 路径是否 clean
   - 当前能否 sync
   - 如果不能，第一真实 blocker 是谁

5. 你已经把 Unity 留在：
   - `Edit Mode`

---

## 六、你的回执格式

仍按固定两层回：

### A1. 用户可读汇报层

至少写清：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

并且必须直接报实：

- 当前版本是否已被用户认可：`是/否`
- targeted probe 红面现在是否仍保留：`是/否`
- 这些红面当前身份是不是 release blocker：`是/否`

### B. 技术审计层

至少补：

- 当前在改什么
- 这轮是否重跑 live；如果没有，为什么合法不需要
- `final acceptance pack` 当前 latest 红绿总图
- 当前是否还能把“右键停位偏上”写成已关闭
- changed_paths
- 当前 own 路径是否 clean
- blocker_or_checkpoint
- `Begin-Slice / Ready-To-Sync / Park-Slice`
- 当前 live 状态

---

## 七、thread-state 要求

如果你这轮从只读进入真实施工或要更新 tracked 文档：

1. 先补或重开 `Begin-Slice`
2. 若尝试 sync，必须跑 `Ready-To-Sync`
3. 若这轮停在 blocker 或停车态，必须跑 `Park-Slice`

这轮结束前必须报实：

- `Begin-Slice=是否已跑`
- `Ready-To-Sync=是否已跑`
- `Park-Slice=是否已跑`
- 当前是 `ACTIVE / READY / PARKED / BLOCKED`

---

## 八、一句话总结

**这轮不是继续修导航，而是把“用户已经认可当前导航版本”这件事正式落成最终验收交接与可归仓判定；targeted probe 红面可以保留为后续 polish 诊断项，但不准再把它写成当前版本不可用。**
