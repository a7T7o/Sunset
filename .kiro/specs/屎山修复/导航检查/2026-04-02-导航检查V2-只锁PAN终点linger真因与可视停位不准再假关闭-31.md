# 2026-04-02-导航检查V2-只锁PAN终点linger真因与可视停位不准再假关闭-31

先给这轮裁定：

1. 我接受你这轮 **确实完成了** `-29` 的两件事：
   - dedicated endpoint 的 fake green 已从 runner 口径里剔掉；
   - `NavigationLiveValidationMenu.cs` 的 dedicated endpoint 标准入口也真的接回了。
2. 我接受 dedicated endpoint 当前 fresh `raw ×3` 都不再是假绿，而是稳定失败。
3. 但我 **不接受** 你现在就把“未到点就提前失活并 linger”写成已经实锤的 runtime 真因。
4. 我也 **不接受** 你把 ground / static / 右键停位偏上的问题继续往 `Collider.center` 单指标里藏。用户最新真人反馈仍然是：
   - 右键导航整体体验还是胡闹；
   - 玩家停位仍有“落在点击点上方”的体感；
   - 所以 center-only 的 probe 结果最多只能算结构证据，不能算用户可感知体验已关闭。

---

## 一、当前已接受的基线

这些继续接受，不准回漂：

1. `-25` 继续只算 `carried partial checkpoint`
2. `-29` 继续只算：
   - `partial-checkpoint:-29-fake-green-removed-and-fresh-matrix-restated`
3. dedicated endpoint 当前已经不是：
   - runner 没案子
   - menu 没入口
   - blocker shell 假绿
4. 这轮新的唯一主刀应该回到：
   - `PlayerAutoNavigator.cs`
   - 而不是继续回 runner / menu / solver / NPC roam / scene / static runner

---

## 二、这轮唯一主刀

这轮只做一件事：

- **只锁 `PlayerAutoNavigator.cs` 里 dedicated endpoint / crowd 共同暴露出来的 linger 真因，并且同时把“用户可视停位仍偏上”与“center-only 结构绿”彻底拆开报实**

换句话说，这轮不是继续补 probe，不是继续修 toolchain，而是：

1. 先把 endpoint / crowd 失败时到底是哪条 PAN 响应链吃掉了窗口重新钉死；
2. 再在 **同一簇** 完成语义 / blocker 语义里补最小一刀；
3. 然后用 fresh 重新验证：
   - dedicated endpoint
   - crowd
   - ground 右键停位可视真相

---

## 三、这轮我明确不接受的两种偷换

### 1. 不接受“Linger = 已实锤就是提前失活”

你这轮回执已经足够证明：

- 不是 fake green
- 不是 interaction hijack
- 不是推着 NPC 走
- 不是 runner/menu 缺口

但还 **不够证明**：

- 一定就是哪条 `CompleteArrival() / path=0 / !HasWaypoint / Cancel()` 旧链在吃窗口

所以这轮你必须把 **精确 runtime 签名** 补出来，不能只凭 outcome 名字往下 claim。

### 2. 不接受“Ground raw 绿了 = 右键停位偏上已经关闭”

当前用户关心的是：

- 玩家最后停下来的 **体感位置**

不是：

- 你的 `Collider.center` 有没有对准点击点

所以只要：

- `Transform`
- `Rigidbody`
- `Foot`
- 或玩家可视停位代理

仍明显落在点击点上方，你就必须继续把这条写成：

- `用户可视停位未关闭`

不准再写：

- `static 点偏上已关闭`
- `ground 已过线`

除非你真的拿当前 fresh 证明“结构真值”和“用户可视真值”已经一致。

---

## 四、这轮允许的作用域

### 允许修改

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\memory.md`
4. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

### 允许只读，不允许再写

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\Editor\NavigationLiveValidationMenu.cs`

### 明确禁止

1. 不准再改 runner / menu 口径
2. 不准回 solver
3. 不准碰 NPC roam
4. 不准碰 scene / static runner
5. 不准把这轮 scope 扩成“顺手继续修 crowd 全体验”

---

## 五、这轮必须先补的 runtime 真因证据

在动代码前，先用当前 `-29` 代码口径 fresh 补出下面这组 **失败瞬间真值**。

### A. dedicated endpoint `raw ×3`

每条至少补：

1. `Requested`
2. `Resolved`
3. `Transform`
4. `Rigidbody`
5. `Collider`
6. `Foot`
7. `IsActive`
8. `pathCount`
9. `pathIndex`
10. `DebugLastNavigationAction`
11. `outcome`
12. `playerCenterDistance`
13. `playerFootDistance`
14. `endpointContractSatisfied`

### B. `Crowd raw ×1`

同样至少补：

1. `Requested`
2. `Resolved`
3. `Transform`
4. `Rigidbody`
5. `Collider`
6. `Foot`
7. `IsActive`
8. `pathCount`
9. `pathIndex`
10. `DebugLastNavigationAction`
11. `crowdStallDuration`
12. `playerCenterDistance`

### C. 你必须回答清楚

当前真正吃窗口的是哪一类：

1. `TryFinalizeArrival(...) -> CompleteArrival() -> ResetNavigationState()`
2. `ReachedPathEnd -> BuildPath() -> path=0 -> Cancel()`
3. `!waypointState.HasWaypoint -> Cancel()`
4. `path.Count == 0 && !_hasDynamicDetour` fallback
5. 还是别的具体分支

如果 fresh 证据推翻了你现在“提前失活”的判断，你必须当场改判，不准为了维持叙事硬往旧锅上贴。

---

## 六、这轮只允许回到 PAN 的哪一簇

如果证据继续指向完成语义 / blocker 语义，这轮只允许回到下面这一簇：

1. `TryFinalizeArrival(...)`
2. `ShouldDeferActiveDetourPointArrival(...)`
3. `TryHoldPostAvoidancePointArrival(...) / ShouldHoldPostAvoidancePointArrival(...)`
4. `TryGetPointArrivalNpcBlocker(...)`
5. `HasReachedArrivalPoint(...)`

只有当 fresh 证据明确证明必须碰测量语义时，才允许 **非常克制地** 触及：

6. `GetPlayerPosition()`
7. `GetResolvedPathDestination()`

但如果你要碰 6/7，你必须先在回执里解释：

- 为什么这不是再次把 `center` / `transform` / `foot` 混成一锅
- 为什么这不会重新制造 `-17/-26` 那种“结构绿了但用户体感还怪”的回退

---

## 七、这轮完成定义

### 必须同时成立的部分

1. dedicated endpoint 不再只是 `outcome=Linger`
2. 你要能明确说出：
   - 是哪条 PAN 分支被修掉了
   - 它从旧退出条件改成了哪个新保持/恢复条件
3. `EndpointNpcOccupied raw ×3` 不能再稳定停在：
   - `playerCenterDistance ≈ 1.0`
   - `npcPushDisplacement = 0`
   - `blockedInputFrames = 0`
   - 但就是 lingering
4. `Crowd raw ×1` 不能继续维持同一条 lingering 签名

### 同时必须守住的 guardrails

1. `SingleNpcNear raw ×1`
2. `Ground raw ×1`
3. `NpcAvoidsPlayer ×1`
4. `NpcNpcCrossing ×1`

### 关于 ground / static / 停位偏上

这轮 **禁止** 用下面任一说法冒充完成：

- `Ground raw ×1 pass=True，所以偏上关闭`
- `Collider 对准了点击点，所以用户体感也算关闭`

只有当你能证明：

- `Collider`
- `Transform / Rigidbody / Foot`
- 以及用户可视停位代理

已经不再互相打架，才允许把这条写成关闭。

否则必须直接写：

- `ground 结构合同仍成立，但用户可视停位偏上未关闭`

---

## 八、这轮最小 fresh matrix

如果你动了 `PlayerAutoNavigator.cs`，这轮至少 fresh：

1. `Ground raw ×1`
2. `SingleNpcNear raw ×1`
3. `EndpointNpcOccupied raw ×3`
4. 如果出现 hijack，再补 `EndpointNpcOccupied suppressed ×1`
5. `Crowd raw ×1`
6. `NpcAvoidsPlayer ×1`
7. `NpcNpcCrossing ×1`

不准再用旧 run 顶这轮 fresh。

---

## 九、固定回执格式

你这轮回执必须继续分两层：

### A1. 用户可读汇报层

固定 6 项，顺序不得改：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### B. 技术审计层

至少逐项回答：

1. 当前在改什么
2. `-25` 当前如何定性；必须直接写 `carried partial checkpoint`
3. `-29` 当前如何定性；必须直接写 `carried partial checkpoint`
4. 这轮 fresh 先钉出来的第一真实失败签名是什么
5. 它具体落在哪个 PAN 分支 / 条件上
6. 这轮具体改了 `PlayerAutoNavigator.cs` 的哪几个分支 / 条件
7. 它从哪个旧退出条件，改成了哪个新保持 / 恢复条件
8. `Ground raw ×1` 结果
9. `Ground raw ×1` 的：
   - `Requested / Resolved / Transform / Rigidbody / Collider / Foot`
   - 以及你对“用户可视停位是否仍偏上”的直接裁定
10. `SingleNpcNear raw ×1` 结果
11. `EndpointNpcOccupied raw ×3` 结果
12. 每条 endpoint raw 的：
   - `Requested / Resolved / Transform / Rigidbody / Collider / Foot`
   - `IsActive / pathCount / pathIndex / DebugLastNavigationAction`
   - `outcome / endpointContractSatisfied / playerCenterDistance / playerFootDistance`
13. `Crowd raw ×1` 结果
14. crowd raw 的：
   - `Requested / Resolved / Transform / Rigidbody / Collider / Foot`
   - `IsActive / pathCount / pathIndex / DebugLastNavigationAction`
   - `crowdStallDuration / playerCenterDistance`
15. `NpcAvoidsPlayer ×1 / NpcNpcCrossing ×1` 结果
16. 如果仍 fail，新的第一责任点是什么
17. 当前是否还能把“static / 右键停位偏上”写成已关闭；如果不能，直接写 `不能`
18. changed_paths
19. 当前 own 路径是否 clean
20. blocker_or_checkpoint
21. 一句话摘要
22. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
23. 如果没跑，原因是什么
24. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 十、当前停发边界

这轮做完后，默认仍然：

- **不能停给用户验收**

除非你真的同时证明：

1. endpoint lingering 已被 runtime 收掉
2. crowd lingering 不再是同链失败
3. 用户可视停位偏上也不再靠 center-only 口径硬撑

否则就继续如实报 `partial checkpoint`。

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
