# 工具-V1：保持停车，只保留三脚本精确响应权

先接受已经成立的总裁定：

1. 你这条线当前不能再写成 `pure contract-only`
2. 你也不再继续自己处理 same-root remaining dirty
3. 当前 accepted navigation version 已成立
4. 当前真正剩余的业务主线已经回到父线程：
   - `Primary traversal` 的 scene integration / live closure

---

## 一、这轮唯一主线

这轮不是 cleanup，也不是 scene integration。

这轮唯一主线只有一句话：

**继续保持 `PARKED`，只保留 `NavGrid2D.cs / PlayerMovement.cs / SceneTransitionTrigger2D.cs` 的精确响应权；除非父线程或 scene owner 先 fresh 压实为脚本缺口，否则不重新开工。**

---

## 二、当前允许与禁止

### 当前允许

1. 保留 exact-own：
   - `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
   - `Assets/YYY_Scripts/Service/Player/PlayerMovement.cs`
   - `Assets/YYY_Scripts/Story/Interaction/SceneTransitionTrigger2D.cs`
2. 继续维护本线程自己的 prompt / memory / ownership 审计
3. 只有当父线程 / scene owner 精确点名：
   - `contract gap`
   - 或 `runtime trim / rollback`
   才允许 reopen 一刀最小切片

### 当前禁止

1. 不准自己继续处理 same-root dirty
2. 不准自己继续做 shared-root cleanup
3. 不准碰 `Primary.unity`
4. 不准碰 `Town.unity`
5. 不准碰 `PrimaryTraversalSceneBinder.cs`
6. 不准碰通用工具
7. 不准继续给三脚本追加第二刀、第三刀行为
8. 不准继续把当前状态说成“只差归仓”

---

## 三、后续只有什么情况才允许重新开工

只有满足下面任一条件，才允许你重新从 `PARKED` 转回真实施工：

1. 父线程 / scene owner 精确点名：
   - 哪个文件
   - 哪个入口 / 字段 / 行为
   - 缺的是 `contract gap`
2. 父线程 / scene owner 精确点名：
   - 当前某段 runtime behavior 需要最小 `trim / rollback`

如果做不到这三层精确点名：

1. 文件
2. 行为
3. 为什么不是 scene wiring / binder 问题

就不准 reopen。

---

## 四、如果以后被唤醒，必须怎样报实

### A1 保底六点卡

按顺序逐项回答：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### A2 用户补充层

必须额外显式回答：

1. 哪些属于 contract 层
2. 哪些属于 runtime behavior 层
3. 你现在是不是仍然 `PARKED`
4. 这轮有没有处理 same-root dirty；如果没有，为什么不能由你自己处理
5. 你下一次重新开工的唯一触发条件是什么

### B 技术审计层

至少补：

1. changed_paths
2. `NavGrid2D / PlayerMovement / SceneTransitionTrigger2D` 各自新增了哪些 public contract
3. 各自哪些已经影响 runtime behavior
4. 当前 own 路径是否 clean
5. blocker_or_checkpoint
6. `Begin-Slice / Ready-To-Sync / Park-Slice`

---

## 五、thread-state

```text
[thread-state 接线要求｜当前状态]
这轮结束时保持：
  PARKED

如果后续被精确点名重新进入真实施工，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1

第一次准备 sync 前，再跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1

如果再次停下、让位或被 blocker 卡住，继续跑：
  D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1
```

---

## 六、一句话总结

**下一轮工具-V1不再继续动代码，也不再自己收 blocker；它继续保持 `PARKED`，只在父线程 / scene owner 精确压实三脚本缺口时做最小响应。**
