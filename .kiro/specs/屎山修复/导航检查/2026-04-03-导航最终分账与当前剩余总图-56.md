# 2026-04-03｜导航最终分账与当前剩余总图

## 一、先给总裁定

当前导航不再是一条大锅，经过工具-V1回执、`导航检查V2` 回执、以及最新分工 prompt 交叉核对后，当前最准确的总分账是：

1. **已被用户认可的当前玩家导航版本**
   - 已成立
   - 这是当前最高层事实

2. **`导航检查V2` 这条线**
   - 已完成最终验收交接定性
   - 未完成合法归仓

3. **工具-V1 这条线**
   - 三脚本边界守住
   - 但本轮实际改动已是 `contract + runtime behavior change` 混合 slice
   - 当前必须继续 `PARKED`

4. **父线程 `导航检查` 现在真正剩下的唯一业务主线**
   - `Primary traversal` 的 scene integration / live closure
   - 也就是：
     - `Props` 不可穿
     - `Water` 玩家和 NPC 不可进
     - 玩家不可走出 tilemap 外

5. **当前不能 claim 的东西**
   - `final acceptance pack` 全绿
   - `右键停位偏上已关闭`
   - `整个导航系统全线完成`

---

## 二、已完成范围

### A. 用户已认可的当前玩家导航版本

这层已经成立：

1. 用户已明确认可当前版本
2. `PassableCorridor / StaticNpcWall` 红面仍保留
3. 但这两簇红面当前只再算：
   - targeted probe
   - 后续 polish 诊断项
4. 它们**不是**当前 accepted version 的 release blocker

### B. `导航检查V2` 已完成的范围

这条线当前已完成：

1. runner/menu 最终验收交接定性
2. `FinalPlayerNavigationAcceptancePack` 的 latest 红绿总图报实
3. “accepted navigation version”和“`Primary traversal` 独立切片”的彻底分账

但它**未完成**：

1. legal-sync
2. own-root clean

### C. 工具-V1 已完成的范围

这条线当前已完成：

1. `NavGrid2D.cs`
2. `PlayerMovement.cs`
3. `SceneTransitionTrigger2D.cs`

这 3 个文件里的最小边界补强与当前 ownership audit

但它**不能再 claim**：

1. `Primary / Town` scene 已完成
2. pure contract-only
3. 自己继续处理 same-root cleanup

---

## 三、仍未完成范围

### A. 当前真正仍未完成的业务面

只剩父线程新的独立切片：

1. `Primary traversal` scene integration / live closure

展开就是：

1. 玩家不能穿过 `Props`
2. 玩家和 NPC 不能进入 `Water`
3. 玩家不能走出 tilemap 外

### B. 当前仍未完成的治理 / 归仓面

1. `导航检查V2`
   - still blocked by own-root remaining dirty / untracked
2. 工具-V1
   - 已停车，不再继续清 same-root dirty
3. 父线程
   - 还没真正接 `prompt_03` 去收 `Primary traversal`

---

## 四、三条线现在各自负责什么

### 1. 父线程 `导航检查`

唯一主刀：

1. `Primary.unity`
2. `PrimaryTraversalSceneBinder.cs`
3. 与 `Primary traversal` 三个问题直接相关的最小 scene wiring / live integration

### 2. `导航检查V2`

唯一主刀：

1. 最终验收交接
2. accepted navigation version 的分层报实
3. sync / own-root 判定

### 3. 工具-V1

唯一保留：

1. `NavGrid2D.cs`
2. `PlayerMovement.cs`
3. `SceneTransitionTrigger2D.cs`

而且当前只保留**被动响应权**：

1. 父线程 / scene owner 若精确压实为脚本 gap，再按点名 reopen
2. 否则继续 `PARKED`

---

## 五、当前最准的一句话总图

**当前“玩家导航版本可接受”这件事已经成立；`导航检查V2` 已把这件事收成交接，工具-V1也已被压回停车态；真正还没完的只剩父线程独立切出来的 `Primary traversal` scene integration / live closure。**
