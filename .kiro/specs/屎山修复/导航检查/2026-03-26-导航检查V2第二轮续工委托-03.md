# 2026-03-26-导航检查V2第二轮续工委托-03

你现在继续作为 `导航检查V2` 施工。

这轮不是回头重开 solver 泛调，也不是把首轮 detour owner 保活过一个窗口误写成“导航全过了”。

## 当前已接受的基线

1. `519d51bd` 已是当前首轮 accepted checkpoint
2. 首轮已经成立的单一第一进展是：
   - detour owner 保活最小闭环已接上
   - `RealInputPlayerAvoidsMovingNpc` 已拿到首个有效执行窗口
3. 当前仍未被接受的部分是：
   - 整条导航体验还没有通过
   - 还缺至少一组更直接的 NPC 侧 owner 证据

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2首轮启动委托-02.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`

## 这轮唯一主刀

只做：

> 在同一 detour owner 闭环上，补 1 组 NPC 侧 fresh 证据

更具体地说，这轮只允许你完成下面这 1 刀：

1. 不改主叙事，不改热区范围，继续只锁：
   - `NavigationPathExecutor2D.cs`
   - `PlayerAutoNavigator.cs`
   - `NPCAutoRoamController.cs`
2. 优先只跑 1 组 NPC 侧代表性 fresh：
   - `NpcAvoidsPlayer`
3. 如果这组 fresh 直接通过：
   - 立刻 `Stop`
   - 退回 `Edit Mode`
   - 不再继续刷第二组
4. 如果这组 fresh 暴露同一闭环还有缺口：
   - 只允许在同一 owner keepalive / release 闭环里补 1 个最小口
   - 然后最多再补 1 组同场景 fresh

## 允许的作用域

这轮只允许你读取和必要时修改这些范围：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2第二轮续工委托-03.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationPathExecutor2D.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`
6. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
7. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\NavigationAvoidanceRulesTests.cs`

## 明确禁止

1. 不要回漂 `NavigationLocalAvoidanceSolver` 权重
2. 不要回开 `TrafficArbiter / MotionCommand`
3. 不要碰 `Primary.unity`
4. 不要跑大窗 full live
5. 不要把这轮扩成“保护罩 / 抽搐 / 远停一起全修完”
6. 不要把首轮 `RealInputPlayerAvoidsMovingNpc` 通过冒充成这轮已完成

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已拿到 1 组 NPC 侧 fresh 结果，优先 `NpcAvoidsPlayer`
2. 你已明确回答 detour owner 是否也在 NPC 侧进入有效执行窗口
3. 如果未过线：
   - 你已把单一第一阻塞点继续压回同一闭环
4. 你已在拿到结论后立刻 `Stop` 并退回 `Edit Mode`
5. 你已把这轮结果追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

## 最小回执格式

- 已回写文件路径
- detour owner 是否已在 NPC 侧进入有效执行窗口：`yes / no`
- 当前单一第一进展或第一阻塞点
- 一句话摘要

## 一句话总口径

你这轮不要再讲大方向，只围绕已经接上的 detour owner 闭环，再拿 1 组 NPC 侧 fresh 证据，证明它不是只在玩家侧偶然过了一次。
