# 2026-04-02-父线程-V2-33验收尺与大刀闭环拒收清单-34

## 1. 这份文档的目的

这不是施工正文。

这是父线程下一次收 `-33` 回执时的固定验收尺，用来防三种新退化：

1. 又拿 compile gate 做“小 blocker 停车”
2. 又只交 runtime 签名，不交真实改刀
3. 又拿 `Collider.center` 结构绿冒充“右键停位偏上已关闭”

---

## 2. 当前已经钉死的新增事实

### 2.1 compile gate 不能再被模糊描述

父线程这轮已经直接核到：

1. `Editor.log` 里确实存在：
   - `InventorySlotUI.cs`
   - `ToolbarSlotUI.cs`
   对应的 `CS0103`
2. 但当前工作树里，这两个文件里：
   - `TickStatusBarFade()`
   - `ApplyStatusBarAlpha()`
   的方法本体又确实存在

所以后续不能只写：

- `被外部 compile gate 截断`

必须继续回答：

- 这条 gate 在当前 fresh recompile 里到底还是不是活的
- 如果还是活的，为什么当前文件明明有方法本体却仍报缺方法

### 2.2 下一轮不再接受“小 blocker checkpoint”

如果 `V2` 下次只是：

1. 又重跑了一次 compile
2. 又说 gate 还在
3. 然后没做 PAN 改刀

这轮直接拒收。

---

## 3. 下一次回执的固定审查顺序

### 第一关：先审 compile gate 真相有没有塌缩

必须先看：

1. 最新 console / `Editor.log` 关键片段
2. 当前 gate 是：
   - `active`
   - `stale`
   - `cleared`
3. 如果 `active`，有没有回答清楚：
   - 当前文件已有方法本体，为什么仍报 `CS0103`

答不出来，这轮继续拒收。

### 第二关：再审它有没有继续停在“小 blocker 回卡”

下一次如果出现下面任一情况，继续拒收：

1. compile gate 一 cleared 就停车
2. 只交 fresh runtime 签名，不交 PAN 改刀
3. 只交“第一责任点更清楚了”，没有真实体验改善

### 第三关：再审 PAN 大刀是不是至少砍到了两条体验线

这轮父线程最少要看到：

1. endpoint
2. crowd
3. ground 可视停位

这三条里至少两条出现真实改善，而不只是解释更清楚。

### 第四关：最后才审 matrix

最小 matrix：

1. `EndpointNpcOccupied raw ×3`
2. `Crowd raw ×3`
3. `Ground raw matrix ×1`
4. `SingleNpcNear raw ×2`
5. `MovingNpc raw ×1`
6. `NpcAvoidsPlayer ×1`
7. `NpcNpcCrossing ×1`

---

## 4. 下一次回执的最小通过条件

如果它想把 `-33` 判成“这轮站住”，最少必须同时满足：

1. compile gate 真伪已塌缩
2. `PlayerAutoNavigator.cs` 已有新增 runtime 改刀
3. endpoint / crowd / ground 三条里至少两条出现用户可感知改善
4. 不能继续把“右键停位偏上”写成已关闭，除非 `Transform / Rigidbody / Foot / Collider / 可视真值` 一起对齐

---

## 5. 继续拒收的三种新假完成

### 假完成 A

- “gate 还在，所以这轮先不动 PAN”

拒收条件：

- 没有解释当前 gate 为什么在文件已有方法本体时仍活着

### 假完成 B

- “真因已经实锤，但这轮先不补第二刀”

拒收条件：

- 没有 PAN 改刀
- 只有分析，没有推进

### 假完成 C

- “ground 已关闭，只剩 endpoint/crowd”

拒收条件：

- 只有 `Collider.center`
- 没有 `Transform / Rigidbody / Foot`
- 没有用户可视停位裁定

---

## 6. 父线程下一次固定追问

1. compile gate 现在到底是 `active / stale / cleared` 哪一种？
2. 如果 `active`，为什么方法本体已在当前文件里仍会报 `CS0103`？
3. 你这轮 PAN 具体改了哪一簇？
4. endpoint / crowd / ground 三条里，哪两条出现了真实体验改善？
5. 你现在还能不能把“右键停位偏上”写成已关闭？

答不上来，继续拒收。
