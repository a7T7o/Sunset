# 2026-04-01-导航检查V2-强制回拉玩家右键真实入口主线-26

先给这轮裁定：

1. 我接受你刚刚这轮把 `-25` 的 latest guardrail 红面钉死为 runner probe 污染，并且把这条 **NPC roam probe slice** 收回绿。
2. 但我**不接受**你把它上抬成：
   - `runtime 闭环已完成`
   - `下一步只剩 Ready-To-Sync / sync`
3. 原因很简单，而且优先级比你这轮 same-play probe 更高：
   - 用户刚做完真实右键导航手测，结论是：**“简直就是胡闹，啥都干不出来”**
   - 所以当前只能判成：
     - `-25` 的 **结构 / targeted probe** 这两层站住了
     - **真实入口体验没有站住**

这轮不准继续拿 `-25` 自说自话。

你要被**强制回拉**到玩家真正关心的主线：

- 普通地面点导航为什么还会停在点击点上方
- 贴近静止 NPC / 双近距 NPC 通道时为什么还会推、漂、卡、鬼畜
- 玩家真实右键入口为什么还没有成为一个正常可玩的导航

---

## 一、当前已接受的基线

这些我继续接受，不要回头打散：

1. `-25` 当前只作为 **carried partial checkpoint** 存在  
   - managed roam probe 污染清理成立  
   - `NpcRoamPersistentBlockInterrupt / NpcRoamRecoverWindow` 这组局部证据成立  
   - same-play `NpcAvoidsPlayer / NpcNpcCrossing` 回绿成立  
   - 但这只代表 **NPC roam probe slice**
2. 这轮**不再继续补** `NPCAutoRoamController.cs` 的 roam 中断逻辑
3. 这轮**不再继续补** broad own roots cleanup / sync 收口
4. static / 点击点偏上问题，当前**没有被接受为已解决**

---

## 二、这轮唯一主刀

只收这一件事：

- **玩家右键真实入口主线**

把下面 4 个问题当成同一条主链处理，而不是再拆散：

1. 普通地面点导航的到点契约  
   - 用户认可的契约是：
     - **普通地面点导航 = 玩家实际占位中心语义**
     - **跟随交互目标 = `ClosestPoint + stopRadius` 语义**
   - 这两套**不准再混着**
2. static 点偏上  
   - 用户现在仍然在看到：
     - 点了 A
     - 角色却停在 A 的上方
     - 玩家碰撞体中心没有真正对上 A
3. 近距静止 NPC / 终点站着 NPC 时的推土机与死避让
4. 双近距 NPC / 通道 / crowd 时的漂移、来回倒转、卡顿、鬼畜

这轮不是去继续修 roam probe。

这轮是把**玩家右键导航为什么还是胡闹**这件事压清。

---

## 三、当前最窄第一责任点

父线程当前默认怀疑，你先从这里开始，不准漂：

### 第一责任点 A：`PlayerAutoNavigator.cs` 里的普通点导航到点契约仍然错层

当前现场里，`GetPlayerPosition()` 对普通点导航仍然存在：

- `targetTransform == null`
- 优先返回 `playerRigidbody.position / player.position`

而玩家碰撞体中心在 scene 里仍然有明显上偏移。

这就意味着：

- 你虽然可能避免了某些“提前 Inactive”
- 但同时把“普通地面点导航应按玩家实际占位中心收口”的契约打坏了

所以这轮你必须正面回答：

1. 普通地面点导航的 **contract truth** 到底是谁  
   - `Collider.center`
   - `Rigidbody.position`
   - `Transform.position`
2. 当前为什么用户会稳定看到“停在点上方”
3. 你最后怎么改，才能同时满足：
   - 不再提前完成
   - 不再停在点击点上方
   - 普通点导航与跟随目标语义彻底分离

### 第一责任点 B：`PlayerAutoNavigator.cs` 的 player-side 避让链虽然不再是最早那种纯推土机，但真实入口仍未过线

用户最新真实反馈已经明确指向这些坏相：

1. 近距静止 NPC 时，仍容易直接推着 NPC 走
2. 双 NPC 接近时，会在中间漂移、徘徊、卡顿
3. 路线仍很畸形
   - 正常前进时突然倒转避让
   - 很远就开始奇怪偏转
   - 终点有 NPC 时会反复死避让

所以这轮不准再只拿 `NpcAvoidsPlayer / NpcNpcCrossing` 的 NPC 侧 guardrail 绿面来冒充玩家入口过线。

---

## 四、这轮允许的作用域

### 允许修改

只允许：

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerAutoNavigator.cs`

如果你**绝对必须**补验证入口来证明这条玩家主线，最多再允许：

2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLiveValidationRunner.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationStaticPointValidationRunner.cs`

以及你自己的：

4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\memory.md`
6. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查V2\memory_0.md`

### 不允许修改

1. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\NPC\NPCAutoRoamController.cs`
2. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\NavigationLocalAvoidanceSolver.cs`
3. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation\Editor\NavigationLiveValidationMenu.cs`
4. `D:\Unity\Unity_learning\Sunset\Assets\Editor\NavigationStaticPointValidationMenu.cs`
5. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`
6. 任何 `scene / prefab / hotfile`
7. broad own roots cleanup 作为这轮主线
8. 继续讲 `-25` 的 roam probe 成功细节

---

## 五、这轮明确禁止的漂移

1. 不准再把 `-25` 的局部绿面包装成“整体导航 runtime 闭环”
2. 不准再把 `Ready-To-Sync` / `sync` / broad own roots cleanup` 写成下一步主线
3. 不准回漂到 NPC roam interruption / recover 边界
4. 不准回漂到 solver、大架构、scene、菜单、工具层争论
5. 不准只跑 `suppressed` 或 synthetic 对照就宣布“体验可用”
6. 不准只给局部 probe 结果，不回答用户真实右键入口为什么仍然坏

---

## 六、这轮必须正面收掉的体验问题

### P0-1：普通地面点导航不再停在点击点上方

你必须直接给出 fresh 结论：

1. 当前普通点导航的 `Requested / Resolved / Transform / Rigidbody / Collider(center)` 分别是多少
2. 这 5 个量里，哪个才是这轮的 **contract truth**
3. 为什么它现在不会再出现：
   - 玩家点击 A
   - 实际占位中心却稳定停在 A 上方

注意：

- 这轮不准再把 `Transform` 或 `Rigidbody` 偷换成普通点导航的唯一验收口径
- 如果你最后仍然让普通点导航按 `Rigidbody/Transform` 过线，你必须证明：
  - 用户看到的“点偏上”为什么会消失
  - 否则一律按 fail 处理

### P0-2：近距静止 NPC 不再“推着走 / 死避让”

用户现在最烦的不是抽象指标，而是：

- 靠很近路过
- 玩家还在推 NPC
- 或在终点附近不断死避让

这轮必须拿 raw 玩家体验矩阵把这件事压清。

### P0-3：双近距 NPC / crowd 通道不再漂、卡、鬼畜

用户明确点名了这些坏相：

- 在两个 NPC 中间徘徊
- 漂移
- 卡顿
- 倒转避让
- 路线很突兀

只要这些坏相仍然出现，就不准继续说“已经可以用了，只差细节优化”。

---

## 七、这轮验证矩阵

这轮验证必须回到 **玩家右键真实入口矩阵**。

### 1. Ground raw matrix

至少：

1. `RawRightClick ground matrix` 全量 fresh
2. 逐条报：
   - `Requested`
   - `Resolved`
   - `Transform`
   - `Rigidbody`
   - `Collider(center)`
3. 逐条明确：
   - 哪个是这轮 contract truth
   - 哪些旧口径这轮正式降级

这轮 ground pass 的唯一硬要求：

- **普通点导航不能再靠 `Transform/Rigidbody` 绿掉 `Collider/实际占位中心` 的偏差**

### 2. `SingleNpcNear raw ×3`

必须 fresh，且不是拿旧结果顶。

### 3. “终点有 NPC 停留”的 dedicated case 或最接近代理 `×3`

这条不准再一笔带过。

你必须明确说：

- 这轮到底有没有 dedicated case
- 如果没有，代理场景是什么
- 代理为什么足够像用户说的坏相

### 4. 双近距 NPC 通道 / crowd raw `×3`

如果现有 `Crowd raw` 不够贴近用户说的“双 NPC 接近通道”：

- 允许你在 `NavigationLiveValidationRunner.cs` 里加最小 player-side 入口
- 但只允许加**最小验证口**
- 不准再扩成新框架

### 5. `MovingNpc raw ×1`

只做 guardrail，不再拿它冒充整体体验过线。

### 6. 若你碰了 live runner，再补：

1. `NpcAvoidsPlayer ×1`
2. `NpcNpcCrossing ×1`

它们只是 guardrail，不是主成功标准。

---

## 八、这轮完成定义

### 结局 A：可接受

必须同时满足：

1. `-25` 被明确降级为 **carried partial checkpoint**
2. 普通地面点导航的 contract truth 已重新钉死，并且 static 点偏上消失
3. `SingleNpcNear raw ×3` 不再出现稳定推人 / 死避让
4. 双近距 NPC / crowd raw `×3` 不再出现明显漂移、来回倒转、鬼畜卡顿
5. `MovingNpc raw ×1` 不坏
6. 如果你碰了 live runner，`NpcAvoidsPlayer / NpcNpcCrossing` 也不坏
7. 回执里明确承认：
   - 这轮过的是哪一层
   - 还没过的是哪一层

### 结局 B：仍 fail

你必须明确回答：

1. 当前第一真实 blocker 是 static contract，还是 player-side corridor 行为链
2. 哪一条 raw case 仍然最像用户说的“胡闹”
3. 当前最窄第一责任点还剩哪一个方法 / 条件簇
4. 为什么它不是 `-25` 那条 NPC roam slice 了

---

## 九、固定回执格式

### A1. 用户可读汇报层

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

### B. 技术审计层

1. 当前在改什么
2. `-25` 当前被你如何重新定性；必须直接写：
   - `carried partial checkpoint`
   - 或其他，但不准再写成整体闭环
3. `PlayerAutoNavigator.cs` 里你最后改变了哪一组 contract / completion / avoidance 条件
4. 普通地面点导航这轮的 contract truth 是谁，为什么
5. `Ground raw matrix` 最新结果  
   - `Requested / Resolved / Transform / Rigidbody / Collider(center)`  
   - 哪个是 pass truth  
   - 哪些口径被降级
6. `SingleNpcNear raw ×3` 结果
7. `终点有 NPC 停留` 的 dedicated case / 代理 case `×3` 结果
8. 双近距 NPC 通道 / `Crowd raw ×3` 结果
9. `MovingNpc raw ×1` 结果
10. 如果碰了 live runner，再补：
    - `NpcAvoidsPlayer ×1`
    - `NpcNpcCrossing ×1`
11. 如果仍 fail，新的第一责任点是什么
12. static 点偏上这轮到底是否关闭；必须直接写 `是 / 否`
13. changed_paths
14. 当前 own 路径是否 clean
15. blocker_or_checkpoint
16. 一句话摘要
17. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
18. 如果没跑，原因是什么
19. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 十、一句话提醒

你这轮不是去证明 `-25` 很有价值。

你这轮真正要证明的是：

- **为什么用户现在真实右键导航还在胡闹**
- **你有没有把这个玩家入口主线真的拉回正轨**

只要用户还在看到：

- 点偏上
- 推静止 NPC
- 双 NPC 通道漂移卡顿鬼畜

你就不准继续把局部 green probe 写成“已经可用”。

---

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
