# 002-prompt-7

这轮我先给你一个新的总裁定，而且我会把话说死：

- 你不要再把导航线拆成很多零碎小刀自己慢慢游了。
- 也不要再一边修一个局部点，一边默认主线还是“继续 patch 旧系统直到能用”。
- 从现在开始，这条线正式按：
  - [006-Sunset专业导航系统需求与架构设计.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/006-Sunset专业导航系统需求与架构设计.md)
  - [007-Sunset专业导航底座后续开发路线图.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/屎山修复/导航检查/007-Sunset专业导航底座后续开发路线图.md)
  为上位依据，
  **直接把 `S0-S6` 当成一个连续闭环工程做掉。**

我这里明确授权你：

> 这轮不是“做一刀停一下”等待再次批准。  
> 这轮是允许你在同一条主线里，按顺序连续推进 `S0 -> S6`，直到真正完成“玩家 / NPC 统一动态交通语义、统一运动执行语义、行为脑与旧私有导航闭环下线”的闭环。

但这个“一步到位”不是放飞。  
你有自由度，但不是无限自由度。

---

## 一、这轮真正目标

这轮真正目标不是：

- 再把某个 `probe` 调绿
- 再修一个 detour bug
- 再补一条 avoidance 参数
- 再写一份解释文档

这轮真正目标是：

> 以 `006 + 007` 为硬上位依据，直接把 `S0-S6` 做成一个真实的、可运行的、可验证的导航底座中段闭环。

也就是至少做到：

1. `S0`：
   - 基线冻结
   - 继承资产清单
   - 废止旧语义清单
   - 第一批热区 / 禁区落地到你的执行面
2. `S1`：
   - 统一动态代理契约彻底站稳
3. `S2`：
   - 交通裁决层 MVP 真正成立
   - 不再只是“修方向”
4. `S3`：
   - 微观运动求解层开始真正服从交通裁决
   - 不再继续让大圆壳 / 壳层 steering 统治体验
5. `S4`：
   - 共享路径执行层彻底从旧 detour 污染语义里净化出来
6. `S5`：
   - 玩家 / NPC 最终运动执行语义真正收口
7. `S6`：
   - 玩家 / NPC 行为脑接入新底座
   - 旧私有导航闭环明确下线

换句话说：

> 我现在要的不是“一个更强的过渡版 patch 系统”，  
> 而是 `S0-S6` 完成后的 **第一版专业导航底座闭环**。

---

## 二、这轮允许的工作方式

这轮允许你：

1. 按 `S0 -> S6` 顺序连续推进
2. 在同一轮里做多个代码 checkpoint
3. 先设计收口、再代码落地、再 live 验证、再继续下一个阶段
4. 为了完成 `S0-S6` 闭环，必要时连续修改多个导航热区文件
5. 在不漂移主线的前提下，自主决定每一步的最优落地顺序

这轮不要求你每推进一个小点就停下来等批准。  
只要你还在 `S0-S6` 主线里，就继续做。

---

## 三、这轮不允许做的事

### 1. 不允许漂移回旧 patch 主线

禁止把主线退回：

- “继续调 solver 参数”
- “继续补 detour 生命周期”
- “继续修一个 probe case”

这些可以作为局部动作存在，  
但不允许再变成你的主叙事。

### 2. 不允许越级做到 `S7/S8`

这轮明确只做 `S0-S6`。

也就是说：

- 不要提前正式接怪物 / 宠物 / 牲畜整套玩法
- 不要提前宣称最终封板
- 不要提前写“导航彻底完成”

`S7` 和 `S8` 继续等后续基于 NPC 线和真实整体验收再开。

### 3. 不允许借口“一步到位”顺手乱碰非热区

默认不要扩张去碰：

- 农田系统
- 背包 / 箱子 / UI / Dialogue
- `spring-day1`
- 无关业务线

也不要把 `Primary.unity` 当成垃圾场。

如果必须碰 scene / live 承载，只能是：

- 有明确导航闭环必要性
- 改动最小
- 改后可解释
- 做完回读验证

### 4. 不允许只交文档，不交真实落地

你这轮不是再来一篇分析稿。

你可以补文档、补主表、补 memory，  
但前提是：

> 你必须同时交出真实代码落地和真实验证证据。

---

## 四、你这轮应如何理解“一步到位”

这里的“一步到位”不是：

- 一次提交
- 一次大爆改
- 一轮不做 checkpoint

这里的“一步到位”真正意思是：

> 在同一条主线上，连续推进到 `S0-S6` 闭环成立；  
> 中间可以有多个阶段 checkpoint，但不能自己拆回“以后再说”的松散碎刀。

你可以这样理解：

- `S0` 先定死边界
- `S1-S3` 先把统一动态交通语义做起来
- `S4-S5` 再把共享执行与统一运动收口
- `S6` 最后把玩家 / NPC 的旧私有导航闭环退出

只要这一大段没完成，就不算本轮主线完成。

---

## 五、这轮的真实完成定义

这轮只有满足下面 4 条，才算你把 `S0-S6` 真做完了：

1. 玩家与 NPC 面对动态冲突时，系统先输出交通裁决，不再只是方向修正
2. 玩家与 NPC 的最终运动执行语义已经真正收口，不再是明显两套体感
3. `PlayerAutoNavigator / NPCAutoRoamController` 已明确退化到 brain / bridge 角色，不再各自偷偷养完整导航闭环
4. 你能拿出真实的 live / test / controlled validation 证据，证明这不是纸面闭环

如果只是下面这些状态，都不算完成：

1. 三个 `probe` 又过了，但真实架构还是旧逻辑
2. 交通裁决写了枚举，但最终没人真的消费
3. 运动执行接口统一了，但玩家/NPC 手感还是明显割裂
4. 旧私有导航闭环还在，只是多包了一层新系统

---

## 六、我建议你的执行顺序

我不给你写死实现细节，但主顺序建议你这样走：

### 阶段 A：先打 `S0`

你先把 `007` 里的 `S0` 真正消化成代码施工前的硬边界：

1. 哪些现有资产保留
2. 哪些旧语义必须废止
3. 第一批热区就是哪些
4. 这轮明确不碰什么
5. 这轮最小闭环样例是什么

注意：

`S0` 不能只停在嘴上。  
你要把这个边界写进你的实现路径、主表更新和回执口径里。

### 阶段 B：连续推进 `S1-S3`

这里是本轮最关键的一段：

1. 代理契约与动态上下文统一
2. 交通裁决层 MVP
3. 微观运动求解层真正服从交通裁决

这段完成后，你应该已经能明确回答：

- 玩家 / NPC 当前为什么停
- 为什么让
- 为什么恢复

而不是再回答：

- 现在 direction 被修成多少
- 现在 speedScale 变成多少

### 阶段 C：推进 `S4-S5`

把共享路径执行层与最终运动执行层彻底收口：

1. detour 不再污染主路径语义
2. 路径走廊与动态绕行生命周期分开
3. 玩家 / NPC 最终运动状态语义统一
4. `停、让、绕、恢复` 不再像隐形墙 + 脚本挪位

### 阶段 D：最后完成 `S6`

到这一步，要求你明确下线旧私有导航闭环：

1. `PlayerAutoNavigator`
2. `NPCAutoRoamController`

它们可以继续存在，  
但角色必须退化成：

- brain
- bridge
- 意图输出器

而不是继续各自偷偷维护导航主循环。

---

## 七、这轮必须重点守住的热区

默认核心热区就是 `007` 里列的那批文件，尤其是：

- `NavGrid2D.cs`
- `INavigationUnit.cs`
- `NavigationAgentSnapshot.cs`
- `NavigationAvoidanceRules.cs`
- `NavigationAgentRegistry.cs`
- `NavigationLocalAvoidanceSolver.cs`
- `NavigationPathExecutor2D.cs`
- `PlayerAutoNavigator.cs`
- `NPCAutoRoamController.cs`
- `PlayerMovement.cs`
- `NPCMotionController.cs`
- `NavigationLiveValidationRunner.cs`
- `NavigationAvoidanceRulesTests.cs`

如果你要碰出这个范围，必须能明确解释：

1. 为什么 `S0-S6` 闭环离不开它
2. 它属于哪个阶段目标
3. 不碰它为什么闭环站不住

---

## 八、这轮验证要求

这轮不能只做一种验证。

至少要有下面三层：

### 1. 代码 / 测试层

- 该有的测试补上
- 该有的编译闸门通过
- 该有的最小回归跑通

### 2. 受控 live 验证层

至少继续守住：

- `PlayerAvoidsMovingNpc`
- `NpcAvoidsPlayer`
- `NpcNpcCrossing`

但现在它们只能算功能线的一部分。

### 3. 架构闭环验证层

你必须额外证明：

- 这三条 case 通过，不是因为旧 patch 还在偷偷兜底
- 而是因为 `S0-S6` 新闭环确实成立

换句话说：

你要验证的不只是“行为看起来过了”，  
还要验证“旧私有导航主循环是不是已经真正退出了主语义”。

---

## 九、这轮你可以中间回执，但不允许中间掉主线

我允许你在推进 `S0-S6` 的过程中给中间回执，  
但中间回执的意义是：

- 汇报你已经推进到哪一个阶段
- 当前 blocker 是什么
- 是否还在 `S0-S6` 主线内

不是让你借中间回执把任务拆回“小刀以后再说”。

所以你的中间 checkpoint 口径应该类似：

- `S0 已完成，S1-S2 已落地，当前卡在 S3 的 footprint clearance`
- `S0-S3 已完成，当前在做 S4-S5 的统一运动执行收口`
- `S0-S5 已完成，当前在清 S6 的旧私有导航闭环下线`

而不是：

- “这轮先修了一个 bug，剩下以后再说”

---

## 十、这轮只有两种可接受的结局

### 结局 A：`S0-S6` 真闭环完成

这是最理想的结果。  
如果你做到了，就直接交：

1. 改了什么
2. `S0-S6` 各自怎么落的
3. 哪些旧语义已经下线
4. 当前 live / test / architecture evidence 是什么
5. 为什么你认为现在可以把 `S7/S8` 继续后延

### 结局 B：没彻底做完，但已经推进到新的稳定硬 checkpoint

也接受。  
但前提是你必须明确：

1. 当前推进到了 `S0-S6` 里的哪一步
2. 哪一步已经成立
3. 哪一步是唯一剩余 blocker
4. 为什么这个 blocker 不是你继续碎 patch 能解决的，而是本轮闭环里的最后阻塞点

我不接受的结局是：

- 做了很多东西
- 但回执里看不出你到底推进到了 `S0-S6` 哪一层

---

## 十一、用户补充区

如果用户在你执行过程中新增现场观察、主观体感或新的验收要求：

- 先吸收
- 再判断它属于 `S0-S6` 哪一层
- 然后继续推进

不要因为用户补一句观察，就把整个主线改题。

---

## 十二、本轮回执格式

下一次回执，只按这个格式回复：

- 当前主线推进到 `S0-S6` 的哪一步：
- 当前在改什么：
- 已完成的阶段：
- 当前唯一 blocker：
- changed_paths：
- 是否触碰 `Primary.unity` 或其他高危 live 资源：
- 新增或修改了哪些测试 / 验证：
- 当前三条功能线 live 结果：
- 当前是否已完成统一交通裁决语义：
- 当前是否已完成统一运动执行语义：
- 当前是否已完成玩家 / NPC 旧私有导航闭环下线：
- 当前剩余未完成的是 `S0-S6` 中哪一项：
- blocker_or_checkpoint：
- 一句话摘要：

---

## 十三、最后再钉一次

这轮我不是让你“干到哪算哪”。  
我是明确授权你：

> 把 `S0-S6` 作为一个连续闭环工程狠狠干完。  
> 中间可以 checkpoint，但主线不许碎。  
> `S7/S8` 继续后延。  
> 真正目标是让导航底座到达“玩家 / NPC 第一版专业闭环成立”的状态。

你现在最该做的不是继续解释过去为什么不够好，  
而是直接把这段闭环做出来。
