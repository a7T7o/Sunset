# 2026-04-02-导航检查V2-只收PAN-crowd同簇三连红并维持其余矩阵绿面-44

先给这轮裁定：

1. 我 **接受** 你已经把 `Tool_002_BatchHierarchy` 这条假 blocker fresh 压成 `cleared`。
2. 我也 **接受** 你已经把 `queued_action-only` 这条假 live blocker fresh 压成“probe 已真实进入 `scenario_start/setup/observe`”。
3. 我还 **接受** 你这轮已经把“当前最终代码”的稳定矩阵真正从零开始重跑了一遍，而不是继续混算旧样本。
4. 现在这条线的 runtime 真相已经被压得很清楚：
   - `EndpointNpcOccupied raw ×3`：全绿
   - `Ground raw matrix ×1`：绿
   - `SingleNpcNear raw ×2`：全绿
   - `MovingNpc raw ×1`：绿
   - `NpcAvoidsPlayer ×1 / NpcNpcCrossing ×1`：绿
   - **只剩 `Crowd raw ×3` 三连红**
5. 所以你下一轮唯一主刀已经被父线程固定成：
   - **只收 `[PlayerAutoNavigator.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs)` 的 crowd 同簇三连红**
6. 但我同时明确再钉一次：
   - 即使你把 crowd 修绿了，
   - 你也 **不准** 顺手把“右键停位偏上 / 用户可视停位怪”写成已关闭。
   - 因为当前 `Ground raw matrix` 仍然只是 `center-only` 结构绿，不是用户可视停位真值绿。

---

## 一、当前已接受的基线

这些都已经接受，不准回漂：

1. `-25` = `carried partial checkpoint`
2. `-29` = `carried partial checkpoint`
3. `Tool_002_BatchHierarchy` 当前 = `cleared`
4. `queued_action-only` 当前已被推翻
5. 当前最终代码矩阵已真正重跑完成
6. 当前绿面继续固定为：
   - `EndpointNpcOccupied raw ×3`
   - `Ground raw matrix ×1`
   - `SingleNpcNear raw ×2`
   - `MovingNpc raw ×1`
   - `NpcAvoidsPlayer ×1`
   - `NpcNpcCrossing ×1`
7. 当前明确仍不能关闭的点：
   - `右键停位偏上 / 用户可视停位怪`

---

## 二、这轮唯一主刀

只打一刀：

- **`PlayerAutoNavigator.cs` 里的 crowd 同簇三连红**

当前第一责任点已经明确回到：

1. `HasPassiveNpcCrowdOrCorridor(...)`
2. `ShouldDeferPassiveNpcBlockerRepath(...)`
3. `ShouldBreakPassiveNpcCrowdSlowCrawl(...)`

只有当 fresh rerun 证明 crowd 红面已经从这三处漂移到别的 PAN 同簇责任点时，才允许你扩到与它强绑定的一小步。

---

## 三、允许的 scope

这轮只允许：

1. 改 `[PlayerAutoNavigator.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs)`
2. 更新：
   - `导航检查/memory.md`
   - `屎山修复/memory.md`
   - `导航检查V2/memory_0.md`
   - 你的开发日志

---

## 四、明确禁止的漂移

这轮不准回漂去做这些事：

1. 不准再回 blocker 解释层：
   - 不准再围着 `Tool_002`
   - 不准再围着 `queued_action-only`
2. 不准去动：
   - runner
   - menu
   - solver
   - NPC roam
   - scene / UI
   - `PlayerNpcChatSessionService.cs`
3. 不准把 scope 从 crowd 同簇漂回 endpoint 完成语义
4. 不准把当前绿面重新打坏，再拿“至少 crowd 进了一点”当补偿
5. 不准把：
   - `Ground raw matrix pass=True`
   - 偷换成
   - `右键停位偏上已关闭`
6. 不准把“crowd 如果过线”偷换成“整个玩家右键导航体验已过线”

---

## 五、这轮 completion 定义

### A. 这轮可以被判成“crowd 同簇已站住”，只有同时满足：

1. 在当前最新代码上，从零开始连续 fresh 重跑：
   - `Crowd raw ×3`
2. 三次都不再红：
   - `pass=True ×3`
3. 你同时补跑最小护栏，且不回归：
   - `EndpointNpcOccupied raw ×1`
   - `Ground raw matrix ×1`
   - `SingleNpcNear raw ×1`
   - `MovingNpc raw ×1`
   - `NpcAvoidsPlayer ×1`
   - `NpcNpcCrossing ×1`
4. 汇报里明确写：
   - crowd 绿了
   - 但“右键停位偏上”仍不能写成已关闭

### B. 这轮明确不允许宣称的东西

1. 不准写：
   - `玩家右键导航整体已过线`
2. 不准写：
   - `右键停位偏上已关闭`
3. 不准写：
   - `只剩用户验收`
   如果你还没把 crowd 三连红压绿

---

## 六、这轮建议打法

你这轮先不要撒开。

推荐顺序固定为：

1. 先只围绕 crowd 同簇补 **一刀最小口**
2. 然后立即重跑：
   - `Crowd raw ×3`
3. 如果 crowd 仍红，再决定是否补第二刀
4. 每补一刀，都必须重新从头跑受影响矩阵，不准沿用补刀前样本

---

## 七、固定回执格式

### A1. 用户可读汇报层

固定 6 项，顺序不得改：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要用户现在做什么

并且这轮必须额外显式回答 4 句：

1. `Crowd raw ×3` 这轮最终到底是几绿几红
2. 这轮是否只动了 `PlayerAutoNavigator.cs`
3. 护栏是否仍全部为绿
4. 你现在还能不能把“右键停位偏上”写成已关闭

### B. 技术审计层

至少逐项回答：

1. 当前在改什么
2. `-25` 当前如何定性；直接写 `carried partial checkpoint`
3. `-29` 当前如何定性；直接写 `carried partial checkpoint`
4. 这轮具体改了 `PlayerAutoNavigator.cs` 的哪几个分支
5. `Crowd raw ×3` 当前最终代码结果
6. `EndpointNpcOccupied raw ×1` 当前结果
7. `Ground raw matrix ×1` 当前结果
8. `SingleNpcNear raw ×1` 当前结果
9. `MovingNpc raw ×1` 当前结果
10. `NpcAvoidsPlayer ×1 / NpcNpcCrossing ×1` 当前结果
11. 如果仍 fail，新的第一责任点是什么
12. 当前是否还能把“static / 右键停位偏上”写成已关闭；如果不能，直接写 `不能`
13. changed_paths
14. 当前 own 路径是否 clean
15. blocker_or_checkpoint
16. 一句话摘要
17. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
18. 如果没跑，原因是什么
19. 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 八、这轮停发边界

这轮只有三种合法收口：

### 1. crowd 同簇被你压绿，且护栏未回归

或

### 2. crowd 仍红，但你已用 fresh rerun 把第一责任点进一步缩小到更具体的 PAN crowd 分支

或

### 3. 你在这轮发现了新的、晚于当前所有证据的 fresh blocker，并给出了 exact 证据

除此之外，不准再给第四种“已经知道只剩 crowd 红，但还在外围兜圈子”的 partial checkpoint。

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
