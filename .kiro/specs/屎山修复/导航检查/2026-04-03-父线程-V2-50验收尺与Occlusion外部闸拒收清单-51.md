# 2026-04-03-父线程-V2-50验收尺与Occlusion外部闸拒收清单-51

这轮只按 4 件事收：

1. `V2` 有没有越界去碰 `PlayerAutoNavigator.cs`
2. `V2` 有没有越界去碰 `OcclusionManager.cs / OcclusionSystemTests.cs`
3. `Occlusion` 这条 gate 到底有没有被 fresh 判成 `active / stale / cleared`
4. 只有在 gate `cleared` 时，final pack 有没有在父线程当前最新 PAN 工作树上完整重跑

---

## 一、直接通过

### 通过形态 A：blocker receipt

同时满足：

1. `V2` 没碰 `PlayerAutoNavigator.cs`
2. `V2` 没碰 `OcclusionManager.cs / OcclusionSystemTests.cs`
3. fresh `Assets/Refresh + Editor.log + status/archive` 后，`Occlusion` gate 仍被证实是 `active`
4. 回执里给出 latest exact compile lines
5. 明确写：
   - `final pack 本轮未起跑`
   - 原因是这条 external gate

### 通过形态 B：pack rerun receipt

同时满足：

1. `V2` 没碰 `PlayerAutoNavigator.cs`
2. `V2` 没碰 `OcclusionManager.cs / OcclusionSystemTests.cs`
3. fresh 证实 `Occlusion` gate `cleared`
4. final pack 已完整重跑
5. 红绿面按 case 报实
6. 仍明确写：
   - `右键停位偏上 = 不能写已关闭`

---

## 二、直接拒收

出现任一条就拒收：

1. 去碰 `PlayerAutoNavigator.cs`
2. 去碰 `OcclusionManager.cs / OcclusionSystemTests.cs`
3. 没做 fresh `Assets/Refresh + Editor.log + status/archive` 就直接下 gate 结论
4. gate 仍 active，却把 final pack 写成“已起跑/已跑一半”
5. gate 已 cleared，却不跑 final pack
6. 把 `Ground raw matrix` 绿面偷换成：
   - `右键停位偏上已关闭`
7. 继续回到旧 `legacy crowd` 口径当主 acceptance

---

## 三、父线程这轮最关心的答案

1. `Occlusion` gate 现在到底是不是活 blocker
2. 如果不是，父线程最新这两刀之后：
   - `PassableCorridor` 有没有改善
   - `StaticNpcWall` 有没有从 `ReachedBlockedTarget` 翻成 `StableHoldBeforeWall`
3. 如果还是没翻，剩余红面到底压成了什么，而不是又回到“很多东西都不对”
