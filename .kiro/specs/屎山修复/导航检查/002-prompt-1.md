# 002-prompt-1

## 最后通牒前缀

先停下你现在这种“再补一点规则、再补一点参数、再补一点日志”的蠕动式修补。  
这轮不是让你继续解释“为什么理论上已经接上了共享导航核心”，而是让你正面回答一个已经被现场反复证明的问题：

- 十多轮迭代之后，用户验收到的结果为什么还是没有任何本质变化？
- 为什么玩家面对动态 NPC 仍然像推土机？
- 为什么你自己的主表、Gemini 的文本分析、NPC 线程的正式回执，和用户现场体感之间出现了这么大的裂口？

这不是轻微问题。  
这已经是**系统级失真**：

- 文档说的是一套东西
- 代码落的是另一套东西
- Scene / 组件挂载可能又是第三套东西
- 用户最后验收到的行为结果还是旧世界

所以这轮你不允许再把任务理解成“继续补一个局部避让 patch”。  
你必须先看清：

1. 你现在到底已经落地了什么
2. 这些东西为什么没有转化成正确行为
3. 正确的目标形态到底应该是什么
4. 你这轮要不要直接改架构、改挂载、改执行层，而不是继续在表层蠕动

## 你的原始任务背景

用户这轮的真实意思，不是让你再解释一次理论，而是要求你彻底面对失败现场：

“所以你认为现在的代码问题严不严重，你说的这个“已经通的 unityMCP live 对象链路往下打”，是一个怎样的解决方案，你跳脱出来，彻底看看我们这十多次的迭代，始终没有任何的结果，我验收到的结果没有任何改变，玩家就是个推土机，只能对静态内容规避，对动态的npc不能做到规避，其实换言之，我需要的其实本质是npc和玩家以及所有的内容都使用同一个导航系统，难道不应该是这样吗，你觉得呢？你回顾你的文档本身呢？gemini对你的这个文档进行了纯文本分析，.kiro\specs\屎山修复\导航检查\005-gemini-1.md，你又是否是真的做到了这样的内容呢？问题非常严重现在，请你不要再一点一点修复了，需要彻底剖析你当前落地的到底是啥玩意，应该是个啥玩意，你还要确保你的代码写了后配置在正确的物体内，就像如图你的navgrid2d脚本挂在systems里面，但是你现在导航的脚本有这么多，完全可以做一个新的物体，把这个navgrid2d脚本也移动过去，你有mcp可以用就别省着啊，然后还有npc对你的最新回执.kiro\specs\屎山修复\导航检查\003-NPC-2.md，你也要彻底阅读啊，你的任务很重，但是你一直在蠕动，和蛆一样，我真要受不了了，你快去给我落地吧”

## 本轮必读材料

你这轮不能只看最后一句反馈，必须先完整重读并吸收以下材料：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\统一导航重构阶段设计与执行主表.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\memory.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\005-gemini-1.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\003-NPC-2.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\memory_0.md`

并且你必须把这几份材料之间的关系看清：

- `统一导航重构阶段设计与执行主表.md` 是你的设计承诺
- `005-gemini-1.md` 是对你主表的纯文本结构化理解，不是现场真相
- `003-NPC-2.md` 是 NPC 线程给你的正式协作边界和稳定承诺
- 用户现场反馈才是最终验收真相

也就是说：

- 只要现场没变好，纯文本分析说得再漂亮都不算数
- 只要现场没变好，“我已经接了共享 solver”也不算数
- 只要现场没变好，你就必须重新审视“当前落地物”和“目标架构”之间到底断在哪里

## 你必须先回答的四个问题

在继续改任何代码之前，你必须先在主表中明确回答下面四个问题，不能跳过：

1. 现在的代码问题到底严不严重？
   - 你必须明确写：这是局部 bug，还是系统级失败
   - 基于用户现场反馈，这一条我预期你会给出“系统级失败”而不是“局部偏差”

2. 你当前落地的东西，到底是不是你主表里承诺的那套东西？
   - 是哪几层已经真实落地
   - 是哪几层只是“文件存在”
   - 是哪几层根本没有在运行时形成闭环

3. 你所谓“已经通的 unityMCP live 对象链路往下打”，到底是什么意思？
   - 你不能再说抽象话
   - 你必须具体写明：你拿到了哪些 live 对象、哪些组件、哪些 Scene 证据、它们如何服务于这轮修复

4. “玩家、NPC、未来所有动态移动体都应使用同一个导航系统”这件事，你现在到底做到了没有？
   - 如果没有做到，你必须明确写出：现在实际仍然是什么结构
   - 不允许再把“共享了一些局部避让接口”偷换成“已经是同一个导航系统”

## 你这轮必须新增的主表内容

你这轮不允许继续只在主表里打勾和追加小修。  
你必须在 `统一导航重构阶段设计与执行主表.md` 中补出一个新的高优先级段落，至少包含：

- `当前落地偏移审计`
- `现场失败与文档承诺的裂口`
- `当前真实架构`
- `目标架构`
- `为什么十多轮迭代后现场仍然无变化`
- `本轮必须彻底替换或重排的执行层`

你必须明确区分：

- 文档上已经写了什么
- 代码里已经存在什么
- Scene / 组件挂载上真正生效了什么
- 用户现场真正体验到了什么

只要这四层没有完全对齐，你就不能继续假设自己已经在正确轨道上。

## 本轮禁止事项

这轮有几件事绝对禁止：

- 不准继续一点一点补参数然后期待奇迹发生
- 不准再把“已经接上共享核心”当成结论
- 不准只看代码，不看 Scene 挂载和组件配置
- 不准只看纯文本文档，不看 live 对象链路
- 不准把锅继续甩给 NPC tag / prefab 有没有挂脚本这种已经反复核过的方向
- 不准继续把问题伪装成“还差一点点”
- 不准在没有用 MCP 看清现场对象和组件的情况下继续写大段推理

## 这轮必须先做的现场核查

你有 `unityMCP` 可以用，就不要省。  
这轮必须先做 **live Scene / 组件 / 挂载** 核查，不允许只停留在 YAML 和静态代码层。

你必须至少核查这些点：

1. 当前 active scene
2. 当前导航相关根对象和组件分布
3. `NavGrid2D` 现在到底挂在哪个对象上
4. 截图里显示的 `Primary/2_World/Systems` 当前到底承担了哪些职责
5. 玩家侧导航相关组件到底挂在哪些物体上
6. NPC 侧运行时导航相关组件到底挂在哪些物体上
7. 当前共享导航脚本、执行层脚本、调试脚本是否已经出现“脚本很多，但 Scene 组织一团乱”的问题

特别是这条必须正面处理：

- 截图显示 `NavGrid2D` 现在挂在 `Systems` 上，同时这个对象上还挂着 `WorldSpawnService`、`WorldSpawnDebug`
- 当前导航相关脚本已经明显增多
- 你必须明确审计：`Systems` 继续承载 `NavGrid2D` 到底合理不合理
- 如果不合理，不要犹豫，直接利用 MCP 创建更清晰的导航专属物体，并把 `NavGrid2D` 以及相关导航核心组件迁到更正确的承载对象上

注意：

- 这不是让你为了“好看”乱搬组件
- 而是要求你把 **架构语义、Scene 承载、组件配置** 对齐

## 关于 `NavGrid2D` 与 Scene 承载的硬要求

你必须明确给出结论：

1. `NavGrid2D` 作为静态路径层，是否应该继续留在当前 `Systems`
2. 当前导航核心是否应该拥有一个更独立的承载对象，例如：
   - `NavigationSystemRoot`
   - `NavigationRoot`
   - 或其他你认为更清晰的命名
3. 如果你认为应该迁移，那就不要只是写建议
   - 这轮直接用 MCP 落地迁移
   - 确保引用、配置、运行态都正确
4. 如果你认为不该迁移，那你必须写出不迁移的理由
   - 并且这个理由必须能经得起当前现场问题的拷打

## 关于 NPC 回执的硬边界

你必须完整吃透：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\003-NPC-2.md`

NPC 线程已经明确告诉你了：

- 哪些是它当前稳定不动的基线
- 哪些不是你该继续怀疑的点
- 你该认领的代码范围在哪里

因此这轮你不允许再漂移去修 NPC 表现层、气泡层、测试样本工具链。  
你真正应该认领的是：

- `Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs`
- `Assets/YYY_Scripts/Service/Navigation/*`
- `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs`
- 玩家 / NPC 共享的动态占位、避让、让行、重规划、执行层收口逻辑

除非真的需要 NPC 线程配合改接口，否则不要把它的表现层和工具链一起卷进去。

## 你这轮真正要做的，不是继续补 patch，而是重排导航闭环

基于用户这轮反馈，你必须先接受一个现实：

- 现在的问题已经不是“共享 solver 方向还差一点”
- 现在的问题是：**玩家、NPC、动态代理、静态路径、执行层、Scene 挂载，根本没有真正形成一套被现场验证通过的统一导航闭环**

所以这轮你必须直接重排下面这些东西：

1. 静态路径层
   - `NavGrid2D` 只承担什么
   - 不承担什么

2. 动态代理层
   - 共享 registry / snapshot / rules / solver 到底解决什么
   - 解决不到什么程度就必须上更统一的执行层

3. 共享路径执行层
   - 你必须明确判断：现在最根本的问题是不是因为 S4 还没有真正成立
   - 如果答案是“是”，那你这轮就不该继续拿 S3/S5/S6 的碎补丁拖时间

4. 玩家 / NPC 最终运动语义
   - 你必须再次正面判断：
     - 玩家和 NPC 当前是否真的在同一导航系统中运动
     - 还是只是“共享部分路径和局部避让逻辑，但最终运动语义仍然分裂”

5. Scene / 组件承载
   - 代码写了不算
   - 配在正确物体上、生效在正确对象里，才算

## 这轮建议你采取的执行顺序

你不要再自由发挥顺序了。  
这轮建议按下面顺序推进：

1. 先重读主表、memory、`005-gemini-1.md`、`003-NPC-2.md`
2. 直接用 MCP 读 live scene / gameobject / components
3. 在主表中补“当前落地偏移审计”
4. 明确写出：
   - 当前真实架构
   - 目标架构
   - 当前差的不是哪一个参数，而是哪一层没真正成立
5. 再决定本轮是否必须直接上：
   - 共享路径执行层真正落地
   - 玩家 / NPC 最终运动语义统一收口
   - `NavGrid2D` 承载对象迁移与导航根对象整理
6. 然后直接实现，不要再做一轮纯分析后停下
7. 实现后必须做 live 验证，不准只靠静态编译自我安慰

## 本轮最低验收标准

你这轮交付不能再是“理论更像样了”。  
最低必须能回答并验证下面三件事：

1. 玩家绕移动 NPC 的真实行为结果
   - 不是日志好看
   - 是真实体感不再推着走

2. NPC 绕玩家、NPC/NPC 会车的真实行为结果
   - 不是只看一帧
   - 是能稳定复现并稳定规避

3. Scene / 挂载 / 配置是否已对齐
   - `NavGrid2D` 和导航核心是否在正确物体上
   - 是否还存在“脚本加了很多，但 Scene 组织还是糊的”情况

## 最终回执格式

不要继续在聊天里给我一大段“我觉得”。  
你完成后，只按下面格式回：

- 当前在改什么：
- 是否已补主表中的“当前落地偏移审计”：
- 是否已用 MCP 完成 live Scene / 组件核查：
- 是否调整了导航相关物体 / 组件挂载：
- 改了哪些文件：
- 玩家绕移动 NPC 的真实结果：
- NPC 绕玩家 / NPC-NPC 会车的真实结果：
- 当前剩余失败样本或边界：
- blocker_or_checkpoint：
- 一句话摘要：

## 最后再强调一次

你这轮不是来继续“写几个看起来很努力的导航补丁”的。  
你这轮是来彻底面对一个已经反复失败的系统级问题：

- 你当前落地的到底是啥玩意
- 正确目标到底应该是啥玩意
- 为什么十多轮之后现场还是原样
- 你这轮怎么把这条线真正掰回正轨

如果你这轮还继续在局部参数、局部规则、局部日志上蠕动，那这条线就继续没有意义。  
直接去读、去看、去改、去验，不要再漂移了。
