# 002-prompt-2

你上一轮的“当前落地偏移审计”我收到了。  
这一步不算白做，而且它已经给出了一个很关键的结论：

- 当前问题是系统级失败，不是局部参数没调好

既然你自己已经用 unityMCP 证明：

- `Primary/2_World/Systems` 当前混装了 `NavGrid2D + WorldSpawnService + WorldSpawnDebug + GameInputManager`
- 玩家与 3 个 live NPC 的 `navGrid` 引用都指向同一个 `Systems/NavGrid2D`
- 用户现场体感仍然是“玩家像推土机，动态 NPC 规避无效”

那你现在就没有资格再停在“我先把裂口写实”这一步。  
下一轮你必须进入真正的结构落地阶段。

## 一、这轮 checkpoint 已接受，但只接受为“前置审计完成”

你上一轮完成的事情我认账：

- 已补主表里的“当前落地偏移审计”
- 已做 live Scene / 组件核查
- 已确认当前落地物和主表承诺之间存在实质裂口
- 已确认 `Systems` 当前的导航承载语义不干净

但这不意味着线程可以停下。  
这个 checkpoint 的唯一作用是：

- 证明你现在终于不该继续局部 patch 了

所以从这轮开始，你的主线正式切换为：

- `导航结构重排 + S4 共享路径执行层落地 + live 行为终验`

## 二、你这轮不允许再停在“继续分析”

这轮你不能再交：

- 新一版长篇审计
- 新一版“当前问题很严重”的说明
- 新一版“理论目标架构”

因为这些你已经说过了。  
现在你必须把下面三件事往前推进：

1. `NavigationRoot` 承载对象裁决与必要迁移
2. S4 共享路径执行层真正落地
3. 玩家 / NPC / NPC-NPC 的 live 终验

## 三、先做但不能只做的，是 `NavigationRoot` 五段分析

你上轮自己已经给出下一步了：

- 先按 `scene-modification-rule.md` 输出 `NavigationRoot` 迁移五段分析

这一条你这轮必须做，而且要直接补进：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\统一导航重构阶段设计与执行主表.md`

五段内容至少必须包含：

1. 当前 `Systems` 的真实职责污染
2. 为什么 `NavGrid2D` 继续挂在 `Systems` 上不利于语义收口
3. `NavigationRoot` 的候选结构
4. 迁移风险、引用面和回滚点
5. 本轮落地顺序

但是注意：

- 这五段分析不是让你写完再停
- 它是这轮结构落地前的最后一道设计裁决

## 四、默认预期：除非你拿出强证据，否则这轮直接落地 `NavigationRoot`

基于你上一轮的 live 审计结果，这轮的默认预期非常明确：

- 除非你能拿出足够强的现场证据证明“继续把 `NavGrid2D` 留在 `Systems` 完全合理”
- 否则这轮就不要继续犹豫，直接利用 unityMCP 完成 `NavigationRoot` 迁移

迁移的最低要求：

1. 为导航核心建立更清晰的承载对象
   - 名称可以是 `NavigationRoot`、`NavigationSystemRoot`，但必须语义明确
2. 把导航核心承载和 `WorldSpawnService / WorldSpawnDebug / GameInputManager` 解耦
3. 迁移后核对：
   - 玩家侧引用
   - NPC 侧引用
   - 运行态导航核心引用
4. 不要顺手把不相关系统全搬了

也就是说：

- 你这轮不是做“场景美化整理”
- 你这轮是把架构承载和组件语义收回来

## 五、真正的硬骨头不是挂载，而是 S4

你自己上一轮已经承认了：

- `S4 共享路径执行层仍未落地`
- 玩家和 NPC 仍保留私有路径执行主循环
- 最终运动语义仍分裂

那这轮就别再绕了。  
真正的硬骨头不是“再补一点 registry / solver / rule”，而是：

- 你必须把玩家与 NPC 的最终路径执行语义真正拉到同一套闭环里

这轮你必须正面回答并落地：

1. 当前玩家路径执行主循环是什么
2. 当前 NPC 路径执行主循环是什么
3. 哪些部分已经共享
4. 哪些部分仍然分裂
5. S4 真正落地后，共享的到底是：
   - 路径段推进
   - 到达判定
   - 动态让行
   - 重规划触发
   - 阻塞恢复
   - 还是整条执行语义

如果只是“又共享了一点局部接口”，那不算 S4 真成立。

## 六、你这轮的推荐硬顺序

不要再自由发挥。  
这轮建议按下面顺序推进：

1. 在主表补齐 `NavigationRoot` 五段分析
2. 用 unityMCP 完成 `Primary` live 承载对象调整
3. 核对迁移后的引用与组件分布
4. 进入 S4 共享路径执行层的真实收口
5. 补必要的代码与测试
6. 做 live 终验

这轮默认不允许做成“分析一轮，下轮再迁移，再下轮再修执行层”。  
除非你遇到下面这种硬阻塞，才允许拆轮：

- `Primary.unity` 被别的线程占用
- Unity live 写窗口拿不到
- S4 落地暴露出新的编译级断裂，必须先单独收敛

否则不要自己给自己降级节奏。

## 七、本轮 live 终验要求

这轮不是编译过了就算完成。  
你最低必须给出这三类真实结果：

1. 玩家绕移动 NPC
   - 不是静态障碍
   - 是 live 移动 NPC
   - 结果必须明确回答“是否仍像推土机”

2. NPC 绕玩家
   - 至少有一个稳定复现场景

3. NPC-NPC 会车 / 交错
   - 至少有一个稳定复现场景

如果其中任何一条没过，你就必须明确写失败样本，不准模糊成“已有改善”。

## 八、本轮禁止事项

这轮绝对禁止：

- 继续只补主表和 memory 不动代码
- 继续只做 solver / rule 层的碎 patch
- 在没有调整承载或执行层的情况下继续谈“统一导航”
- 继续把问题伪装成“只差一点点”
- 没做 live 行为终验就宣布通过

## 九、本轮回执格式

下一次回执，不要再交“审计已完成”。  
请严格按下面格式回复：

- 当前在改什么：
- 是否已补 `NavigationRoot` 五段分析：
- 是否已创建或迁移到 `NavigationRoot`：
- 实际调整了哪些 live 物体 / 组件挂载：
- 是否已落 S4 共享路径执行层关键改动：
- 改了哪些文件：
- 玩家绕移动 NPC 的真实结果：
- NPC 绕玩家 / NPC-NPC 会车的真实结果：
- 当前剩余失败样本或边界：
- blocker_or_checkpoint：
- 一句话摘要：

## 十、最后再说一次

你上一轮最有价值的地方，不是“你写得很清楚”，而是你终于承认了这不是局部问题。  
既然已经承认，那你这轮就必须开始真正重排结构，不准再回头做看起来很努力、但不会改变现场体感的小补丁。
