你现在不用再怀疑 NPC 有没有接导航入口了。  
NPC 线程这边已经把自己的稳定基线收住了，你现在要解决的是“动态避让闭环为什么还没真正成立”。

一、当前 live 基线
- 项目：D:\Unity\Unity_learning\Sunset
- 分支：main
- HEAD：e339ccd6
- 当前 NPC 相关成果已经在 main
- 003 已正式变成验证样本 NPC，不再只是临时压测对象

二、NPC 线程当前已经稳定的东西
1. 状态语义稳定
- NPCAutoRoamController 仍以：
  - Inactive
  - ShortPause
  - Moving
  - LongPause
  作为核心状态
- 这套语义当前不要再怀疑，也不要假设 NPC 线程会突然改掉

2. IsMoving 语义稳定
- NPCAutoRoamController.IsMoving：状态层意义上的“是否在 Moving”
- NPCMotionController.IsMoving：动画桥接层基于速度阈值得出的“是否在动”
- 当前这两层含义都视为稳定

3. 最终位移语义稳定
- NPC 当前最终位移主路径仍是 rb.MovePosition(...)
- 路径推进在 NPCAutoRoamController
- 动画和速度桥接在 NPCMotionController
- 当前阶段 NPC 线程不会主动把它改成另一套运动写法

4. Prefab 基线稳定
- 基线样本：
  - Assets/222_Prefabs/NPC/001.prefab
  - Assets/222_Prefabs/NPC/002.prefab
  - Assets/222_Prefabs/NPC/003.prefab
- 当前共同基线：
  - Rigidbody2D
  - BoxCollider2D
  - NPCAnimController
  - NPCMotionController
  - NPCAutoRoamController
  - NPCBubblePresenter
- 当前碰撞/刚体基线：
  - isTrigger = false
  - 下半身实体碰撞
  - mass = 6
  - linearDamping = 8
  - interpolation = Interpolate
  - collisionDetection = Continuous

5. 003 的定位稳定
- 003 是长期保留的验证样本 NPC
- 默认绑定 NPC_BubbleReviewProfile
- 默认保留 NPCBubbleStressTalker
- NPCBubbleStressTalker 是长期保留测试入口，但不是正式生产行为组件

三、NPC 线程最近已经补好的工具能力
1. 场景化入口已在位
- Assets/Editor/NPCAutoRoamControllerEditor.cs
- Assets/Editor/NPCSceneIntegrationTool.cs
- 已支持：
  - Home Anchor 创建 / 清空 / 吸附
  - Profile 复制 / 应用 / 选中
  - Scene NPC 批量切 Production / BubbleReview
  - 批量补挂或移除 NPCBubbleStressTalker

2. 生成器分流已在位
- Assets/Editor/NPCPrefabGeneratorTool.cs
- 已支持：
  - 正式 NPC / 验证样本 NPC 自动分流
  - 默认把 003 识别为验证样本
  - 验证样本自动绑定 NPC_BubbleReviewProfile
  - 按需自动补挂 NPCBubbleStressTalker

这意味着：
- 后续如果需要更多复现样本，NPC 线程已经能稳定生成
- 你现在不用再把“NPC 样本不稳定”当作主要怀疑项

四、你现在真正要解决的问题
P0：
- 玩家右键自动导航时，路线遇到移动中的 NPC，当前仍会出现“玩家推着 NPC 走”
- NPC 与 NPC 相遇时，当前仍可能撞住、顶住、互挤，而不是自然绕开

P1：
- 玩家路径执行面对突然经过的 NPC，没有形成稳定动态避让
- NPC 自己的漫游执行也没有形成稳定的局部避障 / 分离 / 让行
- 缺少动态障碍占位与实时重规划闭环
- 缺少窄路会车、路口礼让、优先级处理
- 玩家 agent 与 NPC agent 还缺少更明确的优先级 / 半径 / 运动语义协商

一句话：
不是“NPC 不会走”，而是“会走了，但不会像人一样在动态环境里互相让开”。

五、你应认领的代码范围
优先认领：
- Assets/YYY_Scripts/Service/Navigation/NavGrid2D.cs
- Assets/YYY_Scripts/Service/Navigation/*
- Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs
- 玩家/NPC 共享的动态占位 / 避让 / 让行 / 重规划逻辑

不要顺手改成你负责的：
- Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs
- Assets/YYY_Scripts/Controller/NPC/NPCBubbleStressTalker.cs
- Assets/Editor/NPCBubbleStressTalkerEditor.cs
- Assets/Editor/NPCAutoRoamControllerEditor.cs
- Assets/Editor/NPCSceneIntegrationTool.cs
- Assets/Editor/NPCPrefabGeneratorTool.cs

除非需要 NPC 线程配合改接口，否则不要把 NPC 表现层、测试样本和工具链一起卷进去。

六、NPC 线程给你的稳定承诺
在你排查导航 P0/P1 期间，NPC 线程当前不会主动乱动这些基线：
- Moving / ShortPause / LongPause
- IsMoving 的现有含义
- rb.MovePosition(...) 作为 NPC 当前最终位移主路径
- 001 / 002 / 003 的 Rigidbody2D + BoxCollider2D + roam 基线
- 003 作为验证样本的正式定位

如果未来必须改，我会先明确同步：
- 会不会动 IsMoving
- 会不会动 pause / move 状态定义
- 会不会动最终位移方式

七、我需要你回给我的验收报告
请不要只回一句“已经接上导航”。

我需要你明确回：
1. 改了哪些文件
2. 玩家绕移动 NPC 的真实行为结果
3. NPC 绕玩家、NPC/NPC 会车的真实行为结果
4. 还剩哪些失败样本、失败条件和边界情况
5. 如果需要 NPC 线程配合，精确指出要改哪个接口、字段或执行点

最好再附一份最小联调清单，让我能在你交付后立刻做 NPC 侧接入和终验。
