# 2026-04-02-父线程-V2-46验收尺与crowd语义纠偏拒收清单-47

## 1. 这份文档的目的

这是父线程下一次收 `-46` 回执时的固定验收尺。

它专门防四件事：

1. `V2` 明知旧 `Crowd raw` 语义错了，却继续拿它当主 acceptance case
2. `V2` 还没纠偏测试语义，就先继续改 `PlayerAutoNavigator.cs`
3. `V2` 把“blocked-wall 不再要求撞穿”偷换成“什么都不做也算 pass”
4. `V2` 再次把 `Ground raw matrix` 的 `center-only` 结构绿偷换成“右键停位偏上已关闭”

---

## 2. 下一次回执的固定审查顺序

### 第一关：先审它有没有真的纠偏 crowd 语义

必须先看：

1. 旧 `Crowd raw` 有没有被明确降级 / 改名 / 标记为 legacy
2. 它有没有新增两类新 case：
   - `PassableCorridor`
   - `StaticNpcWall`
3. 这两类 case 有没有各自明确的：
   - 目标语义
   - outcome
   - pass/fail 判定

只要缺任一项，继续拒收。

### 第二关：再审它有没有先停 runtime 改动

必须先看：

1. 这轮是否实际改了 `[PlayerAutoNavigator.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs)`

如果它在语义纠偏没完成前就继续改 runtime，继续拒收。

### 第三关：再审新 case 是否真的 fresh 跑了

必须至少看见：

1. `PassableCorridor ×3`
2. `StaticNpcWall ×3`
3. 最小护栏：
   - `EndpointNpcOccupied raw ×1`
   - `Ground raw matrix ×1`
   - `SingleNpcNear raw ×1`
   - `MovingNpc raw ×1`

### 第四关：最后才审它有没有给出“留/回”裁定

必须看见：

1. 最近两刀 `PlayerAutoNavigator.cs` crowd 补口，在新语义下是：
   - `建议保留`
   - 还是 `建议下轮回退`
2. 并且给出原因

---

## 3. 下一次回执的最小通过条件

如果它想把 `-46` 判成“这轮站住”，最少必须同时满足：

1. crowd 测试语义已纠偏
2. 新 case 已 fresh 跑起来
3. 旧 crowd case 已被明确降级
4. 已给出 PAN crowd 两刀补口的“留/回”裁定
5. 没有偷换“右键停位偏上已关闭”

---

## 4. 继续拒收的四种假完成

### 假完成 A

- “旧 crowd case 虽然语义不完美，但先继续拿它看趋势”

拒收条件：

- 用户已经正式纠正该语义

### 假完成 B

- “我已经改了新的 PAN crowd 代码，下一轮再补语义”

拒收条件：

- 顺序颠倒

### 假完成 C

- “StaticNpcWall 现在 pass，因为玩家没有动”

拒收条件：

- 没有明确 outcome，或只是把不作为包装成 pass

### 假完成 D

- “Ground raw 继续 pass，所以停位偏上应该可以先算关”

拒收条件：

- 仍没有用户可视停位真值成立证据

---

## 5. 父线程下一次固定追问

1. 旧 `Crowd raw` 现在到底被你降级成什么语义类别？
2. `PassableCorridor ×3` 新结果是什么？
3. `StaticNpcWall ×3` 新结果是什么？
4. 最近两刀 `PlayerAutoNavigator.cs` crowd 补口，你现在建议保留还是下轮回退？
5. 你为什么现在仍然不能把“右键停位偏上”写成已关闭？
