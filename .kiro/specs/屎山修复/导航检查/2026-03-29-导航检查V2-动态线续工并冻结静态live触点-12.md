# 2026-03-29-导航检查V2-动态线续工并冻结静态live触点-12

这不是让你停工，  
而是父线程已经正式接手“静态点导航回归止血”，  
你这轮必须继续动态线，但同时冻结静态 runtime 触点与 shared Unity 的菜单 live 抢占。

---

## 一、你这轮先接受的现场事实

父线程这轮已经亲自落地并验证了下面这些事实：

1. `PlayerAutoNavigator` 的普通地面点导航契约已经改成：
   - `GetPlayerPosition()` 优先取 `Collider.bounds.center`
   - 普通点导航的 `GetPathRequestDestination()` 不再叠加旧 offset
   - 点导航完成判定改用玩家实际占位中心对点击点
2. 父线程已经独立建立了静态验证链：
   - `NavigationStaticPointValidationRunner.cs`
   - `NavigationStaticPointValidationMenu.cs`
3. 当前静态 runner 最新 fresh 证据已经证明：
   - `case_start` 里 `navTarget == target`
   - 样本筛选已不再是旧的 `path.Count > 2` 错判
   - 当前真正卡住静态线 fresh 的，是 shared Unity 里外部 `ExecuteMenuItem / SpringUiEvidence` 抢占窗口，而不是静态契约又没改

所以这轮你不准再把“普通点停偏”拿去当动态线的主刀理由。

---

## 二、这轮唯一主刀

只继续动态线。  
只继续你自己的动态高保真矩阵 / 动态坏相收窄。  
不要再碰父线程当前静态点 runtime 触点。

更具体地说：

1. 你可以继续做：
   - 动态避让坏相收窄
   - 动态测试矩阵整理
   - code-only 根因压窄
   - 不进入 shared Unity live 的脚本/文档/报告工作
2. 你这轮不准做：
   - 改 `PlayerAutoNavigator.GetPlayerPosition()`
   - 改 `PlayerAutoNavigator.GetPathRequestDestination()`
   - 改 `PlayerAutoNavigator.CompleteArrival()` 里的普通点语义
   - 改 `NavigationStaticPointValidationRunner.cs`
   - 改 `NavigationStaticPointValidationMenu.cs`
   - 在 shared Unity 里再发任何 `execute_menu_item / Play / pending action` 类型 live

换句话说：

父线程当前持有“静态点导航 + 静态验证窗口”；
你继续做“动态线代码/矩阵/责任点收窄”，
但暂时不要再抢 shared Unity 的 live 菜单入口。

---

## 三、你这轮允许的 scope

优先：

1. `NavigationLocalAvoidanceSolver.cs`
2. `NavigationLiveValidationRunner.cs`
3. `NavigationLiveValidationMenu.cs`
4. `NPCAutoRoamController.cs`
5. 你自己的动态测试报告 / memory / 续工文档

谨慎：

1. `PlayerAutoNavigator.cs`

只有在完全不碰“普通地面点导航静态契约触点”的前提下，  
你才可以读或局部分析这个文件；  
这轮默认不要写它。

---

## 四、你这轮禁止漂移

1. 不准再把普通点静态偏移重新解释成“动态 residual”
2. 不准再动父线程刚搭的静态 runner/menu
3. 不准在 shared Unity 里继续排菜单型 live
4. 不准把这轮回执写成“我先等等父线程”
5. 不准重新漂回 solver 大宣讲

---

## 五、你这轮最少要交什么

你这轮至少交出下面其中之一：

### 结局 A

动态线在不碰静态 runtime 的前提下，  
你把当前第一责任点继续压窄了。

### 结局 B

你明确说明：

1. 当前若还需要 shared Unity live，为什么这轮先不进
2. 你在 code-only / matrix / 报告层把动态线继续推进到了哪
3. 下一刀动态线最窄责任点是什么

但无论 A 还是 B，  
你都不能再动父线程静态 slice 的触点。

---

## 六、固定回执格式

只按下面字段回：

1. 当前在改什么
2. 这轮是否触碰 `PlayerAutoNavigator` 静态点契约触点（`yes/no`）
3. 这轮是否触碰 `NavigationStaticPointValidationRunner/Menu`（`yes/no`）
4. 这轮是否进入 shared Unity live / execute_menu_item / Play（`yes/no`）
5. 当前动态线最窄第一责任点
6. changed_paths
7. blocker_or_checkpoint
8. 一句话摘要

---

## 七、一句话提醒

父线程现在要先把“静态到底是不是还坏，以及哪些 fresh 被外部菜单污染了”钉死。  
你继续做动态线，但这轮别再来抢静态 runtime 和 shared Unity live 窗口。
