# TD_13_阶段21历史回读暂停与分支租约死锁修复

- 状态：进行中
- 正式正文：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\21_第一次唤醒复盘与shared-root分支租约闸门`
- 记录目的：
  - 只记录当前暂停点、恢复点与紧急插单。
  - 不在本 TD 内承接阶段正文分析。

## 一、当前被暂停的原主线

### 主线名称
- 阶段 `21` 的历史回读、规则来源压实与查漏补缺。

### 当前任务清单
1. 摸底 `.kiro/hooks`、`Steering规则区优化`、`Claude迁移相关` 目录与文件。
2. 按主题回读 2 月历史文档与 hooks 关键实现。
3. 归纳这些历史材料对当前 Sunset 规则与用户需求的补强结论。
4. 继续补齐尚未全文精读的大文件。
5. 扫描历史对话与现有工作区，确认是否还有遗漏、未收口事项或可继续完善项。

### 当前已完成进度
- 已读完 `.kiro/hooks` 四件套。
- 已读完 `2026.02.06规则更新`、`2026.02.15_代办规则体系` 等 2 月小文件。
- 已完成 `CLAUDE.md`、`.claude/settings*.json`、`.claude/hooks/*` 的回读。
- 已完成 `History/2026.03.07-Claude-Cli-历史会话交接/` 的目录级与关键索引级回读。
- 已确认一条关键历史主线：
  - 旧治理结论早就明确 “不要神化 hook，真正要迁移的是收尾纪律、唯一入口、工作区先行与 append-only”。
- 已确认当前读取方式需统一按 `UTF-8`，此前终端乱码主要是读取方式噪音，不是文件批量损坏。

### 当前恢复点
- 下一步优先继续精读：
  - `Claude迁移与规划/全面理解报告.md`
  - `Claude迁移与规划/工作流反思与整改方案_2026-03-06.md`
  - `Claude迁移与规划/修改清单_2026-03-06.md`
  - `History/2026.03.07-Claude-Cli-历史会话交接/规则修复.md`
  - `History/2026.03.07-Claude-Cli-历史会话交接/春一日V1.md`
  - `History/2026.03.07-Claude-Cli-历史会话交接/春一日V2.md`
  - `2026.02.11智能加载/memory_0.md`
  - `2026.02.14_Hook机制问题与方案/memory.md`

## 二、紧急插单 Bug

### 问题名称
- `grant-branch -> ensure-branch` 在 shared root 上发生自锁。

### 现场事实
- `grant-branch` 会先写脏：
  - `.kiro/locks/shared-root-branch-occupancy.md`
- 随后 `ensure-branch` 直接用“工作树必须全干净”的逻辑拦截。
- 导致合法租约刚发出，就被自己制造的 occupancy dirty 卡死。

### 已知影响
- 线程行为本身是合规的。
- 当前故障属于 `git-safe-sync.ps1` 的逻辑漏洞，不是线程违规。
- 会让 shared root 停在：
  - `main`
  - 但 `shared-root-branch-occupancy.md` dirty
  - 且 `lease_state = branch-granted`

### 紧急修复目标
1. 修复 `D:\Unity\Unity_learning\Sunset\scripts\git-safe-sync.ps1`。
2. 让 `ensure-branch` 在 shared root / `main` / 已拿到合法 grant 的前提下，允许将 `.kiro/locks/shared-root-branch-occupancy.md` 视为合法 dirty。
3. 保持其余 dirty 仍然会阻断，不能把白名单放大成通配豁免。
4. 修完后给出回到 neutral 的处理口径与重测建议。

## 三、一句话恢复口径

- 原主线不是取消，而是被 `P0` Bug 暂停。
- 当前先修租约死锁，修完再回到阶段 `21` 的历史回读与查漏补缺。
