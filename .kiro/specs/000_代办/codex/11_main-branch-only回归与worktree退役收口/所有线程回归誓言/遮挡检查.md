1. 不需要。按当前现行规则和线程映射，`遮挡检查` 的默认入口仍是 `D:\Unity\Unity_learning\Sunset @ main`，`worktree` 只作为高风险隔离、故障修复、特殊实验的例外工具，不是这个线程的默认前提。

2. 接受。后续如果进入实际整改，我接受默认走“共享根目录 + 分支”模式；只有共享热文件冲突不可接受、需要高风险隔离，或要做特殊实验时，才升级到独立 `worktree`。

3. 当前最合理的 branch-only 开工入口是：
- 先回到 `D:\Unity\Unity_learning\Sunset` 的 `main`
- 先做一次新的 `preflight` / 热文件占用核查
- 如果确认正式开整改，再创建并切入新的任务分支
- 推荐分支名：`codex/occlusion-remediation-001`