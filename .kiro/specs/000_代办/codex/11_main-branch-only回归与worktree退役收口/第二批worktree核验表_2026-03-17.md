# 第二批 worktree 核验表（2026-03-17）

## 1. 文档目的
- 本文件用于记录第二批待退役 worktree 的最新真实现场。
- 这样后续删除第二批时，不需要再重复做基础取证。

## 2. 核验结果

### A. farm-1.0.2-cleanroom001
- 路径：`D:\Unity\Unity_learning\Sunset_worktrees\farm-1.0.2-cleanroom001`
- 分支：`codex/farm-1.0.2-cleanroom001`
- `HEAD`：`66c19fa17a55afec7bf2e0a2a1c695aa0c7f75d0`
- 工作树状态：`CLEAN`
- 当前判断：
  - 已具备作为 farm continuation branch carrier 的条件
  - 当前剩余问题不是它脏，而是 shared root 还没恢复成可供它迁回的 `main`
  - 删除前提已明确：shared root 回 `main` 后，在根目录成功检出 `codex/farm-1.0.2-cleanroom001` 并保持 `git status` clean

### B. NPC_roam_phase2_continue
- 路径：`D:\Unity\Unity_learning\Sunset_worktrees\NPC_roam_phase2_continue`
- 分支：`codex/npc-roam-phase2-002`
- `HEAD`：`6e2af71b204750f554935eae3728108d23c86bb2`
- 工作树状态：`CLEAN`
- 当前判断：
  - continuation branch 已 clean
  - 当前剩余问题不是它脏，而是 shared root 还没恢复成可供它迁回的 `main`
  - 删除前提已明确：shared root 回 `main` 后，在根目录成功检出 `codex/npc-roam-phase2-002` 并保持 `git status` clean

### C. NPC_roam_phase2_rescue
- 路径：`D:\Unity\Unity_learning\Sunset_worktrees\NPC_roam_phase2_rescue`
- 分支：`codex/npc-roam-phase2-001`
- `HEAD`：`28aef95d16f176ac5538bbd93c73769b2c686a8c`
- 工作树状态：`CLEAN`
- 当前判断：
  - `spring-day1` 二轮裁定已完成，并已实际执行：
    - 两处字体 dirty 的证据已导出
    - 当前 dirty 已全部丢弃，保留各自已提交版本
  - 它现在已经退化为纯 rescue 取证点，不再挂着他人尾巴
  - 当前删除前提只剩：
    - shared root 回 `main`
    - 根目录 branch-only 检出验证完成

### D. spring-day1-story-progression-001
- 路径：`D:\Unity\Unity_learning\Sunset_worktrees\spring-day1-story-progression-001`
- 分支：`codex/spring-day1-story-progression-001`
- `HEAD`：`a9c952b78f1b0184edc6622820158b70ee1d7ea6`
- 工作树状态：`CLEAN`
- 当前判断：
  - 它当前是 clean checkpoint 容器
  - `spring-day1` 的 branch-only 入口已经给出
  - 当前剩余问题不是它脏，而是 shared root 还没恢复成可供它重新检出的 `main`

## 3. 当前结论
- 第二批 worktree 里：
  - `farm-1.0.2-cleanroom001`：clean
  - `NPC_roam_phase2_continue`：clean
  - `NPC_roam_phase2_rescue`：clean
  - `spring-day1-story-progression-001`：clean
- 所以第二批当前真正的阻塞已经进一步缩成：
  1. shared root 还没恢复为 `main`
  2. 还没做根目录 branch-only 检出验证

## 4. 删除前提
1. shared root 回 `main` 的前置条件表完成。
2. 在根目录完成：
   - `codex/spring-day1-story-progression-001`
   - `codex/npc-roam-phase2-002`
   - `codex/farm-1.0.2-cleanroom001`
   的 branch-only 检出验证。

## 5. 当前一句话结论
- 第二批 worktree 现在已经全部 clean，缺的只剩 shared root 中性化和根目录 branch-only 验证。
