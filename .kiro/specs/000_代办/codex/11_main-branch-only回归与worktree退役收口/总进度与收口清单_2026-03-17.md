# 总进度与收口清单（2026-03-17）

## 1. 当前结论
- 当前用户视角完成度：`11` 阶段 `100%`。
- 当前治理推进度：`11` 阶段 `100%`，Git/现场层面的 branch-only 回归已经真实完成。
- 当前不再需要继续讨论“worktree 是否能常态化”；这个问题在 Sunset 已经被实际收口为“不能”。
- 当前也不再需要继续给 `NPC / farm / spring-day1` 发 branch-only prompt；三条业务线的根目录检出验证已经真实做完。

## 2. 已完成

### 2.1 规则与口径层
- 已明确 `11` 是当前收口主线，`09/10` 只保留前置治理成果，不再继续堆剩余任务。
- 已明确红线：
  - `worktree` 只允许作为事故隔离例外
  - 最终必须回到 `共享根目录 + 分支`

### 2.2 Git / worktree 事实层
- 已完成 worktree 全景盘点，并收口到终局现场：
  - `D:\Unity\Unity_learning\Sunset @ main`
- 已锁定三条主线的分支 carrier：
  - `farm -> codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
  - `NPC -> codex/npc-roam-phase2-002 @ 6e2af71b`
  - `spring-day1 -> codex/spring-day1-story-progression-001 @ a9c952b7`
- 已将治理收口提交安全落盘到 `main`：
  - 污染链侧治理提交：`af3060f9`
  - `main` 干净终局提交：`763ba4a1`

### 2.3 三大线程事实层
- `farm`
  - cleanroom 分支已被证实可用，且已完成根目录 branch-only 检出验证。
  - 后续唯一入口为：
    - `D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
- `NPC`
  - continuation branch 已完成根目录 branch-only 检出验证。
  - 后续唯一入口为：
    - `D:\Unity\Unity_learning\Sunset @ codex/npc-roam-phase2-002 @ 6e2af71b`
- `spring-day1`
  - clean checkpoint 已明确，branch-only 入口已明确为：
    - `D:\Unity\Unity_learning\Sunset @ codex/spring-day1-story-progression-001 @ a9c952b7`
  - `Primary.unity / DialogueUI.cs` 已明确不再属于未固化尾巴。
  - 二轮裁定已落地，shared root 与 rescue 的字体 dirty 已完成证据导出并清空。
  - 已完成根目录 branch-only 检出验证。

### 2.4 文档与治理层
- 已重写 `11` 阶段分析、任务、执行方案与 memory。
- 已新增本文件和 branch-only 分发 prompt 文件。
- 已新增：
  - `所有线程回归誓言.md`（线程回包索引）
  - `所有线程回归誓言\*.md`（线程原始回包）
  - `共享根目录dirty归属初版_2026-03-17.md`
  - `第二批worktree核验表_2026-03-17.md`
- 已把 `11` 纳入现行状态说明和现行入口总索引。
- 已新增终局快照：
  - `终局快照_2026-03-17.md`

### 2.5 真实现场推进
- 已真实退役第一批纯历史 worktree：
  - `main-reflow-carrier`
  - `NPC`
  - `farm-10.2.2-patch002`
- 已真实退役第二批 worktree：
  - `farm-1.0.2-cleanroom001`
  - `NPC_roam_phase2_continue`
  - `NPC_roam_phase2_rescue`
  - `spring-day1-story-progression-001`
- `npc_restore.zip` 与 `Assets/Screenshots*` 已移出仓库工作树。
- 当前 `git worktree list --porcelain` 只剩共享根目录。

## 3. 11 阶段剩余项
- 无。
- 这一阶段的 Git/现场目标已经全部完成，后续不再向 `11` 塞入新的过程尾巴。
- 如果把视角放大到整个 `000_代办/codex`，后续治理工作应回到：
  - `09_强制skills闸门与执行规范重构`
  - 或针对新的治理问题另立阶段

## 4. 当前 prompt 结论
- 当前没有仍需继续下发的 `11` 阶段 prompt。
- [分发Prompt_2026-03-17.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/000_代办/codex/11_main-branch-only回归与worktree退役收口/分发Prompt_2026-03-17.md) 现在只保留为过程留档。
- 若后续继续治理，应直接基于现行状态入口和对应阶段文档开工，不再重复走 `11` 的回包流程。

## 5. 当前一句话判断
- 现在已经不是“怎么把 worktree 退役”的执行阶段，而是“`11` 已完成、shared root 已回 `main`、Sunset 已恢复 `main + branch-only` 常态”的终局状态。
