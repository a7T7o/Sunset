# 总进度与收口清单（2026-03-17）

## 1. 当前结论
- 现在不是“worktree 是否合理”的讨论阶段，而是“怎么把它们彻底退役”的执行阶段。
- 当前用户视角完成度仍按 `0%` 计。
- 当前治理推进度约 `65%`：
  - 事故事实已经厘清
  - 合法 branch carrier 已基本找齐
  - 第一轮 branch-only 回包已收齐
  - `spring-day1` 二轮裁定已收齐并已实际执行
  - 第一批纯历史 worktree 已真实删除
  - shared root 初版归属图与第二批核验表已落盘
  - 但共享根目录还没回 `main`
  - 第二批临时容器还没退役

## 2. 已完成

### 2.1 规则与口径层
- 已明确 `11` 是当前收口主线，`09/10` 只保留前置治理成果，不再继续堆剩余任务。
- 已明确红线：
  - `worktree` 只允许作为事故隔离例外
  - 最终必须回到 `共享根目录 + 分支`

### 2.2 Git / worktree 事实层
- 已确认共享根目录当前仍是事故现场：
  - `D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-correct001 @ 11e0b7b4`
- 已完成 worktree 全景盘点。
- 已锁定当前三条主线的分支 carrier 候选：
  - `farm -> codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
  - `NPC -> codex/npc-roam-phase2-002 @ 6e2af71b`
  - `spring-day1 -> codex/spring-day1-story-progression-001 @ a9c952b7`

### 2.3 三大线程事实层
- `farm`
  - cleanroom 已成功，可编译，可作为 continuation branch carrier。
  - 已确认后续唯一 branch-only 入口应为：
    - `D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
- `NPC`
  - continuation branch 已 clean，但仍临时挂在 worktree 容器上。
  - 已确认后续唯一 branch-only 入口应为：
    - `D:\Unity\Unity_learning\Sunset @ codex/npc-roam-phase2-002 @ 6e2af71b`
- `spring-day1`
  - clean checkpoint 已明确，branch-only 入口已明确为：
    - `D:\Unity\Unity_learning\Sunset @ codex/spring-day1-story-progression-001 @ a9c952b7`
  - `Primary.unity / DialogueUI.cs` 已明确不再属于未固化尾巴。
  - 二轮裁定已落地，shared root 与 rescue 的字体 dirty 已完成证据导出并清空。

### 2.4 文档与治理层
- 已重写 `11` 阶段分析、任务、执行方案与 memory。
- 已新增本文件和 branch-only 分发 prompt 文件。
- 已新增：
  - `所有线程回归誓言.md`（线程回包索引）
  - `所有线程回归誓言\*.md`（线程原始回包）
  - `共享根目录dirty归属初版_2026-03-17.md`
  - `第二批worktree核验表_2026-03-17.md`
- 已把 `11` 纳入现行状态说明和现行入口总索引。

### 2.5 真实现场推进
- 已真实退役第一批纯历史 worktree：
  - `main-reflow-carrier`
  - `NPC`
  - `farm-10.2.2-patch002`
- 当前 `git worktree list` 已缩减为：
  - 共享根目录
  - `farm-1.0.2-cleanroom001`
  - `NPC_roam_phase2_continue`
  - `NPC_roam_phase2_rescue`
  - `spring-day1-story-progression-001`
- 当前第二批里，`NPC_roam_phase2_rescue` 也已经恢复为 `CLEAN`。

## 3. 未完成

### 3.1 当前最核心的未完成
- `spring-day1` 的二轮回复已经执行到现场，但 shared root 还没完成治理尾巴与杂项 `untracked` 的最终裁定。
- 共享根目录还没有回到 `main`。
- 第二批临时容器还在。
- 用户如果此刻直接验收 `NPC` 最新 continuation，仍需要打开另一个 Unity 工程。

### 3.2 按优先级排序的剩余任务
1. 清掉 shared root 里阻止 `checkout main` 的剩余治理尾巴。
2. 裁定并处理 `Assets/Screenshots*`、`npc_restore.zip`。
3. 恢复共享根目录到 `main` 中性现场。
4. 用 shared root 对 `spring-day1 / NPC / farm` 做一次真正的 branch-only 检出验证。
5. 退役第二批临时 worktree。
6. 生成“worktree 已退役、branch-only 已回归”的最终快照。

## 4. 当前 prompt 结论
- 当前只剩 2 个杂项归属确认 prompt 值得发：
- `农田交互修复V2`
  - 确认 `Assets/Screenshots*` 是否只是 farm checkpoint 证据，是否可删除/归档而不影响 carrier
- `NPC`
  - 确认 `npc_restore.zip` 是否只是恢复期本地备份，是否可删除/归档而不影响 carrier
- shared root 回 `main` 之后，才需要再发预备 prompt 给：
- `NPC`
  - 做根目录 branch-only 检出验证
- `farm`
  - 做根目录 branch-only 检出验证
- 当前不需要立即再发 prompt 的线程：
- `导航检查`
  - 当前不是 worktree 问题核心。
  - 若后续进入整改，默认走共享根目录 + 分支。
- `遮挡检查`
  - 当前不是 worktree 问题核心。
  - 若后续进入整改，默认走共享根目录 + 分支。
- `项目文档总览`
  - 当前更偏历史总览和归档，不需要独立 worktree。

## 5. 当前一句话判断
- 现在已经不是“业务线程尾巴”阶段，而是“shared root 自身治理收口 + 两个杂项证据物裁定 + root 回 `main`”的执行阶段。
