# 阶段任务：历史编码巡检与治理边界收紧

## 目标
- 全面区分 Sunset 当前文档体系中的“终端假乱码”与“真实编码损坏”。
- 对仍在使用的活文档、skills 元数据、规则入口进行安全修复。
- 对历史归档中的错码对象建立明确的降级与忽略边界，不再继续浪费精力。
- 复核并收紧当前治理执行边界，尤其是现行文档口径与 `git-safe-sync.ps1` 的 `governance` 白名单边界。

## 待办
- [x] 建立本阶段目录并固定任务范围。
- [x] 扫描活文档、历史归档、skills、AGENTS 中的可疑编码对象。
- [ ] 对可安全修复的小文件直接修复为 UTF-8 健康版。
- [x] 对长文档判断是重建、保留历史、还是标记 `historical-ignore`。
- [x] 复核 `git-safe-sync.ps1` 当前 `governance` / `task` 边界是否仍有误伤或过宽。
- [x] 将本轮巡检结论同步到现行规则与记忆链。

## 完成标准
- 当前仍会被直接使用的规则 / skill / 路由文件，不再保留真实编码损坏。
- 历史错码对象有明确分类：修、重建、降级忽略。
- `governance` 执行边界至少形成明确结论：保留现状、文档纠偏、或脚本收紧。
