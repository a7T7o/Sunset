# 阶段任务：线程解冻与 WIP 收口

## 目标
- 把冻结期遗留的线程状态，转成有序复工而不是重新混乱。
- 优先处理实体资产与高风险 WIP。

## 待办
- [ ] 唤醒 `NPC` 线程，先固化动画 / Prefab / Sprite meta 资产 WIP。
- [ ] 唤醒 `spring-day1`，明确下一项真实剧情 / 对话新增需求。
- [ ] 唤醒 `农田交互修复V2`，决定本轮结案还是继续新需求。
- [ ] 唤醒 `项目文档总览`，先收口它当前的文档改动。
- [ ] 唤醒 `导航检查`，先固化当前审计文档状态。
- [ ] 唤醒 `遮挡检查`，先固化当前审计文档状态。
- [x] `Skills和MCP` 已按用户最新口径直接归档，不再进入唤醒队列。
- [ ] 建立一份“线程首轮复工完成表”，确认谁已完成遗留状态收口。

## 完成标准
- 所有冻结线程至少完成一轮“先收遗留，再报下一步”的新格式复工。
- `NPC` 资产 WIP 不再处于高丢失风险状态。
