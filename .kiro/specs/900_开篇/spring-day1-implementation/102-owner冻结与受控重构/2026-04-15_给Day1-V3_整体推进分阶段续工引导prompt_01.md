# 2026-04-15｜给 Day1-V3｜整体推进分阶段续工引导 prompt

请先完整读取下面 5 份文件，不要再从长聊天里自己拼上下文：

1. [Day1 现存问题总览与整体施工总表](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/102-owner冻结与受控重构/2026-04-15_Day1-V3_Day1现存问题总览与整体施工总表.md)
2. [施工方向与分阶段清单](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/102-owner冻结与受控重构/2026-04-15_Day1-V3_施工方向与分阶段清单.md)
3. [owner 冻结总表](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/102-owner冻结与受控重构/2026-04-15_Day1-V3_owner冻结总表.md)
4. [给导航的语义同步与执行边界 contract](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/102-owner冻结与受控重构/2026-04-15_Day1-V3_给导航_语义同步与执行边界contract.md)
5. [给 NPC 的协作边界与 facade 落地 prompt](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/102-owner冻结与受控重构/2026-04-15_Day1-V3_给NPC_协作边界与facade落地prompt.md)

当前你自己的唯一任务不是重新解释方向，而是按阶段真正推进 `Day1` 与用户语义重新同步。

## 这条线的当前唯一主线

`把剧情后 resident 生命周期从 Day1 手里剥掉，让 Day1 回到剧情语义层。`

## 你自己的推进顺序

只能按下面顺序走，不要跳：

1. `Package A` 已完成
   现在不要重写它，只把它当 authoritative 基线。

2. `Package B｜Day1 自己退权`
   这是你下一刀的唯一主刀。
   只收：
   - `SpringDay1NpcCrowdDirector` 不再持续持有剧情后 resident lifecycle
   - `SpringDay1Director` 不再保留 `003` opening 之后的单独 runtime shim

3. `Package C / D`
   不是你自己吞掉做完。
   你要按文档边界，把 `NPC facade` 和 `导航执行合同` 交回各自 owner。

4. `Package E / F`
   等 `NPC / 导航` 语义同步后，再做 call-site 迁移、测试重写和 live 终验。

## 这轮你允许做什么

1. 继续收 `CrowdDirector` 的剧情后 resident owner
2. 收 `Director` 对 `003` 的 opening 后单独 handoff shim
3. 为后续 `NPC facade / 导航 contract` 留清晰 call-site 接口位

## 这轮你不允许做什么

1. 不扩 `dinner`
2. 不扩 `UI / Prompt / Primary`
3. 不自己吞掉 `NPC facade`
4. 不自己替 `导航` 设计执行细节
5. 不再把结构进展说成体验过线

## 你要持续记住的语义

1. `opening / dinner` staged contract 仍归 Day1
2. `001/002` 只在剧情段保留特殊性
3. `003` opening 之后最终应并回普通 resident release contract
4. `Day1` 只决定：
   - 谁要动
   - 去哪
   - 什么时候放手
   - 什么时候 snap
5. resident 一旦 release：
   - NPC 决定进哪个 state
   - 导航负责 movement execution

## 你的完成定义

你这一阶段不是“把所有代码做完”，而是先做到：

1. `CrowdDirector` 开着时，不再成为普通 resident 剧情后 lifecycle owner
2. `Director` 不再为 `003` 留 opening 后的单独 runtime shim
3. 你的 call-site 已经明确能接 NPC facade 和导航 contract

## 你的回执要求

继续按：

1. `A1 保底六点卡`
2. `A2 用户补充层`
3. `B 技术审计层`

并且必须明确区分：

1. 结构线做成了什么
2. 体验线还没证明什么

不要再把“结构对了”说成“体感过线”。
