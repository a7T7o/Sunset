# 2026-04-14 spring-day1 opening 问题修改表

| 序号 | 问题现象 | 当时判断 | 实际改动 | 当前结果 | 结论 / 教训 |
| --- | --- | --- | --- | --- | --- |
| 1 | `101~203` 在 `opening` 后被继续扣在 `EnterVillage_PostEntry`，只剩 `001~003` 在动 | `CrowdDirector` 没有在合适的 release 边界放人 | 在 [SpringDay1Director.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs) 增加 `ShouldLatchEnterVillageCrowdRelease()`，在 [SpringDay1NpcCrowdDirector.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs) 增加 `_enterVillageCrowdReleaseLatched` | 只解决了“release 边界显式化”，没解决错误时机放人 | 不能只补 latch；必须同时校正“何时允许 release” |
| 2 | `opening` 后居民会先走两步，然后又卡住/抖动 | 以为是 `return-home` 仍在 `scripted control active / scripted move inactive` 半态 | 把 resident 的 return 从 `CrowdDirector scripted move` 改成 `NPCAutoRoamController.DebugMoveTo()` 的 autonomous return | 证明可以脱离 director owner，但仍没收住完整时机链 | 放权和完成交接是两件事；只放权不等于真正恢复正常 NPC |
| 3 | `VillageGate` 还没正式对白，就提前放人；对白后又可能被下一次 sync 抓回 | 发现 `ShouldReleaseEnterVillageCrowd()` 吃了 `_villageGateSequencePlayed`，把“已排队”误当“已完成” | 把 `ShouldReleaseEnterVillageCrowd()` 改成只认 `HasVillageGateCompleted()` / `HouseArrival` / `TownLead` runtime；并把 `VillageGateSequenceId` 完成事件里的 `ForceImmediateSync()` 挪到 `TryBeginTownHouseLead()` 之后 | live 已证实：居民不再停在 `CrowdDirector` owner，能进入 `return-home` | 以后所有 `localPlayedFlag` 都不能直接拿来当“体验已完成”的 release 条件 |
| 4 | 散场后居民能回到 anchor，但到 anchor 就停住，不进入 roam；有时 crowd runtime 还会掉成 `spawned=0` | 现在已经确认：主问题从“没放人”变成“放人后没有稳定交接回正常 roam” | 在 [SpringDay1NpcCrowdDirector.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs) 增加 `RecoverReleasedTownResidentsWithoutSpawnStates(...)`，并把 `spawned=0` 的 Town 恢复逻辑改成无条件重建尝试 | 正在施工，本轮目标就是把“到 anchor 后 resume roam”彻底收平 | 以后 `spawned=0 / missing=101~203` 这种状态必须当 P0 事故；不能再假设 runtime 自己会恢复 |
| 5 | 就算没掉表，resident 到 anchor 后也会先站桩，看起来像“回去了但没活” | 发现 `FinishResidentReturnHome()` 和补绑恢复都还在用 `StartRoam()`，会先进短停态 | 把 `FinishResidentReturnHome()`、`CancelResidentReturnHome()`、`RecoverReleasedTownResidentsWithoutSpawnStates()` 统一改成 `RefreshRoamCenterFromCurrentContext() + RestartRoamFromCurrentContext(tryImmediateMove: true)` | 代码已落，等待下一次 live 验证 | 这条线以后不能再只看“有没有 StartRoam”；还要看玩家体感是不是“立刻接回正常 roam” |
| 6 | resident 已经回到 anchor，但 controller 可能还停在旧 `ShortPause/假 roam` 半态，表面上 `IsRoaming=true`，实际仍像木桩 | 之前把 `IsRoaming` 当成“已经恢复 roam”的充分证据，这是不够的 | 在 [SpringDay1NpcCrowdDirector.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs) 新增 `ForceRestartResidentRoam(...)`，让 `FinishResidentReturnHome()`、`CancelResidentReturnHome()`、`TryReleaseResidentToDaytimeBaseline()`、`RecoverReleasedTownResidentsWithoutSpawnStates()` 都改成先清旧半态，再强制重启 roam；并补 [SpringDay1DirectorStagingTests.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Tests/Editor/SpringDay1DirectorStagingTests.cs) 守“controller 看起来还在 roam 时也必须真正重启” | 代码已落，live 待验证 | 以后不能再拿 `IsRoaming` 一个布尔值冒充体验已恢复；必须额外防“假 roam / 长短停残留态” |
| 7 | `opening` 后 `101~203` 走两步后高频小幅抽搐，`003` 继续罚站 | 重新核下来不是同一个毛病：`101~203` 是 `CrowdDirector` 还在手搓 `return-home` fallback；`003` 是 `spring-day1-director` 的 opening owner 没在交棒边界释放 | 在 [SpringDay1NpcCrowdDirector.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1NpcCrowdDirector.cs) 去掉 daytime release/return-home 里的 `DebugMoveTo / StopRoam / RestartRoam / transform step fallback` 组合，改成只保留 `DriveResidentScriptedMoveTo(...) -> 到 anchor 后 release -> 空闲时 StartRoam()`；同时在 [SpringDay1Director.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs) 增加 `ReleaseOpeningThirdResidentControlIfNeeded(...)`，让 `VillageGate -> house lead` 交棒时把 `003` 放回 resident contract，并在 [SpringDay1DirectorStagingTests.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Tests/Editor/SpringDay1DirectorStagingTests.cs) 补 `003` release 守护与“禁止手搓 step fallback”守护 | 代码层 clean，live 待验证 | release contract 的红线应该是：Day1 只下“回 anchor / 放人”语义，不再自己补 locomotion fallback；另外 `003` 只要还残留在 director owner 里，就不可能和 `101~203` 真正同合同 |

## 当前明确还没收完的点

1. `return-home -> 到 anchor -> resume roam` 这条现在代码口已经改成“只发导航请求、不再手搓 fallback”，但 live 还没过线，不能冒充体验闭环。
2. `003` 已补 opening 交棒 release 口，但还没拿到 fresh live 票，不能冒充“彻底与 `101~203` 同合同”已经被体验证明。
3. `return-home` 沿途避让体感仍然不够，当前是 Day1 own 先把“交回导航自由态”收平，路径细节再看是否还需要导航 own 跟进。
4. 只要再出现一次 `spawned=0 / missing=101~203`，就必须继续按这张表追加，而不是重新走一遍旧弯路。
