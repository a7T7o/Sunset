# 26.03.26-Day1V2共享根大扫除与白名单收口-11

## 当前已接受的基线

- 这轮不是继续 `Day1` 业务续工，也不是继续 formal-face / prefab 审面。
- 当前 live 现场固定为：
  - `D:\Unity\Unity_learning\Sunset @ main @ 519d51bd20d98e662eafb94cea0c5bbbeb314cec`
- `Primary.unity` 仍是 mixed dirty；`shared-root-branch-occupancy = neutral-main-ready` 只说明 Git 入口中性，不等于 hot scene 可写。
- 你这条线当前最像 own 的尾账，至少包括：
  1. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese BitmapSong SDF.asset`
  2. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
  3. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SDF.asset`
  4. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SoftPixel SDF.asset`
  5. `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
  6. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
  7. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`
  8. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\memory.md`
  9. `spring-day1 / spring-day1V2 / 003-进一步搭建\` 下这几天留下的一串 `V2交接文档`、`首轮/第二轮委托`、Day1 UI prompt 产物，以及 `V1.0_UI样式快照_2026-03-25\`

## 这轮唯一主刀

> 只做 spring-day1 这条线 own dirty / untracked 的认领、清扫和白名单收口；不恢复 Day1 主线施工。

## 这轮允许做什么

1. 核对上面这些 assets / memory / untracked docs 哪些确实属于你。
2. 对属于你且当前该保留的文件，做最小必要补记、整理、提交。
3. 对属于你但不该继续留在 shared root 的旧 prompt / 交接尾巴，按最小范围清理或归位。
4. 如果你判断其中有项已经不是你 own，必须报实原因，但不要硬吞。

## 明确禁止

- 不准继续 `Day1` 新功能、UI 新改动或玩法扩题
- 不准碰 `Primary.unity`
- 不准碰导航 / 农田 / NPC dirty
- 不准碰 `Assets/Editor/StaticObjectOrderAutoCalibrator.cs`
- 不准把 `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs` 当成你这条线的顺手收口
- 不准因为 shared root 里还有 foreign dirty，就不收自己的白名单

## 完成定义

只有同时满足下面这些，才算这轮完成：

1. 你已经把 own dirty / untracked 与 foreign dirty 清楚分开；
2. 你 own 的可收部分已经按白名单 sync 到 `main`，或者你已经把不能 sync 的真实 blocker 说清楚；
3. 你没有顺手恢复 `Day1` 主线施工；
4. 你明确说明了当前这条线对自己是否已经 clean。

## 固定回执格式

- 当前在扫什么：
- 你确认属于 spring-day1 这条线的 dirty / untracked：
- 你确认不属于 spring-day1、因此没有去碰的 dirty：
- changed_paths：
- 是否触碰高危目标：
- code_self_check：
- pre_sync_validation：
- 当前是否可直接 whitelist sync 到 main：
- 提交 SHA：
- 当前 git status 是否 clean：
- blocker_or_checkpoint：
- 一句话摘要：

## 最后一句

这轮你不是来继续做 Day1 的，是来把自己留在 shared root 里的尾账先收干净。
