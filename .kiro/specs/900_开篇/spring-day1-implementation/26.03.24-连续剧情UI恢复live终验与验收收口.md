# 26.03.24 连续剧情 UI 恢复 live 终验与验收收口

你这轮我先给真实裁定：

- `代码侧根因定位，接受`
- `DialogueUI` 连续剧情时序补丁，接受
- `静态断言补齐，接受`

但这条线现在还不能算做完。

原因很简单：

- 你当前停点还是“请用户复测这 2 件事”

我不接受把这一步留给用户兜底。  
这条线程下一轮必须自己把 live 结果拿出来。

## 一、当前真实状态

你现在的真实状态不是：

- Day1 结尾 UI 恢复问题已经通过验收

你现在的真实状态是：

- 代码侧已经把 bug 从“用户可能测太快”收敛为 `DialogueUI` 连续剧情时序 bug
- 代码补丁已经落地
- 但 live 终验还没拿到

所以这条线下一轮主线非常明确：

- `single-instance + 8888 MCP live 终验`

## 二、本轮不再允许停在“让用户自己试”

下一轮明确禁止：

- 继续只做本地代码闸门
- 继续让用户自己跑完整剧情帮你验
- 继续补新的 UI 猜想修复

你下一轮只有两种合法结果：

1. 你自己跑出 live 通过结果
2. 你自己锁到“是哪一个 UI 根物体没恢复”的首个真实失败点

## 三、这轮固定 live 验证顺序

按这个顺序来，不要自由发挥：

1. 确认主工程单实例
2. 确认 `Edit Mode`
3. 清 Console
4. 进入 Play
5. 连续把 Day1 主线完整跑一遍
6. 在剧情完全结束后只验下面 2 条
7. 如果失败，直接锁首个没恢复的 UI 根物体 / 恢复链路
8. 修完后只重跑对应条目
9. 结束后退回 `Edit Mode`

## 四、必须验证的 2 条 live 结果

### 1. 连续剧情完整跑通

目标：

- 从当前 Day1 可进入点开始
- 连续把剧情完整跑完
- 中途不允许因为旧 `DialogueEndEvent` 误收尾导致 UI 状态漂移

你必须明确回答：

- 是否完整跑通
- 如果没跑通，卡在哪一段

### 2. 剧情结束后的 UI 恢复

目标：

- 剧情完全结束后：
  - `Toolbar` 恢复
  - `Tab / 背包` 能正常打开

你必须明确回答：

- 是否通过
- 如果失败：
  - 是 `Toolbar` 没恢复
  - 还是 `Tab / 背包` 输入没恢复
  - 还是某个 UI 根物体保持隐藏

## 五、失败时的唯一追法

如果这轮还有残留：

- 不准继续散着修
- 直接收第一责任点：
  - 是哪一个 UI 根物体没恢复
  - 是 `DialogueUI` 快照链
  - 还是别的 UI 恢复链

也就是说：

- 这轮可以不一次修通
- 但不能再停在“我怀疑可能是哪个 UI”

## 六、通过后的收口要求

如果这 2 条都通过：

- 不要继续扩写 spring-day1 新功能
- 直接新增一份 focused 验收记录：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\26.03.24-Day1连续剧情UI恢复验收报告.md`

这份报告至少写：

1. 本轮修复目标
2. 根因
3. 改动文件
4. 静态验证证据
5. live 验证证据
6. 用户还应重点验什么
7. 当前是否可接受为“这个 UI 恢复问题已完成”

## 七、本轮回执格式

下一次回执，只按这个格式回复：

- 当前在改什么：
- 是否已完成 live 终验：
- Day1 连续剧情是否完整跑通：
- 剧情结束后 `Toolbar / Tab / 背包` 是否恢复：
- 是否已锁定首个未恢复 UI 根物体：
- 是否已生成 focused 验收报告：
- changed_paths：
- 新增或修改了哪些测试 / live 证据：
- 当前剩余 blocker：
- blocker_or_checkpoint：
- 一句话摘要：

## 八、最后再钉一次

这轮你不是没做事。  
相反，你已经把 bug 从模糊用户体感收敛成了清晰时序根因。

但现在还差最后一步：

- 把它从“代码修了”变成“live 真的恢复了”

你自己去把这个结果拿回来，不要停在让用户代你验。
