问题一：对话系统和事件触发毫无代码，现在没有多个场景只有一个场景，只有UI系统有初步设计了，你可以查看.kiro\specs\UI系统的内容，有几个子工作区，还有主memory，但是不一定主memory就是权威的，可能有疏忽，你可以大致看看
问题二：首先我认为我只是一个审核方案和测试实际功能是否符合我预期的一个项目经理？游戏成什么样都靠我来调控，但是其实你才是实际实施者，代码的把控设计与执行，方案的落地都是靠你，unity里面的配置我都可以做到，但是你有MCP，你能直接操作Unity，所以所有你可以进行的操作请你先记录在文档然后告知我，并直接执行创建，当然你的权限只有创建没有修改的权利！如果需要修改请务必告知我，也就是要遵守：“
## 🎮 第九章：MCP 自动化沙盒与 Unity 权限控制协议
作为具备 MCP (Model Context Protocol) 能力的执行引擎，你可以直接对 Unity 环境进行自动化干预。但为了保证工程资产的绝对安全，你的操作必须被限制在严格的权限沙盒内：
1. **绝对权限红线：【允许创建 (Create-Only)】/【严禁修改 (No-Modify)】**。
2. **“创建类”任务 SOP（自动化放行）**：
   - **步骤 A（文档化）**：在调用任何 MCP 操作前，必须先在当前工作区的文档（如 `tasks.md`）中记录你需要创建的资产清单。
   - **步骤 B（终端宣告）**：向用户输出冷峻的通知（例：“检测到需要创建新资产，已记录文档。即将通过 MCP 自动创建...”）。
   - **步骤 C（直接执行）**：宣告后立刻调用工具进行创建，无需等待用户确认。
3. **“修改类”任务 SOP（物理拦截）**：
   - 任何涉及**修改已存在资源**的操作（如：调整已有 Prefab 的参数、修改场景层级、替换已有材质、覆盖 ScriptableObject 数据），**立刻阻断你的 MCP 调用权限**。
   - 你必须停止一切动作，将所需的修改操作整理成一份极其精确的《手动配置清单》，并输出：“越权警告：该任务涉及修改现有 Unity 配置。请用户手动执行以下操作：...”。等待用户完成后再继续推进代码。”
问题三：对话框UI我可以找，但我大概率是找网上的png然后转成tile资源，我有tile转sprite的工具，这个也是你写的，你可以在editor里面找到，然后NPC头像我也可以做，CG还没做，气泡对话我也可以做………也就是现在啥都没有我都得自己做，我觉得可以，你先替代吧，如果可以用黄色的谢谢
问题四：我认为还可以，但是你可以多布置一些，不按照第几天内做出，而是第一阶段第二阶段这样，我可以努力一天多做几个阶段，你只需要把控就好
我希望报告肯定有详细的方向和清单，然后也要有部分实现思路，伪代码你觉得必要也可以写，代码实现这个就不是这个需求拆分需要有的内容了，拆分报告的职责就是彻底厘清所有内容！然后也要厘清各个内容