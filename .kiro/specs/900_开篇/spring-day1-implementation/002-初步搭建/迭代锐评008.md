**【首席架构师与项目经理联合终极执行令：全域防爆破、数据完整性缝合与全线绿灯】**

Kiro，我们已经对你提交的全套核心代码、底层依赖以及所有的性能核查报告进行了最终维度的“显微镜级”会审。

在 UI 性能（TMP 脏标记短路机制）的底层判断上，你的源码级探查完全正确，我已彻底撤销此前要求你增加 `_lastUpdatedText` 的过度优化指令。保持你的极简刷新逻辑。

但是！在追求极简与防守性能红线的过程中，你的代码在“需求覆盖率”与“底层数据结构支撑”上，出现了两处足以导致核心业务逻辑（春1日语言不通剧情）彻底瘫痪的严重缺失。结合影子审核员的极限排雷，我们在授予你最终 MCP 写盘权限前，必须在你的“虚拟缓存”中强制打入以下 4 个补丁。

请启动你的最高级别上下文注意力机制，精准吸收以下溯源依据与修正代码，随后直接触发自动化写入。

---

### 🚨 致命数据坍塌：彻底遗漏“乱码文本”底层支撑（强制修复）

* **溯源定位**：查阅你提交的 `DialogueNode.cs` 源码，序列化字段仅包含 `speakerName`, `text`, `speakerPortrait`, `isInnerMonologue`, `isBubble`, `typingSpeedOverride`。
* **依据与打假**：查阅《需求文档 (requirements.md)》“需求2：语言乱码转换”的强制验收标准：“通过对话数据中的标记字段区分乱码文本和正常文本”。春1日开篇，主角遇到村长时由于语言不通，必须输出乱码。如果没有底层数据支撑，UI 表现层将成为无米之炊，整个乱码翻译的核心剧情将直接流产。
* **强制修正令**：
在你即将生成的 `DialogueNode.cs` 中，必须且绝对要在 `[Header("内容")]` 或相关区域，补回这两个支撑核心玩法的序列化字段：
```csharp
[Tooltip("是否为无法听懂的乱码语言")]
public bool isGarbled;

[TextArea(3, 10)]
[Tooltip("乱码状态下显示的文本内容")]
public string garbledText;

```



### 🚨 视觉表现缺失：内心独白状态机残缺与头像遗漏（强制修复）

* **溯源定位**：在 `DialogueUI.cs` 的 `OnDialogueNodeChanged` 回调中，你目前仅处理了 `speakerNameText.text = e.Node.speakerName;`。
* **依据与打假**：《需求文档》明确验收标准：“内心独白需使用区别于普通对话的视觉样式（斜体/不同颜色）”。《设计文档》进一步将其量化为极其严苛的视觉规范：“文本颜色：浅灰色，字体样式：斜体，无头像显示，**背景更透明 (Alpha=0.5)**”。
* **架构剖析**：你不仅忘记了绑定 `speakerPortrait` 头像组件，甚至在处理内心独白时，完全忽略了对字体、颜色以及最关键的“背景透明度”的状态切换。如果不做处理，主角的心声和外部的咆哮将毫无视觉差异。
* **强制修正令**：
1. 在 `DialogueUI.cs` 的序列化区域补充：
```csharp
[SerializeField] private Image portraitImage;
[SerializeField] private Image backgroundImage; // 用于控制对话框底板透明度

```


2. 在 `OnDialogueNodeChanged` 中，彻底重构为以下具备完全需求覆盖率的严格状态机：
```csharp
private void OnDialogueNodeChanged(DialogueNodeChangedEvent e)
{
    if (e.Node == null) return;

    if (e.Node.isInnerMonologue)
    {
        // 内心独白模式：斜体、浅灰色、隐藏名字与头像、背景半透明
        if (dialogueText != null)
        {
            dialogueText.fontStyle = FontStyles.Italic;
            dialogueText.color = new Color(0.8f, 0.8f, 0.8f, 1f);
        }
        if (backgroundImage != null)
        {
            Color bgColor = backgroundImage.color;
            bgColor.a = 0.5f;
            backgroundImage.color = bgColor;
        }
        if (speakerNameText != null) speakerNameText.gameObject.SetActive(false);
        if (portraitImage != null) portraitImage.gameObject.SetActive(false);
    }
    else
    {
        // 普通对话模式：正常字体、白色、显示名字与头像、背景恢复不透明
        if (dialogueText != null)
        {
            dialogueText.fontStyle = FontStyles.Normal;
            dialogueText.color = Color.white;
        }
        if (backgroundImage != null)
        {
            Color bgColor = backgroundImage.color;
            bgColor.a = 1.0f; // 假设默认不透明为1.0f，视实际预制体而定
            backgroundImage.color = bgColor;
        }
        if (speakerNameText != null)
        {
            speakerNameText.gameObject.SetActive(true);
            speakerNameText.text = e.Node.speakerName;
        }
        if (portraitImage != null)
        {
            portraitImage.gameObject.SetActive(true);
            portraitImage.sprite = e.Node.speakerPortrait;
            portraitImage.gameObject.SetActive(portraitImage.sprite != null);
        }
    }
}

```





### 🛡️ 架构安全性与代码规范闭环（约束提醒）

1. **生命周期的“自毁”风险隔离**：
针对 `root.SetActive(false)` 可能导致 `DialogueUI` 连带禁用并触发 `OnDisable` 永久退订事件的风险。**强制要求：** 在 `DialogueUI.cs` 的类声明顶部，必须加上 XML 注释：`/// <summary>警告：此脚本必须挂载在常驻活跃的 Canvas 根节点上，严禁挂载于可能被 SetActive(false) 的 UI 面板自身，以防止事件总线永久退订。</summary>`。
2. **清理冗余引用**：
生成 `StoryEvents.cs` 时，绝对剔除顶部的 `using Sunset.Events;` 声明（同命名空间下禁止无效引用）。

---

### 🟢 终极放行令：MCP 自动化最高权限已解锁 (Final Write Execution)

Kiro，事实已经 100% 澄清，需求边界与数据模型已经完成了最后的铜墙铁壁级缝合。

**现在，不需要再向我进行任何文本回复，不需要输出任何 Diff 确认！**

请立刻、直接开启你的 MCP 文件操作工具，将上述 4 个补丁融入你的代码体系，全线写入 Unity 真实的工程目录中！你的执行清单如下：

1. **创建基础数据骨架**：
* `DialogueNode.cs`（**必须包含 `isGarbled` 和 `garbledText**`）
* `DialogueSequenceSO.cs`
* `StoryEvents.cs`（必须剔除冗余 using，且包含带参数的 `DialogueNodeChangedEvent` 及空状态事件）


2. **创建核心中枢**：
* `DialogueManager.cs`（保留你优秀的 StringBuilder 协程与零 GC 设计）


3. **修改输入拦截**：
* `GameInputManager.cs`（确保使用 `OnEnable/OnDisable` 管理生命周期，并使用 `owner: this` 和 `UnsubscribeAll`）


4. **创建 UI 表现层**：
* `DialogueUI.cs`（保留 `Update` 中的直接赋值，**必须缝合包含 `backgroundImage` 的完整内心独白状态机与头像绑定逻辑**，加入防自毁注释）



展现你完美的工程执行力，去创造这套核心骨架吧！所有文件写入完毕后，直接在终端向我进行完工报告！