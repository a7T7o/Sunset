**【首席架构师终极补充排雷报告（带刺的绿灯）】**

Kiro，你的 `DialogueManager.cs` 和 `GameInputManager.cs` 核心逻辑非常出色！你在打字机协程中完美实现了零 GC（缓存 WaitForSeconds），并且精准地在 `GameInputManager` 中开辟了 `SetInputEnabled` 通用接口。这种状态解耦和防御性编程证明了你极高的专业水准！

但是！在正式授予你 MCP 自动化执行权限之前，我在对你的代码进行最后一次“像素级”安全扫描时，抓到了三个如果不立刻纠正，**将绝对导致 Unity 当场编译报错或引发恶性 Bug 的 API 幻觉与生命周期隐患**。

请立即吸收以下 3 点强制修正要求，在脑海中完成代码替换后，再开始最终的自动化生成：

### 🚨 强制修正一：剔除凭空捏造的 EventBus API 与事件参数（编译炸弹）

* 
**你的代码缺陷**：你在 `GameInputManager` 中使用了 `EventBus.Subscribe<DialogueStartEvent>(OnDialogueStart, owner: this);` 和 `EventBus.UnsubscribeAll(this);`。同时，在 `DialogueManager` 中你使用了 `new DialogueStartEvent { SequenceId = sequence.sequenceId }` 。


* **依据与打假**：《设计文档 (design.md)》的“StoryEvents”章节清晰定义了 `DialogueStartEvent` 是一个没有任何属性的空类（`public class DialogueStartEvent : IGameEvent { }`），**它根本不存在 `SequenceId` 这个属性！** 同时，“事件总线优化”章节规定了标准的 API 用法，**没有 `owner: this` 参数，也没有 `UnsubscribeAll` 方法！**。
* **执行指令**：
1. 立刻移除 `DialogueStartEvent` 和 `DialogueEndEvent` 实例化时的 `{ SequenceId = ... }` 赋值。
2. 严格恢复标准的事件注册/注销语法：使用单纯的 `EventBus.Subscribe<DialogueStartEvent>(OnDialogueStart);` 和 `EventBus.Unsubscribe<DialogueStartEvent>(OnDialogueStart);`。



### 🚨 强制修正二：极其危险的生命周期订阅（“幽灵锁死”Bug）

* 
**你的代码缺陷**：在 `GameInputManager` 中，你将事件订阅塞进了 `Awake()`，将注销塞进了 `OnDestroy()` 。


* **依据与打假**：这是极不规范的 Unity 实践。如果该 GameObject 被 `SetActive(false)`（禁用）但未被销毁，它将在后台继续监听对话事件并疯狂阻断输入，引发无法追踪的“幽灵锁死”。《设计文档》也明文规定了：事件订阅必须在 `OnEnable()` 中，注销必须在 `OnDisable()` 中。
* **执行指令**：将 `GameInputManager` 中的所有订阅逻辑移入 `OnEnable()`，将所有对应的单独注销逻辑移入 `OnDisable()`。废除你脑海中的 `OnDestroy()` 注销方案。

### 🚨 强制修正三：UI 组件类型的最后警告（防止旧病复发）

* **背景与依据**：你在上一轮的微设计中曾错误使用了旧版的 `UnityEngine.UI.Text`。根据《需求拆分报告》修正阶段及《设计文档》的“UI设计规范”，项目中**强制且唯一**支持的文本组件必须是 `TMPro.TextMeshProUGUI`。
* **执行指令**：在你接下来自动化生成 `DialogueUI.cs` 文件时，必须引入 `using TMPro;`，并绝对禁止出现任何 `UnityEngine.UI.Text` 类型的变量声明。

---

🟢 **最终执行指令 (Final Go-Ahead)**

以上 3 点是你最后的红线。只要你在自动生成代码时确保这三处隐患被彻底拔除，**我正式授予你 MCP 自动化执行权限！**

请立即开始调用 MCP 执行《阶段1：最小对话系统》的代码落地：

1. **创建** `StoryEvents.cs`, `DialogueSequenceSO.cs`, `DialogueNode.cs` 等基础数据结构脚本（必须遵循空事件类设计）。
2. **创建** 修正后的 `DialogueManager.cs`。
3. **修改** `GameInputManager.cs`（必须将事件逻辑移入 OnEnable/OnDisable，并剔除不存在的 EventBus API）。
4. **创建** `DialogueUI.cs`（配合 Update 读取 CurrentTypedText，并严格使用 TMPro）。

开始调用你的 MCP 工具进行写入吧！文件全部创建/修改完毕后，向我汇报！