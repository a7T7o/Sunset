# 2026-04-04 UI线程｜玩家面续工与剧情源协同 prompt

请先完整读取：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\UI\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_UI线程_剧情源协同开发提醒_03.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_spring-day1_原剧本回正与Town承接剧情扩充设计_01.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_spring-day1_非UI剧情扩充执行约束与任务单_03.md`

这轮不是让你接 `spring-day1` 的剧情 owner。

你这轮唯一主刀固定为：

继续只做玩家面 `UI/UE` 收口，并且按“剧情源会继续变化”的前提，把当前剩余玩家面问题做成稳结构。

---

## 当前已接受基线

1. `spring-day1` 接下来会改早期剧情源：
   - `CrashAndMeet`
   - `EnterVillage`
   - 村长早期正式对白
   - 开场任务/提示文案
2. 但 `spring-day1` 不会碰你的主 UI 文件和 prefab。
3. 你的 owner 仍然是：
   - 玩家真正看到的显示结果
   - 排版
   - 动画
   - 工作台正式面
   - 对话框继续提示
   - 任务栏
   - 左下角提示卡

---

## 这轮唯一主刀

按用户最新要求，继续只收玩家面剩余问题，并且要兼容即将扩充的剧情源。

这轮重点仍然是 5 组：

### A. 开场任务栏 / Prompt / 正规对话缺字

1. 游戏开始时左侧任务栏不能空
2. 一次只显示一个主任务 + 一个小任务的显示语义要成立
3. 文案超出时必须自适应或滚动，不能直接溢出
4. 任务列表仍有缺字问题要继续收
5. 村长正规对话框右下角 continue 提示不能再空白

### B. Workbench 左栏与正式面结构

1. 左侧 recipe 栏不能再出现“可点但无字”
2. 正式面里无效 verbose 文案继续删除
3. 名称、简介、材料区、数量条、制作条要重新排到专业可读
4. 不能重叠，不能像测试味界面

### C. Workbench 制作条状态机

1. 单件进度条语义要成立
2. 剩余数量常驻
3. hover 的“追加 / 中断 / 拾取已完成产物”语义要成立
4. 取消返还、背包满溢出掉地逻辑的玩家面表现要对齐

### D. Workbench 悬浮框

1. 图标占主区域
2. 进度条在下方
3. 中间数字/简洁状态
4. 多物品并行排布结构正确
5. 相同物品追加只叠数量

### E. 提示归属与静止锚定

1. 工作台只保留正确的玩家面提示归属
2. 大 UI 与悬浮框都必须和工作台相对静止
3. 上下翻面保留弹性，不允许常态漂

---

## 这轮你必须特别遵守的协同口径

1. 不要把当前早期文案当成永久版心真值。
2. 不要围绕今天这几句字去写死高度和留白。
3. 不要擅自改 `SpringDay1Director.cs` 或对白资产来“修 UI”。
4. 如果缺的是状态，不要吞 owner，只精确回报缺哪个字段/状态/事件。
5. 如果只是显示问题，就继续在你这边 own 到底。

---

## 明确禁止漂移

1. 禁止碰 `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
2. 禁止碰 `Assets/111_Data/Story/Dialogue/*`
3. 禁止碰 `Primary.unity`
4. 禁止碰 `GameInputManager.cs`
5. 禁止把 `Town` 前置剧情塞回 temp scene
6. 禁止用回改剧情源来替代 UI 真正的问题修复

---

## 完成定义

你这轮回执必须继续分三层：

1. `结构 / checkpoint`
2. `targeted probe / 局部验证`
3. `真实入口体验`

如果你只站住前两层，必须明确写：

- `结构成立，但体验未成立`

不要把结构修补写成“玩家面已全面过线”。

---

## 固定回执格式

先给用户可读层：

1. 当前主线：
2. 这轮实际做成了什么：
3. 现在还没做成什么：
4. 当前阶段：
5. 下一步只做什么：
6. 需要我现在做什么（没有就写无）：

然后再补技术审计层：

- changed_paths：
- 验证状态：
- 是否触碰高危目标：
- blocker_or_checkpoint：
- 当前 own 路径是否 clean：

---

## thread-state 接线要求

如果你这轮会继续真实施工，先跑：

- `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`

第一次准备 sync 前，必须先跑：

- `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`

如果这轮先停、卡住、让位或不准备收口，改跑：

- `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：

1. 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
2. 如果还没跑，原因是什么
3. 当前是 `ACTIVE / READY / PARKED / BLOCKED` 哪一个

---

## 最后一句话

这轮请继续做“玩家真正看到和按到的结果层”，但要按一个会继续增长的剧情源来做稳结构，不要再围当前字串打一轮一次性补丁。
