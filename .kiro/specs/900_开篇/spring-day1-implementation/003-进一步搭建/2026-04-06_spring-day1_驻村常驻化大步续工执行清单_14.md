# 2026-04-06｜spring-day1｜驻村常驻化大步续工执行清单

这不是给别的线程的 prompt。

这是我自己下一轮必须硬执行的任务清单。

目的只有一个：

- 不再一刀一刀碎推进；
- 而是在一轮内，把 `spring-day1` 当前最值钱、最能带来大量真实产出的内容狠狠干到一个大 checkpoint。

---

## 一、这轮唯一总目标

把 `day1` 从：

- “导演工具已可用、剧情数据写回了一部分”

推进到：

- “后半段 crowd 的存在方式正式改成驻村常驻化，并继续用导演工具把多个真实段落压成可消费数据和可资产化文本”

这轮不是二选一。

这轮必须并行推进两组内容：

1. `A组`：resident deployment / director runtime 承接
2. `B组`：继续把剧情和导演数据压成真实产物

---

## 二、本轮必须完成的主刀

## A1. 先改 crowd 的存在方式

这一刀必须先做，不允许再往后拖。

目标：

1. 不再让 `101~301` 默认表现成“到 cue 才 instantiate 的临时演员”
2. 改成“已有 resident actor，被导演接管和调度”
3. 在当前边界下，先以 `Primary` 代理 resident 承接站住

这刀的完成定义：

1. `SpringDay1NpcCrowdDirector.cs` 这一层出现明确的 resident 化 contract
2. crowd 的生成/存在/接管逻辑不再默认依赖“阶段到了再生人”
3. 至少第一批关键位要被这套逻辑吃进去：
   - `EnterVillageCrowdRoot`
   - `KidLook_01`
   - `NightWitness_01`
4. 如果还能继续，再吃：
   - `DinnerBackgroundRoot`
   - `DailyStand_01`

## A2. 驻村化后，继续用导演工具补更多真实 cue

不是只证明 resident 化能跑，而是要继续生产。

这轮至少要尽可能继续往下压这些段：

1. `EnterVillage_PostEntry`
2. `DinnerConflict_Table`
3. `ReturnAndReminder_WalkBack`
4. `FreeTime_NightWitness`
5. `DayEnd_Settle`
6. `DailyStand_Preview`

完成定义：

1. 能新写回的 cue 尽量继续写回
2. `DinnerBackgroundRoot` 不再停在薄壳
3. 次日站位不再只停在文档层
4. 所有新增导演数据都要能被现有工具链消费

## A3. 把“已有设计”继续压成正式剧情产物

这轮不允许只做 deployment 和 blocking，不继续压剧情文本。

至少要尽量推进：

1. `CrashAndMeet`
2. `EnterVillage`
3. `HealingAndHP / WorkbenchFlashback / FarmingTutorial`
4. `DinnerConflict / ReturnAndReminder / FreeTime / DayEnd`

完成定义不是“再写思路”，而是：

1. 能落成对白资产的就落对白资产
2. 能落成正式稿的就落正式稿
3. 能挂到事件/导演消费链的就挂进去

---

## 三、本轮推荐执行顺序

顺序固定，不要乱：

1. 先吃 `CrowdDirector` resident deployment 第一刀
2. 每完成一个最小责任簇就立刻做 no-red 自检
3. 再继续用导演工具把 resident 化后的关键段落往下排
4. 再把可直接资产化的剧情文本/对白包继续往下压
5. 最后做最小验证、更新 memory、决定是否可提交

---

## 四、本轮允许做大的地方

这轮允许“大步”，但大在这些地方，不是乱扩：

1. 可以一轮内同时改 `deployment + director data + dialogue assets`
2. 可以一次推进多个 phase
3. 可以为了形成一个完整 checkpoint 而连跑多次工具和验证

---

## 五、本轮绝对禁止事项

1. 不要回 UI
2. 不要碰 `Town.unity`
3. 不要碰 `Primary.unity`
4. 不要碰 `GameInputManager.cs`
5. 不要回吞 NPC own 气泡/会话底座
6. 不要把这一轮又做成“继续讨论方案”
7. 不要只做 deployment 不补剧情产物
8. 不要只做剧情文本不改存在方式
9. 不要爆红；一旦 own red，立即止血

---

## 六、本轮止损线

如果这轮中途遇到真实 blocker，只允许卡在这些地方：

1. `resident deployment` 第一刀遇到当前代码/资产结构真实冲突
2. 导演工具对 resident actor 的消费链出现明确 runtime 断点
3. 某个剧情资产落地被真实引用链卡住

只有命中这种真实 blocker，才允许停。

不允许因为：

1. scope 大
2. 要想一想
3. 可能后面还要调

就提前收手。

---

## 七、本轮对并行线程的使用方式

`Town` 和 `NPC` 这轮不是旁观者，要并行跟上。

### Town 这轮要给的

1. resident root / anchor 语义继续压实
2. scene-side contract 继续压窄
3. 为后续迁回 `Town` 做承接准备

### NPC 这轮要给的

1. crowd 常驻居民语义矩阵
2. 各 phase 的 resident presence 强度
3. 哪些人默认白天常在，哪些夜里更强，哪些只是玩家未先看见

---

## 八、本轮完成定义

只有同时满足下面 6 条，这轮才算真的迈出“大一步”：

1. `CrowdDirector` resident deployment 第一刀已落
2. 至少一批关键位已不再依赖“到点 instantiate”
3. `StageBook / cue` 又新增了一批真实可消费数据
4. 至少一部分剧情正式产物继续落地
5. no-red 仍然成立
6. 给用户的汇报里，能明确说出“这轮新增了哪些真实玩家可感知结果”

---

## 九、本轮收尾必须做

1. 最小 no-red 自检
2. 必要的菜单/测试/console 验证
3. 更新：
   - 子工作区 memory
   - 父层 memory
   - 线程 memory
   - skill-trigger-log
4. 跑 `check-skill-trigger-log-health.ps1`
5. 如果达到可提交状态，提交能提交的 own 内容

---

## 十、一句话提醒

这轮最怕的不是做太多，而是又回到：

- 做一点工具
- 做一点文档
- 做一点判断
- 但真正的 deployment 和剧情落地都没狠狠干下去

这次不允许这样收尾。
