# 2026-04-04 UI线程｜继续施工引导 prompt

请先完整读取：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\UI\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_UI线程_剧情源协同开发提醒_03.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_UI线程_玩家面续工与剧情源协同prompt_03.md`

这轮不是让你停下来单独回“开发提醒已读”。

这轮的正确动作只有一个：

把上面两份 `spring-day1 -> UI` 协同文档吸收成新的约束，然后 **继续你当前正在做的玩家面 UI/UE 施工**。

---

## 你这轮该怎么理解

1. 你当前的 active 主线不变：
   - 继续收玩家真正看到和按到的结果层
2. `spring-day1` 接下来会改早期剧情源；
   - 这会影响你读取到的任务文案、对话长度和前半段节奏
3. 但这不等于你要停下来等剧情线程先做完；
   - 你应当继续把 UI 做成“能承受剧情源增长”的稳结构

也就是说：

1. 继续做
2. 不中断
3. 不回吞剧情 owner
4. 不围当前字串打一轮一次性补丁

---

## 你这轮唯一主刀

继续只做玩家面 `UI/UE` 收口。

重点还是你当前 own 的这些：

1. 开场任务栏 / Prompt / continue 提示缺字
2. Workbench 左栏、正式面、悬浮框、进度条与状态机
3. 左下角提示卡与交互归属
4. 排版、自适应、滚动、静止锚定、弹性与正式感

---

## 这轮新增的协同约束

1. 把 `剧情源协同开发提醒_03` 当成上位协同边界
2. 把 `玩家面续工与剧情源协同prompt_03` 当成你继续施工的正式业务要求
3. 不要为了回复提醒而额外中断自己当前 slice
4. 如果当前已有 active slice，就沿用当前 slice 继续

---

## 只有这类情况才需要你额外回报剧情线程

只有当你真的发现：

1. 缺字段
2. 缺状态
3. 缺事件
4. 缺阶段标签

才需要把问题精确报回。

如果只是：

1. 空字
2. 溢出
3. 左栏空白
4. continue 空白
5. 悬浮框排版不好
6. 交互态显示不顺

那都继续算你这边 own。

---

## 明确禁止

1. 不要碰 `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
2. 不要碰 `Assets/111_Data/Story/Dialogue/*`
3. 不要碰 `Primary.unity`
4. 不要碰 `GameInputManager.cs`
5. 不要停下来只回一条“已读提醒”

---

## 回执要求

继续按你现在的固定回执格式回。

如果这轮不准备收口，就继续施工；不要为了这份引导先中断。

---

## 最后一句话

你这轮不是被叫停来“回应提醒”，而是被要求在吸收新协同边界后，继续把玩家面剩余问题一口气往前推。
