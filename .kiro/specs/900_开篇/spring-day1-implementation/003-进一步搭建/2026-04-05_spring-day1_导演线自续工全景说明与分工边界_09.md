# 2026-04-05｜spring-day1｜导演线自续工全景说明与分工边界

这不是一张“下一刀只改某 1 个方法”的任务单。

这是给我自己看的全景说明，目的是防止我下一轮又被局部问题带偏。

我要记住的不是某个单点 bug，而是：

1. 当前整条线已经推进到哪
2. 哪些工作已经正式分给别人
3. 我自己现在真正该做的是什么
4. 哪些事现在还不能做得太深

---

## 一、当前主线真正是什么

当前 `spring-day1` 的主线已经不是单纯“把 Day1 跑通”。

当前真正主线是：

- 把 Day1 从“骨架能跑”推进到“导演层可分场、可承接村庄层、可继续扩成正式剧本”

这条主线当前有两半：

1. 已经做成的 Day1 own 正式桥接
2. 接下来要做的导演分场与群像承接

---

## 二、已经做成的，不要再忘

### 1. opening 前半段

现在已经不是空白：

1. `CrashAndMeet`
2. `EnterVillage`
3. `FirstDialogue / Followup / VillageGate / HouseArrival`

所以我后续不再回到“开场要不要补”这种空问题。

### 2. 后半段 bridge

当前已经站住：

- `HealingAndHP -> WorkbenchFlashback -> FarmingTutorial -> DinnerConflict -> ReturnAndReminder -> FreeTime -> DayEnd`

并且至少已经拿到一轮结构与 targeted validation：

- `Midday Bridge Tests = 8 PASS / 0 FAIL`

### 3. 尾声矩阵

当前 `ReturnAndReminder / FreeTime / DayEnd` 已经进入：

- formal gate
- intro pending / complete
- night pressure
- DayEnd 收束

这部分不再是我接下来优先回头修的主矛盾。

---

## 三、用户这轮真正把我的任务改写成了什么

用户现在真正要我开始想的，不是继续磨一个小红错。

用户最新真正关心的是：

1. 什么时候可以开始把剧情按场地分清
2. 什么时候可以开始排 NPC 的剧本层出场、站位、围观、夜间见闻
3. `Primary` 只是现在的临时承载，不是长期正式剧情场地
4. `Town` 既然已经做了一部分，那么导演线什么时候开始真正消费它

所以我现在最该做的，不是再漂去 UI，也不是再漂去 Town scene 本体，而是：

- 把导演线和场地承接边界说清，再继续写剧本层分场和调度

---

## 四、当前已经正式分出去的内容

这一条必须记住，不然下一轮还会串线。

### 1. 已分给 `UI` 的

`UI` owns：

1. 任务栏
2. Prompt
3. DialogueUI
4. continue 提示
5. 工作台正式面 / 悬浮面
6. 玩家面中文显示与排版

我不再 owns 这些玩家面结果层。

### 2. 已分给 `NPC` 的

`NPC` owns：

1. NPC own 气泡
2. 会话状态
3. pair / casual / ambient 底座
4. 之后的群像层承接

我不再 owns NPC own runtime 气泡本体。

### 3. 仍在 `Town / 外线治理` 的

`Town` 当前还没 `sync-ready`。

仍归外线治理的包括：

1. 相机 / frustum
2. `DialogueUI / 字体链`
3. `PlacementManager.cs` 编译红

这些我不能拿来假装是导演线自己的活。

---

## 五、当前已经拿到的关键真值

这是我接下来所有导演推进都要以它为准的地方。

### 1. `Town` 当前正式身份

`Town` 现在应视为：

- 村庄承载层
- 负责 Day1 后续生活面、背景层、群像层、夜间见闻层、日常站位层

它不是：

- `CrashAndMeet / EnterVillage` 前半段剧情源 owner

### 2. 导演线现在可消费的 Town 范围

现在已经可以消费的是：

- 导演层
- 分场层
- 背景调度层

现在可直接按 `Town` 去写的，是：

1. `EnterVillage` 进村后的围观层
2. 小屋外部周边生活感
3. `DinnerConflict` 的晚餐背景层
4. `FreeTime / DayEnd` 的夜间见闻层
5. 第二天开始的村中日常站位层

### 3. 现在仍不能假设的

现在还不能假设 `Town` 已能稳定承接：

1. 完整切场
2. 精确路径
3. 相机联动
4. 最终 runtime 触发
5. tile 级路径
6. 秒级移动脚本

也就是说：

- 我现在可以开始按 Town 分戏
- 但不能把 runtime 已闭环当成前提

---

## 六、我现在真正该做的，不是再泛改，而是二分推进

### 1. 继续留在临时承载的段落

这些段落现在仍要保留在当前临时/抽象承载中：

1. `CrashAndMeet` 全段
2. `EnterVillage` 的进村前半段
3. `HealingAndHP`
4. `WorkbenchFlashback`
5. `FarmingTutorial`

### 2. 开始按 `Town` 分场的段落

这些段落现在应该开始正式按 `Town` 分场：

1. `EnterVillage` 的 post-entry crowd
2. `DinnerConflict` 的背景承接
3. `ReturnAndReminder` 的回村氛围承接
4. `FreeTime` 的夜间村庄层
5. `DayEnd` 的夜间收束与次日站位预示

这就是我接下来真正的导演线工作面。

---

## 七、我现在应该开始写的，不是 runtime，而是导演脚本

### 1. 现在我可以写到什么粒度

我现在可以开始写：

1. 分场
2. 站位
3. 视线关系
4. 围观关系
5. 背景层密度
6. 谁在什么时候出现
7. 谁承担哪种村庄层语义

### 2. 现在我不要写死什么

我现在还不要写死：

1. runtime 切场
2. 精确移动路线
3. 秒级 timing
4. nav route
5. 相机和触发脚本

所以当前正确口径是：

- 先写“导演脚本级分场与调度”
- 暂不写“runtime 工程级精确驱动”

---

## 八、当前可直接消费的 anchors，要开始当成戏位，不是摆设

当前这些位点已经有导演语义：

1. `EnterVillageCrowdRoot`
   - 进村后围观层
   - 让位层
   - 初见集体视线层
2. `KidLook_01`
   - 单点儿童观察位
   - 好奇目光位
3. `DinnerBackgroundRoot`
   - 晚餐背景层
   - 远景生活层
   - 非主戏人群层
4. `NightWitness_01`
   - 夜间见闻位
   - 夜里有人看见主角或投来一句话的位置
5. `DailyStand_01`
   - 次日村中日常站位 1
6. `DailyStand_02`
   - 次日村中日常站位 2
7. `DailyStand_03`
   - 次日村中日常站位 3

我下一步必须开始把这些位点真正变成：

- 分场依据
- 视线依据
- 群像层依据

---

## 九、我和 `NPC` 的协同关系

这条也必须记清楚，不然又会互相抢。

### 1. 我来做的

我来定：

1. 哪段戏发生在哪
2. 哪个层次需要什么气质
3. 哪个锚点承担什么戏
4. 哪类人应该出现在那个层

### 2. `NPC` 来做的

`NPC` 去承接：

1. 具体群像层
2. 背景层
3. 观察层
4. 夜间见闻层
5. 次日站位层

换句话说：

- 我给“戏”
- 他给“人”和“群像承接”

---

## 十、我和 `Town` 的协同关系

### 1. 我不 owns `Town`

我不去写：

1. `Town.unity`
2. 相机 / frustum
3. `DialogueUI / 字体链`
4. `PlacementManager.cs`

### 2. 但我已经可以消费 `Town`

我现在可以用它来做：

1. 空间分层
2. 群像分层
3. 背景层承接
4. 夜间见闻层承接

所以我下一轮如果还停在“等 Town 完全好了再说”，那就是我自己在拖。

---

## 十一、我下一阶段真正该产出的，不再是泛需求，而是 3 类东西

### 1. 分场说明

把以下几段正式写成场地化分场：

1. `EnterVillage` post-entry
2. `DinnerConflict` 背景层
3. `ReturnAndReminder` 回村氛围
4. `FreeTime` 夜间村庄层
5. `DayEnd` 夜间收束与次日预示

### 2. 群像调度说明

把这些位点的戏位写清：

1. 围观
2. 观察
3. 晚餐背景
4. 夜间见闻
5. 次日站位

### 3. 与当前 phase 真值不冲突的承接说明

我要始终守住：

- 现在是导演层可开始
- 不是 runtime 层已闭环

---

## 十二、我下一轮最容易犯的错

必须提前写清，避免再犯。

### 1. 漂去 UI

错。  
玩家面不是我 owns。

### 2. 漂去 Town scene 本体

错。  
我消费 Town，不 owns Town。

### 3. 漂去 NPC own bubble / session

错。  
那些已分给 NPC。

### 4. 继续只写 phase 文案，不开始分场

也错。  
当前用户关心的已经是场地分戏和剧本走位层了。

---

## 十三、一句话提醒自己

我现在该做的，不是再证明 Day1 能跑。

我现在该做的是：

- 从 `EnterVillage post-entry` 开始，正式把 Day1 写成“发生在场地里的戏”，并把群像层交给 `NPC` 去承接。
