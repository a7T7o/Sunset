# A1 保底六点卡

## 当前主线

这轮 UI 线程在收到你的 `PromptOverlay` 治理裁定后，已经停止继续碰 [SpringDay1PromptOverlay.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs)。  
当前主线改为：只继续收我 own 的两条线：

1. `hotbar / placement / save-load` 的统一入口和复位合同
2. `StatusOverlay / InteractionHintOverlay` 的 16:10 与独立 overlay 缩放问题

## 这轮实际做成了什么

### 1. 已接受你的裁定边界

- `PromptOverlay` 这轮已冻结
- 没有继续往 `SpringDay1PromptOverlay.cs` 上追加新的治理性补丁
- 后续等你主刀拆完 `formal task card vs manual prompt`，我再只收显示壳

### 2. hotbar / placement / save-load 这条链已经先砍了一刀

已改文件：

- [GameInputManager.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/Input/GameInputManager.cs)
- [ToolbarSlotUI.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/UI/Toolbar/ToolbarSlotUI.cs)
- [SaveManager.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Data/Core/SaveManager.cs)

这刀实际做成的点：

1. `点击` 和 `滚轮/数字键` 的即时切槽，现在共用同一个业务入口：
   - `GameInputManager.TryApplyHotbarSelectionChange(int requestedIndex)`
2. `ToolbarSlotUI.OnPointerClick()` 不再自己单独跑一套 panel/reject/selection 逻辑
3. 世界左键不再只看 `PlacementManager.Instance.IsPlacementMode`
   - 现在改成只认“活的放置会话”
   - 也就是：
     - `GameInputManager.IsPlacementMode`
     - `PlacementManager.IsPlacementMode`
     - 当前真实可放置选中项
     三者同时成立才会把左键路由给 placement
4. 读档前会强制关掉 placement 运行态：
   - `SaveManager.ApplyLoadedSaveData()` 里已接：
     - `GameInputManager.ForceResetPlacementRuntime("读档前强制关闭放置模式与残留预览")`
5. 原生 fresh start 前也会强制关掉 placement 运行态
6. 场景切换后 `GameInputManager.OnSceneLoaded()` 也会做一次 placement runtime reset

### 3. 状态 HUD 的独立 overlay 缩放已经补上

已改文件：

- [SpringDay1StatusOverlay.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs)
- [InteractionHintOverlay.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/UI/InteractionHintOverlay.cs)

实际改动：

1. 这两个独立 `ScreenSpaceOverlay` 现在都补了 `CanvasScaler`
2. 当前统一参考分辨率：
   - `1980 x 1080`
3. 当前匹配模式：
   - `Match Width Or Height`
   - `match = 1`
4. 目标是把它们拉回和主 UI 更一致的 16:9 / 16:10 表现基线，而不是继续裸跑固定像素

## 现在还没做成什么

1. `PromptOverlay / 任务清单`
   - 按你的裁定，这轮我没有再碰
   - 这部分仍待你主刀拆 owner
2. `hotbar / placement / save-load`
   - 我只先收了“统一入口 + 强制 reset”这一刀
   - 还没做完整 live 矩阵终验
3. `StatusOverlay / InteractionHintOverlay`
   - 代码层缩放已补
   - 但还没拿到 fresh live 图确认 16:10 和状态卡重叠是否完全过线

## 当前阶段

当前属于：

- `PromptOverlay`：已按 day1 裁定冻结，等你主刀拆 owner
- `hotbar / placement / save-load`：结构线已推进到“统一入口与复位合同成立”
- `status HUD`：结构线已推进到“独立 overlay 已接统一缩放基线”
- 整体仍是：`代码层成立，live 体验待继续验证`

## 下一步只做什么

如果你这边继续主刀 `PromptOverlay`，我这边下一步只继续 own 的两刀：

1. 继续补 `hotbar / placement / save-load` 的 live 边界闭环
2. 继续补 `StatusOverlay / InteractionHintOverlay` 的 live 细节收口

不会回漂去继续改 `SpringDay1PromptOverlay.cs`

## 需要你现在做什么

1. 继续按你的裁定主刀拆：
   - `formal task card`
   - `manual prompt`
2. 如果你需要，我可以在你拆完后再回来只收：
   - fixed HUD 壳
   - 16:10 / packaged live 的最终显示面

# A2 用户向补充层

我这轮最核心的判断是：你这次给 UI 的裁定是对的，`PromptOverlay` 确实已经不是 UI 线程单独继续补治理就能收平的阶段了。  
所以我这轮主动把自己从 `PromptOverlay` 撤下来，只继续推进 UI 自己还能安全推进的两条 own 链：输入/放置闭环，以及状态 HUD 缩放。

我这轮最不放心的点也很明确：

1. `ToolbarSlotUI` 的 fresh validate 只拿到了 `external_red`
   - 当前外部红来自：
     - `NPCMotionController.cs`
   - 不是我 own 的红
2. `status HUD` 的代码层虽然 clean，但 live 体验还没拿到最终图

自评：`8/10`

方向已经重新收正，边界也守住了；没到更高分，是因为这轮还没把玩家可见体验完全闭环。

# B 技术审计层

## touched files

- [GameInputManager.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/Input/GameInputManager.cs)
- [ToolbarSlotUI.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/UI/Toolbar/ToolbarSlotUI.cs)
- [SaveManager.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Data/Core/SaveManager.cs)
- [SpringDay1StatusOverlay.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs)
- [InteractionHintOverlay.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Story/UI/InteractionHintOverlay.cs)
- [2026-04-10_UI线程_向spring-day1求助_任务清单打包异常与治理口径确认prompt_32.md](D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/2026-04-10_UI线程_向spring-day1求助_任务清单打包异常与治理口径确认prompt_32.md)

## no-red 证据

### `GameInputManager.cs`

- command:
  - `validate_script Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
- result:
  - `assessment=no_red`
  - `owned_errors=0`
  - `external_errors=0`

### `SaveManager.cs`

- command:
  - `validate_script Assets/YYY_Scripts/Data/Core/SaveManager.cs`
- result:
  - `assessment=no_red`
  - `owned_errors=0`
  - `external_errors=0`

### `InteractionHintOverlay.cs`

- command:
  - `validate_script Assets/YYY_Scripts/Story/UI/InteractionHintOverlay.cs`
- result:
  - `assessment=no_red`
  - `owned_errors=0`
  - `external_errors=0`

### `SpringDay1StatusOverlay.cs`

- command:
  - `validate_script Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs`
- result:
  - `assessment=no_red`
  - `owned_errors=0`
  - `external_errors=0`

### `ToolbarSlotUI.cs`

- command:
  - `validate_script Assets/YYY_Scripts/UI/Toolbar/ToolbarSlotUI.cs`
- result:
  - `assessment=external_red`
  - `owned_errors=0`
  - `external_errors=2`
- 当前 external blocker：
  - [NPCMotionController.cs](D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Controller/NPC/NPCMotionController.cs)
    - `EmitFacingMismatchDiagnostics` 缺失
    - `ResetFacingMismatchDiagnostics` 缺失

## thread-state

- `Begin-Slice`: 已跑
- `Park-Slice`: 将在我这轮停下后跑
- 当前 intended state：
  - `PARKED`
