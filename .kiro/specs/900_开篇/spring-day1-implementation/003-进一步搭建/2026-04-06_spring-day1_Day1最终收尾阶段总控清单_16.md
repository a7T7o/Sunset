# 2026-04-06｜spring-day1｜Day1 最终收尾阶段总控清单

这不是普通续工 prompt。

这是 `spring-day1` 作为 `Day1 owner / 导演 / 主控台 / 最终整合位` 的当前正式执行清单。

从这一版开始，`day1` 不再把自己理解成四条并行施工线里的其中一条，而是：

1. 定义 `Day1` 最终什么叫完成
2. 吃回 `UI / NPC / Town` 三线已经做成的内容
3. 把 `opening -> DayEnd` 真正整成一个完整可停 baseline

---

## 开始前统一口径

### 允许按需使用 subagent

允许按需使用 `subagent`，但只用于：

1. 只读梳理
2. 独立文件实现
3. 补测试 / 补验证

关键架构、总整合、主线判断、最终裁定必须仍由 `spring-day1` 自己掌控。

### 执行纪律

1. 有问题先回到 `spring-day1` 主控台，不要各线自行改题
2. 没问题就继续下一部分，不要轻易停在“分析完成”
3. 这份清单只列 `Day1 最终收尾阶段` 直接相关的内容，不等于各线程全部 backlog

---

## 当前已核实的真实起点

### 1. 当前外部红不是第一问题

当前 fresh console 已核到：

- `errors=0`
- `warnings=0`

### 2. NPC 侧可被 Day1 直接消费的 contract 已站住

已核实真实存在：

1. `BuildBeatConsumptionSnapshot()`
2. `SpringDay1CrowdDirectorConsumptionRole`
3. `NPCFormalDialogueState`
4. `HasConsumedFormalDialogueForCurrentStory()`
5. `WillYieldToInformalResident()`

### 3. Town 侧 Day1 场景承接层已不是空壳

已核实 [Town.unity](D:/Unity/Unity_learning/Sunset/Assets/000_Scenes/Town.unity) 里真实存在：

1. `Town_Day1Carriers`
2. `Town_Day1Residents`
3. `EnterVillageCrowdRoot`
4. `KidLook_01`
5. `DinnerBackgroundRoot`
6. `NightWitness_01`
7. `DailyStand_01`
8. `ResidentSlot_* / DirectorReady_* / BackstageSlot_*`

### 4. UI 不再是全线失火

已核实：

1. `Prompt / 任务卡` 已基本退主战场
2. `DialogueUI` 的字体 / fallback portrait / continue 文案基础链已在
3. 当前 UI 主战场收敛到：
   - `Workbench`
   - `DialogueUI` 正式玩家面 live 终验

### 5. Day1 自己已具备可用导演工具底座

已核实：

1. `SpringDay1DirectorStaging.cs`
2. `SpringDay1DirectorStagingWindow.cs`
3. `SpringDay1DirectorStageBook.json`

都已经有 `semantic anchor` 驱动起点/偏移的数据链。

---

## 最终完成定义

`Day1` 当前阶段的完成，不是“再多做一点内容”，而是同时满足下面 5 条：

1. 正式剧情、正式任务、正式聊天都满足 `一次性消费，不可重入`
2. `101~301` 不再给玩家强烈“阶段到了才突然蹦出来”的体感
3. 后半段 crowd / resident / director cue / Town anchor 已被主链真实吃回
4. `UI / NPC / Town` 三线成果已经并回 `Day1` 主链，而不是各自悬空成立
5. 从 `opening` 到 `DayEnd` 能跑出一条完整 baseline，且当前证据下没有新的主线级 blocker

---

## 第一部分：先把 one-shot 逻辑彻底守死

这一部分是 `day1` 当前最高优先级，不守住就是 bug。

### 目标

把下面三类全部改成正式的 `单次消耗、不可重入`：

1. 正式剧情
2. 正式任务推进
3. 正式 NPC 聊天

### 必须做成

1. 任何已经走过的正式剧情段，不得再次以正式推进身份重播
2. 任何已经完成的任务，不得再次触发同一正式任务推进
3. 任何已经消费过 formal 的 NPC，再次交互时只能落到：
   - `informal`
   - `resident 日常句池`
   - `phase 后非正式补句`
4. `Prompt / proximity hint / director beat / 对话入口` 的对外语义必须一致

### 这部分主要由 day1 自己吃

不要把这部分甩给 `UI` 或 `NPC`。

原因是：

1. `NPC` 已经把 formal-consumed contract 交出来了
2. 真正要把它吃回主链、变成整个 `Day1` 运行时统一规则的人，是 `day1`

---

## 第二部分：把 resident deployment 和 director consumption 真正吃回主链

这一部分是当前第二主刀，不做就还是“合同在，现场没接上”。

### 目标

让 `101~301` 从“到 cue 才像演员一样生成”转成“更像 Day1 村里本来就在的人”。

### 优先顺序

先吃：

1. `EnterVillageCrowdRoot`
2. `KidLook_01`
3. `NightWitness_01`

站住后继续吃：

4. `DinnerBackgroundRoot`
5. `DailyStand_01`
6. `DailyStand_02`
7. `DailyStand_03`

### 必须做成

1. `SpringDay1NpcCrowdDirector` 真正消费 resident / semantic anchor / scene-side parent
2. `BuildBeatConsumptionSnapshot()` 不只是存在，而是被 `day1` 真正用于 beat 消费
3. `director cue`、`resident parent`、`scene duty` 三层不要互相打架
4. 玩家看到的存在方式要尽量从“突然出现”转向“早就在场 / 让位 / 后台待命 / 夜段出场”

---

## 第三部分：把后半段导演数据继续压到当前能压的最深处

导演工具现在不是成果本身，而是生产工具。

### 目标

继续把后半段 beat/cue 压成可真实消费的数据，而不是停在“有编辑器、有结构”。

### 当前必须继续推进的段落

1. `EnterVillage_PostEntry`
2. `DinnerConflict_Table`
3. `ReturnAndReminder_WalkBack`
4. `FreeTime_NightWitness`
5. `DayEnd_Settle`
6. `DailyStand_Preview`

### 当前最值钱的补刀点

1. `DinnerBackgroundRoot` 再往多人背景层推进一刀
2. `DailyStand_02 / 03` 真正补进当前 director/runtime 可消费链
3. 继续把 stale 的绝对 path / proxy cue 往 `semantic anchor + offset` 迁

### 完成定义

不是“又多了几个字段”，而是：

1. 写进 stage book
2. 能被当前 runtime 消费
3. 尽可能拿到 live / 测试 / fresh 证据

---

## 第四部分：把剧情正式产物继续往可资产化层推进

这部分不要求一口气做完整案，但要求不再停在抽象思路。

### 至少继续推进的主段

1. `CrashAndMeet`
2. `EnterVillage`
3. `HealingAndHP`
4. `WorkbenchFlashback`
5. `FarmingTutorial`
6. `DinnerConflict`
7. `ReturnAndReminder`
8. `FreeTime`
9. `DayEnd`

### 当前应优先落成的形态

能落什么落什么：

1. 正式对白稿
2. 可拆对白包的话术
3. 可拆事件包的节点顺序
4. 与 director beat / phase 推进相接的正式文本层

### 原则

不要再写成“后面再资产化”的抽象说明。

当前阶段应尽量写成：

1. 后续可直接进对白资产
2. 后续可直接进事件资产
3. 后续可直接挂回主链

---

## 第五部分：把三条协作线成果真正吃回 Day1

这是 `day1` 的总控职责，不属于任何协作线自己。

### UI 要吃回来的，不是代码所有权，而是结果

当前 `day1` 应接收的 UI 结果是：

1. `Workbench` 玩家面最终表现
2. `DialogueUI` 正式玩家面最终表现
3. formal/informal/resident 提示壳在 one-shot 规则下的对外一致性

### NPC 要吃回来的，是 contract 和内容层

当前 `day1` 应接收的 NPC 结果是：

1. resident consumption snapshot
2. formal-consumed 后让位 contract
3. resident / informal / post-phase 内容承接

### Town 要吃回来的，是场景承接层

当前 `day1` 应接收的 Town 结果是：

1. resident root / slot / anchor 可被 runtime 正常承接
2. `EnterVillage / Dinner / NightWitness / DailyStand` 这些 anchor 在真实 scene 里可持续消费

---

## 第六部分：最终总验证

### 至少要补齐的验证层

1. fresh console / 最小 no-red
2. 导演链自己的可运行验证
3. 关键 phase 的 one-shot 回归验证
4. 从 `opening` 到 `DayEnd` 的整链 baseline

### 当前最该优先看的回归点

1. formal 是否会重播
2. 任务是否会回滚成“再次正式推进”
3. resident 是否仍像突然生成
4. Workbench / DialogueUI 是否还拖主线体验

---

## 当前阶段判断

当前已经不是“建设阶段”，而是 `Day1 最终收尾总阶段`。

这阶段的正确顺序固定为：

1. 先守 one-shot
2. 再吃 deployment / director consumption
3. 再压导演数据与正式剧情产物
4. 最后做整链 baseline 与总验收

---

## 当前不该再做的事

1. 不要把自己再次降级成普通施工线
2. 不要把 UI 壳体细节回吞给 day1
3. 不要把 NPC 会话底座回吞给 day1
4. 不要把 Town 当成“整份 scene 已 fully ready 的静态背景”
5. 不要再回到“谁回执声音大就跟谁走”的被动调度
