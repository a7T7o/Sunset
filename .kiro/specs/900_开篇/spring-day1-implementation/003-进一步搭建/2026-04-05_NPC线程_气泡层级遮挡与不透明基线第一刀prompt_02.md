# 2026-04-05 `NPC` 线程｜气泡层级 / 遮挡 / 不透明基线第一刀 prompt `02`

请先完整读取：

1. `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\0.0.2清盘002\memory.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\memory.md`

这轮不是让你回头吞：

- `InteractionHintOverlay.cs`
- `SpringDay1PromptOverlay.cs`
- `SpringDay1WorkbenchCraftingOverlay.cs`
- `DialogueUI.cs`
- `Town / Primary.unity`
- shared prompt shell
- 左下角提示壳本体

这轮也不是让你继续泛讲 `NPC` 以后怎么做。

---

## 一、先冻结当前 accepted baseline

你这次回执里，下面这些边界我方接受：

1. 你继续只守 `NPC own`：
   - `NPCBubblePresenter.cs`
   - `PlayerNpcChatSessionService.cs`
   - `speaking-owner / pair / ambient bubble` 底座
2. `UI` 继续守 shared 玩家面壳体：
   - 左下角提示壳
   - `PromptOverlay`
   - `WorkbenchOverlay`
   - `Town DialogueUI`
3. `spring-day1` 继续守：
   - Day1 剧情顺序
   - phase / 任务约束
   - 不是 NPC bubble 底座 owner

但我要补一条收紧：

- “提示类 UI 的可隐藏设置接口”不归你整体吞并
- 正确口径是：
  - shared UI 壳的 hide/show 接口仍归 `UI`
  - 你只负责给出 NPC own 一侧的语义信号 / contract / speaking-owner 真值

---

## 二、这轮唯一主刀

你这轮只做一刀，而且就是：

**把 NPC own 的气泡可见性基线真正收稳**

只包含 3 件事：

1. 谁在说话，谁的气泡就在上面
   - `玩家 ↔ NPC`
   - `NPC ↔ NPC`
   - `ambient / pair` 同屏时也要守住 speaking-owner
2. NPC 气泡不再被树木或场景物体压住
   - 这轮只收 NPC own 气泡链
   - 不扩到 shared prompt / 左下角壳
3. 气泡背景彻底不透明
   - 保持现有 live 主题
   - 不准顺手重做一套新皮肤

一句话说死：

- 这轮只收 `谁在说话谁在上面 + 不被场景挡 + 背景不透明`

---

## 三、允许的作用域

你这轮允许碰的面，按从主到辅排序：

1. `Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs`
2. `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs`
3. `Assets/YYY_Tests/Editor/PlayerThoughtBubblePresenterStyleTests.cs`
4. `Assets/YYY_Tests/Editor/PlayerNpcConversationBubbleLayoutTests.cs`

必要时如果 speaking-owner 真值必须补 contract，才允许极少量触达：

5. `NPCDialogueInteractable.cs`
6. `NPCInformalChatInteractable.cs`

但只允许碰与：

- speaking-owner
- 会话 owner 释放
- 气泡显示触发真值

直接相关的切片。

---

## 四、这轮明确不要做

1. 不要去改 `PromptOverlay / Workbench / 左下角提示壳`
2. 不要去改 `DialogueUI`
3. 不要去改 `Primary.unity`
4. 不要去改 `GameInputManager.cs`
5. 不要去做 NPC 头顶提示退场总整合
6. 不要去做正式/闲聊全链交互系统重构
7. 不要去泛修 `NPC 很多体验`

特别强调：

- 这轮不解决 “formal 提示该显示什么壳”
- 这轮只解决 “NPC own 气泡本体怎么正确显示”

---

## 五、为什么这刀现在最该先做

因为当前用户最直接反复抱怨、而且不撞 `UI` 壳 owner 的，就是：

1. 气泡被场景压住
2. 谁在说话却不在最上面
3. 背景半透明导致观感不对

这些都属于 `NPC own`，不需要继续等 `UI` 或 `spring-day1` 来兜。

相比之下：

- 左下角提示 formal 语义
- shared 提示壳 hide/show
- Town 对话面字体/底板

都还会继续撞到 `UI` own 壳，不适合让你这轮先吞。

---

## 六、完成定义

这轮只有下面这些同时成立，才算过：

1. speaking-owner 排序在 `玩家↔NPC / NPC↔NPC / ambient/pair` 三类场景都守住
2. NPC 气泡不再被树木/场景物体盖住
3. 气泡背景完全不透明
4. 没改现有气泡主题样式
5. 没越权吞 shared UI 壳

如果外部编译红仍卡在：

- `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs(1694,23)`

那你可以停在：

- `代码完成 + own tests/static 通过 + runtime/live 被外部 blocker 卡住`

但不能把这轮说成已经 live 终验。

---

## 七、回执格式

先给用户可读层六点卡：

1. 当前主线：
2. 这轮实际做成了什么：
3. 现在还没做成什么：
4. 当前阶段：
5. 下一步只做什么：
6. 需要我现在做什么（没有就写无）：

然后补一个 `本轮三项结果`：

1. speaking-owner 置顶：
2. 气泡遮挡：
3. 背景不透明：

然后再补技术审计层：

- changed_paths：
- owned_files：
- code_self_check：
- 当前 owned errors：
- 当前 external blockers：
- 当前 warnings：
- 当前 live 状态：

---

## 八、thread-state

[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：

- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

---

## 最后一句话

你这轮不是继续“做 NPC 方向很多东西”，
你这轮只做：

- **NPC own 气泡本体的显示正确性基线**

也就是：

- 说话者在上
- 不被场景压
- 背景不透明

别的都不要顺手带出来。
