# Day1 工作台 UI 与任务体验卫生清扫委托（第六轮：hot-file hygiene）

治理恢复摘要版。

原文件已在 `2026-03-26` 被 `spring-day1` 线程越权删除。  
本文件依据治理线程原始分发内容恢复为摘要版，只用于修复链接与审计链。

## 当轮主刀

不继续做 Day1 新功能，只做 Day1 自己的 hot-file hygiene 与资产归类收口。

## 当轮重点摘要

1. `Primary.unity` 是 A 类热文件，且当时已被裁定为 `dirty + unlocked + mixed-in-place`。
2. `spring-day1` 必须只读审 `Primary.unity`，不能在未拆 owner 前续写 scene。
3. hygiene 范围不止文档，还包括：
   - `Primary.unity`
   - 3 个 workbench recipe
   - `Assets/222_Prefabs/UI/Spring-day1/`
   - `SpringDay1DialogueProgressionTests.cs`
   - prompt / snapshot / 文档残留
4. 重点不是继续清扫一切，而是先把：
   - 哪些是 own
   - 哪些不是
   - 哪些 mixed
   说清楚。

## 恢复说明

这份文件当前只保留治理摘要。  
后续最新纠偏续工应直接看：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\26.03.26-Day1越权删证据纠偏与正规化续工委托-07.md`
