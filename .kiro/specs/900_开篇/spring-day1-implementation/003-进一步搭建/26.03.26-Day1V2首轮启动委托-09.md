# 26.03.26-Day1V2首轮启动委托-09

你现在是新线程 `spring-day1V2`。

这轮不是继续乱碰 `Primary.unity`，也不是继续替 shared root 做清扫。

## 当前已接受的基线

1. 当前已接受的正式 checkpoint 是：
   - `ee318757`
2. 当前 own 正式面已经被钉死为：
   - `Assets/222_Prefabs/UI/Spring-day1/`
   - `Assets/Resources/Story/SpringDay1Workbench/*.asset`
   - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese*.asset`
   - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
3. 当前最重要的 blocker 是：
   - `Assets/000_Scenes/Primary.unity` 仍是 `mixed hot-file blocker`

## 先完整读取

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\V2交接文档\00_交接总纲.md`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\V2交接文档\05_当前现场_高权重事项_风险与未竟问题.md`
3. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\V2交接文档\06_证据索引_必读顺序_接手建议.md`
4. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\26.03.26-Day1越权删证据纠偏与正规化续工委托-07.md`
5. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`

## 这轮唯一主刀

只做 `spring-day1V2` 接班后的：

> live preflight + 非热正式面首刀裁定

更具体地说，这轮只允许你完成下面这 1 刀：

1. 先只读复核：
   - `cwd / branch / HEAD / git status`
   - `shared-root-branch-occupancy.md`
   - own 正式面是否仍完整存在
   - `Primary.unity` 是否仍是 mixed hot-file blocker
2. 然后给出 V2 的首刀裁定：
   - 如果当前仍不能安全碰 scene，就把第一实现切口固定到非热正式面，并明确是哪一刀；
   - 如果你判断 scene 热区条件已意外转绿，也不能直接开写，先停下说明为什么现在值得申请进入。

## 明确禁止

1. 不要先碰 `Primary.unity`
2. 不要先删 `.kiro` 证据
3. 不要先认领 `GameInputManager.cs / PlacementManager.cs / StaticObjectOrderAutoCalibrator.cs / TagManager.asset`
4. 不要先从“我要把 shared root 清干净”开始
5. 不要先续写新功能代码
6. 不要把子工作区乱码 memory 当成主入口

## 允许的作用域

这轮只允许你读取和必要时改动这些范围：

1. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\V2交接文档\`
2. `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`
4. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\`
5. `D:\Unity\Unity_learning\Sunset\Assets\Resources\Story\SpringDay1Workbench\`
6. `D:\Unity\Unity_learning\Sunset\Assets\TextMesh Pro\Resources\Fonts & Materials\DialogueChinese*.asset`
7. `D:\Unity\Unity_learning\Sunset\Assets\YYY_Tests\Editor\SpringDay1DialogueProgressionTests.cs`

## 完成定义

只有同时满足下面全部条件，这轮才算完成：

1. 你已经把当前 live preflight 读实；
2. 你已经明确回答 `Primary.unity` 当前是否仍是 mixed hot-file blocker；
3. 你已经给出 V2 第一刀到底是哪个“非热正式面切口”或“为什么现在值得申请 scene 进入”的单一裁定；
4. 你没有直接开写 scene，也没有扩认别线文件；
5. 你已把这轮结果追加记录至：
   - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1V2\memory_0.md`

## 最小回执格式

- 已回写文件路径
- 当前是否仍判 `Primary.unity` 为 blocker：`yes / no`
- V2 第一刀裁定是什么
- 一句话摘要

## 一句话总口径

你这轮先别急着续写 Day1，而是先用 live preflight 把边界重新钉实，然后只给出一个真正安全的 V2 第一刀。
