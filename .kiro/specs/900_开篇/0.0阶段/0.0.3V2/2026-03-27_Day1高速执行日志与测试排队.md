# Day1 高速执行日志与测试排队

- 模式：高速列车模式
- 启动时间：2026-03-27
- 当前基线：`main @ 1401ae8c`
- 当前原则：
  - 先一路埋头做非阻塞内容
  - 只在出现可测窗口时插测
  - 测成了记结果，测不成也记原因，不因为测试断点停工
  - `Primary.unity` 与 hot/mixed 面只在真正需要时再判是否进入

## 1. G0 施工闸门快照

### G0-01｜当前 live blocker / mixed owner 复核

- 当前可直接推进的 formal-face：
  - `Assets/YYY_Scripts/Story/`
  - `Assets/111_Data/Story/Dialogue/`
  - `Assets/222_Prefabs/UI/Spring-day1/`
  - `Assets/Resources/Story/SpringDay1Workbench/`
  - `Assets/Editor/Story/SpringDay1WorkbenchSceneBinder.cs`
  - `Assets/Editor/Story/SpringDay1BedSceneBinder.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 当前确认仍属 hot / mixed / same-root blocker 的面：
  - `Assets/000_Scenes/Primary.unity`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
  - `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
  - `Assets/YYY_Scripts/Service/Placement/PlacementManager.cs`
  - `Assets/YYY_Scripts/Farm/FarmToolPreview.cs`
  - `Assets/YYY_Scripts/Service/Player/PlayerInteraction.cs`
  - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
  - `Assets/YYY_Scripts/UI/Inventory/InventoryPanelUI.cs`
  - `Assets/YYY_Scripts/World/Placeable/ChestController.cs`
  - `Assets/YYY_Scripts/Controller/TreeController.cs`
- 当前明确 foreign / 非本刀不收：
  - `NPC`、`农田交互修复V3`、`导航检查`、`Codex规则落地` 各自 memory / 文档脏改
  - `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`

### G0-02｜P0 单纵切当前文件闭包

- 本轮高速优先直推的 P0 文件闭包：
  - `Assets/YYY_Scripts/Story/Managers/StoryManager.cs`
  - `Assets/YYY_Scripts/Story/Managers/DialogueManager.cs`
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`
  - `Assets/YYY_Scripts/Story/Interaction/CraftingStationInteractable.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorkbenchCraftingOverlay.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1UiLayerUtility.cs`
  - `Assets/YYY_Scripts/Service/Player/HealthSystem.cs`
  - `Assets/YYY_Scripts/Service/Player/EnergySystem.cs`
  - `Assets/111_Data/Story/Dialogue/SpringDay1_FirstDialogue.asset`
  - `Assets/111_Data/Story/Dialogue/SpringDay1_FirstDialogue_Followup.asset`
  - `Assets/Resources/Story/SpringDay1Workbench/Recipe_9100_Axe_0.asset`
  - `Assets/Resources/Story/SpringDay1Workbench/Recipe_9101_Hoe_0.asset`
  - `Assets/Resources/Story/SpringDay1Workbench/Recipe_9102_Pickaxe_0.asset`
  - `Assets/222_Prefabs/UI/Spring-day1/SpringDay1PromptOverlay.prefab`
  - `Assets/222_Prefabs/UI/Spring-day1/SpringDay1WorkbenchCraftingOverlay.prefab`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 当前明确禁碰清单：
  - `Primary.unity`
  - `DialogueChinese*`
  - `GameInputManager.cs`
  - `PlacementManager.cs`
  - `FarmToolPreview.cs`
  - `PlayerInteraction.cs`
  - `InventoryPanelUI.cs`
  - `PlayerThoughtBubblePresenter.cs`
  - `ChestController.cs`
  - `TreeController.cs`

### G0-03｜P0 白名单 preflight 摘要

- 执行命令口径：
  - `sunset-git-safe-sync.ps1 -Action preflight -OwnerThread spring-day1V2 -Mode task`
- 本次 preflight 结论：
  - `Assets/YYY_Scripts/Story`、`Assets/111_Data/Story/Dialogue`、`Assets/222_Prefabs/UI/Spring-day1`、`Assets/Resources/Story/SpringDay1Workbench` 可作为当前 P0 高速主通道
  - 但若把 `HealthSystem.cs / EnergySystem.cs / SpringDay1DialogueProgressionTests.cs` 直接纳入白名单，同根会被以下 remaining dirty 阻断：
    - `Assets/YYY_Scripts/Service/Player/PlayerInteraction.cs`
    - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
    - `Assets/YYY_Tests/Editor/NPCToolchainRegularizationTests.cs`
- 当前分流口径：
  - 可以继续先做：`Story/* + Dialogue 资产 + Workbench UI/recipe + PromptOverlay + Director`
  - 可以继续写但暂不承诺立刻白名单收口：`HealthSystem.cs / EnergySystem.cs / SpringDay1DialogueProgressionTests.cs`
  - 仍必须等待热窗或 owner 裁定：`Primary.unity`、`DialogueChinese*`、木材防 bypass 那组 hot/mixed 面

### G0-04｜高速模式最小回执模板

- `当前任务编号`
- `当前在改什么`
- `changed_paths`
- `是否触碰高危目标`
- `当前代码自检`
- `当前测试结果`
- `测试阻断 / 待测项`
- `当前是否可直接白名单提交`
- `blocker_or_checkpoint`

## 2. 执行日志

## E-001｜高速模式启动与 G0 复核

- 结论：
  - shared root 当前仍是 `main + neutral`
  - `Primary.unity` 当前仍按 mixed hot-file blocker 处理
  - P0 formal-face 主通道仍然成立，但 `Service/Player` 与 `YYY_Tests/Editor` 存在 same-root 收口阻断

## E-002｜P0 formal-face 现状扫读

- 已确认当前代码面已经存在较完整骨架：
  - 首段对话资产、解码态、剧情阶段推进
  - 疗伤 + HP / EP 显隐与动画
  - 工作台入口、任务卡、制作浮层、进度与晚餐/回住处桥
  - 晚餐、提醒、自由时段、睡觉结束
- 当前判断：
  - 这条线不是从零开始缺代码，而是需要继续补“体验过线”和“收口/验证可用性”缺口

## E-003｜工作台浮层补“手调/微调位置”

- 任务归属：
  - `T-P0-08` 正式补口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorkbenchCraftingOverlay.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 本次补口内容：
  - 新增工作台浮层顶部 `DragHandle`
  - 新增 `PanelDragHandle`
  - 新增运行时 `HandleManualPanelDrag / ApplyManualOffsetDelta / ResetManualPanelOffset`
  - 浮层位置从“纯自动锚定”升级为“自动锚定 + 用户拖拽微调偏移”
  - 切换承载物或会话重置时清空手调偏移
- 当前状态：
  - 代码已写完
  - 尚未跑 Unity live 验拖拽手感

## E-004｜工作台制作反馈补“正式工作态 / 导演态 / 完成态”

- 任务归属：
  - `T-P0-09` 正式补口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorkbenchCraftingOverlay.cs`
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 本次补口内容：
  - 工作台制作中不再只剩一个填充条
  - 浮层内新增“旅人正在工作”的正式提示文案
  - 制作中会驱动玩家进入明确工作动作姿态，并继续驱动工作台 `Animator` 工作态
  - 离开浮层后保留的小进度条改为显示“配方名 + 件数/百分比”，不再只剩孤立数字
  - 导演层新增 `NotifyWorkbenchCraftProgress`，任务卡 / 任务页能同步显示“工作台制作中”
  - 制作失败或中断时会给出正式失败反馈，不再静默断掉
  - 完成后反馈区分“单次完成 / 多次完成 / 部分完成后中断”
- 当前状态：
  - formal-face 已写完
  - 尚未跑 Unity live 验“玩家工作动作 + 浮层/任务卡同步体感”

## E-005｜任务卡签名防抖修正

- 任务归属：
  - `T-P0-09` 衍生稳定性补口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 工作台制作进度进入导演层后，任务卡若继续把实时百分比纳入唯一签名，可能导致高频状态重算甚至抖动
- 本次补口内容：
  - `PromptOverlay` 现在区分：
    - 结构签名
    - 实时显示签名
  - 只有阶段/行结构变化才走 `TransitionToPendingState`
  - 实时数值变化改走 `ApplyPendingStateWithoutTransition` 原位刷新
- 当前状态：
  - 已完成文本级稳定性补口
  - 尚未跑 Unity live 看任务卡在制作中是否完全无抖动

## E-006｜首段对话自动 follow-up 与疗伤后置收口

- 任务归属：
  - `T-P0-01 / T-P0-02 / T-P0-03`
- 已完成修改：
  - `Assets/YYY_Scripts/Story/Managers/DialogueManager.cs`
  - `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - `SpringDay1_FirstDialogue.asset` 已配置 `followupSequence`，但现有代码没有把“首段完成后自动接 follow-up”正式接上；
  - 导演层在 `spring-day1-first` 完成后会立即启动疗伤，顺序会抢到 follow-up 前面；
  - 连续对话场景下，旧 `DialogueEndEvent` 还会让 NPC/UI 提前恢复。
- 本次补口内容：
  - `DialogueManager` 新增 `ResolveFollowupSequence`，对资产上的 `followupSequence` 做正式自动续播
  - `SpringDay1Director` 显式识别 `spring-day1-first-followup`，把疗伤推迟到 follow-up 收束之后
  - `EnterVillage` 相位变化改为先看当前是否仍在连续对话占用中，不再抢跑疗伤
  - `NPCDialogueInteractable / PromptOverlay / StatusOverlay / WorldHintBubble` 补 `ShouldIgnoreDialogueEndEvent()`，连续对话时忽略旧 End 事件
  - `SpringDay1DialogueProgressionTests.cs` 新增：
    - follow-up 自动续播锚点
    - 疗伤后置锚点
    - HP/EP 节奏锚点
- 当前状态：
  - formal-face 已补完
  - 文本级自检通过
  - 仍未进 Unity live 复跑这条链

## E-007｜运行态验收入口与导演文案对齐

- 任务归属：
  - `T-P0-03 / T-P0-06` 衍生 formal-face 补口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 首段 follow-up 链收紧后，导演层与运行态验收入口仍存在几处旧口径：
    - `EnterVillage` 任务标签仍像首段本体
    - `WorkbenchFlashback` 在工作台已打开后仍可能建议再次触发交互
    - 快照里还看不到 `follow-up` / `workbench flashback` 这两个关键判位
- 本次补口内容：
  - `EnterVillage` 任务标签改为 `0.0.2 首段后续说明`
  - `WorkbenchFlashback` 进度 / 焦点提示细分为：
    - `等待打开工作台`
    - `工作台回忆进行中`
    - `工作台已打开，等待回忆收束`
  - 新增：
    - `IsFirstFollowupPending()`
    - `IsWorkbenchFlashbackAwaitingInteraction()`
  - `SpringDay1LiveValidationRunner` 的推荐动作 / 单步触发不再在工作台已打开后反向再次触发交互
  - `BuildSnapshot()` 追加：
    - `followupPending=`
    - `workbenchAwaiting=`
- 当前状态：
  - 文本级对齐已完成
  - 尚未在 Unity live 快照里实际读取这两个新旗标

## E-008｜后半链验收旗标补记与自由时段文案收紧

- 任务归属：
  - `T-P1-01 / T-P1-02 / T-P1-04 / T-P1-05` 的 non-hot formal-face 预收口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 运行态验收入口已经补上 `followupPending / workbenchAwaiting`，但晚餐、归途提醒和自由时段仍缺少可直接读的阶段判位；
  - 高速模式下如果不把这些后半链阶段也补成显式旗标，后续 live 复跑仍会难以区分“对白进行中”“已排队待接管”“已经允许睡觉收束”。
- 本次补口内容：
  - `SpringDay1Director` 追加：
    - `IsDinnerDialoguePendingStart()`
    - `IsReminderDialoguePendingStart()`
    - `IsSleepInteractionAvailable()`
  - `BuildSnapshot()` 继续扩展：
    - `dinnerPending=`
    - `reminderPending=`
    - `sleepReady=`
  - 导演层用户可见文案继续细分：
    - `晚餐对白进行中`
    - `等待晚餐对白接管`
    - `归途提醒对白进行中`
    - `等待归途提醒对白接管`
    - `现在可以自由活动，也可以直接回住处睡觉。`
- 当前状态：
  - formal-face 已补完
  - 已进入 live 快照窗口实读
  - 但后半链本体仍未在这轮一路跑到晚餐/提醒/睡觉

## E-009｜工作台 live 验收入口补“脚本交互兜底”

- 任务归属：
  - `T-P0-06 / T-P0-11` 的验收入口继续收口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 最小 live 纵切已经能推进到 `WorkbenchFlashback`
  - 但 `Step Spring Day1 Validation` 在工作台阶段会因为玩家没被移动到交互包络线内而卡住
- 本次补口内容：
  - `SpringDay1LiveValidationRunner.TryTriggerWorkbenchInteraction()` 现在在：
    - 当前阶段为 `WorkbenchFlashback`
    - 且 `workbenchAwaiting=true`
    - 但玩家不在工作台交互包络线内
    时，允许走一次“只服务验收入口”的脚本交互兜底
  - 真实游戏交互距离与 `1.5m` 自动关闭规则没有修改
- 当前状态：
  - 代码与文本级锚点已补完
  - 已通过结构验证与编译刷新
  - 但还缺一轮更长 live 窗口，把疗伤自动段走完后真正看见工作台回忆被这条兜底接住

## E-010｜no-red 收口：世界提示气泡与 TimeManager 退场不再反向造对象

- 任务归属：
  - 高速模式里的 live / PlayMode 尾场红错清理
- 已完成修改：
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
  - `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`
  - `Assets/YYY_Scripts/Story/Interaction/CraftingStationInteractable.cs`
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 退出 Play 时出现：
    - `Assertion failed on expression: 'go.IsActive()'`
    - `Some objects were not cleaned up when closing the scene.`
  - `Editor.log` 已定位两条 owned 根因：
    - `NPCDialogueInteractable.OnDisable()` 通过 `SpringDay1WorldHintBubble.Instance?.Hide(...)` 反向触发 `EnsureRuntime()`
    - `SpringDay1Director.ReleaseStoryTimePause()` 在 `OnDisable()` 里调用 `TimeManager.Instance`，导致退场时新建 `TimeManager`
- 本次补口内容：
  - `SpringDay1WorldHintBubble` 新增：
    - `HideIfExists()`
  - `NPCDialogueInteractable / CraftingStationInteractable` 的 hide-only 气泡回收全部改成：
    - `HideIfExists(transform)`
  - `SpringDay1Director.ReleaseStoryTimePause()` 改成只恢复“已存在的 TimeManager”，不再为了 `ResumeTime` 新建对象
- 当前状态：
  - 这组 no-red 修复已通过结构验证、编译刷新和最小 `Play -> Stop` 烟雾测试
  - 当前 smoke 结果只剩：
    - `There are no audio listeners in the scene`
    warning
  - 之前的 assert 与 cleanup error 已不再复现

## 3. 测试记录

## T-001｜P0 白名单 preflight

- 状态：已执行，失败但不阻断继续开发
- 结果：
  - formal-face 主通道成立
  - 同根 remaining dirty 阻断 `Service/Player` 与 `YYY_Tests/Editor` 直接收口
- 当前处理：
  - 继续往前写，不因 preflight 失败停工

## T-002｜工作台浮层拖拽微调

- 状态：待测
- 目标：
  - 打开工作台浮层后，可拖动顶部把手做位置微调
  - 拖动后仍保留自动贴边与屏幕边界约束
  - 切换工作台承载物或会话重开后，偏移会正常复位
- 当前阻断：
  - 尚未进入 Unity live 窗口

## T-003｜工作台制作态补口文本级自检

- 状态：已执行，通过
- 范围：
  - `SpringDay1WorkbenchCraftingOverlay.cs`
  - `SpringDay1Director.cs`
  - `SpringDay1PromptOverlay.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `git diff --check` 通过
  - 已确认新增工作态 / 导演态 / 防抖签名相关钩子全部落入目标文件
- 当前备注：
  - 只有 CRLF/LF 警告
  - 仍未进入 Unity live / PlayMode / MCP 实测

## T-004｜首段 follow-up / 疗伤后置文本级自检

- 状态：已执行，通过
- 范围：
  - `DialogueManager.cs`
  - `NPCDialogueInteractable.cs`
  - `SpringDay1Director.cs`
  - `SpringDay1PromptOverlay.cs`
  - `SpringDay1StatusOverlay.cs`
  - `SpringDay1WorldHintBubble.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `git diff --check` 通过
  - 已确认：
    - `ResolveFollowupSequence`
    - `PlayDialogue(followupSequence)`
    - `FirstFollowupSequenceId`
    - `ShouldIgnoreDialogueEndEvent()`
    - `HealingAndEnergyPacing_RemainsBoundToDay1FormalSequence`
    全部落入目标文件
- 当前备注：
  - 仍未进入 Unity live / PlayMode / MCP 复跑 `NPC001 -> follow-up -> 疗伤`

## T-005｜运行态验收入口对齐文本级自检

- 状态：已执行，通过
- 范围：
  - `SpringDay1Director.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `git diff --check` 通过
  - 已确认：
    - `0.0.2 首段后续说明`
    - `工作台回忆进行中`
    - `工作台已打开，等待回忆收束`
    - `等待工作台回忆完整播完。`
    - `followupPending=`
    - `workbenchAwaiting=`
    全部落入目标文件
- 当前备注：
  - 仍未在 Unity live 快照里实际读取这两个新增判位

## T-006｜unityMCP 基线与会话归位检查

- 状态：已执行，通过
- 范围：
  - `check-unity-mcp-baseline.ps1`
  - 当前 Codex 会话 `list_mcp_resources / list_mcp_resource_templates`
  - `mcpforunity://project/info`
  - `mcpforunity://instances`
- 结果：
  - `config.toml` 中只保留 `unityMCP`
  - `127.0.0.1:8888` 监听正常
  - `Library/MCPForUnity/RunState/mcp_http_8888.pid` 存在且与监听 pid 一致
  - 当前会话 resources / templates 暴露的 server 为 `unityMCP`
  - 当前实例匹配：
    - `Sunset@21935cd3ad733705`
    - `Unity 6000.0.62f1`
- 当前备注：
  - `editor_state` 资源持续给出 `stale_status`
  - 但工具执行链本身可用，因此当前应归类为“资源状态陈旧噪音”，不是 `unityMCP` 服务未起

## T-007｜Day1 owned 脚本结构验证

- 状态：已执行，通过
- 范围：
  - `DialogueManager.cs`
  - `NPCDialogueInteractable.cs`
  - `SpringDay1Director.cs`
  - `SpringDay1PromptOverlay.cs`
  - `SpringDay1StatusOverlay.cs`
  - `SpringDay1WorkbenchCraftingOverlay.cs`
  - `SpringDay1WorldHintBubble.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `validate_script` 全部 `0 errors`
  - warning 仅为已有性能提示：
    - `GameObject.Find in Update()`
    - `String concatenation in Update()`
- 当前备注：
  - 当前没有我这轮 owned C# 结构性红错
  - warning 继续按 warning 报实，不混说成 compile blocker

## T-008｜Day1 最小 live 验证：Bootstrap / Snapshot / Step

- 状态：已执行，部分通过且形成下一步阻断信息
- 进入前现场：
  - MCP 基线已通过
  - 当前实例为 `Sunset@21935cd3ad733705`
  - 初始 `editor_state` 显示卡在 `Primary.unity / playmode_transition`
  - 先执行一次 `stop`，把 Editor 退回 `Edit Mode`
- 本轮 live 动作：
  1. 清空 Console
  2. 进入 Play Mode
  3. 执行 `Sunset/Story/Debug/Bootstrap Spring Day1 Validation`
  4. 执行 `Log Spring Day1 Validation Snapshot`
  5. 执行有限次 `Step Spring Day1 Validation`
  6. 拿到足够证据后退出 Play 取证
- 结果：
  - `SpringDay1LiveValidationRunner` 能在 live 中成功 bootstrap
  - live 快照已真实输出：
    - `followupPending=`
    - `workbenchAwaiting=`
    - `dinnerPending=`
    - `reminderPending=`
    - `sleepReady=`
  - 首次 `Step` 已实际触发：
    - `NPC001` 首段对话
    - `Dialogue=spring-day1-first[1/3]|typing=True`
    - `NPC roam=False|state=Inactive`
    - `Input=False`
  - 继续有限次 `Step` 后，流程已自动推进到：
    - `Phase=WorkbenchFlashback`
    - `Decoded=True`
    - `HP=85/100|visible=True`
    - `Director=0.0.4 工作台闪回|等待打开工作台|followupPending=False|workbenchAwaiting=True|dinnerPending=False|reminderPending=False|sleepReady=False`
  - 这说明：
    - `NPC001 -> 首段 -> follow-up -> 疗伤 -> 工作台闪回`
      至少已经能推进到 `WorkbenchFlashback` 前缘，不再卡死在首段后
- 当前阻断：
  - 自动 `Step` 继续推进时，出现：
    - `工作台 Anvil_0 当前不在交互包络线内。`
  - 因此这轮最小 live 仍停在“已到工作台闪回入口，但还未自动完成工作台交互”
- 额外噪音：
  - 本轮 Play 期间混入了 `NavigationLiveValidationRunner` 的自动日志心跳
  - 当前按 external runtime noise 报实，不并入 Day1 owned 结果
- 当前备注：
  - 本轮 live 没有新增项目级 error
  - 明确可继续追的下一步是：
    - `T-P0-06 / T-P0-11` 相关的工作台交互距离 / 验收入口继续收口
  - 仍未在这轮进入：
    - `Primary.unity` 持久写
    - `DialogueChinese*`
    - 其他 hot/mixed 面

## T-009｜工作台验收入口兜底代码闸门

- 状态：已执行，通过
- 范围：
  - `SpringDay1Director.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `validate_script` 通过，`0 errors`
  - `git diff --check` 通过
  - 编译刷新后 Console 无新增 error
- 当前备注：
  - 当前只缺更长 live 窗口去验证这条兜底真正接住了 `WorkbenchFlashback`

## T-010｜no-red Play 退场烟雾验证

- 状态：已执行，通过
- 范围：
  - `SpringDay1WorldHintBubble.cs`
  - `NPCDialogueInteractable.cs`
  - `CraftingStationInteractable.cs`
  - `SpringDay1Director.cs`
  - `SpringDay1DialogueProgressionTests.cs`
- 结果：
  - `Editor.log` 已定位之前的两条 owned 根因：
    - `WorldHintBubble.Instance?.Hide(...)` 反向创建 UI
    - `TimeManager.Instance` 在退场时反向创建 `TimeManager`
  - 修复后重新执行：
    - 编译刷新
    - `Play -> Stop`
  - 当前 Console 仅剩：
    - `There are no audio listeners in the scene`
    warning
  - 之前的：
    - `go.IsActive()` assert
    - `Some objects were not cleaned up when closing the scene`
    已不再复现
- 当前备注：
  - 这轮 owned no-red 尾场已收干净

## 4. 待测队列

1. `SpringDay1WorkbenchCraftingOverlay` 拖拽微调是否手感稳定
2. 工作台制作中：
   - 玩家工作动作
   - 工作台动画
   - 浮层内进度
   - 任务卡进度
   - 离台小进度条
   是否保持同一状态语义
3. `NPC001 -> 首段对话 -> 解码 -> 疗伤 -> 工作台闪回`
   - 已证明可推进到 `WorkbenchFlashback`
   - 待补“工作台交互距离 / 自动验收入口”后继续验证
4. `HealthSystem / EnergySystem` 改动面是否需要与 `PlayerInteraction / PlayerThoughtBubblePresenter` 同批收口
5. `P0` 进入 `Primary.unity` 热窗前，是否仍有可继续前推的非热代码面
6. 连续对话场景下：
   - NPC 漫游
   - PromptOverlay
   - 状态条
   - 世界提示气泡
   是否都不会因旧 `DialogueEndEvent` 提前恢复
7. `SpringDay1LiveValidationRunner.BuildSnapshot()` 已确认输出：
   - `followupPending=`
   - `workbenchAwaiting=`
   - `dinnerPending=`
   - `reminderPending=`
   - `sleepReady=`
   后续待继续在晚餐/提醒/自由时段逐段核对应值
8. `Step Spring Day1 Validation` 能否在不碰 `Primary.unity` 持久面的前提下，继续覆盖工作台交互入口
9. 若要继续实证工作台脚本交互兜底，需要给疗伤自动段一个更长但仍受控的 live 窗口

## 5. 下一步直推口径

- 优先继续：
  - `T-P0-06 / T-P0-11`
    - 工作台交互距离
    - 工作台闪回入口
    - 验收入口继续推进能力
    - 当前已有脚本兜底，后续优先补 live 实证
  - `T-P0-04 / T-P0-05`
    - 若还能继续补非热正式面，就把 HP / EP 体验锚点再压实
  - `T-P1-01 ~ T-P1-05`
    - 继续只补 formal-face 验收锚点与导演层清晰度，不抢进 scene 热窗
- 暂不现在碰：
  - `Primary.unity`
  - `DialogueChinese*`
  - 木材 bypass 那组 hot/mixed 兼容面

## T-011｜工作台闪回兜底 live 实证与 Console 分诊

- 状态：已执行，通过
- 范围：
  - `SpringDay1Director.cs`
  - `DialogueDebugMenu.cs`
  - `SpringDay1DialogueProgressionTests.cs`
  - `unityMCP` live 验收入口
- live 边界：
  - 只跑 `1` 轮
  - 流程固定为：
    - `Edit Mode -> compile refresh -> clear console -> Play -> Bootstrap -> Snapshot -> 有限次 Step -> Stop`
  - 目标只验证：
    - 疗伤自动段收束后，工作台脚本兜底是否真的接住 `WorkbenchFlashback`
  - 不扩到：
    - `Primary.unity` 持久写
    - `DialogueChinese*` 资产处理
    - 其他 hot / mixed 面
- 结果：
  - `Bootstrap` 正常起效：
    - `Phase=CrashAndMeet`
    - `Director=0.0.2 首段推进链|首段对话进行中`
  - 有限次 `Step` 后，live 先后真实推进到：
    - `Dialogue=spring-day1-first[1/3]|typing=True`
    - `Phase=HealingAndHP`
    - `Decoded=True`
    - `HP=85/100|visible=True`
    - `Phase=WorkbenchFlashback`
    - `workbenchAwaiting=True`
  - 在 `WorkbenchFlashback` 再执行一次 `Step` 后，Console 明确输出：
    - `工作台 Anvil_0 当前不在交互包络线内，已通过验收入口脚本触发工作台回忆。`
  - 同步快照确认：
    - `Dialogue=spring-day1-workbench[1/2]|typing=True`
    - `Director=0.0.4 工作台闪回|工作台回忆进行中`
    - `workbenchAwaiting=False`
  - 这说明：
    - `T-P0-06` 当前 non-hot 自有刀面已经具备真实 live 实证
    - 工作台验收入口兜底不是只存在于代码文本里，而是能在运行态真正接住 Day1 主链
- Console 分诊：
  - Play 期间曾出现一条：
    - `Importer(NativeFormatImporter) generated inconsistent result ... "DialogueChinese BitmapSong SDF.asset"`
  - 该项不在本轮 own 范围，且属于用户明确要求不扩认的字体资产族
  - 处理方式：
    - 先记证
    - Stop 回到 `Edit Mode`
    - clear console
    - 空闲复核 `error / warning`
  - 复核结果：
    - `0 error`
    - `0 warning`
  - 当前按：
    - 外来瞬时噪音 / 非本轮 own 面
    报实，不并入 Day1 owned red
- 当前备注：
  - 本轮 live 结束后 Unity 已回到 `Edit Mode`
  - 当前 Console 已归零
  - 当前最自然的下一步是：
    - 把本次实证回填到任务列表状态
    - 若继续业务推进，优先看 `T-P0-05` 和 `T-P1-*` 仍缺 live 体验证据的 formal-face 面

## T-012｜撤掉自定义 HP / EP 面并重接回原始 slider

- 状态：已执行，通过
- 背景：
  - 用户明确裁定：
    - 不再使用我后来补出来的那对 HP / EP 自定义面
    - 改回项目里最初已经做好的那两条
    - 相关自定义内容彻底删除，并做好逻辑善后和重接
- 处理范围：
  - `Assets/YYY_Scripts/Service/Player/HealthSystem.cs`
  - `Assets/YYY_Scripts/Service/Player/EnergySystem.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - 删除：
    - `Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs`
    - `Assets/YYY_Scripts/Story/UI/SpringDay1StatusOverlay.cs.meta`
- 实际处理：
  - `HealthSystem`
    - 不再创建/依赖 `SpringDay1StatusOverlay`
    - 重新接回 `UI/State/HP` 原 slider
    - reveal / heal 动画改回直接驱动原 slider 与其 `CanvasGroup`
  - `EnergySystem`
    - 不再创建/依赖 `SpringDay1StatusOverlay`
    - 重新接回 `UI/State/EP` 原 slider
    - reveal / restore / low-energy fill pulse 改回直接作用于原 slider 与其 fill image
  - 测试
    - 去掉对 `SpringDay1StatusOverlay` 文件本体的依赖断言
    - 保留 Day1 其余逻辑与 live 验收入口相关断言
- 现场复核：
  - 场景内原始对象仍在：
    - `UI/State/HP`
    - `UI/State/EP`
  - `SpringDay1StatusOverlay` 场景内搜索结果：
    - `0`
  - 代码搜索：
    - 已无 `SpringDay1StatusOverlay / statusOverlay / SetSectionVisible / SetSectionAlpha / SetFillColor` 残引用
- 验证：
  - `validate_script`
    - `HealthSystem.cs`：`0 errors`
    - `EnergySystem.cs`：`0 errors`（仅保留既有性能 warning）
    - `SpringDay1DialogueProgressionTests.cs`：`0 errors`
  - `git diff --check` 通过
  - compile refresh 后：
    - `0 error`
    - `0 warning`
  - 最小 smoke：
    - `Bootstrap -> Step* -> HealingAndHP -> WorkbenchFlashback`
    - 期间无新增 error
    - live 快照已再次证明：
      - `HP=85/100|visible=True`
      - `EP=150/150|visible=False|warn=False`
      在删掉自定义面后仍能正常推进 Day1 主链
- 当前备注：
  - 这轮只回拉了我自己补的那套 HP / EP 自定义视觉层
  - 没有回滚：
    - Day1 对话链
    - 工作台验收入口
    - 后半链导演态
    - 其他非热 formal-face 推进

## T-013｜失焦 live 稳定化与 `T-P0-05` 运行态硬证补齐

- 状态：已执行，通过
- 范围：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `unityMCP` live 验收入口
  - `2026-03-27_Day1全盘详细落地任务列表.md`
- 触发原因：
  - 继续追 `T-P0-05` 时，Day1 own live 暴露出两个真实阻断：
    - Editor 失焦后，疗伤 / 工作台这类自动段不继续跑帧
    - 工作台回忆排队播对白时，`DialogueManager.Instance` 短暂掉空会在 `SpringDay1Director.cs` 打 `NullReferenceException`
- 实际处理：
  - `SpringDay1LiveValidationRunner`
    - `BootstrapRuntime()` 现会临时接管 `Application.runInBackground`
    - 在 runner 销毁时恢复原始后台运行设置
    - 这样 Day1 自己的 live 验收入口在失焦编辑器下也能继续推进自动段
  - `SpringDay1Director`
    - `PlayDialogueWhenReady()` 改成逐帧重新解析 `DialogueManager`
    - 只有在 manager 存在且当前空闲时才真正 `PlayDialogue(sequence)`
    - 消除了工作台回忆队列的空引用
  - `SpringDay1DialogueProgressionTests`
    - 补进上述两条 live 稳定化文本防回归锚点
  - `2026-03-27_Day1全盘详细落地任务列表.md`
    - 已把 `T-P0-05` 从 `in-progress` 回填为 `done`
- live 结果：
  - 修正后重新跑受控窗口：
    - `Play -> Bootstrap -> Step* -> Stop`
  - 当前已真实拿到 Day1 own 运行态证据：
    - 疗伤段：
      - `HP=85/100|visible=True`
    - 工作台闪回：
      - `Dialogue=spring-day1-workbench[1/2]|typing=True`
      - 未再出现 `SpringDay1Director.cs` 的 workbench queue `NullReferenceException`
    - farming 第一步：
      - `EP=80/200|visible=True|warn=False`
    - farming 第二步：
      - `EP=45/200|visible=True|warn=False`
    - farming 第三步：
      - `EP=20/200|visible=True|warn=True`
      - `Move=runtimeMultiplier=0.80`
  - 这说明：
    - `T-P0-05` 的三件事现在不只是代码锚点，而是已经具备运行态硬证：
      - EP 首次出现
      - low-energy warning 生效
      - 低精力移速惩罚生效
- no-red 分诊：
  - `validate_script`
    - `SpringDay1Director.cs`：`0 errors`（仅既有性能 warning）
    - `SpringDay1DialogueProgressionTests.cs`：`0 errors`
  - `git diff --check` 通过
  - compile refresh 后：
    - 未见 owned error
  - live 尾场中出现过 `There are no audio listeners in the scene` warning
    - 已按 warning 报实，不冒充 owned red
    - 最终 explicit `Stop` + `clear console` 后，当前 Console 为：
      - `0 error`
      - `0 warning`
- 当前备注：
  - `T-P0-05` 现在可按 `done` 处理
  - 本轮白名单 `preflight` 已补跑，但当前不能直接 sync：
    - `Assets/YYY_Scripts/Service/Player` own root 下还有未纳入本轮白名单的 remaining dirty
    - 当前脚本明确点名：
      - `PlayerAutoNavigator.cs`
      - `PlayerInteraction.cs`
      - `PlayerNpcNearbyFeedbackService.cs`
      - `PlayerNpcRelationshipService.cs`
      - `PlayerThoughtBubblePresenter.cs`
  - `T-P0-11` 仍不能 claim done：
    - 它的正式面还牵涉 `PlayerInteraction / PlayerThoughtBubblePresenter` 等 hot / mixed 文件
  - 当前仍值得继续推进的非热主线：
    - `T-P1-01 / T-P1-04 / T-P1-05`
    - 以及在不碰 hot 文件前提下还能继续 formalize 的 Day1 后半链

## E-014｜后半链晚餐入口 low-energy 状态重同步补口

- 任务归属：
  - `T-P1-01 / T-P1-04 / T-P1-05` 的一致性补口
- 已完成修改：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
- 触发原因：
  - 上一轮 live 已真实推进到 `DinnerConflict`
  - 但晚餐入口快照出现：
    - `EP=20/200|warn=True`
    - `Move=runtimeMultiplier=1.00`
  - 这说明 low-energy warning 还在，但移速惩罚在 phase 切换时出现了状态脱节
- 本次补口内容：
  - `SpringDay1Director.HandleStoryPhaseChanged(...)`
    - 现在会在剧情阶段切换时主动执行 `ResyncLowEnergyState(false)`
  - `HandleEnergyChanged(...)`
    - 收口为统一走 `ResyncLowEnergyState(true)`
  - 新增 `ResyncLowEnergyState(bool allowPrompt)`
    - 直接基于 `EnergySystem.CurrentEnergy` 与 `_staminaRevealed` 重新计算：
      - low-energy warning 视觉
      - 低精力移速惩罚
    - phase 切换时只做状态重对齐，不重复弹 warning 提示
  - `SpringDay1DialogueProgressionTests.cs`
    - 已补进上述 phase 切换重同步的文本防回归锚点
- 当前状态：
  - 这刀 formal-face 已落地
  - 当前是在不碰 `Primary.unity / GameInputManager.cs / PlayerInteraction.cs / PlayerThoughtBubblePresenter.cs` 的前提下，对 Day1 自己的后半链状态一致性做最小补口

## T-014｜晚餐入口 low-energy 状态重同步验证

- 状态：部分通过，live 复证未完全收束
- 范围：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `unityMCP` 短窗口 live 复跑
- 结果：
  - `git diff --check` 通过
  - `validate_script`
    - `SpringDay1Director.cs`：`0 errors`（仅既有性能 warning）
    - `SpringDay1DialogueProgressionTests.cs`：`0 errors`
  - 短窗口 live 第一段已确认：
    - `Bootstrap` 正常
    - `Step` 能正常触发 `NPC001` 首段对话
  - 但在继续追到 `DinnerConflict` 的过程中，出现了两类工具级噪音：
    - `unityMCP` 一次 `ExecuteMenuItem` 返回 `plugin session ... disconnected while awaiting command_result`
    - 随后同轮 `console` / 菜单日志回读表现不稳定，未能可靠拿到晚餐入口的新快照
- 当前处理：
  - 按工具级会话噪音报实，不外推成 Day1 own 编译红或逻辑红
  - 已显式 `Stop` 回 `Edit Mode`
  - 本轮不继续硬追 live，先保留 formal-face 补丁与代码闸门结果
- 当前备注：
  - 这刀已经把最直接的 formal-face 根因补上：
    - 不再只靠 `OnEnergyChanged`
    - 而是 phase 切换也会重新对齐 low-energy warning 与移速惩罚
  - 下一次若 `unityMCP` 菜单会话稳定，应优先只复验：
    - `DinnerConflict`
    - `EP=20/200|warn=True`
    - `Move=runtimeMultiplier=0.80`
    这组晚餐入口快照

## E-015｜账本纠偏与 `UiLayerUtility` 公开访问级防回归

- 任务归属：
  - 高速模式下的 formal-face 账本修正 / no-red 护栏
- 已完成修改：
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `2026-03-27_Day1全盘详细落地任务列表.md`
  - `2026-03-27_Day1高速执行日志与测试排队.md`
- 本次补口内容：
  - 为 `SpringDay1UiLayerUtility` 新增 `public static class` 访问级别的文本防回归锚点，避免 `NPCInformalChatValidationMenu.cs` 这类 Editor 验证菜单再次因访问级别回退而编译阻断
  - 把当前账本 / 高速日志里的现役文件闭包纠正到真实现场：
    - 移除已删除的 `SpringDay1StatusOverlay.cs`
    - `T-P0-04 / T-P0-05` 改写为经 `HealthSystem.cs / EnergySystem.cs` 间接承载原始 `UI/State/HP`、`UI/State/EP`
  - 把 `T-P1-02` 当前状态从 `pending` 回填为 `in-progress`
- 当前状态：
  - 当前执行标准已与真实代码面重新对齐，不再把用户已否掉的自定义 HP / EP 面误记成现役文件

## T-015｜`nightPressure` 短 live 复证：Bootstrap 可用，但 Step 批推进会中途掉出 PlayMode

- 状态：部分通过，当前按工具 blocker 记账
- 进入前现场：
  - 已显式 `Stop -> Clear Console`
  - `validate_script`：
    - `SpringDay1Director.cs` `0 errors`
    - `SpringDay1BedInteractable.cs` `0 errors`
    - `SpringDay1UiLayerUtility.cs` `0 errors`
    - `SpringDay1DialogueProgressionTests.cs` `0 errors`
  - compile refresh 后 Console `0 error`
- live 边界：
  - 目标只验证：
    - `Bootstrap` 是否仍可用
    - 能否继续推进到 `FreeTime`
    - `nightPressure=night / midnight / final-call`
    - 两点规则收束
  - 不扩到：
    - `Primary.unity`
    - `GameInputManager.cs`
    - `PlayerInteraction.cs`
    - `PlayerThoughtBubblePresenter.cs`
- 结果：
  - `Bootstrap Spring Day1 Validation` 可稳定打出快照：
    - `Scene=Primary`
    - `Phase=CrashAndMeet`
    - `Director=0.0.2 首段推进链`
    - `nightPressure=inactive`
  - 第一批 `Step` 能真实推进首段对白：
    - 已推进到 `spring-day1-first[3/3]|typing=False`
    - 说明菜单入口不是完全失效，Day1 自己的验证 runner 仍能起跑
  - 但第二批 `Step + Snapshot` 随后直接打出：
    - `请先进入 PlayMode 再执行 spring-day1 验收步骤。`
    - `请先进入 PlayMode 再记录 spring-day1 验收快照。`
  - 同轮还混入 `PlayerAutoNavigator` 的导航卡顿 warning；当前按外部噪音报实，不并入 Day1 own 结论
- 当前判断：
  - 当前更准确的 live blocker 已经不是“`unityMCP` 完全不可用”，而是：
    - `Bootstrap` 可用
    - 但批推进过程中 Play 状态会中途漂掉，导致无法稳定追到 `FreeTime`
  - `Stop -> Clear` 后若继续做 MCP 状态 / Console 读回，还会重新出现：
    - `PlayerAutoNavigator` 导航卡顿 warning
    - `GameObjectSerializer` playback 相关 error
    当前按外部 / MCP 回读噪音报实，不并入 Day1 own compile red
  - 因此这轮尚未拿到：
    - `nightPressure=night`
    - `nightPressure=midnight`
    - `nightPressure=final-call`
    - 两点规则收束快照
- 收尾状态：
  - 已显式 `Stop`
  - Unity 当前回到 `Edit Mode`
  - Console 已清空
- 下一步：
  - 若后续继续追 `nightPressure`，应改用：
    - `Play -> Bootstrap -> 小批量 Step -> 立刻 Snapshot`
    - 不再用长批量菜单推进
  - 在工具窗口恢复稳定前，本轮不再把 `nightPressure` live 缺证误判成 Day1 逻辑回退

## E-016｜后半链夜压锚点补记与任务账本回填

- 任务归属：
  - `T-P1-02 / T-P1-03 / T-P1-04 / T-P1-05` 的防回归与账本对齐
- 已完成修改：
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `2026-03-27_Day1全盘详细落地任务列表.md`
  - `2026-03-27_Day1高速执行日志与测试排队.md`
- 本次补口内容：
  - 在 Day1 文本防回归测试里补进：
    - `已模拟推进到夜里 10 点`
    - `已模拟推进到午夜`
    - `已模拟推进到凌晨一点`
    - `nightPressure=final-call`
    - `验收入口：已模拟两点规则触发，Day1 应进入结束态。`
  - 把任务账本回填到当前真实现场：
    - `T-P1-02` -> `done`
    - `T-P1-03` -> `done`
    - `T-P1-04 / T-P1-05` 继续 `in-progress`
    - 当前未闭环点明确收缩为：
      - `两点规则 -> DayEnd` 最后一拍的运行态证据
- 当前状态：
  - 当前后半链账本、测试锚点与 live 证据已重新对齐，不再停留在“只有 `nightPressure=` 这个宽泛字段”的口径

## T-016｜后半链短 live 复证：Dinner -> Reminder -> FreeTime -> night / midnight / final-call

- 状态：部分通过，`final-call` 已取证，`两点规则 -> DayEnd` 尚缺最后一拍
- 范围：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Scripts/Story/Interaction/SpringDay1BedInteractable.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `unityMCP` 短窗口 live 验收入口
- live 边界：
  - 继续坚持：
    - `Play -> Bootstrap -> 小批量 Step -> 立刻 Snapshot`
  - 目标只验证：
    - `DinnerConflict`
    - 晚餐回血
    - `ReturnAndReminder`
    - `FreeTime`
    - `night / midnight / final-call`
    - `sleepReady`
  - 不扩到：
    - `Primary.unity`
    - `GameInputManager.cs`
    - `PlayerInteraction.cs`
    - `PlayerThoughtBubblePresenter.cs`
    - `DialogueChinese*` 持久改写
- 结果：
  - `DinnerConflict` 已真实落点为：
    - `Dialogue=spring-day1-dinner[2/3]|typing=True`
    - `EP=20/200|warn=True`
    - `Move=runtimeMultiplier=0.80`
  - 晚餐链继续推进后，`ReturnAndReminder` 已真实出现：
    - `Dialogue=spring-day1-reminder[2/2]|typing=False`
    - `EP=50/200|warn=False`
    - `Move=runtimeMultiplier=1.00`
  - 提醒收束后已进入 `FreeTime`：
    - `sleepReady=True`
    - `nightPressure=settled`
  - 自由时段短批推进已真实拿到：
    - `22:00 -> nightPressure=night`
    - `00:00 -> nightPressure=midnight`
  - `Editor.log` 已补到最后一段硬证：
    - `验收入口：已模拟推进到凌晨一点，最终催促应已触发。`
    - `01:00 AM`
    - `nightPressure=final-call`
  - 因此这轮已把：
    - 晚餐进场
    - 晚餐回血
    - 归途提醒
    - 自由时段开放
    - 夜里 10 点
    - 午夜
    - 凌晨一点最终催促
    这整条后半链核心状态全部补到运行态证据
- 当前 blocker：
  - 在 `final-call` 证据之后，Play 因：
    - `Importer(NativeFormatImporter) generated inconsistent result ... "DialogueChinese V2 SDF.asset"`
    触发 domain reload / 退出
  - 因此 `两点规则触发 -> DayEnd` 这最后一拍，当前仍缺运行态快照
- 额外噪音：
  - Play 期间持续混入：
    - `NPCValidation 002` 闭环 trace timeout warning
  - 当前按外部线程噪音报实，不并入 Day1 own 逻辑结论
- 当前备注：
  - 这轮最重要的新结论是：
    - `T-P1-02 / T-P1-03` 已具备真实运行态证据，可回填为 `done`
    - `T-P1-04 / T-P1-05` 已不再缺 `night / midnight / final-call / sleepReady`
    - 当前只剩 `两点规则 -> DayEnd` 的最终收束未拿到
  - 当前 Unity 已回到 `Edit Mode`

## E-017｜DayEnd 正式桥文本锚点补强与 blocker 重新分类

- 任务归属：
  - `T-P1-05` 的 no-red 正式面补强 / 任务状态重分类
- 已完成修改：
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `2026-03-27_Day1全盘详细落地任务列表.md`
- 本次补口内容：
  - 给 `DayEnd` 正式收束链补进文本防回归锚点：
    - `StoryManager.Instance.SetPhase(StoryPhase.DayEnd);`
    - `EnergySystem.Instance.FullRestore();`
    - `ApplyLowEnergyMovementPenalty(false);`
    - `SpringDay1PromptOverlay.Instance.Show("春1日结束。明天继续。")`
  - 把任务状态重新校准到当前真实现场：
    - `T-P1-04` -> `done`
    - `T-P1-05` -> `blocked-external`
- 当前状态：
  - 现在 Day1 自己的 `DayEnd` 正式桥代码面已经钉死；
  - 当前缺的是最终运行态快照，不是这条链还没写

## T-017｜`DayEnd` 最后一拍复证：当前被外部 live 现场双重打断

- 状态：未闭环，当前按 `blocked-external`
- 范围：
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
  - `Assets/YYY_Scripts/Story/Interaction/SpringDay1BedInteractable.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `unityMCP` / 当前 shared Play 现场
- 本轮附加复证结果：
  - 新一轮 `Play -> Bootstrap -> 连续 Step` 没能把 `DayEnd` 稳定打出来；
  - `Editor.log` 这次再次确认了两类外部现场干扰同时存在：
    - `DialogueChinese V2 SDF.asset` importer inconsistent result / domain reload
    - `NavigationLiveValidationRunner` 自动 `runtime_launch_request=RunRealInputSingleNpcNear`
  - 当前最新现场不是 Day1 自己又回红，而是：
    - Play 会在 shared live 噪音里被拉离稳定窗口
    - `read_console` 也会在会话未 ready 时失去可靠性
- 当前备注：
  - 截至这轮，Day1 自己能直接补的内容已经继续补完：
    - 后半链主体 live 证据
    - `DayEnd` 正式桥文本锚点
    - 任务状态重分类
  - 当前唯一剩余主线缺口，已经明确只剩：
    - `final-call -> 两点规则 -> DayEnd`
      运行态快照

## E-018｜`DayEnd` 末段运行时测试补强与 no-red 清红

- 任务归属：
  - `T-P1-05` 当前在 live 外还能直接补的 formal-face / no-red 护栏
- 已完成修改：
  - `Assets/YYY_Tests/Editor/SpringDay1LateDayRuntimeTests.cs`
- 本次补口内容：
  - 新增 2 条反射式 EditMode 运行时测试：
    - `FreeTimeValidationStep_AdvancesFromFinalCallToDayEnd`
    - `BedBridge_EndsDayAndRestoresSystems`
  - 测试重点不是再做文本锚点，而是把以下链条补成可执行证明：
    - `final-call -> 两点规则 -> DayEnd`
    - `床交互 -> 睡眠桥 -> 满精力恢复 -> 低精力减速撤销`
  - 首版测试曾因直接引用项目类型而引入编译红；本轮已改为与现有 Editor 测试一致的反射口径，并重新清红：
    - `validate_script`：`0 errors`
    - `git diff --check`：通过
    - `refresh_unity(request + force + all)` 后 Console：`0 entries`
- 当前状态：
  - 现在 Day1 最后一拍不仅有文本锚点，还有独立的运行时测试承载面；
  - 我这轮新引入的 compile red 已经清掉，没有把 shared root 留红。

## T-018｜最终 live 复追：可重新稳跑到 `DinnerConflict`，但 `Bootstrap` 仍会偶发 MCP 插件短断

- 状态：部分通过，`T-P1-05` 继续 `blocked-external`
- 范围：
  - `unityMCP` / `DialogueDebugMenu` 短窗口验收入口
  - `Assets/YYY_Tests/Editor/SpringDay1LateDayRuntimeTests.cs` 清红后的 shared Editor 现场
- 本轮附加复证结果：
  - 在清空 Console 后重进 Play，本轮一度拿到比旧口径更干净的窗口：
    - 没有再立即出现 `NavigationLiveValidationRunner runtime_launch_request`
    - `Bootstrap Spring Day1 Validation` 成功
    - 批量 `Step` 真实重新推进到：
      - `HealingAndHP`
      - `WorkbenchFlashback`
      - `FarmingTutorial`
      - `DinnerConflict`
    - 并再次打到：
      - `EP=80/200|visible=True`
      - `EP=45/200|visible=True`
      - `EP=20/200|warn=True`
      - `Move=runtimeMultiplier=0.80`
  - 但在重新清编后再发起“最后一轮最短 `Bootstrap` 复追”时，插件级短断再次出现：
    - `Unity plugin session ... disconnected while awaiting command_result`
    - Console 同时出现：
      - `[WebSocket] Unexpected receive error: WebSocket is not initialised`
  - 因而当前最后缺口的最准确定性更新为：
    - Day1 自己可以重新稳跑到 `DinnerConflict`，说明主线并未掉回前半链；
    - 但 `final-call -> DayEnd` 的最后 live 收束，当前仍会被 `unityMCP` 插件短断 / 会话重连打断。
- 收尾状态：
  - 已显式 `Stop`
  - Unity 当前回到 `Edit Mode`
  - 当前没有我 own 的新增 compile red

## T-019｜`PromptOverlay` 运行时空指针已收口，但 `WorkbenchFlashback` live 窗口再次被 shared-root 外部脚本刷新拍断

- 状态：部分通过，`T-P1-05` 继续 `blocked-external`
- 范围：
  - `Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1LateDayRuntimeTests.cs`
  - `unityMCP` / 当前 shared root Play 现场
- 本轮附加复证结果：
  - 新一轮 live 在 `WorkbenchFlashback` 前先撞到一条新的项目级红错：
    - `NullReferenceException`
    - `Assets/YYY_Scripts/Story/UI/SpringDay1PromptOverlay.cs:147`
  - 已最小修成“显示态缓存丢失时自愈首帧重建”，不扩 Day1 业务，不碰 `Primary.unity` / `GameInputManager.cs`：
    - `if (!_hasDisplayedState || _displayedState == null)`
  - 同轮补了对应运行时回归测试：
    - `PromptOverlay_RecoversWhenDisplayedStateCacheIsMissing`
  - 代码闸门结果：
    - `validate_script`
      - `SpringDay1PromptOverlay.cs`：`0 errors`（仅 1 条已有 warning）
      - `SpringDay1LateDayRuntimeTests.cs`：`0 errors`
    - `git diff --check`：通过
  - 清红后重新 `Play -> Bootstrap -> Step`：
    - `Bootstrap Spring Day1 Validation` 成功
    - 链路再次稳定推进到：
      - `CrashAndMeet`
      - `HealingAndHP`
      - `WorkbenchFlashback`
    - `PromptOverlay` 本轮未再复现空指针
  - 但在刚进入 `WorkbenchFlashback` 后，Play 再次被外部现场打断：
    - Console 最后变成：`[DialogueDebugMenu] 请先进入 PlayMode 再执行 spring-day1 验收步骤。`
    - `editor_state` 复核到：
      - `is_playing=false`
      - `external_changes_dirty=true`
    - `Editor.log` 新增更硬的 shared-root 现场证据：
      - `[ScriptCompilation] Requested script compilation because: Assetdatabase observed changes in script compilation related files`
- 当前备注：
  - 这轮已经把新的 own 运行时红错收掉；
  - 当前最后 live 缺口的最新最准确定性，已从“插件短断”继续收紧为：
    - shared root 外部脚本变更触发编译 / 域重载
    - 进而拍断 `WorkbenchFlashback -> ... -> DayEnd` 的 Play 窗口
  - 因而当前没闭环的仍然只有：
    - `final-call -> 两点规则 -> DayEnd`
      运行态快照

## T-020｜用户要求“只剩测试就直接去试”；实测 Day1 窗口会被导航 live 自动抢占

- 状态：`blocked-external`
- 范围：
  - `unityMCP` / 当前 shared root Play 现场
  - `Assets/YYY_Scripts/Service/Navigation/Editor/NavigationLiveValidationMenu.cs`
  - `Assets/YYY_Scripts/Service/Navigation/NavigationLiveValidationRunner.cs`
  - `Assets/Editor/Story/DialogueDebugMenu.cs`
- 本轮附加复证结果：
  - 先等到 shared root 短暂安静：
    - `editor_state.is_playing=false`
    - `external_changes_dirty=false`
    - Console=`0 entries`
  - 然后按用户要求直接重试最终 live：
    - `Play`
    - `Bootstrap Spring Day1 Validation`
  - 但本轮新证据表明，即便外部脚本刷新暂时停下，导航 live 仍会自己在 Play 入口抢占：
    - `[NavValidation] runtime_launch_request=RunRealInputSingleNpcNear`
    - `scenario_queued=RealInputPlayerSingleNpcNear`
    - `scenario_start=RealInputPlayerSingleNpcNear`
    - `scenario_end=RealInputPlayerSingleNpcNear pass=False ...`
  - Day1 的 `Bootstrap` 并不是没执行，而是被挤到了导航 live 收尾之后才落日志：
    - `[DialogueDebugMenu] 已确保 StoryManager / Day1Director / PromptOverlay / HP / EP / Time 运行时对象就位...`
    - `[SpringDay1LiveValidation] Label=bootstrap ...`
  - 但在这之后 Play 立即退出，窗口直接结束：
    - `FarmTileManager OnDisable`
    - `runner_disabled`
    - `runInBackground_restored value=False`
    - `runner_destroyed`
    - `editor_state.is_playing=false`
- 当前备注：
  - 这轮已经完成了“只剩测试就直接去试”；
  - 最新最准 blocker 需要再收紧一层：
    - 不是单纯 Day1 own 逻辑
    - 也不只是 shared-root 外部脚本刷新
    - 而是当前 shared live 被导航验证线程的 pending / auto-launch 占窗，Day1 `Bootstrap` 只能在窗口尾部落下，来不及继续推到 `DayEnd`

## T-021｜最终 live 闭环：`final-call -> DayEnd` 已实机取到，Day1 最后一拍完成

- 状态：`done`
- 范围：
  - `unityMCP` / `DialogueDebugMenu` / 当前 shared root Play 现场
  - `Assets/YYY_Scripts/Story/Managers/SpringDay1Director.cs`
- 本轮附加复证结果：
  - 在用户明确“导航停了”后，先做了一次最小 `scripts refresh + compile request`，把 shared root 外部脚本尾巴清掉：
    - `external_changes_dirty=false`
    - Console=`0 entries`
  - 之后重新 `Play -> Bootstrap -> 连续 Step`，这次 Day1 真实完整推进到：
    - `CrashAndMeet`
    - `HealingAndHP`
    - `WorkbenchFlashback`
    - `FarmingTutorial`
    - `DinnerConflict`
    - `ReturnAndReminder`
    - `FreeTime`
    - `DayEnd`
  - 已拿到最终关键运行态证据：
    - `nightPressure=night`
    - `nightPressure=midnight`
    - `nightPressure=final-call`
    - `验收入口：已模拟两点规则触发，Day1 应进入结束态。`
    - `Label=after-step, Scene=Primary, Phase=DayEnd`
    - `EP=200/200|visible=True|warn=False`
    - `Move=runtimeMultiplier=1.00`
    - `nightPressure=inactive`
    - `clock=Year 1 Spring Day 2 06:00 AM`
    - `Director=0.0.6 睡觉结束|春1日已结束`
  - 取证完成后已显式 `Stop`，Unity 当前回到 `Edit Mode`
- 当前备注：
  - `Day1` 这条线的最后主线缺口已关闭；
  - 本轮 live 中仍再次出现：
    - `Importer(NativeFormatImporter) generated inconsistent result ... DialogueChinese V2 SDF.asset`
  - 但这次它没有阻断 `ReturnAndReminder -> FreeTime -> DayEnd` 闭环，因此当前应改判为：
    - Day1 主线：已完成
    - 字体 importer 错误：残余独立风险 / 需单独治理
