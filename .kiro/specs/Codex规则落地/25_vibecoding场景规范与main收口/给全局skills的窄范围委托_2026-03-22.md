```text
这轮请你不要发散到整套全局体系重构，也不要直接改全局基线文件。

你只做一个窄范围任务：
基于你刚刚已经完成的复核报告，替 Sunset 当前现场输出一份
“sunset-startup-guard 一级告警处理方案”。

你的范围只限于这 4 件事：
1. 判断 `sunset-startup-guard` 当前 `manual-equivalent` 一级告警应该走哪条三选一路径：
   - 修 discovery / 路由
   - 上提为更硬前置
   - 正式降级退出当前告警口径
2. 判断 `skill-trigger-log.md` 现在应该如何强制统一格式，避免继续分叉
3. 判断 `Missed-Skills` 为什么长期形同虚设，以及最小修复动作是什么
4. 判断 `Promote-To-Learning` 接下来最该补哪一类：
   - correction
   - capability-gap

请你不要做的事：
- 不要直接修改 `global-learning-system.md`
- 不要直接修改 `global-learnings.md`
- 不要设计新的第三种 trigger log 格式
- 不要扩成一整套新的 skills 宪法

你只回我下面这些：
- 对 `sunset-startup-guard` 的三选一裁定
- 你的裁定理由
- 对 Sunset 当前现场最小可执行的 3~5 步动作
- 哪些适合我这条治理线程自己做
- 哪些后续再交给你做
- blocker_or_checkpoint
- 一句话摘要
```
