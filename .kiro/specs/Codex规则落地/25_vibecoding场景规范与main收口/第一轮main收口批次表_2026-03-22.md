# 第一轮 main 收口批次表

## 收件来源
- 固定回收卡：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\000全员回执.md`

## 本轮范围
- 本轮已收件线程：
  - 农田
  - `spring-day1`
  - NPC
  - 导航
  - 遮挡检查
  - 全局 skills
- 本轮未纳入：
  - `scene-build`
- 说明：
  - `scene-build` 当前不在 shared root `main` 收口面里，它继续按独立 worktree 线处理。

## 批次裁定

### A组：可直接进入统一 main 收口窗口
- 农田
  - 当前面：`1.0.3` 建档收口 + `InventorySlotUI` shake 收尾
  - 原因：
    - 改动已在 shared root `main`
    - 未触碰高危目标
    - 不需要 Unity / MCP live 写
    - 有真实 checkpoint，不是纯结论文档
  - 收口建议：
    - 作为第一批次优先收
    - 仅白名单：
      - `Assets/YYY_Scripts/UI/Inventory/InventorySlotUI.cs`
      - `.kiro/specs/农田系统/2026.03.16/1.0.3基础UI与交互统一改进/`
      - `.kiro/specs/农田系统/2026.03.16/1.0.2纠正001/memory.md`
      - `.kiro/specs/农田系统/2026.03.16/memory.md`
      - `.kiro/specs/农田系统/memory.md`
      - `.codex/threads/Sunset/农田交互修复V2/memory_0.md`

### B组：可进 main，但需先做载体转换或顺序处理
- `spring-day1`
  - 当前面：`0.0.2` 基础脊柱实现
  - 当前阻塞：
    - 成果还在 `codex/spring-day1-0.0.2-foundation-001`
    - 不是 shared root 现成 dirty，需要后续摘取或回放到 `main`
  - 收口建议：
    - 不与 A 组混提
    - 等第一批 `main` 收净后，再做 branch checkpoint 到 `main` 的定向迁入窗口

- 遮挡检查
  - 当前面：`2.0.0整改设计 + BatchAddOcclusionComponents`
  - 当前阻塞：
    - 成果仍在 `codex/occlusion-audit-001@295e8138`
    - 不是当前 `main` 现场里的直接可提改动
  - 收口建议：
    - 先保留 carrier checkpoint
    - 放到后续 branch checkpoint 到 `main` 的迁入窗口

### C组：当前不建议进入 main
- NPC
  - 当前面：只补了 NPC / 导航协作口径的 memory 与日志
  - 不进原因：
    - 本轮没有新的业务代码收口
    - 只有记忆 / 日志，不值得单独形成一刀 `main` 收口
  - 下一步建议：
    - 回到真实 NPC 业务修正，再等下一轮统一回执

- 导航
  - 当前面：`2.0.0整改设计` docs-first checkpoint
  - 不进原因：
    - 还没进入首个非热文件代码 checkpoint
    - 当前更像设计基线，不是本轮要优先推入 `main` 的实装面
  - 下一步建议：
    - 继续推进到首个真实代码 checkpoint 后再回执

### D组：撞车或需人工强裁
- 本轮暂无

## 支援收件
- 全局 skills
  - 本轮身份：外部支援，不属于 `main` 收口线程
  - 当前收件结果：
    - 已给出 `sunset-startup-guard` 一级告警处理方案
  - 治理侧处理：
    - 可直接吸收为本地裁定输入
    - 不进入 `main` 收口批次

## 本轮结论
- 第一轮统一回执已经完成。
- 第一轮 `main` 收口批次表已经形成。
- 当前最合理的顺序不是“所有线程一起乱提”，而是：
  1. 先收 A组 农田
  2. 再挑 B组 中最值得优先迁入 `main` 的 branch carrier
  3. C组继续开发，不制造只含文档/日志的小碎提交

## 下一步建议
1. 先给农田发第一批 `main` 收口执行 prompt
2. 同时把全局 skills 的窄范围结论吸收到 `25` 阶段正文
3. 再裁定 B组 先迁 `spring-day1` 还是先迁遮挡检查

## 一句话摘要
- 第一轮回执结果显示：本轮真正适合先入 `main` 的只有农田，`spring-day1` 和遮挡检查是下一批 branch carrier 迁入对象，NPC 与导航则继续开发更划算。

## 执行更新（2026-03-22）
- A组 农田已按第一批完成 `main` 收口：
  - `f40d228d`
- B组 `spring-day1` 已完成基础脊柱代码迁入 `main`：
  - `83d809a9`
- 当前批次状态应更新为：
  - 已完成：农田、`spring-day1`
  - 当前下一候选：遮挡检查
  - 继续开发：NPC、导航
- 当前结论也随之收紧为：
  - 第一轮统一回执不再只是“生成批次表”，而是已经完成了 A组 第一刀和 B组 首个 branch carrier 迁入验证。
