# 2026-03-29｜全局警匪定责清扫第六轮｜导航停表、农田继续、Primary 迁移改判｜01

## 本轮先纠正一个关键误判

上一轮如果只看 `Assets/000_Scenes/Primary.unity`，会误判成“Primary 被直接删了”。

但当前 live 现场真实情况是两件事同时存在：

1. `D:\Unity\Unity_learning\Sunset\Assets\000_Scenes\Primary.unity`
   - 当前状态：`deleted`
2. `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\UI\Spring-day1\Primary.unity`
   - 当前状态：`untracked`
   - 文件实际存在，大小约 `2.37 MB`

所以本轮把这件事改判为：

- 这不是“裸删除”
- 这更像是 `Primary.unity` 的迁移态
- 后续治理不能再把它只当成旧 scene 删除面单独描述

另外，用户已明确接受两类内容可直接归仓：

- `.kiro/specs/UI系统` 旧文档迁移 / 删除
- `Assets/TextMesh Pro/Resources/Fonts & Materials` 当前中文字体 churn

## 本轮四类裁定

### 1. 继续发 prompt

- `农田交互修复V3`

但只继续一刀，而且刀口已经收紧：

- 不是继续扩白名单
- 不是继续把 `PlayerInteraction.cs` / `TreeController.cs` 带回
- 而是只在 `GameInputManager.cs` 内，把更深一层的 mixed-root 编译依赖切成本地兼容入口，再重新跑真实 `preflight -> sync`

### 2. 无需继续发

- `导航检查`
- `NPCV2`
- `项目文档总览`

其中 `导航检查` 本轮明确停表，不再继续发：

- live Git 已有：
  - `acfc7f27 2026.03.29_导航检查_01`
  - `3f6e445b 2026.03.29_导航检查_02`
- 当前再看：
  - `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Navigation`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查`
  已无相对 `HEAD` 的剩余 diff

### 3. 停给治理位分析 / 审核

- `UI-V1 / spring-day1`
- `spring-day1V2`
- `导航检查V2`
- `场景搭建（外包）`

这轮不重新唤醒它们的原因不是“没东西可做”，而是：

- `UI-V1 / spring-day1`
  - 当前卡的仍是 same-root hygiene + 迁移包整理
  - 用户虽已接受 UI 文档迁移与字体 churn，但这条线的 own-root 问题还没靠最新回执自行闭环
  - `Primary.unity` 现在应按“旧 scene 删除 + 新 scene 落到 UI/Spring-day1”这一迁移包来治理，不该草率回投给旧 prompt 继续自转

## 第六轮唯一真实刀口

### `农田交互修复V3`

第五轮已经把事实跑得很清楚：

- same-root blocker 仍然没有回来
- 当前第一真实 blocker 已升级成 `GameInputManager.cs` 自己对更深 mixed-root 的依赖

exact blocker 现已钉到：

1. `GameInputManager.cs:2666`
   - `playerInteraction.LastActionFailureReason`
2. `GameInputManager.cs:3249`
   - `targetTree.ShouldBlockAxeActionBeforeAnimation(...)`

而当前 live 脏改又显示：

- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\PlayerInteraction.cs` = dirty
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\TreeController.cs` = dirty
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Service\Player\` 下还有多条他线 dirty / untracked
- `D:\Unity\Unity_learning\Sunset\Assets\YYY_Scripts\Controller\` 下也有 NPC 线 dirty

所以第六轮不允许继续“最小扩包再扩一层”，否则会把农田线重新拖回 broad mixed-root。

第六轮唯一允许的推进方式是：

- 只在 `GameInputManager.cs` 内
- 把这两个 compile-time 依赖改成本地 compat / fallback
- 然后仍用“第五轮同一组白名单”重跑 `preflight -> sync`

## 第六轮领取说明

继续施工的线程去各自目录读取：

- `农田交互修复V3`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\2026-03-29_全局警匪定责清扫第六轮_在GameInputManager内切断更深mixed依赖并重新归仓_01.md`

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- `已上 git / 第一真实阻断已钉死 / 已停表`
