这轮是 `shared-root` 上传治理第二波。

唯一改判如下：
1. 不再沿用第一波“clearly-own 能交的先整批交”的口径。
2. 从这一波开始，统一改成：
   - `按过去每一小刀开发历史去还原批次`
   - `一次只允许一个历史小批次`
   - `不能顺手清 blocker`
   - `不能借上传继续开发`
3. 如果某个线程当前最小历史批次仍然撞到 shared / mixed / CodeGuard / own-root 扩根阻断：
   - 立刻停车
   - 只报这一个批次的 exact blocker
   - 不允许在同一轮里换第二个批次继续试

本波当前定向线程：
1. `spring-day1`
2. `UI`
3. `NPC`
4. `存档系统`
5. `导航检查`
6. `农田交互修复V3`

本波额外 catch-all：
1. `其它已施工但还没正式回执的线程`
   - 统一先走“只读补交”口径
   - 不默认继续上传
   - 先把“到底已经提交了什么 / push 了什么 / 还卡在哪”报实

统一回执要求：
1. 仍然直接对用户回复：
   - `A1 保底六点卡`
   - `A2 用户补充层（可选）`
   - `B 技术审计层`
2. `B 技术审计层` 至少写：
   - `提交 SHA`
   - `push 状态`
   - `当前 own 路径是否 clean`
   - `remaining blocker files`
3. 这波如果线程实际进入真实施工或真实上传尝试，继续按 `thread-state` 规范补：
   - `Begin-Slice`
   - `Ready-To-Sync`
   - `Park-Slice`

统一禁止事项：
1. 不要把第二波写回成“清 blocker 模式”
2. 不要一锅端当前所有 clearly-own
3. 不要把 shared/mixed 被卡住包装成“只是差一点点”
4. 不要顺手修功能
5. 不要顺手吞别线程同根脏改
6. 不要只报技术 dump，不给用户可读结论

这波治理位当前已核实的事实：
1. `spring-day1 / UI / NPC / 存档系统 / 导航检查` 第一波回执都和仓库事实大体对得上。
2. 它们都已经各自安全上传了一部分，但都还没有把剩余 own 尾账按历史小批次收完。
3. 当前第一波 6 条线程回执现已收齐。
