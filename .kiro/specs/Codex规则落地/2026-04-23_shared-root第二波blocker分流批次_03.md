这轮是 `shared-root` 上传治理第三波。

这波不再沿用“历史小批次继续试一轮”的统一口径。

当前唯一改判如下：
1. 第二波 6 条回执已经把问题压成了 `3` 类不同 blocker。
2. 从这一波开始，必须按 blocker 类型分流，不准继续群发同一种 `prompt_03`。

当前 3 类 blocker 固定为：

## A 类：same-root / 父根扩根
- `spring-day1`
- `UI`
- `导航检查`
- `农田交互修复V3`

这类线程下一刀的目标不是“再试同一个小批次”，而是：
- 承认 `prompt_02` 已经完成
- 把前一刀已经证实存在的同根 blocker 真整进本轮唯一切片
- 仍然只允许根内最小整合，不准顺手扩到跨根 shared/mixed 面

## B 类：工具链 / preflight incident
- `存档系统`

这类线程下一刀的目标不是继续业务上传，而是：
- 把工具 incident 收成可判断的证据
- 明确到底是 `CodexCodeGuard / git-safe-sync / launcher / process` 哪一层挂
- 不准再把这类问题包装成“业务线程继续试一刀”

## C 类：历史小批本身不独立，先补一致性
- `NPC`

这类线程下一刀的目标不是继续问“这批能不能传”，而是：
- 先把让这批不独立的引用/主表一致性补齐
- 再回到上传

本波当前定向线程：
1. `spring-day1`
2. `UI`
3. `NPC`
4. `存档系统`
5. `导航检查`

本波明确后置，不在本轮发：
1. `农田交互修复V3`
   - 原因：同刀跨 `Assets/Editor / Assets/YYY_Scripts/Controller / Assets/222_Prefabs/Rock` 三个宽根，风险高于当前前 5 条
2. `019d4d18-bb5d-7a71-b621-5d1e2319d778`
   - 原因：虽然补交的 `README` docs-only 上传属实，但当前 thread-state 仍是 `ACTIVE`，且 `README.md` 仍有新本地脏改，不按 parked 候选处理
3. `云朵与光影`
   - 原因：历史提交 `7e4508d0` 已确认在远端，但当前 `DayNightManager.cs` 仍 dirty，不适合混入这波
4. `树石修复`
   - 原因：补交的只是历史 blocked 回执，不是漏报成功上传

统一要求：
1. 所有 `prompt_03` 都必须先继承：
   - `prompt_02` 对应那一刀已经执行完
   - 不准重复重跑同一小批
2. 这波每条线程仍然只允许一个唯一切片。
3. 撞 blocker 就停车，不准顺手换第二批。
4. 仍然直接对用户回复：
   - `A1 保底六点卡`
   - `A2 用户补充层（可选）`
   - `B 技术审计层`

这波治理位当前最核心的判断：
1. 现在严重，但不是“成果坏了”的严重。
2. 真正严重的是如果继续拿统一模板发下一轮，会把：
   - same-root 问题误当成“再试一次”
   - tool incident 误当成“业务线程自己排”
   - 不独立批次误当成“删了就能传”
