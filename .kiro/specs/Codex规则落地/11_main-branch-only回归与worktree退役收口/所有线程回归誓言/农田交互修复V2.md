**结论**

1. `codex/farm-1.0.2-cleanroom001 @ 66c19fa1` 现在就是 farm 后续唯一 continuation branch carrier。
证据：
- `git rev-parse codex/farm-1.0.2-cleanroom001` = `66c19fa17a55afec7bf2e0a2a1c695aa0c7f75d0`
- `git log -1 --oneline codex/farm-1.0.2-cleanroom001` = `66c19fa1 2026.03.17-01`
- 该分支已推到远端：`origin/codex/farm-1.0.2-cleanroom001`
- cleanroom worktree 当前干净：`D:\Unity\Unity_learning\Sunset_worktrees\farm-1.0.2-cleanroom001` 上 `git status --short --branch` 为空

2. `codex/farm-1.0.2-correct001` 以后应只保留事故取证身份，不应再继续承载 farm 新开发。
证据：
- `git rev-parse codex/farm-1.0.2-correct001` = `11e0b7b4c1340e0359a546b038d711b03836dc72`
- 共享根目录当前仍绑在它上面：`D:\Unity\Unity_learning\Sunset` 的 `git worktree list --porcelain` 显示 root worktree 对应 `refs/heads/codex/farm-1.0.2-correct001`
- 根目录现场仍有大量 shared dirty，明显不适合作为 farm 正常续开现场

3. 共享根目录恢复为 `main` 之后，farm 的最小 branch-only 入口应当是：
- 工作目录：`D:\Unity\Unity_learning\Sunset`
- 要检出的分支：`codex/farm-1.0.2-cleanroom001`
- 起点 commit：`66c19fa17a55afec7bf2e0a2a1c695aa0c7f75d0`
证据：
- `main` 当前是 `b9b6ac48`
- `git merge-base main codex/farm-1.0.2-cleanroom001` = `b9b6ac48...`
- 说明 cleanroom carrier 就是从当前 `main` 基线起出来并继续推进到 `66c19fa1` 的唯一干净 farm 续开分支

**证据**

4. `farm-1.0.2-cleanroom001` 这个物理 worktree 可以在“共享根目录已重新中性化并能用 branch-only 模式续开”后退役；它现在缺的不是代码，而是现场迁回动作。
证据：
- cleanroom 分支已推远端，且 worktree 本身干净
- cleanroom carrier 已不是只存在于本地 worktree 的临时状态，而是完整 branch carrier
退役条件：
- 治理线程先把共享根目录从 `codex/farm-1.0.2-correct001` 脱离出来，恢复到干净的 `main`
- 确认之后 farm 续开统一改为“根目录 + `codex/farm-1.0.2-cleanroom001` 检出”
- 确认不再需要用该 worktree 作为事故隔离现场
branch-only 入口：
- 一旦上面 3 条完成，后续 farm 直接在 `D:\Unity\Unity_learning\Sunset` 检出 `codex/farm-1.0.2-cleanroom001`

5. 为了让共享根目录重新变成中性现场，farm 还需要治理线程先补 1 个 shared root 级动作：把根目录从污染分支切回干净 `main`，并对当前根目录上的 shared dirty 做统一裁定/收口。
证据：
- 共享根目录现在不是中性现场，而是 `codex/farm-1.0.2-correct001`
- 根目录 `git status --short --branch` 显示大量与 farm cleanroom 无关的 shared 改动和未跟踪文件
- 这意味着问题不再是 farm carrier 缺失，而是 root 现场还没治理回“可供多线程共用”的 `main`

**退役条件**

6. 最终裁定：
- 当治理线程完成“共享根目录切回干净 `main` + 确认后续 farm 一律从根目录检出 `codex/farm-1.0.2-cleanroom001@66c19fa1` 继续”这一步后，farm 就可以完全回到“共享根目录 + 分支模式”，不再需要 cleanroom worktree。
证据：
- continuation branch carrier 已经存在且已推远端
- cleanroom worktree 已无未提交依赖
- 当前剩下的唯一问题是 shared root 还没从事故占用态恢复为中性 `main`