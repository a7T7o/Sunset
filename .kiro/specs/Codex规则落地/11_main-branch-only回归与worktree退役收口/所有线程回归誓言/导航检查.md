截至 `2026-03-17` 的只读核查结论如下。

1. 不需要。  
`导航检查` 当前不需要任何独立 `worktree` 才能继续后续导航重构工作。[Sunset当前唯一状态说明_2026-03-17.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/Steering规则区优化/当前运行基线与开发规则/Sunset当前唯一状态说明_2026-03-17.md) 已明确写到“`导航检查` 不需要新建 worktree，若后续进入整改，优先共享根目录 + 分支”；[线程分支对照表.md](/D:/Unity/Unity_learning/Sunset/.codex/threads/线程分支对照表.md) 也把它定义为 `D:\Unity\Unity_learning\Sunset @ main` 默认入口、转整改再切 `codex/...`。当前真正的问题不是缺 `worktree`，而是共享根目录此刻实际 checkout 在 `codex/farm-1.0.2-correct001 @ 11e0b7b4`。

2. 接受。  
如果后续进入实际整改，我接受默认走“共享根目录 + 分支”模式；只有共享热文件冲突不可接受，或出现高风险隔离 / 故障修复 / 特殊实验这类例外时，才升级为 `worktree`。这和 [AGENTS.md](/D:/Unity/Unity_learning/Sunset/AGENTS.md) 以及 [git-safety-baseline.md](/D:/Unity/Unity_learning/Sunset/.kiro/steering/git-safety-baseline.md) 的现行口径一致。

3. 当前线程后续最合理的 branch-only 开工入口：  
先回到 `D:\Unity\Unity_learning\Sunset @ main` 这个默认入口，再在进入真实整改前执行 `git-safe-sync.ps1 -Action ensure-branch -BranchName codex/navigation-remediation-001`，然后只在该 `codex/...` 分支上开工。也就是说，最合理入口不是新建 `worktree`，而是“共享根目录恢复到 `main` 后，再切导航整改分支”。  

本轮未改文件，只做了只读核查。