# 执行方案：main-branch-only 回归与 worktree 退役收口

## 1. 执行原则
- 分支保留，`worktree` 退役。
- 先确认 branch carrier，再退役物理目录。
- 先确认 owner，再恢复共享根目录。
- 先回到 branch-only，再谈后续新功能推进。
- 不再接受“为了方便继续开发，所以长期留着 worktree”这种理由。

## 2. 推荐顺序

### 第一步：收三大业务线 owner 结论
- `spring-day1`
- `NPC`
- `farm`

目标：
- 锁死每条业务线的唯一合法 branch carrier。
- 锁死每条业务线当前 worktree 的临时身份。
- 锁死每条业务线回归 branch-only 的迁回方案。

### 第二步：冻结历史容器
- `main-reflow-carrier`
- `NPC`
- `farm-10.2.2-patch002`

目标：
- 把这些目录从“可能还会被继续拿来干活”降级为“已确认不再续写的历史容器”。
- 为后续物理删除准备清单。

### 第三步：恢复共享根目录
- 归属 shared dirty
- 形成 shared root 清理表
- 回到 `main`
- 重新声明“根目录 + 分支”为默认入口

### 第四步：退役临时容器
- `NPC_roam_phase2_rescue`
- `NPC_roam_phase2_continue`
- `farm-1.0.2-cleanroom001`
- `spring-day1-story-progression-001`

目标：
- 清空“当前还需要额外打开另一个 Unity 工程才能开发/验收”的现状。

## 3. 对三条业务线的要求

### 3.1 spring-day1
- 必须回答：
  - 4 个 TMP 资源是否需要真实固化
  - 如果固化，最终应落在哪条分支
  - 如果不固化，是删除、重建还是归档
  - 共享根目录恢复后，后续如何只用 branch 继续
- 禁止继续给出“新建 continuation worktree”作为默认答案。

### 3.2 NPC
- 必须回答：
  - `codex/npc-roam-phase2-002` 是否已经是唯一 continuation branch
  - `NPC_roam_phase2_continue` 什么时候可退役
  - `NPC_roam_phase2_rescue` 什么时候可删除
  - 共享根目录恢复后，如何只在根目录检出该分支继续

### 3.3 farm
- 必须回答：
  - `codex/farm-1.0.2-cleanroom001` 是否已经是唯一 continuation branch
  - `farm-1.0.2-cleanroom001` 什么时候可退役
  - `codex/farm-1.0.2-correct001` 是否永久只保留取证身份
  - 共享根目录恢复后，如何只在根目录检出该分支继续

## 4. 当前不急着发 prompt 的线程

### 导航检查
- 当前是审计/重构准备线程。
- 当前没有活跃 worktree 问题。
- 当前不需要新建 worktree，也不需要立即发“迁回 prompt”。
- 若进入整改，优先使用共享根目录 + 分支。

### 遮挡检查
- 当前是审计成果固化线程。
- 当前没有活跃 worktree 问题。
- 当前不需要新建 worktree，也不需要立即发“迁回 prompt”。
- 若进入整改，优先使用共享根目录 + 分支。

### 项目文档总览
- 当前更接近历史归档与导航线程。
- 不应继续争夺现行入口。
- 不需要独立 worktree。

## 5. 当前一句话执行口径
- `11` 阶段不是继续造新的隔离面，而是把现有隔离面全部变成“可删的临时容器”，最终只留下“共享根目录 + 分支”这一套单工程工作方式。
