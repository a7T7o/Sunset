# 共享根目录 dirty 归属可执行版（2026-03-17）

## 1. 文档目的
- 本文件是对 [共享根目录dirty归属初版_2026-03-17.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/Codex规则落地/11_main-branch-only回归与worktree退役收口/共享根目录dirty归属初版_2026-03-17.md) 的升级。
- 初版解决“先分箱”；本文件解决“下一步谁先动、动到什么程度、什么条件下才允许 shared root 回 `main`”。
- 它不是最终清理记录，而是 shared root 回 `main` 之前的执行底稿。

## 2. 当前 shared root 真实现场
- 工作目录：`D:\Unity\Unity_learning\Sunset`
- 当前分支：`codex/farm-1.0.2-correct001`
- 当前 `HEAD`：`11e0b7b4`
- 当前结论：
  - 这里仍是事故取证现场
  - 这里不是中性 `main`
  - 当前不能直接在这里继续业务开发
  - 当前也不能直接粗暴 `checkout main`，因为 tracked dirty / deleted / untracked 还没收口

## 3. 已经不再阻塞的事项

### A. carrier 判断已经完成
- `farm`：
  - 唯一 continuation carrier：`codex/farm-1.0.2-cleanroom001 @ 66c19fa1`
- `NPC`：
  - 唯一 continuation carrier：`codex/npc-roam-phase2-002 @ 6e2af71b`
- `spring-day1`：
  - 唯一 branch-only 入口分支：`codex/spring-day1-story-progression-001 @ a9c952b7`

### B. 辅助线已经退出阻塞清单
- `导航检查`：接受 `main -> codex/navigation-remediation-001`
- `遮挡检查`：接受 `main -> codex/occlusion-remediation-001`
- `项目文档总览`：确认降级为归档辅助线，不再争夺现行入口

### C. `spring-day1` 字体双现场问题已经完成证据导出与清尾
- 证据目录：
  - [证据导出_2026-03-17_spring-day1字体双现场](/D:/Unity/Unity_learning/Sunset/.kiro/specs/Codex规则落地/11_main-branch-only回归与worktree退役收口/证据导出_2026-03-17_spring-day1字体双现场)
- 执行动作：
  - 已导出 shared root 与 rescue 两处字体 dirty 的补丁和 diffstat
  - 已在 shared root 丢弃 `BitmapSong / Pixel / SoftPixel / V2` 当前 dirty
  - 已在 `NPC_roam_phase2_rescue` 丢弃 `BitmapSong / Pixel / SDF / V2` 当前 dirty
- 当前结果：
  - shared root 本轮字体 dirty 已清空
  - `NPC_roam_phase2_rescue` 已恢复为 `CLEAN`

## 4. 当前真正阻塞 shared root 回 `main` 的问题

### A. shared root 上的治理/归档 dirty 仍需治理线程自己收口
这部分不属于 `farm / NPC / spring-day1` 的业务回迁动作，属于治理线程自己的尾巴。

#### 代表范围
- `.kiro/specs/Codex规则落地/**`
- `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/**`
- `.kiro/specs/Steering规则区优化/文档归档/**`
- `.codex/threads/Sunset/Codex规则落地/**`

#### 当前判断
- 这些改动里有真实有效的新入口、新阶段文档和新的治理结论。
- 但也混着旧现行文件的 `deleted`、新归档层的 `untracked`、迁移中间态。
- 在这些治理尾巴没有形成一次安全白名单同步前，shared root 还不具备“切回 `main` 并继续多人共用”的条件。

### B. 杂项 `untracked` 仍未裁定
这部分如果不先判归属，后面很容易被错误地跟进任意一个线程的提交。

#### 当前待裁定项
- `Assets/Screenshots.meta`
- `Assets/Screenshots/`
- `npc_restore.zip`

#### 当前判断
- `Assets/111_Data/NPC 1.meta` 已确认为误复制残留并已删除。
- 当前剩余这 2 类内容不能默认并入 `farm / NPC / spring-day1` 任一业务收口。
- 当前初判：
  - `Assets/Screenshots/` 是 farm checkpoint 截图证据，不是正式运行时资产
  - `npc_restore.zip` 是 NPC 恢复期的本地快照备份，不是当前 continuation branch carrier 的一部分
- 必须先判定为：
  - 验收证据
  - 历史备份
  - 临时垃圾
  - 仍需留仓的正式资产

## 5. 已明确的执行顺序
1. 治理线程单独处理 shared root 上自己的治理/归档 dirty，形成一次安全可解释的治理收口。
2. 单独裁定 `Assets/Screenshots*`、`npc_restore.zip` 的最终去留。
3. 只有当 shared root 不再挂着以上两类阻塞时，才允许把 `D:\Unity\Unity_learning\Sunset` 切回 `main`。
4. shared root 回 `main` 后，在根目录依次做 branch-only 检出验证：
   - `codex/spring-day1-story-progression-001`
   - `codex/npc-roam-phase2-002`
   - `codex/farm-1.0.2-cleanroom001`
5. 三条分支验证 clean 后，再退役第二批 worktree。

## 6. 当前不能直接做的事
- 不能把“回包已齐”误判成“shared root 已可切回 `main`”。
- 不能在当前 shared root 继续给 `farm / NPC / spring-day1` 写新业务。
- 不能在治理尾巴与剩余 `untracked` 还未裁定前，硬切 shared root 回 `main`。

## 7. 当前一句话结论
- 现在 shared root 回 `main` 的最大阻塞，已经从业务线程尾巴收缩成“治理 shared root 自身仍未收口 + 剩余截图/zip 杂项未裁定”。
