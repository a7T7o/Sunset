# 分发 Prompt（branch-only 回归版，2026-03-17）

## 当前状态
- 本文件已完成历史使命。
- 本文件中的 prompt 已经全部发出、收到回包并执行完成。
- `NPC / farm / spring-day1` 的 shared root branch-only 检出验证已经真实做完，不再需要继续沿用本文件下发新的 `11` 阶段 prompt。
- 以下正文只保留为过程留档。

## 0. 分发与回包说明
- 线程回复回来后，统一汇总到：
  - 原始回包优先放到：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\11_main-branch-only回归与worktree退役收口\所有线程回归誓言\`
  - 索引/提炼写到：`D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\11_main-branch-only回归与worktree退役收口\所有线程回归誓言.md`
- 如果线程需要理解 shared root 为什么还不能直接回 `main`，可附上：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\11_main-branch-only回归与worktree退役收口\共享根目录dirty归属可执行版_2026-03-17.md`

## 1. 发给 spring-day1
你现在不是继续提新 worktree 方案，而是要帮助 Sunset 回到 `main + branch-only` 常态。

已知前提：
- 共享根目录当前仍是事故现场：`D:\Unity\Unity_learning\Sunset @ codex/farm-1.0.2-correct001 @ 11e0b7b4`
- 你当前已认领 4 个 TMP 资源尾巴，它们也是 `NPC_roam_phase2_rescue` 剩余 dirty 的真实归属：
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese BitmapSong SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
- 你已有 clean checkpoint：`codex/spring-day1-story-progression-001 @ a9c952b78f1b0184edc6622820158b70ee1d7ea6`

本轮请只做只读核对与方案输出，不要新增 worktree，不要继续写业务代码。

请严格回答下面 6 点：
1. 你确认这 4 个 TMP 资源最终应如何处理：
   - 保留并固化到哪条分支
   - 还是放弃、删除、重建、归档
2. 共享根目录恢复到 `main` 之后，`spring-day1` 的 branch-only 唯一入口应该是什么：
   - 根目录
   - 检出哪条分支
   - 起点 commit 是什么
3. 你是否确认后续 spring-day1 不再需要长期独立 worktree 才能继续开发或验收。
4. 如果需要一个过渡步骤，请给出“从当前状态迁回 branch-only”的最小步骤清单，但禁止再建议新建 continuation worktree。
5. 请判断 `Primary.unity`、`DialogueUI.cs` 当前是否还存在必须由你继续认领但尚未固化的尾巴；如果没有，请明确说没有。
6. 最后请给出一句裁定：
   - `spring-day1` 何时可以完全回到共享根目录 + 分支模式

输出要求：
- 只做只读核查
- 用“结论 -> 证据 -> 下一步”结构输出
- 不要泛泛讨论
- 不要再给新的长期 worktree 提案

## 2. 发给 NPC
你现在不是继续把 `NPC_roam_phase2_continue` 当长期现场，而是要帮助 Sunset 从临时容器回到 `main + branch-only` 常态。

已知前提：
- 当前 continuation branch carrier 是：`codex/npc-roam-phase2-002 @ 6e2af71b204750f554935eae3728108d23c86bb2`
- 当前 rescue 容器是：`D:\Unity\Unity_learning\Sunset_worktrees\NPC_roam_phase2_rescue @ codex/npc-roam-phase2-001 @ 28aef95d`
- rescue 中剩余 4 个 TMP 资源 dirty 已确认归属 `spring-day1`，不归属 NPC
- 用户红线已经锁死：`worktree` 只能是事故隔离例外，NPC 后续不能长期停在 worktree 里开发

本轮请只做只读核对与 branch-only 迁回方案设计，不要继续写新业务代码。

请严格回答下面 6 点：
1. 你是否确认 `codex/npc-roam-phase2-002 @ 6e2af71b` 已经是 NPC 后续唯一 continuation branch carrier；如果不是，请给出唯一正确 carrier。
2. 你是否确认 `NPC_roam_phase2_continue` 只是临时物理容器，而不是 NPC 的长期合法现场。
3. 在共享根目录恢复为 `main` 之后，如果要继续 NPC 开发，最小 branch-only 入口是什么：
   - 工作目录
   - 要检出的分支
   - 起点 commit
4. `NPC_roam_phase2_rescue` 何时可以彻底降级为只读取证点，何时可以删除。
5. `NPC_roam_phase2_continue` 何时可以退役；退役前还缺什么验证或迁移动作。
6. 请给出一句最终裁定：
   - NPC 什么时候可以完全回到共享根目录 + 分支模式，而不再需要任何 NPC 专用 worktree

输出要求：
- 只做只读核查
- 用“结论 -> 证据 -> 退役条件 -> branch-only 入口”结构输出
- 不要再把 continuation worktree 描述成长期现场

## 3. 发给 farm
你现在不是继续把 cleanroom 当长期现场，而是要帮助 Sunset 从事故 cleanroom 回到 `main + branch-only` 常态。

已知前提：
- 当前 cleanroom branch carrier 是：`codex/farm-1.0.2-cleanroom001 @ 66c19fa17a55afec7bf2e0a2a1c695aa0c7f75d0`
- 当前污染分支是：`codex/farm-1.0.2-correct001 @ 11e0b7b4`
- cleanroom 已编译通过，可作为 continuation branch carrier
- 用户红线已经锁死：`worktree` 只能是事故 cleanroom 例外，不能变成 farm 的长期默认现场

本轮请只做只读核查与 branch-only 迁回方案设计，不要继续写新业务代码。

请严格回答下面 6 点：
1. 你是否确认 `codex/farm-1.0.2-cleanroom001 @ 66c19fa1` 已经是 farm 后续唯一 continuation branch carrier；如果不是，请给出唯一正确 carrier。
2. 你是否确认 `codex/farm-1.0.2-correct001` 以后只保留事故取证身份，不再继续承载 farm 新开发。
3. 在共享根目录恢复为 `main` 之后，如果要继续 farm 开发，最小 branch-only 入口是什么：
   - 工作目录
   - 要检出的分支
   - 起点 commit
4. `farm-1.0.2-cleanroom001` 这个物理 worktree 什么时候可以退役；退役前还缺什么。
5. 为了让共享根目录重新变成中性现场，你认为 farm 还需要治理线程先补什么 shared root 级动作。
6. 请给出一句最终裁定：
   - farm 什么时候可以完全回到共享根目录 + 分支模式，而不再需要 cleanroom worktree

输出要求：
- 只做只读核查
- 用“结论 -> 证据 -> 退役条件 -> branch-only 入口”结构输出
- 不要再把 cleanroom worktree 描述成 farm 的长期现场

## 4. 可选：发给导航检查
你当前不是 worktree 问题核心线程。本轮请只做只读核查，并回答：
1. 你当前是否需要任何独立 worktree 才能继续后续导航重构工作；如果不需要，请明确回答不需要。
2. 如果后续进入实际整改，你是否接受默认走“共享根目录 + 分支”模式；只有共享热文件冲突不可接受时再升级例外隔离。
3. 请给出你当前线程后续最合理的 branch-only 开工入口。

## 5. 可选：发给遮挡检查
你当前不是 worktree 问题核心线程。本轮请只做只读核查，并回答：
1. 你当前是否需要任何独立 worktree 才能继续后续遮挡重构工作；如果不需要，请明确回答不需要。
2. 如果后续进入实际整改，你是否接受默认走“共享根目录 + 分支”模式；只有共享热文件冲突不可接受时再升级例外隔离。
3. 请给出你当前线程后续最合理的 branch-only 开工入口。

## 6. 可选：发给项目文档总览
你当前线程已降级为历史总览/归档辅助线。本轮请只做只读核查，并回答：
1. 你是否确认自己不再争夺现行入口身份。
2. 你是否确认后续也不需要独立 worktree。
3. 你建议如何把你现有文档成果继续留在归档层，而不是混入现行活入口。

## 7. 二轮：发给 spring-day1
你现在不是继续泛泛确认 branch-only，而是要专门裁定 `spring-day1` 当前最后一个真正阻塞点：字体 dirty 在两个现场分裂，而且文件集合不一致。

已知前提：
- shared root 当前 dirty 字体文件是：
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese BitmapSong SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SoftPixel SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
- `D:\Unity\Unity_learning\Sunset_worktrees\NPC_roam_phase2_rescue` 当前 dirty 字体文件是：
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese BitmapSong SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SDF.asset`
  - `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
- 也就是说，两边共同部分是 `BitmapSong / Pixel / V2`，但 shared root 独有 `SoftPixel`，rescue 独有 `DialogueChinese SDF`
- 用户红线已经锁死：不能因为这种尾巴继续长期保留 worktree

本轮请只做只读核查，不要新增改动，不要顺手修字体资源。

请严格回答下面 6 点：
1. 你是否确认 shared root 这组字体 dirty 与 rescue 这组字体 dirty 都属于 `spring-day1` 语义范围；如果不是，请逐个文件改判归属。
2. `DialogueChinese SoftPixel SDF.asset` 与 `DialogueChinese SDF.asset` 分别是什么身份：
   - 谁是当前项目仍应保留的资产
   - 谁只是一次临时生成/历史残留
3. 对这 5 个相关字体资产，请逐个给出最终动作：
   - 保留已提交版本
   - 导出证据后丢弃当前 dirty
   - 需要重生成
   - 需要转移到别处归档
4. 如果 shared root 想回 `main`，你建议先清哪一边：
   - shared root 的字体 dirty
   - `NPC_roam_phase2_rescue` 的字体 dirty
   - 还是必须先各自导出证据
5. 当这组字体问题处理完后，你是否确认 `spring-day1-story-progression-001` 也可以退役，后续只保留 `D:\Unity\Unity_learning\Sunset @ codex/spring-day1-story-progression-001` 作为 branch-only 唯一入口。
6. 最后请给一句裁定：
   - `spring-day1` 距离彻底回到 `main + branch-only` 还差哪一个最小动作

输出要求：
- 用“结论 -> 证据 -> 最终动作 -> 对 shared root 回 main 的影响”结构输出
- 不要再提新 worktree
- 不要泛泛说“都可以之后再处理”

## 8. 预备：shared root 回 `main` 后发给 NPC
当 shared root 已恢复为 `D:\Unity\Unity_learning\Sunset @ main` 后，再发这一条：

你现在只做 1 次 branch-only 验证，不要新增功能，不要改业务代码。

请在 shared root 执行并回答：
1. 从 `D:\Unity\Unity_learning\Sunset @ main` 检出 `codex/npc-roam-phase2-002`
2. 回答当前 `HEAD` 是否仍为 `6e2af71b204750f554935eae3728108d23c86bb2`
3. 回答 `git status --short` 是否为空
4. 如果为空，请明确裁定 `NPC_roam_phase2_continue` 是否现在就可退役

## 9. 预备：shared root 回 `main` 后发给 farm
当 shared root 已恢复为 `D:\Unity\Unity_learning\Sunset @ main` 后，再发这一条：

你现在只做 1 次 branch-only 验证，不要新增功能，不要改业务代码。

请在 shared root 执行并回答：
1. 从 `D:\Unity\Unity_learning\Sunset @ main` 检出 `codex/farm-1.0.2-cleanroom001`
2. 回答当前 `HEAD` 是否仍为 `66c19fa17a55afec7bf2e0a2a1c695aa0c7f75d0`
3. 回答 `git status --short` 是否为空
4. 如果为空，请明确裁定 `farm-1.0.2-cleanroom001` 是否现在就可退役

## 10. 当前阶段：发给农田交互修复V2
你现在只做只读核查，不要新增修改。shared root 当前仍残留以下 untracked 截图证据：

- `Assets/Screenshots.meta`
- `Assets/Screenshots/farm_checkpoint_primary_maincamera.png`
- `Assets/Screenshots/farm_checkpoint_primary_maincamera.png.meta`

本轮请严格回答下面 4 点：
1. 你是否确认这组 `Assets/Screenshots*` 是 farm 线程生成的 checkpoint / 验收证据，而不是正式项目运行时资产。
2. 它们对 `codex/farm-1.0.2-cleanroom001 @ 66c19fa1` 这个 continuation branch carrier 是否还有任何必要依赖；如果没有，请明确说没有。
3. 为了让 shared root 回 `main`，你推荐的最终动作是什么：
   - 直接删除
   - 移出仓库后归档
   - 暂时保留到某个明确节点
4. 最后请给一句裁定：
   - 这组截图是否会继续阻塞 farm 回到 `main + branch-only`

输出要求：
- 用“结论 -> 证据 -> 最终动作 -> 对 shared root 回 main 的影响”结构输出
- 不要泛泛讨论
- 不要顺手扩展到其他 farm 业务

## 11. 当前阶段：发给 NPC
你现在只做只读核查，不要新增修改。shared root 当前仍残留 1 个 untracked 备份包：

- `npc_restore.zip`

我已经实核过 zip 内容，它主要包含：
- `.codex/threads/Sunset/NPC/memory_0.md`
- `.kiro/specs/NPC/**`
- `Assets/100_Anim/NPC/**`
- `Assets/222_Prefabs/NPC/**`
- `Assets/Editor/NPCPrefabGeneratorTool.cs`
- `Assets/Sprites/NPC/*.meta`
- `Assets/YYY_Scripts/Controller/NPC/**`

本轮请严格回答下面 4 点：
1. 你是否确认 `npc_restore.zip` 只是 NPC 恢复期的本地快照备份，而不是当前 continuation branch carrier 的组成部分。
2. 它对 `codex/npc-roam-phase2-002 @ 6e2af71b` 这条 continuation branch 现在是否还有任何必要依赖；如果没有，请明确说没有。
3. 为了让 shared root 回 `main`，你推荐的最终动作是什么：
   - 直接删除
   - 移出仓库后归档
   - 暂时保留到某个明确节点
4. 最后请给一句裁定：
   - `npc_restore.zip` 是否会继续阻塞 NPC 回到 `main + branch-only`

输出要求：
- 用“结论 -> 证据 -> 最终动作 -> 对 shared root 回 main 的影响”结构输出
- 不要泛泛讨论
- 不要再解释 worktree carrier 本身
