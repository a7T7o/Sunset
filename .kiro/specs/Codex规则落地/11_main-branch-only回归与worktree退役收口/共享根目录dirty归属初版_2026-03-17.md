# 共享根目录 dirty 归属初版（2026-03-17）

## 1. 文档目的
- 本文件只做一件事：把 `D:\Unity\Unity_learning\Sunset` 当前 shared root 里的 `modified / deleted / untracked` 先分箱。
- 它不是最终清理执行单，而是 shared root 恢复 `main` 之前的第一版所有权地图。
- 后续所有“回 main”“删第二批 worktree”“统一验收入口”的动作，都应以这份归属表为前置参考，而不是凭感觉清理。

## 2. 当前 shared root 真实现场
- 工作目录：`D:\Unity\Unity_learning\Sunset`
- 当前分支：`codex/farm-1.0.2-correct001`
- 当前 `HEAD`：`11e0b7b4`
- 当前结论：
  - 这里仍是事故取证现场
  - 这里不是中性 `main`
  - 这里不适合继续让业务线程直接写新实现

## 3. 归属分类

### A 类：治理线程当前活跃改动
这部分属于 `Codex规则落地 / 000_代办/codex / 当前运行基线与开发规则` 体系，后续应由治理线程自己收口。

#### 代表路径
- `.kiro/specs/Codex规则落地/**`
- `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/**`
- `.codex/threads/Sunset/Codex规则落地/memory_0.md`
- `.codex/threads/Sunset/Codex规则落地/memo y_1.md`

#### 当前判断
- 这些内容属于本轮治理续办真实产物。
- 它们不应被混进 `spring-day1 / NPC / farm` 的业务收口。
- shared root 回 `main` 前，治理线程需要先决定：
  - 哪些保留为现行入口
  - 哪些进入归档
  - 哪些仍只是过渡文件

### B 类：spring-day1 明确认领内容
这部分已经有较强证据表明属于 `spring-day1`。

#### 代表路径
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese BitmapSong SDF.asset`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese Pixel SDF.asset`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese SoftPixel SDF.asset`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/DialogueChinese V2 SDF.asset`
- `.codex/threads/Sunset/spring-day1/memory_0.md`
- `.kiro/specs/900_开篇/spring-day1-implementation/memory.md`
- `.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/`（当前为 untracked 目录）

#### 当前判断
- 4 个 TMP 资源尾巴已经被 `spring-day1` 明确认领。
- 但它们最终是：
  - 保留并固化
  - 还是归档 / 删除 / 重建
  还需要 `spring-day1` 给最终 branch-only 方案。

### C 类：NPC 线程当前认领内容
这部分不应由治理线程越权处理，只应记录归属。

#### 代表路径
- `.codex/threads/Sunset/NPC/memory_0.md`
- `.kiro/specs/NPC/memory.md`
- `.kiro/specs/NPC/1.0.0初步规划/memory.md`

#### 当前判断
- 这些是 NPC 当前活动线留下的记忆同步。
- 它们不应混入 shared root 的治理清理动作。
- 后续应随 NPC 的 branch-only 回归方案一起收口。

### D 类：治理归档/重组历史残留
这部分主要是旧现行层文档的 `deleted` 与新归档层的 `untracked` 并存，属于治理重组残留，不属于业务线程。

#### 代表路径
- `.kiro/specs/Steering规则区优化/当前运行基线与开发规则/` 下多份 `2026-03-15` 老文件的删除状态
- `.kiro/specs/Steering规则区优化/文档归档/**` 下大量 `untracked` 目录和文件

#### 当前判断
- 这批内容大概率是“旧现行层下线，新归档层接盘”的重组残留。
- 后续需要治理线程单独判断：
  - 是真正待提交的归档迁移
  - 还是中途重排留下的半成品

### E 类：待核验杂项 / 可疑垃圾
这部分当前归属不清，shared root 恢复 `main` 前必须单独核定。

#### 代表路径
- `Assets/111_Data/NPC 1.meta`
- `Assets/Screenshots.meta`
- `Assets/Screenshots/`
- `npc_restore.zip`

#### 当前判断
- 这批内容现在不能直接混入任何线程默认收口。
- 它们应在 branch-only 回归后单独判定为：
  - 验收证据
  - 历史残留
  - 待删除垃圾
  - 仍有价值的手工备份

## 4. 当前 shared root 还不能回 main 的原因
当前至少还有四个前置条件未满足：

1. `spring-day1 / NPC / farm` 的 branch-only 回归答复还没收齐。
2. `spring-day1` 认领的 4 个 TMP 资源还没给最终处理策略。
3. 治理层旧现行文件删除与新归档层 untracked 还没做最终裁定。
4. 杂项 `Screenshots / npc_restore.zip / NPC 1.meta` 还没完成归属判断。

## 5. 下一步建议
1. 先发 `spring-day1 / NPC / farm` 的 branch-only prompt。
2. 收到三方答复后，把本表从“初版”升级为“可执行归属表”。
3. 再推进 shared root 回 `main`。
4. 最后删第二批临时 worktree。

## 6. 当前一句话结论
- shared root 现在最缺的不是“再多一点讨论”，而是一份明确的归属图；本文件就是第一版归属图，它说明 shared root 还远没到可以直接切回 `main` 然后假装干净的程度。
