# 2026-04-03_典狱长_树石修复_Rock-prefab损坏与同类序列化体检修复_01

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\树石修复\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\树石修复\memory.md`
- `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\Rock\C1.prefab`
- `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\Rock\C2.prefab`
- `D:\Unity\Unity_learning\Sunset\Assets\222_Prefabs\Rock\C3.prefab`

你这轮不是继续 Tree 批量工具，也不是继续等外部编译红自己消失。

你这轮唯一主刀只有一个：

- **把 `Assets/222_Prefabs/Rock/C1.prefab`、`C2.prefab`、`C3.prefab` 这 3 份 Rock prefab 的损坏/序列化异常彻查并修到可稳定加载，同时顺手做同目录同类 prefab 的最小同类体检。**

## 一、当前已接受基线

1. 你的线程当前最新已接受进度是：
   - `TreeController / StoneController` 编辑态预览与运行态状态修复已做完；
   - 当前 live 状态是 `PARKED`；
   - 之前停车原因是外部编译 blocker，不是你 own 范围内的逻辑没修完。
2. 这次新 incident 的硬证据已经成立：
   - Unity 启动时报：
     - `Unknown error occurred while loading 'Assets/222_Prefabs/Rock/C1.prefab'`
     - `Unknown error occurred while loading 'Assets/222_Prefabs/Rock/C2.prefab'`
     - `Unknown error occurred while loading 'Assets/222_Prefabs/Rock/C3.prefab'`
   - 事故发生时，这 3 份 prefab 在磁盘上曾出现整文件 `NUL` 空字节的真实损坏。
3. 治理位已经做过一次只为止血的本地处理：
   - 原始坏文件已备份到：
     - `D:\Unity\Unity_learning\Sunset\.codex\artifacts\corruption-backups\rock-prefabs_2026-04-03_12-21-33`
   - 当前 working tree 里的 `C1/C2/C3.prefab` 已临时用 `HEAD` 内容写回，目的是先让仓库不再是“纯空文件”。
4. 上面这步只算止血，不算真正修复：
   - 还没有人核实这 3 份 prefab 回到 `HEAD` 后是否已经与当前 `StoneController` / Rock 资产链重新一致；
   - 也没人核实 `Assets/222_Prefabs/Rock/` 下是否还有同类序列化损坏或引用断裂。

## 二、本轮唯一主刀

只做这一刀：

1. 彻查并修复：
   - `Assets/222_Prefabs/Rock/C1.prefab`
   - `Assets/222_Prefabs/Rock/C2.prefab`
   - `Assets/222_Prefabs/Rock/C3.prefab`
2. 对 `Assets/222_Prefabs/Rock/` 做最小同类体检：
   - 只查是否还有同类“打不开 / 非法序列化 / 明显空洞引用”；
   - 不扩成整仓 prefab 大扫除。
3. 如果你发现真正根因直接落在以下支撑文件，允许最小联动：
   - `Assets/YYY_Scripts/Controller/StoneController.cs`
   - `Assets/Editor/StoneControllerEditor.cs`
4. 只有在你能证明“只修 prefab 还不够”时，才允许改上面两份支撑文件。

## 三、这轮允许的 scope

本轮允许进入的路径只有：

- `Assets/222_Prefabs/Rock/`
- `Assets/YYY_Scripts/Controller/StoneController.cs`（仅在直接相关时）
- `Assets/Editor/StoneControllerEditor.cs`（仅在直接相关时）
- 你自己的 memory / 工作区 memory

## 四、本轮明确禁止

- 不准碰 `Primary.unity`
- 不准碰 `Town.unity`
- 不准碰 `Assets/TextMesh Pro/`
- 不准扩到 `TreeController` 另一轮功能优化
- 不准顺手去修 NPC / Day1 / UI / 导航
- 不准把这轮扩成“树石系统全面重构”
- 不准把治理位的临时止血，直接当作“已经修好”

## 五、完成定义

你这轮只有一个过线标准：

1. `C1/C2/C3.prefab` 在当前 Unity 里不再报 `Unknown error occurred while loading ...`
2. 这 3 份 prefab 的内容不再是空洞/损坏序列化
3. 你已明确回答：
   - 当前只是把 `HEAD` 止血版确认过线
   - 还是做了进一步结构修复
4. 如果你顺手查了 `Rock` 同目录其他 prefab，必须明确说：
   - `还有同类问题`
   - 或 `未发现同类问题`
5. 如果你动了 `StoneController.cs` 或 `StoneControllerEditor.cs`，必须明确说明为什么 prefab 级修复本身不够

## 六、最容易犯的错

- 看到治理位已经把 `C1/C2/C3` 写回 `HEAD`，就误以为这题已经结束
- 把 scope 从 Rock prefab 扩到整套树石玩法
- 顺手把 `TreeController`、`Town`、`Primary` 也带进去
- 只说“现在不报错了”，却不交代根因到底是“止血够了”还是“还做了额外修复”

## 七、固定回执格式

回执按这个顺序：

- `A1 保底六点卡`
- `A2 用户补充层`
- `B 技术审计层`

其中 `A1 保底六点卡` 仍必须逐项显式写：

1. `当前主线`
2. `这轮实际做成了什么`
3. `现在还没做成什么`
4. `当前阶段`
5. `下一步只做什么`
6. `需要用户现在做什么`

## 八、A2 用户补充层必须额外显式回答

你必须额外补 5 条：

1. `C1/C2/C3 现在是否都能稳定加载`
   - 只能写 `是 / 否`
2. `这轮是否只靠 HEAD 止血版就过线`
   - 只能写 `是 / 否`
3. `这轮是否改了 StoneController 或 StoneControllerEditor`
   - 只能写 `是 / 否`
4. `Rock 同目录是否发现其他同类损坏`
   - 只能写 `是 / 否`
5. `如果没过线，第一真实 blocker 是什么`

一句话收口：

- **这轮只处理 Rock/C1~C3 prefab 的真实损坏与同类序列化健康，不扩题。**

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
