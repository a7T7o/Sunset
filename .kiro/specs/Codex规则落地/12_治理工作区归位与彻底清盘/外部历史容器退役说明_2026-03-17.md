# 外部历史容器退役说明（2026-03-17）

## 1. 文件身份
- 本文件用于说明为什么 `Sunset_external_archives` 与 `Sunset_backups` 现在可以退役。
- 它不是新的备份容器，而是这次退役动作的最终裁定说明。

## 2. 两个目录分别是什么

### `D:\Unity\Unity_learning\Sunset_external_archives`
- 身份：
  - 本轮 `11_main-branch-only回归与worktree退役收口` 时，为避免 shared root 被 `untracked` 干扰而临时移出的仓库外证据容器。
- 本轮核定到的内容：
  - `npc_restore.zip`
  - `Assets/Screenshots*`
- 结论：
  - 它们都不是当前 `farm / NPC / spring-day1` continuation branch 的必要 carrier。

### `D:\Unity\Unity_learning\Sunset_backups`
- 身份：
  - `2026-03-11` 历史恢复阶段留下的脏改备份集合。
- 本轮核定到的内容：
  - `git_dirty_backup_20260311_160631`
  - `git_dirty_backup_20260311_160659`
  - `pre_restore_current_20260311_165004`
- 结论：
  - 它们是历史恢复兜底，不再承担当前 Sunset 的正式承载职责。

## 3. 为什么现在可以退役
- shared root 已恢复为：
  - `D:\Unity\Unity_learning\Sunset @ main`
- `git worktree list --porcelain` 已只剩 shared root。
- 当前正式承载面已经稳定为：
  - `farm -> codex/farm-1.0.2-cleanroom001`
  - `NPC -> codex/npc-roam-phase2-002`
  - `spring-day1 -> codex/spring-day1-story-progression-001`
- 因此，仓库外这两个目录只剩“历史取证 / 兜底”的意义，不再属于当前可执行现场。

## 4. 最终动作
- 本轮裁定：
  - 这两个目录都执行退役删除。
- 删除后解释口径：
  - 历史文档里若仍出现这些路径，含义是“当时历史上曾存在过，现已退役”，不再代表当前 live 现场依赖它们。

## 5. 一句话口径
- `Sunset_external_archives` 与 `Sunset_backups` 都只属于历史容器，当前 Sunset 已不再需要依赖它们继续开发或继续治理。
