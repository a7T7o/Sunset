# 12 阶段设计：治理工作区归位与彻底清盘

## 1. 背景
- `11_main-branch-only回归与worktree退役收口` 已经完成 Git 与现场层面的终局收口。
- 但治理文档层仍残留一个结构性问题：
  - `000_代办/codex` 被当成了正式工作区。
- 这和用户的明确规则冲突：
  - 代办区只能记录和读取代办，不能承载工作区正文。

## 2. 本阶段要修的不是内容，而是承载关系
- 这轮不是继续新建大量治理规则。
- 这轮要修的是：
  - 工作区归位
  - 代办区降级
  - live 路由纠偏
  - 历史尾项封板
  - 仓库外历史容器退役

## 3. 结构选择
- 选择方案：单独新建 `Codex规则落地` 工作区。
- 不选择继续挂在 `Steering规则区优化` 下。
- 不选择继续让 `000_代办/codex` 兼任工作区。

## 4. 这样做的原因
- 用户已经明确指出：
  - 一直挂在 `Steering` 下面语义别扭。
  - `000_代办` 不是工作区。
- 新建单独工作区后，职责最清晰：
  - `Steering` 负责规则正文和 live 入口
  - `Codex规则落地` 负责治理执行正文
  - `000_代办/codex` 负责 TD 镜像

## 5. live 路由调整原则
- 所有“当前治理续办入口”都改指向：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地`
- 所有“代办读取入口”都改指向：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex`
- 旧历史记忆中的旧路径不要求逐条抹去历史事实。
- 但当前可执行入口、当前规则、当前 skill 说明必须全部纠正。

## 6. 对旧阶段的处理原则
- `01/02/07/09`：
  - 继续保留在正式工作区中
  - 但把剩余待办裁定并收掉
- `03/10/11`：
  - 已被后续动作覆盖
  - 本轮只做显式封板，不再继续扩写

## 7. 对仓库外目录的处理原则
- `Sunset_external_archives` 与 `Sunset_backups` 都已经失去当前 carrier 身份。
- 只需在正式工作区里留一份退役说明，再删除实体目录。

## 8. 结果预期
- 用户以后看 Sunset 时，只会看到：
  - 正式工作区在 `.kiro/specs/Codex规则落地`
  - 代办镜像在 `.kiro/specs/000_代办/codex`
  - shared root 默认回到 `main`
  - worktree 不再介入日常开发
