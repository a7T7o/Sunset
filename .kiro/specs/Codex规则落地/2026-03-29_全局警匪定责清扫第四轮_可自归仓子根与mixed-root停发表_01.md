# 2026-03-29｜全局警匪定责清扫第四轮｜可自归仓子根与 mixed-root 停发表｜01

## 本轮改题

第三轮的真实结果已经说明：

- `项目文档总览` 这类纯 docs-tail 线程，已经可以真实上 git 并停表
- 但其余线程里，很多不是“没尝试归仓”，而是被 `own roots` 的 folder-level 同根闸门卡住
- 如果继续让所有线程重复拿整根目录去撞 `preflight`，只会一轮轮复读同一个 blocker

所以第四轮不再按“整条 still-own 包一起撞 same-root”推进，而是改成两类处理：

1. `可自归仓子根`
   - 当前还能在不吞 mixed root 的前提下，先把自己真正干净的子根收掉
   - 这类线程继续发 prompt
2. `mixed-root 停发表`
   - 当前已经卡进 `Assets/Editor`、`Assets/YYY_Scripts/Service/Player`、`Assets/YYY_Scripts/Story/UI` 这种根级混线
   - 继续让线程自己撞，只会继续假装“差一点就 sync”
   - 这类线程本轮不再继续自转，转入治理位统一接盘

## 本轮四类裁定

### 1. 继续发 prompt

- `导航检查`
- `农田交互修复V3`
- `NPCV2`

这三条允许继续，但只准冲我已经替你们切好的 `可自归仓子根`，不准再把 mixed-root 一起带上。

### 2. 停给治理位接盘 mixed-root

- `导航检查V2`
- `UI-V1`

原因不是它们没干活，而是当前核心脏改压在：

- `Assets/YYY_Scripts/Service/Player`
- `Assets/YYY_Scripts/Story/UI`
- `Assets/YYY_Tests/Editor`

这些根下面现在明显已经是多线程混写；继续按单线程 `task preflight` 撞，只会一直被同根残留卡住。

### 3. 无需继续发

- `项目文档总览`

复核结论：

- `736028b0fb0c1bea1dbf348dda1c4550d1745a21`
- `6aaf4e9327b020994058a597a46d86ea08b5eb69`

两次 thread-doc 提交都真实存在，且当前 `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\项目文档总览` 已 clean。

### 4. 继续默认停发

- `spring-day1V2`
- `场景搭建（外包）`

说明：

- `spring-day1V2` 继续停发的是“活跃 feature / cleanup 自转”身份，不是删除它的历史结论
- 它当前残留出来的 mixed 面，我后续会按治理位 root backlog 处理，不再把它放回普通施工池

## 当前 mixed-root backlog

### A. `Assets/Editor`

当前混在一起的活跃对象至少包括：

- `Assets/Editor/Story/DialogueDebugMenu.cs`
- `Assets/Editor/NavigationStaticPointValidationMenu.cs`
- `Assets/Editor/NPCInformalChatValidationMenu.cs`
- `Assets/Editor/Story/SpringUiEvidenceMenu.cs`

这是根级 mixed，不再让普通线程各自顺手吞。

### B. `Assets/YYY_Scripts/Service/Player`

当前混在一起的活跃对象至少包括：

- `EnergySystem.cs`
- `HealthSystem.cs`
- `PlayerAutoNavigator.cs`
- `PlayerInteraction.cs`
- `PlayerNpcNearbyFeedbackService.cs`
- `PlayerNpcRelationshipService.cs`
- `PlayerNpcChatSessionService.cs`
- `PlayerThoughtBubblePresenter.cs`

这是本轮最典型的多线程根级混写面：

- Day1
- 导航
- NPC
- 农田

都在里面留了脏改。

### C. `Assets/YYY_Scripts/Story/UI`

当前混在一起的活跃对象至少包括：

- `SpringDay1PromptOverlay.cs`
- `SpringDay1WorkbenchCraftingOverlay.cs`
- `SpringDay1UiLayerUtility.cs`
- `SpringDay1UiPrefabRegistry.cs`
- `SpringUiEvidenceCaptureRuntime.cs`
- `SpringDay1WorldHintBubble.cs`
- `NpcWorldHintBubble.cs`

`UI-V1` 自己也已经在回执里承认：

- 当前 still-own 是 `Prompt / Workbench + registry + evidence + runtime test`
- `SpringDay1WorldHintBubble.cs` 不是 ordinary own

所以这根先不再让它单线程继续撞。

## 本轮执行纪律

1. 继续发 prompt 的线程，只准按我指定的 `可自归仓子根` 跑真实 `preflight -> sync`
2. 不准把 mixed-root 文件重新带回自己的白名单
3. 如果本轮 `sync` 成功，必须给 SHA
4. 如果本轮仍失败，必须给第一真实 blocker
5. `导航检查` 不准再写 `sync=yes` 但无 SHA 的矛盾口径

## 线程领取说明

继续施工的线程去各自目录读取：

- `导航检查`：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第四轮_可自归仓子根收口_01.md`
- `农田交互修复V3`：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\2026-03-29_全局警匪定责清扫第四轮_可自归仓子根收口_01.md`
- `NPCV2`：
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPCV2\2026-03-29_全局警匪定责清扫第四轮_可自归仓子根收口_01.md`

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- `已上 git / 第一真实阻断已钉死`

不要再长篇复述。
