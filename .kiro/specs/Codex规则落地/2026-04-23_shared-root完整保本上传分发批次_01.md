# 2026-04-23｜shared-root 完整保本上传分发批次 01

本轮性质：
- 这不是继续开发轮。
- 这也不是治理位代收大包轮。
- 当前唯一目标是：在 **绝不回退、绝不删改现有成果、绝不误吞 shared/mixed** 的前提下，让各线程把自己当前本地已经存在的成果安全归仓并推到 `origin`。

本轮只发 6 条线程：
- `spring-day1`
- `NPC`
- `UI`
- `农田交互修复V3`
- `存档系统`
- `导航检查`

各线程领取入口：
- `spring-day1`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给spring-day1_shared-root完整保本上传与own尾账归仓prompt_01.md`
- `NPC`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给NPC_shared-root完整保本上传与own尾账归仓prompt_01.md`
- `UI`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给UI_shared-root完整保本上传与own尾账归仓prompt_01.md`
- `农田交互修复V3`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给农田交互修复V3_shared-root完整保本上传与own尾账归仓prompt_01.md`
- `存档系统`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给存档系统_shared-root完整保本上传与own尾账归仓prompt_01.md`
- `导航检查`：
  `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-23_给导航检查_shared-root完整保本上传与own尾账归仓prompt_01.md`

统一硬要求：
1. 本轮只做 `own 上传`，不做新功能、不做体验补口、不做顺手重构。
2. 不允许因为“想让 status 更干净”而删掉任何当前本地已有成果。
3. 不允许吞并以下 shared/mixed 热面，除非 prompt 明确点名批准：
   - `Assets/000_Scenes/Home.unity`
   - `Assets/000_Scenes/Primary.unity`
   - `Assets/000_Scenes/Town.unity`
   - `ProjectSettings/*`
   - `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
4. 对于看起来像截图、备份、临时证据、探针产物的 untracked：
   - 不允许偷偷删
   - 要么作为 own 证据一起归仓
   - 要么在回执里显式报成“保留项 / 待治理位最后统一处理”
5. 如果 `git-safe-sync` 或 own-root clean 规则拦住：
   - 不准扩大 scope 硬吞
   - 直接报 exact blocker

统一聊天回执格式：
- 线程直接对用户回复时，必须先给：
  - `A1 保底六点卡`
  - `A2 用户补充层（可选）`
  - `B 技术审计层`
- 其中必须显式回答：
  - `当前主线`
  - `这轮实际做成了什么`
  - `现在还没做成什么`
  - `当前阶段`
  - `下一步只做什么`
  - `需要用户现在做什么`
- 技术审计层必须额外补：
  - `本轮提交 SHA`
  - `是否已 push 到 origin`
  - `当前 own 路径是否 clean`
  - `如果未 clean，exact blocker files`

本轮裁定口径：
- 当前正确顺序不是“谁看起来最大就先吞谁”。
- 当前正确顺序是：
  1. 各线程先只收自己的 own
  2. 治理位最后再接 shared/mixed
- 本批次的完成，不以 `git status overall clean` 为定义；
- 本批次的完成，只以“各线程 own 成果被安全归仓或被明确 blocker 化”作为定义。
