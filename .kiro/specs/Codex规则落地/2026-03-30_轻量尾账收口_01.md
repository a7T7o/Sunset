# 2026-03-30｜轻量尾账收口

## 一句话结论

这轮真正可以直接收掉的轻量项有两类：

1. 文档尾账
2. `.codex/artifacts` 的说明文件

## 轻量项 1：`.codex/artifacts`

### 现场判断

当前工作树里显示为：

- `?? .codex/artifacts/`

但这不是“整目录截图垃圾都该进仓库”。  
当前 `.gitignore` 已经明确写死了：

- `.codex/artifacts/**` 默认忽略
- 只放行：
  - `.codex/artifacts/`
  - `.codex/artifacts/README.md`

### 裁定

1. 实际截图与 JSON 侧车继续留在本地，不进仓库。
2. 说明文件 `README.md` 应作为正式目录说明纳入仓库。
3. 这轮不改 artifact 策略，不改保留期，不改 capture 流程。

## 轻量项 2：`项目文档总览` 线程 memory 尾账

### 现场判断

当前剩余的 docs-only 尾账是：

- `.codex/threads/Sunset/项目文档总览/memory_0.md`

它当前是纯文档追加，不是业务代码、scene、prefab 或 importer 事故面。

### 裁定

1. 这份 memory 尾账可以直接按 docs-only 路径收掉。
2. 不需要为它再开新的业务线程。
3. 不应再让它继续挂在仓库根里当“剩余 dirty”。

## 本轮执行范围

这轮轻量收口只包含：

1. `.codex/artifacts/README.md`
2. `.codex/threads/Sunset/项目文档总览/memory_0.md`

## 这轮故意不做什么

1. 不把 `.codex/artifacts` 里的截图和 JSON 侧车提交进仓库。
2. 不把轻量尾账扩写成新的治理规范改动。
3. 不借“收文档尾账”顺手吞别的 shared root。
