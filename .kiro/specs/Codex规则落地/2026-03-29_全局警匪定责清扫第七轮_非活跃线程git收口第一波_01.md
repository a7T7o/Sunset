# 2026-03-29｜全局警匪定责清扫第七轮｜非活跃线程 git 收口第一波

## 这轮主线

活跃功能施工线程已经清空。  
这一轮不再继续发功能 prompt，而是进入 `非活跃线程 git 收口波次` 的第一波。

第一波只做两类最稳、最不容易误吞的收口：

1. 明确 still-own 的 thread-doc / docs-tail 小尾账
2. 明确 still-own 的独立代码残包

这轮**不碰**两类大混包：

1. `spring-day1(UI-V1)` 的 Day1 UI 迁移大包
2. `导航检查V2` 的 `Assets/YYY_Scripts/Service/Player` mixed-root 残包

原因很简单：

- `UI-V1` 当前同根还夹着 `NpcWorldHintBubble.cs` 这类 NPC foreign
- `导航检查V2` 当前同根还夹着 `PlayerNpcChatSessionService.cs` 这类 NPC 残包
- 先把 NPC 旧残包剥走，再做第二波，整体会更快、更干净

## 本轮四类裁定

### 继续发 prompt

1. `NPC`
   - 目标：收掉老 `NPC` 线程挂着的非正式聊天残包
   - 当前 live 仍可见：
     - `Assets/Editor/NPCInformalChatValidationMenu.cs`
     - `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs`
     - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
     - `Assets/YYY_Scripts/Story/UI/NpcWorldHintBubble.cs`
     - `Assets/YYY_Tests/Editor/NPCInformalChatInterruptMatrixTests.cs`
     - `.codex/threads/Sunset/NPC/memory_0.md`
   - 这是高杠杆清理项，因为它同时给 `UI-V1` 与 `导航检查V2` 去 foreign / mixed 噪音

2. `spring-day1V2`
   - 目标：不再继续 Day1 feature，只收 docs-tail + font-stopgap 底账
   - 当前 live 仍可见：
     - `.codex/threads/Sunset/spring-day1V2/*`
     - `.kiro/specs/900_开篇/0.0阶段/0.0.3V2/*`
     - `.kiro/specs/900_开篇/spring-day1-implementation/memory.md`
     - `.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/memory.md`
     - `Assets/111_Data/UI/Fonts/Dialogue/DialogueFontLibrary_Default.asset`

3. `项目文档总览`
   - 目标：只收 thread-memory 尾账
   - 当前 live 只剩：
     - `.codex/threads/Sunset/项目文档总览/memory_0.md`

4. `场景搭建（外包）`
   - 目标：只收 thread-memory 尾账
   - 当前 live 只剩：
     - `.codex/threads/Sunset/场景搭建（外包）/memory_0.md`

### 暂缓到第二波

1. `spring-day1(UI-V1)`
   - 暂缓原因：
     - `Assets/YYY_Scripts/Story/UI` 同根当前仍有 `NpcWorldHintBubble.cs`
     - 现在强发，只会让它再次卡在 NPC foreign / same-root gate
   - 下一波前置条件：
     - 先看 `NPC` 这一轮是否把 `NpcWorldHintBubble.cs` 真认走并归仓

2. `导航检查V2`
   - 暂缓原因：
     - `Assets/YYY_Scripts/Service/Player` 同根当前仍夹着 `PlayerNpcChatSessionService.cs`
     - 现在强发，只会继续把 NPC 残包和玩家导航残包混成一坨
   - 下一波前置条件：
     - 先看 `NPC` 这一轮是否把 `PlayerNpcChatSessionService.cs` 真认走并归仓

### 无需继续发

1. `NPCV2`
2. `导航检查`
3. `农田交互修复V3`

这三条当前都已经有 live 提交，且其本轮 own scope 不再是继续发 prompt 的优先对象。

## 本轮目标

只拿 4 条回执，且只认 2 种结果：

1. `A｜已真实 sync，并给出 SHA`
2. `B｜第一真实 blocker 已钉死`

不接受新的功能汇报，不接受“顺手再做一点业务”，也不接受“我还没 clean，但先讲讲原因”式空转。

## 本轮后恢复点

如果第一波顺利：

1. `NPC` 会先把 `UI-V1` 与 `导航检查V2` 的关键 foreign / mixed 噪音剥走
2. 第二波再专门处理：
   - `spring-day1(UI-V1)` Day1 UI 迁移包
   - `导航检查V2` 玩家侧导航 mixed-root 包

如果第一波仍有人卡住：

1. 先按 `第一真实 blocker` 做更细定责
2. 仍然不回头发功能 prompt

