# 2026-03-30｜全局警匪定责清扫第九轮｜第一批开工线与冻结线

## 这轮主线

第八轮之后，旧线程自提阶段已经结束。  
这轮不再问“谁还能自己碰碰运气 sync 一下”，而是直接回答：

**今天到底哪条线能先开工。**

## 当前现场

`UI系统 + 屎山修复` docs 迁移大包已由治理位真实吸收，最新提交：

- `463d46ac 2026.03.30_对话丢失修复_01`

吸掉这包之后，live 未提交现场已降到：

- `TOTAL = 82`
- `TRACKED = 39`
- `UNTRACKED = 43`
- `DELETED = 2`

这说明现在可以开始按代码根做真正的开工裁定了。

## 第一批裁定

### 继续发 prompt

1. `导航检查V2`

#### 原因

现在最像“能先开”的，不是 `NPC`，也不是 `spring-day1(UI-V1)`，而是：

**`Assets/YYY_Scripts/Service/Player` 整根。**

这根当前完整现场是：

- `EnergySystem.cs`
- `HealthSystem.cs`
- `PlayerAutoNavigator.cs`
- `PlayerInteraction.cs`
- `PlayerNpcNearbyFeedbackService.cs`
- `PlayerNpcRelationshipService.cs`
- `PlayerThoughtBubblePresenter.cs`
- `PlayerNpcChatSessionService.cs`
- `PlayerNpcChatSessionService.cs.meta`

这是一整根真实 mixed-root，但它至少是：

- 同一代码根
- 同一运行侧
- 以玩家侧服务链为主

因此这轮不再让 `导航检查V2` 只盯 `PlayerAutoNavigator.cs`，而是直接把它升级成：

**`Assets/YYY_Scripts/Service/Player` root-integrator 首开线**

这不是让它“洗作者归属”，而是让它：

- 以 integrator 身份面对整根
- 显式区分 still-own / carried foreign / 明确不能带
- 再按整根决定 `preflight -> sync` 或第一真实 blocker

### 暂不发 prompt

1. `spring-day1(UI-V1)`
2. `NPC`

#### 原因

今天这两条如果硬开，只会继续撞在：

- `Assets/YYY_Scripts/Story/Interaction`
- `Assets/YYY_Scripts/Story/Managers`
- `Assets/YYY_Scripts/Story/UI`
- `Assets/YYY_Tests/Editor`
- `Assets/Editor`

这些真正的 shared story roots 上。

也就是说：

- `UI` 和 `NPC` 现在不是“各自差一点”
- 而是**物理上缠在同一批 `Story` 根里**

所以这两条今天不该虚开。  
它们下一步要等治理位先把 `Story` 大根切成真正能落地的 integrator 包，再发。

### 继续冻结

1. `Primary.unity` 迁移包
2. TMP 中文字体 churn
3. `Assets/Editor` shared menus

#### 原因

这些还没有形成足够小、足够稳的一刀。  
现在先别拿它们去挡第一条能开的代码线。

## 当前恢复点

这轮结束后，当前唯一正确的下一步顺序是：

1. 先让 `导航检查V2` 以 root-integrator 身份吃下 `Assets/YYY_Scripts/Service/Player`
2. 治理位继续拆 `Assets/YYY_Scripts/Story/**`
3. 然后再决定 `spring-day1(UI-V1)` 和 `NPC` 谁先开、开哪一包

一句话收口：

**今天真正能开的第一条线是 `导航检查V2 -> Assets/YYY_Scripts/Service/Player`；`UI` 和 `NPC` 今天还不能假开。**
