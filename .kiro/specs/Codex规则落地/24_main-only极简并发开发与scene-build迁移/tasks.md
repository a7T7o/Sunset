# 阶段任务：main-only 极简并发开发与 scene-build 迁移

## 用户原文
> 1、首先你刚刚的prompt还是夹杂在你之前的文件夹里面，这是不对的，这是很差劲的整理习惯，因为已经是完全和前面的阶段完全不一致的内容了，请你整理一下  
> 2、在你完成之前我就已经让spring-day1和场景搭建继续干活了，因为他们是时间消耗大头，而且对于场景搭建应该独立在他的工作树里面，然后只在那里面工作，无论如何都不会回来，因为他只是场景搭建，搞好了我就复制到mian，然后spring-day1是交接，把他的内容交接给场景搭建，但是场景搭建的工作其实还是和之前我说的问题一样，你的codex线程显示和界面内的git分支切换还是出问题，这个是老问题，现在他所在的工作树其实是应该定性为另一个项目的文件夹，他不应该还在sunset的文件夹下面，因为在这里他始终不能被我手动切换到场景搭建分支来确保他不会在现在的宽松环境下出错，你懂我意思吗？所以我需要你除了刚刚的prompt，还需要出两个prompt来获取刚刚完成我的任务的场景搭建和spring-day1线程，以及场景搭建应该被你用之前已经实现过的移动方式去移动到他应该在的项目文件夹下面，现在这样会很混乱  
> 3、对于你当前的瓦解是否真的成立？这是你的课题，而且你的文件太多了，你必须要彻底重新规划一下，你的命名和分区还是太差劲了，我很难找也很难翻，完全就是胡闹，你的问题很大，你先要完成上面的所有内容然后再开始你的文件管理以及重新彻底开始做我当前需要的vibecoding的场景的规范适配，你现在只是临时豁口，这不是长久之道  
>
> 对于以上三点主要内容每一点里面都有特别多的小点，你需要彻底剖析把每句话反应出来的需求和设计厘清，先把我的原文记入代办文档的头部，然后再在下面写入你对每一句的理解和这一句所给出的任务，然后再开始按优先级一步一步完成  
> 好的其实也不是那么详细，你应该也懂我意思，我不希望你忽略我的任何一点小的需求，请你彻底深度思考，高效的完成这一切

## 逐条理解与对应任务
### 1. 新 prompt 不能继续夹在旧阶段目录里
**理解**
- 当前 `main-only` 极简并发开发已经不是旧“恢复开发总控”阶段的延长线。
- 如果继续把现行 prompt 放在旧阶段目录里，项目经理会继续把当前阶段误读成旧阶段的尾巴。
- 这不是整理习惯问题，而是“当前入口能不能一眼看懂”的问题。

**对应任务**
- [x] 新建独立阶段目录，承接 `main-only` 极简并发开发。
- [x] 把当前直接可发 prompt 补进新目录。
- [x] 把旧目录显式标成历史阶段。
- [x] 更新现行入口文件，让它们指向新目录。

### 2. scene-build / spring-day1 已在跑，scene-build 还要迁出 Sunset 文件夹
**理解**
- `scene-build` 和 `spring-day1` 不是等待态，而是正在消耗时间的活线程。
- `spring-day1` 当前职责不是再开一个大施工面，而是把可用内容交给 `scene-build`。
- `scene-build` 当前虽然还在 worktree 里跑，但从项目经理视角，它已经应该被定性为“另一个项目文件夹”，而不是 Sunset 普通线程。
- 因为这两条线已经在跑，所以不能拍脑袋直接搬；必须先收回执、先冻结、再迁移。

**对应任务**
- [x] 产出 `scene-build_当前任务回执与迁移前冻结.md`
- [x] 产出 `spring-day1_当前任务回执与向scene-build交接.md`
- [x] 明确 `scene-build` 的目标迁移路径
- [x] 明确 `scene-build` 的迁移前提
- [x] 明确 `scene-build` 的迁移方式固定为 `git worktree move`
- [x] 在总控中把 `scene-build` 定性为“待迁移的独立项目现场”

### 3. 当前瓦解不能只是临时豁口，命名与分区必须真的重做
**理解**
- 用户接受简化，但不接受为了简化把目录搞得更乱。
- 当前痛点不是只有规则多，还包括：
  - 文件太多
  - 命名差
  - 入口多
  - 旧阶段和现行阶段混在一起
- 这轮的目标是先把当前混乱收掉，再为后续 `vibecoding` 场景规范适配留出干净入口。

**对应任务**
- [x] 重定义“当前阶段目录 / 历史阶段目录 / 当前 prompt 目录”的边界
- [x] 用更短、更直观的命名重建当前入口
- [x] 把“直接开发 prompt”和“回执 / 冻结 / 交接 prompt”分开
- [x] 不把这轮临时豁口伪装成长久方案完成
- [ ] 单开 `vibecoding` 场景规范适配阶段

## 当前优先级
### P0
- [x] 建立新阶段目录与新 prompt 入口
- [x] 产出 `scene-build` 回执 / 冻结 prompt
- [x] 产出 `spring-day1` 回执 / 交接 prompt
- [x] 把总控入口改到新目录

### P1
- [x] 明确 `scene-build` 迁移目标目录、迁移前条件、迁移动作
- [x] 把旧目录标记为历史，不再作为当前入口

### P2
- [ ] 为后续 `vibecoding` 场景规范适配单独立项

## 当前正式迁移口径
- `scene-build` 当前现场：
  - `D:\Unity\Unity_learning\Sunset_worktrees\scene-build-5.0.0-001`
- `scene-build` 目标路径：
  - `D:\Unity\Unity_learning\SceneBuild_Standalone\scene-build-5.0.0-001`
- 迁移方式：
  - `git worktree move "D:\Unity\Unity_learning\Sunset_worktrees\scene-build-5.0.0-001" "D:\Unity\Unity_learning\SceneBuild_Standalone\scene-build-5.0.0-001"`
- 迁移前提：
  1. 先收 `scene-build_当前任务回执与迁移前冻结.md`
  2. 当前一刀已 checkpoint，或至少已明确冻结点
  3. 当前没有正在写的 Unity / MCP live 会话
  4. 目标路径不存在冲突目录

## 执行进展
### 已完成
- [x] 新阶段目录已经正式建立为：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01`
- [x] 新目录下已经补齐当前直接开发 prompt：
  - `scene-build_当前开发放行.md`
  - `NPC_当前开发放行.md`
  - `农田交互修复V2_当前开发放行.md`
  - `导航检查_当前开发放行.md`
  - `遮挡检查_当前开发放行.md`
  - `spring-day1_当前开发放行.md`
- [x] 已补齐两个新回执 prompt：
  - `scene-build_当前任务回执与迁移前冻结.md`
  - `spring-day1_当前任务回执与向scene-build交接.md`
- [x] 现行入口文件已全部改指向新目录
- [x] 旧目录 `2026.03.21_恢复开发总控_01` 已被明确标记为历史阶段

### 当前剩余事项
- [ ] 等项目经理向 `scene-build` 分发 `scene-build_当前任务回执与迁移前冻结.md`
- [ ] 等项目经理向 `spring-day1` 分发 `spring-day1_当前任务回执与向scene-build交接.md`
- [ ] 基于两份真实回执，执行 `scene-build` 的正式迁移
- [ ] 在上面三项完成后，单开 `vibecoding` 场景规范适配阶段

## 完成标准
- [x] 项目经理不再需要从旧批次目录里翻当前 prompt
- [x] `scene-build` 与 `spring-day1` 都有当前可直接发的回执型 prompt
- [x] `scene-build` 的“为什么要迁出 Sunset 文件夹、什么时候迁、怎么迁”被明确写死
- [x] 当前极简并发开发有新的清晰入口，不再只是临时聊天结论
- [ ] `scene-build` 已完成正式迁移
- [ ] `vibecoding` 场景规范适配已单独立项
