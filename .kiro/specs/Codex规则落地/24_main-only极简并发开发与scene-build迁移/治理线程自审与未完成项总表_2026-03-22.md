# 治理线程自审与未完成项总表

## 审计目的
- 把当前治理线程自己留下的未完成项、误判残留、文档债、以及已经被用户裁定作废但文档还没清干净的内容一次性摊开。
- 这份文档不替代历史 `memory.md`，而是把分散在 `24`、`23`、根层 `memory`、TD 镜像和线程记忆里的欠账做一次集中对账。

## 审计范围
- [tasks.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\tasks.md)
- [memory.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\memory.md)
- [memory.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\memory.md)
- [tasks.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\23_前序阶段补漏审计与并发交通调度重建\tasks.md)
- [TD_14_自动化skill触发日志Hook待办.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex\TD_14_自动化skill触发日志Hook待办.md)
- [memory_0.md](D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\Codex规则落地\memory_0.md)

## 先给结论
- 我这条治理线已经真正做完的核心成果，不是“又建了一堆规则”，而是：
  - 把 `main-only` 极简并发开发的当前入口和可分发 prompt 建起来了。
  - 把 `scene-build` 的 Git 误迁移认知收回，并把唯一正式现场重新钉死在旧 worktree。
  - 把 `scene-build` 在 Codex 桌面端里的应用层归位问题定位并修到了 `sqlite / rollout / session_index / global-state` 四层。
  - 把 `scene-build` 的 4 个 `DialogueChinese*` TMP dirty 从错误回退里纠正出来，重新定回 `spring-day1` 保护资产。
- 但我这条线没有完全收口，真实剩余问题仍然有 5 类。
- 其中有些是“真没做完”，有些是“已经被用户口头推翻，但文档还残着假待办”，还有一类是“我自己的治理文档已经进入坏编码/坏入口状态”。

## A. 真正还没做完的内容

### A1. `scene-build` 的 Codex 归位还缺最后一脚验收回执
- 当前状态：
  - 我已经把 `scene-build` 线程的应用层绑定修回旧 worktree，也补掉了 rollout 里最后 1 条旧 `cwd`。
  - 但在最终四层修复完成后，没有拿到一条新的、明确的“现在已经不再回跳 `Sunset`”用户实测回执。
- 为什么它仍算未完成：
  - 这是本轮治理线最关键的一项真实成果，但现在停在“我判断应该好了”，还没形成最终闭环证据。
- 当前正确口径：
  - 这不是 Git 迁移问题。
  - 这是 Codex 应用层归位的最后验收未收件。

### A2. `vibecoding` 场景规范适配阶段还没真正立项
- 证据来源：
  - `24/tasks.md` 里仍保留：
    - `单开 vibecoding 场景规范适配阶段`
    - `为后续 vibecoding 场景规范适配单独立项`
- 当前状态：
  - 我没有把这一阶段真正建出来。
  - 也没有把它明确裁定为废弃。
- 结论：
  - 这是当前治理线里最明确、最实在的一条功能性未完成项。

### A3. 当前批次目录下的治理 prompt 已改动，但还没做统一收口
- 现场：
  - [README.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_main-only极简并发开发_01\README.md)
  - 多个 `scene-build / spring-day1 / 治理线程` prompt 文件仍在 dirty 状态
- 当前状态：
  - 它们已经被我按“只认旧 worktree、只认 main-only”做过一轮修正。
  - 但我还没有给这批入口文件做最后一版“哪些继续保留、哪些只留历史、哪些应该冻结”的明确收口。
- 结论：
  - 这是一组文档侧未提交、未封板的治理尾项。

### A4. `TD_14` 自动化 `skill-trigger-log` hook 仍是低优先级挂账
- 证据来源：
  - [TD_14_自动化skill触发日志Hook待办.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex\TD_14_自动化skill触发日志Hook待办.md)
- 当前状态：
  - 这条待办没有被正式转正成一个新阶段。
  - 也没有被执行。
- 当前判断：
  - 它不是当下主线阻塞。
  - 但它确实是治理线自己留下的一条低优先级技术债，不该装作不存在。

### A5. 我还没做一版“治理线当前 dirty 归属收口”
- 当前状态：
  - 仓库里现在混着 NPC、spring-day1、旧剧情目录删除、治理文档修改等多股 dirty。
  - 我虽然能口头区分哪些是我的、哪些是别人的，但还没在治理侧落成一份正式的“本线 dirty 归属说明”。
- 影响：
  - 后续如果继续在 shared root 上做治理，很容易再次出现“混锅”和“误收件”。

## B. 历史上看起来没完成，但现在应视为作废或降级的内容

### B1. `23` 阶段剩余的 queue / 多 checkpoint 调度模型
- 证据来源：
  - [23/tasks.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\23_前序阶段补漏审计与并发交通调度重建\tasks.md)
- 仍然挂着的条目包括：
  - 补写根层入口说明，指明主线转入 `23`
  - 定义多 checkpoint 持续推进模型
  - 定义 Unity / MCP 单实例下调度边界
  - 让并发交通调度模型形成正式正文
- 当前判断：
  - 这些条目是在旧 queue-aware / 租约式治理模型下留下的。
  - 用户已经明确把治理口径裁到 `main-only` 极简并发开发。
  - 所以它们不该继续算“当前必须完成的主线任务”。
- 正确归类：
  - 历史遗留。
  - 有参考价值。
  - 但当前不应继续按原计划推进。

### B2. `scene-build` “正式迁移到新路径”整组任务
- 证据来源：
  - [24/tasks.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\tasks.md)
- 仍残留的条目包括：
  - 等项目经理分发迁移前冻结 prompt
  - 基于真实回执执行正式迁移
  - `scene-build` 已完成正式迁移
  - 等从新路径回执后再恢复精修
- 当前判断：
  - 这整组任务已经被用户明确推翻。
  - 现行正确口径是：不再做物理迁移，只认旧 worktree，真正要修的是 Codex 映射。
- 正确归类：
  - 已作废。
  - 不是待办。
  - 现在的问题是文档还没清干净。

## C. 我自己留下的治理文档债

### C1. `24/tasks.md` 已经变成“半过时 + 半乱码”的坏入口
- 事实：
  - 文件仍保留了大量已作废的迁移任务。
  - 文件尾部还出现了编码异常的段落。
- 影响：
  - 这个文件现在不能再被视为可靠入口。
  - 它会把后来接手的人再次带回错误迁移叙事。

### C2. `24/memory.md` 已经不是健康 UTF-8
- 事实：
  - 该文件当前已被检测到不是有效 UTF-8。
  - 这意味着它已经进入“还能凑合读，但不适合继续当健康活文档叠写”的状态。
- 影响：
  - 后续如果继续在这个文件上机械叠加，很容易让当前阶段工作区越来越不可维护。

### C3. 根层 `memory.md` 虽然健康，但记的是完整历史，不是“当前总表”
- 事实：
  - 根层记忆是健康的。
  - 但它是历史流水追加，不等于“当前治理线一眼看懂的现状总表”。
- 结论：
  - 所以才需要补这份自审总表。

## D. 我自己的错误历史，不洗白

### D1. 我先后造过两次错误路径认知
- 第一次：
  - `SceneBuild_Standalone`
- 第二次：
  - `D:\Unity\Unity_learning\scene-build-5.0.0-001`
- 问题本质：
  - 用户要的是 Codex 归位。
  - 我却把它误解成 Git / 目录层迁移。

### D2. 我把“Codex 映射问题”误做成“Git worktree 问题”
- 直接后果：
  - 浪费时间。
  - 制造错误副本。
  - 让 `24/tasks.md` 和根层记忆一度沿着错误叙事越写越深。

### D3. 我在明知 `TMP dirty` 有既有归属的情况下，还把件退回给 `scene-build`
- 这是纯治理失误。
- 不是线程没汇报清楚。
- 也不是信息不足。

### D4. 我让当前阶段文档自己进入了坏编码状态
- `24/tasks.md`
- `24/memory.md`
- 这说明我在治理线最该保持干净的时候，没有把文档健康度一起守住。

## E. 当前 dirty 归属的粗分

### E1. 明显属于我这条治理线的 dirty
- [memory.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\memory.md)
- [memory.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\memory.md)
- [tasks.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\tasks.md)
- [memory_0.md](D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\Codex规则落地\memory_0.md)
- `2026.03.21_main-only极简并发开发_01` 批次目录下被我改过的一组入口文件
- 新增的 [scene-build线程归位修复最高优先级方案_2026-03-22.md](D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\24_main-only极简并发开发与scene-build迁移\scene-build线程归位修复最高优先级方案_2026-03-22.md)

### E2. 明显不是我这条治理线本轮应该认领的 dirty
- NPC 代码与 prefab 改动
- `spring-day1` 的 memory / requirements / handoff 改动
- 旧剧情目录 `0.0.1剧情初稿` 那批删除与新增

## F. 现在最诚实的状态判断
- 如果只看“我做过什么”，这条线成果不少。
- 如果看“我有没有把自己的文档和入口收得干净”，答案是不够。
- 当前最真实的未完成结构是：
  1. `scene-build` 归位缺最终验收回执
  2. `vibecoding` 场景规范适配没有立项
  3. `24` 阶段文件本身需要重写/换新入口
  4. 当前批次 prompt 目录还没统一封板
  5. `TD_14` 仍是低优先级挂账

## G. 建议的收口顺序
1. 先收 `scene-build` 归位最终实测回执，确认这条线真正闭环。
2. 立刻把 `24/tasks.md` 和 `24/memory.md` 定为“不再继续叠写的坏入口”，改由新健康文档接管。
3. 单开 `vibecoding` 场景规范适配阶段，不再把它挂在旧迁移任务下面。
4. 对 `2026.03.21_main-only极简并发开发_01` 批次目录做一次保留/归档裁定。
5. 最后再决定要不要把 `TD_14` 升级成正式阶段。
