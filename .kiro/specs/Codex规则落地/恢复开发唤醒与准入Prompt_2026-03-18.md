# 恢复开发唤醒与准入 Prompt（2026-03-18）

## 当前 shared root 健康基线
- shared root：`D:\Unity\Unity_learning\Sunset`
- 当前 live 分支：`main`
- 当前 live HEAD：`b0ceca78`
- 当前 `git status --short --branch`：clean
- 当前 occupancy：`main + neutral + branch_grant_state = none`
- 统一模型：`main-common + branch-task + checkpoint-first + merge-last`

## 分发规则
- 阶段一可以并发分发，但必须是纯只读。
- 阶段二不能并发抢 shared root；谁拿到 grant，谁独占进入一次 `ensure-branch -> checkpoint -> return-main`。
- 任何线程只要进入 Unity / MCP / Play Mode，都要把它当成共享单实例资源；做完就退回 `Edit Mode` 再交还现场。
- 任何线程调用 `git-safe-sync.ps1` 都必须显式带 `-OwnerThread`。

## NPC

### 阶段一｜纯只读唤醒 Prompt
```text
【阶段一｜只读唤醒｜NPC】

当前 Sunset 已恢复到统一健康基线：
- shared root = D:\Unity\Unity_learning\Sunset
- branch = main
- HEAD = b0ceca78
- git status clean
- shared root = neutral

你这一轮先不要写文件、不要切分支、不要进入 Play Mode。
你的第一条 commentary 必须显式说明你正在使用 skills-governor、sunset-workspace-router，并先完成只读 preflight。

请只读完成以下动作：
1. 在 shared root 复核 cwd / branch / HEAD / git status --short --branch。
2. 读取：
   - D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\memory_0.md
   - D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\memory.md
   - 当前相关 NPC 子工作区 memory
3. 只读确认当前 continuation branch 是否仍是：
   - codex/npc-roam-phase2-003 @ 7385d123
4. 只读判断你下一轮真实恢复开发是否会碰：
   - Unity / MCP
   - Play Mode
   - A 类热文件

本轮禁止：
- 禁止写文件
- 禁止 ensure-branch
- 禁止进入 Play Mode

回报格式固定为：
- cwd / branch / HEAD / git status
- 读了哪些文档
- 是否确认 codex/npc-roam-phase2-003 仍是唯一 continuation branch
- 下一轮若恢复真实开发，最小 checkpoint 是什么
```

### 阶段二｜准入 Prompt
```text
【阶段二｜准入｜NPC】

你已通过阶段一，只要你的 live 复核仍显示 shared root 处于：
- D:\Unity\Unity_learning\Sunset
- main
- clean
- occupancy = neutral

现在授权你按 NPC 的唯一 continuation branch 进入真实开发：
- codex/npc-roam-phase2-003 @ 7385d123

请严格执行：
1. 先执行 grant：
   git-safe-sync.ps1 -Action grant-branch -OwnerThread "NPC" -BranchName "codex/npc-roam-phase2-003"
2. 再执行准入：
   git-safe-sync.ps1 -Action ensure-branch -OwnerThread "NPC" -BranchName "codex/npc-roam-phase2-003"
3. 进入分支后先做最小 live 基线复核，再开始你的真实 checkpoint。
4. 如果你进入 Unity / MCP / Play Mode，做完后必须先退回 Edit Mode。
5. 一个 checkpoint 完成后：
   - commit + push
   - 如涉及锁则释放锁
   - 执行 return-main 归还 shared root

本轮禁止：
- 禁止在 main 上写 NPC 业务内容
- 禁止绕过 grant 直接 ensure-branch
- 禁止做完 Play Mode 不退回 Edit Mode

回报格式固定为：
- grant 是否成功
- ensure-branch 是否成功
- 当前 branch / HEAD
- 本轮最小 checkpoint 是什么
- 是否进入过 Unity / MCP / Play Mode，若进入是否已退回 Edit Mode
```

## farm

### 阶段一｜纯只读唤醒 Prompt
```text
【阶段一｜只读唤醒｜农田交互修复V2】

当前 Sunset 已恢复到统一健康基线：
- shared root = D:\Unity\Unity_learning\Sunset
- branch = main
- HEAD = b0ceca78
- git status clean
- shared root = neutral

你这一轮先不要写文件、不要切分支、不要进入 Play Mode。
你的第一条 commentary 必须显式说明你正在使用 skills-governor、sunset-workspace-router，并先完成只读 preflight。

请只读完成以下动作：
1. 在 shared root 复核 cwd / branch / HEAD / git status --short --branch。
2. 读取：
   - D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V2\memory_0.md
   - D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\memory.md
   - 当前相关 farm 子工作区 memory
3. 只读确认当前 continuation branch 是否仍是：
   - codex/farm-1.0.2-cleanroom001 @ e4ec0d8e
4. 只读判断你下一轮真实恢复开发是否会碰：
   - Unity / MCP
   - Play Mode
   - A 类热文件

本轮禁止：
- 禁止写文件
- 禁止 ensure-branch
- 禁止恢复旧 worktree 作为默认现场
- 禁止进入 Play Mode

回报格式固定为：
- cwd / branch / HEAD / git status
- 读了哪些文档
- 是否确认 codex/farm-1.0.2-cleanroom001 仍是唯一 continuation branch
- 下一轮若恢复真实开发，最小 checkpoint 是什么
```

### 阶段二｜准入 Prompt
```text
【阶段二｜准入｜农田交互修复V2】

你已通过阶段一，只要你的 live 复核仍显示 shared root 处于：
- D:\Unity\Unity_learning\Sunset
- main
- clean
- occupancy = neutral

现在授权你按 farm 的唯一 continuation branch 进入真实开发：
- codex/farm-1.0.2-cleanroom001 @ e4ec0d8e

请严格执行：
1. 先执行 grant：
   git-safe-sync.ps1 -Action grant-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001"
2. 再执行准入：
   git-safe-sync.ps1 -Action ensure-branch -OwnerThread "农田交互修复V2" -BranchName "codex/farm-1.0.2-cleanroom001"
3. 进入分支后先做最小 live 基线复核，再开始真实 checkpoint。
4. 如果你进入 Unity / MCP / Play Mode，做完后必须先退回 Edit Mode。
5. 一个 checkpoint 完成后：
   - commit + push
   - 如涉及锁则释放锁
   - 执行 return-main 归还 shared root

本轮禁止：
- 禁止在 main 上写 farm 业务内容
- 禁止绕过 grant 直接 ensure-branch
- 禁止把 shared root 当成长驻私有现场

回报格式固定为：
- grant 是否成功
- ensure-branch 是否成功
- 当前 branch / HEAD
- 本轮最小 checkpoint 是什么
- 是否进入过 Unity / MCP / Play Mode，若进入是否已退回 Edit Mode
```

## spring-day1

### 阶段一｜纯只读唤醒 Prompt
```text
【阶段一｜只读唤醒｜spring-day1】

当前 Sunset 已恢复到统一健康基线：
- shared root = D:\Unity\Unity_learning\Sunset
- branch = main
- HEAD = b0ceca78
- git status clean
- shared root = neutral

你这一轮先不要写文件、不要切分支、不要进入 Play Mode。
你的第一条 commentary 必须显式说明你正在使用 skills-governor、sunset-workspace-router，并先完成只读 preflight。

请只读完成以下动作：
1. 在 shared root 复核 cwd / branch / HEAD / git status --short --branch。
2. 读取：
   - D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md
   - D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md
   - 当前相关 spring-day1 子工作区 memory
   - D:\Unity\Unity_learning\Sunset\.kiro\steering\ui.md
3. 只读确认当前 continuation branch 是否仍是：
   - codex/spring-day1-story-progression-001 @ 27dc06a1
4. 只读判断你下一轮真实恢复开发是否会碰：
   - Primary.unity
   - DialogueUI.cs
   - 共享字体 / 共享 UI 资源
   - Unity / MCP / Play Mode

本轮禁止：
- 禁止写文件
- 禁止 ensure-branch
- 禁止进入 Play Mode
- 禁止把“能显示/能跑”当成通过；样式、气泡、字体、布局、美观度仍是硬验收项

回报格式固定为：
- cwd / branch / HEAD / git status
- 读了哪些文档
- 是否确认 codex/spring-day1-story-progression-001 仍是唯一 continuation branch
- 下一轮若恢复真实开发，是否会碰热文件 / 共享 UI 资源
- 你的最小 checkpoint 是什么
```

### 阶段二｜准入 Prompt
```text
【阶段二｜准入｜spring-day1】

你已通过阶段一，只要你的 live 复核仍显示 shared root 处于：
- D:\Unity\Unity_learning\Sunset
- main
- clean
- occupancy = neutral

现在授权你按 spring-day1 的唯一 continuation branch 进入真实开发：
- codex/spring-day1-story-progression-001 @ 27dc06a1

请严格执行：
1. 先执行 grant：
   git-safe-sync.ps1 -Action grant-branch -OwnerThread "spring-day1" -BranchName "codex/spring-day1-story-progression-001"
2. 再执行准入：
   git-safe-sync.ps1 -Action ensure-branch -OwnerThread "spring-day1" -BranchName "codex/spring-day1-story-progression-001"
3. 进入分支后先做最小 live 基线复核，再开始真实 checkpoint。
4. 如果这轮会碰：
   - Primary.unity
   - DialogueUI.cs
   - 共享字体 / 共享 UI / 材质
   先查锁 / 查热区，再写。
5. 如果你进入 Unity / MCP / Play Mode，做完后必须先退回 Edit Mode。
6. 一个 checkpoint 完成后：
   - commit + push
   - 如涉及锁则释放锁
   - 执行 return-main 归还 shared root

本轮禁止：
- 禁止在 main 上写 spring-day1 业务内容
- 禁止绕过 grant 直接 ensure-branch
- 禁止做出粗糙 UI 样式后把“能用”当验收通过

回报格式固定为：
- grant 是否成功
- ensure-branch 是否成功
- 当前 branch / HEAD
- 本轮最小 checkpoint 是什么
- 是否碰热文件 / 共享 UI 资源，若碰是否已先核锁
- 是否进入过 Unity / MCP / Play Mode，若进入是否已退回 Edit Mode
```

## 导航检查

### 阶段一｜纯只读唤醒 Prompt
```text
【阶段一｜只读唤醒｜导航检查】

当前 Sunset 已恢复到统一健康基线：
- shared root = D:\Unity\Unity_learning\Sunset
- branch = main
- HEAD = b0ceca78
- git status clean
- shared root = neutral

你这一轮默认身份仍是只读审计线程，不是直接进入写入线程。
你的第一条 commentary 必须显式说明你正在使用 skills-governor、sunset-workspace-router，并先完成只读 preflight。

请只读完成以下动作：
1. 在 shared root 复核 cwd / branch / HEAD / git status --short --branch。
2. 读取你的线程 memory、相关导航工作区文档、当前治理入口文档。
3. 在 main 上只做只读审计：
   - 读取导航相关现状
   - 分层问题
   - 判断哪些结论只读即可成立
   - 判断若要整改，是否需要单独开写入分支

本轮禁止：
- 禁止写文件
- 禁止 ensure-branch
- 禁止进入 Play Mode
- 禁止直接把 shared root 切成自己的长期现场

只有当你只读后明确判定“确实必须写”时，才允许申请阶段二。

回报格式固定为：
- cwd / branch / HEAD / git status
- 本轮只读看了哪些内容
- 导航问题的分层结论
- 是否确实需要下一轮开写入分支
- 若需要，最小 checkpoint 是什么
```

### 阶段二｜条件式准入 Prompt
```text
【阶段二｜条件式准入｜导航检查】

仅当你在阶段一已明确判定“必须进入真实写入整改”时，才允许执行本轮。

当前前提仍必须满足：
- D:\Unity\Unity_learning\Sunset
- main
- clean
- occupancy = neutral

默认建议的新分支名：
- codex/navigation-audit-001

请严格执行：
1. 先执行 grant：
   git-safe-sync.ps1 -Action grant-branch -OwnerThread "导航检查" -BranchName "codex/navigation-audit-001"
2. 再执行准入：
   git-safe-sync.ps1 -Action ensure-branch -OwnerThread "导航检查" -BranchName "codex/navigation-audit-001"
3. 进入分支后只做你在阶段一已经说明过的最小整改 checkpoint。
4. 如果要碰 Unity / MCP / Play Mode 或共享热区，做完后必须退回 Edit Mode。
5. checkpoint 完成后 return-main 归还 shared root。

本轮禁止：
- 禁止无阶段一结论直接进入写入
- 禁止扩大整改范围
- 禁止在 main 上直接写导航整改

回报格式固定为：
- grant 是否成功
- ensure-branch 是否成功
- 当前 branch / HEAD
- 本轮最小 checkpoint 是什么
- 是否进入过 Unity / MCP / Play Mode，若进入是否已退回 Edit Mode
```

## 遮挡检查

### 阶段一｜纯只读唤醒 Prompt
```text
【阶段一｜只读唤醒｜遮挡检查】

当前 Sunset 已恢复到统一健康基线：
- shared root = D:\Unity\Unity_learning\Sunset
- branch = main
- HEAD = b0ceca78
- git status clean
- shared root = neutral

你这一轮默认身份仍是只读审计线程，不是直接进入写入线程。
你的第一条 commentary 必须显式说明你正在使用 skills-governor、sunset-workspace-router，并先完成只读 preflight。

请只读完成以下动作：
1. 在 shared root 复核 cwd / branch / HEAD / git status --short --branch。
2. 读取你的线程 memory、相关遮挡工作区文档、当前治理入口文档。
3. 在 main 上只做只读审计：
   - 读取遮挡系统现状
   - 分层问题
   - 判断哪些结论只读即可成立
   - 判断若要整改，是否需要单独开写入分支

本轮禁止：
- 禁止写文件
- 禁止 ensure-branch
- 禁止进入 Play Mode
- 禁止直接把 shared root 切成自己的长期现场

只有当你只读后明确判定“确实必须写”时，才允许申请阶段二。

回报格式固定为：
- cwd / branch / HEAD / git status
- 本轮只读看了哪些内容
- 遮挡问题的分层结论
- 是否确实需要下一轮开写入分支
- 若需要，最小 checkpoint 是什么
```

### 阶段二｜条件式准入 Prompt
```text
【阶段二｜条件式准入｜遮挡检查】

仅当你在阶段一已明确判定“必须进入真实写入整改”时，才允许执行本轮。

当前前提仍必须满足：
- D:\Unity\Unity_learning\Sunset
- main
- clean
- occupancy = neutral

默认建议的新分支名：
- codex/occlusion-audit-001

请严格执行：
1. 先执行 grant：
   git-safe-sync.ps1 -Action grant-branch -OwnerThread "遮挡检查" -BranchName "codex/occlusion-audit-001"
2. 再执行准入：
   git-safe-sync.ps1 -Action ensure-branch -OwnerThread "遮挡检查" -BranchName "codex/occlusion-audit-001"
3. 进入分支后只做你在阶段一已经说明过的最小整改 checkpoint。
4. 如果要碰 Unity / MCP / Play Mode 或共享热区，做完后必须退回 Edit Mode。
5. checkpoint 完成后 return-main 归还 shared root。

本轮禁止：
- 禁止无阶段一结论直接进入写入
- 禁止扩大整改范围
- 禁止在 main 上直接写遮挡整改

回报格式固定为：
- grant 是否成功
- ensure-branch 是否成功
- 当前 branch / HEAD
- 本轮最小 checkpoint 是什么
- 是否进入过 Unity / MCP / Play Mode，若进入是否已退回 Edit Mode
```

## 当前剩余未完成内容清单
- 待你实际分发以上 5 组 prompt，并收回阶段一回报。
- 阶段二准入必须串行发放，不能多个线程同时抢 shared root。
- `导航检查` 与 `遮挡检查` 目前还没有被证明需要真实写入；它们是否进入阶段二，取决于阶段一只读结论。
- 自动化 skill trigger hook 仍只在 TD 层登记，尚未开发：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\000_代办\codex\TD_14_自动化skill触发日志Hook待办.md`
- 之前暂停的历史治理回读主线仍未恢复：
  - `.kiro/hooks`
  - `Steering规则区优化` 2 月历史文档
  - `Claude迁移` 相关目录
- 若后续任何线程要碰 A 类热文件、Unity 热区、共享 UI / 字体 / 材质，仍需逐轮核锁，不因为 Git 已恢复健康就自动放开。
