# 2026-04-06｜给 Town｜day1 驻村常驻化大步续工补充

请先完整读取：

- [D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-06_给Town_day1转向驻村常驻化承接与scene-side准备prompt_06.md]

这份是补充 prompt，不是推翻 `06`。

新的要求只有一个：

- `day1` 下一轮不会只试探一小刀，而是准备直接狠狠干 resident deployment 与导演数据的大 checkpoint。

所以你这轮也不要只回“方向说明”，而要尽量把 `Town` 这边能并行压实的 scene-side 承接做到更深。

---

## 一、这轮 day1 会往前狠狠干什么

`day1` 自己下一轮会直接吃：

1. `CrowdDirector` 的 resident deployment 第一刀
2. resident 化后的导演数据继续生产
3. 剧情正式产物继续下沉

也就是说，你这轮提供的东西，不该再只是“以后可以怎么接”，而是：

- 要尽量让 `day1` 这一轮做完后，下一步迁回 `Town` 时有现成承接面。

---

## 二、你这轮最值钱的并行产出

### 1. 把 resident root / group root 继续压实

请尽量给出：

1. 这批 `101~301` crowd 在 `Town` 里的合理 resident 分层
2. 哪些是默认日常在场 root
3. 哪些是导演 takeover root
4. 哪些只是背景层，哪些能承主戏边缘调度

### 2. 把 anchor 语义再往 scene-side contract 压一层

至少继续明确：

1. `EnterVillageCrowdRoot`
2. `KidLook_01`
3. `NightWitness_01`
4. `DinnerBackgroundRoot`
5. `DailyStand_01~03`

分别对应的：

1. 默认 resident presence
2. 允许的接管方式
3. 不该被误用的方式
4. 迁回时不能再变的命名和层级约束

### 3. 继续压“Primary 代理 -> Town 真承接”的最小迁移 contract

不是宏大方案，而是尽量给出：

1. 后面迁回 `Town` 时，哪些 root/anchor/启用状态必须保持一致
2. 哪些 scene-side 前提这轮就可以先钉死
3. 哪些东西如果现在不钉死，后面会逼得 `day1` 重排一次

### 4. 尽量给出一份“第一批迁回优先级”

请你继续判断：

1. 哪些 anchor 最适合第一批迁回
2. 哪些 anchor 可以晚一轮
3. 哪些当前仍不值得 `day1` 立刻硬吃

---

## 三、这轮不要做的事

1. 不要抢 `spring-day1` 当前 active 代码文件
2. 不要回到泛边界说明
3. 不要把这轮又做成 docs-only 的空说明
4. 不要把“常驻化”偷换回“更早 spawn 就行”

---

## 四、你回来的东西最好至少讲清 6 件事

1. `Town` 这轮实际又压实了哪些 resident root / anchor / scene-side contract
2. 哪些东西已经足够让 `day1` 后面迁回
3. 哪些仍是第一真实 blocker
4. 第一批建议迁回的 anchor 是哪些
5. 哪些 scene-side 命名/结构现在必须锁死不再漂
6. 你判断 `day1` 下一次什么时候最适合正式把球交回给你

---

## 五、一句话目标

请你把 `Town` 从：

- “能提供 resident 方向说明”

继续推进到：

- “等 day1 这轮大 checkpoint 砍完后，就能接下一步迁回动作的 scene-side 承接层”
