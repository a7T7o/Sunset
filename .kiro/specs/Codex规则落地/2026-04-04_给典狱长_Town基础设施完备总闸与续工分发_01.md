# 2026-04-04_给典狱长_Town基础设施完备总闸与续工分发_01

你现在不要继续审 `spring-day1` 的前半段剧情，也不要回到 UI、NPC 群像、字体底座或 `Primary` 清扫。

你当前唯一主线固定为：

- 把 `Town` 这条线收成“可稳定承接 Day1 后续生活面、可继续建设、可继续交给别的线程消费”的基础设施完备基线。

这轮不是只做背景分析。
你要按典狱长模式先审当前 `Town` 已接受基线，再决定：

1. 哪些线程还需要继续发 prompt
2. 哪些线程应该停给用户验
3. 哪些部分如果无人更合适，你治理线程自己就地接盘做最小一刀

最终目标不是“再说一遍 Town 以后很重要”，而是：

- 让 `Town` 进入一个可以被后续 `spring-day1 / NPC / 场景承载` 真正继续消费的健康基础设施状态。

---

## 一、先读这些，不要跳

### 1. 先读当前剧情与 Town 边界基线

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\2026-04-04_spring-day1_原剧本回正与Town承接剧情扩充设计_01.md`

### 2. 再读 spring-day1 父/子层对 Town 的最新 accepted baseline

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`
  - 重点看 `2026-04-04 补记：Town 已从边界判断推进到可承接 Day1 后续生活面的最小壳`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\003-进一步搭建\memory.md`
  - 重点看 `2026-04-04 补记：Town 已补成 spring-day1 可接的“村庄承载层壳”`

### 3. 最后读治理层对 Town 当前收口状态的最新记录

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\memory.md`
  - 重点看 `2026-04-04｜Town 承接稳定化 slice 已提交并合法停车`

---

## 二、当前已接受的 Town 基线

下面这些已经不是假设，而是当前 accepted baseline：

1. `Town.unity` 已正式进仓库，并有已提交 checkpoint：
   - `8df1b4e0 2026.04.04_Codex规则落地_03`
2. `Town` 当前正确职责已经说死：
   - 它是“村庄承载层”
   - 不是 `CrashAndMeet / EnterVillage` 的前半段剧情源 owner
3. `Town` 当前已经具备一组 `Day1 carrier anchors`：
   - `Town_Day1Carriers`
   - `EnterVillageCrowdRoot`
   - `KidLook_01`
   - `DinnerBackgroundRoot`
   - `NightWitness_01`
   - `DailyStand_01`
   - `DailyStand_02`
   - `DailyStand_03`
4. `Town` 相机/边界链已有结构补线：
   - `Main Camera`
   - `_CameraBounds`
   - `CinemachineConfiner2D`
   - `CameraDeadZoneSync`
5. 同一轮还已经补过 Town 相关运行时基础设施支撑：
   - `PersistentManagers.cs`
   - `PersistentObjectRegistry.cs`
   - `DialogueUI.cs`
   - `CameraDeadZoneSync.cs`
6. 当前 accepted 口径不是“Town 全部完工”，而是：
   - `结构与提交成立，live 待验证`

---

## 三、当前真正还没完成的东西

这条线真正未闭环的，不是剧情源，也不是 UI 审美，而是 Town 自己的基础设施完备度。

当前至少还差下面 5 件事被明确说死：

1. `Town` 的低负载 Unity live 复验还没完成
   - 还没有一轮干净的“打开 / 进入 / 保持可用”证据
2. `Town` 当前是否仍残留 `out of view frustum` 这类相机/边界侧问题，还没拿到最终结论
3. `Town` 当前是否已经达到“别的线程可以继续拿它承接内容”的健康现场，还没被真正裁定
4. `Town` 的 manager/bootstrap/字体显示链是否在真实进入 Town 时稳定，不是只靠静态 YAML 成立
5. 当前还没有一份“Town 基础设施完备”的最终 blocker matrix 和 owner 拆分裁定

也就是说，这轮最值钱的不是再增更多 anchors，也不是再补文档，而是把 `Town` 从“结构上差不多”推进到“运行上可接盘”。

---

## 四、本轮唯一主刀

你这轮唯一主刀固定为：

- 把 `Town` 收成“可稳定打开 / 可稳定进入 / 可继续建设 / 可被后续 Day1 与 NPC 消费”的基础设施完备基线

这轮允许你做的只有两类动作：

1. 典狱长模式下的 Town 专项总闸裁定
2. 为仍需继续施工的线程生成下一轮硬切 prompt

如果你判断某个 Town 剩余问题必须由你治理线程自己亲自接盘一刀，允许你自续工；
但前提是你必须先在回执里明确说清：

- 为什么不是别的线程更适合
- 你自己亲自做的唯一切片是什么
- 为什么这不是又一轮 scope 漂移

---

## 五、明确禁止的漂移

1. 不要把这轮偷换成 `spring-day1` 前半段剧情扩写
2. 不要把 `Town` 再说成前半段正式剧情源
3. 不要漂到玩家面 UI、工作台正式面、任务栏、Prompt 样式
4. 不要漂到 NPC 群像设定、NPC prefab/content/roam 生产链
5. 不要回到底座字体/TMP 全局化大修，除非它精确阻断 `Town` live 进入
6. 不要顺手碰 `Primary.unity`
7. 不要把“基础设施完备”偷换成“继续只加空锚点”
8. 不要把“live 待验证”继续拖成“下一轮再说”
9. 不要发一坨并列大目标 prompt；Town 续工必须继续硬切片

---

## 六、你要给出的 Town 完成定义

这轮结束时，你必须把 `Town 基础设施完备` 的完成定义说死。

至少要覆盖下面这些判定项：

1. `Town.unity` 是否能稳定打开
2. `Town` 是否能稳定进入或切入，不再处于“结构成立但现场没人敢接”的状态
3. `Town` 的 manager/bootstrap 链是否够稳
4. `Town` 的相机/边界/confiner 链是否够稳
5. `Town` 的中文对话/UI 基础显示链是否够稳
6. `Town_Day1Carriers` 是否保留并可被后续消费
7. 当前还剩哪些 blocker，精确到 owner / 文件 / 类型
8. 最终结论到底是：
   - `可进 sync-ready`
   - 还是 `仍需拆 owner 继续做`

你不能只说“Town 还差一点 live 验证”，必须把“差哪一点、谁来做、做完算什么”明确列出来。

---

## 七、推荐执行顺序

你这轮默认按这个顺序做：

1. 先按典狱长模式审当前 Town accepted baseline
2. 再把 Town 剩余问题压成清晰的 blocker matrix
3. 再做四类裁定：
   - `继续发 prompt`
   - `停给用户验收`
   - `停给用户分析 / 审核`
   - `无需继续发`
4. 只有仍需继续施工的，才生成下一轮 prompt 文件
5. 如果最终判断 Town 已经只差用户 live 终验，就停发，不要机械继续派活

---

## 八、如果要继续发 prompt，只允许围绕这些 Town 剩余面切

后续 Town 续工合法范围，只能从下面挑，不准混刀：

1. `Town` live 进入/转场复验
2. `Town` manager/bootstrap/持久层接入稳定性
3. `Town` 相机/边界/confiner 现场复验
4. `Town` 中文字体/DialogueUI 显示链在 Town 内的真实验证
5. `Town` scene 健康体检与 blocker 报实

不准把以下内容塞进同一轮：

1. Day1 前半段剧情源
2. Town 美术/布局精修
3. NPC 正式站位与群像内容生产
4. UI 玩家面 polish

---

## 九、回执格式

你这轮回我时，先给用户可读层，不要先丢技术 dump。

### A. 用户可读层

按顺序逐项写：

1. 当前主线
2. 这轮实际做成了什么
3. 现在还没做成什么
4. 当前阶段
5. 下一步只做什么
6. 需要我现在做什么

### B. Town 专项裁定层

再单独给：

1. `Town` 当前已接受基线
2. `Town` 当前剩余 blocker matrix
3. 你的四类裁定结果
4. 如果继续发 prompt：
   - 对应文件路径
   - 可直接复制的转发 text 代码块

### C. 技术审计层

最后才补：

1. 你这轮读取了哪些关键文件
2. 你是否新增 prompt 文件
3. 如果有，分别给谁
4. 你是否判断 Town 已可 sync-ready
5. 如果没有，精确 blocker 是什么

---

## 十、一句话底线

这轮不要再让 `Town` 停在“看起来已经差不多、但谁也不敢接着用”的半悬空状态。
你要把它收成一个真正可裁定的结果：

- 要么 `Town` 已经是健康的村庄承载层基线
- 要么明确拆出剩余 owner，并用硬切片 prompt 把最后几件事推进去

