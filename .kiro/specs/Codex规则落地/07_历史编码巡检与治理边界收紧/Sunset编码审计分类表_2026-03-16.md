# Sunset 编码审计分类表（2026-03-16）

## 1. 文件身份
- 本文件是 `07_历史编码巡检与治理边界收紧` 阶段的第一版分类表。
- 它不负责重写一切文档，而是把“终端假乱码”“可原地修复”“必须重建”“历史忽略”先分清。
- 当前结论优先服务治理层与现行入口层，不主动碰 `NPC`、`farm`、`spring-day1` 的 owner-held 业务现场。

## 2. 审计方法
- 对同一文件至少比较两次读取：
  - `Get-Content -Encoding UTF8`
  - `Get-Content -Encoding Default`
- 对关键可疑对象补看字节证据：
  - `Format-Hex`
- 结论分类固定为：
  - `healthy-terminal-noise`
  - `convertible-in-place`
  - `rewrite-required`
  - `historical-ignore`

## 3. 当前分类结果

| 目标对象 | 读取证据 | 分类 | 当前动作 | 当前状态 |
|---|---|---|---|---|
| `C:\Users\aTo\.codex\skills\skills-governor\SKILL.md` | `UTF8` 正常、`Default` 出现“鍦/浠/鏄”等错码；`Format-Hex` 显示标准 UTF-8 字节 | `healthy-terminal-noise` | 不改文件，只要求后续按 UTF-8 读取 | 健康，可继续使用 |
| `C:\Users\aTo\.codex\skills\sunset-workspace-router\SKILL.md` | `UTF8` 读取健康；本体未见不可逆错码 | `healthy-terminal-noise` | 不改文件，继续按 UTF-8 读取 | 健康，可继续使用 |
| `D:\Unity\Unity_learning\Sunset\.kiro\steering\workspace-memory.md` | `UTF8` 读取正常，`Default` 出现错码 | `healthy-terminal-noise` | 不改文件，保留当前 UTF-8 正文 | 健康，可继续使用 |
| `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\memory.md` | `UTF8` 读取正常，`Default` 错码明显 | `healthy-terminal-noise` | 不改文件，避免把终端噪音误判成坏文件 | 健康，可继续使用 |
| `C:\Users\aTo\.codex\skills\sunset-thread-wakeup-coordinator\agents\openai.yaml` | 此前已实锤存在编码问题并完成修复 | `convertible-in-place` | 保持已修后的 UTF-8 版本，后续只做抽检 | 已修复，继续巡检 |
| `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\memory_0.md` | 搜索命中过往错码片段，但该文件已是历史长卷，不再承担现行入口 | `historical-ignore` | 不做原地重写，只在需要追溯时按历史卷读取 | 历史保留 |
| `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\文档归档\2026-03-Codex恢复与迁移收口\...` 下历史对象 | 历史归档中仍可能存在阶段性错码或旧终端转储噪音 | `historical-ignore` | 仅保留证据价值，不拉回现行层重修 | 历史保留 |

## 4. 当前未发现的类别
- `rewrite-required`
  - 本轮在“现行活文档 / steering / 核心治理 skills”范围内，未新增发现必须重建的活跃文件。
- 这不代表历史库绝对干净，只代表当前高频入口没有出现新的“活文件已坏到必须重建”的证据。

## 5. 本轮明确不碰的对象
- `NPC`、`farm`、`spring-day1` 当前 owner-held 的业务代码、场景、Prefab、资源和阶段工作区文档。
- 活跃业务线程的 `memory`、`tasks` 若出现可疑片段，本阶段只记录“后续可审计”，不越权重写。

## 6. 当前稳定结论
- 当前大量“乱码感”并不是文件普遍损坏，而是终端用 `Default` 读 UTF-8 文本导致的假象。
- 编码治理接下来应优先盯：
  - 新增 skills 的 `agents/openai.yaml`
  - 新建 `SKILL.md`
  - 新生成的小型路由 / 配置文件
  - 历史归档中确实仍会被直接引用的对象
- 对现行活文档层，当前正确策略不是“全面重写”，而是“保持 UTF-8、减少误报、只修实锤问题”。
