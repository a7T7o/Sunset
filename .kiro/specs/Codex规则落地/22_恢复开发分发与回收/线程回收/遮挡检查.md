# 遮挡检查 回收卡

## 固定信息
- owner_thread: `遮挡检查`
- 默认阶段二分支候选: `codex/occlusion-audit-001`
- 当前阶段工作区: `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收`

## 阶段一｜线程回收区（由 遮挡检查 填写）
- 回写时间: `2026-03-18 22:34:59 +08:00`
- cwd: `D:\Unity\Unity_learning\Sunset`
- branch: `main`
- HEAD: `首次 clean preflight = 14838753b4ae9b09b2146b92fb3bfdc9ac82b2a0；回写收口前 latest live = d0c6bb72ae1100b0ef5626685c6cfe1ee6a9d958`
- git status --short --branch: `首次 clean preflight = ## main...origin/main；回写后当前 live 为治理文档 dirty`
- 已读取文档:
  - `C:\Users\aTo\.codex\skills\skills-governor\SKILL.md`
  - `C:\Users\aTo\.codex\skills\sunset-workspace-router\SKILL.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收\可分发Prompt\遮挡检查.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收\线程回收\遮挡检查.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset当前唯一状态说明_2026-03-17.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\shared-root-branch-occupancy.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-single-instance-occupancy.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\locks\mcp-hot-zones.md`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\遮挡检查\memory_0.md`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\999_全面重构_26.03.15\遮挡检查\memory.md`
- 本轮只读看了哪些内容:
  - 在 shared root 复核 `cwd / branch / HEAD / git status --short --branch`
  - 回读遮挡线程记忆、遮挡工作区记忆、当前治理入口与阶段 22 回收入口
  - 重新核对遮挡主链 live 代码、场景与树 Prefab，确认旧审计结论在首次 clean preflight 的 `14838753...` 与回写收口前 latest live `d0c6bb72...` 上都仍成立
- 遮挡问题的分层结论:
  - `P0 结构层`：树 Prefab 仍是父/子双 `OcclusionTransparency`；`TreeController` 仍只抓当前节点组件；`OcclusionManager` 仍按 `OcclusionTransparency` 组件粒度维护 `registeredOccluders/currentForest`
  - `P0 判定层`：`FindConnectedForest()` 与 `HandleForestOcclusion()` 仍在当前主链中，单棵物理树因双组件被误计入树林判定的风险仍未消除
  - `P1 交互一致性层`：`GameInputManager.cs` 仍走 `GetBounds()` / `SpriteRenderer.bounds`，`PlayerToolHitEmitter.cs` 仍走 `GetColliderBounds()`，点击与工具命中双标准仍在
  - `P1 性能/配置层`：`OcclusionTransparency` 默认 `usePixelSampling = true`，`GetPixel` 路径仍真实存在；`Primary.unity` 当前 live 参数仍是 `useOcclusionRatioFilter = 1`、`minOcclusionRatio = 0.4`、`rootConnectionDistance = 1.5`、`enableSmartEdgeOcclusion = 1`
  - `P2 支撑层`：`BatchAddOcclusionComponents.cs` 仍写不存在的 `affectChildren` / `occlusionTags`，工具层与真实结构脱节
- 是否确实需要下一轮进入真实写入: `是，但必须先经过阶段二 grant/ensure-branch，且应在独立写入分支 codex/occlusion-audit-001 上进入`
- 若需要，最小 checkpoint: `只验证并收口“树 Prefab 父/子双 OcclusionTransparency 是否是当前误判主根因”，暂不扩成整条遮挡链重写`
- 是否会碰热文件 / Unity 热区:
  - 最小整改路径大概率先碰树 Prefab、`OcclusionManager.cs`、`OcclusionTransparency.cs`、`TreeController.cs`
  - 若扩到命中标准统一，会碰 `GameInputManager.cs`，属于 A 类热文件
  - 若扩到全局阈值/场景参数调校，会碰 `Primary.unity`，属于 Unity 热区
  - 当前没有证据表明第一步必须先碰共享 UI / 字体 / 材质 / Sorting / Layer
- 当前阻断点（若有）:
  - 当前没有 `main` 脏树或 shared root 占用阻断
  - 唯一阻断是本线程尚未获阶段二显式 grant，不能直接进入真实写入
  - 第一阶段 prompt 正文里写死的 `HEAD = 9b14814b` 已过时；本次回收先在 clean preflight 上取到 `14838753...`，回写收口前 latest live 已前移到 `d0c6bb72ae1100b0ef5626685c6cfe1ee6a9d958`

## 阶段二｜线程回收区（由 遮挡检查 填写）
- 回写时间:
- grant 是否成功:
- ensure-branch 是否成功:
- 当前 branch:
- 当前 HEAD:
- 本轮最小 checkpoint:
- 是否进入 Unity / MCP / Play Mode:
- 若进入，是否已退回 Edit Mode:
- 是否碰热文件 / 热区:
- 若碰，是否已先查锁:
- 当前阻断点（若有）:

## 治理裁定区（由 Codex规则落地 填写）
- 阶段一裁定: `通过。阶段一只读复核充分，遮挡主根因假设在当前 live 上仍成立。`
- 阶段二裁定: `允许进入阶段二候选，但优先级低于导航检查。最小 checkpoint 只验证“树 Prefab 父/子双 OcclusionTransparency 是否为主根因”，不扩成整条遮挡链重写。`
- 后续动作: `待导航低风险验证完成后，再决定是否对 codex/occlusion-audit-001 发 grant；若扩到 GameInputManager.cs 或 Primary.unity，必须先查锁后再写。`
