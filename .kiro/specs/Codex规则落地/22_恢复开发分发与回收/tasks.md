# 阶段任务：恢复开发分发与回收

## 阶段目标
- 把“可以恢复开发”从口头结论变成一个有明确工作区、明确发放入口、明确回收位置、明确格式模板的执行闭环。
- 让用户后续只需要分发 prompt，不需要再手工补“回写到哪里、按什么格式写、哪些能并发、哪些不能并发”。

## 当前状态
- **阶段状态**：进行中
- **工作性质**：恢复开发运营工作区 / prompt 分发 / 回收模板 / 串行准入调度
- **前置事实**：
  - shared root 当前为 `main + neutral + clean`
  - `farm` / `NPC` / `spring-day1` 已具备最新 branch-local 闸机
  - `导航检查` / `遮挡检查` 当前默认仍为只读审计线程

## A. 工作区显式化
- [x] 立项 `22_恢复开发分发与回收`
- [x] 为本阶段建立 `tasks.md`
- [x] 为本阶段建立 `memory.md`
- [x] 建立“可分发 Prompt”目录
- [x] 建立“线程回收”目录

## B. Prompt 成品化
- [x] 为 `NPC` 生成带固定回收文件路径的 prompt 文件
- [x] 为 `farm` 生成带固定回收文件路径的 prompt 文件
- [x] 为 `spring-day1` 生成带固定回收文件路径的 prompt 文件
- [x] 为 `导航检查` 生成带固定回收文件路径的 prompt 文件
- [x] 为 `遮挡检查` 生成带固定回收文件路径的 prompt 文件
- [x] 在 prompt 中区分：
  - 阶段一只读唤醒
  - 阶段二准入
  - 并发允许边界
  - shared root 串行边界

## C. 回收模板化
- [x] 为 5 个线程建立固定回收文件
- [x] 为每个回收文件建立统一字段：
  - live Git 事实
  - 阶段一回收区
  - 阶段二回收区
  - Unity / Play Mode / 热文件触碰声明
  - 治理裁定区
- [x] 在 prompt 中写死“不要只在聊天里回复，要回写到指定文件”

## D. 入口收口
- [x] 将根层 `恢复开发唤醒与准入Prompt_2026-03-18.md` 降级为路由页
- [x] 明确本阶段工作区是新的唯一正文入口

## E. 待用户分发后的待办
- [x] 用户实际分发 5 个阶段一 prompt
- [x] 收回 5 份阶段一回收文件
- [x] 对阶段一结果做治理裁定
- [x] 将“批次分发 / 回执约束”上提为治理线程根层规则，不再把通用群发文件固化在阶段 22
- [x] 修正 5 份 prompt 模板中的静态 `HEAD` 口径，改为 live preflight
- [ ] 只对需要真实写入的线程串行发放阶段二准入
- [ ] 收回阶段二回收文件并更新 shared root 调度状态

## F. 当前剩余补强，但不阻塞恢复开发
- [ ] 自动化 `skill trigger hook` 仍只在 TD，不在本阶段开发
- [ ] `.kiro/hooks` / `Steering规则区优化` 2 月历史文档 / `Claude迁移` 的治理回读仍待恢复
- [ ] 如后续出现新 continuation branch 恢复失败，优先排查 branch-local drift

## 完成标准
- [x] 用户已经不需要手工补“回写路径 / 回写格式 / 阶段边界”
- [x] 5 条线程至少完成阶段一回收
- [x] 阶段一治理裁定已经落到固定回收卡与阶段 22 正文
- [ ] shared root 阶段二准入按单写者串行执行
- [x] 本阶段形成一份可持续复用的发放与回收工作区，而不是一次性对话产物

## 2026-03-19 审计补记
- `22` 当前仍是进行中，但它的职责边界已经收紧。
- `22` 已经完成的部分：
  - 统一的线程专属 prompt 与固定回收卡
  - 阶段一全量回收与治理裁定
  - 批次分发 / 回执约束上提为治理根层规则
  - 至少一条阶段二低风险闭环（`导航检查`）
- `22` 继续保留的 in-scope 内容：
  - 真实阶段二准入的串行发放
  - 阶段二回收文件持续收件
  - shared root 当前调度状态回写
- `22` 不再承担的内容：
  - 自动化 `skill trigger hook`
  - `.kiro/hooks` / 2 月历史文档 / `Claude迁移` 的总回读
  - 更完整的并发排队 / 等待态 / 中断消化模型
- 上述被移出的内容统一迁入后续新阶段，避免 `22` 同时背“恢复开发运营”与“并发模型重建”两条主线。
