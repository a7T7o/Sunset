# 阶段22总入口：如何分发与回收

## 本阶段唯一正文入口
- 当前“恢复开发发放与回收”唯一正文工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收`

## 你之后只需要做什么
1. 如果本轮需要群发，由治理线程先按根层协议生成“本轮批次分发文件”：
   - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\治理线程批次分发与回执规范.md`
2. 每个线程会自己去 `可分发Prompt/` 找与自己同名的文件。
3. 阶段一只读结果必须写回 `线程回收/` 中的固定文件，不能只留在聊天里。
4. 你只需要告诉我“阶段一已回写，请去收件箱接收”，或者直接让我读对应回收卡。
5. 我根据回收文件判断是否进入阶段二。
6. 阶段二必须串行发放，不能多人同时拿 grant。

## 目录说明
- `可分发Prompt/`
  - 放的是你可以直接发给线程的 prompt 正文文件。
  - 这里只保留线程专属 prompt，不再保留治理线程自己的批次分发入口文件。
- `线程回收/`
  - 放的是线程必须回写的固定文件。
- `tasks.md`
  - 记录这个阶段还没收口的事项。
- `memory.md`
  - 记录本阶段的执行脉络与恢复点。

## 当前 live 口径
- shared root 固定入口：`D:\Unity\Unity_learning\Sunset`
- `branch / HEAD / git status / occupancy` 不再在这里写死
- 任何线程启动阶段一时，都必须自己做 live preflight
- 只有线程自己复核出 `main + occupancy neutral`，才能把阶段一写成健康结论
- 如果 live 现场不是中性，线程必须如实回写，不得沿用旧快照

## 统一边界
- 阶段一可以并发，但必须纯只读。
- 阶段二必须串行，谁拿到 grant 谁独占一次 `ensure-branch -> checkpoint -> return-main`。
- 任何线程只要进入 Unity / MCP / Play Mode，做完必须退回 `Edit Mode`。
- 任何线程调用 `git-safe-sync.ps1` 都必须显式带 `-OwnerThread`。
- 统一群发时，不再手工逐条补“去哪里领 / 回写到哪里 / 用什么格式”；但这类批次分发文件由治理线程按轮次临时生成，不固化在阶段目录中。
