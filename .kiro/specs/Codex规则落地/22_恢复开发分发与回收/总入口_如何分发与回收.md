# 阶段22总入口：如何分发与回收

## 本阶段唯一正文入口
- 当前“恢复开发发放与回收”唯一正文工作区：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收`

## 你之后只需要做什么
1. 去 `可分发Prompt/` 找对应线程文件。
2. 先发该线程的“阶段一｜只读唤醒”。
3. 线程会被要求把完整结果写入 `线程回收/` 中的指定文件。
4. 你只需要把聊天里那句简短确认转给我，或直接让我读回收文件。
5. 我根据回收文件判断是否进入阶段二。
6. 阶段二必须串行发放，不能多人同时拿 grant。

## 目录说明
- `可分发Prompt/`
  - 放的是你可以直接发给线程的 prompt 正文文件。
- `线程回收/`
  - 放的是线程必须回写的固定文件。
- `tasks.md`
  - 记录这个阶段还没收口的事项。
- `memory.md`
  - 记录本阶段的执行脉络与恢复点。

## 当前 shared root 基线
- shared root：`D:\Unity\Unity_learning\Sunset`
- live branch：`main`
- live HEAD：`9b14814b`
- `git status --short --branch`：clean
- occupancy：`main + neutral + branch_grant_state = none`

## 统一边界
- 阶段一可以并发，但必须纯只读。
- 阶段二必须串行，谁拿到 grant 谁独占一次 `ensure-branch -> checkpoint -> return-main`。
- 任何线程只要进入 Unity / MCP / Play Mode，做完必须退回 `Edit Mode`。
- 任何线程调用 `git-safe-sync.ps1` 都必须显式带 `-OwnerThread`。
