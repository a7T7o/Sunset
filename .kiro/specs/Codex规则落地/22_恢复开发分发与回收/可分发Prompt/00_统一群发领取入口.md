# 统一群发领取入口｜当前批次专用，不可跨轮复用

```text
【统一群发｜请领取你的专属 Prompt 并完成阶段一回收】

注意：
- 本文件只服务当前这一轮分发。
- 下一次再做类似群发时，治理线程应新建一份“本轮专用统一领取入口”，而不是继续复用这份旧文案。

你现在不要等我在聊天里继续手工转抄你的专属指令。
请直接进入：
D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收\可分发Prompt

然后严格执行：
1. 找到与你 owner_thread 同名的 `.md` 文件，并完整读取。
2. 只执行其中 `## 阶段一` 的内容；本轮不要越级进入阶段二。
3. 把完整结果写回：
   D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\22_恢复开发分发与回收\线程回收\<你的线程名>.md
4. 只填写你对应回收卡里的“阶段一｜线程回收区”，不要改治理裁定区。
5. 写完后在聊天里只回复：
   - 已回写文件路径
   - 是否通过阶段一
   - 一句话摘要

硬边界：
- 禁止写业务文件
- 禁止切分支
- 禁止执行 ensure-branch
- 禁止恢复旧 worktree
- 禁止进入 Unity / MCP / Play Mode
- 第一条 commentary 必须显式说明你正在使用 `skills-governor`
- 如果命中 Sunset 工作区，还要显式说明 `sunset-workspace-router`
- 如果你正在执行线程分发 / 回收，还要显式说明 `sunset-thread-wakeup-coordinator`，或明确说明你做的是手工等价流程
- 不要沿用旧快照里的 HEAD / branch / status；一切以你启动当刻的 live preflight 为准

如果你在 `可分发Prompt/` 找不到与你同名的文件，不要自行猜测，直接停下并汇报。
```
