# 2026-03-29_全局警匪定责清扫第一轮裁定与第二轮放行_01

## 文件目的

这不是最终总清算。

这是在第一轮认定回执基础上，结合当前 shared root 现场，给出的第一批正式裁定：

1. 哪些线程已经足够成熟，可以先进入第二轮“清扫认领”
2. 哪些线程现在必须停发
3. 哪些线程还得继续等别线回执，不能硬推

## 当前现场前提

- 当前 shared root 仍是明显 mixed 现场，不存在“整体已干净”的前提。
- `Primary.unity` same-GUID / 路径分裂事故面仍未收口，不能被任何线程顺手吞并。
- `GameInputManager.cs` 仍是 mixed hot-file，不能按整文件独占分配。
- `UI-V1` 的第一轮自查回执尚未回到治理位，因此 UI 线本轮不能继续往下推第二轮。

## 第一批裁定结果

### 一、继续发 prompt

这些线程的 owner 边界已经足够清楚，可以先进入第二轮“只清自己、只认自己”的执行：

1. `导航检查`
   - 继续方向：静态 validation 工具链 + 父线程审核 / 分发 docs
   - 不再继续认动态 runtime 或 `Primary.unity`

2. `导航检查V2`
   - 继续方向：`PlayerAutoNavigator.cs` + 自己线程 / 工作区文档
   - 不再继续吞 `GameInputManager.cs`、static runner/menu、`Primary.unity`

3. `NPCV2`
   - 继续方向：NPC 非正式聊天、NPC 气泡 / 提示壳、NPC 关系 / 内容这组 own 面
   - 不再继续吞 `DialogueManager.cs`、整份 `NPCDialogueInteractable.cs`、整张 `Primary.unity`

4. `农田交互修复V3`
   - 继续方向：放置 / 树木 / 工具事务 / hover / chest / inventory / tooltip / 玩家气泡 / 状态条展示这一圈 true own
   - `GameInputManager.cs` 只保留 touchpoint 级责任，不再整文件 claim
   - `PlayerAutoNavigator.cs` 明确退回 dependency / mixed-by-history

### 二、停给用户分析 / 审核

这些线程现在不该继续直接发第二轮施工 prompt：

1. `场景搭建（外包）`
   - 原因：它当前更像 UI 父线程治理 / 审核线，不是实现 owner
   - 当前还缺 `UI-V1` 第一轮自查回执，UI 线 owner 不能单边继续放行
   - 恢复点：等 `UI-V1` 第一轮回执回来后，再一起裁 UI 父线 / 实施线的第二轮分工

### 三、无需继续发

这些线程本轮不再继续派第二轮清扫 prompt：

1. `项目文档总览`
   - 结论：repo 内只剩最小线程文档尾账，业务 owner 已归零
   - 当前最合理状态：docs-only / 可停发

2. `spring-day1V2`
   - 结论：`B｜最终停表`
   - 它当前价值在于 blocker / owner / stop-list 裁定，不在于继续施工

## 当前仍待回来的关键回执

### `UI-V1`

- 当前状态：第一轮自查 prompt 已补发，但回执还没回来
- 当前影响：
  - `场景搭建（外包）` 不进入第二轮
  - Day1 UI / SpringUI / registry 链相关路径暂不做最终 owner 清扫分配

## 2026-03-29 晚间补充裁定：`UI-V1` 第一轮已回，UI 实施线可单独进入第二轮

### 新增可继续发 prompt

5. `UI-V1 = spring-day1 / SpringUI`
   - 继续方向：`Prompt / Workbench + prefab-first + registry + evidence + runtime test` 这条 still-own 实施链
   - 不再继续吞：
     - `NpcWorldHintBubble.cs`
     - same-GUID `Primary.unity` 事故整面
     - 父线程治理 / 审核 / 分发面
     - UI 工作区迁移本身
   - 两个手调 prefab、`SpringDay1WorldHintBubble.cs`、`Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 当前统一按 mixed 面处理，不按 ordinary own 一口吞并

### 对 `场景搭建（外包）` 的补充裁定

- 当前不进入第二轮执行书
- 当前定位保持：
  - `停给用户分析 / 审核`
  - 或后续单独做 docs / governance 收口
- 它不再承担 `UI-V1` 这条实施清扫线的 repo-side 执行责任

## 第二轮统一口径

本批第二轮 prompt 统一只做：

- 清自己
- 认自己
- 退 foreign
- 报 mixed

明确不做：

- 新功能实现
- 新 live
- 新 hot-file 扩张
- 把“历史碰过”重新包装成“现在还归我”

## 一句话结论

可以先放行第二轮的，是：

- `导航检查`
- `导航检查V2`
- `NPCV2`
- `农田交互修复V3`
- `UI-V1`

现在必须继续等的，是：

- `场景搭建（外包）`

现在应停发的，是：

- `项目文档总览`
- `spring-day1V2`
