# 2026-03-19 批次分发 06 - 真实开发准入批次 02

## 本轮用途
- 本轮承接批次 01 的两条新裁定点。
- 当前批次包含：
  - `NPC`：phase2 carrier 集成清洗批次，可立即发车
  - `农田交互修复V2`：`GameInputManager.cs` 热文件专项批次，先生成好，待 `NPC` 退场后再发

## 生成时 live 基线
- 仓库：`D:\Unity\Unity_learning\Sunset`
- 分支：`main`
- HEAD：`3247541f`
- `git status --short --branch = ## main...origin/main`
- occupancy：`main + neutral`
- A 类热文件锁状态：
  - `GameInputManager.cs = unlocked`
  - `Primary.unity = unlocked`

## 线程领取入口
- 现在就发给 `NPC`：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\2026.03.19_真实开发准入批次_02\可分发Prompt\NPC_phase2集成清洗.md`
- `农田交互修复V2` 暂不立即发车；等 `NPC` 成功 `return-main` 后再发：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\2026.03.19_真实开发准入批次_02\可分发Prompt\农田交互修复V2_GameInputManager热文件专项.md`

## 固定回收卡
- `NPC`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\2026.03.19_真实开发准入批次_02\线程回收\NPC_phase2集成清洗.md`
- `农田交互修复V2`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\2026.03.19_真实开发准入批次_02\线程回收\农田交互修复V2_GameInputManager热文件专项.md`

## 聊天最小回执格式
- 已读 prompt 路径
- 本轮状态字段
- 一句话摘要

## 本轮硬规则
- `NPC` 本轮目标不是重复根因排查，而是把 `codex/npc-roam-phase2-003` 收束成可交付的 phase2 carrier。
- `农田交互修复V2` 本轮目标不是重复第一检查点，而是进入 `GameInputManager.cs` 热文件阶段。
- `农田` 未收到本轮专项 prompt 前，不准自行先拿 `GameInputManager.cs` 锁。
- 任何线程在 `main` 上都禁止写 tracked memory / 回收卡 / 治理文档。
- 任何线程如果拿到锁后进入“已改热文件但无法安全完成 checkpoint”的中间态，必须停写并保留锁，不准自行释放。

## 下一步预告
- `NPC` 回执成功后，如果 shared root 仍是 `main + neutral + clean`，就发 `农田交互修复V2_GameInputManager热文件专项.md`
