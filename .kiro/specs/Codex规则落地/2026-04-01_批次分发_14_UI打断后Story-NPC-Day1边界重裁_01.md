# 2026-04-01 批次分发 14：UI 打断后 Story / NPC / Day1 边界重裁

## 本轮定位

这轮不是继续做实现，也不是继续放任 `UI / NPC / spring-day1V2` 各自往前冲。

这轮只做一件事：

**在 `UI` 已经 `PARKED` 的前提下，重新问清 `NPC` 和 `spring-day1V2` 到底谁还是这批共享 Story 文件的真实继续 owner。**

## 当前已接受基线

- `UI` 已被打断并明确回执：
  - 当前已改代码：`是`
  - 已改文件：
    - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
    - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
    - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - 仍在现场的未跟踪文件：
    - `Assets/YYY_Scripts/Story/Interaction/SpringDay1ProximityInteractionService.cs`
    - `Assets/YYY_Tests/Editor/SpringDay1InteractionPromptRuntimeTests.cs`
  - 当前线程状态：`PARKED`
  - 已执行 `Park-Slice`
  - 未 `sync`
- 当前 `thread-state` 现场里：
  - `NPC` = `PARKED`
  - `spring-day1V2` = `PARKED`
- 这说明当前不是“谁还在合法施工”，而是“谁的 owner 声明和真实继续意图需要重新核定”。

## 本轮只发给谁

- `NPC`
- `spring-day1V2`

`UI` 这轮不再发实现 prompt，维持 `PARKED`，先不继续扩写。

## 本轮统一硬规则

1. 这轮两条线程都只做 **只读 owner 审计**。
2. 不改代码。
3. 不改 prefab / scene。
4. 不 `sync`。
5. 不把“我以后也许能做”写成“我现在继续认领”。
6. 如果本轮结论只是“我该继续 `PARKED`”，就老实写 `PARKED`，不要硬找一刀继续施工。

## 领取入口

- `NPC`：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-01_典狱长_NPC_UI打断后共享文件收边界问卷_01.md`
- `spring-day1V2`：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-01_典狱长_spring-day1V2_UI打断后owner真伪复核问卷_01.md`

## 回复要求

- 这轮回复对象最终会被用户直接看，所以不能只给最小技术回卡。
- 统一先交：
  - `A1 保底六点卡`
  - `A2 用户补充层`
  - `B 技术审计层`
- 但这轮是只读 owner 审计：
  - 不要求 claim 过线
  - 不要求 claim clean
  - 只要求把 owner 边界和下一步恢复点说清楚

## 关于 thread-state

这轮 prompt 本身不让线程进入真实施工，所以正文里不追加 `thread-state` 续工尾巴。

如果这轮审计完，后续要恢复真实施工，再按当时的最新裁定追加 `Begin-Slice / Ready-To-Sync / Park-Slice` 要求。
