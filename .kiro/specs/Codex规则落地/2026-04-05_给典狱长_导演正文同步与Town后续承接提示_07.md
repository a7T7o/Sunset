# 2026-04-05｜给典狱长｜导演正文同步与 Town 后续承接提示

这不是让你替我接盘 `spring-day1`，也不是一张“你下一刀必须只做 X”的硬命令单。

这是一次必要的状态升级同步：

- `spring-day1` 这边不只是拿到了 `Town` 的边界真值，
- 还已经把这层真值继续往下推进成了可以被别人直接消费的导演正文。

所以你现在需要知道的，不再只是“导演线想按 Town 分戏”，而是：

1. 它已经具体分到什么程度
2. 哪些内容已经稳定到可供 Town / NPC / 治理继续引用
3. 你这边后续若继续推进 Town 总治理，最该对接的是什么

---

## 一、这轮 `spring-day1` 新增的不是 prompt，而是 3 份正文

当前新增的是：

1. [2026-04-05_spring-day1_导演分场与Town承接脚本_10.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/2026-04-05_spring-day1_导演分场与Town承接脚本_10.md)
2. [2026-04-05_spring-day1_NPC剧本走位与群像层矩阵_11.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/2026-04-05_spring-day1_NPC剧本走位与群像层矩阵_11.md)
3. [2026-04-05_spring-day1_阶段承载边界与后续runtime落位清单_12.md](/D:/Unity/Unity_learning/Sunset/.kiro/specs/900_开篇/spring-day1-implementation/003-进一步搭建/2026-04-05_spring-day1_阶段承载边界与后续runtime落位清单_12.md)

这 3 份意味着：

- 导演线当前能抽象推进的部分，已经基本写到了最深处。

也就是说，接下来如果还有空白，不再是“导演线没想清楚”，而更可能是：

1. `Town` 还没就位到能承接 runtime
2. `NPC` 还没把群像层接成 runtime
3. 或需要回到正式剧情资产化

---

## 二、这 3 份正文已经把什么写死了

## 1. `10` 号文档把后半段“按场地分戏”写实了

它已经把：

1. `EnterVillage` 后半段
2. `DinnerConflict` 背景层
3. `ReturnAndReminder`
4. `FreeTime`
5. `DayEnd`

写成了正式导演分场。

里面已经明确：

1. 每场主锚点是什么
2. 主戏角色是谁
3. 背景层职责是什么
4. 玩家在这一场里应该感到什么
5. 哪些 runtime 项这轮故意不写死

换句话说：

- “Town 在 Day1 后半段到底承担什么戏”这件事，现在不再是口头层。

## 2. `11` 号文档把可消费的锚点矩阵写实了

它已经把：

1. `EnterVillageCrowdRoot`
2. `KidLook_01`
3. `DinnerBackgroundRoot`
4. `NightWitness_01`
5. `DailyStand_01~03`

写成了可直接消费的群像矩阵。

不只是说“这里以后有人”，而是写清：

1. 哪类人适合放
2. 哪类人不适合放
3. 朝向谁
4. 说话强度到哪
5. 必须给谁让位

## 3. `12` 号文档把 phase 级边界与后续 runtime 落位拆开了

它已经正式写清：

### 继续留在临时/抽象承载的

1. `CrashAndMeet`
2. `EnterVillage` 前半段
3. `HealingAndHP`
4. `WorkbenchFlashback`
5. `FarmingTutorial`

### 已经可以按 `Town` 写导演层、但还不该假装 runtime ready 的

1. `EnterVillage` 后半段
2. `DinnerConflict`
3. `ReturnAndReminder`
4. `FreeTime`
5. `DayEnd`

### 以后最合理的 runtime 落位优先顺序

1. `EnterVillageCrowdRoot`
2. `KidLook_01`
3. `DinnerBackgroundRoot`
4. `NightWitness_01`
5. `DailyStand_01~03`

这层边界现在也不再只是聊天判断。

---

## 三、这对你意味着什么

### 1. 现在最不该做的，是再把导演线和 Town 边界混回去

当前已经足够清楚：

1. 导演线已经把“怎么分戏”写出来了
2. `NPC` 已经有了可接的群像矩阵
3. `Town` 这边后续最值钱的，不再是重新裁定边界本身

### 2. 你现在如果继续往下推进，最值钱的是 Town 后续承接与治理

这时候你继续最值得覆盖的，不是再替导演线改戏，而是：

1. `Town` 何时能从“导演层可消费”推进到“更接近 runtime 可承接”
2. 哪些 anchor 先值得真的做成 live 承接点
3. 哪些外线 blocker 继续卡着 `Town`
4. 什么时候可以把 `Town` 的状态从“可开始写戏”升级到“可开始吃 runtime”

---

## 四、你现在最该盯的，不再是内容真值，而是承接条件

当前导演真值这边，已经够用了。

你现在继续最该盯的，是这 4 类条件：

## 1. 场地条件

1. `EnterVillageCrowdRoot` 是否真的适合承接进村 crowd
2. `DinnerBackgroundRoot` 是否真的适合承接晚餐背景
3. `NightWitness_01` 是否真的适合承接夜间见闻
4. `DailyStand_01~03` 是否已经够稳定，可供次日站位消费

## 2. 外线 blocker 条件

1. `camera / frustum`
2. `DialogueUI / 字体链`
3. `PlacementManager.cs`

这些仍然是 `Town` 更进一步的真 blocker。

## 3. 协同条件

1. `NPC` 是否已经能按 `11` 号矩阵去承接群像层
2. `Town` 是否已经开始为这些锚点提供真正的可用空间
3. 后续是否还需要再生成一次新的 Town 升级同步说明

## 4. 升级条件

后续最值钱的不是一句“Town 还没 ready”，而是：

1. 什么时候它只是导演消费层
2. 什么时候它开始具备 runtime 消费资格
3. 哪个锚点先升级
4. 哪个锚点还必须继续留在语义层

---

## 五、你现在可以把这 3 份正文理解成什么

你可以直接把这 3 份理解成：

1. `10`
   - 导演已经把后半段戏位拆完了
2. `11`
   - NPC 已经拿到了可接群像层的角色矩阵
3. `12`
   - Town / NPC / 导演各自的 runtime 落位边界，已经拆清了

也就是说：

- 现在如果 Town 还推不动，原因不该再归咎于导演线没说清自己要什么。

---

## 六、你现在最值得继续形成的产物

如果你继续往下做，我最希望你产出的不是“再来一轮同样的边界裁定”，而是更像下面这些：

1. `Town` 当前各 anchor 的可承接等级说明
2. `Town` 当前最接近 runtime 化的 anchor 顺序
3. 外线 blocker 对 Town 升级的影响说明
4. 哪些 Town 线程/外线线程该继续，哪些该停
5. 当 `Town` 状态变化后，应该如何再回灌给：
   - `spring-day1`
   - `NPC`

---

## 七、你现在不需要做的事

1. 不需要回头替导演线继续写 phase 内容
2. 不需要替 `NPC` 写群像矩阵
3. 不需要把 `Primary` 再当 Day1 正式长期场地
4. 不需要重新审“Town 能不能开始分戏”这件事本身

因为这几件事现在都已经有现成真值了。

---

## 八、这轮给你同步的核心只有一句

之前我给你的，是“导演线想怎么和 Town 协同”。

这次我给你的，是“导演线已经把 Town 消费面写成什么样了”。

所以你这边现在继续的时候，可以直接把注意力放到：

- Town 何时接得住这些戏
- 哪些 anchor 先升级
- 哪些 blocker 继续压

而不是再回到“导演线到底要什么”。
