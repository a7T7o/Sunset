# 2026-03-29｜全局警匪定责清扫第五轮｜代码闸门分流与停发表｜01

## 本轮改题

第四轮真实结果已经把 3 条线程分成了 2 种完全不同的状态：

1. `NPCV2`
   - 已把 `Assets/111_Data/NPC + own docs/thread` 真实上 git
   - `preflight` 放行
   - `sync` 已完成
   - own 路径已 clean
2. `导航检查`、`农田交互修复V3`
   - 已经不再卡 same-root
   - 现在卡的是 `代码闸门`
   - 而且两条线的代码闸门性质不同

所以第五轮不再把它们放在一个统一口径里推进。

## 本轮四类裁定

### 1. 继续发 prompt

- `导航检查`
- `农田交互修复V3`

但这次继续不是让它们再解释 blocker，而是：

- `导航检查`：把 `Service/Navigation` 从 mixed-root 依赖里剥出来，变成可独立编译包
- `农田交互修复V3`：在不重新吞 broad mixed-root 的前提下，只最小扩包引入 2 个实际缺失依赖，再重新归仓

### 2. 无需继续发

- `NPCV2`

复核结论：

- 第四轮回执已 claim：
  - `preflight = yes`
  - `sync = yes`
  - `SHA = 70fdd44f`
  - `own path = yes`
- live Git 继续显示：
  - `70fdd44f 2026.03.29_NPCV2_01`
  - `a08a9b26 2026.03.29_NPCV2_02`
- 当前 `HEAD` 下再看：
  - `Assets/111_Data/NPC`
  - `.codex/threads/Sunset/NPCV2`
  - `.kiro/specs/NPC/2.0.0进一步落地`
  已无相对 `HEAD` 的剩余 diff

所以 `NPCV2` 这条线当前应退出活跃施工池，不再继续发 cleanup prompt。

### 3. 继续停给治理位接盘 mixed-root

- `导航检查V2`
- `UI-V1`
- `spring-day1V2`
- `场景搭建（外包）`

第五轮没有新证据改写这个裁定。

## 第五轮的两个真实刀口

### A. `导航检查`

第四轮的真实 blocker 已经不是 same-root，而是：

- `NavigationLiveValidationRunner.cs` 对 `PlayerAutoNavigator.Debug*` 的硬编译依赖
- `NavigationLiveValidationMenu.cs` 对 `NPCInformalChatValidationMenu.ExclusiveValidationLockKey` 的硬编译依赖

也就是说：

- `Service/Navigation` 这包已经收窄成功
- 但它还没被写成“脱离 mixed-root 也能编译”的独立包

所以第五轮只允许它做：

- 去掉对 `Assets/YYY_Scripts/Service/Player/PlayerAutoNavigator.cs` mixed diff 的硬编译依赖
- 去掉对 `Assets/Editor/NPCInformalChatValidationMenu.cs` 的硬编译依赖
- 仍然只在 `Service/Navigation + own docs/thread` 范围内完成真实 `preflight -> sync`

### B. `农田交互修复V3`

第四轮真实 blocker 也已经不是 same-root，而是：

- `PlacementManager.cs` 需要 `GameInputManager.ShouldPreservePlacementModeForCurrentRightClick(...)`
- `InventorySlotUI.cs`、`ToolbarSlotUI.cs` 需要 `ToolRuntimeUtility.*`

但这次和导航不一样：

- `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs` 当前是独立子根
- `Assets/YYY_Scripts/Data/Core/ToolRuntimeUtility.cs` 当前也是独立子根
- 它们不是整个 `Service/Player` 或 `Assets/Editor` 那种 broad mixed-root

所以第五轮允许农田线程最小扩包：

- 带回 `GameInputManager.cs`
- 带回 `ToolRuntimeUtility.cs`

但不准借这个机会重新吞：

- `Controller/NPC`
- `TreeController.cs`
- `Service/Player/*`
- broad `Assets/YYY_Scripts/Data/*`

## 本轮领取说明

继续施工的线程去各自目录读取：

- `导航检查`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\导航检查\2026-03-29_全局警匪定责清扫第五轮_去除mixed依赖并重新归仓_01.md`
- `农田交互修复V3`
  - `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\农田交互修复V3\2026-03-29_全局警匪定责清扫第五轮_最小共享依赖扩包归仓_01.md`

## 聊天最小回卡

聊天里只回：

- 已回写文件路径
- 是否完成本轮要求
- 当前 own 路径是否 clean
- `已上 git / 第一真实阻断已钉死 / 已停表`
