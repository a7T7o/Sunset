# 执行方案：共享根仓库分支漂移与现场占用治理
## 1. 当前治理裁定
### 裁定 A：共享根目录立刻进入业务写入冻结
- 冻结对象：`D:\Unity\Unity_learning\Sunset`
- 允许：
  - 只读取证
  - 治理线程写治理文档与记忆
- 禁止：
  - `NPC`
  - `farm`
  - 其他业务线程
  在这个目录继续新增业务提交

### 裁定 B：共享根目录当前视为 farm 事故现场
- 这不表示 `farm` 还能继续在这里开发。
- 它只表示：当前根目录真实 checkout 仍在 `codex/farm-1.0.2-correct001 @ 11e0b7b4`，所以后续所有裁定都必须把这里视为 farm 污染现场处理。

### 裁定 C：旧 NPC worktree 暂不清理
- `D:\Unity\Unity_learning\Sunset_worktrees\NPC @ codex/npc-generator-pipeline`
- 当前身份：
  - 历史检出点
  - 取证参考
- 在事故完全收口前，不贸然删除。

## 2. NPC 侧执行结论
- `NPC` 当前唯一救援基线：`codex/npc-roam-phase2-001 @ f6b4db2f`
- 当前判断：
  - `f6b4db2f` 已包含 NPC 核心资产 / Prefab / meta / 运行时代码
  - 不应继续留在 NPC 线的是 [FarmToolPreview.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Farm/FarmToolPreview.cs) 的 farm 侧改动
  - [NPCPrefabGeneratorTool.cs](/D:/Unity/Unity_learning/Sunset/Assets/Editor/NPCPrefabGeneratorTool.cs) 以 `f6b4db2f` 为准

## 3. farm 侧执行结论
- `farm` 不再沿 `codex/farm-1.0.2-correct001` 继续开发。
- `farm` cleanroom 推荐起点：`b9b6ac48`
- 回放范围：
  - `07ffe199` 中仅 [FarmToolPreview.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Farm/FarmToolPreview.cs) 的 farm 部分
  - `11e0b7b4` 中的 8 个 farm 代码文件
  - `1.0.2纠正001` 的 `requirements.md / analysis.md / design.md / tasks.md`
- 不回放：
  - `18f3a9e1`
  - `07ffe199` 里的 NPC 资产、脚本、文档
  - 污染分支上的 memory 直接机械搬运

## 4. 规则层新增结论
- 后续 Sunset 任务不再只检查 A 类热文件锁。
- 进入任何实质性任务前，还必须检查：
  - 当前工作目录
  - 当前分支
  - 当前 `HEAD`
  - 当前目录是否已被其他线程占用
- 这层能力应纳入新的启动闸门 skill，而不是继续靠人工提醒。

## 5. 发给 NPC 的最终执行令
```text
【治理裁定：NPC 改走救援分支，不再借用共享根目录】
从现在开始，你不要再在 `D:\Unity\Unity_learning\Sunset` 这个共享根目录里继续做 NPC 业务写入。

治理线程已确认：
- 共享根目录当前真实 checkout 在 `codex/farm-1.0.2-correct001 @ 11e0b7b4`
- 它现在属于事故取证现场，不再作为 NPC 的可写现场

你本轮的首要动作不是继续扩写功能，而是：
1. 以 `codex/npc-roam-phase2-001 @ f6b4db2f` 作为唯一救援基线
2. 只读确认这条线已经包含 NPC 核心资产 / Prefab / meta / 运行时代码
3. 在这条线内给出最小收口建议：
   - `Assets/YYY_Scripts/Farm/FarmToolPreview.cs` 的 farm 侧改动如何剔除
   - `Assets/Editor/NPCPrefabGeneratorTool.cs` 保持 `f6b4db2f` 版本是否足够
   - 下一步最小验证应做什么

本轮禁止：
- 禁止继续在共享根目录写 NPC 代码
- 禁止直接操作 farm 分支
- 禁止顺手扩写新的 NPC 功能

汇报格式固定：
1. 你当前确认的唯一救援基线
2. 你确认当前还缺不缺核心 NPC 文件
3. 你确认哪些文件不该继续留在 NPC 线
4. 你建议下一步如何把 NPC 恢复到干净可继续开发状态
```

## 6. 发给 farm 的最终执行令
```text
【治理裁定：farm 停止沿污染分支继续开发，转入 cleanroom】
从现在开始，你不要再在 `codex/farm-1.0.2-correct001` 上继续追加业务提交。

治理线程已确认：
- 这条分支已经混入 `NPC` 与 `mixed` 提交
- 它只保留为事故取证现场，不再作为 farm 正常开发现场

你本轮的首要动作不是继续修功能，而是准备 cleanroom 收口方案：
1. 以 `b9b6ac48` 作为 cleanroom 推荐起点
2. 明确 farm-only 文件清单
3. 明确最小回放顺序：
   - 第一步回放什么
   - 第二步回放什么
   - 哪些文件绝不能再从污染分支带入
4. memory 与线程 memory 不直接机械照搬，而是按 cleanroom 新现场重写

本轮禁止：
- 禁止继续在 `codex/farm-1.0.2-correct001` 上开发
- 禁止擅自重写污染分支历史
- 禁止切去别的业务分支继续干活

汇报格式固定：
1. cleanroom 推荐起点 commit
2. farm-only 文件清单
3. 需要回放的提交顺序
4. 需要完全排除的污染文件清单
5. 你认为 cleanroom 完成后，共享根目录是否可以释放
```

## 7. 一句话口径
- 这次收口不是让 `NPC` 和 `farm` 互相背锅，而是先把共享根目录从事故现场里抽离出来，再让 `NPC` 走现有救援线、让 `farm` 走 cleanroom。

## 8. 第一轮执行结果后的修正版
### 8.1 NPC
- `NPC` 第一轮正式执行已经基本成功：
  - 独立现场：`D:\Unity\Unity_learning\Sunset_worktrees\NPC_roam_phase2_rescue`
  - 当前 `HEAD`：`f7a1c0f562a476febe50084124dbeee382d31ac9`
  - `FarmToolPreview.cs` 已按 `8aed637f` 从 NPC 线剔除
  - 编译与 Prefab / 动画 / Sprite / 漫游组件链抽查通过
- 但该 worktree 仍有 4 个无关 TMP 字体资源 dirty，后续若要继续 NPC 新开发，应先处理“工作树污染”问题，而不是直接在这个现场继续扩写。

### 8.2 farm
- `farm` 第一轮 cleanroom 已证明：
  - 边界隔离方案方向正确
  - 但原白名单配方少带了 `11e0b7b4` 版 [FarmToolPreview.cs](/D:/Unity/Unity_learning/Sunset/Assets/YYY_Scripts/Farm/FarmToolPreview.cs) 的后续接口闭环
- 修正版执行口径应改为：
  1. 继续使用 `b9b6ac48` cleanroom 起点
  2. `FarmToolPreview.cs` 的最终目标版本以 `11e0b7b4` 为准，而不是停留在 `07ffe199`
  3. 重新验证以下闭环：
     - `PreviewCellKey` 相关集合/方法签名
     - `PromoteToExecutingPreview(int layerIndex, Vector3Int cellPos)`
     - `RemoveExecutingPreview(int layerIndex, Vector3Int cellPos)`
     - `UnityEngine.Random.Range`
  4. 再跑 batchmode 编译，确认 cleanroom 是否可接替污染分支
