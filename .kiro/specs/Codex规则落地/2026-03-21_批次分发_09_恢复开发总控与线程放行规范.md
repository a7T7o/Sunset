# 2026-03-21 批次分发 09 - 恢复开发总控与线程放行规范

## 本文件身份
- 这不是某一条线程的一次性 prompt。
- 这是当前恢复开发阶段的总入口。
- 从现在开始，后续线程发放、等待、回执和更新，以本文件指向的“当前开发放行”文件为准。

## 当前总控文件
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\00_工作区导航\恢复开发总控与线程放行规范_2026-03-21.md`

## 当前总规则
1. shared root 线程一次只发一条。
2. 每条 shared root 线程做完一个最小 checkpoint 后，必须先回执给治理线程，再决定下一条。
3. `scene-build` 是特殊 worktree 施工线，可以继续，但它仍占全局 Unity / MCP 单写资格。
4. `spring-day1` 当前不放行真实开发，只放行集成波次前只读准备。
5. 如果任何线程回执改变了风险或顺序，治理线程必须先更新总控文件和对应 prompt，再告诉用户。

## 当前推荐顺序
1. `scene-build` 继续 `结构层`
2. shared root 下一条优先：`NPC`
3. 然后：`农田交互修复V2`
4. 然后：`导航检查`
5. 然后：`遮挡检查`
6. `spring-day1` 单独等待后续集成波次

## 当前可分发 Prompt
- `scene-build`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\scene-build_结构层继续施工.md`
- `NPC`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\NPC_当前开发放行.md`
- `农田交互修复V2`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\农田交互修复V2_当前开发放行.md`
- `导航检查`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\导航检查_当前开发放行.md`
- `遮挡检查`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\遮挡检查_当前开发放行.md`
- `spring-day1`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.21_恢复开发总控_01\可分发Prompt\spring-day1_集成波次前只读准备.md`

## 聊天最小回执格式
- shared root 线程统一回：
  - 已读 prompt 路径
  - request-branch
  - ensure-branch
  - hotfile_lock
  - sync
  - return-main
  - carrier_ready
  - main_ready
  - main_ready_basis
  - changed_paths
  - blocker_or_checkpoint
  - 一句话摘要
- `scene-build` 特殊线统一回：
  - 已读 prompt 路径
  - 当前 branch / HEAD
  - 当前 git status
  - 本轮 checkpoint
  - 是否触碰 Unity / MCP
  - 是否已经 clean
  - blocker_or_checkpoint
  - 一句话摘要

## 当前禁止事项
- 不准复用旧 prompt 当作当前口径。
- 不准没经过治理裁定就同时发两条 shared root 线程。
- 不准因为 `scene-build` 在 worktree 里，就误以为其他线程也能一起进 Unity / MCP。
- 不准让 `spring-day1` 直接跳过本文件进入真实实现。

## 一句话口径
- 从这一轮开始，用户只需要看这一份总入口和它指向的当前 prompt；治理线程负责持续更新，不再让你靠旧聊天内容自己拼规则。
