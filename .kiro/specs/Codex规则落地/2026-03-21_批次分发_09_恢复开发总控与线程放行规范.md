# 2026-03-21 批次分发 09 - 极简并发开发口径

## 这份文件现在只回答 3 个问题
- 现在还要不要排队放行：不要。
- 现在还要不要先切 branch：不要。
- 现在治理线程还管什么：只管高危撞车、Unity / MCP 单写冲突、以及收烂摊子。

## 现在直接可发的 prompt
- `scene-build_高质量初稿后续精修与spring-day1剧本对齐.md`
- `NPC_当前开发放行.md`
- `农田交互修复V2_当前开发放行.md`
- `导航检查_当前开发放行.md`
- `遮挡检查_当前开发放行.md`
- `spring-day1_当前开发放行.md`

## 当前最短执行法
1. 直接把对应 prompt 发给线程。
2. 不再先做放行测试。
3. 线程只要没撞上高危目标，就继续干。
4. 只有下面三种情况，才把回执贴回治理线程：
   - 撞了同一个高危目标
   - 需要 Unity / MCP live 写，但已经有人在写
   - 已经把现场写坏了，需要收烂摊子

## 当前高危目标定义
- 同一个 Scene
- 同一个 Prefab
- `Primary.unity`
- `Assets/000_Scenes/SceneBuild_01.unity`
- `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
- 当前正被别的线程 live 写入的 Unity 资源

## 一句话口径
- 这轮不再搞复杂交通调度了，直接并发开发，只有高危碰撞才打断。
