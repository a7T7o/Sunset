# 2026-03-29｜全局警匪定责清扫第八轮｜转入 mixed-root 治理接盘波

## 这轮主线

`场景搭建（外包）` 第三轮已经真实归仓。  
到这一刻为止，旧线程“自己提自己那一小包 git”的阶段可以正式收口，不再继续把希望寄托在更多旧线程自提奇迹上。

从这一轮开始，治理主线正式切到：

**mixed-root 治理接盘波**

也就是：

- 不再继续给旧线程发“你自己再跑一次 `preflight -> sync`”式 prompt
- 改由治理位先把交叉根系拆清，再决定后续 one-shot owner、integrator 包和上传顺序

## 本轮验收结论

### 已确认归仓完成

1. `spring-day1V2`
   - 提交：`cd42f0bf 2026.03.29_spring-day1V2_01`
2. `项目文档总览`
   - 提交：`dc4d68ee 2026.03.29_项目文档总览_03`
3. `场景搭建（外包）`
   - 提交：`58ebf240 2026.03.29_场景搭建（外包）_01`
   - 提交：`a300f331 2026.03.29_场景搭建（外包）_02`

### 已确认不再继续沿旧线程自转

1. `NPC`
   - 第一真实 blocker：`Assets/Editor/Story/DialogueDebugMenu.cs`
   - 真实问题：残包已横跨 `Assets/Editor`、`Assets/YYY_Scripts/Service/Player`、`Assets/YYY_Scripts/Story/UI`
2. `spring-day1(UI-V1)`
   - 第三轮已证明它不是“再补一点 thread-tail 就能提掉”，而是 `Story/UI + Prefab/Scene + Fonts + UI系统迁移` 的混包
3. `导航检查V2`
   - 第一轮已把 own / mixed / foreign / stale narrative 说清，但当前不再适合沿旧线程自己吞 mixed-root 包

## 对旧线程的裁定

### 无需继续发

1. `spring-day1V2`
2. `项目文档总览`
3. `场景搭建（外包）`
4. `NPCV2`
5. `导航检查`
6. `农田交互修复V3`

### 无需继续发旧线程自提 prompt，改入治理接盘池

1. `NPC`
2. `spring-day1(UI-V1)`
3. `导航检查V2`

这里的意思不是“它们以后不用管”，而是：

- 不再让这 3 条线继续用旧线程口径各自自提
- 改由治理位把交叉根、foreign、same-root hygiene 和迁移包先拆成可提交单元

## 当前 live 总账快照

按当前 `git status --porcelain` 复核，live 未提交现场是：

- `TOTAL = 161`
- `TRACKED = 92`
- `UNTRACKED = 69`
- `DELETED = 51`

这说明两件事同时成立：

1. 旧线程自提这波不是没成果，数量确实已经继续往下降
2. 剩下这 161 条已经明显不是“再多等几个旧线程就自然消失”的结构

## mixed-root 接盘波的第一目标

这一阶段不再问“哪个旧线程还能不能再勉强提一下”，而是先问：

**当前真正的交叉根，到底已经缠成了哪几包。**

当前优先级最高的接盘根是：

1. `Assets/Editor`
   - 重点看 `DialogueDebugMenu.cs` 与 NPC editor residual
2. `Assets/YYY_Scripts/Service/Player`
   - 重点看 `PlayerNpcChatSessionService.cs`、`PlayerAutoNavigator.cs` 以及玩家侧残留
3. `Assets/YYY_Scripts/Story/UI`
   - 重点看 `NpcWorldHintBubble.cs`、`SpringDay1*`、`Registry`、`evidence capture`
4. `Assets/222_Prefabs/UI/Spring-day1`
   - 重点看 `Primary.unity` 迁移态与 Day1 UI prefab 现状
5. `Assets/000_Scenes`
   - 重点看旧 `Primary.unity` 删除态是否只是迁移包的一半
6. `Assets/TextMesh Pro/Resources/Fonts & Materials`
   - 重点看 TMP 中文字体 churn 是否继续影响 Day1 / UI 包
7. `.kiro/specs/UI系统`
   - 重点看大规模旧文档迁移 / 删除是否已经形成独立 docs 包

## 这一阶段的硬边界

1. 不再给 `NPC`、`spring-day1(UI-V1)`、`导航检查V2` 发旧式“你自己 `preflight -> sync`” prompt
2. 不把 mixed-root 包继续伪装成“线程自己小尾账”
3. 不把 `Primary.unity`、字体 churn、`Story/UI` mixed 包拆散成互相甩锅的碎片叙事
4. 先做治理拆账，再决定 one-shot owner 或 integrator 提交顺序

## 当前恢复点

下一步唯一正确的方向是：

**治理位直接进入 mixed-root 总账拆包。**

也就是下一轮开始，重点不再是审“哪个旧线程能不能自己提”，而是：

1. 逐根核真实 still-own / foreign / mixed
2. 形成可提交的责任包
3. 再按包发 one-shot 接盘或直接治理吸收

一句话收口：

**旧线程自提阶段到这里正式结束；现在进入的是治理接盘阶段，不再继续等旧线程自己奇迹归仓。**
