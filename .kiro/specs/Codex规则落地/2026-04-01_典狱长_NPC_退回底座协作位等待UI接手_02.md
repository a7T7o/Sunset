# 2026-04-01 典狱长｜NPC｜退回底座协作位，等待 UI 接手

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-01_典狱长_UI_接管玩家面UIUE整合主刀_03.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\NPC.json`

## 当前已接受基线

1. 这轮用户最新裁定不是让你继续主刀“唯一提示 / 唯一 E / 视觉归属一致”。
2. 当前新的执行图是：
   - `UI`：接后半段玩家面 UI/UE 整合主刀
   - `你`：退回 NPC 底座 / 体验守门 / 协作位

## 这轮唯一要求

**继续 `PARKED`，不要恢复这刀实现。**

你现在只保留两件事：

1. 守住 NPC 底座文件：
   - `NPCBubblePresenter.cs`
   - `PlayerNpcChatSessionService.cs`
   - `PlayerThoughtBubblePresenter.cs`
2. 如果后续 `UI` 为了玩家面整合越界吞到 NPC 会话 / 气泡体验整包，你再报 blocker

## 固定最小回复

- 你是否继续 `PARKED`
- 你当前保留的 NPC 底座边界是什么
- 哪种情况下你会认为 `UI` 越界
