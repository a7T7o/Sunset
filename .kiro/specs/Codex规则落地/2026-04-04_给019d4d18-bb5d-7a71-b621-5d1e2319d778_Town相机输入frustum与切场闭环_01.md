# 2026-04-04_给019d4d18-bb5d-7a71-b621-5d1e2319d778_Town相机输入frustum与切场闭环_01

你现在不要回到 `Primary` traversal 泛修、导航 NPC 桥水边缘、UI、Town scene wiring 或通用工具。

你当前唯一主刀固定为：

- 只收 `Town` 进入链上的相机 / 输入 / `frustum` 问题，把它推进到“切进 Town 后不再爆视口越界红屏”的基础设施闭环。

## 一、先承认的 Town 基线

下面这些已经是 accepted baseline：

1. `Town.unity` 已提交在：
   - `8df1b4e0 2026.04.04_Codex规则落地_03`
2. `Town` scene 里已经有：
   - `Main Camera`
   - `_CameraBounds`
   - `CinemachineConfiner2D`
   - `CameraDeadZoneSync`
3. 你这条线历史上已经把 `CameraDeadZoneSync.cs + GameInputManager.cs` 的 `frustum` 主嫌疑压到过正确深度口径。
4. 但用户最新 live 已再次坐实：
   - 进入 `Town` 后仍有视口 / `frustum` 侧红屏

## 二、你这轮只准做什么

你这轮只准围绕下面这一个目标施工：

- 把 `Town` 进入链上的 `frustum` / 相机 / 输入深度问题收干净

合法范围：

1. `Assets/YYY_Scripts/Service/Camera/CameraDeadZoneSync.cs`
2. `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`
3. `Assets/YYY_Scripts/World/WorldSpawnDebug.cs`
4. 只在必要时补与你 own 触点直接相关的最小契约

明确禁止：

1. 不要碰 `Town.unity`
2. 不要碰 `Primary.unity`
3. 不要扩回导航 manager / traversal 大面
4. 不要碰 UI / Dialogue / 字体
5. 不要把这轮偷换成“整个 camera 体验 polish”

## 三、这轮必须回答的 3 个问题

1. 当前用户看到的 `frustum` 红屏，是否仍然来自你 own 的 `ScreenToWorldPoint` / viewport / confiner 触点？
2. 如果是，你最小应该怎么修，才能让 `Primary -> Town` 或等价进入链不再复现？
3. 如果不是，你必须把 blocker 精确转交出去：
   - owner
   - 文件
   - 类型

## 四、完成定义

只有在下面几条同时成立时，才算命中完成定义：

1. 代码面最小闭环完成，不扩题。
2. `GameInputManager.cs` 如果有改动，回执里必须补：
   - `touched_touchpoints`
3. 代码闸门通过。
4. 已给出低负载 Unity 侧证据，证明：
   - 进入 `Town` 后不再复现 `frustum` 红屏
   - 相机 / confiner 没因为这刀反向坏掉
5. 若仍未过线，必须明确：
   - 这条问题还差哪一点
   - 它是不是已经离开你 own 范围

## 五、回执要求

继续先交用户可读层六点卡，再给技术审计层。  
不要只说“我以前修过这条线”；要以用户最新这次 `Town` live 作为裁定基线。

## 六、thread-state 接线尾巴

```text
[thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
```
