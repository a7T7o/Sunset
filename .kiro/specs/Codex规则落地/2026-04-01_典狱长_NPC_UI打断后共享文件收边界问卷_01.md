# 2026-04-01 典狱长｜NPC｜UI 打断后共享文件收边界问卷

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-01_批次分发_14_UI打断后Story-NPC-Day1边界重裁_01.md`

## 当前已接受基线

- `UI` 已经被打断，且已回执自己这轮实际碰了：
  - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
  - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - 未跟踪：
    - `Assets/YYY_Scripts/Story/Interaction/SpringDay1ProximityInteractionService.cs`
    - `Assets/YYY_Tests/Editor/SpringDay1InteractionPromptRuntimeTests.cs`
- 你当前 `thread-state` 是：
  - `PARKED`
  - `owned_paths` 里仍声明了：
    - `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs`
    - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
    - `Assets/YYY_Scripts/Story/Interaction/SpringDay1ProximityInteractionService.cs`
    - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
    - `Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs`
    - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
- 你当前 blocker 仍写着：
  - `人工终验：闲聊进行中的左下角提示与头顶箭头最终观感`

## 这轮唯一主刀

**只做只读 owner 收边界。**

目标不是继续修 NPC，也不是继续补闲聊体验，而是回答：

**为了后续继续把 NPC 非正式聊天体验收完，你现在到底还必须继续认领哪些文件，哪些应该立刻释放回 Story / Day1 / UI 集成 owner。**

## 允许范围

你这轮只允许做这些事：

1. 读取你自己的当前 `thread-state`
2. 读取你自己的最新 `memory`
3. 读取当前 working tree 里与你这条 slice 相关的文件状态
4. 对照 `UI` 已回报的文件，做 file-level owner 裁定
5. 只用“当前 blocker 真要收完还需不需要碰它”来判断边界

## 明确禁止

1. 不改任何代码
2. 不改任何 prefab / scene / test
3. 不补新功能
4. 不把“也许顺手会碰”算成 own
5. 不把 `SpringDay1ProximityInteractionService.cs`、`SpringDay1WorldHintBubble.cs` 自动继续吞成 NPC own，除非你能证明不碰它们就根本收不完你当前 blocker
6. 不 `sync`

## 你必须正面回答的核心问题

1. 为了继续完成你当前 blocker，下面 6 个文件里哪些是 **NPC 必须继续 own**，哪些应该 **释放**：
   - `Assets/YYY_Scripts/Service/Player/PlayerNpcChatSessionService.cs`
   - `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
   - `Assets/YYY_Scripts/Story/Interaction/SpringDay1ProximityInteractionService.cs`
   - `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
   - `Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs`
   - `Assets/YYY_Scripts/Service/Player/PlayerThoughtBubblePresenter.cs`
2. `SpringDay1DialogueProgressionTests.cs` 和 `SpringDay1InteractionPromptRuntimeTests.cs` 是否是你这条 NPC slice 后续必须继续碰的测试面；如果不是，明确释放。
3. 你当前 blocker 能不能在 **不继续 own** `SpringDay1ProximityInteractionService.cs` 与 `SpringDay1WorldHintBubble.cs` 的前提下推进；如果不能，为什么。
4. 你下一次如果真的恢复施工，唯一最小主刀应该是什么；不要写大包。
5. 你当前 live 状态最准确应继续是 `PARKED`、改成 `BLOCKED`，还是你认为后续应重新 `Begin-Slice` 后再开工。

## 完成定义

这轮做完，不是 claim “过线”，而是要把下面 3 件事说死：

1. `NPC` 真实继续 own 的最小文件集
2. `NPC` 真实释放出去的文件集
3. 你后续若恢复施工，唯一最小下一刀是什么

## 固定回复格式

请直接按下面结构回复，不要省项：

### A1 保底六点卡

1. 当前主线  
2. 这轮实际做成了什么  
3. 现在还没做成什么  
4. 当前阶段  
5. 下一步只做什么  
6. 需要用户现在做什么  

### A2 用户补充层

1. 这轮最终裁定：  
   - `A｜当前 6 文件基本继续由 NPC own`
   - `B｜NPC 应收窄到更小文件集`
   - `C｜这条 slice 不该继续由 NPC 主拿`
2. 你继续认领的 exact files  
3. 你明确释放的 exact files  
4. 你是否还需要 `SpringDay1DialogueProgressionTests.cs / SpringDay1InteractionPromptRuntimeTests.cs`  
5. 当前 live 状态最准确应是 `PARKED / BLOCKED / 未来需重新 Begin-Slice` 哪一个  
6. 这轮是否触碰任何仓库文件  

### B 技术审计层

至少补清：

- 你读取了哪些 state / memory / 当前文件现场
- 你为什么认为 `SpringDay1ProximityInteractionService.cs` 该留或该放
- 你为什么认为 `SpringDay1WorldHintBubble.cs` 该留或该放
- 你当前 blocker 和这些文件的因果关系
- 如果你可能判断错，最可能错在哪

## 额外要求

- 这轮如果你没有触碰任何仓库文件，就明确写 `否`
- 这轮如果你只做只读审计，不要假装自己已经补了实现
- 自评一句必须写
