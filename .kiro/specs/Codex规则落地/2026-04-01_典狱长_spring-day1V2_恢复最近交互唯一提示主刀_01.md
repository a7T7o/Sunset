# 2026-04-01 典狱长｜spring-day1V2｜恢复“最近交互唯一提示 / 唯一 E 仲裁”主刀

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\2026-04-01_Story向UIUE集成owner边界与派工约定.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\UI系统\0.0.1 SpringUI\memory.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\场景搭建（外包）\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\spring-day1V2.json`
- `D:\Unity\Unity_learning\Sunset\.kiro\state\active-threads\NPC.json`

## 当前已接受基线

1. 当前三方现场已经停住：
   - `UI`：`PARKED`
   - `NPC`：`PARKED`
   - `spring-day1V2`：`PARKED`
2. 当前这刀最热问题不是 `Prompt / Workbench`，而是：
   - 最近目标
   - 唯一提示
   - 唯一 `E`
   - 视觉归属一致
   - 同 NPC 上剧情优先于闲聊
3. 当前文件职责与 state 更支持你这条线做主刀：
   - `SpringDay1ProximityInteractionService.cs`
   - `NPCDialogueInteractable.cs`
   - `NPCInformalChatInteractable.cs`
   - `CraftingStationInteractable.cs`
   - `SpringDay1BedInteractable.cs`
   - `SpringDay1WorldHintBubble.cs`
   - `InteractionHintOverlay.cs`
4. `NPC` 这条线当前更像：
   - NPC 闲聊 / 气泡 / 会话体验守门
   - 不是这刀总实现 owner
5. `UI / SpringUI` 这条线当前更像：
   - contract / 审稿 / formal-face 顾问位
   - 不是这刀当前实现位

## 这轮唯一主刀

**只恢复一刀：**

**把“最近交互唯一提示 / 唯一 E 仲裁 / 视觉归属一致 / 同 NPC 上剧情优先于闲聊”收成当前主链的唯一实现切片。**

## 允许范围

这轮只允许围绕下面文件施工：

- `Assets/YYY_Scripts/Story/Interaction/SpringDay1ProximityInteractionService.cs`
- `Assets/YYY_Scripts/Story/Interaction/NPCDialogueInteractable.cs`
- `Assets/YYY_Scripts/Story/Interaction/NPCInformalChatInteractable.cs`
- `Assets/YYY_Scripts/Story/Interaction/CraftingStationInteractable.cs`
- `Assets/YYY_Scripts/Story/Interaction/SpringDay1BedInteractable.cs`
- `Assets/YYY_Scripts/Story/UI/SpringDay1WorldHintBubble.cs`
- `Assets/YYY_Scripts/Story/UI/InteractionHintOverlay.cs`
- 如验证确实需要，可带：
  - `Assets/YYY_Tests/Editor/SpringDay1DialogueProgressionTests.cs`
  - `Assets/YYY_Tests/Editor/SpringDay1InteractionPromptRuntimeTests.cs`

## 当前现场特别说明

1. 这组文件里已经存在前序 dirty / untracked：
   - 不要先入为主把它们全当成“别人的脏东西”直接回滚
2. 你恢复施工前，先做 1 次只读核对：
   - 当前文件现场哪些改动已经与本刀目标一致
   - 哪些改动与本刀无关或互相打架
3. 如果现场改动已经部分替你铺好了这刀，允许你把它们当成当前 slice 的 carried WIP 继续收；
4. 如果你无法诚实分清哪些变更可继续继承，先停在 blocker，不要硬写。

## 明确禁止

1. 不回到 `Prompt / Workbench` 正式面
2. 不碰 `NPCBubblePresenter.cs`
3. 不碰 `PlayerNpcChatSessionService.cs`
4. 不碰 `PlayerThoughtBubblePresenter.cs`
5. 不碰 `Primary.unity`
6. 不碰 `GameInputManager.cs`
7. 不把 `UI / SpringUI` 的 formal-face 线重新吞进来
8. 不把 `NPC` 聊天气泡体验整包吞成你 own

## 完成定义

这轮做完，至少要站住 4 件事：

1. 最近目标唯一焦点成立
2. 屏幕 / 世界提示只剩一个主提示，不再多头打架
3. 唯一 `E` 的触发对象与视觉归属一致
4. 同一个 NPC 上，剧情交互优先于闲聊提示

## 固定回执格式

请直接按下面结构回复：

### A1 保底六点卡

1. 当前主线  
2. 这轮实际做成了什么  
3. 现在还没做成什么  
4. 当前阶段  
5. 下一步只做什么  
6. 需要用户现在做什么  

### A2 用户补充层

1. 这轮是否已恢复主刀：`是 / 否`
2. 当前是否顺利接住现场已有 dirty：`是 / 否`
3. 这轮 exact changed files  
4. 这轮是否需要两份测试一起带  
5. 当前 live 状态：`ACTIVE / READY / PARKED / BLOCKED`
6. 如果没过线，第一真实 blocker 是什么  

### B 技术审计层

至少补清：

- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 恢复施工前你如何处理现场已有 dirty
- 哪些文件改动被你继承，哪些被你排除
- 结构线证据
- 体验线证据
- 如果你可能判断错，最可能错在哪

## [thread-state 接线要求｜本轮强制]
从现在起，这条线程按 Sunset 当前 live 规范接入 `thread-state`：

- 如果你这轮会继续真实施工，先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Begin-Slice.ps1`
- 旧线程不用废弃重开；但最晚必须在“下一次真实继续施工前”补这次 `Begin-Slice`
- 第一次准备 `sync` 前，必须先跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Ready-To-Sync.ps1`
- 如果这轮先停、卡住、让位或不准备收口，改跑：
  `D:\Unity\Unity_learning\Sunset\.kiro\scripts\thread-state\Park-Slice.ps1`

回执里额外补 3 件事：
- 这轮是否已跑 `Begin-Slice / Ready-To-Sync / Park-Slice`
- 如果还没跑，原因是什么
- 当前是 `ACTIVE / READY / PARKED`，还是被 blocker 卡住

这不是建议，是当前 live 规则的一部分。除非这轮始终停留在只读分析，否则不要跳过。
