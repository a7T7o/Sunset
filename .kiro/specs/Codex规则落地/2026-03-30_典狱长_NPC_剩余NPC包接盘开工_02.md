# 2026-03-30_典狱长_NPC_剩余NPC包接盘开工_02

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\NPC\2026-03-29_全局警匪定责清扫第一轮回执_01.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1V2\2026-03-30_断开DialogueDebugMenu对NPC菜单编译耦合回执_02.md`

你现在不是继续守着旧的第一轮 blocker。

`UI-V1`、`导航检查V2`、`spring-day1V2` 这三刀都已经把原来卡你的大根 peeled 掉了。你这轮要接的是当前剩下的 **NPC-dominant package**，而且这轮不再拿 `Assets/Editor/Story/DialogueDebugMenu.cs` 当停车位。

---

## 一、当前已接受基线

下面这些事实已经成立，不要再回到旧口径：

1. `UI-V1` 已完成并 sync：
   - `Story/UI`
   - `Spring-day1 prefab`
   - `Resources/Story`
   现在都不是你这轮的事

2. `导航检查V2` 已完成并 sync：
   - `Assets/YYY_Scripts/Service/Player/**`
   现在也不是你这轮的事

3. `spring-day1V2` 已完成并 sync：
   - `Story/Interaction`
   - `Story/Managers`
   - `Editor/Story`
   - `Tests/Editor`
   并且已经在 `DialogueDebugMenu.cs` 内切掉了对你验证菜单锁 key 的编译耦合

一句话翻成人话：

- 你第一轮当时撞到的 `DialogueDebugMenu.cs` same-root / compile blocker，已经不存在了
- 这次你终于可以不被 Story 大根卡死，真正接自己的剩余 NPC 包

---

## 二、本轮唯一主刀

只做这一刀：

- **把当前剩余的 NPC-dominant package 收成一个真实可 `preflight -> sync` 的切片**

当前允许纳入的包，分成 3 块：

### 1. `Controller/NPC`

- `Assets/YYY_Scripts/Controller/NPC/NPCAutoRoamController.cs`
- `Assets/YYY_Scripts/Controller/NPC/NPCBubblePresenter.cs`

### 2. `Data/NPC*`

- `Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs`
- `Assets/YYY_Scripts/Data/NPCRelationshipStage.cs`
- `Assets/YYY_Scripts/Data/NPCRoamProfile.cs`
- `Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs`
- `Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs.meta`

### 3. `Editor/NPC` 验证菜单收口

这轮不要继续把验证菜单挂在 `Assets/Editor` 直系根。

你这轮必须把：

- `Assets/Editor/NPCInformalChatValidationMenu.cs`
- `Assets/Editor/NPCInformalChatValidationMenu.cs.meta`

迁入：

- `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs`
- `Assets/Editor/NPC/NPCInformalChatValidationMenu.cs.meta`

并把：

- `Assets/Editor/NPC.meta`

一起纳入本轮。

### 4. 线程文档

- `.codex/threads/Sunset/NPC/**`

如果你这轮更新了 NPC 工作区 memory，也可以带上；否则不要顺手扩到别的文档根。

---

## 三、这轮为什么必须先做“迁入 Editor/NPC”

因为你这轮不该再去碰 `Assets/Editor` 直系根。

当前 `Assets/Editor` 直系根里还留着：

- `Assets/Editor/NavigationStaticPointValidationMenu.cs`
- `Assets/Editor/NavigationStaticPointValidationMenu.cs.meta`

这是导航那边的 Editor 菜单残面，不该让你继续在同一个直系根里硬吞。

所以这轮正确做法不是：

- 把 `NavigationStaticPointValidationMenu.cs` 一起带走

而是：

- **把你自己的 NPC validation 菜单移进 `Assets/Editor/NPC/` 子根**

这样你就能避开 `Assets/Editor` 直系根 mixed blocker。

---

## 四、这轮绝对不要碰

### 1. 不准碰已经 peeled 完的 Story roots

- `Assets/YYY_Scripts/Story/UI/**`
- `Assets/YYY_Scripts/Story/Interaction/**`
- `Assets/YYY_Scripts/Story/Managers/**`
- `Assets/Editor/Story/**`
- `Assets/YYY_Tests/Editor/**`
- `Assets/222_Prefabs/UI/Spring-day1/**`
- `Assets/Resources/Story/**`

### 2. 不准碰导航 / Service / scene / 字体

- `Assets/YYY_Scripts/Service/Player/**`
- `Assets/YYY_Scripts/Service/Rendering/OcclusionManager.cs`
- `Assets/000_Scenes/Primary.unity`
- `Assets/TextMesh Pro/Resources/Fonts & Materials/**`

### 3. 不准碰 `Assets/Editor/NavigationStaticPointValidationMenu.cs`

这不是你这轮要带的 foreign leaf。

### 4. 不准再把这轮说成“非正式聊天残包 exact 回补”

这轮你不是回补旧 exact files。
这轮你是 **NPC integrator**。

---

## 五、你这轮必须做的事

### 1. 先按 NPC package 做 integrator 认定

回执里必须诚实分开：

1. `NPC still-own core`
2. `carried foreign but can ride with this package`
3. `first exact blocker`（如果还有）

这里允许你明确写：

- `NPCDialogueContentProfile.cs / NPCRoamProfile.cs / NPCRelationshipStage.cs`
  这几份虽然历史上被别线碰过，但这轮可按 NPC-dominant package 一起带走

### 2. 再真实跑 preflight

推荐 include paths 直接按根与 exact file 混合：

```powershell
$include = @(
  'Assets/YYY_Scripts/Controller/NPC',
  'Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs',
  'Assets/YYY_Scripts/Data/NPCRelationshipStage.cs',
  'Assets/YYY_Scripts/Data/NPCRoamProfile.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs.meta',
  'Assets/Editor/NPC',
  'Assets/Editor/NPC.meta',
  '.codex/threads/Sunset/NPC'
)
& 'C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1' `
  -Action preflight `
  -Mode task `
  -OwnerThread NPC `
  -IncludePaths $include
```

### 3. 如果 preflight 通过

立刻同白名单 `sync`：

```powershell
$include = @(
  'Assets/YYY_Scripts/Controller/NPC',
  'Assets/YYY_Scripts/Data/NPCDialogueContentProfile.cs',
  'Assets/YYY_Scripts/Data/NPCRelationshipStage.cs',
  'Assets/YYY_Scripts/Data/NPCRoamProfile.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs',
  'Assets/YYY_Scripts/Data/NPCInformalChatExitModel.cs.meta',
  'Assets/Editor/NPC',
  'Assets/Editor/NPC.meta',
  '.codex/threads/Sunset/NPC'
)
& 'C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1' `
  -Action sync `
  -Mode task `
  -OwnerThread NPC `
  -IncludePaths $include
```

### 4. 如果 preflight 不通过

不要越过脚本。

只允许回：

- 第一真实 blocker
- exact path
- exact reason
- 当前 own 路径是否 clean

---

## 六、这轮完成定义

你这轮只接受两种结果：

### A｜剩余 NPC package 已真实 sync

- `Controller/NPC + Data/NPC* + Editor/NPC + thread dir` 这组切片成功归仓
- 你给出提交 SHA
- 当前 own 路径 clean

### B｜第一真实 blocker 已钉死

- 你真实跑过 `preflight`
- 仍然被挡住
- 你只报第一真实 blocker，不准扩题

不接受：

- 继续拿旧的 `DialogueDebugMenu.cs` 当 blocker
- 继续把 `Assets/Editor` 直系根 mixed 当默认停车位
- 顺手扩到 Story peeled roots
- 顺手扩到 scene / font / service / rendering

---

## 七、你这轮回执必须额外写清

继续按：

- `A1 保底六点卡`
- `A2 用户补充层`
- `停步自省`
- `B 技术审计层`

但这轮必须额外回答：

1. 你是否接受当前身份已经改成 `NPC integrator`
2. `NPCInformalChatValidationMenu.cs` 是否已从 `Assets/Editor` 直系根迁入 `Assets/Editor/NPC/`
3. 本轮是否碰了 `Assets/Editor/NavigationStaticPointValidationMenu.cs`
   正确答案默认应为：`否`
4. `Data` 这几份文件这轮按什么口径带上
   不要只说“本来就是我的”；你要诚实写是 `still-own core` 还是 `NPC-dominant package carried`
5. 当前 own 路径是否 clean

---

## 八、你这轮最容易犯的错

- 继续围着旧回执说 `DialogueDebugMenu` 当时卡我
- 不迁文件，还去碰 `Assets/Editor` 直系根
- 顺手把 `NavigationStaticPointValidationMenu.cs` 一起吞掉
- 顺手回漂去 `Story/Interaction` 或 `Tests/Editor`
- 把 `Data` 根的这几份文件一概说成“都本来是我 own”，不做 integrator 诚实分层

一句话收口：

- **现在轮到你把剩余的 NPC-dominant package 真接起来了；第一步先把 NPC 验证菜单移出 `Assets/Editor` 直系根。**
