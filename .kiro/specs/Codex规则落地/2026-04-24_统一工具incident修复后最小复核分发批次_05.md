这轮不是继续群发所有业务线程。

当前治理位已经先把统一工具 incident 的第一刀修掉了：
1. `CodexCodeGuard Program.cs` 不再用整仓非代码脏改去拖代码闸门
2. `git-safe-sync.ps1` 在 `CodexCodeGuard timeout / no JSON / bad JSON / exit-no-block` 时，已经会返回结构化 blocker

这意味着现在最值得做的，不是把所有历史线程一起叫起来，而是只做 `2` 条最小复核：
1. `存档系统`
2. `UI`

本波唯一目标：
- 验证这两条线现在是否已经从“工具黑盒 incident”降级成“真实业务 blocker / compile blocker / 或真实可上传”
- 不在这一轮继续修业务代码
- 不顺手扩到第二批或其它线程

## 1. `存档系统`
- 领取文件：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-24_给存档系统_工具修复后DataCore最小复核prompt_05.md`
- 当前唯一目标：
  - 只对白名单 `Data/Core` 三文件重做 `1` 次真实 preflight / upload 尝试
  - 确认它现在吐出的已经是“真实 blocker”，而不是 `CodexCodeGuard hang / no JSON`

## 2. `UI`
- 领取文件：
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-04-24_给UI_工具修复后UITabs最小复核prompt_05.md`
- 当前唯一目标：
  - 只对白名单 `UI/Tabs` 七文件重做 `1` 次真实 preflight / upload 尝试
  - 确认它现在吐出的已经是“真实 blocker”，而不是 `CodexCodeGuard returned no JSON`

这轮明确不发：
1. `spring-day1`
2. `导航检查`
3. `NPC`
4. `农田交互修复V3`
5. 其它仍未开工线程

统一要求：
1. 这轮每条线程只允许 `1` 个既定白名单，不准换第二批。
2. 这轮不准顺手修业务代码；只做真实复核。
3. 这轮如果 blocker 仍在，必须把它写成用户能直接调度的人话：
   - `A1 保底六点卡`
   - `A2 用户补充层（可选）`
   - `B 技术审计层`

这轮最核心的治理判断：
1. 现在不是“工具已经修好，所以所有业务线马上开闸”。
2. 现在只是“工具不再黑盒挂死，因此可以先叫最值钱的 2 条线回来复核真 blocker”。
3. 前面那些还没开工的线程继续保持后置，不在这轮顺手推进。
