# 2026-03-26_批次分发_11_shared-root大扫除认领与白名单收口_01

本轮不是继续 second-wave 业务续工。

本轮是一次 shared-root 大扫除：

- 各线程只认领自己的 dirty / untracked
- 能按白名单直接 sync 到 `main` 的，就直接收口
- 不能安全收口的，就如实汇报 blocker / foreign dirty

## 运行态修正

- 用户后续已明确纠偏：
  - `谁是卧底V2` 并未被实际投入本轮 cleanup 执行
  - 它仍留在用户单独下发的原任务线上
- 因此本轮实际收件统计口径应为：
  - active cleanup set = `spring-day1V2 / NPCV2 / 导航检查V2 / 农田交互修复V3 / 项目文档总览`
  - `谁是卧底V2` 不纳入本轮 cleanup 完成率统计

## 当前现场

- `D:\Unity\Unity_learning\Sunset @ main @ 519d51bd20d98e662eafb94cea0c5bbbeb314cec`
- `Primary.unity` 仍是 `M`
- `git diff --stat -- Assets/000_Scenes/Primary.unity` 仍是 `76 insertions / 4 deletions`
- 物理锁状态仍是 `unlocked`
- `shared-root-branch-occupancy = neutral-main-ready` 只表示 shared root Git 入口中性，不等于热 scene 可写
- `Primary.unity` 内再次搜索 `HomeAnchor`，仍无命中
- `GameInputManager.cs` 仍是 mixed hot-file dirty
- `TagManager.asset` 当前 owner 仍未明确

## 本轮领取入口

### `spring-day1V2`

去读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\26.03.26-Day1V2共享根大扫除与白名单收口-11.md`

### `NPCV2`

去读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\NPC\2.0.0进一步落地\2026-03-26-NPCV2共享根大扫除与owner报实-03.md`

### `导航检查V2`

去读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\屎山修复\导航检查\2026-03-26-导航检查V2共享根大扫除与白名单收口-04.md`

### `农田交互修复V3`

去读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\农田系统\2026.03.16\1.0.4交互全面检查\2026-03-26-农田交互修复V3共享根大扫除与白名单收口-04.md`

### `谁是卧底V2`

去读取：

- `D:\迅雷下载\开始\.codex\threads\系统全局\谁是卧底\2026-03-26_谁是卧底V2共享根大扫除与AutoCalibrator收口-04.md`

### `项目文档总览`

去读取：

- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\项目文档总览\2026-03-26_项目文档总览共享根大扫除与文档尾账收口-02.md`

## 聊天回执要求

- 聊天里不要贴长摘要
- 只按各自专属 prompt 里的固定回执格式回复
- 如果已经完成白名单 sync，要把提交 `SHA` 一并带上
- 如果没法 sync，也必须把 blocker 说清楚，不准只回一句“还有脏改”

## 本轮统一禁止事项

- 不准顺手继续业务实现
- 不准把这轮 cleanup 偷换成 third-wave 续工
- 不准碰 `Primary.unity`
- 不准把 `shared-root-branch-occupancy = neutral-main-ready` 误解成热 scene 可写
- 不准擅自吞并 `GameInputManager.cs`
- 不准擅自吞并 `TagManager.asset`
- 不准替别的线程扫地
- 不准用无边界 `git add -A`

## 一句话口径

这轮不是继续干活，是先把 shared root 里各自留下的垃圾认清、收干净、能白名单提交就提交，不能提交就老实报异常。
