# 2026-03-20 批次分发 07 - 真实业务开发批次 03

## 本轮用途
- 本轮正式从“准入护航”切到“真实业务开发恢复”。
- 本轮只发两条线：
  - `NPC`：`phase2` 交付面收口与 carrier 清洗
  - `农田交互修复V2`：`1.0.2` 交付面收口与热文件前后两段式推进
- `导航检查`、`spring-day1`、`遮挡检查` 不在本轮发车范围，等本轮两条业务线起稳后再进下一波。

## 生成时 live 基线
- 仓库：`D:\Unity\Unity_learning\Sunset`
- 分支：`main`
- HEAD：`9f1dbafc`
- 说明：本批次生成时 shared root 的 tracked dirty 来自治理线程本轮文档/记忆写入；真正执行前仍以各线程自己的 live preflight 为准。

## 建议发送顺序
- 建议先发 `NPC`，再发 `农田交互修复V2`。
- 如果你想两条线连续群发也可以，最终以 queue live 返回为准：
  - 拿到 `GRANTED / ALREADY_GRANTED` 的线程立刻进槽
  - 拿到 `LOCKED_PLEASE_YIELD` 的线程进入 Draft 等待，不准在 `main` 写 tracked 文档

## 线程领取入口
- `NPC`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.20_真实业务开发批次_03\可分发Prompt\NPC_phase2交付面收口.md`
- `农田交互修复V2`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.20_真实业务开发批次_03\可分发Prompt\农田交互修复V2_1.0.2交付面收口.md`

## 固定回收卡
- `NPC`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.20_真实业务开发批次_03\线程回收\NPC_phase2交付面收口.md`
- `农田交互修复V2`
  - `D:\Unity\Unity_learning\Sunset\.kiro\specs\共享根执行模型与吞吐重构\01_执行批次\2026.03.20_真实业务开发批次_03\线程回收\农田交互修复V2_1.0.2交付面收口.md`

## 聊天最小回执格式
- 已读 prompt 路径
- request-branch
- ensure-branch
- hotfile_lock
- sync
- return-main
- changed_paths
- blocker_or_checkpoint
- 一句话摘要

## 本轮硬规则
- shared root live 准入统一走：
  - `powershell -ExecutionPolicy Bypass -File C:\Users\aTo\.codex\tools\sunset-git-safe-sync.ps1 ...`
- 没拿到 `GRANTED / ALREADY_GRANTED` 前，禁止 `ensure-branch`。
- waiting 线程禁止在 `main` 写 tracked `memory`、回收卡或治理文档；草稿只准写进：
  - `D:\Unity\Unity_learning\Sunset\.codex\drafts\<OwnerThread>\`
- `NPC` 本轮必须真的处理 carrier 噪音和交付面，不准只回“内容早就存在”。
- `农田交互修复V2` 本轮必须真的推进 `1.0.2` 交付面，不准再次只做“检查确认上轮已有 checkpoint”。
- `农田交互修复V2` 若要触碰 `Assets/YYY_Scripts/Controller/Input/GameInputManager.cs`，必须先查锁再拿锁；拿不到锁就退回排队或退出，不准硬抢。
- 任一线程完成本轮 checkpoint 后，必须立刻 `sync`，随后 `return-main`，不要把 shared root 当成长时间工作台。
