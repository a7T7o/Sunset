# 2026-04-02｜看守长旧线程热修复补丁｜01

这条不是让你继续施工，也不是让你解释规则。

你现在只做一件事：把你当前线程里的“看守长”口径热修到最新 live 规则，并在后续用户再次喊 `看守长` 时直接按新口径执行。

## 先读
1. `D:\Unity\Unity_learning\Sunset\AGENTS.md`
2. `D:\Unity\Unity_learning\Sunset\.kiro\specs\Steering规则区优化\当前运行基线与开发规则\Sunset当前规范快照_2026-03-22.md`
3. `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\看守长模式_线程验收交接规范.md`
4. `C:\Users\aTo\.codex\skills\acceptance-warden-mode\SKILL.md`
5. `C:\Users\aTo\.codex\skills\sunset-acceptance-handoff\SKILL.md`

## 你必须立刻改正的 4 件事
1. 以后用户在你这条业务线程里喊 `看守长`，默认审的不是“别的线程”，也不是“这条线整体”，而是：
   - 你上一轮刚完成
   - 且已经向用户汇报过
   的那一刀
   如果这次不是紧跟在完成汇报后，你也必须自己回看当前工作区 / 线程 memory，选最近一次“已完成且已向用户交代过结果”的切片来交接；最多只允许用一句话报明这次按哪一刀交接，仍不允许把材料索取工作甩回给用户
2. 不要再先说：
   - `我先切到看守长模式`
   - `我先按当前治理口径处理`
   - `我先读某个 skill`
   - `把线程回执 / prompt 文件 / 线程名发来`
   - `在 / 看守长到了 / 看守长到位`
3. 你的第一屏必须直接进入验收正文，风格至少接近：
   - `这轮不继续补刀。下面这份你可以直接照着验。`
   - `我先把判断说在前面：……`
   - 如果当前是旧线程兜底场景，也可以先用一句话报明：`这轮先停给你验。我这次按“上一轮刚完成并已向你汇报过的 XXX 这一刀”来交接。`
4. 后续用户再次喊 `看守长` 时，你必须直接输出完整验收包，而不是治理说明。

## 完整验收包至少要有
1. `看守长总判断`
2. `线程已自验`
3. `本轮仍需用户终验的点`
4. `统一前提`
5. `建议顺序`
6. `验收清单矩阵`
7. `最少必测包`
8. `快捷回执单`
9. `完整版回执单`

## 现在回我时只允许回这 4 行
1. `已读文件：`
2. `我后续默认验收对象：`
3. `我后续首屏开头方式：`
4. `我已准备按完整验收包执行：是 / 否`
