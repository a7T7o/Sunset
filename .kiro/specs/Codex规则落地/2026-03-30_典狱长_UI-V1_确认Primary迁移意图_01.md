# 2026-03-30_典狱长_UI-V1_确认Primary迁移意图_01

请先完整读取：

- `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\2026-03-30_单独立案_Primary.unity删除面_01.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\Codex规则落地\memory.md`
- `D:\Unity\Unity_learning\Sunset\.codex\threads\Sunset\spring-day1\memory_0.md`
- `D:\Unity\Unity_learning\Sunset\.kiro\specs\900_开篇\spring-day1-implementation\memory.md`

你这轮不是继续做 UI，不是继续跑 `preflight -> sync`，也不是让你顺手去修 scene。

你这轮唯一主刀只有一个：

- **基于你自己的聊天记录、thread memory、工作区 memory 和当前仓库现场，回答 `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 到底是不是你们当初想保留的最终 canonical path。**

## 一、当前已接受基线

1. mixed-root 清扫主线已经收口；这轮不是继续大扫除。
2. `UI-V1` 已经把 `Story/UI` 整根真实归仓，提交：
   - `2c6276eb`
3. 当前仓库里与 `Primary` 相关的核心现场是：
   - `Assets/000_Scenes/Primary.unity(.meta)` 在 working tree 里是删除面
   - `Assets/222_Prefabs/UI/Spring-day1/Primary.unity(.meta)` 仍存在
   - 两份 scene 的 `.meta` GUID 相同
   - `ProjectSettings/EditorBuildSettings.asset` 和 `Assets/Editor/NPCAutoRoamControllerEditor.cs` 仍指向旧路径 `Assets/000_Scenes/Primary.unity`
4. 历史证据已经足够说明：
   - 这份新路径 scene 和 `Spring/UI` 线关系很强
   - 但“你带过它”不自动等于“它就是最终 canonical path”

## 二、本轮你必须回答的核心问题

### 1. 新路径 scene 的真实语义到底是什么

你必须在下面 3 个选项里明确选 1 个，不能打模糊球：

- `A｜最终迁移目标`
  - `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 就是你们最终想保留的 canonical `Primary`
- `B｜迁移 sibling / 临时复制面`
  - 它只是迁移过程里带出来的 sibling，不该取代旧 canonical path
- `C｜证据仍不足`
  - 你自己的记忆和现场都不能支持 A / B 任一边，需要单独 single-writer 再判

### 2. 如果你选 A

你必须继续回答清楚：

1. 当时迁到 `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 的真实意图是什么
2. 还缺哪些成套动作，这条迁移才算真的成立：
   - `Build Settings`
   - `NPCAutoRoamControllerEditor.cs`
   - 旧路径删除
   - same-GUID duplicate 收口
3. 这套成套迁移是否应该继续由你这条 `UI-V1 / Spring UI` 线来接

### 3. 如果你选 B

你必须继续回答清楚：

1. 你当时为什么会把它作为迁移 sibling 带上
2. 为什么它不能直接被当成新的 canonical `Primary`
3. 你的判断是否支持：
   - 旧路径 `Assets/000_Scenes/Primary.unity` 应先恢复
   - 新路径 scene 后续再按 duplicate / sibling 另案处理

### 4. 如果你选 C

你必须说清：

1. 证据到底缺在哪里
2. 是你自己的 thread 记忆不够，还是现场语义本身就没定过
3. 这案子下一步最合理的 single-writer 应该是谁来接

## 三、这轮明确禁止

- 不准改任何 scene 文件
- 不准改 `ProjectSettings/EditorBuildSettings.asset`
- 不准改 `Assets/Editor/NPCAutoRoamControllerEditor.cs`
- 不准顺手把这题扩回 `Story/UI`、TMP 字体或别的 UI 施工
- 不准只说“我当时碰过 / 我带过 / 这看起来像我的”，你必须给语义裁定

## 四、完成定义

你这轮只有一个合格结果：

- **把 `Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 的语义裁定成 `A / B / C` 三选一，并说明为什么。**

注意：

- 这轮不是要你上 git
- 这轮不是要你 claim scene writer
- 这轮只要你把“历史上它为什么会在你这边出现”说清，并回答“现在能不能把旧路径删除直接提交掉”

## 五、固定回执格式

回执按这个顺序：

- `A1 保底六点卡`
- `A2 用户补充层`
- `B 技术审计层`

其中 `A1 保底六点卡` 仍必须逐项显式写：

1. `当前主线`
2. `这轮实际做成了什么`
3. `现在还没做成什么`
4. `当前阶段`
5. `下一步只做什么`
6. `需要用户现在做什么`

## 六、A2 用户补充层必须额外显式回答

你必须额外补 5 条：

1. `我的最终裁定`
   - 只能写 `A / B / C`
2. `我选这个裁定的直接证据`
   - 必须来自你自己的聊天记录、thread memory、workspace memory 或当前仓库现场
3. `旧路径现在能不能直接删掉提交`
   - 只能写 `能 / 不能`
4. `如果后续真要做这案子，我是否认为应由 UI-V1 / Spring UI 继续接`
   - 只能写 `是 / 否 / 只接其中一段`
5. `这轮是否触碰任何仓库文件`
   - 预期应为 `否`

## 七、你这轮最容易犯的错

- 把“我带过这个文件”直接说成“它当然就是最终 canonical path”
- 把“它很像 Spring/UI 迁出来的”直接偷换成“旧路径现在就能删”
- 回去重新讲 `Story/UI` 已归仓，却不正面回答 `Primary` 新路径到底是迁移目标还是 sibling
- 顺手去碰 scene、build settings、编辑器代码

一句话收口：

- **这轮只回答一件事：`Assets/222_Prefabs/UI/Spring-day1/Primary.unity` 对你来说，到底是最终迁移目标，还是只是一份迁移 sibling。**
